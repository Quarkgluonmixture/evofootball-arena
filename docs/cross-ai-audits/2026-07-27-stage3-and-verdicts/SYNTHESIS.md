# SYNTHESIS — commander × gpt-5.6-sol (xhigh, zero-preset), 2026-07-27

## Agreed independently (high confidence)
- Stage III P1's effect-size/power risk is unconfronted (mine a1 = codex 11);
  paired same-seed forks + positive-control displacement + ETA/adherence
  mediators are required before freezing.
- The gene→weight mapping needs an ablation family or the eye is
  unattributable (mine a2 = codex 12).
- C4's second-body branch was closed by doctrine, not measurement
  (mine c1 = codex 14) — the one cheap compliant fork never run.
- C5's parking of a UNIVERSAL forced hold is sound; the verdict's breadth
  is the question (mine b2 = codex 4/13).

## Unique to Codex — VERIFIED against code (100% fact-checked)
1. **Fallback ladder label-only** (`c5-t1-waiting-census.ts:323-330`):
   `ladder` computed, `rates` always from raw sparse cell ⇒ the committed
   table cannot price T2 and #29.3's unpark condition (cell CI reaching 0)
   is not computable. CONFIRMED.
2. **Concession clock skew**: shot frozen at elapsed=240, conceded read at
   240+k ⇒ hold arms get k extra ticks to concede; release-twin includes
   lost-possession rows. CONFIRMED. "Expensive at both ends" and part of
   the twin's force are withdrawn pending rerun; the primary shot estimate
   stands.
3. **Goal estimand wrong** (`c4-t1-flight.ts:300-323`): goal = first shot
   scored; miss-then-goal counts as no goal; window closes at next cross.
   CONFIRMED ⇒ "fewer goals, resolved" is unsupported as stated.
4. **ShieldHold is omniscient** (`actionExecutor.ts:433+` scans true
   `opp.players`). CONFIRMED ⇒ T1 censused a non-compliant capability.
5. "Resolved absence" vs PROBE-CONTRACTS' own sixth threshold type: an MDE
   is a power property, not an equivalence band — C4 T2's D1 is
   INCONCLUSIVE-on-sign with effects >+1.46pp excluded. Semantics correct.
6. P1 identity control impossible as written (no stored incumbent target);
   unilateral census → universal deployment identification gap; consumer
   leak (zonal/shapeReady/support). #35.3's READ ruling partially answers
   the leak; the no-override identity arm + adoption/saturation arms fold
   into P1/P2 constraints.

## Unique to me
- Face-asymmetric horizons (concede value accrues slower than score value).
- Anticipatory density (perceived teammate MOTION as a feature) as the
  named pile-up lever.
- The 14.45m distance-law / trigger-distance seat framing (banked #31.4).

## Genuine forks → the user
- F-C5: repair rerun now (ladder+clock+percept shield, eligible-choice
  moments) vs fold repairs into the enrichment re-census. Parking stands
  either way; the claim narrows either way.
- F-C4: closure stands (doctrine) vs run the two cheap oracles (per-cross
  flight fork; second-body station fork) before closure is final.

Verdicts' fate: no banked verdict is relabeled; claims NARROW per the
verified defects. Stage III P1 constraint set expands (commander work).
