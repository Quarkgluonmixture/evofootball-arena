# PW — 传球力量轴 (THE PASS-WEIGHT CONTRACT: direction × weight on the pass — the lane's physics lever)

> Opened by the USER at the #289 fork gate (2026-08-15, verbatim: 「1」— the pass-weight door,
> the commander's recommendation ratified); bound by ruling #290. This is the PASS HALF of the
> user's TOUCH-WEIGHT DOCTRINE (#284.2, M-CB.5 named the carry half — still on the menu): today
> the ball has ONE pace per range, exactly the gap the user found in the carry (「不能选择大小」).

## §0 The diagnosis chain (why this door, three-times-proven)

The corridor owns **82.08 %** of the outfield behind-ball option loss (BU-T1 GK-split, #289;
BU-C0 floor ≥ ~67 %); outfield end-to-end conversion 21 %. No banked seam covers it: DV as
shipped = 57/57 null (#287); a LOUD zone price re-routes AWAY from circulation (#288); corridor
lethality is PRICE-INVARIANT at every rung (#288.3); MT opens the lane only marginally (#289).
THE PHYSICS: `launchSpeed = clamp(d·0.6 + 8.2, 9, 22)` (mechanics.ts:392) — one pace per range.
The weight INPUT already exists in `performPass` and every live caller omits it (Match.ts:2309);
`perceivedPassChoice.ts:289`: "The live game has no power chooser"; `evaluatePassAffordance`
already accepts a `powerMultiplier` (all three verifier-corroborated, BU-C0/#286). The corridor
is a RACE between a defender's ETA and the ball's travel time — a firmer ball shortens every
window without moving a single body.

## §1 The claims

* **H-PW.1 (scored, mechanism grain, at the assembled composition)**: with the weight axis
  armed and priced through the EXISTING machinery, (a) weight is genuinely CHOSEN (non-default
  weights used at claim grain — usage > 0, distribution reported, not one degenerate corner)
  AND (b) corridor survival of backward/lateral options rises resolvedly at reception grain.
  能选多大力,回传的球路就活了 — capability + honest prices; no usage promise.
* **H-PW.2 (REPORTED, never gated)**: circulation usage (the face the whole BU arc could not
  move), the R-乙 chain faces (Q01/Q05/Q06/Q07/Q14), and the BU census faces (GK-split) — the
  standing institutions are this arc's before/after rulers. Direction pre-registered: the
  affordance oracle already prices interception risk, so a surviving backward ball should be
  CHOSEN more without any nudge — if usage stays flat while survival rises, that verdict routes
  to the menu (chooser-side, not physics-side).

## §2 Mechanism (M-PW)

* **M-PW.1 — PHYSICS HONESTY FIRST**: the weight parameter's honest propagation (launch speed →
  travel time → friction → arrival speed) is AUDITED before anything is built (PW-C0). The
  expressible range is DERIVED from the shipped clamp's own bounds and the multiplier's actual
  algebra (the BU-T0b λ_LIN idiom: find the linear region, cap at its edge, never invent a
  transform). Weight rungs are DERIVED from engine constants — never taste.
* **M-PW.2 — THE CHOOSER IS THE ONE TABLE**: weight enters as candidate enumeration through the
  EXISTING affordance oracle (`powerMultiplier`) priced by the EXISTING score — no new pricing
  table, no move library. 扣/搓/大力平传-shaped behaviour must emerge as CHOSEN REGIONS of
  direction × weight (the #284.2 anti-move-library form, pass side).
* **M-PW.3 — RECEIVING-COST HONESTY (the dominance hazard, named)**: if the engine today charges
  NOTHING for receiving a faster ball, max weight strictly dominates and the axis degenerates.
  PW-C0 audits what the engine already charges (control/first-touch/completion as f(arrival
  speed)). If nothing: a DERIVED receiving cost (from the existing touch/control constants)
  becomes a NAMED PREREQUISITE SLICE before any chooser ships — derived, never taste (#200).
* **M-PW.4 — SCOPE & DEBTS**: no carry-weight (M-CB.5 stays on the menu); no perception, no
  coached shape (their own doors). Flags off ⇒ byte-identity, per seam. M-BU.2's composition
  law carries over: any armed world alongside the CB/L3 stack proves the lifecycle at ITS
  composition first (#283.2(iv) stated per arm design). ⭐ If PW src work touches the CB seat's
  arming block, the S∧¬T guard debt (named at #287.3, restated #289.1) falls DUE in the same slice;
  if not, it stays the CB seam's named debt.

## §3 Instruments & the arc

* **PW-C0 — THE WEIGHT-PHYSICS CENSUS (instrument-only, first)**: (a) the PHYSICS AUDIT —
  machine-read the pass pipeline; does `powerMultiplier` propagate honestly; where do clamps
  bite; derive the expressible weight region and the rung ladder from the engine's own
  constants. (b) THE CORRIDOR-RESPONSE CENSUS — on virgin seeds in the v7 world, re-evaluate
  the BU corridor rung through the oracle's own `powerMultiplier` at each derived rung:
  survival per rung × direction (backward/lateral/forward) × GK-split; THE KEY NUMBER: how much
  outfield backward-option survival a firmer ball buys. (c) THE RECEIVING-COST AUDIT (M-PW.3):
  what the engine charges today, machine-read, with the dominance-hazard verdict. (d) the
  overshoot/out face. The census PICKS the chooser-slice design (rungs; whether the receiving
  cost must be built first).
* **PW-T0.. — the dormant seam slices** (order set by PW-C0): the weight chooser flag-gated,
  byte-identical off; then the exam at the composition (BU faces, commensurable); then the
  entry rung. **Exit = the play-test (USER GATE)**: 回传能活下来了吗,组织进攻看得出来了吗.

## §4 Non-claims

No build-up cure promised (the arc opens the lane's physics and prices it honestly; the chooser
and the play-test decide the football). No shipping without the play-test. An all-rockets world
is a FAILURE mode, not a success (M-PW.3 guards it).

## §6 VISION audit (the #91 form)

* vs §-1 (tactics emerge): direction × weight priced by the one table — chosen regions, no move
  library; the user's own doctrine, pass side. PASS.
* vs 底座给能力: weight is BODY capability (innate-legal, #248.1 form) — the substrate provides
  it; choices and their consequences stay per-team/per-situation. PASS.
* vs #200 (no taste constants): rungs derived from the shipped clamp's algebra; receiving cost,
  if needed, derived from existing touch constants; prices via the existing oracle. PASS.
* vs the assembly law: the BU arc proved row-by-row that no banked seam covers the lane (#289.4)
  — M-BU.1's own admissibility condition for a NEW cut is satisfied, and the user opened the
  door. PASS.

## §7 REALITY audit (the #201 rule)

* Real football chooses pass weight constantly: firm balls beat pressing lines (the corridor
  race is real physics), soft balls are receivable, overhit balls die — the trade-off this
  contract prices is the real one. PASS.
* Honest limits, stated: real weight choice also serves aerial/lofted passes (a different
  capability family, out of scope), and real receivers CUSHION heavy balls (receiving craft —
  if the engine has no cost today, M-PW.3 builds the derived one; the craft/skill half belongs
  to later capability slices). The census decides which half binds first. PASS.
