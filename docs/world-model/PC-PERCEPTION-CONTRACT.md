# PC — 感知合同 slice 1 (THE PERCEPTION CONTRACT: reaction latency — 处理时间是信息差的货币)

> Opened by the USER at the #295 item 5 fork (2026-08-16, verbatim: 「2」 — skip the PW entry,
> bank the seam dormant, open the perception contract); bound by ruling #296. This implements
> **INFO-DOCTRINE.md §0–§1 primitive 2 ONLY** (两档处理延迟 + 神经底). Primitives 3 (private
> snapshots/scanning) and 4 (injection/leakage) are LATER slices of this same doctrine — not
> this contract. The doctrine file is the design authority; this contract is its mechanism.

## §0 The diagnosis chain (why this door, and why now)

The lane is the six-times-confirmed binding rung of 没有 build up (#286–#295: bodies exist ·
DV null · loud price anti-circulation · price-invariance · MT marginal · PW real-but-thin).
Every confirmation shares one cause: **today's defenders are omniscient and instantaneous** —
the INFO-DOCTRINE §3 baseline measured every defender's steering re-targeting the truth ball
within 1 tick. A corridor is "cut" because the cutter reacts at machine speed. 现实里走廊会开,
是因为封路的人要先看见、再决定 — the processing time IS the currency (§-1 message 3, verbatim
in the doctrine). This slice buys that currency for the world.

## §1 The claims

* **H-PC.1 (scored, mechanism grain, at the matured armed world)**: (a) reaction latency
  DIFFERENTIATES BY EARNED RECOGNITION — bodies pay the SHORT tier in situation classes their
  own book covers and the LONG tier where it does not (measured at event grain; the novice
  pays long BY CONSTRUCTION, never by a written rule); AND (b) the information gap at the
  touch-past turns POSITIVE — the carrier-anchored separation (#266.2(i)'s instrument) grows
  resolvedly when the beaten side pays real processing time. 过人终于买到时间.
* **H-PC.2 (REPORTED, never gated)**: the sim-grain disease faces — completion · interception
  terminal share · the BU census supply faces · R-乙 Q01/Q05/Q06/Q14 · pressing efficacy (the
  pressed-loss ratio) — the doctrine PREDICTS pressing becomes a time-budget attack (primitive
  6, derivable never built) and lanes open at SIM grain even while the full-truth oracle's
  priced corridor stands still. Directions pre-registered; the standing rulers are the
  before/after.

## §2 Mechanism (M-PC)

* **M-PC.1 — THE LATENCY LAW**: on a SURPRISE event (an event the body did not initiate, in
  his perceptual relevance — the census enumerates the classes from the engine's own event
  grain), the affected body's REACTIVE channels hold their STALE plan for
  latency = the recognized tier: **simple ≈ 0.2 sim-s** (his book covers the class) or
  **choice ≈ 0.4–0.5 sim-s** (it does not) — the psychology literature's own two constants,
  ratified into the doctrine at #272 (§0). ⭐ CLOCK RULING: reaction constants live on the SIM
  clock because body physics does (players sprint ~real m/s in sim seconds; the 22.5× mapping
  is scoreboard-only). Exact tick values derived at build time from the traced constants and
  TICK_DT — stated as APPLIED ticks, the #280 form.
* **M-PC.2 — STALE PLAN, NOT BLINDNESS**: during latency the body CONTINUES its current plan
  (action + steering target frozen as they were) — wrongness during processing is FREE and
  EMERGENT (doctrine §0). No new decision layer, no snapshot infrastructure (that is slice 2):
  slice 1 is TIMERS + TARGET-HOLDS on the reactive channels the census maps (per-tick
  truth-tracking steering is a reactive channel and MUST hold, or the gap never exists).
* **M-PC.3 — RECOGNITION IS AN EARNED BOOK** (the self-cluster form, fifth instantiation):
  gene-free · born ABSENT · own events only · match-local views · season reset · no franchise
  writes · nothing in info.genome (#270). The book counts LIVED EXPOSURES per situation class
  (the census derives the classes from the engine's own vocabulary — R-甲's grain, no invented
  taxonomy); coverage ⇒ the simple tier. Role-differentiated reaction EMERGES from
  role-differentiated exposure (doctrine §0) — no positional constants, ever.
* **M-PC.4 — SELF-INITIATED = ZERO LATENCY**: the initiator pays nothing for his own act
  (碰到的瞬间就开始走 — the user's own simplification, doctrine §0; knock-and-go already
  implements the carry case and stays untouched). The census inventories the initiator paths.
* **M-PC.5 — SCOPE & DEBTS**: no scanning/attention (slice 2), no feints/leakage (slice 3),
  no coach channel. Flags off ⇒ byte-identity, per seam. The M-BU.2-form composition law
  binds: the exam proves the doors/lifecycle at the FULL armed composition; the S∧¬T guard
  falls due if any PC src touches the CB seat's arming block (M-PW.4's form). GK scope: the
  keeper's reactions are IN (he is a body), his existing special channels stated honestly.

## §3 Instruments & the arc

* **PC-C0 — THE REACTION-BASELINE CENSUS (instrument-only, first)**: (a) TODAY'S REACTION
  STRUCTURE at event grain — for each candidate surprise class (touch-past release · pass
  release · interception/turnover · deflection/50-50), the tick-lag distribution until each
  affected body's steering/decision reflects the new truth (the §3 baseline systematized);
  (b) THE INSERTION-SEAM MAP, machine-read — every reactive channel that re-reads fresh truth
  (decision cadence/decisionTimer structure INCLUDED: the world's existing cadence is itself
  a latency the seam composes with — measure it); (c) THE SITUATION-CLASS DERIVATION from the
  engine's own event grain + EXPOSURE RATES per class per role per season (the L3-T1
  slow-knowledge lesson: if books cannot fill in feasible seasons, the design must know NOW);
  (d) the SELF-INITIATED inventory (which paths already carry self-knowledge). The census
  PICKS the slice order and the class list.
* **PC-T0 — the dormant seam** (latency timers + target-holds + the recognition book, flag-
  gated, byte-identical off) → **PC-T1 — the learning exam** (books fill from own exposure;
  tier transitions at event grain; the 16-team form) → **PC-T2 — the armed-world read**
  (H-PC.1 scored; H-PC.2 faces; the doors matrix at the full composition) → **the entry rung**
  → **Exit = the play-test (USER GATE)**: 过人时对面真的慢半拍了吗 · 逼抢读作时间攻击了吗 ·
  世界更像足球了吗.

## §4 Non-claims

No promise that build-up appears (the doctrine says lanes should open at sim grain; the
chooser's oracle stays full-truth this slice — the PRICED corridor unchanged is expected and
honest). No skill/attribute constants. Nothing ships without the play-test.

## §6 VISION audit (the #91 form)

* vs §-1 (tactics emerge): 博弈 becomes visible exactly when reaction time exists — 卡身位/
  positioning as the latency-free answer must EMERGE, never be coded (doctrine §1 ⭐). PASS.
* vs 底座给能力: the two tiers are literature-traced BODY knowledge (#248.1 innate-legal);
  everything differentiating is EARNED (born-absent books, own exposure). PASS.
* vs #200 (no taste constants): two traced constants + derived ticks; tier assignment by
  coverage of own lived events, no written novice rule. PASS.
* vs the doctrine (#272): this is its slice 1 verbatim — the ratified decomposition built as
  specified, nothing beyond it. PASS.

## §7 REALITY audit (the #201 rule)

* Real football's information game IS this: the defender who has seen a thousand knocks reads
  the next one early; the rookie watches the ball roll past. Processing time per situation
  class, faster with lived experience — the sports-science account, not an invention. PASS.
* Honest limits, stated: real perception also has GAZE and attention (slice 2), and real
  recognition transfers across similar situations (our classes are discrete; transfer is a
  later refinement). Real reaction also varies WITHIN a person (fatigue, arousal) — out of
  scope, stated. The play-test judges whether discrete-class latency already reads as 博弈.
  PASS.
