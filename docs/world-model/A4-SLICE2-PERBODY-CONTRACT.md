# A4 — SLICE 2 CONTRACT (per-body obedience: 每个人自己的"该往哪走")

> Drafted by the commander 2026-08-06 under ruling #158 (the user rules 甲
> at the #157.5 fork). Binding once the embedded §8 VISION audit is ruled
> (same-round, the #91 form). Parent: [`A4-ASSIGNMENT-CONTRACT.md`](A4-ASSIGNMENT-CONTRACT.md)
> (slice 1, CLOSED #154) — its §3 invariants and M1′–M5 mechanism are
> INHERITED, not restated.

## §0 Lineage — why per-body, and why now

* #154.3 named slice 2 (per-body obedience offsets, the roleMix
  homogenization motive) as the first of three next steps.
* #157: the user's FIRST play verdict on the armed A4 world —
  verbatim "很多时候球员不知道自己该往哪走,尤其是防守的时候" — and the
  forensic re-read corroborated it (dupRun **+7.8%** on PRIOR−R3p;
  spacingUnder4 +5.3%; the proximity block's contracted verdict
  authority = the user's eyes, exercised NEGATIVE).
* #158: the user rules **甲** — slice 2 opens; the offside axis (乙)
  HANGS unworked; wholesale revert (丙) declined for now.
* The plain-language estimand: slice 1 proved a shared whisper-volume
  agreement DEFENDS (deep −0.7395, box −0.380/set, #154); slice 2 asks
  whether each body having its OWN relationship to that agreement —
  the user's 爱压上的后卫和爱回撤的前锋 (VISION §1 verbatim, 2026-08-02)
  — cures the duplication that reads as 不知道该往哪走, WITHOUT selling
  the defence slice 1 bought.

## §1 The claim under exam (H-A4.2)

Giving each body its own obedience relationship to the kickoff
agreement (a bounded offset around the team whisper), at MATCHED mean
dose, (a) reduces same-job duplication — dupRun, the measured face of
撞车 (VISION §1(b): the failure table of 位置) — versus the uniform
whisper; (b) holds defensive value non-inferior (deep/box entries vs
the slice-1 PRIOR arm, margin frozen at sizing); (c) holds the
inherited football hard gates. The heterogeneity-at-matched-mean
clause is the DISCRIMINATOR for H-157c (homogenization causes the
look): if heterogeneity alone does not move dupRun, H-157c is wrong
and is recorded wrong.

## §2 Mechanism (M-S2, all dormant; the M1′–M5 skeleton inherited)

* **M-S2.1 — the gene family.** `homePriorObedienceOffset` per squad
  slot (policyGenome idiom): born **ABSENT** (`undefined` ⇒ offset 0 ⇒
  bit-identical to the slice-1 uniform whisper — born-equivalence is a
  GATE, not a claim); bounded; effective per-body obedience =
  `clamp01(homePriorObedience + offset_i)`; mutate/crossover mirror the
  existing `homePriorObedience` idiom; evolve flag DEFAULT OFF, flips
  only with the existing master switches.
* **M-S2.2 — execution.** Per-body strength =
  `homePriorStrength(effective_i)` consumed through the SAME
  `eye.v4.homePrior` seam. NO new consumer mechanism; the soft-bias
  form is unchanged; clamps remain the certified disease (P1b).
* **M-S2.3 — instrument seam.** The banked P1c single-body
  `homeRegionGrant` generalizes to a per-body dose VECTOR
  (instrument-side ONLY: vectors are measurement forks, never shipped
  content).
* **M-S2.4 — plumbing.** NO new percepts. The offsets are kickoff
  agreement CONTENT (common knowledge like the regions, per M5 — 大家
  开场就知道谁爱压上): the eye's crowding/duplication terms may price
  "he holds home, I can push" with no exclusion law. Zero live state;
  opponents still cannot read it.

## §3 Invariants

Slice-1 §3 I-series inherited verbatim. Additionally binding:
* Road B throughout: nothing ships; production fingerprint
  `57b0bdab…c673` unchanged; flags-off byte-identity re-proven at
  every stage; offsets-absent bit-identity to the slice-1 arms proven
  at S2-P2 (RNG-stream identity, the slice-1 P2 form).
* ⭐ **BIRTH NEUTRALITY (the §8 audit's one live-risk clause).** The
  shipped birth distribution must NOT pre-differentiate by role — no
  "defenders born obedient". Offsets are born absent/zero everywhere;
  differentiation is EARNED by selection or granted only inside
  instruments. A role-derived birth default = 替球队定X (VISION §1)
  and is REJECTED at review regardless of what it measures.
* Frozen-before-sight; one writer per tree; explicit-path staging;
  seeds only from the remaining pool (§9).

## §4 The instrument arc (one executor step per round, §0.0 form)

* **S2-P1 — dose-vector fork-and-grant.** Pre-registered obedience
  vector grid over the P1c machinery. Axes (frozen here; magnitudes at
  the stage doc): `uniform` (= slice-1 PRIOR control) · `spread`
  (heterogeneity at MATCHED mean — the H-157c discriminator) ·
  `back-loaded` · `front-loaded` · `single-anchor` (one high-obedience
  body, the P1c echo). Role-derived vectors are legal HERE only
  (instrument forks). Sizing smoke → frozen N → gates frozen ex ante
  in the stage doc before any battery sight.
* **S2-P2 — build the gene family dormant.** M-S2.1/M-S2.2 landed
  behind the flags; born-equivalence + RNG-stream identity proofs;
  full suite + tsc; byte-identity flag-off.
* **S2-P3 — the frontier battery.** DIFF (the best pre-registered
  vector, gene-expressed) vs PRIOR vs R3p; primary = dupRun falls on
  DIFF−PRIOR; non-inferiority = deep/box on DIFF−PRIOR within the
  frozen margin; football hard gates inherited.
* **S2-P4 — the play-test exit.** The #156 entry idiom, armed with
  DIFF; the user's eyes adjudicate the look this slice was built to
  cure (紧凑像球还是像堆 + 知不知道该往哪走). The slice CANNOT close
  on counters alone (§2 VISION: watchability has no instrument).

## §5 The #157 instrument debt (BINDING on every slice-2 battery)

* **Offsides**: its own descriptive + FLAG on every arm contrast
  (never gated in slice 1 — the user's eye caught a ≈2×-production
  level; threshold frozen at stage sizing).
* **A dedicated foul counter** is built at S2-P1 (gap #1 — restartTicks
  is an aggregate proxy and cannot separate 犯规 from other restarts).
* **The E4 combination counters** (third-man releases, overlaps,
  forward-pass share, chain length) carried as REPORTED dimensions on
  every battery (gap #2 — the user's 配合确实更多了 currently has no
  counter to corroborate it).
* The proximity block stays DESCRIPTIVE, verdict authority = the
  user's play-test (#152), exercised at S2-P4.

## §6 FAIL modes, pre-named

* **F-S2a**: `spread` does not move dupRun — H-157c recorded WRONG;
  return to the commander; the named alternative = the
  punish-compactness substrate program (#154.3).
* **F-S2b**: dupRun falls but deep/box inferior beyond margin — the
  look-vs-value fork goes to the USER (watchability authority).
* **F-S2c**: the whole vector grid prices ≤ 0 — the uniform whisper is
  already optimal; slice 2 banks DORMANT (the P1b precedent, honest).
* **F-S2d**: the offside/foul flag fires on any candidate — the axis
  returns to the user (乙 hangs by #158; no offside work is smuggled
  in as a side effect).

## §7 Non-claims (slice 2 does NOT)

No offside-rule change (乙). No in-match adaptation / coach loop (the
doctrine ladder's later rungs). No opponent inference. No
selection/PAYS second half (the standard second step, named later).
No HEIGHT-variation claims (slice-1 §7). Nothing ships.

## §8 VISION audit record (clause-by-clause at drafting, the #91 form)

* **vs §1(b) 位置 = per-head 粗共识,功能防撞车**: per-body obedience is
  NATIVE — the user's 爱压上的后卫和爱回撤的前锋 verbatim IS per-body
  differentiation; dupRun is VISION's own named 撞车表. PASS.
* **vs 共同 prior ≠ 逐 tick commander**: offsets are kickoff content,
  static in-match; no commander appears. PASS.
* **vs capability-not-mandate**: effective-zero obedience stays legal;
  an all-anarchist team is a legal gamble. PASS.
* **vs I-A3 genes are evolution's seat**: shipped form = genes born
  absent; hand vectors live only in instruments. PASS **with the one
  live risk anchored as §3 BIRTH NEUTRALITY** (a role-derived birth
  default would be the violation; rejected ex ante).
* **vs §2 watchability has no instrument**: the slice exits at the
  user's eyes (S2-P4), counters carry only the countable faces. PASS.
* Amendments produced: §3 BIRTH NEUTRALITY (binding). No other clause
  moved.

## §9 Execution map (PROGRAMME idiom)

S2-P1 → S2-P2 → S2-P3 → S2-P4; one executor step per user "go"
(Workflow Draft→Verify, §0.0); stage docs pre-register and freeze
before sight; results dispose by numbered ruling. Seeds: the remaining
pool 12.237M–12.3M + stats 1014xx+ (disjointness ledger per stage).
