# PROGRAMME — Commander rulings (verbatim; LIVE FILE, #382 onward)

> This file holds commander rulings **#382 onward, verbatim**, APPENDED in numeric
> order — nothing is ever reworded. Earlier eras, byte-verbatim: #2–#284 in
> [`PROGRAMME-RULINGS-ARCHIVE-001-284.md`](PROGRAMME-RULINGS-ARCHIVE-001-284.md);
> #285–#302 (BU→PW→PC) in
> [`PROGRAMME-RULINGS-ARCHIVE-285-302.md`](PROGRAMME-RULINGS-ARCHIVE-285-302.md);
> #303–#330 (perception gate→BK/IN/DF nights) in
> [`PROGRAMME-RULINGS-ARCHIVE-303-330.md`](PROGRAMME-RULINGS-ARCHIVE-303-330.md);
> #331–#345 (BK-C1→GC-T1, nights 6–7 opening) in
> [`PROGRAMME-RULINGS-ARCHIVE-331-345.md`](PROGRAMME-RULINGS-ARCHIVE-331-345.md)
> #346–#365 (GC-T1B→the DX/RA arc→the twelfth world) in
> [`PROGRAMME-RULINGS-ARCHIVE-346-365.md`](PROGRAMME-RULINGS-ARCHIVE-346-365.md)
> #366–#372 (the passing audit ratified→RC-C0/PT-C0/RC-T0/RC-T1a+fix) in
> [`PROGRAMME-RULINGS-ARCHIVE-366-372.md`](PROGRAMME-RULINGS-ARCHIVE-366-372.md)
> #373–#381 (RC-C0b→the BF arc→RC-T0b+fix→RC-T1b FAIL→BN-C0) in
> [`PROGRAMME-RULINGS-ARCHIVE-373-381.md`](PROGRAMME-RULINGS-ARCHIVE-373-381.md)
> (the unnumbered 2026-07-24 ruling remains in `PROGRAMME.md`'s context block).
> **Resume = `tail -n 120` of THIS file.** Find any ruling by number:
> `grep -n "RULING #N " docs/world-model/PROGRAMME-RULINGS*.md`. Rotation rule:
> ~1,500 lines ⇒ rotate the closed era in the same round as a ruling (#303 item 2).
> **COMMANDER RULING #382 (2026-09-05 — ⭐⭐⭐ BN-C0 BANKED: the user's
> 「传到人身上弹回」 is, in the form he plays, a CONTROL-QUALITY event —
> the intended target meets the ball, the engine records a control
> attempt, and the attempt FAILS; the dosed world agrees and the read is
> precedence-invariant there; the failing law is LOCATED (a chance roll
> capped at 0.4, already carrying its own trace ledger); the verifier's
> FAIL was prose alone, corrected in place; ⭐⭐ BQ-C0 THE FIRST-TOUCH
> CENSUS DISPATCHED; rulings #373–#381 ROTATED to ARCHIVE):**
>
> 1. **BANKED** (freeze `dd14ed2`, results `b11b036`, executing #381 item
>    6). VERIFIER OF RECORD: **FAIL — zero HIGH on numbers, one HIGH on
>    prose** (item 2); every headline re-derived from `perSeedCells` alone
>    on both arms; the trace flag proven byte-inert by reading every
>    branch on it (it only appends to `contestEpisodes`) and by the stored
>    ON/OFF whole-match signatures; K = **3** ticks READ off
>    `CONTACT_CONTROL_DELAY_TICKS` at its `readyTick` site (a constant
>    offset — it does not read `relativeSpeed`); the kind/origin/outcome
>    read from the ledger, the one declared heuristic (the lagged maxSpeed
>    cross-check) agreeing with the ledger on 0.984894; 679 prose
>    literals swept, all traced; X-SRC-ZERO; fingerprint UNCHANGED;
>    N_FROZEN **998**, BOOKED = WALKED = **1,998**; 14/14 gates. THE
>    NUMBERS OF RECORD (E = empty book · D = the form the user plays): a
>    ground pass meets one of OUR bodies first on **0.679460** (E) /
>    0.678021 (D) of measured passes — the intended target on 0.573585 /
>    0.581780, a NON-target teammate on 0.105875 / 0.096240, an opponent
>    on 0.319312 / 0.321270; THE BOUNCE RATE (no possession for our side
>    at +K): on the intended target **0.227278** [0.223011, 0.231120] (E,
>    9,660 of 42,503) / **0.219146** (D); on a non-target teammate
>    **0.805683** (E) / 0.818851 (D) — a teammate the ball was not meant
>    for loses it four times in five; the loss is almost always to a
>    LOOSE ball (9,397 of 9,660 intended-target bounces on E; 263 out;
>    never straight to an opponent on that class). ⭐⭐ THE COMPOSITION of
>    resolved bounces (the frozen precedence C1 > C2 > C3 > C4 > C5): **C1
>    — intended target, `controlAttempt` recorded, attempt FAILED at its
>    readyTick — 0.589439 (E) / 0.603483 (D)**; C3 — own non-target body
>    first — 0.395608 / 0.382070; C2 — intended target above his maxSpeed
>    — **0.000000** on both, STRUCTURALLY (item 4(iv)); C4 exclusive
>    0.000438 / 0.000350 (its OVERLAP 0.397923 (E) vs **0.181802** (D) —
>    Δ −0.216120 at 22.110 hw); C5 0.014515 / 0.014097. THE READS PRINTED,
>    VERBATIM: *"THE THIRD SENTENCE IS A CONTROL-QUALITY QUESTION — the BK
>    quality law is named."* · *"THE DOSED WORLD AGREES ON THE MAJORITY
>    CLASS"*. Adopted.
> 2. **THE PROSE DEFECTS — CORRECTED IN PLACE** (BN-C0 doc §CORR 1–6):
>    HIGH — "ALWAYS to a loose ball, NEVER straight to an opponent" was an
>    unstored universal falsified by the stored ladder (263 intended-target
>    bounces went OUT; one non-target bounce went to an opponent); the
>    stored truth (opponent 0.000000 on the intended target, both arms)
>    is now the sentence. MEDIUM — "an intended target can never produce a
>    deflection" overreached (the GK branch wins: an intended-target keeper
>    carries 23); corrected. MEDIUM — the C4-first counterfactual: a
>    C4-first precedence prints NO majority on E (C1 0.486267) and a
>    SURVIVING C1 majority on D (**0.594396** = (10,360 − 156) / 17,167);
>    HONEST LIMIT 3 now shows the arithmetic. LOW ×2 (duplicated words;
>    no per-sector n column — the counts are in the artifact). The results
>    commit message repeats the HIGH sentence and is superseded by §CORR 1.
> 3. **RATIFIED** (the executor's declared deviations): the ledger's kind
>    vocabulary has NO `bodyStrike` — a `bodyStrike` CLAIM is recorded as
>    `body` when applied; #381 item 6(iii)'s list is corrected by the
>    census (the vocabulary is parsed off `ContestContactKind` at run
>    time); the pass corridor is the census's own construction from the
>    corridor family's normalizer `DV_CORRIDOR_SCALE` = 4 with
>    `DV_CLEAR_RADIUS` = 1.5 (no shipped boolean width exists), the
>    CONTROL_RADIUS bin beside — declared; N_FROZEN 998, tail 12,540,998
>    declared; the first-touch heading stage is the last pre-contact
>    sample (declared, and distinct from the sector's end-of-tick
>    convention); a C5 majority maps to MIXED; the sizing targets are
>    E-arm face half-widths (the reads rest on shares, not Δs).
> 4. **THE COMMANDER'S READ** (VISION + REALITY; the user's intuition
>    tested at its third station): (i) ⭐⭐ IN THE WORLD THE USER PLAYS, THE
>    THIRD SENTENCE IS A CONTROL-QUALITY EVENT, AND THAT READ SURVIVES
>    THE PRECEDENCE: the intended receiver gets to the ball, the engine
>    records a deliberate control attempt, and about one in four-and-a-half
>    fails (bounce 0.219146; `controlAttemptPossessionAtReadyTick`
>    0.772759 on E). The READINESS story is closed twice over (RC-T1b;
>    here the flight turns about half of front-on receivers side-on by
>    first touch — 0.469720 held vs 0.459649 bounced — and it does NOT
>    discriminate the outcome; side is the SAFEST sector, 0.286644 against
>    front 0.310931 and back 0.492804). (ii) THE REACTION LEVER IS SPENT IN
>    THE USER'S WORLD — for the third time: C4's overlap with the bounces
>    halves from 0.397923 (E) to 0.181802 (D); on the empty book a
>    C4-first ordering would leave NO majority, on the dosed form C1 holds
>    at 0.594396. Consistent with RC-T1a and RC-T1b. (iii) THE SECOND CLASS
>    IS LANE OCCUPANCY: 0.38–0.40 of bounces are a teammate the ball was
>    not meant for, and he loses it 0.81 of the time — 「有人挤人」 given a
>    number; steps ②/③ of the ratified order inherit it. (iv) ⭐ PASS
>    WEIGHT HAS NO EXISTENCE PRICE FOR THE INTENDED TARGET IN THIS ENGINE:
>    his control branch is 24 m/s = `DEFLECT_MAX_SPEED`, so no ground pass
>    is ever "too hard to touch" for the man it was meant for — REALITY
>    disagrees (a 24 m/s pass is a shot). Pass weight reaches him ONLY
>    through the quality roll's speed term (item 5). Of record for step ④.
>    (v) 「传到对面身上」: under the wide corridor 0.565846 (E) / 0.580164
>    (D) of opponent first contacts were ALREADY inside the lane at
>    release — the passer's own geometry (step ⑤'s question) — 0.368288 /
>    0.376977 were struck through (the ball reached him elsewhere), and
>    only 0.065867 / 0.042859 arrived during the flight; the tight corridor
>    moves the split hard (0.281296 in the lane at release) — the boolean
>    is the census's construction and both are of record. (vi) VISION:
>    nothing hand-coded; a user sentence has become an engine class with a
>    count. REALITY: a receiver does lose the ball to a bad first touch —
>    the question is whether ONE IN FOUR-AND-A-HALF is a human rate, and
>    whether a CHANCE is the honest law. PASS as a census. (vii) THE
>    RATIFIED ORDER: step ① CLOSED; the third sentence's repair is the
>    QUALITY law (a BK-family law, not a step of the passing order); ②/③
>    hold C3's number; ④ holds item (iv); ⑤ holds item (v). The quality
>    law goes first because it is the majority class in the user's world
>    and precedence-invariant there.
> 5. ⭐⭐ **THE FAILING LAW, LOCATED** (code facts of record, symbol-stable):
>    `Match.resolvePendingControlAttempt()` at `readyTick` calls
>    `mech.attemptFirstTouch(this, p, {relativeSpeed, incomingDir})`; a
>    keeper or a ball at `speed ≤ 6` is clean without a roll; otherwise
>    `misalign = (1 + incomingDir·heading) / 2` (face = 0, behind = 1),
>    `pressure = pressureAt(pos, opponents)`, and **`pFail =
>    touchFailChance(speed, pressure, misalign, attrs.dribbling,
>    attrs.positioning, edsTouchCost)`** = `clamp(raw · (1.3 − 0.85 ·
>    technique), 0, 0.4)` with `raw = 0.01 + clamp01((speed − 6) / span) ·
>    weight + (pressure · 0.1 + misalign · 0.05) · aware`, `aware = 1 −
>    (positioning − 0.5) · 0.6`, the curve `{span 8, weight 0.07}` (base)
>    or `{16, 0.24}` (E1b's heavy, behind `edsTouchCost`, default = the
>    `EDS_BUNDLE` env — the census pins world 12's value); a re-collected
>    own touch pays ×0.45; **`clean = !rng.chance(pFail)`**; a failure
>    increments `miscontrols`, knocks the ball loose at 3.5–6.5 m/s rotated
>    ±0.8 rad and sets `kickCooldown` 0.5. ⭐ The E1a INSTRUMENT already
>    exists: `traceFirstTouch: true` appends every adjudication's exact
>    terms (`intendedTarget · relativeSpeed · pressure · misalign ·
>    technique · positioning · pFail · clean`) to the public
>    `firstTouchTrace[]` — a LEDGER, observation-only (E1a's own exact
>    validity). E1a/E1b measured this law on STAGED passes in a held world
>    (pressure 0, misalign 0); no one has read its terms on world 12 live.
>    ⭐ THE FORM QUESTION, NAMED FOR THE USER IN PLAIN FOOTBALL: 「停球失误
>    是掷骰子掷出来的」 — in this engine whether a first touch sticks is a
>    coin weighted by speed, pressure, blind side and technique, capped
>    at 40 %. Whether the weights are human, and whether a coin is the
>    honest law at all (the BK arc removed the EXISTENCE roll and kept
>    QUALITY as skill-priced rolls), is what BQ-C0 measures and BQ-T0
>    decides.
> 6. ⭐⭐ **BQ-C0 DISPATCHED — 「停球」 THE FIRST-TOUCH CENSUS** (a C0 census;
>    the BN-C0 form; X-SRC-ZERO; definitions frozen at the executor's §P):
>    (i) ARMS E / D, world 12's composition, the composer CALLED, BOTH
>    with `traceFirstTouch: true` (observation-only; gTraceInert ON vs OFF
>    signatures on shared scratch seeds) and `traceContests: true` (for the
>    outcome ladder); the world conjunct PINS `edsTouchCost`'s value in
>    world 12 (expected false — the base curve — unless the env arms the
>    bundle; the census states which curve it measured). (ii) POPULATION:
>    every control-attempt RESOLUTION (`resolvePendingControlAttempt`
>    reaching its body) — split into the FREE TRAPS (keeper, or `speed ≤
>    6`: no roll, no trace entry — counted as resolutions minus trace
>    entries, anchored to the two early-return lines) and the ROLLED
>    ADJUDICATIONS (the `firstTouchTrace` entries), each entry split
>    `intendedTarget` vs not. (iii) FACES per arm, INTENDED TARGETS
>    PRIMARY (all bodies beside): the realised FAIL share vs the mean
>    logged `pFail` (the roll's calibration — E1a's I1 form, live: a
>    stored boolean "calibrated" iff the realised share lies inside the
>    bootstrap interval of the mean pFail); the pFail distribution (bins
>    0.01 × 41) and the CAP-HIT share (pFail = 0.4); ⭐ THE TERM
>    DECOMPOSITION at every adjudication — the four addends of `raw`
>    (floor 0.01 · speed term · pressure term · misalign term) recomputed
>    from the logged terms with the shipped `touchFailChance` arithmetic
>    (IMPORTED constants; a fixture proves the recomposition equals the
>    logged pFail bit-exactly), each term's MEAN SHARE of raw over FAILED
>    adjudications and over ALL, the technique multiplier's distribution,
>    and THE MAJORITY TERM over failures STORED as a boolean per term
>    (majority = share > 0.5; none ⇒ 'noMajority' true); the marginals —
>    P(fail | speed bin 2 m/s), P(fail | pressure bin 0.1), P(fail |
>    misalign bin 0.1), P(fail | dribbling bin 0.1), P(fail | positioning
>    bin 0.1) — with counts; the distributions of speed, pressure, misalign,
>    dribbling, positioning on intended-target adjudications (bins); the
>    own-touch (×0.45) share; WHAT THE OTHER CURVE WOULD HAVE SAID — the
>    heavy (or base) pFail recomputed per adjudication and published
>    beside, never scored; the `miscontrols` team stat per match against
>    the trace's fail count (a cross-check receipt); THE FAIL'S AFTERMATH
>    — BN-C0's settle ladder at +K after a failed touch (same side /
>    opponent / loose / out) and the knock speed bins; the SPEED SOURCE —
>    for intended-target adjudications the pass's launch speed, distance
>    and power (C1-A's `PASS-POWER-SLICE` — every live call passes 1.0;
>    anchored) so item 4(iv)'s pass-weight question has its live
>    distribution. (iv) THE REALITY ANCHOR — the executor cites the
>    literature / coaching figures for first-touch failure rates on
>    ground passes (open vs under pressure; elite vs amateur) with the
>    caveat of its access, the BF-C0 form; the commander ratifies at
>    banking. (v) PRE-REGISTERED READS (frozen literals on the E arm's
>    stored majority-term booleans; D beside; the dosed-agrees boolean
>    printed): SPEED majority ⇒ *"THE FIRST TOUCH FAILS ON PASS WEIGHT —
>    the speed term is named, and step ④'s power chooser with it."*;
>    PRESSURE majority ⇒ *"THE FIRST TOUCH FAILS UNDER PRESSURE — the
>    pressure term is named."*; MISALIGN majority ⇒ *"THE FIRST TOUCH FAILS
>    ON THE BLIND SIDE — the misalign term is named; the receiver's body
>    returns as a quality term."*; the FLOOR majority ⇒ *"THE FIRST TOUCH
>    FAILS BY THE FLOOR — the roll's constant and its technique multiplier
>    are named; the law's FORM is the commander's question."*; no majority
>    ⇒ *"THE FIRST TOUCH FAILS ON A MIX — the commander decides with the
>    table."*; and, from the calibration boolean, *"THE ROLL IS CALIBRATED
>    ON WORLD 12"* / *"… IS NOT"*. (vi) SEEDS: block **12,541,000–999** (N
>    sized by a disclosed 12-seed smoke on scratch 900,002,900–911; smoke
>    receipt 900,002,920; trace-inert 900,002,980–981; lockstep
>    900,002,990–991; receipt 12,541,999); ZERO stats; registry 73;
>    compact JSON; the hash receipt outside the body; honest-limits single
>    home; every read word STORED; the prose sweep; the two-fractions rule;
>    engine ledgers before heuristics. DOC `BQ-C0-FIRST-TOUCH-CENSUS.md`;
>    INSTRUMENT `scripts/probes/bq-c0-first-touch-census.ts` (BN-C0's
>    arms, pairing, estimator, hash order and settle ladder REUSED; E1a's
>    ledger READ); ARTIFACT `docs/world-model/data/bq-c0-first-touch-census.json`.
> 7. **CONSUMPTION THIS RULING**: BN-C0 consumed block **12,540,000–999**
>    whole (battery 12,540,000–997; tail 12,540,998 declared virgin, never
>    re-opened; receipt 12,540,999; 1,998 walks); scratch 900,002,800–811,
>    820, 880–881, 890–891 used. BQ-C0's block **12,541,000–999** opens at
>    its freeze; after it ≥ 12,542,000; stats ≥ 117,600; registry 73.
> 8. **GOVERNANCE ROTATION** (#303 item 2's law): the live rulings file
>    would pass ~1,500 lines at this ruling. Rulings **#373–#381** (RC-C0b
>    → BF-C0 · BF-T0 (+fix) · BF-T1 → RC-T0b (+fix) → RC-T1b FAIL → BN-C0)
>    rotate BYTE-VERBATIM to
>    [`PROGRAMME-RULINGS-ARCHIVE-373-381.md`](PROGRAMME-RULINGS-ARCHIVE-373-381.md),
>    `cmp`-verified in this round; the live file = **#382 onward**;
>    `PROGRAMME.md`'s resume line updated (seven ARCHIVE files). The BK
>    contract carries the STATUS line naming the quality law; the BN-C0 doc
>    §CORR 1–6.
