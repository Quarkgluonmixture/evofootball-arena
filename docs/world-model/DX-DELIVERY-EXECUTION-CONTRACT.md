# DX — THE DELIVERY-EXECUTION CONTRACT (the body expresses what the chooser elects)

> Bound by **commander ruling #352** (2026-08-26), consuming: **the user's mandate,
> verbatim** — 「对啊,肯定得修,包括高空球,弧线球,力度等,」 (given on the #348 fork's
> plain-language re-presentation, choosing the wind-up fix AND generalizing it) — plus
> the GC arc's measured chain (#341 red → BK-C2 → GC-T1/T1B/T2) and the structural fact
> that forced this contract: **O1-WINDUP-PRECEDENCE** (GC-T1B: 30,318 wind-up decisions,
> displaced-aim share EXACTLY 0 — a wound-up pass cannot express a re-aimed line).
> Arc prefix **DX**; slice 1 = DX-T0 (the wind-up aim door) → DX-T1 (the exam);
> slices 2–4 = TBD contracts-or-extensions in the sequence §3 binds.

## §-1 THE DOCTRINE

The chooser's election is already PRICED (the DV/BK/GC seats, one currency). What is
broken is EXPRESSION: the execution layer cannot perform some elections the pricing
layer can make. 「脑子会选,腿不会踢」 is an artifact of our seams, not a football fact —
a real player's body executes his intent (his ERROR is a separate, real thing; see §4).
⇒ **THE EXECUTION VOCABULARY — aim displacement · flight family/落点 · curl · weight —
becomes electable, slice by slice, each by measurement, each priced, never a ban, zero
taste constants.** A team whose genes ignore a new degree of freedom loses nothing; a
team that uses it badly is punished by selection. 涌现的判据不变: the substrate ALLOWS,
never assigns (VISION §1 铁律, quoted not re-typed: the audit trail is the ruling).

## §2 MECHANISMS — SLICE 1 (the wind-up aim door)

* **M-DX.1 THE DOOR.** `dxWindupAim` — new Match flag, additive, dormant, default
  `false`, never env/bundle-armed, byte-identity off, pin suite from birth. Armed: a
  pass that enters the WIND-UP channel stores and resolves through **the elected
  candidate's OWN aim** — the exact point the argmax scored (a led candidate's displaced
  point, a strike-plane candidate's grid point, a to-feet candidate's mate.pos — which
  for to-feet is byte-identical to today). ⛔ NOTHING about the flight itself changes:
  no speed, no height, no curl, no new physics — the ONE change is WHICH POINT the
  already-shipped kick is struck toward. The shipped one-touch bypass (which already
  honours elected aims) is untouched.
* **M-DX.2 NO NEW CHOICE.** The door adds zero candidates, zero scoring terms, zero
  genes: it makes the EXISTING elected aim survive the wind-up. The DLC gene family
  (`passLeadSupport`) and the corridor gene (`dvExposureWeight`) keep their seats; a
  world arming `dxWindupAim` without DLC armed changes NOTHING measurable (to-feet aims
  are today's aims) — G-INERT, measured never assumed.
* **M-DX.3 THE COMPOSITION CLAIM.** The door's value is compositional: DLC (alternative
  lines exist) + GC (blocked lines cost) + DX (the body can strike the alternative).
  DX-T1 measures exactly that stack.

## §3 THE ARC & THE SEQUENCE (user-mandated breadth, one slice at a time)

* **DX-T0 — THE DORMANT SEAM** (dispatched at #352): the flag + the aim plumb-through at
  the wind-up seam (`armPendingPass` → `pendingKick.aim` → `resolvePendingKick`) + the
  seam map + the pin suite (G-OFF byte-identity both world shapes · G-INERT (armed, no
  DLC ⇒ identity) · G-BITE (armed + DLC + a displaced election ⇒ the struck point IS the
  elected point, fixture-proven) · prohibitions · mutants) + fingerprint receipt. ZERO
  behaviour change anywhere unarmed.
* **DX-T1 — THE EXAM**: arms = world-11 stack + DLC pair + `bkGroundCorridor` (the
  GC-T2 composition, gene 0.5, geneOk VALUE check) in BOTH arms, SHUT vs ARMED on
  `dxWindupAim` alone. **H-DX.1**: (a) `altDisplacedShare` on wind-up flights leaves
  ZERO resolvedly (the door demonstrably expresses); (b) the H-GC.2(b) face — ground
  passes per match — holds a non-inferiority band from THIS shut arm (does expression
  cure the suppression GC-T2 resolved?); (c) the strike faces do not worsen and are
  REPORTED with leave-one-out sensitivity (the #346/#348 orders); (d) the lofted
  controls flat. Season ladder REPORTED (gene axis = the DLC gene, evolvable). Sizing
  from GC-T2's variances at its own robust magnitudes; the power gate (gN) with the
  env-escape closed (#348 §CORR 2's order).
* **SLICE 2 — WEIGHT (力度)**: the banked, dormant PW chooser (`pwWeightChooser`,
  PW-PASSWEIGHT-CONTRACT.md) enters an exam world beside DX — its own contract already
  exists; the slice re-freezes its exam at the current stack. ⭐ It also answers the
  interventional half of the user's speed question (BK-C2 (v) was observational only).
* **SLICE 3 — CURL (弧线球)**: #330's held election door — census FIRST (curl exists in
  flight physics; the chooser never elects it; C0 maps what exists vs what a curl
  election would consume), then contract.
* **SLICE 4 — LOFTED TARGET CHOICE (高空球落点)**: BK-T4's named binding constraint
  (the volume-dominant dink has no alternative line) — the corridor at the LOFTED
  chooser's target choice; BK-C1's held exclusion governs; its own contract.
* **Exit of every slice = its exam + the play-test (USER GATE)**; nothing ships to the
  default world; entries per the entry-rung form.

## §4 NON-CLAIMS & HELD DOORS

No execution-ERROR model is built here (real mis-hits are a separate, real dimension —
a named door, not slice work); no promise the suppression fully converts (DX-T1
measures); no new flight physics anywhere in slice 1; slices 2–4 bind nothing until
their own contracts/extensions land; the graded hazard (GC contract §4) stays a held
door behind DX-T1's read.

## §6 VISION AUDIT (the #91 form)

* vs 底座给能力: expression is the purest capability — the substrate stops FORBIDDING
  what the chooser already elected; no content is assigned. PASS.
* vs #200 (no taste constants): the elected aim is the chooser's own scored point;
  slice 1 adds zero constants, zero genes, zero terms. PASS.
* vs tactics-emerge: usage stays gene-gated (DLC/DV genes); selection decides who
  benefits. PASS.
* vs the assembly law: one seam (the wind-up aim), the existing candidates, the
  existing prices — no duplicated path. PASS.

## §7 REALITY AUDIT (the #201 rule)

* A real player who decides to play his teammate into space DOES strike the ball into
  space — intent and execution are one skill; our wind-up discarding the elected point
  is a seam artifact with no football referent. Fixing it is realism repair, not a new
  behaviour. PASS.
* Real weight/curl/trajectory choices are the SAME integrated kick (the user's 「包括高
  空球,弧线球,力度」 is how a footballer actually thinks — one foot, many parameters);
  the slice order exists for MEASUREMENT attribution, not because the sport separates
  them. Stated so the sequence is never mistaken for a claim about football. PASS.
* Honest limit: expression without error makes players MORE precise than humans; the
  execution-error door (§4) is where realism pushes back — named, not smuggled.
