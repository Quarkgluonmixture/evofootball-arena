# EK-HOLD — 挣来的持球观 (THE EARNED HOLD-BELIEF CONTRACT: discharging #248.2(i), the archetype debt)

> Drafted by the commander 2026-08-12 under the user's substrate self-drive
> order (基建底座自走) and ruling #259. THE DEBT (#248.2(i), the ledger's
> archetype): the whetherEye seat consumes a CERTIFIED, WORLD-MEASURED cost
> table as knowledge SHARED by every armed body — world-price knowledge wired
> innate, no belief layer, nothing earned. THE STANDARD (#247/#248, proven by
> the DV/T2 pilot #250–#258): dimensions may be hand-built; THIS WORLD'S VALUES
> must be EARNED — truth instrument-side, belief born absent, learnable from
> own events. Binding once the §6/§7 audits are ruled. HONEST BLAST RADIUS:
> `whetherEye` is null in every production path and NO a4 play-test world arms
> it — the debt lives in probe worlds only; this discharge builds the honest
> form, it changes no shipped behaviour.

## §0 Why the DV pattern does NOT port mechanically (the drafting's load-bearing finding)

The certified table's cost is a COUNTERFACTUAL quantity — hold-k vs act-now,
measured by paired probe worlds. A team living in ONE world cannot observe the
counterfactual; no account book can be commensurable with it (the #256.2
lesson: the yardstick must be the book's own quantity). ⇒ The earnable layer
learns an OBSERVABLE label instead, and gets ITS OWN truth census. The
certified counterfactual table REMAINS the capability instrument (exams that
dose truth); it stops being the only form hold knowledge can take.

## §1 The claim (H-EK — the commander's port; no user registration attached)

Given only their own hold outcomes (the observable label), teams can EARN a
hold-risk map over pressure whose SHAPE matches the world's own measured
truth — the DV/T2 result replayed on the programme's second knowledge family.

## §2 Mechanism (M-EK, dormant behind its own explicit flags)

* **M-EK.1 — THE OBSERVABLE HOLD LABEL.** At an eligible hold decision moment
  (the seat's OWN eligibility predicate, unchanged), when the body HOLDS: the
  event is indexed by the PERCEIVED pressure band b at the decision (the
  seat's own band placement — the book indexes what the chooser reads, the
  #256.2/#257.2 commensurability rule applied at the source), and the label
  closes as PUNISHED iff the holding team LOSES possession within the traced
  window W. ⭐ W and the loss semantics are TRACED from the C5 census family's
  own committed definitions (EK-C0's frozen half shows the trace; nothing
  re-typed). Acted-now moments carry no label in slice one.
* **M-EK.2 — THE BELIEF + THE BOOK.** Three per-pressure-band hold-risk
  weights, BORN ABSENT; the per-team account book = (holds[b], punished[b]),
  belief[b] = punished/holds, empty ⇒ ABSENT, season-reset — the T2-T0 form
  verbatim (zero-constant, own events only, public state only, match-local
  gene-view writes, NO franchise writes — the Lamarck catch inherited).
* **M-EK.3 — CONSUMPTION.** A NEW explicit belief flag; armed (belief flag +
  the seat armed), the seat's hold pricing consumes the team's OWN believed
  risk in place of the injected certified table, under a substitution form
  drafted at EK-T0 that preserves R-B strict no-subsidy (#64.1) — the belief
  can only make holding LESS attractive than the neutral form, never subsidise
  it. Truth-dosing = the instrument writes the EK-C0 census values into the
  belief (the exam idiom); the certified counterfactual table is NOT consumed
  on this path.
* **M-EK.4 — SCOPE.** The seat's eligibility, percept honesty (the one E3R2
  pull), scope machinery and decision classes are UNTOUCHED. Doors-shut
  qualifier inherited where applicable. Coarseness = pressure bands ONLY in
  slice one (the T2-C0 rarity lesson pre-applied: 27 cells would starve the
  book; stale/support axes are named later slices). No production path changes.

## §3 Instruments & the arc

* **EK-C0 — THE OBSERVABLE HOLD-OUTCOME CENSUS** (instrument-only): in the
  canonical whetherEye-armed world (traced from C5-T2's own committed exam
  world), measure P(punished | held, pressure band b) with CIs + the event-rate
  moments (holds per band per team per match — the EK-T1 sizing source, the
  #256.3 lesson pre-applied). ⭐ #246 shape check pre-registered: higher
  pressure ⇒ higher hold risk expected; an inversion routes to the 街机偏离
  test, never corrected into the table. This census IS the belief's
  convergence yardstick.
* **EK-T0 — THE DORMANT BELIEF SEAM**: the book + the substitution form +
  the flags; full identity stack + two-doors vs every banked seam; G-NOTABLE
  extended to the EK-C0 values; the T2-T0 gate canon (incl. G-NOLAMARCK).
* **EK-T1 — THE CONVERGENCE EXAM**: the #257.3 frame form — learn-only arm
  (world byte-identical to the armed-neutral form), sharpened conjunction
  predicate (mean-vector ordering resolved AND ordered-book share ≥ frozen τ
  at ex-ante-sized M from EK-C0's moments), learning curve reported,
  F-EK-a/b/c pre-named per the F-DV2 forms.
* **Exit** = the #248 ledger entry (i) marked DISCHARGED of record; whether
  any play world arms the earned form is a LATER user decision (nothing ships
  by this arc).

## §4 Non-claims

No change to any production or a4 world. No re-cut of the certified
counterfactual table or its exams. No stale/support-axis beliefs, no
per-player books, no coach/opponent/imitation channels (the six-source
ledger's own rungs). The certified table is not deleted — it remains the
capability-exam instrument; this arc adds the EARNED form beside it.

## §6 VISION audit (the #91 form)

* vs §1 底座给能力: the capacity to remember one's own hold outcomes is
  substrate; the content is earned per team. PASS.
* vs #200: zero hand-painted values — the book fills from own events; both
  censuses stay instrument-side. PASS.
* vs 感知诚实: the label indexes the seat's OWN perceived band (no new
  channel; the percept pull is the seat's existing one); the outcome reads
  own possession + public clock. PASS.
* vs the certified-price idiom: this DISCHARGES the idiom's own recorded debt
  by adding the earned layer the standard demands — the pilot's proven form
  replayed. PASS.
* vs emergence: born absent; the shape must emerge from punishment or the
  claim scores negative. PASS.

## §7 REALITY audit (the #201 rule)

* Real players learn "don't dwell on the ball under pressure" from being
  robbed — an experience lesson before it is a coached one. PASS (the
  coach-first honest limit stated, as in DV-T2 §7).
* The observable label (held → lost possession soon) is exactly the lesson's
  real currency; the counterfactual cost is the ANALYST's quantity, not the
  player's — the split mirrors reality. PASS.
* Pressure-band coarseness matches how the lesson is actually held ("under
  pressure, release it") — finer context is coached later. PASS.
