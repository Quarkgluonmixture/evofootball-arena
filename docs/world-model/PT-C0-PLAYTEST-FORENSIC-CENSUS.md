# PT-C0 — THE PLAY-TEST FORENSIC CENSUS (用户的三句话,变成三把尺子)

> **The census that gives the user's eyes instruments.** Authorized by
> **COMMANDER RULING #368 item 3**. Lineage: **#365** (the RA entry `?a4world=12` shipped as a
> play-test entry, gated on the user's eyes) → **#368 item 1** (the user's world-12 verdict,
> REGISTERED VERBATIM — the RA entry is NOT confirmed by the eyes) → **#368 item 2** (the three
> observations, each labelled MEASURED / HYPOTHESIS, with a DISCRIMINATING PREDICTION frozen) →
> **#368 item 3** (this census).
> Census form of record: [`RC-C0-COOPERATION-CENSUS.md`](RC-C0-COOPERATION-CENSUS.md) — this
> census mirrors its structure. Classes and ladders reused: **DX-C2 §P.B**'s four-way outcome
> ladder, **RA-T1B**'s measured-ground-pass predicates and its `groundPassesPerMatch` face,
> **the A4 grant battery's** own spacing and dup-run limbs with their own constants.
> Instrument: `scripts/probes/pt-c0-playtest-forensic-census.ts`.
> Artifact: `docs/world-model/data/pt-c0-playtest-forensic-census.json`
> (**or its `.RED.json` SIDE PATH** if any gate is red — the red-routing idiom, #334 item 5).
>
> ⛔ **THIS IS A CENSUS.** It publishes MEASUREMENTS. **IT ATTRIBUTES NOTHING**, scores no
> hypothesis and arms no mechanism. The ONE pre-registered item is #368 item 2(a)'s
> **DISCRIMINATING PREDICTION**, PRINTED from the frozen definitions — **REPORTED, NO GATE**.
> The commander rules.
> ⛔ **X-SRC-ZERO**: no file under `src/` is created or edited. The probe CALLS the shipped
> exports and reads `Match` state per tick. **THERE IS NO WRAPPER AT ALL** — `gLockstep` proves
> observed ≡ unobserved byte for byte, **PER ARM**.
> ⛔ **WORLD 12 IS NEITHER REVERTED NOR PROMOTED** (#368 item 4): it remains the play-test entry
> the exams measured, and the user's gate stays the user's.

## §0 THE WORDS OF RECORD, AND WHAT THIS CENSUS IS FOR

**THE USER'S VERDICT, VERBATIM** (#368 item 1, received 2026-09-03):

> 「12我看了下,还是有人挤人,传不出去球,传到人身上弹回,或经常传到对面身上」

**#368 item 3, the scope this census instruments, verbatim:**

> *"⭐⭐ **PT-C0 DISPATCHED — THE PLAY-TEST FORENSIC CENSUS** (有故事就要有探针, #144 (a); the
> census form of record — DX-C2 / RC-C0; X-SRC-ZERO; ⛔ a census: it ATTRIBUTES nothing; it
> turns the user's three sentences into instruments every later exam can carry as faces).
> Question groups, definitions frozen at the executor's §P:
> (i) **挤人** — the A4 battery's OWN spacing and dupRun limbs (`a4-p1c-grant-census.ts`,
> anchored; no new constant) on the attacking side in open play, PLUS the raw
> nearest-same-side-outfielder distance distribution per tick (bins stored, so any cut
> re-derives); (ii) **传不出去** — the carrier's possession-spell length from first touch to
> release (bins), the share of carrier decisions where a pass candidate was scored but LOST to
> Dribble / HoldUp / ShieldHold (the action's own stored `scores` top-4, `PlayerBrain.ts`,
> anchored; if not observable byte-inertly, DECLARE), ground passes/match, carries/match;
> (iii) **传到人身上** — for every ground pass the FIRST body the ball contacts before the
> intended receiver's first touch: none / own target / own non-target / opponent, with the BK
> shell sector (front / side / back) at contact for own bodies (the `bkContactLedger` and the
> contact law's own channel, anchored; any class not observable from Match state after the step
> is DECLARED), the REBOUND share (「弹回」 — an own-body contact whose post-contact velocity
> along the launch line is NEGATIVE, defined at §P), the receiver's facing sector at his first
> touch on completed passes, the outcome partition beside; item 2(a)'s discriminating prediction
> printed against the frozen definitions (REPORTED, no gate).*
> *ARMS, PAIRED on the same seeds, the world's own composer CALLED: world 12 in THE FORM THE
> USER PLAYS (the DOSED default — the shipped loaders' own tables, `loadA4Tables` /
> `loadL3Dose` / `loadPcDose` or the SAME artifact files they read, with a fixture proving the
> tables ≡ what `GameApp` composes; if not reachable byte-identically from Node, DECLARE and
> run empty-book only) · world 12 EMPTY-BOOK (the exams' form) · world 11 (dosed — the user's
> comparison) · the SHIPPED default (no a4 arming — the world the league plays). Paired Δs
> 12−11 and 12−shipped REPORTED on every face; the dosed-vs-empty pair tests #365's first-look
> disclosure. Block **12,534,000–999** opens (N sized by the disclosed smoke; arms share seeds;
> receipt at 12,534,999; the tail declared); scratch **900,001,900–999** ONLY; ZERO stats;
> registry 73."*

**#368 item 2(a)'s DISCRIMINATING PREDICTION, verbatim** — the ONE pre-registered item here:

> *"⭐ **DISCRIMINATING PREDICTION, FROZEN HERE (the #140 form):** on world 12 ground passes, if
> H2 dominates the FIRST body the ball touches is the OWN TARGET with a side/back sector more
> often than an OPPONENT; if H1 dominates, opponents and own NON-target bodies dominate the
> first contact. REPORTED by PT-C0, no gate."*

with the two hypotheses named at #368 item 2(a), verbatim: *"**H1 priced ≠ struck** — the
corridor price is binary and static at election while the strike carries noise, orientation and
the lead correction (audit §1.5, structural; steps ④/⑤); **H2 the receiver is not READY** — not
told, not turned, the ball meets his side/back shell (`BodySector`, `physical.ts`) and rebounds
— the RC seat's own mechanism."*

**In plain football language.** The user played the twelfth world and said three things are still
wrong. This census does not argue with him and does not explain him. It builds one measuring
stick per sentence and reads it on the world he actually played, on the world he compares it to
(the eleventh), on the form the exams measured (the empty-book twelfth) and on the shipped game:

1. **挤人 — do they stand on top of each other?** When your side has the ball, how far is the
   nearest teammate from each of your outfielders — and how often are the two closest men inside
   four metres of each other, which is the battery's own name for a 撞车?
2. **传不出去 — does the man on the ball get stuck?** How long does he hold it before anything
   happens, split by what ended the hold (a pass / a shot / a clearance / losing it)? And when
   he decided, how often was a pass sitting there in his own shortlist while he chose to carry
   or shield instead? Then the plain volumes: passes a match, carries a match.
3. **传到人身上 — whose body does the ball hit?** For every ground pass, whose is the FIRST body
   the ball touches — the man it was meant for, another of ours, or an opponent? If it is one of
   ours, which part of him did it hit (front, side, back — the game's own classifier)? Did it
   come back at the passer (「弹回」 = the ball's speed along the intended line goes negative)?
   And when the pass did arrive, which way was the receiver facing?

⛔ Nothing here is decided and nothing is blamed. The census publishes the three answers on four
worlds, prints the ONE frozen prediction sentence, and stops.

## §P THE FREEZE (all of it BEFORE any battery seed)

### §P.A (i) 挤人 — CROWDING

| quantity | frozen form |
|---|---|
| **OPEN PLAY** | `m.phase === 'playing'` |
| **THE ATTACKING SIDE** | `ball.owner.side` when the ball is OWNED; else the passer's side of a **LIVE tracked measured-ground-pass flight** (the ball in a pass flight); else the sampled tick has **NO possession side** and is **EXCLUDED** — counted in `crowd.unattributedSampleShare`, never folded |
| **SAMPLE CADENCE** | every `SAMPLE_EVERY` = **10 ticks** (`simTick % 10 === 0`) — the A4 battery's OWN spacing-sample cadence, **anchored**. ⛔ NO new constant |
| **THE POPULATION** | the A4 limb's own filter, verbatim: `role !== 'GK' && !sentOff` on the attacking side |
| **THE SPACING LIMB** | the A4 battery's own arithmetic, reused: for each attacking outfielder `a`, `nearest_a = min_{b≠a} hypot(pos_a − pos_b)` over the same-side **OUTFIELDERS**. Published as the mean over `(sampled tick, outfielder)` pairs, with **RAW BINS 0.5 m × 61** (0–30 m, last = overflow) so any cut re-derives |
| **THE DUP-RUN LIMB** | the A4 battery's own limb and its own constant, **anchored**: attacking outfield **PAIRS** (`b > a`, each counted once) with `hypot < DUP_RUN_M` = **4 m**, per sampled tick |
| **⭐ THE 撞车 FACE** | the share of sampled ticks on which the attacking side's **MINIMUM PAIRWISE** outfield distance is `< DUP_RUN_M` (bins 0.5 m × 61 stored) |
| **MEDIANS** | **BIN-DERIVED** (the lower edge of the bin whose cumulative count first reaches n/2) so `gFaces` re-derives every one off disk |

⭐ **THE A4 LIMBS DEPEND ON NO DESIGNATION.** They are purely geometric — positions, roles,
sent-off — so #368 item 3's escape clause ("if the battery's definition depends on a designation
the shipped world does not carry, DECLARE and use the raw faces below as primary") does **not**
fire: nothing needed declaring away, and the limbs and the raw faces are **BOTH primary**.

### §P.B (ii) 传不出去 — THE BALL THAT CANNOT BE PASSED OUT

| quantity | frozen form |
|---|---|
| **THE POSSESSION SPELL** | from the tick `ball.owner.gid` becomes `g` (the previous tick's owner gid was not `g`) to the **first** tick at which the owner gid is no longer `g`. Length in ticks × `DT` = sim-seconds. **Bins 0.25 sim-s (15 ticks) × 41** (0–10 sim-s, last = overflow) |
| **THE RELEASE KIND** | read at the closing tick in this **FROZEN PRECEDENCE**: `passed` (`pendingPass.t` changed this tick with `passerGid === g`) > `shot` (his side's `shots` stat rose this tick) > `cleared` (his side's `clearances` rose) > `lost`. Published whole-population AND per kind |
| ⚠ **A DECLARED REFINEMENT** | #368 item 3 names three buckets (passed / shot / lost) and a clearance in its release list. `cleared` is published as **its OWN bucket**, not folded into either — folding it would hide a release inside a loss |
| **AN OPEN SPELL** | a spell still open at full time is **COUNTED** (`spell.openAtFullTimePerMatch`) and enters **NO** spell face |
| **A CARRIER DECISION** | a tick at which `ball.owner.gid === p.gid` **AND** `p.action` is a **DIFFERENT OBJECT** from the previous tick's for that gid. Every carrier decision assigns a FRESH action object literal (`p.action = { type: …, scores }` — the switch's own branches, anchored) and a tick on which no decision ran leaves the same object in place, so **object identity IS the decision-tick detector**. The map is primed from the constructed match BEFORE the first step, so the first tick cannot count as a spurious decision |
| **⭐⭐ PASS-LOST-TO-CARRY** | a carrier decision whose stored `p.action.scores` contains an entry with `action ∈ {Pass, LoftedPass, ThroughBall, Cross}` while `p.action.type ∈ {Dribble, HoldUp, ShieldHold}`. Denominator = **ALL** carrier decisions; the same loss **conditioned** on a pass candidate being visible at all is published beside |
| ⚠ **THE TRUNCATION, DECLARED** | `scores` is `cands.slice(0, 4)` — the **TOP-4 ONLY** (anchored at the carrier branch's own site, and the off-ball branch's `slice(0, 4)` is anchored separately so the two can never be confused). A pass candidate ranked **fifth or lower is INVISIBLE** to this face ⇒ **the face is a LOWER BOUND on the loss** |
| **GROUND PASSES / MATCH** | **RA-T1B's own reported face, REUSED and anchored**: measured ground passes = `isMeasurableGroundPass` (`shortPass` \| `throughBall` \| `cutback`, ground launch, with a pending-pass target), per match on the 240 s clock |
| ⚠ **CARRIES / MATCH — A DECLARED DEVIATION** | **RA-T1B publishes NO `carriesPerMatch` face.** #364 item 2's "more carries" is **PROSE**, not a measured face (verified by search over the exam's instrument and its doc). This census therefore **DEFINES** it as the **ENGINE'S OWN `dribbles` team stat** per match, **anchored at its single increment site** (`Match.ts`, `if (!recollect) team.stats.dribbles++;`), and publishes the spell volumes beside |

### §P.C (iii) 传到人身上 — THE BALL THAT HITS A BODY

| quantity | frozen form |
|---|---|
| **THE POPULATION** | every **MEASURED GROUND PASS** — RA-T1B's `isMeasurableGroundPass`, reused and anchored; registered **at the strike** via `pendingPass` (a pass with no pending target is not in the population). **ONE flight is tracked at a time**: a new release RETIRES the previous one (RA-T1B / RC-C0's own idiom) |
| **E, THE AIM POINT AT THE STRIKE** | where the strike resolved a **TRACKED wind-up record** for this passer and target, `E = aim + (aimLead ?? 0)` — the record's own elected point. Otherwise (a synchronous strike) `E =` the target's own position at the strike tick, which is `performPass`'s own aim base `mate.pos`. **LAUNCH** = `ball.pos − ball.vel · DT`. `u = unit(E − launch)`; `|E − launch| ≤ 1e-6` ⇒ **NO launch line**, and the flight enters no rebound face (COUNTED in `contact.noLaunchLineShare`) |
| **⭐⭐ THE FIRST BODY** | the **first** tick after the release at which `ball.lastTouch` is a body **OTHER than the passer**. CLASSES: `ownTarget` (=== the pendingPass target) / `ownNonTarget` (same side) / `opponent` / `none` (the flight retires without such a tick). This is the engine's OWN touch channel: every path that moves the ball off a body assigns `ball.lastTouch = p` (**four honest sites**, anchored — the two shipped control/deflection ones and the contact law's own two) |
| **WHAT 'CONTACT' MEANS PER ARM** | the contact law's own `bodyStrike` **sub-class** is recorded when `bkContactLedger.strikesApplied` **ROSE** on the contact tick. Where the law is **not** armed that counter never moves — `gClassesNonVacuous` asserts it is **EXACTLY ZERO** on arm D — and the sub-class is **STRUCTURALLY EMPTY** there: on the shipped default the ball **passes THROUGH** a body the shipped filter drops, so "contact" means a **DELIBERATE or DEFLECTED** touch only. **DECLARED per arm, never zero-imputed** |
| **⭐⭐ THE SECTOR** | the law's **OWN** classifier, **CALLED**: `ballAccessGeometry(body, ball, CONTROL_RADIUS).sector` (`physical.ts`) — `front` ⇔ `facingCos ≥ Math.SQRT1_2`, `back` ⇔ `facingCos ≤ −Math.SQRT1_2`, else `side`, where `facingCos = unit(body → ball) · unit(bodyDir)`. The five-line classifier is **anchored verbatim** and **never re-implemented**; the vocabulary is read off `BodySector`'s own union |
| ⚠ **WHEN THE SECTOR IS READ** | at the **END of the contact tick**. With no wrapper the pre-resolution instant is not observable from state, and by then the law has already released the ball **along the same body→ball normal the classifier reads** ⇒ the sector IS that normal, up to **one tick** of drift. Declared, not hidden |
| **⭐⭐ THE REBOUND (「弹回」)** | an **OWN-body** first contact whose **POST-CONTACT** ball velocity component along the launch line is **NEGATIVE**: `dot(ball.vel, u) < 0`, read at the contact tick. Signed bins **1 m/s × 21** stored. Published over own-body first contacts (primary) and over ALL ground passes (beside, so the two denominators can never be confused) |
| **THE RECEIVER'S FACING AT TOUCH** | on **COMPLETED** passes, the same classifier called on the **intended receiver** at the tick his side's `passesCompleted` rose |
| **THE OUTCOME LADDER** | **DX-C2 §P.B's own FOUR-WAY ladder, reused**: `completed` / `intercepted` / `out` / `unresolved`, first terminal event wins, **TEMPORAL NOT CAUSAL**. Flight retire rules inherited: a new owner other than the passer, any terminal event, or `FLIGHT_RETIRE_TICKS` = 720 |

#### ⭐ THE DISCRIMINATING PREDICTION — ITS PRINTED FORM, FROZEN HERE

**'RESOLVEDLY' is defined as CI SEPARATION AND NOTHING ELSE**: the 95 % cluster-bootstrap
interval of the contrast lies **ENTIRELY ABOVE ZERO** (`ciLo > 0`). ⛔ There is **no threshold
beyond the interval**.

* **H2 TEST**: `Δ_H2 = P(ownTarget ∧ sector ∈ {side, back}) − P(opponent)`, CI entirely > 0.
* **H1 TEST**: `Δ_H1 = [P(opponent) + P(ownNonTarget)] − P(ownTarget ∧ side/back)`, CI entirely > 0.

Both probabilities are over the **whole measured ground-pass population** of the arm. **THE
FOUR-WAY VERDICT, FROZEN so no precedence is invented after sight:**

| case | the sentence PRINTED |
|---|---|
| **exactly H2** | *"THE NUMBERS FAVOUR H2 ("the receiver is not READY"): the first body the ball touches is the OWN TARGET with a side/back sector RESOLVEDLY more often than it is an OPPONENT."* |
| **exactly H1** | *"THE NUMBERS FAVOUR H1 ("priced ≠ struck"): opponents and own NON-target bodies RESOLVEDLY dominate the first contact."* |
| **BOTH** | *"MIXED — both conjuncts separate above zero at once, so the FROZEN form favours NEITHER hypothesis resolvedly."* |
| **NEITHER** | *"NEITHER RESOLVEDLY — no contrast's interval lies entirely above zero at this power, so the census prints no favoured hypothesis and no null may be cut either."* |

Printed on **BOTH world-12 arms** (A the dosed form the user plays, B the empty-book form the
exams measured). ⛔ **REPORTED, NO GATE**: the census prints the sentence from the frozen
definitions and stops. It **attributes nothing** — the commander rules (#368 item 3).

### §P.D THE ARMS — four, PAIRED on the same seeds

Arm `k` walks seed `s` with the **IDENTICAL population construction** (RC-C0's own `buildMatch`
plumbing: genomes and squads drawn from the seed exactly as it does, the same 240 s match), so
the four arms differ **ONLY** in the world's own composition and **every Δ is PAIRED per seed**.

| arm | composition — the composer **CALLED, never copied** | gate on the match |
|---|---|---|
| **A** | **world 12 DOSED — THE FORM THE USER PLAYS**: `a4MatchFlags(12)` as construction flags + `armA4World(m, null, 12, l3Dose, pcDose)`, the doses from the **SHIPPED LOADERS THEMSELVES** | `raArmedVersion(m) === 12` |
| **B** | **world 12 EMPTY-BOOK — the exams' form**: `a4MatchFlags(12)` + `armA4World(m, null, 12)` | `raArmedVersion(m) === 12` |
| **C** | **world 11 DOSED — the user's own comparison**: `a4MatchFlags(11)` + `armA4World(m, null, 11, l3Dose, pcDose)` | `corridorArmedVersion(m) === 11` |
| **D** | **THE SHIPPED DEFAULT**: `new Match({ seed, teamA, teamB })` — no a4 flags, no arming | **STRUCTURAL**: `raArmedVersion === 0` AND `corridorArmedVersion === 0` AND `bkContactLaw` OFF |

**⭐⭐ THE DOSED ARM IS REACHABLE, AND THE `loadA4Tables` HALF OF #368 item 3's REQUIREMENT
DISSOLVES ON INSPECTION.** Three traced facts, all anchored:

1. `GameApp.armA4` puts world 12 (and world 11) inside its `pcStack` predicate, and the
   `loadA4Tables()` branch is guarded by `… && !pcStack && this.a4Tables === null` ⇒ **the A4
   census tables are NEVER loaded for `?a4world=12`**. The user plays with `a4Tables === null`.
2. `armA4World`'s **RA branch RETURNS** (`if (isRaWorld(version)) { armRaWorld(match, l3Dose,
   pcDose); return; }`) **before** the `if (tables === null) return;` refusal ⇒ the `tables`
   argument **cannot reach** worlds 11/12 at all. The same holds for the corridor branch.
3. ⇒ **the user's form and the exams' form differ ONLY in the two DOSES** — which is exactly
   what the **A−B** pair measures. ⛔ Nothing is approximated; there is no table to approximate.

**THE DOSES.** `loadL3Dose()` and `loadPcDose()` are **CALLED** — the shipped loaders, not
copies. Each imports its own artifact file and enforces its **own declared-SHA identity guard**
before returning, so a swapped file **REFUSES TO ARM**. `gDoseSource` additionally publishes the
**sha256 of the FILE BYTES this process read** from the two paths the loaders name
(`docs/world-model/data/l3-t1-convergence-exam.json`,
`docs/world-model/data/pc-t1-learning-exam.json`) and asserts both pooled doses are **NON-EMPTY**
— canon, VERBATIM: *"a dose-source guard should hash the bytes it reads, not a self-declared
field"* (home: `BU-T1-MT-COMPOSITION.md` §COMMANDER CORRECTIONS item 6). ⚠ The PC loader says of
itself that under bare node the `?raw` query is stripped and the module loader hands back a
parsed object, so `pcDoseGuard.bytesChecked` reads **false** there — which is **precisely why**
this gate hashes the bytes independently. **If the doses had not been reachable the instrument
would REFUSE TO RUN** (`process.exit(3)`) rather than silently approximate one.

**WHAT `null` MEANS FOR ARM B.** `null` L3 dose ⇒ the two defence books stay as the season left
them (the shipped law's own season-one state; the dose is deliberately not part of
`l3ArmedVersion`). `null` PC dose ⇒ the recognition books are **born absent** — everyone a
novice, so every reaction pays the long tier. `null` tables ⇒ nothing, by fact 2 above.

**ARM D AND THE WORKER-FIXTURE CANON.** Canon, VERBATIM: *"WORKER-SIMMED fixtures play the
SHIPPED world (League.toJSON omits matchFlags; true since #155, stated now, test-pinned; refines
#270's E4 correction; matches the perf diagnostic)"* (home: ruling #283.2(iv)).
`gShippedConstruction` proves arm D **is** that construction with a fixture on out-of-band
scratch: a League round-tripped through `toJSON`/`fromJSON` exactly as the worker's `simRunner`
does, then `League.createMatch(f)` against a Match built with **arm D's constructor shape** at
the SAME derived seed / `TeamInfo` / duration / derby — the two **WHOLE-MATCH SIGNATURES are
identical** after both run to completion. Three receipts beside: `matchFlags` absent from
`toJSON`, `undefined` on the `fromJSON` league, and the **BOOLEAN FLAG SET** of a worker-built
match equals a bare `new Match({ seed, teamA, teamB })`. ⚠ Stated: arm D uses **this census's own
population plumbing** (so the Δs stay paired), not the league's franchises — the fixture proves
the **CONSTRUCTION SHAPE**, which is what the canon is about.

**THE CONTACT LAW PER ARM.** World 12 ⊃ 11 ⊃ 10 ⊃ **9**, so `bkContactLaw` is **ARMED on A, B and
C** and **OFF on D**. `gClassesNonVacuous` asserts both halves structurally (applied strikes > 0
on A/B/C, **exactly 0** on D). ⇒ **the (iii) contact faces are not the same question on arm D**
and §R says so wherever it quotes them.

**⭐ THE PAIRED Δs (#368 item 3).** The bootstrap resamples **SEEDS**, and both arms of a pair
move together inside every draw, so the interval is a **PAIRED** one by construction. The three
pairs of record: **A−C** (12 vs 11, the user's comparison) · **A−D** (12 vs shipped) · **A−B**
(dosed vs empty — the test of #365's first-look disclosure). REPORTED on **every** face;
⛔ **nothing here is scored**.

### §P.E THE SEEDS AND THE SIZING

* **Block 12,534,000–999**: battery seeds **12,534,000–12,534,998** (**N_FROZEN = 999**),
  construction receipt **12,534,999** — the block is consumed **WHOLE** and the tail is EMPTY.
  Each seed is walked **ONCE PER ARM** ⇒ **4,000 walks booked = walked**.
* Smokes and lockstep on out-of-band scratch **900,001,900–999 ONLY** (smoke 900,001,900–911 +
  receipt 912; lockstep 900,001,990–991; the `gShippedConstruction` fixture at 900,001,995–996) —
  canon, VERBATIM: *"verifier scratch walks use the stage's own consumed band or the out-of-band
  scratch range (≥ 900,000,000) — never the next virgin block"* (home:
  `PW-T0C-OBJECTIVE-FIDELITY.md` §COMMANDER CORRECTIONS item 6).
* **Stats consumed: ZERO.** Registry **73** untouched.
* **SIZING** (the RC-C0 §15 / DX-C2 §15 house form; §DEV-PREFLIGHT's smoke is the variance
  source). Targets 0.05 on the three pre-registered **A−C paired Δs** of #368 item 3:

| face (paired A−C) | realised hw (12 clusters) | target | N required | resolvable at 999 |
|---|---|---|---|---|
| `crowd.crashShare` (the 撞车 share) | 0.04584622674874325 | 0.05 | **21** | ✅ |
| `lost.passLostToCarryShare` | 0.0422021157351426 | 0.05 | **18** | ✅ |
| `contact.ownTargetSideBackShare` | 0.019852935605322836 | 0.05 | **4** | ✅ |

  All three are resolvable well inside the block, so **N_FROZEN takes the block's own maximum**
  — 999 battery seeds × 4 arms — and **no row is declared unresolvable**. Expected half-widths at
  N_FROZEN: 0.005024715499510116 · 0.004625323392667285 · 0.0021758683390356967 (MDEs
  0.007182360889706785 · 0.006611467224557938 · 0.003110200301084686). ⚠ **What is NOT
  resolvable here is stated instead of sized**: every face on the **`none`** first-contact class
  and on the **`cleared`** spell bucket is a small-denominator face and is reported with its own
  realised interval; and ⛔ **no null may be cut anywhere in this census at all** — it is a
  census, not an exam.
* **Bins** (frozen): nearest-mate 0.5 m × 61 · min-pairwise 0.5 m × 61 · spell 15 ticks
  (0.25 sim-s) × 41 · along-launch velocity ±1 m/s × 21 signed · first-contact class × 4 ·
  first-contact `bodyStrike` × 4 · own-body sector 3 × 2 classes · receiver facing × 3 ·
  outcome × 4. **Estimator**: cluster bootstrap, **2,000 draws**, rng seeded from the block base.
* **Medians** are **BIN-DERIVED** so `gFaces` re-derives every one of them off disk — canon,
  VERBATIM: *"the re-derivation gate covers EVERY published face; a percentile face requires
  stored bins"* (home: ruling #287 item 1 + `PC-C0-REACTION-BASELINE.md` §COMMANDER CORRECTIONS
  item 4).

### §P.F THE GATES (all liveness/receipt — NEVER direction)

`gWorld` (per arm, on EVERY walked match and the receipt: A/B `raArmedVersion === 12`, C
`corridorArmedVersion === 11`, D structural) · `gDoseSource` (the shipped loaders CALLED; the
FILE BYTES of the two named artifacts hashed; both pooled doses non-empty) ·
`gShippedConstruction` (the whole-match-signature fixture above) · `gGenomeClean` (`info.genome`
carries no world-12 pin and no corridor weight on ANY arm — canon: dose placement, #270.2 /
#334 item 1) · `gAnchoredConstants` (**33 anchored sites with line receipts**: `DUP_RUN_M` ·
`SAMPLE_EVERY` · the A4 outfielder filter, dup-run pair test and nearest-mate accumulation · the
CARRIER branch's own `cands.slice(0, 4)` **and** the off-ball branch's, named separately · the
single `dribbles` increment site · `AI_INTERVAL` · the `bodyStrike` channel, `bkContactLedger`
and its `strikesApplied` counter · the four `ball.lastTouch = p` sites · the `BodySector` union
**and the law's own classifier thresholds verbatim** · `ballAccessGeometry`'s entry point ·
`registerPass`'s `pendingPass` · `CONTROL_RADIUS` · `GRAVITY` · `bodyDir` · RA-T1B's
`isMeasurableGroundPass` / `isGroundLaunch` / klass ladder / `groundPassesPerMatch` face · worlds
12 and 11's own flag and arming lines · the `armA4World` RA branch that proves the `tables`
argument can never reach these arms · the league's ONE `new Match(` site and the
`...this.matchFlags` spread · the `ActionType` vocabulary read off its own union) ·
`gWalkFixtures` (the A4 spacing / dup-run / min-pairwise arithmetic, RA-T1B's delivery and
ground-launch predicates, the outcome ladder, the first-contact classes, **the rebound sign on
constructed contacts**, **the law's own sector classifier CALLED on six constructed geometries
including both 45° boundaries**, the pass-lost-to-carry predicate, the spell release ladder and
every bin helper) · `gClassesNonVacuous` (every arm has ground-pass flights, spacing samples,
closed spells, carrier decisions and own-body first contacts; **and the contact classes live
where the law is armed and NOWHERE else** — strikes > 0 on A/B/C, exactly 0 on D) · `gLockstep`
(no wrapper; observed ≡ unobserved byte for byte, **per arm**, on out-of-band scratch) ·
`gSrcUntouched` (`git diff --stat HEAD -- src` **AND** `git status --porcelain -- src` both empty
— canon: xSrcUntouched) · `gSeedsBookedEqualWalked` · `gN` (N_FROZEN honoured) · `gHashOrder`
(the body hash computed **LAST** off an explicit **ALLOWLIST SCHEMA**; the file byte-hash
published in §R) · `gFaces` (**EVERY** published face, paired Δ, bin, median, partition, the
printed prediction read and every sizing row re-derived off the **SERIALIZED** artifact).

Canon quoted where it applies — VERBATIM: *"the hashed body is built from an explicit ALLOWLIST
SCHEMA — a field not in the schema never enters the body; forbidden-name lists are retired"*
(home: `PC-T0-LATENCY-SEAM.md` §COMMANDER CORRECTIONS item 1); VERBATIM: *"a src-extracted
constant pins its extraction to the NAMED call site — anchored match + line receipt — never
first-occurrence"* (home: `BK-C0-BODYBALL-CENSUS.md` §COMMANDER CORRECTIONS item 1, ruling #306
item 4); VERBATIM: *"a field carries the unit its name claims"* (home: ruling #294 item 3);
VERBATIM: *"a scored face's walk-side predicate is pinned — anchored extraction or fixture —
because the re-derivation gate proves arithmetic, not definitions"* (home:
`DF-T3-SURFACE-EXAM.md` §COMMANDER CORRECTIONS item 2); VERBATIM: *"a stage doc's prose quotes
artifact FIELDS verbatim or the number becomes a gated face"* (home: `PC-T2-ARMED-WORLD-READ.md`
§COMMANDER CORRECTIONS item 4); VERBATIM: *"a starred finding states its |Δ|÷half-width ratio"*
(home: `BU-T0B-PRICE-SEPARATION.md` §COMMANDER CORRECTIONS item 2); VERBATIM: *"a stage doc's
HONEST LIMITS list is the ONE home; the artifact stores that list verbatim or stores none"*
(home: `RC-C0-COOPERATION-CENSUS.md` §COMMANDER CORRECTIONS item 3, ruling #367 item 3) — **this
artifact stores NONE**; §R HONEST LIMITS below is the list of record. Arming and plumbing
receipts (the applied-strike counts, the dose hashes, the construction signatures) are **never**
quoted as football effect sizes (ruling #289 item 1 + `BU-T1-MT-COMPOSITION.md` §COMMANDER
CORRECTIONS item 5).

## §DEV-PREFLIGHT — the sizing smoke, disclosed in full

A **12-cluster scratch smoke** (`PTC0_MODE=smoke PTC0_N=12`, seeds **900,001,900–911**, receipt
912, lockstep on **900,001,990–991**, artifact off the canonical path at `/tmp/pt-c0-smoke.json`)
was run **BEFORE this freeze**. Its realised half-widths were read out of the smoke artifact's
own `deltas[].halfWidth` fields — **never re-typed from the console's rounded print** — and are
hardcoded in the instrument's `SIZING_INPUTS` (the three rows in §P.E's table).

**Disclosed honestly:**

* The first 12-cluster run went **RED on two gates**, both instrument defects fixed before this
  freeze and both stated here so the record shows what moved and when:
  (a) `gAnchoredConstants` — the `ball.lastTouch = p;` needle was pinned at **3** occurrences
  and the file honestly carries **4** (the two shipped control/deflection assignments and the
  contact law's own two). The pin was corrected to 4, which is the anchored-extraction canon
  working exactly as intended: a wrong count is a RED gate, not a silent pass.
  (b) `gFaces` — **2 of 448** face checks failed on a face computed over an **EMPTY class**
  (`spell.meanSimSeconds.cleared` on arm B: no clearance ended a spell in 12 matches). The cause
  is that **JSON has no NaN literal**: `JSON.stringify` writes NaN as `null`, so the naive
  identity check saw `null ≠ NaN`. The gate now recognises `null` as **the serialization of
  NaN** — and nothing else; every other value must still match **bit for bit**. ⛔ The empty
  class is **reported as empty**, not zero-imputed.
  After both fixes the same 12-cluster smoke re-ran **13/13 GREEN**, with `gFaces` at **448/448
  face-and-Δ** and **73/73** stored-bin / median / partition / prediction-read / sizing checks.
* ⚠ **12 clusters is a NOISY variance estimate** — a strictly weaker assumption than sizing off
  a published battery. Said here, before the battery.
* ⚠ The smoke's own POINT readings are **UNPOWERED and bind nothing**. For the record, so nobody
  can claim the freeze was written after seeing a battery: on 12 scratch seeds the smoke read
  a 撞车 share ≈ 0.45 (A) / 0.54 (C) / 0.50 (D), a nearest-mate mean ≈ 8.7 m (A), a
  pass-lost-to-carry share ≈ 0.33 (A), a spell mean ≈ 0.60 sim-s (A), first-contact ownTarget
  ≈ 0.58 (A) against opponent ≈ 0.32 (A) with ownTarget-side/back ≈ 0.43, a rebound share ≈ 0.64
  (A), and the printed prediction read **H2** on both world-12 arms. **None of these numbers is
  a finding**; the battery's own §R replaces every one of them, and a battery that printed a
  different sentence would be reported as-is.
* The smoke ALSO confirmed instrument liveness: every arm carried measured ground passes,
  spacing samples, closed spells, carrier decisions and own-body first contacts; the contact
  law's applied-strike counter was live on A/B/C and **exactly zero** on D; `gLockstep` was green
  on all 8 arm × scratch-seed walks; and `gShippedConstruction`'s whole-match signatures matched.
* **This section binds nothing.** The freeze is §0–§P.F above.

## §R RESULTS

*(written at the results commit; every number QUOTES the artifact's own fields at 6 dp — the
artifact is the numbers of record, per the #357 standing order)*
