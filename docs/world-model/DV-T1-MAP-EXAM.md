# DV T1 — THE MAP EXAM (地图考试: does a CORRECT RISK MAP fix the #244 deflation?)

Status: **FROZEN, SMOKED, then RUN AT FULL BATTERY.** Everything from §FORM to §NON-CLAIMS — the
world, the eight arms and their exact door declarations, ⭐⭐ **the DOSES and their derivations**,
the pre-registered JOINT and its two limbs, the pre-named forks, the ruler set, the guards, the
estimator, the seed ledger, the N rule and the gate list — is the design fixed **in the probe's
own frozen constants and gate predicates before any receipt ran**, and every clause below is
machine-checkable against the committed artifact rather than a promise about it.

⚠ **This document reports; it does not adjudicate (#203).** The JOINT, its limbs and its
neighbours are printed exactly as the artifact records them, with the mechanical CI and predicate
flags the probe computed. **F-DV-a/b/c are the commander's.** ⭐ **A FIRED FORK IS STILL A
COMMIT** (the #244 precedent): the honest adjudicated result is banked either way.

Authority chain: contract [`DELIVERY-VALUE-CONTRACT.md`](DELIVERY-VALUE-CONTRACT.md) — §3 **DV-T1**
(*"the DLC-T1s battery form re-run with DV armed over the strike plane (arms: ABSENT · plane-alone
anchor re-walk · ⭐ plane+DV at hand doses · plane+DV × CHECK-AND-SHOW). PRE-REGISTERED JOINT:
supply RETAINED (resolved helpful) AND goals RECOVER (in band, or resolvedly toward it vs the
plane-alone anchor). F-DV-a … F-DV-b … F-DV-c"*), §1 **H-DV**, §2 **M-DV.1 / M-DV.2 as amended by
#247 / M-DV.3**, §4 the non-claims. Rulings **#244** (⛔ THE FAILURE THIS EXAM INVERTS) · **#245 /
#246 / #247** (the map-vs-reality audit; METHOD reality's, NUMBERS this world's, SHAPE the fidelity
check; ⭐⭐ TRUTH vs BELIEF) · **#248** (the earned-knowledge ledger — this arc is the PILOT) ·
**#249** (DV-C0 BANKED — THE TRUE TABLE) · **#250**, and in particular ⭐ **#250.4**, whose five
binding facts are restated in [§FACTS](#facts--the-250-4-facts-that-bind-this-exam) below ·
**#225.3(c)** (per-dose STOP granularity) · **#228.6** (the exam world must be percept-armed) ·
**#203** (per-arm ROWS, never verdicts) · **#181.2** (every HARD gate in-probe) · **#197-M1/#198**
(hashed body vs unhashed envelope) · **#163** (seed/stats disjointness) · **#20** (cluster = match
seed) · **#128** (wall is CONTEXT ONLY) · **#207** (checkpoint) · **#229.2** (no table typed that
the artifact does not carry — discharged by a committed generator).

The instruments this stage stands on: [`DLC-T1S-STRIKE-EXAM.md`](DLC-T1S-STRIKE-EXAM.md) —
inherited **whole** (world, rulers, guards, estimator, N rule, checkpoint, guard block, per-dose
STOP granularity, the delivered-rate publication), and that inheritance is **proved** rather than
asserted by ⭐⭐ **G-ANCHOR**, which re-walks DLC-T1s's committed **battery** rows **on its `plane`
arm**. The seam being dosed is [`DV-T0-DORMANT-SEAM.md`](DV-T0-DORMANT-SEAM.md), banked at
`c08f2eb`/`c1cafe7` (#250); the belief dose is
[`DV-C0-LOSS-COST-CENSUS.md`](DV-C0-LOSS-COST-CENSUS.md)'s **TRUE TABLE**, read at exam time from
its committed artifact.

> ⭐ **INSTRUMENT-ONLY ROUND.** `src/**` is **byte-untouched** (X-SRC-UNTOUCHED is a HARD gate).
> Every seam is already banked; the arms are built by three MatchConfig flags (`dlcStrikePlane`,
> `dvDeliveryValue`, `obmMovement`) plus three gene channels (the scalar `passLeadSupport`, the
> 16-weight OBM matrix, and ⭐⭐ the DV pair `dvExposureWeight` + `dvLossBelief`) written on all
> three genome views of both teams. No engine byte moves.

---

## §FACTS — the #250.4 facts that bind this exam

Restated in the frozen half, before any receipt, exactly as the ruling requires:

1. ⭐ **H-250a**: at T0-tested doses the DV price acted as a **LEVEL SUPPRESSANT, not a
   reorderer** — 0/64 target flips, with exposure = 1 suppressing 21 of 32 base strike-ticks and
   own-belief = 1 suppressing 5 of 32. ⇒ this exam **publishes per arm** the delivered rate (the
   #242 lesson: the upstream perceived-target guard DISCARDS plane choices in percept worlds), the
   base strike-tick **suppression/creation**, and a **flip-vs-suppress counterfactual AT THIS
   EXAM'S OWN DOSES**, in the T0 verifier's one-tick form (step to tick T zero-dosed, dose, take
   ONE tick, compare the struck pass).
2. ⚠ **NO COST PERCENTAGE IS QUOTED ANYWHERE.** The T0 cost floor is machine-specific (1.10 % on
   one machine, −3.36 % on another); the durable fact is the exact tick-equality of `zeroArmed` and
   `bornArmed`. This stage measures no cost at all; its tick-equality fact is the **byte-identity**
   `dvInert ≡ planeAnchor`.
3. ⚠⚠ **THE INDEX-AXIS FACT** (DV-T0 §HONESTY 8): the belief is read at the candidate's
   **RECEPTION** zone; DV-C0's hazards are indexed by the **LOSS (release)** point. The truth dose
   therefore writes **TRUE VALUES ONTO A SHIFTED INDEX**. The mechanism choice is RATIFIED
   (reception indexing prices *passing INTO danger*; release indexing would tax the own-third
   outlet ball itself — anti-purpose, H-169a). **Adequate for THIS exam's shape-capability
   question — own > middle > final either way — and NOT resolved here: DV-T2's drafting must
   resolve commensurability first.**
4. ⚠ **THE EXPOSURE READING SATURATES HIGH** (mean **0.8126** over 51,420 priced pairs in the
   percept world; a body covers ~7–9 m in the ~1.08 s mean flight against the corridor family's own
   4 m scale). ⇒ the ladder **includes 0 and stays far below 1**; §DOSES derives every rung.
5. **PROBE HYGIENE** (#250.3): machine timings ride the **UNHASHED envelope only**, so
   `resultSha256` re-derives on another machine; every grep-gate percentage form is **formatted
   before `String()`** (no floating-point-tail dead limbs); **G-DET** (the whole core run twice,
   byte-identical digests) is the portable anchor.

## §FORM

### The world — ONE percept-armed base world, IDENTICAL in every arm

```text
new Match({ seed, teamA, teamB, duration: 240, edsPerceivedChoice: true })
```

**DLC-T1s's world, exactly** — because the CONTRAST ANCHOR re-walks DLC-T1s's own `plane` arm, and
any other world would forfeit G-ANCHOR and with it the right to say the risk price is being
measured against ruling #244's own arm. ⚠ Declared, as inherited: `edsPerceivedChoice` also moves
the CARRIER onto the perceived-snapshot pass chooser, so absolute levels are not comparable across
exams; the paired contrast is clean because **all eight arms share this world exactly**.

### The arms — 8, paired on ONE shared seed list (#20: cluster = match seed)

| arm | plane door | DV price | OBM | the football sentence |
| --- | --- | --- | --- | --- |
| **ABSENT** | — | — | — | the CONTROL: the percept-armed world with every seat unreached |
| ⭐⭐ **planeAnchor** | `sp`, gene 1 | **none** | — | **THE CONTRAST ANCHOR = ruling #244's own PLANE arm**, re-walked on this stage's seeds (its published battery reading: supply +0.1365 pp resolved helpful, goals 1.7063 OUT of band, paired delta −0.4395 resolved DOWN) |
| ⭐ **dvInert** | `sp`, gene 1 | flag ON, **both genes at ZERO** | — | **THE ARMING IDENTITY AT EXAM GRAIN**: the seat exists, an exposure IS computed for every priced candidate, the subtraction IS performed, and `0·e + 0·v` is exactly `+0` ⇒ **byte-identical to planeAnchor per seed** (DV-T0's G-ZERO, re-measured not cited) |
| ⭐⭐ **dvTruth0** | `sp`, gene 1 | belief = **THE TRUE TABLE**, exposure **0** | — | **THE PRIMARY CELL**: the correct map and nothing else — the capability question in its cleanest form |
| **dvTruthP** | `sp`, gene 1 | belief = TRUE TABLE, exposure **0.01214** | — | rung 1: the exposure limb at PARITY with the map |
| **dvTruthG** | `sp`, gene 1 | belief = TRUE TABLE, exposure **0.054822** | — | rung 2: PARITY × the census's own own/final gradient |
| ⭐ **dvLoud** | `sp`, gene 1 | belief = **TRUE TABLE × 12.254902**, exposure 0.01214 | — | **THE SEPARATELY REGISTERED ABOVE-TRUTH ARM** (shape preserved, level raised) — INELIGIBLE for the truth-map JOINT |
| **dvTruthPxCas** | `sp`, gene 1 | belief = TRUE TABLE, exposure 0.01214 | CHECK-AND-SHOW | the relational pair (the DLC-T1s planeXCas form): the receiver comes short and asks AND the passer prices his kick |

**Why exactly these eight.** The contract's four families — control · the plane-alone anchor
re-walk · plane+DV at hand doses · plane+DV × CHECK-AND-SHOW — plus the two things the precedent
form requires and the contract does not forbid: the **arming-identity arm** (DLC-T1s's PLANE-INERT
in this seam's own idiom) and the **separately registered above-truth dose** that DV-T0 §HONESTY 5
demands be an arm with numbers rather than a quiet re-scale. ⭐ **Every treatment arm rides the
IDENTICAL banked plane at the IDENTICAL gene value**, so the only thing that varies across them is
the risk price.

## §DOSES — pre-registered, derived from published numbers, machine-checked (G-DOSE)

### The belief dose IS THE TRUE TABLE, exactly

The instrument opens `docs/world-model/data/dv-c0-loss-cost.json` at exam time, reads
`result.census.yardstick.zones[z].hazard` in the frozen `own · middle · final` order, and **writes
those three numbers into `dvLossBelief`** on all three genome views of both teams:

```text
dvLossBelief = [0.0816, 0.04209, 0.01807]     ← read, never typed
```

**INSTRUMENT → GENES, at exam time; never CODE → TABLE.** `src/**` has never heard of the census
(DV-T0's G-NOTABLE), and this exam **re-greps the whole tree on the exact values it is about to
write** (X-NOTABLE) rather than citing that gate.

### The exposure ladder — 0, and two rungs, each a quotient of published numbers

| rung | value | derivation (both inputs READ from committed artifacts in-probe) |
| --- | --- | --- |
| **0** | `0` | ⭐ THE MANDATORY ZERO RUNG (contract §3): the truth MAP ALONE |
| **PARITY** | **0.01214** | `truthDosedMeanRiskPrice / meanExposure` = **0.009865 / 0.8126** = 0.0121400443… ⇒ at this weight the exposure limb subtracts, ON AVERAGE, exactly what the truth map subtracts. The two limbs are equally loud and neither carries the other. Inputs: DV-T0's `reported.truthDosedSmoke.arms[1].meanRiskPrice` and `gates.gExposure.percept.meanExposure` |
| **PARITY × G** | **0.054822** | `G` = the census's OWN own/final hazard gradient = **0.0816 / 0.01807 = 4.515772** (ruling #249.2's 4.5× landscape). The loudest rung is the parity rung amplified by the WORLD'S OWN measured risk gradient — a number the census produced, not a taste factor. **5.5 % of the dose H-250a measured** |

### The above-truth belief arm, separately registered (DV-T0 §HONESTY 5)

`dvLoud` scales the WHOLE census vector by `1 / hazard(own)` = **12.254902** ⇒
`dvLossBelief = [1, 0.515809, 0.221446]`. That is the **loudest SHAPE-PRESERVING dose the gene
domain [0,1] admits**: the most dangerous zone sits at exactly 1 and the census's shape
(own : middle : final) is untouched. It answers *"is the truth dose simply too quiet to matter?"*,
it is labelled everywhere, and it **cannot satisfy the truth-map JOINT**.

**G-DOSE** proves all of it mechanically: the belief equals the artifact zone for zone; each frozen
rung equals its derivation to 5e-7; the ladder contains 0 and every rung is < 0.1; the loud vector
is one scalar factor on the whole census vector and inside the gene domain.

## §SUCCESS — the pre-registered JOINT, frozen verbatim

> **contract §3, verbatim:** supply **RETAINED** (resolved helpful) **AND** goals **RECOVER** (in
> band, or resolvedly toward it vs the plane-alone anchor). *The #244 failure inverted.*

Operationally, frozen with the probe's own predicates:

* **LIMB A — SUPPLY RETAINED.** (i) vs the CONTROL: `trueHoldableShare` paired-delta CI excludes
  zero in the **UP** direction (`resolvedHelpful` — the reading that made the plane's +0.1365 pp
  "resolved helpful" at #244); **AND** (ii) vs the **PLANE-ALONE ANCHOR**: the anchor-referenced
  paired CI is **not entirely below zero** — the price did not resolvedly KILL the plane's own
  gain. (ii) is what makes F-DV-b a real branch rather than a figure of speech.
* **LIMB B — GOALS RECOVER.** EITHER the `goals` band dimension is **INSIDE** the frozen band
  (baseline 2.3944, tolerance ±15 % ⇒ **[2.0352, 2.7536]**), **OR** `bandDistance` strictly
  decreases vs the anchor **AND** the anchor-referenced paired delta on `goalsPerMatch` is
  **RESOLVED in the band's direction** (a point-estimate drift is NOT recovery).
* ⭐ **JOINT = A AND B, at the SAME arm.** Neither limb alone: the anchor already HAS limb A — that
  is exactly why #244 hurt — and the CONTROL has limb B for free.
* ⭐ **THE ARC-LEVEL READING** (#225.3(c) per-dose granularity): the JOINT is computed at every arm;
  the arc-level flag reads **only the TRUTH-dosed ladder** (`dvTruth0` · `dvTruthP` · `dvTruthG`).
  `dvLoud` is above-truth and `dvTruthPxCas` is the relational pair — both published, neither able
  to carry the truth-map claim.

⚠ **THE BAND LIMB'S GRAIN**, inherited verbatim: the equilibrium band **GATES AT BATTERY N ONLY**,
and a dimension the ABSENT arm itself fails is EXCLUDED and DISCLOSED.

### Pre-named failure branches (contract §3)

* **F-DV-a** — *the map fix does not move the deflation*: limb B false while limb A holds ⇒ the
  blind spot is elsewhere; **the 乙 band census becomes mandatory**.
* **F-DV-b** — *it kills the supply gain*: limb A false ⇒ **overcorrection STOP**.
* **F-DV-c** — *a guard STOPs.*

⚠ The probe fires none of them (#203). ⭐ **A fired fork is still a commit.**

### ⭐ REPORTED headline (secondary, never a primary)

1. ⭐⭐ **THE DELIVERED RATE PER ARM**, both forms (the #242 lesson): the zero-pull **strike-time**
   share on the exam walks themselves, and the **LIVE-GRID-conditioned** decoded rate with its
   substitution split on the declared observational seed, with its lockstep twin.
2. ⭐⭐ **THE H-250a READING AT THIS EXAM'S OWN DOSES**: per dose, how many of the anchor's own base
   strike-ticks the price **SUPPRESSED**, how many strikes it **CREATED** at sampled non-strike
   ticks, and — where a strike survived — how often it **REORDERED the target (a FLIP)** versus
   merely moved the aim. ⭐ The instrument's own validity check is published beside it: the
   ZERO-dosed fork must reproduce the reference at every sampled tick.
3. The chosen-strike distribution over the nine grid members, the emergent led share, and the
   tier-2 #218 shares with CIs.

No gate and no success condition reads any of them.

## §GUARDS — every tolerance frozen ex ante (inherited verbatim)

| limb | direction | tolerance |
| --- | --- | --- |
| `interceptionsPerMatch` | ceiling | `NI_FRACTION · \|control\|` |
| `spreadYOut` | floor | `NI_FRACTION · \|control\|` |
| `spacingMedian` | floor | `NI_FRACTION · \|control\|` |
| `spacingUnder4` | ceiling | `NI_FRACTION · \|control\|` |
| offsides/match | the #157 **FLAG** form — a resolved increase returns to the commander, flips no gate |
| equilibrium band (goals/crosses/headers/longBalls/cutbacks) | **gates at battery N only**; ⭐ `goals` is *also* limb B of the primary, so its exclusion state is published with the primary |

`NI_FRACTION = 1 − 0.275/0.380` (PM-T1 §5). `breach = resolved AND beyondTolerance` — **evidence**
for F-DV-b/c, never the firing of it.

## §SEEDS — fresh, strictly above everything DV-T0 consumed, and SKIPPING its test block

| block | range | use |
| --- | --- | --- |
| **smoke** | **12,430,027 – 12,430,038** (12) | all 8 arms |
| **delivered-dose read** | **12,430,040** | the DECLARED observational block (it pulls percepts, so it may never touch exam data) |
| **strike read** | **12,430,045** | the member distribution + the substitution decode + its untraced LOCKSTEP TWIN |
| ⭐⭐ **H-250a counterfactual read** | **12,430,047** | the one-tick flip-vs-suppress forks |
| **exit-semantics guard** | **12,430,050 – 12,430,099** | any `DVT1_N`/`DVT1_SKIP_FP` override is routed here and adjudicates nothing |
| **battery** | **12,430,100 – (12,430,100 + N − 1)**, contiguous, cap **446** (⇒ ≤ 12,430,545) | the battery |
| ⚠ **DV-T0's test block** | **12,430,900 – 12,430,911** | ⭐ THE ORDERED SKIP — entered in this probe's consumed ledger so the skip is a MACHINE-CHECKED disjointness fact, not a promise |

⭐⭐ **AND THE ANCHOR ARM'S RE-WALK BLOCK: 12,428,100 – 12,428,107** — *inside* DLC-T1s's own
battery block, with the **INVERTED** predicate: it MUST collide with its source. Five further
re-walk blocks (O2-T1 · #173 · GGC · CTB-T1 · OBM-T1) ride unchanged.

Disjointness is computed **in-probe** against the COMPLETE consumed ledger, for every block this
stage touches, never asserted here.

**Stats stream**: base **106,200** (DLC-T1s consumed 105,800; DV-T0 drew none). A bootstrap IS
drawn here — the paired per-seed cluster bootstrap, 2,000 resamples — so the base is real and its
min gap to every published base is machine-checked (≥ 200 required).

## §NRULE — the DLC-T1s MDE form, recomputed for THIS stage

```text
m_iid  = (z.975 + z.80)² · (p0(1−p0) + p1(1−p1)) / (p1 − p0)²
DEFF   = MAX( inherited O2-T1 paired-delta DEFF , this stage's own smoke DEFF when it exists )
m_req  = DEFF · m_iid
N(q)   = ceil( m_req / momentsPerSeed )
N      = max_q N(q) , capped by the LEDGER ROOM and by the precedent cap (446 — DLC-T1s's own
         battery N, so the anchor's committed rows exist for every seed this stage could want)
```

The same-world DEFF source is this stage's **most-perturbed** arm (`dvTruthPxCas`). The cap is a
**CEILING, not a target**, and is FLAGGED when it binds (`capBinds` beside `nRaw`).

## §GATES — frozen ex ante, ALL computed in-probe (#181.2)

| gate | predicate | kind |
| --- | --- | --- |
| **X-DET** | the whole core (8 arms + 7 receipt walks + 8 dose reads + 8 strike reads × 2 matches + ⭐⭐ the H-250a counterfactual + summaries + bootstrap) runs **TWICE**, byte-identical digests | HARD |
| **X-FP-PROD** | the production fingerprint re-derives `57b0bdab…c673` in-probe | HARD |
| **X-SRC-UNTOUCHED** | `git diff --stat -- src` is EMPTY | HARD |
| ⭐⭐ **G-ANCHOR** | a re-walk of the committed DLC-T1s **battery** block's first rows **on its `plane` arm** reproduces **every published field exactly** — signature (rng stream state inside) AND the delivered-strike columns — and this exam's PLANE-ANCHOR arm is proved CONFIGURATION-IDENTICAL to it | HARD |
| ⭐⭐ **G-DOSE** | the BELIEF dose equals DV-C0's committed table zone for zone; each frozen exposure rung equals its published-number derivation to 5e-7; the ladder contains 0 and every rung < 0.1; the above-truth vector is one scalar factor on the census vector, inside the gene domain | HARD |
| ⭐⭐ **X-NOTABLE** | the #247 split re-greped AT EXAM TIME on the exact values being written: no file in `src/**` names the census artifact, its schema, or any zone hazard — as written or as a percentage **formatted** to 2 and 4 decimals | HARD |
| **G-REPRO-OBMT1 · -CTBT1 · -O2T1 · -173 · -GGC** | the inherited receipt walks, every field exact | HARD |
| **G-TRACE-SP · -PTP · -RADIUS** | the plane's and the projection's constants read from the shipped modules, the radius family parsed from source | HARD |
| **G-FORK-TOKENS-SP · -DLC · -PTP · (OBM)** | the banked read-fork inventories re-run; zero unclassified. ⭐ THIS STAGE'S DECLARED COMPLETION: two DV-era classes (`DV_RISK_PRICE`, `DV_SEAT_BODY`) for the risk pricer's own lines, with every frozen count asserted UNCHANGED and the risk-price call site pinned at exactly **ONE** | HARD |
| ⭐ **G-BLIND-WORLD** | the percept trunk is LIVE in every arm's constructed world; `allFeaturesZeroShare` published as an UPPER BOUND on blindness | HARD |
| ⭐ **G-ARM** | delivery on the axes an arm doses, silence on the ones it does not; the plane's MEMBERSHIP law re-derived through the shipped `groundStrikeGrid`; the gene on all six genome views wherever declared | HARD |
| **FLAG-HYGIENE** | ⭐⭐ `dvInert` ≡ `planeAnchor` **per seed** on the whole-match signature **and** every row field; `ctbSupportPlane` / `ptpPassLead` / `dlcDeliveryChoice` FALSE in every arm; the plane door and gene IDENTICAL in every treatment arm; ⭐ the DV two-limb arming checklist asserted per arm off the REAL constructed matches (flag + both genes at the DECLARED dose on all three genome views of both teams, read back through the shipped accessors); the truth arms carrying the census vector byte for byte | HARD |
| **SEED-DISJOINT** | every block vs the COMPLETE consumed ledger, each under its own predicate; the **six RE-WALKS** must land INSIDE their sources' consumed intervals; ⚠ **DV-T0's 12,430,900–911 entered in the ledger so the ordered skip is machine-checked**; battery clash-free | HARD |
| **STATS-DISJOINT** | base 106,200, min gap ≥ 200 against the complete published namespace | HARD |
| **G-CLEAN-INVOCATION** | any `DVT1_N` / `DVT1_SKIP_FP` override routes onto the guard block, reds this gate and exits 1 | HARD |
| **REPORTED (a)** | ⭐⭐ the DELIVERED RATE per arm, both forms, with the substitution decode and its lockstep twin | REPORTED |
| **REPORTED (b)** | ⭐⭐ the H-250a flip-vs-suppress counterfactual at this exam's doses, with its null-arm validity check | REPORTED |
| **REPORTED (c)** | the chosen-strike distribution, the emergent led share, the tier-2 #218 shares with CIs | REPORTED |

Checkpoint/resume is the #207 form (`DVT1_MODE` / `DVT1_RESUME` / `DVT1_CHECKPOINT`), resilience
only: the unit is the per-(pass, seed) set of 8 arm rows, nothing pooled is stored, a resumed run is
byte-identical to a fresh one, and the checkpoint is /tmp scratch read by no gate.

**Pre-named FAIL ⇒ STOP**: any HARD gate failing, any `src/**` diff, or any contradiction between
this document and the run — reported as-is, never re-cut after sight.

## §NON-CLAIMS

This stage claims **no** football effect and adjudicates **nothing**: it produces per-arm rows,
paired deltas and mechanical flags. It changes no engine byte; `src/**` is untouched and the
production fingerprint is unchanged. It does **not** give any team the census's map in code (#247):
the values travel INSTRUMENT → GENES at exam time and the code has never heard of the table. It
does **not** claim the risk law is RIGHT (the closing model is `topSpeed · t` and the map is three
cells — DV-T0 §HONESTY), does **not** resolve the index-axis question (§FACTS 3 — DV-T2's), prices
**no** loft, through ball, cross or cutback (DV-T0 §SEAM's scope note), does **not** claim the
ladder exhausts the dose space, and answers **nothing** about whether evolution FINDS the map
(DV-T2's). It quotes **no** cost percentage. It cannot authorize DV-T2 or any entry gate — only the
commander can (#203).

---

## §RESULT — the smoke (plumbing, NOT evidence)

12 seeds × 8 arms, block **12,430,027–12,430,038**, artifact
[`data/dv-t1-map-exam-smoke.json`](data/dv-t1-map-exam-smoke.json) — **24/24 gates PASS**,
`resultSha256` `524b3f10…ad61`, G-DET core digest `b20cd304…6d65` on both passes. It sized the
battery (the N rule's same-world p0 and moments-per-seed come from its ABSENT arm) and shook the
plumbing out; ⚠ **at 12 clusters it is evidence about nothing** — the band gates at battery N only
and the CIs are wider than anything they could detect. One receipt worth naming because it is the
new instrument's own validity check rather than a reading about football: the H-250a
counterfactual's **ZERO-dosed null arm reproduced the reference strike at all 40 sampled ticks**
(`nullArmReproducesReference: true`).

## §RESULT — the FULL BATTERY

*(every number below is quoted FROM the committed artifact
[`data/dv-t1-map-exam.json`](data/dv-t1-map-exam.json), recomputed by
`DVT1_MODE=full npx tsx scripts/probes/dv-t1-map-exam.ts`, and every table below was **GENERATED
PROGRAMMATICALLY** from that artifact by
[`scripts/analysis/dv-t1-battery-result.ts`](../../scripts/analysis/dv-t1-battery-result.ts) — the
generator is committed beside the doc, so the #229.2 rule is discharged in code rather than
promised in prose. No cell below was typed.)*

<!-- GENERATED: npx tsx scripts/analysis/dv-t1-battery-result.ts -->

### The run

* **`resultSha256`** `4d23f9b1c8043c18d8172eb821a763f8496abd0856fc9f2a8b0b153cf89e9883`
* **G-DET core digest** `d807bcd1da3d9885b5204020bc52a0b52f41d69db59cdc84dbee1bc48209cefd` (both passes)
* **283 seeds × 8 arms** (block 12430100..12430382), paired on one shared seed list, **plus** the seven receipt walks (O2-T1 · #173 · GGC · CTB-T1 · OBM-T1 × 2 arms · ⭐⭐ G-ANCHOR on DLC-T1s's own `plane` arm), the 8 delivered-dose reads, the 8 strike reads (each a traced match + its untraced LOCKSTEP TWIN) and ⭐⭐ the H-250a flip-vs-suppress counterfactual — and the whole core runs **twice** (G-DET).
* Verdict: **ALL 24 GATES PASS** (`allGatesPass: true`).
* Wall is CONTEXT ONLY (#128) and rides the UNHASHED envelope with the git head and the paths (#197-M1 / the #250.3 hygiene note), so `resultSha256` re-derives on another machine.

### Gate table

| gate | result | evidence (all recomputed in-probe, #181.2) |
| --- | --- | --- |
| `xDet` | ✅ PASS | two passes of the whole core, identical digests `d807bcd1da3d9885…` |
| `xFpProd` | ✅ PASS | observed `57b0bdab389122af5e4cacd75c4e13020b8ff248a413a7fcd71cc6215ba4c673` = baseline (seed 1337, 2 seasons) |
| `xSrcUntouched` | ✅ PASS | `git diff --stat -- src` **EMPTY** — no engine byte moved |
| `gReproObmT1` | ✅ PASS | 8 + 8 rows × 22 fields, 0 / 0 mismatches (ABSENT / CHECK-AND-SHOW) |
| `gAnchor` | ✅ PASS | block 12428100..12428107 against the committed DLC-T1s battery artifact (`82a7dd2a…`), arm `plane`: **8 rows × 27 fields, 0 mismatches** (of 446 committed rows available) — whole-match signature (rng stream state inside) AND the delivered-strike columns included |
| `gTracePtp` | ✅ PASS | ⭐ THE SECOND SEAM'S CONSTANTS, TRACED (PTP-T0 §LAW 1, the MARK_SAG_BALL_SPEED form): `18` is the THROUGH-BALL loop's OWN flight divisor and `1.6` is `runBurstPoint`'s OWN in-stride lead factor — the family that ALREADY p |
| `gTraceSp` | ✅ PASS | ⭐ THE PLANE's OWN CONSTANTS, TRACED: K = 9, the ±1 step set and the zero-point INDEX are read from the SHIPPED module's exports (never typed here), and the zero-point index is CHECKED to be the (direction 0, power 0) mem |
| `gForkTokensPtp` | ✅ PASS | 71 src occurrences, **0 unclassified**; AIM_APPLY sites 3 (frozen at 3, UNCHANGED) and ⭐ the risk-price call site pinned at **1** — this stage's declared DV-era completion (DV_RISK_PRICE · DV_SEAT_BODY) |
| `gForkTokensDlc` | ✅ PASS | 21 src occurrences, 0 unclassified |
| `gForkTokensSp` | ✅ PASS | 24 src occurrences, 0 unclassified — the plane's banked inventory unchanged |
| `gReproCtbT1` | ✅ PASS | 8 rows × 20 fields, 0 mismatches (signature included) |
| `gBlindWorld` | ✅ PASS | see the artifact |
| `gForkTokens` | ✅ PASS | 37 src occurrences, 0 unclassified — OBM-T1's own inventory |
| `gReproO2T1` | ✅ PASS | 12 rows, 0 mismatches |
| `gRepro173` | ✅ PASS | target {"pressedShare":0.7959,"pressed":1049,"unpressed":269,"all":1318} vs observed {"pressedShare":0.7959,"pressed":1049,"unpressed":269,"all":1318} |
| `gReproGgc` | ✅ PASS | 63 committed fields, 0 mismatches |
| `gTraceRadius` | ✅ PASS | the short-option instrument's radius family is PARSED out of src/ai/formations.ts (the #202 form: derived from source, never typed), so it cannot drift from the seat it is taken from. |
| `seedDisjoint` | ✅ PASS | every block machine-checked against the complete consumed ledger (65 entries, incl. DV-C0's and DV-T0's — ⚠ **DV-T0's ordered-skip block 12,430,900–911 among them**); the re-walks land INSIDE their sources under the inverted predicate |
| `statsDisjoint` | ✅ PASS | base **106200**, min gap **400** against 51 published bases |
| `flagHygiene` | ✅ PASS | 283/283 seeds ⭐⭐ **dvInert ≡ planeAnchor** — whole-match signature **and** every row field; the doors row: dial never armed true · contest never armed true · DV flag matches declaration true · DV genes as declared on all six views true · truth arms carry the census vector true |
| `gArm` | ✅ PASS | plane membership re-derived through the shipped `groundStrikeGrid`: planeUnmatched 0 / 0 / 0 / 0 / 0 / 0 / 0 / 0 |
| `gDose` | ✅ PASS | belief = the census table zone for zone ([0.0816,0.04209,0.01807], sha `c2ad2f27…`); PARITY frozen 0.01214 vs derived 0.012140044302239725; gradient rung frozen 0.054822 vs derived 0.05482167211194033; zero rung present true; every rung < 0.1 true; loud factor 12.254902 ⇒ [1,0.515809,0.221446] |
| `xNotable` | ✅ PASS | 139 `src/**` files scanned against 11 needles (the artifact name, the schema name, every zone hazard as written and as a formatted percentage) — **0 hits** |
| `gCleanInvocation` | ✅ PASS | any DVT1_N / DVT1_SKIP_FP override is BY DEFINITION not the exam: the run is routed onto the exit-semantics guard block, this gate goes RED and the process exits 1. |

### ⭐⭐ THE DOSES AS EXECUTED (G-DOSE)

| arm | `dvDeliveryValue` | `dvLossBelief` | `dvExposureWeight` |
| --- | --- | --- | --- |
| ABSENT (control) | off | — (absent) | — (absent) |
| ⭐⭐ PLANE-ANCHOR (#244's arm) | off | — (absent) | — (absent) |
| ⭐ DV-INERT (genes at zero) | ON | [0,0,0] | 0 |
| ⭐⭐ **DV-TRUTH-0** (the map alone) | ON | [0.0816,0.04209,0.01807] | 0 |
| DV-TRUTH-P (exposure PARITY) | ON | [0.0816,0.04209,0.01807] | 0.01214 |
| DV-TRUTH-G (PARITY × gradient) | ON | [0.0816,0.04209,0.01807] | 0.054822 |
| ⭐ DV-LOUD (above truth) | ON | [1,0.515809,0.221446] | 0.01214 |
| DV-TRUTH-P × CHECK-AND-SHOW | ON | [0.0816,0.04209,0.01807] | 0.01214 |

The belief dose is DV-C0's committed table, read at exam time (docs/world-model/data/dv-c0-loss-cost.json → result.census.yardstick.zones[z].hazard, in the frozen own·middle·final order). The exposure rungs are derived from published numbers: mean exposure **0.8126** and truth-dosed mean risk price **0.009865** give PARITY **0.01214**; the census's own own/final gradient **4.515771997786387** gives the loudest rung **0.054822**; the above-truth arm scales the whole census vector by **12.254902** = 1/hazard(own).

### ⭐⭐ THE DELIVERED RATE PER ARM — the treatment AS REALLY DELIVERED (#242, ordered by #250.4)

| arm | strike-time rate (BATTERY GRAIN, all seeds) | live-grid decoded rate (one declared seed) | substitution rate | lockstep |
| --- | ---: | ---: | ---: | --- |
| ABSENT (control) | 0 | n/a | 0.5283 | true |
| ⭐⭐ PLANE-ANCHOR (#244's arm) | 0.23041 | 0.39344 | 0.48889 | true |
| ⭐ DV-INERT (genes at zero) | 0.23041 | 0.39344 | 0.48889 | true |
| ⭐⭐ **DV-TRUTH-0** (the map alone) | 0.23509 | 0.5 | 0.41333 | true |
| DV-TRUTH-P (exposure PARITY) | 0.23208 | 0.5 | 0.47761 | true |
| DV-TRUTH-G (PARITY × gradient) | 0.23571 | 0.4375 | 0.5 | true |
| ⭐ DV-LOUD (above truth) | 0.23667 | 0.37838 | 0.55556 | true |
| DV-TRUTH-P × CHECK-AND-SHOW | 0.23448 | 0.44444 | 0.47778 | true |

⚠ **READ THE COLUMNS EXACTLY.** The STRIKE-TIME rate is `ledPassesNonZero / passesChosen` across ALL battery seeds with zero percept pulls — the rate the treatment was delivered at in the matches every ruler is computed on. The LIVE-GRID rate is the DLC-T1s decode on ONE declared observational seed, conditioned on decisions where the plane really had another kick to offer. ⚠ Neither is a rate for the RISK PRICE, which is delivered on every priced candidate by construction — they are the PLANE's delivery, published because a null must never be read as a strong treatment that failed when it was a treatment that reached the ball on a quarter of kicks.

### ⭐⭐ H-250a AT THIS EXAM'S OWN DOSES — the one-tick FLIP-vs-SUPPRESS counterfactual

Seed 12430047; the zero-dosed reference walk ran 14756 ticks and struck on **131** distinct ticks, of which **40** were sampled evenly across the match, plus **40** non-strike ticks for CREATION.

| dose state | exposure | belief | suppressed | created | **TARGET FLIPS** | aim-only changes | reproduced the reference |
| --- | ---: | --- | ---: | ---: | ---: | ---: | ---: |
| ⭐ DV-INERT (genes at zero) | 0 | ZERO | 0/40 | 0/40 | **0/40** | 0 | 40/40 |
| ⭐⭐ **DV-TRUTH-0** (the map alone) | 0 | TRUTH | 2/40 | 0/40 | **0/38** | 0 | 38/40 |
| DV-TRUTH-P (exposure PARITY) | 0.01214 | TRUTH | 2/40 | 0/40 | **0/38** | 0 | 38/40 |
| DV-TRUTH-G (PARITY × gradient) | 0.054822 | TRUTH | 4/40 | 0/40 | **0/36** | 1 | 35/40 |
| ⭐ DV-LOUD (above truth) | 0.01214 | LOUD | 6/40 | 0/40 | **0/34** | 1 | 33/40 |

⭐ **THE INSTRUMENT'S OWN VALIDITY CHECK: `nullArmReproducesReference` = true.** The ZERO-dosed fork must reproduce the reference strike at every sampled tick; a counterfactual whose null arm does not is measuring the fork, not the dose.

⚠ ⚠ H-250a AT T0 DOSES, for contrast only (ruling #250.3(iii), n = 64): 0/64 target flips; exposure = 1 suppressed 21/32 base strike-ticks and own-belief = 1 suppressed 5/32. Those doses are 18× to 82× this exam's loudest rung.

### ⭐⭐ THE PRE-REGISTERED JOINT — the #244 failure inverted (mechanical flags only, #203)

> ⭐⭐ THE PRE-REGISTERED JOINT (contract §3, VERBATIM): "supply RETAINED (resolved helpful) AND goals RECOVER (in band, or resolvedly toward it vs the plane-alone anchor)" — the #244 failure INVERTED. Frozen in this probe's own predicates before any receipt ran, computed at every arm, adjudicated at none.

| arm | supply Δ vs CONTROL | helpful | supply Δ vs ANCHOR | killed | **LIMB A** | goals/match | in band | bandDist (anchor 0.4769) | goals Δ vs ANCHOR | toward band resolved | **LIMB B** | **JOINT** |
| --- | --- | :---: | --- | :---: | :---: | ---: | :---: | ---: | --- | :---: | :---: | :---: |
| ⭐⭐ PLANE-ANCHOR (#244's arm) | 0.001344 [-0.000076, 0.002758] | no | n/a | no | no | 1.5583 | no | 0.4769 | n/a | no | no | ❌ false |
| ⭐ DV-INERT (genes at zero) | 0.001344 [-0.000076, 0.002758] | no | 0 [0, 0] | no | no | 1.5583 | no | 0.4769 | 0 [0, 0] | no | no | ❌ false |
| ⭐⭐ **DV-TRUTH-0** (the map alone) | 0.000962 [-0.000406, 0.002455] | no | -0.000382 [-0.001303, 0.000493] | no | no | 1.6042 | no | 0.431 | 0.045936 [-0.102473, 0.187279] | no | no | ❌ false |
| DV-TRUTH-P (exposure PARITY) | 0.001052 [-0.000311, 0.002375] | no | -0.000292 [-0.001487, 0.000859] | no | no | 1.8021 | no | 0.2331 | 0.243816 [0.045936, 0.420495] | **yes** | **yes** | ❌ false |
| DV-TRUTH-G (PARITY × gradient) | 0.000855 [-0.000488, 0.002145] | no | -0.000489 [-0.002004, 0.00089] | no | no | 1.7703 | no | 0.2649 | 0.212014 [0.010601, 0.420495] | **yes** | **yes** | ❌ false |
| ⭐ DV-LOUD (above truth) | 0.001076 [-0.000368, 0.002561] | no | -0.000269 [-0.001873, 0.001381] | no | no | 1.8304 | no | 0.2048 | 0.272085 [0.063604, 0.487633] | **yes** | **yes** | ❌ false |
| DV-TRUTH-P × CHECK-AND-SHOW | 0.001858 [0.000454, 0.003261] | **yes** | 0.000514 [-0.00112, 0.002117] | no | **yes** | 1.689 | no | 0.3462 | 0.130742 [-0.070671, 0.318021] | no | no | ❌ false |

* the frozen band on goals: **[2.0352, 2.7536]** (baseline 2.3944 ± 0.15); the CONTROL is itself in band: **true** (the band gates at battery N only, and a dimension the control fails is excluded and disclosed).
* ⭐ **JOINT at ANY truth-dosed ladder rung = false** · at EVERY rung = false. The ladder is ["dvTruth0","dvTruthP","dvTruthG"]; `dvLoud` is above-truth and INELIGIBLE, `dvTruthPxCas` is the relational pair.
* the PRIMARY CELL is `dvTruth0`: the TRUE MAP ALONE at exposure 0 — the contract's capability question in its cleanest form, with the ladder read beside it per dose (#225.3(c)) rather than pooled into it.

#### ⭐ WHAT THE ROWS SAY, STRICTLY AS THE FROZEN PREDICATES READ THEM (still no verdict — #203)

1. **LIMB B MOVED, AND THIS IS THE FIRST TIME IT HAS.** At every NON-ZERO rung the goals deflation recovers RESOLVEDLY toward the band against the plane-alone anchor — parity 0.243816 [0.045936, 0.420495], gradient 0.212014 [0.010601, 0.420495] — with band distance falling from the anchor's 0.4769 to 0.2331. No arm reaches INSIDE the band.
2. ⚠ **THE MAP ALONE (exposure 0) DOES NOT DO IT**: at `dvTruth0` the goals delta vs the anchor is 0.045936 [-0.102473, 0.187279] — unresolved. On these rows the limb that moves the deflation is the FLIGHT-EXPOSURE one, not the loss-cost belief at truth scale (which DV-T0 §HONESTY 5 said ex ante would be small: ~0.4–1.6 % of a pass's base value).
3. ⚠⚠ **LIMB A IS UNRESOLVED AT THE ANCHOR ITSELF** on these seeds: the plane-alone arm's supply delta vs the control is 0.001344 [-0.000076, 0.002758] — the CI touches zero. So the JOINT's limb A fails on every ladder rung for a reason that is present BEFORE the risk price: **no arm shows a resolved supply LOSS against the anchor** (`resolvedKilledVsAnchor` is false everywhere, and every anchor-referenced supply CI straddles zero). The mechanical `whichLimbFails` string therefore names the F-DV-b SHAPE while the anchor-referenced evidence does NOT show the overcorrection F-DV-b describes. That distinction is the commander's to adjudicate; it is printed rather than resolved here.
4. **THE INTERCEPTION GUARD FALLS MONOTONICALLY WITH DOSE** and breaches nothing: the control-referenced delta goes 6.565371 → 6.565371 → 6.328622 → 5.879859 → 5.106007 → 2.031802 across anchor → inert → truth-0 → parity → gradient → loud, every row inside the frozen tolerance.

⚠ ⚠ MECHANICAL PREDICATE FLAGS ON PUBLISHED CIs AND ON THE FROZEN BAND, exactly like `resolved` (#203). THIS PROBE ADJUDICATES NOTHING: at SMOKE GRAIN none of these rows is evidence, the band gates at battery N only, and F-DV-a/b/c are the commander's.

### THE GUARDS (tolerance = NI_FRACTION · |control level|, frozen ex ante)

| guard | direction | control | tolerance | ⭐⭐ PLANE-ANCHOR (#244's arm) | ⭐ DV-INERT (genes at zero) | ⭐⭐ **DV-TRUTH-0** (the map alone) | DV-TRUTH-P (exposure PARITY) | DV-TRUTH-G (PARITY × gradient) | ⭐ DV-LOUD (above truth) | DV-TRUTH-P × CHECK-AND-SHOW |
| --- | --- | ---: | ---: | --- | --- | --- | --- | --- | --- | --- |
| `interceptionsPerMatch` | ceiling | 27.088339 | ±7.484936 | 6.565371 (resolved) | 6.565371 (resolved) | 6.328622 (resolved) | 5.879859 (resolved) | 5.106007 (resolved) | 2.031802 (resolved) | 6.187279 (resolved) |
| `spreadYOut` | floor | 5.761296 | ±1.591937 | 0.260444 (resolved) | 0.260444 (resolved) | 0.281699 (resolved) | 0.279844 (resolved) | 0.252872 (resolved) | 0.196276 (resolved) | 0.27374 (resolved) |
| `spacingMedian` | floor | 13.166511 | ±3.638115 | 0.100675  | 0.100675  | 0.136407 (resolved) | 0.179897 (resolved) | 0.148002 (resolved) | 0.158761 (resolved) | 0.139656 (resolved) |
| `spacingUnder4` | ceiling | 0.089677 | ±0.024779 | -0.00758 (resolved) | -0.00758 (resolved) | -0.007559 (resolved) | -0.008429 (resolved) | -0.006262 (resolved) | -0.004308 (resolved) | -0.007859 (resolved) |

Offsides/match (the #157 FLAG form — a resolved increase returns to the commander and flips no gate): planeAnchor Δ 0.222615 resolvedIncrease=false · dvInert Δ 0.222615 resolvedIncrease=false · dvTruth0 Δ 0.325088 resolvedIncrease=true · dvTruthP Δ 0.074205 resolvedIncrease=false · dvTruthG Δ 0.151943 resolvedIncrease=false · dvLoud Δ -0.042403 resolvedIncrease=false · dvTruthPxCas Δ 0.077739 resolvedIncrease=false.

Equilibrium band — gated dimensions ["goals","crosses","headers","longBalls","cutbacks"] · EXCLUDED (the control itself out of band) []. All gated dimensions in band, per arm: absent true · planeAnchor false · dvInert false · dvTruth0 false · dvTruthP false · dvTruthG false · dvLoud false · dvTruthPxCas false.

### THE N RULE AS EXECUTED (in-probe, from the committed artifacts)

DEFF **0.8302** (MAX(inherited 0.8302, same-world smoke 0.8289) — the MOST-PERTURBED arm (dvTruthPxCas — the relational pair with the chooser free) paired-delta CI on ruler 2, this stage, 12 clusters — NOISY by construction and therefore used only through a MAX with the inherited DEFF.) — inherited 0.8302, same-world smoke 0.8289. q1 ⇒ N 283, q2 ⇒ N 53, binding **q1TrueHoldable**, nRaw 283, **N\* = 283**; ledger room 800 (binds=false), cap 446 (binds=**false**). Battery block **12430100..12430382**. `sourceOfP0`: THIS WORLD (this stage's OWN committed smoke, its ABSENT arm).

### The populations the CIs are built on

| arm | matches | eligible moments | TRUE-holdable | first receptions | possession ticks | goals |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| ABSENT (control) | 283 | 17476 | 97 | 11043 | 914101 | 614 |
| ⭐⭐ PLANE-ANCHOR (#244's arm) | 283 | 16680 | 115 | 12081 | 797069 | 441 |
| ⭐ DV-INERT (genes at zero) | 283 | 16680 | 115 | 12081 | 797069 | 441 |
| ⭐⭐ **DV-TRUTH-0** (the map alone) | 283 | 16737 | 109 | 12086 | 807385 | 454 |
| DV-TRUTH-P (exposure PARITY) | 283 | 16813 | 111 | 11959 | 807730 | 510 |
| DV-TRUTH-G (PARITY × gradient) | 283 | 16860 | 108 | 11893 | 799736 | 501 |
| ⭐ DV-LOUD (above truth) | 283 | 17205 | 114 | 11111 | 833444 | 518 |
| DV-TRUTH-P × CHECK-AND-SHOW | 283 | 16873 | 125 | 12051 | 811367 | 478 |

⚠ **THE INDEX-AXIS FACT STANDS UNRESOLVED, AS FROZEN** (§FACTS 3): the belief was dosed at the RECEPTION zone while the census indexed the LOSS (release) zone. This exam's question is the map's SHAPE-CAPABILITY, for which the dose is adequate either way; commensurability is DV-T2's to resolve first.
