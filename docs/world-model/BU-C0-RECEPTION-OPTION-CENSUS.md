# BU-C0 — THE RECEPTION-OPTION CENSUS (what does a body with the ball actually have?)

Status: **PRE-REGISTERED — everything above [§RESULT](#result) was written and COMMITTED BEFORE
THE BATTERY RAN** (#266.3(c), the freeze-commit canon). The measured numbers arrive only in
§RESULT, and every number there is quoted FROM the committed artifact
[`data/bu-c0-reception-option-census.json`](data/bu-c0-reception-option-census.json).

Authority chain: the **BUILD-UP CONTRACT**
[`BU-BUILDUP-CONTRACT.md`](BU-BUILDUP-CONTRACT.md) §3 (**BU-C0 — THE RECEPTION-OPTION CENSUS**),
bound by **#285.1** and dispatched by **#285.2**, under the user's play verdict **#274**
(「没有build up」 — disease 2 of record). Hygiene canon in full: **#163** (booked = walked) ·
**#181.2** (gates computed in-probe) · **#200** (no invented literal) · **#229.2** (nothing typed
that the artifact can print) · **#246** (the pre-registered direction) · **#256.2** (COMMENSURABILITY
— reuse the engine's own machinery, never a parallel oracle) · **#261.2** (env
WHITELIST-OR-REFUSE) · **#264.2 / #268.3** (the DEAD-PREDICATE class) · **#266.3(a)** (the hashed
body excludes ALL invocation context) · **#266.3(c)** (the freeze commit) · ⭐⭐ **#268.3(a)**
(LIVENESS BY MACHINE, EXACTLY-ONE ENFORCED) · **#270** (the dose never in `info.genome`) ·
**#272.3(ii) / #280.2(iii)** (clock honesty, APPLIED values) · **#273.3** (tree-clean gates compare
WORKTREE vs HEAD) · **#282.2(ii)** (⭐ standing artifacts store PER-CLUSTER ROWS — the `gFaces`
promise must be executable by a stranger) · **#283.2(iv)** (⭐ worker-simmed fixtures play the
SHIPPED world) · **#276.3 → #278.2(ii) → #279.2(i) → L3-T2 MED-4** (⭐ THE STANDING CITATION HUNT).

> ⭐ **INSTRUMENT-ONLY ROUND.** `src/**` is **BYTE-UNTOUCHED** — `xSrcUntouched` is a HARD gate and
> compares the **WORKTREE against HEAD** (#273.3) plus an untracked-file conjunct. **ZERO `src/**`
> edits were needed**: every function this census leans on is already exported
> (`evaluatePassAffordance`, `evaluatePassCorridorInterception`, `passChoiceCandidateGids`,
> `capturePerceptionTruth`, `oraclePerceptionSnapshot`, `Match.reachProfiles`, `poolT1DoseCells`,
> `armA4World`, `l3ArmedVersion`). No export-only change was requested and none was made.
>
> ⭐⭐ **NOTHING HERE IS GATED ON A FOOTBALL NUMBER.** This is a census. No claim is scored, no seam
> is armed, no fork in this document can fail on a measured rate. The contrast arm exists to say
> what the arming did to the picture, and no gate reads that difference.

---

### ⭐ §CORRECTIONS-READ — the standing hunt, discharged explicitly

Every prior-document number quoted below was read **after** opening that document's own corrections
section, and the checks are stated rather than implied:

* **R-乙 `R-YI-STANDING-GAP-TABLE.md` §FIX (the instrument corrections of record, #272.4(b)) AND
  §RESULT-2's ⭐⭐ SUPERSESSIONS OF RECORD — READ IN FULL.** Binding here: (i) the rows this stage
  reuses or cross-checks — **Q01 · Q05 · Q06 · Q07 · Q14** — are **NOT among the eight superseded
  lines** (the supersessions touch Q09 · Q10 · Q11 · Q13 · Q17 · Q18 · Q20 · Q21), so their
  semantics and their epoch-2 readings stand as published; (ii) ⭐⭐ **Q07's own semantics note is
  the reason instrument 2 exists**: *"`passesForward` is DEFINED by the engine as a pass played
  ≥2 m toward the opponents' goal, so its complement pools BACKWARD and LATERAL together.
  Backward vs lateral is NOT SEPARABLE with existing instrument semantics"* — this stage splits
  that pooled complement and says exactly how; (iii) the declared clock convention (#272.3→ (ii))
  is inherited: **1 sim-second = 22.5 display-seconds**, both terms extracted from `src/**` at run
  time, never typed.
* **L3-T2 `L3-T2-ARMED-WORLD-READ.md` §COMMANDER CORRECTIONS + ARC RULINGS (#282.2/#282.3) — READ
  IN FULL.** Binding here: (i) ⭐ **MED (ii), the CANON NOTE**: *"standing artifacts store
  per-cluster rows — the gFaces promise must be executable by a stranger"* — discharged here by
  `perSeedCells` (every published face re-derives from them, `gFaces`); (ii) the arm-C dose idiom
  and the `doseBook` form are inherited, but this stage takes **NO football number** from L3-T2;
  (iii) #282.3(1)'s registered surprise (the armed world is slightly **QUICKER**, not calmer) is
  the background against which the tempo-shaped cross-checks below are read.
* **L3-ENTRY-RUNG `L3-ENTRY-RUNG.md` §COMMANDER CORRECTIONS (#283.2) — READ IN FULL.** Binding
  here: (i) **#283.2(iv)** — worker-simmed fixtures play the SHIPPED world because
  `League.toJSON` omits `matchFlags`; this probe therefore **constructs every match directly**
  and **asserts the arming on the very match it walks** (a `gArms` conjunct, not a comment);
  (ii) the entry's pooled matured dose is a *presentation* of L3-T1's committed cells (#270 form),
  which is exactly the object this stage doses with — **through the shipped entry function
  `poolT1DoseCells` + `armA4World(m, null, 7, dose)`**, not a probe-local copy.
* **BU-BUILDUP-CONTRACT §0's diagnosis chain** is quoted only as the CONTRACT's own text (it cites
  #274.3); no number is lifted from it into a measurement here.
* ⚠ **THE #246 BAND IS THE PROGRAMME'S OWN PRE-REGISTRATION, NOT A CITED FOOTBALL STATISTIC.**
  "Real build-up teams keep 2–3 behind-ball options" is registered by ruling #246 and repeated by
  the contract; **no public source for it was located this round**, and it is used here exactly as
  a pre-registered DIRECTION, never as a band a measured number is scored against.

---

## §0 THE QUESTION, in football

⭐ **一个刚拿到球的人,身后到底有没有人可以回?** The contract's structural sentence is a claim about
the world: *"even an unpressed carrier has NOWHERE BACKWARD TO GO — the world cannot circulate
because the circulation options do not exist."* That sentence has never been measured. This stage
measures it — at the moment it matters (the touch), in the world the user actually plays
(`?a4world=7`), with the engine's own definition of "could that ball get to him".

Four photographs, one battery:

1. **BEHIND-BALL OPTIONS** — at every reception and every pressed-carrier moment, how many
   team-mates are behind the ball AND actually passable to.
2. **THE DIRECTION MIX** — of the passes that come off, how many go forward / sideways / back.
3. **THE TERMINAL CENSUS** — how possessions actually END.
4. **E7 AT WORLD GRAIN** — where the bodies are, relative to the ball, when someone receives it.

---

## §FORM

### The world — ⭐ THE POLISHED ARMED WORLD, and its bare contrast

| arm | construction | role |
| --- | --- | --- |
| ⭐ **`armed`** | `new Match({ seed, teamA, teamB, ...a4MatchFlags(7) })` + `armA4World(m, null, 7, poolT1DoseCells(L3-T1))` | **THE PRIMARY ARM** — the `?a4world=7` world exactly: the CB layer + `l3DefenceLearn` + `l3DefenceVeto`, books dosed with the SHIPPED entry's own **pooled matured cells**. The treated swarm is the honest background. |
| **`bare`** | `new Match({ seed, teamA, teamB })` | **THE CONTRAST** — bare production, SAME seeds, SAME instrument. **DESCRIPTIVE ONLY**: no gate reads the difference. |

Squads are redrawn per fixture (`team('A', seed·2+1)`, `team('B', seed·2+2)`) — the banked draw, so
the sampling population is the censuses' own. The clock is the **ENGINE DEFAULT** (`MATCH_DURATION`
240 sim-s, never overridden; `gClock` asserts it on every walk).

⭐⭐ **#283.2(iv), STATED AS THE DESIGN REQUIRES.** `League.toJSON` omits `matchFlags`, so a
fixture-routed arm would silently measure the SHIPPED world. Every match here is constructed
directly with its flags, and `gArms` asserts on **every walked match** that
`l3ArmedVersion(m) === 7`, that both L3 doors and the carry door are live, that the books carry the
dose cell-for-cell, and that **nothing of the dose is in `info.genome`** (house law #270).

### ⭐⭐ Instrument 1 — THE BEHIND-BALL OPTION, as a LADDER of engine verdicts

No rung is invented by this probe. Every one is the engine's own machinery answering its own
question (#256.2 commensurability):

| rung | what it asks | whose code answers |
| --- | --- | --- |
| **L1 POSITION** | is the team-mate BEHIND the ball line (toward our own goal)? `Δ = localX(mate) − localX(ball)`; **BEHIND** ⇔ `Δ ≤ −2 m`, **LATERAL** ⇔ `|Δ| < 2 m`, **AHEAD** ⇔ `Δ ≥ +2 m` | ⭐ the **±2 m band is Q07's own**, EXTRACTED at run time from the engine's forward-pass line in `src/sim/mechanics.ts` — never typed in the probe |
| **L2 THE BALL GETS THERE** | does the engine's own ground-pass prediction say the ball ARRIVES at all (its friction, its launch-speed law)? | `predictGroundPass` via `evaluatePassAffordance` (`flight.reachable`) |
| **L3 THE RECEIVER WINS THE RACE** | does the intended receiver reach the landing point before ANY opponent? `arrivalMargin > 0` | `evaluatePassAffordance` — a SIGN test on the engine's own quantity, no invented threshold |
| **L4 THE CORRIDOR IS NOT CUT** | can any opponent meet the ball ON ITS PATH? | `evaluatePassCorridorInterception` — `earliestFeasiblePoint !== null` ⇔ some defender's ETA beats the ball at some sampled corridor point (again a sign test, inside the engine's own sampler) |
| **L5 (reported beside)** | is the option inside the engine's OWN pass-choice window (6–30 m, keepers excluded)? | `passChoiceCandidateGids` — the window the live perceived chooser enumerates over |

⭐ **THE PUBLISHED "OPTION" IS L1 ∧ L2 ∧ L3 ∧ L4.** Every weaker rung is published beside it, so
the **drop-off itself is a measured face**: how many bodies are behind the ball, how many of those
the ball can even reach, how many win the race, how many survive the corridor. A census that
published only the loosest rung would have answered a different question, and the ladder is what
makes "which missing option binds first" arguable from arithmetic instead of taste.

The snapshot fed to both evaluators is `oraclePerceptionSnapshot` — the FULL-TRUTH snapshot the
engine itself ships *for offline oracle probes* ("never a live perception path"), built from
`capturePerceptionTruth(match)` with the match's own `reachProfiles()`. ⚠ **This is a CAPABILITY
census, not a perception census**: it asks *could the ball get there*, never *did the carrier see
him*. The perception half belongs to the INFO-DOCTRINE contract and is out of scope here (§4).

### The three populations

| population | definition |
| --- | --- |
| **reception** | every tick at which the ball's owner CHANGES while `phase === 'playing'` — #173's ownership-episode start. The census is taken **for the receiver**. |
| **pressed reception** | a reception whose receiver has an opponent inside the pressure radius (**4.2 m** — `TOUCH_CONTROL_DIST`, the #173/Q14 switch, imported not typed). |
| **pressed-carrier moment** | a NON-reception carrier tick, sampled every **12 ticks (0.2 sim-s)**, at which the carrier is pressed. ⚠ A declared SAMPLE, not every tick. |

### Instrument 2 — the direction mix, and exactly what is reused

**FORWARD is R-乙 Q07 VERBATIM**: the engine's own `team.stats.passesForward` counter, attributed
to an individual attempt by its **per-tick delta** at the tick the attempt is registered. Q07's own
semantics note (quoted in §CORRECTIONS-READ) records that the complement POOLS backward and
lateral. This stage splits that complement with **the same ±2 m rule on the same displacement**
(`localX(target) − localX(passer)`), measured on the positions of the tick **before** the strike,
and:

* the **FORWARD** share always uses the **ENGINE'S** verdict, never the probe's re-derivation;
* the probe's re-derivation is used **only inside the engine's pooled complement**, so a
  disagreement can only mis-file a pass **within** backward-vs-lateral, never into or out of
  forward;
* the **agreement rate** between the two is published (`q07Receipt.agreementShare`), so the size of
  that exposure is a measured number rather than an assurance.

A pass is **COMPLETED** when the engine's own `passesCompleted` fires and `lastCompletedPass` names
this attempt's passer. Attribution book-keeping is a gate (`gQ07`): every engine pass is either
attributed to an observed attempt or counted as UNATTRIBUTED, and neither the forward nor the
completion count may exceed the engine's own.

### Instrument 3 — the spell terminal census

The segmentation is **#173 / R-乙 Q01 VERBATIM** (ported from `scripts/probes/l3-t2-armed-world-read.ts`,
itself the #173 walk): a maximal same-team ownership interval while `phase === 'playing'`,
SUSPENDED (not ended) while the ball is loose in play, ended by an opponent establishing ownership /
the phase leaving `playing` / full time; `openPlay` origin is the headline population; #173's
6-tick foul lookahead is inherited unchanged.

⭐ **THE TERMINAL CLASS IS READ FROM THE ENGINE'S OWN EVENT TAPE, SPELL-SCOPED.** A tackle sets
`ball.owner = null` and the ball SQUIRTS — the spell does not end until an opponent establishes
ownership, which can be many ticks later. A fixed window around the terminating tick therefore
misses tackles entirely (**it did: the first build measured 0 tackles**, which is how the tape
design was found). The class is the **LATEST qualifying engine event inside the spell's own span** —
no invented window:

| terminator | class, from the tape |
| --- | --- |
| opponent establishes ownership | `tackled` / `intercepted` / `badTouch` (opp `tackles`, opp `interceptions`, own `miscontrols`) → else `lostOther` |
| ball leaves play | `shot` / `forcedLong` (own `shots`; own `clearances` or `longBalls`) → else `outOfPlay` |
| foul (#173 lookahead) | `foulWon` / `foulCommitted` |
| goal / full time | `goal` / `matchEnd` |

⚠ **THE CLASS IS THE LAST RECORDED EVENT**, so a bad touch that an opponent then intercepts files
as `intercepted`. That is a stated convention, not a claim about which caused which.

### Instrument 4 — E7 at world grain

The distribution of team-mate longitudinal offsets from the ball at reception moments: the share
behind / lateral / ahead, and the mean Δ. The E7 row ("support is ALWAYS ahead of the ball";
aheadBias 0.75/0.35) is a claim about a mechanism's SETTINGS; this measures where the bodies
actually end up.

---

## §NRULE — the sizing, frozen before the battery

**N = 300 seeds per arm, paired.** The binding precision term is the per-reception option count,
whose cluster is the MATCH (~150+ receptions per match ⇒ ~45,000 receptions per arm); 300 clusters
is the size at which the cluster-bootstrap CI on a per-reception mean is comfortably inside the
0–1 vs 2–3 distance the #246 pre-registration turns on. ⚠ No formal MDE is claimed: **nothing here
is scored**, so N buys descriptive precision, not power against a hypothesis. The wall cost was measured on
the smoke (≈0.2 s per walk ⇒ ≈4 minutes for 1,250 walks including the G-DET second run and the
non-perturbation controls) and never bound the choice.

Estimator: **cluster bootstrap over match seeds** (#20), **2,000 resamples**, percentile 95 % CI,
**ratio of sums**. One resample-index matrix is drawn once and shared by every face and both arms,
so the levels and the contrast are the same resampled worlds.

## §SEEDS — booked = walked

```text
block          12,486,000 – 12,486,999   (ruling #285.2)
core battery   12,486,100 – 12,486,399   300 seeds, WALKED BY BOTH ARMS (paired)
smoke          12,486,000 – 12,486,019
guard/preflight 12,486,040 – 12,486,059
world identity 12,486,900
stats stream   base 111,600 (floor from #285.2; step 200 from every published base)
```

## §GATES — the set, frozen ex ante (**16**)

| gate | what it proves |
| --- | --- |
| `gDet` | ⭐ G-DET: the whole core runs TWICE and re-derives bit-identically (same seeds, identical cells). |
| `xSrcUntouched` | `src/**` is byte-untouched — **WORKTREE vs HEAD** (#273.3), plus no untracked/staged src file. |
| `gArms` | ⭐⭐ #283.2(iv): every walked match carries its arm LIVE (`l3ArmedVersion === 7`, both doors, the carry door, the dose in the books, **nothing in `info.genome`**); the identity seed separates the two worlds; the arms walk the SAME seeds. |
| `gDose` | the dose is the committed L3-T1 exam's own pooled cells (declared SHA), non-empty, both arrival groups. |
| `gNonPerturbing` | ⭐ THE INSTRUMENT DOES NOT MOVE THE FOOTBALL: the same worlds re-walked with the oracle switched OFF end on the identical signature, spell count and pass count. |
| `gOracle` | the engine's evaluator was actually called; it answered; **both** race verdicts and **both** corridor verdicts occur (no constant predicate — the #264.2/#268.3 dead-predicate class); behind-ball bodies were seen; the ±2 m band still traces to `src/**`. |
| `gQ07` | the direction mix book-keeps against the engine's own counters: every engine pass attributed or counted unattributed; forward and completion counts never exceed the engine's own; attribution covers the overwhelming majority. |
| `gSpells` | every spell lands in EXACTLY one terminal class; the open-play population is non-empty; the class set is the frozen one. |
| `gNonVacuity` | ⭐ NO PUBLISHED RATE HAS A ZERO DENOMINATOR (a zero-denominator cell is published as UNMEASURED, never as 0); the option histogram sums to its own denominator. |
| `gFaces` | ⭐ #282.2(ii): every published face re-derives from the STORED PER-SEED CELLS alone — a stranger can redo every headline. |
| `gClock` | APPLIED, not nominal: the engine-default clock, the 90 read out of `Match.minute()`'s own expression, the mapping derived not typed, every walk actually stepped. |
| `gSeed` | booked = walked: no clash with the consumed ledger, no internal clash, every walked seed inside the claimed battery. |
| `gStats` | the stats base is the dispatched floor 111,600, ≥200 from every published base; the resample count is the frozen one. |
| `gEnvClean` | WHITELIST-OR-REFUSE: no rogue `BUC0_*`, no engine env door set, a preflight may never write a canonical path. |
| `gHashEnvelope` | ⭐ #266.3(a): the body re-derives its digest from disk, a cross-OUT with a DIFFERENT envelope has the IDENTICAL digest, and no invocation fact (`wallMs` / `generatedAt` / `head`) is inside the hashed body. |
| `gMutants` | ⭐⭐ #268.3(a): every conjunct of every gate owns a mutant, and flipping it (EXACTLY ONE at a time, every sibling unchanged) flips the outcome. An incomplete coverage map REFUSES THE RUN (exit 3). |

## §ENVELOPE — what `resultSha256` covers

`resultSha256` is taken over the RESULT BODY only: the frozen design, the run counts, the faces,
the histograms, the terminal census, the receipts, the per-seed cells, the seeds/stats ledgers, the
G-DET digests, the gates and the mutant map. **OUTSIDE it**: `generatedAt`, `head`, `outPath`,
`preflight`, `wallMs` — the whole `envelope` object. The same measurement written to another path
re-derives the same receipt byte for byte (`gHashEnvelope` proves both halves live).

## §PRE-REGISTRATION (#246) — written before the battery

* **THE DIRECTION**: real build-up teams keep **2–3** behind-ball options; **ours is expected
  ≈ 0–1**. ⭐ An **INVERSION** (ours ≥ 2 on the published rung) **routes to DIAGNOSIS, never to
  celebration** — the first suspect being the instrument itself (a rung too loose), the second
  being that the disease lives in USE rather than in EXISTENCE.
* **THE SECOND-ORDER EXPECTATION**, stated so it can be wrong: if options do not exist, the
  completed-pass mix should be **forward-heavy** and the terminal census should be dominated by
  **losing the ball to an opponent** rather than by shots or by the ball going out.
* **NO PREDICTION IS MADE about the armed-vs-bare difference.** The contrast is descriptive.

---

<a id="result"></a>

## §RESULT

**300 seeds × 2 paired arms · block 12,486,100–12,486,399 · 16/16 gates PASS · 56/56 mutants LIVE**,
`resultSha256` **`cc1530fd…e53e`**. Every number below is printed by
`scripts/analysis/bu-c0-reception-option-census-result.ts` from the committed artifact; none is
typed (#229.2).

### The run

```text
match clock       240 sim-seconds ⇔ 90′   (1 sim-second = 22.5000 display-seconds; both terms
                  extracted from src at run time — MATCH_DURATION, and the 90 out of Match.minute())
pressure radius   4.2 m   (TOUCH_CONTROL_DIST)
forward band      2 m     (EXTRACTED from the engine's own passesForward line, src/sim/mechanics.ts)
walks             600 measured + 50 NON-PERTURBATION controls + the whole core re-run for G-DET
receptions        armed 41,328 · bare 39,518        pressed receptions  armed 27,345 · bare 23,610
pressed-carrier   armed 64,282 · bare 57,401        open-play spells    armed 10,790 · bare 10,361
pass attempts     armed 27,552 · bare 29,529        oracle calls        1,263,228 (+4,653,542 corridor)
estimator         cluster bootstrap by match seed, 2,000 resamples, percentile 95 % CI,
                  ratio of sums; stats base 111,600
G-DET             the second run re-derived the core BIT-IDENTICALLY (a7d09d75…02f1)
dose              the shipped entry's pooled matured cells, 27,368 labels
                  [{lunges 23896, punished 18812}, {lunges 3472, punished 2867}]
```

### ⭐⭐ THE HEADLINE — the #246 pre-registration is CONFIRMED, not inverted

| face | ARMED (`?a4world=7`) | BARE | armed − bare |
|---|---|---|---|
| ⭐⭐ **behind-ball options per reception** | **0.7775** [0.7617, 0.7943] (n = 41,328) | 0.7638 [0.7475, 0.7801] | +0.0137 RESOLVED |
| … at PRESSED receptions | 0.8500 [0.8340, 0.8666] (n = 27,345) | 0.8693 [0.8522, 0.8869] | −0.0193 RESOLVED |
| … at PRESSED-CARRIER moments | 0.7062 [0.6893, 0.7241] (n = 64,282) | 0.6666 [0.6490, 0.6858] | +0.0395 RESOLVED |
| ⭐ receptions with **ZERO** behind-ball option | **0.4373** [0.4287, 0.4458] | 0.4415 [0.4328, 0.4502] | −0.0042 unresolved |
| receptions with **2 or more** (the #246 band) | **0.1812** [0.1741, 0.1888] | 0.1713 [0.1642, 0.1785] | +0.0099 RESOLVED |

**#246 stands as written**: the pre-registration said *real build-up teams keep 2–3, ours ≈ 0–1*.
Ours is **0.78 per reception**, and the distribution says it plainly — **43.7 % of receptions offer
NOTHING behind the ball**, 38.2 % offer exactly one, and only **18.1 %** reach the two-or-more band
real football is supposed to live in. The armed and bare pictures are the same picture.

The option histogram (share of receptions offering k options, k = 0…4):

```text
armed   all        0.4373 · 0.3815 · 0.1483 · 0.0323 · 0.0007      (n = 41,328)
armed   pressed    0.4075 · 0.3823 · 0.1639 · 0.0455 · 0.0008      (n = 27,345)
bare    all        0.4415 · 0.3872 · 0.1380 · 0.0327 · 0.0006      (n = 39,518)
bare    pressed    0.3998 · 0.3846 · 0.1628 · 0.0520 · 0.0008      (n = 23,610)
```

### ⭐⭐ THE DIAGNOSIS — it is the LANE, not the BODY

This is the row the stage exists for. The ladder, pooled over the armed arm's 41,328 receptions
(derived from the artifact's stored per-seed cells; the printer re-derives it):

```text
L1  behind the ball line        107,664 bodies
L2  the ball actually arrives    99,462   ( 92.38 % of L1 )      — 8,202 lost
L3  the receiver wins the race   90,633   ( 91.12 % of L2 )      — 8,829 lost
L4  the corridor is NOT cut      32,133   ( 35.45 % of L3 )      — ⭐ 58,500 lost
    end to end                             29.85 %
```

⭐⭐ **THE CORRIDOR RUNG OWNS 77.5 % OF THE ENTIRE LOSS** (58,500 of the 75,531 bodies that fail to
become options). Range owns 10.9 %, the receiver's race owns 11.7 %. Said in football: **the men
ARE behind the ball — 2.61 of them at every reception, 52.3 % of the team-mates on the pitch — and
the ball can reach them, and they would win the race. Somebody can step into the pass.**

| the E7 row, MEASURED at world grain | ARMED | BARE |
|---|---|---|
| team-mates BEHIND the ball line at a reception | **0.5230** [0.5182, 0.5277] | 0.4761 |
| team-mates AHEAD of the ball line | 0.3347 [0.3297, 0.3403] | 0.3913 |
| mean team-mate offset (+ = ahead) | **−5.41 m** [−5.58, −5.24] | −3.43 m |
| behind-ball bodies per reception | 2.6051 [2.5804, 2.6292] | 2.3734 |
| bodies-behind → option conversion | **0.2985** [0.2922, 0.3047] | 0.3218 |
| bodies-ahead → option conversion | 0.1184 [0.1146, 0.1224] | 0.1063 |

⚠ **E7's sentence needs re-reading, and this census is why.** The contract's map says *"support is
ALWAYS ahead of the ball"* — a claim about `supportSpot`'s `aheadBias`. At WORLD grain the bodies
are not ahead: the mean team-mate stands **5.41 m behind** the ball and the majority of the team is
behind the ball line at any reception. The E7 mechanism is real (the SUPPORTER the fan generates is
ahead-biased) but the WORLD is not short of behind-ball bodies — it is short of behind-ball bodies
**standing where the ball can survive the trip**. The conversion rate is the honest E7 face:
**29.85 % end-to-end**, and behind-ball bodies convert 2.5× better than bodies ahead (0.2985 vs
0.1184), so the geometry is not the villain either — the lane is.

Two more rows that change how the supply reads:

* ⭐ **54.20 % of the surviving behind-ball options ARE THE GOALKEEPER.** Outfield behind-ball
  supply is therefore **0.7775 × (1 − 0.5420) = 0.356 options per reception** — one outfield
  backward option every ~2.8 receptions.
* Only **35.22 %** of behind-ball options sit inside the engine's OWN 6–30 m pass-choice window
  (0.2738 per reception): most of what survives is either a keeper ball or outside the range the
  engine's own chooser would even enumerate.

### THE DIRECTION MIX — Q07 split for the first time

Q07's own note says its complement pools backward and lateral and that the split *"is NOT separable
with existing instrument semantics"*. It is separable now, and the receipts say the split is safe:
**attribution 99.97 %** of the engine's own passes, and the probe's ±2 m re-derivation agrees with
the engine's own `passesForward` verdict on **99.85 %** of attempts (a disagreement can only
mis-file inside the pooled complement, never into or out of forward).

| | ARMED | BARE |
|---|---|---|
| forward share of ATTEMPTS (Q07 verbatim) | 0.5818 [0.5746, 0.5889] | 0.5784 [0.5721, 0.5851] |
| backward share of attempts | 0.2833 [0.2780, 0.2887] | 0.3017 [0.2953, 0.3076] |
| lateral share of attempts | 0.1349 [0.1301, 0.1397] | 0.1198 [0.1155, 0.1241] |
| ⭐ forward share of COMPLETIONS | 0.5720 [0.5646, 0.5798] (n = 18,121) | 0.5588 [0.5521, 0.5662] |
| ⭐ backward share of COMPLETIONS | **0.2982** [0.2922, 0.3042] | 0.3252 [0.3181, 0.3317] |
| ⭐ lateral share of COMPLETIONS | 0.1298 [0.1242, 0.1352] | 0.1160 [0.1112, 0.1209] |
| completion rate (Q06 cross-check) | 0.6577 [0.6521, 0.6632] | 0.7336 [0.7275, 0.7394] |

⭐ **THE SECOND-ORDER PRE-REGISTRATION IS FALSIFIED, AND THAT IS THE ROUND'S MOST USEFUL SENTENCE.**
§PRE-REGISTRATION expected a forward-heavy mix if options did not exist. The mix is **not**
forward-heavy in the way that story needs: **29.8 % of completed passes go BACKWARD** and 13.0 %
lateral — 42.8 % of everything that comes off is a circulation ball. The world is **not refusing to
pass backward**. It plays the backward ball at roughly the rate real teams do, out of a supply of
0.78 options — and more than half of that supply is the keeper. ⚠ **The disease named in the
contract's §0 ("nowhere backward to go") is HALF TRUE and its usual reading is wrong**: the
carrier does go backward; what he does not have is a backward option that is neither the keeper nor
in a cut lane.

### THE TERMINAL CENSUS — how possessions actually end (open-play spells)

| class | ARMED (n = 10,790) | BARE (n = 10,361) |
|---|---|---|
| ⭐⭐ **intercepted** | **49.46 %** [48.25, 50.64] | 40.48 % |
| lost, unattributed | 13.82 % [13.09, 14.57] | 9.18 % |
| outOfPlay | 9.47 % [8.86, 10.08] | 8.97 % |
| foul won | 8.80 % [8.25, 9.39] | 6.07 % |
| shot | 8.14 % [7.54, 8.79] | 9.09 % |
| ⭐ tackled | 4.49 % [4.09, 4.90] | **19.01 %** |
| goal | 3.56 % [3.19, 3.96] | 3.94 % |
| forced long | 1.74 % [1.48, 2.02] | 2.56 % |
| bad touch | 0.52 % [0.40, 0.65] | 0.70 % |
| foul committed · match end | 0.00 % · 0.00 % | 0.00 % · 0.00 % |

⭐⭐ **HALF OF EVERY OPEN-PLAY POSSESSION IN THE POLISHED WORLD ENDS WITH A PASS CUT OUT** — 3.6×
the next-largest class. This is an INDEPENDENT instrument (engine stat deltas over #173 spells,
nothing to do with the affordance oracle) arriving at the same place as the ladder: **the lane is
where possession dies.**

The armed-minus-bare contrast is descriptive and is exactly the L3 arc's own story read from a new
angle: the veto took the reckless dive away (tackled 19.01 % → 4.49 %) and those possessions now end
**intercepted** (+8.98 pp) or **lost-unattributed** (+4.64 pp) instead — substitution, not
salvation, for the team in possession.

### THE R-乙 CROSS-CHECKS (definition-commensurable; ⚠ NOT the same arm)

| | this stage, ARMED (v7) | this stage, BARE | R-乙 epoch 2 `post-CB-polish` (v6 CB / bare) |
|---|---|---|---|
| Q01 spell mean (sim-s) | 4.0916 [3.9932, 4.1922] | 4.4690 [4.3677, 4.5695] | 4.1749 / 4.4145 |
| Q05 touches per spell | 2.5449 [2.5021, 2.5894] | 2.5215 [2.4699, 2.5704] | 2.5635 / 2.5526 |
| Q06 completion | 0.6577 [0.6521, 0.6632] | 0.7336 [0.7275, 0.7394] | 0.6602 / 0.7364 |
| Q07 forward share (attempts) | 0.5818 [0.5746, 0.5889] | 0.5784 [0.5721, 0.5851] | 0.5819 / 0.5784 |

⚠ **The armed arms are DIFFERENT WORLDS** (v7 here, v6 there) and the seeds are different, so this
is a **definition** check, not a replication. It is a strong one: Q07 reproduces to the fourth
decimal on both arms, and Q06/Q05 land within a few thousandths — the instruments are measuring the
same quantities the standing gap table measures.

| context row (armed / bare) | value |
|---|---|
| receptions per match (convention B; × 22.5 for convention A) | 137.76 [135.04, 140.50] / 131.73 |
| open-play spells per match | 35.97 [34.99, 36.95] / 34.54 |
| attributed pass attempts per match | 91.84 [89.97, 93.80] / 98.43 |
| pressed-reception share (⚠ ALL receptions, NOT Q14's first-of-spell population) | 0.6617 / 0.5974 |

### Gate table

| gate | result | evidence |
|---|---|---|
| `gDet` | **PASS** | the whole core ran twice and re-derived bit-identically (`a7d09d75…02f1`) |
| `xSrcUntouched` | **PASS** | `git diff -- src` and `git status --porcelain -- src` both empty (WORKTREE vs HEAD) |
| `gArms` | **PASS** | 600/600 walks carried their arm LIVE (`l3ArmedVersion === 7`, both doors, carry door, the dose cell-for-cell, nothing in `info.genome`); the identity seed separates the worlds; the arms walked the SAME 300 seeds |
| `gDose` | **PASS** | the committed exam's declared SHA, 27,368 labels, both arrival groups |
| `gNonPerturbing` | **PASS** | ⭐ 50/50 control walks ended on the IDENTICAL signature, spell count and pass count with the oracle off |
| `gOracle` | **PASS** | 1,263,228 affordance calls · 0 nulls · 4,653,542 corridor evaluations · both race verdicts and both corridor verdicts occur · the ±2 m band still traces to `src/**` |
| `gQ07` | **PASS** | attribution 99.97 % · forward and completion counts never exceed the engine's own · 39,794/39,794 completions attributed |
| `gSpells` | **PASS** | 30,752/30,752 spells classified, 21,151/21,151 open-play, the frozen class set |
| `gNonVacuity` | **PASS** | 0 zero-denominator cells across 90 published arm-cells · the histogram sums to its own denominator on 600/600 rows |
| `gFaces` | **PASS** | 90 re-derivations from the stored per-seed cells, 0 mismatches |
| `gClock` | **PASS** | 240 s applied, the 90 out of `Match.minute()`, the mapping derived, every walk stepped |
| `gSeed` | **PASS** | no clash with the consumed ledger, no internal clash, every walked seed inside the claimed battery |
| `gStats` | **PASS** | base 111,600 · min gap 200 · 2,000 resamples |
| `gEnvClean` | **PASS** | no rogue `BUC0_*`, no engine door set, the canonical-write guard live |
| `gHashEnvelope` | **PASS** | the body re-derived from disk; a cross-OUT with a DIFFERENT envelope has the IDENTICAL digest; no invocation fact inside the body |
| `gMutants` | **PASS** | ⭐⭐ **56 conjuncts, 56 mutants, 56 LIVE, 0 dead** — EXACTLY-ONE enforced (each flips its own conjunct and leaves every sibling unchanged) |

---

## §SLICE-ORDER RECOMMENDATION

⭐ **THE CENSUS PICKS THE ORDER BY ARITHMETIC, AND THE ARITHMETIC PICKS A DIFFERENT SEAM THAN THE
CONTRACT'S §0 STORY DOES.**

**Step 1 — where is the loss?** Of the 107,664 behind-ball bodies at armed receptions, 75,531 fail
to become options. **58,500 of them (77.5 %) die in the LANE**; 8,202 (10.9 %) die out of range;
8,829 (11.7 %) lose the race. Per reception: **1.42 options per reception are lost to the corridor
alone**, against a ceiling of 2.19 if the lane were never cut.

**Step 2 — is that one instrument's opinion?** No. The terminal census, built on the engine's own
stat deltas over #173 spells and sharing no code with the affordance oracle, says **49.46 % of
open-play possessions END intercepted** — the largest class by 3.6×.

**Step 3 — which banked seam acts on that rung?** Row by row, against the M-BU.1 inventory:

| seam | the rung it acts on | this census's arithmetic |
|---|---|---|
| **CTB** (check-to-ball plane) | **BODY** (L1) | ⛔ NOT the binding rung: 2.61 behind bodies already exist per reception and L1 is not where the loss is. ⭐ **This census EXPLAINS CTB-T1's failure** (#226, F-CTB-a): the plane delivered position totally (behindBall 0.09 % → 97.15 %) and moved no ruler while interceptions rose at every depth dose — bodies added to a rung that owns 22.5 % of the loss, into lanes that eat them. |
| **OBM** (off-ball eyes/timing) | **BODY, timed** (L1) | ⛔ same rung; #230's F-OBM-a already fired with zero resolved supply contrasts. |
| **MT keep/hold (#213)** | tempo / retention | ↔ acts on how long the ball is kept, not on whether the backward ball survives. A REPORTED-face lever, not the binding one. |
| **EK hold beliefs** | hold-vs-release pricing | ↔ #263/#264 closed with NO seam defect and the books faithful to their venue; nothing here says holds are the constraint. |
| ⭐ **DV learned risk maps (#259)** | **CHOICE among lanes** | ✅ the only banked seam whose object is the rung that binds. M-DV.3's price is `score′ = score − wExposure·exposure − belief[zone(aim)]·passBase`: the team learns from **its own losses** which deliveries get cut. Headroom is enormous and measured: 64.6 % of race-winning behind-ball balls are cut, and half of every possession ends intercepted. |
| **O1 / O2** (wind-up + outlet look) | perception | ⚠ UNRANKABLE BY THIS CENSUS — the oracle is full-truth, so this stage cannot see a perception rung at all. Stated, not scored. |
| **B9 lay-off** | a combination keyed to hold-up | ⛔ **DO NOT SCHEDULE**: R-甲 has it DEGENERATE — the bonus keys to `action.type === 'HoldUp'`, and hold-up is extinct (A10, 5/120,000). B9 is downstream of a capability that does not exist. |

**⭐ THE RECOMMENDATION: BU-T0 = ARM THE DV LEARNED RISK MAPS (#259) IN THE v7 COMPOSITION.**

1. It is the **only banked seam acting on the rung that owns 77.5 % of the loss**, and it acts on it
   the way M-BU.3 demands — by **arming knowledge the world earned**, never by nudging `passBackPen`.
2. It is corroborated by a **second, independent instrument** (the terminal census).
3. It needs **no new engine cut** (M-BU.1 satisfied without argument).
4. It **discharges the #269.2(iv) arming-lifecycle debt** in exactly the composition the arc has to
   prove anyway (the `clearTouchPastArming` staleness class, CB + L3 + DV together).
5. ⚠ **Its honest limit, stated before it runs**: DV prices the **destination ZONE**, not the
   **lane**. It can learn *"balls aimed into this zone get cut"* — a zone-level PROXY for a
   corridor-level fact. If BU-T0 measures the option supply and it does not move, the labelled
   hypothesis is already written: **zone-grain knowledge cannot price a lane-grain risk.**

**SECOND: MT keep/hold (#213)** — the user's own words (后场倒脚太多), and the tempo half of H-BU.2.
It is scheduled second because it moves REPORTED faces, not the binding rung.

**⭐⭐ THE HOLE THE CENSUS PROVES, ROUTED TO THE COMMANDER (a scope question, not a slice request).**
Nothing in the banked inventory changes the corridor's own **physics**. The corridor is decided by a
race between a defender's ETA and the ball's own travel time, and the ball has exactly one pace per
range: `launchSpeed = clamp(distance·0.6 + 8.2, 9, 22)` (`src/sim/mechanics.ts`) — R-甲's **B4,
DEGENERATE: the passer cannot choose to drill it** (the weight input exists in `performPass` and
every live caller omits it). A firmer ball shortens every corridor window in the same geometry, and
it is the only lever this census can see that acts on L4 without moving a single body. **M-BU.4
scopes it out by name** ("B4's pace axis = a named LATER capability slice, not this arc's"), so
this stage does not request it — it reports that the census's own arithmetic points there, and
leaves the scope line to the commander. ⭐ It is also the natural sibling of **M-CB.5, the weight
axis** (#284's 方向×力量 doctrine): the same missing control, one on the carry, one on the pass.

**A CEILING, STATED HONESTLY SO NOBODY OVER-SELLS THE ARC.** Even a perfect corridor lever tops out
at **2.19 behind-ball options per reception** (the L3 count) — and reaching the #246 band of 2–3
would require ~91–100 % lane survival, which no single lever buys. If the arc's exit is judged on
the #246 band, it will fail; if it is judged on **moving 0.78 toward the ceiling of 2.19 while the
completion and interception faces improve**, it is winnable.

---

## §DOUBTS

1. ⚠ **THE CORRIDOR RUNG COULD BE TOO STRICT, AND I TESTED THE OBVIOUS WAY IT WOULD BE.** L4 calls a
   lane cut if ANY opponent can meet the ball at ANY sampled point, including points a few
   centimetres from the passer — so a marker on the carrier's toes could in principle cut
   everything. If that dominated, survival would collapse under pressure. It does not: survival is
   **34.25 % at pressed receptions vs 39.03 % at unpressed ones** (derived from the stored cells;
   the gap is 4.8 pp, not a collapse). The rung is measuring lanes, not markers. It remains a SIGN
   test with no tolerance, so a defender who arrives level with the ball counts as cutting it — a
   real football judgement call that a future stage may want to widen into a margin distribution.
2. ⚠ **THE #246 BAND WAS NEVER SCALED TO 6v6.** Real teams have ten team-mates; ours have five. Our
   2.61 behind-ball BODIES out of 5 mates is not obviously worse than a real team's, and the band
   "2–3 options" may be a 11-a-side number being asked of a 6-a-side world. The band is a
   pre-registered DIRECTION (no source was located, see §CORRECTIONS-READ) and this doubt is why
   the recommendation argues from the LADDER rather than from the distance to the band.
3. ⚠ **CAPABILITY, NOT CHOICE, NOT PERCEPTION.** The oracle is full-truth: it says the ball COULD
   get there. It does not say the carrier saw the man (the INFO-DOCTRINE contract's rung) or that
   the chooser would pick him. A world where every option exists and none is seen would measure
   exactly like this one. ⭐ This is why O1/O2 are marked UNRANKABLE above rather than ranked low.
4. ⚠ **THE TERMINAL CLASS IS THE LAST RECORDED EVENT.** A bad touch that an opponent then intercepts
   files as `intercepted`, which is one reason `badTouch` reads 0.52 %. The 49.46 % interception
   figure is therefore an upper bound on "the pass was cut" and a lower bound on nothing — it
   absorbs some loose-ball scrambles that began as control errors.
5. ⚠ **`foulCommitted` and `matchEnd` read 0.0000 on both arms.** These are real zeros of the
   partition (an attacking team's own foul ends its spell by a route #173's lookahead attributes to
   the other side, and the final spell is a single row per match diluted by 10,790), not
   unmeasured cells — every denominator here is non-vacuous and `gNonVacuity` proves it. They are
   nonetheless the two rows a future terminal census should re-derive rather than trust.
6. ⚠ **THE PRESSED-CARRIER POPULATION IS A 0.2 s SAMPLE.** Its rate (0.7062) is consistent with the
   reception population, but a carrier who is pressed for a long time contributes many correlated
   samples; the cluster bootstrap is over MATCHES, so that correlation is inside the cluster and the
   CI is honest, but the population is not "moments" in any exchangeable sense.
7. ⚠ **THE KEEPER SHARE (54.20 %) IS AN ARTEFACT-SHAPED NUMBER I DID NOT PRE-REGISTER.** It changes
   the reading of the headline substantially (outfield supply 0.356), and it was found after the
   battery, so it is reported as a measured fact and NOT used as the primary argument for the slice
   order — the ladder is.
8. ⚠ **NO PERFORMANCE CLAIM IS MADE ABOUT THE INSTRUMENT'S COST** beyond `gNonPerturbing`: the
   oracle adds ~1.26 M affordance and ~4.65 M corridor evaluations per battery and is probe-side
   only, but the artifact deliberately carries no timing (#266.3(a)), so no per-walk cost is quoted.

## §NON-CLAIMS

1. ⭐ **INSTRUMENT-ONLY**: nothing here is gated on a football number, and no seam is armed or built.
2. The bare-production contrast is **DESCRIPTIVE** — no gate reads the armed-minus-bare difference.
3. The oracle answers **"could the engine's own machinery get the ball there"**, NOT "would the
   chooser pick it" and NOT "did the carrier see him" — capability, never choice, never perception
   (#200; the perception half is the INFO-DOCTRINE contract's).
4. The pressed-carrier population is a **SAMPLE at a declared cadence**, not every tick.
5. The slice-order recommendation is **ARITHMETIC over these rows**; the ORDER itself is the
   commander's to bind.
