# MT T1 — THE RULER RE-RUN (the PM-T1 compression exam, re-run with the MARK-SAG seam armed)

Status: **PRE-REGISTERED 2026-08-09. The battery has NOT been run.** Every arm, the seed
blocks, the N rule, every gate predicate with its numbers, every tolerance and its source
was frozen **before any full-N number existed**. The only numbers in this document are
(a) the SMOKE's, labelled `SMOKE — PLUMBING ONLY`, which adjudicate **nothing** and tune
**no** threshold, and (b) constants quoted from published, committed prior stages with
their citations. [§RESULT](#result--the-gates-run) is **empty by design**: the commander
launches the full battery detached per §LAUNCH and adjudicates it.

Authority chain: contract
[`MARK-TIGHTNESS-CONTRACT.md`](MARK-TIGHTNESS-CONTRACT.md) — §1 **H-MT** (the claim under
test) · §2 **M-MT.1–5** (the access-time account; the output is stance DISTANCE only; the
gene; what is NOT touched; the honesty limits) · §3 the **MT-T1** clause and the pre-named
**F-MT-a / F-MT-b / F-MT-c** · §4 the non-claims. Seams:
[`MT-T0-DORMANT-SEAM.md`](MT-T0-DORMANT-SEAM.md) (§LAW's three frozen constants, the
ARMING CHECKLIST, the ⚠ #202 emergent-assignment note) and
[`PM-T0-DORMANT-SEAM.md`](PM-T0-DORMANT-SEAM.md) (`k_PM = clamp01(gene)·0.25`, its own
arming checklist). ⭐ **THE RULER**:
[`PM-T1-COMPRESSION-EXAM.md`](PM-T1-COMPRESSION-EXAM.md) + its probe — this exam re-runs
that instrument set, and its ⚠ CORRECTION rows (#197-M1 head-outside-the-hash, #197-M2
unabridged transcripts, #197-L3 N provenance, #197-L4 the p50-as-mean substitution,
#197-L5 the split steer taxonomy and the pooled guard grain, #197-L6 the recomputed
counterfactual ask) all **bind this build**. Ruling **#202.4** (this dispatch), carrying
**#196.3-D4** (⚠ THE ARMING CHECKLISTS ARE BINDING), **#196.3-D6** (doses travel the REAL
gene channel via genome views — no engine-side dose surface), **#181.2** (receipts =
committed recomputable artifacts; no doc-typed hashes) and **#194/#196** (state each
gate's semantics exactly — say what the arms DIFFER in; completeness only from
`git show --stat`).

Probe: [`../../scripts/probes/mt-t1-ruler-rerun.ts`](../../scripts/probes/mt-t1-ruler-rerun.ts).
Artifacts: [`data/mt-t1-ruler-rerun-smoke.json`](data/mt-t1-ruler-rerun-smoke.json)
(committed, this round) and `data/mt-t1-ruler-rerun.json` (the commander's run).

---

## §1 The question, in the order it is asked

1. ⭐ **THE BODY** (the PRIMARY — the only thing this exam gates on): with the sag armed
   at the top dose, alone and alongside PM, do the **#199-null** body contrasts RESOLVE?
   `bodyLatGap` and `compressionShortfall` fall resolvedly; detachment does not rise.
2. **DOES THE SAG EVEN FIRE?** — the F-MT-a decidability instrument. A null body with a
   dead sag is a plumbing failure; a null body with a **firing** sag is a finding about
   the delivery geometry, and that is the fork the contract pre-named.
3. **WHAT IT COSTS**: marking's defensive function (the equilibrium band **including
   goals**) and the B1-a clump set (spreadY / spacing / dupRun), both at **inherited**
   tolerances.
4. **WHAT IT DRAGS WITH IT**: mark-assignment drift through the map §2.4 positional-
   feedback channel — the one #202.2 named explicitly for this seam. **REPORTED, never
   gated, never designed away.**

## §2 THE FROZEN DESIGN — the five arms

| arm | `pmLaneConvergence` | `defLaneConvergence` | `mtMarkSag` | `markSag` | what it is |
| --- | --- | --- | --- | --- | --- |
| **ABSENT** | off | **ABSENT** | off | **ABSENT** | ⭐ **THE CONTROL** — the production-shaped defensive path |
| **ARMEDZERO** | **on** | ABSENT | **on** | ABSENT | the **BORN-EQUIVALENCE** arm: both branches ENTERED, both weights 0 |
| **PMTOP** | **on** | **1** (`k_PM` 0.25) | off | ABSENT | the **INTERNAL REPLICATION of #199** (its D100 arm) — REPORTED |
| **MTTOP** | off | ABSENT | **on** | **1** (full frozen sag, cap 9 m) | the sag alone |
| **BOTHTOP** | **on** | **1** | **on** | **1** | both seams — the coupled arm the contract designs for |

**The arms differ in exactly what the table says**: two `MatchConfig` booleans and two
gene values. Nothing else moves. `src/**` is untouched (`xSrcZero`, HARD).

### §2.1 The dose channel — no new engine surface

Genes are written onto **all three genome views** of **both** teams (`info.genome` /
`baseGenome` / `effGenome`), the `a4World.ts` `armGenes` idiom — **#196.3-D6** honoured
literally (the PM-T1 §2.4 idiom, re-used unchanged).

⚠ **THE TWO ARMING CHECKLISTS (#196.3-D4), asserted by `gArm`:**
* **PM armed** = the `pmLaneConvergence` flag **+** the `evolveDefLaneConvergence` opt-in
  **+** a non-absent `defLaneConvergence` gene — **all three**.
* **MT armed** = the `mtMarkSag` flag **+** the `evolveMarkSag` opt-in **+** a non-absent
  `markSag` gene — **all three** (MT-T0 §SEAM, verbatim).

This exam is **FIXED-DOSE — no evolution runs** — so the two opt-ins are the channels the
genes *would* travel under selection rather than ones this exam exercises; `gArm` asserts
**both channels are LIVE** (`mutateGenome` writes each key with its own opt-in and leaves
it absent without), the PM-T0/MT-T0 `optInDraws` evidence form. **Stated, not implied.**
`gArm` additionally pins, per arm and per team, that `pmLaneConvergenceK(effGenome)` and
`markSagWeight(effGenome)` equal their frozen values **exactly**.

### §2.2 Both teams are dosed, symmetrically

**FROZEN: symmetric**, for PM-T1 §2.2's three reasons unchanged (the equilibrium counter
is in-engine; the guards are whole-world reads whose baselines are whole-world numbers;
it is the banked A/B form). Consequence, disclosed: every episode's defending team is
dosed, and this exam says nothing about the asymmetric case.

### §2.3 The world

`edsPerceivedDefence: true, edsPerceivedChoice: true` — the percept-armed substrate
PM-T0/PM-T1's receipts ran in. **No station eye** (`stationEye === null`, gated), so the
#188 send precedence reduces to the station field and the `eyeOverride` steer bucket is
empty **by construction**. Prod-shaped and A4-armed worlds are **not** measured (PM-T1
§2.3's boundary, inherited).

### §2.4 The seam constants are NOT re-cut

`k_PM ≤ 0.25` is PM-T0's traced legacy convergence weight. The sag family —
`MARK_SAG_BALL_SPEED = 16` (the engine's own pass flight-time constant,
`mechanics.ts:373`), `MARK_SAG_MAX = 9` (the zonal engagement radius,
`TeamBrain.ts:493`), and the `sagOf` shape (`min(slack·max(topSpeed,0.1), 9)`) — is
MT-T0's traced freeze. **MT-T0 pre-committed to not re-cutting any of them to make MT-T1
succeed, and this document does not.** If MT-T1 needs different numbers, that is a fork
for the commander **with numbers**.

## §3 SEEDS — a fresh block, disjointness proved in-probe

| item | block | status |
| --- | --- | --- |
| A4/O/PM/MT arc consumed through | …12,311,205 · 12,311,300–949 (PM-T1) · **12,312,000–025 + 12,312,900–901 (MT-T0)** | prior |
| **MT-T1 sizing smoke** | **12,313,000 – 12,313,005** (6 seeds × 5 arms) | **CONSUMED this round** |
| **MT-T1 exit-semantics sub-block** | **12,313,100 – 12,313,199** (only 12,313,100–101 stepped) | **CONSUMED this round** — see §6.5 |
| **MT-T1 battery (reserved, VIRGIN)** | **12,313,200 – 12,313,999** (N ≤ 800) | reserved; N = 800 ⇒ 12,313,200–999 |
| free above | 12,314,000 + | later stages |

Disjointness against **every** consumed block (fourteen of them, PM-T1's battery and
MT-T0's blocks included) is computed **in-probe** (`gates.gSeed`), never asserted here.
The three own sub-blocks are asserted mutually disjoint and ordered. ⚠ CORRECTION
(#203, verify finding 3): the first draft claimed an overridden full-mode run "can
never touch a battery seed, by construction" — TRUE only for `MTT1_N ≤ 100`: the
override is BASED on the exit-semantics sub-block (probe :845-847) but `RUN_N` is
unclamped, so a larger override overflows into the battery block and is caught **post
hoc** by `gSeed` (the run exits 1, invalid) — detected, not prevented. Operationally
the commander launches full mode with NO `MTT1_N` (any full-mode override is
`gNDerived`-RED anyway), so the battery block stays virgin (§6.5).

**Stats stream (a third namespace)**: bootstrap base **103,600**, `B = 2000`, cluster =
seed. PM-T1's base was 103,400 ⇒ the gap is exactly the #163 200 floor; disjointness from
all **nine** published bases is gated (`gates.gStats`).

## §4 N — DERIVED IN CODE, PRINTED BEFORE THE FIRST BATTERY MATCH

N is **not** a constant in the probe. The frozen rule (PM-T1 §4's, re-derived for five
arms and this stage's reserved band):

```text
σ_perSeed  = (halfWidth188 / 1.96) · √700          ← SMOKE-FREE, published variance
σ_delta    = √2 · σ_perSeed                        ← conservative (arms treated independent)
pairsNeeded= ceil( (1.96 · σ_delta / 0.5 m)² )     ← target CI half-width 0.5 m
N*         = min( ceil(pairsNeeded / pairYield) rounded UP to 25,
                  floor( 2.0 h / (ms_per_match × 5 arms × 2 X-DET) ),
                  800 )                            ← the reserved band's own ceiling
```

* **The variance term is smoke-free and unchanged**: #188's **published** prod-world CI
  ([FARSIDE-DEFENDER-FORENSIC §8.3](FARSIDE-DEFENDER-FORENSIC.md), weak-side back
  `sendLatGapMean` **p50 19.86 [19.65, 20.04] over 700 per-seed clusters** — the widest of
  its four published worlds). `halfWidth188 = 0.195` ⇒ `σ_perSeed = 2.632263 m`,
  `σ_delta = 3.722347 m`, **`pairsNeeded = 213`**.
* ⚠ **TWO SUBSTITUTIONS, DISCLOSED, NOT HIDDEN.** (a) The interval is a **MEDIAN's**, used
  as if it were a mean's — inherited from PM-T1 §4's own ⚠ CORRECTION (#197-L4); direction
  **conservative** (it over-sizes N). (b) PM-T1 **gated on the ASK**; this exam **gates on
  the BODY**, so a TARGET-metric variance is being used to size a BODY-metric gate.
  **Direction UNKNOWN.** The rule is not re-cut for it: the **achieved half-width of every
  body contrast is PUBLISHED** (`results.primary.achievedHalfWidthM`), so any shortfall is
  **measured and disclosed**, never assumed away (the #188 §8.0 precedent).
* **`pairYield` and `ms_per_match` are the ONLY two numbers read out of the smoke** (the
  #188 §4.3 form: *the smoke informs only N*). ⚠ **Disclosed difference from the ruler**:
  PM-T1 measured the yield on the ASK because the ask was its gated quantity; this exam
  measures it on the **BODY**, which is *this* exam's gated quantity. Same episodes, same
  pairing rule.
* **The committed smoke's two numbers**: `msPerMatch 105.733`, `pairYieldMin 0.1667`
  (the minimum over ARMEDZERO 0.6667 / PMTOP 0.5 / MTTOP 0.5 / **BOTHTOP 0.1667**).
* **N\* = 800.** `ceil(213 / 0.1667) = 1278 → 1300` (precision) · wall term **6,809** ·
  **cap 800**. ⚠ **THE CAP BINDS** (`bindingTerm = reservedBandCap`); projected wall
  **0.235 h**. **Stated honestly, and not re-cut**: the reserved band is a SEED-BUDGET,
  not a statistical statement, and with the precision term at 1,300 the battery may land
  **wider than the 0.5 m target half-width**. Two things follow and both are honoured:
  the shortfall (or its absence) is read off the **published achieved half-widths**, and
  the band is **not** widened after seeing the smoke's yield — widening it now would be
  letting a smoke number move a frozen budget, which §6 forbids. (Context, not a
  justification: the smoke's 0.1667 is one paired seed out of six at BOTHTOP; PM-T1's
  smoke read 0.3333 and its battery delivered ≈0.47. The estimate is noisy and
  pessimistic, so the cap is expected to cost less than the arithmetic implies — but
  **nothing in the rule moves on that expectation**.)
* ⚠ **Where N's inputs are actually read from (#197-L3, inherited verbatim).** The
  committed smoke artifact is
  [`data/mt-t1-ruler-rerun-smoke.json`](data/mt-t1-ruler-rerun-smoke.json), **file
  sha256 `633e336f8009c4aa3534da86958754916eea86f80abd94e1dfc7555868b29353`** (of the
  artifact BYTES as committed this round; distinct from its `resultSha256`, which covers
  the measured body only). Full mode reads the **WORKING-TREE** file at that path, not the
  committed blob, and records the sha256 of the bytes it actually read as
  `nDerivation.smokeArtifactSha256`. The provenance is therefore **discipline-enforced and
  sha-audited, not git-enforced**: compare that recorded value against the pin above at
  adjudication.
* `gates.gNDerived` fails **RED** if the N actually run is not the rule's output — an
  `MTT1_N` override in full mode turns the gate red rather than passing quietly.
  `MTT1_N` is accepted in **smoke mode only** (and, in full mode, forces the run onto the
  exit-semantics seed block; §6.5).

## §5 ⭐ THE FROZEN GATES — verbatim predicates, with their numbers

All contrasts are **paired per-seed deltas** (arm − ABSENT control) of the weak-side
back's per-seed mean, bootstrapped on the **seed** cluster (`B = 2000`, base 103,600, CI =
2.5/97.5 percentiles). ⚠ **Disclosed**: the arms diverge tick-for-tick, so pairing is on
the **seed**, not on the episode.

### §5.0 THE RULER IS INHERITED, NOT RE-IMPLEMENTED — and the differences are declared

Probe scripts in this repo execute on import (top-level side effects + `process.exit`), so
PM-T1's instrument code **cannot be imported**. It is therefore carried into this probe
**VERBATIM**, and a new HARD gate — **`gInherit`** — pins **nineteen defining lines** (⚠ CORRECTION #203: the first draft said eighteen; `gates.gInherit.rows.length === 19` in the committed artifact and the §5.0 enumeration itself lists 19) in
their **ORIGINAL** files (the MT-T0 `G-CONST` idiom): the trigger cuts, the episode floor,
`SPREAD_R`, `ASK_MATERIAL_M`, `NI_FRACTION`, the P3′ constants, both band rows, the three
gated-quantity lines (`bodyGap` / `shortfall` / `detach`), both `formationSpot` ask reads
(from `pm-t1-compression-exam.ts`), both stance-floor replica lines (from
`mt-t0-mark-sag.ts`), and — from `src/ai/actionExecutor.ts` — the `markingAggression`
stance-band line that anchors `SAG_MATERIAL_M` plus the MT-T0 seam line itself. **If the
ruler moves, this gate goes RED** rather than the two exams drifting apart silently.

⚠ **THE DECLARED DIFFERENCES FROM THE RULER** (all of them; also machine-readable in the
artifact at `inheritance.disclosedDifferences`):

1. **ARMS**: five flag/gene arms replace PM-T1's five-point PM dose ladder. Consequence:
   **there is no dose ladder**, so PM-T1's `P2a` monotonicity and `P2b` OLS-slope-on-`k`
   tests **do not exist here** and **no slope is computed anywhere**.
2. **THE GATED QUANTITY MOVES** from the ASK (PM-T1 §5.1) to the **BODY** (PM-T1 §5.2),
   because H-MT claims the body moves. The ask instruments ride **REPORTED**.
3. **THE CONTROL** is now **both flags OFF** (production-shaped). PM-T1's control was
   flag-ARMED with the gene absent; that world is this exam's **ARMEDZERO** arm, and
   `gCtrlEq` proves the two are byte-identical **through both live branches**.
4. **THE BAND** is evaluated on **every** arm and gated over the **body-moving** arms
   (F-MT-b); PM-T1 gated it on the top dose only. Baselines, tolerances and the
   substrate-drift exclusion are unchanged, **goals included**.
5. **GUARD-NI** is the same four limbs at the same fraction, quantified over the
   **body-moving** arms (F-MT-c) rather than the ask-moving doses (F-PM-b). The tolerance
   **scale** is re-derived on **this** run's control.
6. **`pairYield`** (an N input) is measured on the BODY (§4).
7. **NEW INSTRUMENT**: the SAG census (§5.2).
8. **At ABSENT / ARMEDZERO / MTTOP the ask instruments and the swallow share are
   DEGENERATE BY CONSTRUCTION** — `formationSpot`'s PM branch reads
   `pmLaneConvergenceK(gene)`, **not** the MatchConfig flag, and the gene is absent there
   ⇒ `askShiftY ≡ 0` ⇒ zero material ticks ⇒ NaN shares. **Not evidence about anything.**

Two of PM-T1's own disclosures ride unchanged and are restated so nothing is read wider
than it is: the **ask is a RECOMPUTED COUNTERFACTUAL STATION READ** (#197-L6), not a value
the executor consumed; and the steer taxonomy is PM-T1's **SPLIT** of #188's D2 mix
(`markStance` / `markFallback` separated), so it is **not bucket-comparable to #188's**
without re-merging (#197-L5b). The guard limbs likewise **pool both teams per match**
(#197-L5a), harmless here for the same reason: §2.2 doses both sides symmetrically.

### §5.1 ⭐ PRIMARY — the BODY (the whole of what this exam gates)

```text
For BOTH arms a ∈ {MTTOP, BOTHTOP}, paired per-seed vs ABSENT:
  B1  CI_upper( bodyLatGap( a − ABSENT ) )            <  0
  B2  CI_upper( compressionShortfall( a − ABSENT ) )  <  0
  B3  ¬ resolvedPositive( detachMean( a − ABSENT ) )      (CI_lower ≤ 0)

PRIMARY := (B1 ∧ B2 ∧ B3) at MTTOP  ∧  (B1 ∧ B2 ∧ B3) at BOTHTOP
```

`bodyLatGap` = `|p.pos.y − ball.pos.y|`, `compressionShortfall` =
`mean(max(0, |p.y − ball.y| − 9))`, `detachMean` = distance from the centroid of the REST
of his outfield team — **#188 §3.1–§3.3's definitions, inherited verbatim and pinned**.

**The numbers this must beat** (quoted from PM-T1 §RESULT / the committed
`pm-t1-compression-exam.json`, and from nowhere else): at D100 the body read
**−0.323646 [−1.146021, +0.473939]**, shortfall **−0.305054 [−0.969188, +0.416246]**,
detachment **+0.389479 [−0.304823, +1.106214]** — all three unresolved, at every dose.

⚠ `SPREAD_R = 9 m` is a **RULER, not a target** (#188 §3.3's own words). Passing B1/B2
says the weak-side body sits closer to the ball's lane; it does **not** say that is good
football. That verdict is the play-test's.

### §5.2 THE SAG CENSUS — the F-MT-a decidability instrument (NEW this stage)

MT-T0's `stanceCensus` quantities in **two layers**. ⚠ CORRECTION (#203, verify
finding 1): the first draft claimed BOTH layers run "at its own cadence (every 15th
tick)" — TRUE only of the match-wide layer (`tick % SAG_SAMPLE_EVERY === 0`, probe
:635); **the weak-side in-trigger layer — the layer the F-MT-a gate reads — samples
EVERY in-trigger tick** (probe :613, inside the trigger walk's per-tick observe, no
cadence guard). Same quantities, denser sampling on the gated layer; the artifact's
`inheritance.disclosedDifferences` text carries the superseded "cadence 15 verbatim"
wording in the smoke (and will in the full run) — THIS row is the correction of
record (the #198 precedent).

* **match-wide** — every marker on both teams (MT-T0's own layer + cadence, for
  continuity);
* ⭐ **weak-side back, in-trigger, every tick** — the very body the PRIMARY is built
  from. **This is the layer the gate reads.**

For each layer the probe recomputes, per tick, the unmodulated stance (`baseMarkDist`, the
MT-T0 **probe-side REPLICA** of the two stance-floor lines — disclosed exactly as MT-T0
Deviation 5 did, and pinned VERBATIM by `gInherit`) and the sagged one, and publishes
ticks / slack-positive / sagged>base / **tightened** / mean sag / max sag / mean stance
before → after.

```text
SAG-FIRED(arm) := saggedGtBase > 0
                ∧ tightened = 0
                ∧ meanSagOnSlackPositive ≥ SAG_MATERIAL_M = 1.4 m
```

⭐ **`SAG_MATERIAL_M = 1.4 m` is TRACED, not chosen** — and it must be, because unlike
`ASK_MATERIAL_M` **a gate predicate reads it**. `1.4` is the **full span of the stance
band the EXISTING tightness gene can express**: `src/ai/actionExecutor.ts:294` —
`let markDist = ball.owner === mark ? 2.6 : 2.6 - g.markingAggression * 1.4;` — so
`markingAggression` sweeping `[0,1]` moves the stance by exactly 1.4 m. "The sag fires
materially" therefore means: **the new axis moved the stance by at least as much as the
whole of the old one can.** `gInherit` pins that source line verbatim.

### §5.3 F-MT-a — the sag fires but the body does not ⇒ **STOP**

```text
F-MT-a := SAG-FIRED(MTTOP) ∧ SAG-FIRED(BOTHTOP)
        ∧ CI( bodyLatGap(MTTOP  − ABSENT) ) INCLUDES ZERO
        ∧ CI( bodyLatGap(BOTHTOP − ABSENT) ) INCLUDES ZERO
```

Disposition, by contract §3: **STOP — the stance geometry is the wrong delivery; the fork
returns to the user WITH the trace** (the sag census IS the trace).

### §5.4 F-MT-b — marking's defensive function breaks ⇒ **STOP**

The **equilibrium band**, inherited **verbatim** from
[A4-S2P3-GENE-BATTERY §4.2](A4-S2P3-GENE-BATTERY.md) via PM-T1 §5.5, **INCLUDING goals**,
together with its **declared substrate-drift caveat**:

```text
goals     2.3944 ±15 %      crosses 2.4894 ±25 %      headers   9.1039 ±25 %
longBalls 6.2042 ±25 %      cutbacks 3.8151 ±25 %

BAND(a) := arm a inside the band on every dimension the CONTROL arm itself holds
           (a dimension the CONTROL fails is DISCLOSED as substrate drift and EXCLUDED)

BODY-MOVING(a) := CI_upper( bodyLatGap(a − ABSENT) ) < 0

F-MT-b := ( ∃ a body-moving arm ) ∧ ( ∀ such arm: ¬BAND(arm) )
```

A band failure at *some but not all* body-moving arms is **not** F-MT-b; it is reported as
an arm-limited cost and returned to the commander. The band is computed and printed for
**every** arm regardless, so nothing is hidden by the quantifier.

### §5.5 F-MT-c — the clump re-imports ⇒ **STOP**

The PM-T1 §5.4 guard set, verbatim, with the S2 tolerance re-scaled on **this** run's
control:

```text
tol(limb)      = NI_FRACTION × | control-arm level of that limb, THIS run |
NI_FRACTION    = 1 − 0.275/0.380 = 0.2763          ← INHERITED, NOT INVENTED

GUARD-NI(a) := CI_lower( ΔspreadY_outOfPossession ) > −tol   ∧
               CI_lower( Δ spacingMedian )          > −tol   ∧
               CI_upper( Δ spacingUnder4 )          < +tol   ∧
               CI_upper( Δ dupRunShare )            < +tol

F-MT-c := ( ∃ a body-moving arm ) ∧ ( ∀ such arm: ¬GUARD-NI(arm) )
```

`0.2763` is [A4-S2P1-VECTOR-CENSUS §4](A4-S2P1-VECTOR-CENSUS.md)'s `fraction_box`
(#154's certified box price `0.380` and the CI bound nearest zero `0.275`). The **fraction**
is inherited; the **scale** is this run's control level. **Nothing is invented**, and an
UNRESOLVED (too-wide) CI **fails** the limb, per the S2 degeneracy clause. Implementations:
`spreadY` = [`stage3-p0-instruments.ts`](../../scripts/probes/stage3-p0-instruments.ts) §I7
split by possession (the **out-of-possession** face is gated); `spacingMedian` /
`spacingUnder4` / `dupRunShare` = the P3′ pair-distance and duplicate-run instruments
(`SAMPLE_EVERY 10`, `PAIR_SUBSAMPLE 6`, `CLOSE_PAIR_M 4`, `DUP_RUN_M 4`, arriver /
overlapper / corner-crash runners excluded).

### §5.6 GUARDS riding every battery — the #157 instrument debt

* **offside FLAG** — a resolved rise at any armed arm returns the axis to the **USER**; it
  **never flips PASS/FAIL** (the F-S2d form).
* **foul counter**, **penalties** and the **E4 combination counters** (`thirdMan`,
  `overlaps`, forward-pass share) — **REPORTED with CIs, never gated** (A4-S2P3 §4.3's
  binding "not a second bite" rule).

### §5.7 REPORTED — no gate anywhere

* **The swallow share** (PM-T1 §5.3's instrument at `ASK_MATERIAL_M = 1.0 m`, reported at
  0.5/1.0/2.0 m). ⭐ **By contract §3 this is REPORTED, not gated**: the body may compress
  **while `markStance` stays the steer owner** — *the gate is the body, not the label*.
  #199's reading was `markStance` 79.97–84.57 % at every dose. **Degenerate (NaN) at arms
  with no PM gene** (§5.0 difference 8).
* **Mark-assignment drift**: `markShare`, far-side mark share, `distToMark`, `markLatGap`,
  per arm, with CIs — the [MARK-SELECTION-CODE-MAP §2.4](MARK-SELECTION-CODE-MAP.md)
  positional-feedback channel, **named again by #202.2 for exactly this seam** (a 9 m
  sagged body moves the distances `assignMarks`' own 22 m / 9 m gates read, so assignment
  can churn **downstream of the body**). Visible and attributed, **not a predicate**, not
  designed away.
* **The ask instruments**: at PMTOP and BOTHTOP this is the **internal replication of
  #199's PRIMARY** (whose D100 ask fell **−4.705904 [−4.954743, −4.445391]**); at ABSENT /
  ARMEDZERO / MTTOP the ask is **unmodulated by construction** (§5.0 difference 8).
* **The detach / steer receipts**: per-arm episode counts, trigger-tick share, levels
  (mean/p50/p90) for the weak back **and** the ball-side control mirror, the D2 steer mix
  in PM-T1's SPLIT taxonomy, and the six worst-detachment episodes per arm with
  `watchHint` strings.
* **The sag census**, both layers (§5.2).

### §5.8 The X-family (HARD — failure ⇒ the measurement is invalid)

| gate | predicate / semantics |
| --- | --- |
| `xDet` | the whole core computed **twice**, canonical-JSON digests equal |
| `xFpProd` | the shipped fingerprint re-derived **in-probe** (seed 1337, 2 seasons) = `57b0bdab…c673` (#181.2) |
| `xSrcZero` | `git diff --stat -- src` empty — instrument-only |
| `gArm` | **BOTH** #196.3-D4 checklists per arm: the two flags exactly as frozen, each gene on all three views of both teams, `pmLaneConvergenceK` **and** `markSagWeight` exact, **both** evolution opt-in channels shown LIVE |
| `gInherit` | **⭐ the ruler is inherited, not re-implemented**: 19 defining lines pinned VERBATIM in their original files (§5.0; ⚠ #203: "18" was a miscount) |
| `gCtrlEq` | ARMEDZERO (**both** flags on, **both** genes ABSENT) ≡ ABSENT (both flags off), whole-match signature **including the rng stream**, first min(8, N) seeds. ⚠ **THE ARMS DIFFER IN CODE PATH** — armed ⇒ `pmMover` **and** `mtSag` true ⇒ both seam branches are ENTERED and both weights evaluate to 0 ⇒ the born-absent reads are proven inert **through the live branches** (PM-T0 G-BORN + MT-T0 G-BORN, jointly) |
| `gSeed` | block disjointness vs all **fourteen** consumed blocks, in-band, and the three own sub-blocks mutually disjoint + ordered |
| `gStats` | bootstrap base 103,600, gap ≥ 200 from all nine published bases |
| `gReadOnly` | `abandonRestDesignation` / `homeRegionGrant` / `homeMapGrant` null on every match **and** `stationEye === null` |
| `gNDerived` | the N run **is** the frozen §4 rule's output (RED on an `MTT1_N` override in full mode) |

**EXIT SEMANTICS** (the commander's monitor reads these):

```text
0 — X-family green, PRIMARY passes, no pre-named STOP fired
1 — an X-family HARD gate failed              ⇒ the MEASUREMENT is invalid
2 — the exam ran clean and a STOP fired       ⇒ the RESULT is a fork (by contract, the user's):
    PRIMARY fail  ·  F-MT-a  ·  F-MT-b  ·  F-MT-c
```

⚠ CORRECTION (#203, verify finding 4): F-MT-b/c use the contract's ∀-over-body-moving-
arms quantifier, so **exit 0's "with the guards quiet" verdict string can print while a
band or GUARD-NI limb is blown at ONE body-moving arm** (an arm-limited cost, §5.4). The
commander adjudicates from the PER-ARM rows in the artifact, never from the verdict
line.

## §6 FREEZE HONESTY

1. **The smoke adjudicates NOTHING.** It proves plumbing and publishes exactly two sizing
   numbers (ms/match, minimum paired-BODY yield) which feed **only N**. No level, share,
   rate, CI or verdict from it is a finding, and **it may not tune any threshold**. The
   artifact carries this as a machine-readable field (`smokeAdjudicatesNothing`).
2. **No re-cut after sight.** `k_PM ≤ 0.25` is PM-T0's traced constant; `16` / `9` / the
   `sagOf` shape are MT-T0's; `NI_FRACTION` is A4-S2P1 §4's; the band is A4-S2P3 §4.2's;
   `SAG_MATERIAL_M` is `markingAggression`'s own traced stance band. **None may be moved
   to make a limb pass** — and neither may the reserved seed band, even though the smoke's
   pessimistic yield makes the cap bind (§4).
3. **FAILs are reported as-is.** A fired F-MT-a / F-MT-b / F-MT-c is a **result**, not a
   defect to engineer around; a failing guard limb is published with its CI and its
   tolerance; the achieved half-widths are published whether or not they meet the target.
4. **Structural decidability changes made after the plumbing smoke and before any full-N
   sight, declared in the #188 §4.4 / PM-T1 §6.4 form** — both forced by structure, neither
   by a level: (a) the battery base moved from 12,313,100 to **12,313,200** and the cap
   from 900 to **800**, creating the exit-semantics sub-block so that proving
   `gNDerived`-RED / exit-1 can never touch a battery seed (§6.5); (b) an overridden
   full-mode run is **routed** onto that sub-block (⚠ #203: BASED there — an
   `MTT1_N > 100` override overflows into the battery block and is caught post hoc by
   `gSeed`, not prevented; see the §3 correction). **No gate, predicate,
   tolerance, threshold, arm or instrument definition was touched**, and neither change
   was informed by any measured level.
5. **MT-T1 ships nothing.** Road B: `src/**` untouched, both flags absent from every
   bundle and play-test world, both genes born absent, the production fingerprint
   re-derived unchanged.

### §6.5 The exit-semantics sub-block, stated exactly

Proving that a full-mode `MTT1_N` override turns `gNDerived` **RED** and exits **1**
requires stepping full-mode matches. Rather than burn battery seeds on a discarded
plumbing run, the probe routes any overridden full-mode run onto **12,313,100–199**; that
check consumed **12,313,100–101** this round and its artifact was written to `/tmp` and
discarded. **The battery block 12,313,200–999 is therefore VIRGIN.**

## §7 WHAT THIS EXAM DOES NOT DO

* It makes **no oscillation claim** — `switchKey` is retracted (#188 §8.4 / map §5 trap 7)
  and this exam builds no replacement.
* It does not touch mark **selection**: drift is measured and reported, never gated, never
  fixed. **There is no decline/release predicate anywhere** (#200's absolute red line;
  MT-T0's verify pass confirmed the complete conditional set).
* It does not measure prod-shaped or A4-armed worlds, the asymmetric dose, the attacking
  phase, the carrier-state term (M-MT.5's disclosed gap), or the coach layer.
* It does not test a **dose curve**: both seams sit at their top dose only, so nothing here
  says how the sag behaves at intermediate weights.
* It does not decide whether the compression is **good football**, and it cannot authorize
  MT-T2 or the play-test. Nothing reaches the user without the commander's ruling.

---

## §SMOKE — PLUMBING ONLY (adjudicates NOTHING)

6 seeds @ 12,313,000–005 × 5 arms, `MTT1_MODE=smoke`, wall **17.2 s**, `105.7 ms/match`.
Artifact [`data/mt-t1-ruler-rerun-smoke.json`](data/mt-t1-ruler-rerun-smoke.json),
`resultSha256` **`538bccf06e6a4f110ba9d8622ad074fd301c1ba41b01886c8dd527c472fa9172`**,
artifact-bytes sha256
**`633e336f8009c4aa3534da86958754916eea86f80abd94e1dfc7555868b29353`** — both quoted from
**this round's committed artifact** and nowhere else (the #194-M1 lesson). Per #197-M1 the
hashed body is **commit-free and timing-free** (`headContextOnly` / `wallContextOnly` ride
the envelope, outside the hash), so
`MTT1_MODE=smoke npx tsx scripts/probes/mt-t1-ruler-rerun.ts` re-derives `538bccf0…9172`
**at any commit**; the artifact FILE bytes still move run to run, which is why the two
shas are distinct and both are pinned.

**What this section is**: proof that the five arms construct, the instruments produce
numbers, the artifact writes and the exit codes work. **What it is not**: evidence about
anything. At 6 seeds the paired contrasts run on **n = 1–4 seeds**; every number below is
noise with a decimal point.

⚠ **The transcript below is the COMPLETE stdout of the committed run, copied whole**
(the #197-M2 lesson): all four PRIMARY rows, both per-arm verdict rows, all ten sag-census
rows, all five BAND rows **including the four FAILs**, all four GUARD-NI rows **including
the three FAILs**, every REPORTED row and the full gate/verdict tail. **Nothing is
elided.** The `F-MT-b` and `F-MT-c` "firings", the band failures and the `dupRunShare`
blow-outs adjudicate **nothing** — they are n = 1–6 plumbing — but they are the smoke's
output and they are published.

```text
=== MT-T1 RULER RE-RUN (smoke) — HEAD b4e129e — 6 seeds × 5 arms, block 12313000..12313005 ===
arms ABSENT[pm off/absent · mt off/absent]  ARMEDZERO[pm on/absent · mt on/absent]  PMTOP[pm on/1 · mt off/absent]  MTTOP[pm off/absent · mt on/1]  BOTHTOP[pm on/1 · mt on/1]
episodes/arm ABSENT:7  ARMEDZERO:7  PMTOP:4  MTTOP:4  BOTHTOP:3

PRIMARY — THE BODY (paired per-seed vs ABSENT; the #199 nulls are what this must beat):
  ARMEDZERO body +0 [0, 0] n=4 · shortfall +0 [0, 0] n=4 · detach +0 [0, 0] n=4
  PMTOP     body +2.616481 [-1.338122, 8.646565] n=3 · shortfall -0.259112 [-2.380848, 1.062515] n=3 · detach +4.25402 [-4.171, 10.868526] n=3
  MTTOP     body +4.174341 [-12.835523, 17.349277] n=3 · shortfall +2.087973 [-11.502471, 9.757122] n=3 · detach +0.223388 [-8.039067, 9.566257] n=3
  BOTHTOP   body -5.149401 [-5.149401, -5.149401] n=1 ✔ · shortfall -5.149401 [-5.149401, -5.149401] n=1 ✔ · detach -2.111414 [-2.111414, -2.111414] n=1 ✔
  MTTOP     bodyFalls false · shortfallFalls false · detachNotUp true ⇒ FAIL
  BOTHTOP   bodyFalls true · shortfallFalls true · detachNotUp true ⇒ PASS
  ⇒ PRIMARY FAIL   (achieved body half-widths: ARMEDZERO ±0 · PMTOP ±4.992344 · MTTOP ±15.0924 · BOTHTOP ±0)

SAG CENSUS (weak-side back, in-trigger ticks — the F-MT-a decidability limb):
  ABSENT    ticks 1804 · slack+ 1360 · sagged>base 0 · tightened 0 · meanSag(slack+) 6.189774 m · max 9 m · stance 1.898298 → 1.898298 m ⇒ SAG-FIRED false
  ARMEDZERO ticks 1804 · slack+ 1360 · sagged>base 0 · tightened 0 · meanSag(slack+) 6.189774 m · max 9 m · stance 1.898298 → 1.898298 m ⇒ SAG-FIRED false
  PMTOP     ticks 577 · slack+ 397 · sagged>base 0 · tightened 0 · meanSag(slack+) 5.714264 m · max 9 m · stance 1.988142 → 1.988142 m ⇒ SAG-FIRED false
  MTTOP     ticks 461 · slack+ 338 · sagged>base 338 · tightened 0 · meanSag(slack+) 6.71858 m · max 9 m · stance 2.0217 → 6.947687 m ⇒ SAG-FIRED true
  BOTHTOP   ticks 458 · slack+ 354 · sagged>base 354 · tightened 0 · meanSag(slack+) 6.456606 m · max 9 m · stance 2.052287 → 7.042764 m ⇒ SAG-FIRED true
  ABSENT    [match-wide] ticks 17096 · slack+ 11487 · sagged>base 0 · tightened 0 · meanSag(slack+) 4.918496 m · max 9 m
  ARMEDZERO [match-wide] ticks 17096 · slack+ 11487 · sagged>base 0 · tightened 0 · meanSag(slack+) 4.918496 m · max 9 m
  PMTOP     [match-wide] ticks 16423 · slack+ 10937 · sagged>base 0 · tightened 0 · meanSag(slack+) 4.912986 m · max 9 m
  MTTOP     [match-wide] ticks 15989 · slack+ 9865 · sagged>base 9865 · tightened 0 · meanSag(slack+) 3.751043 m · max 9 m
  BOTHTOP   [match-wide] ticks 16038 · slack+ 10170 · sagged>base 10170 · tightened 0 · meanSag(slack+) 3.931312 m · max 9 m

F-MT-a not fired (sag fires at MTTOP+BOTHTOP ∧ body CI includes zero at both)
F-MT-b *** FIRED *** (body-moving arms: BOTHTOP)
BAND — gated crosses,headers,longBalls,cutbacks · excluded as substrate drift goals
  BAND ABSENT    PASS: goals 1.5 [2.03524, 2.75356] OUT · crosses 2 [1.86705, 3.11175] ok · headers 8.5 [6.827925, 11.379875] ok · longBalls 7.666667 [4.65315, 7.75525] ok · cutbacks 4.5 [2.861325, 4.768875] ok
  BAND ARMEDZERO PASS: goals 1.5 [2.03524, 2.75356] OUT · crosses 2 [1.86705, 3.11175] ok · headers 8.5 [6.827925, 11.379875] ok · longBalls 7.666667 [4.65315, 7.75525] ok · cutbacks 4.5 [2.861325, 4.768875] ok
  BAND PMTOP     FAIL: goals 3.666667 [2.03524, 2.75356] OUT · crosses 1.833333 [1.86705, 3.11175] OUT · headers 7.666667 [6.827925, 11.379875] ok · longBalls 5.666667 [4.65315, 7.75525] ok · cutbacks 3.5 [2.861325, 4.768875] ok
  BAND MTTOP     FAIL: goals 1.166667 [2.03524, 2.75356] OUT · crosses 1.5 [1.86705, 3.11175] OUT · headers 4.5 [6.827925, 11.379875] OUT · longBalls 3.666667 [4.65315, 7.75525] OUT · cutbacks 3.166667 [2.861325, 4.768875] ok
  BAND BOTHTOP   FAIL: goals 1.666667 [2.03524, 2.75356] OUT · crosses 1.5 [1.86705, 3.11175] OUT · headers 5 [6.827925, 11.379875] OUT · longBalls 4.5 [4.65315, 7.75525] OUT · cutbacks 3.166667 [2.861325, 4.768875] ok

F-MT-c *** FIRED *** (body-moving arms: BOTHTOP)
  GUARD-NI ARMEDZERO PASS: spreadYOut ok (+0 [0, 0] n=6 vs ±1.522373) · spacingMedian ok (+0 [0, 0] n=6 vs ±3.526903) · spacingUnder4 ok (+0 [0, 0] n=6 vs ±0.027034) · dupRunShare ok (+0 [0, 0] n=6 vs ±0.132938)
  GUARD-NI PMTOP     FAIL: spreadYOut ok (-0.237396 [-0.580493, 0.094557] n=6 vs ±1.522373) · spacingMedian ok (+0.134339 [-0.476774, 0.724939] n=6 vs ±3.526903) · spacingUnder4 ok (-0.000173 [-0.012254, 0.012792] n=6 vs ±0.027034) · dupRunShare BLOWN (+0.141728 [0.070461, 0.212994] n=6 ✔ vs ±0.132938)
  GUARD-NI MTTOP     FAIL: spreadYOut ok (-0.837684 [-1.310052, -0.398738] n=6 ✔ vs ±1.522373) · spacingMedian ok (-0.578396 [-1.391858, 0.481465] n=6 vs ±3.526903) · spacingUnder4 ok (+0.008453 [-0.006492, 0.02391] n=6 vs ±0.027034) · dupRunShare BLOWN (+0.140684 [0.02556, 0.239237] n=6 ✔ vs ±0.132938)
  GUARD-NI BOTHTOP   FAIL: spreadYOut ok (-0.977124 [-1.313338, -0.581571] n=6 ✔ vs ±1.522373) · spacingMedian ok (-0.07406 [-0.896163, 0.842984] n=6 vs ±3.526903) · spacingUnder4 ok (+0.001311 [-0.012824, 0.016945] n=6 vs ±0.027034) · dupRunShare BLOWN (+0.065307 [-0.024874, 0.155409] n=6 vs ±0.132938)

REPORTED — THE ASK (no gate; PMTOP/BOTHTOP replicate #199, the rest are unmodulated by construction):
  ARMEDZERO ask +0 [0, 0] n=4 · askShiftY +0 [0, 0] n=4
  PMTOP     ask -6.401549 [-12.834576, -2.888945] n=3 ✔ · askShiftY +4.059476 [3.424781, 4.516074] n=3 ✔
  MTTOP     ask +2.236846 [-1.589428, 7.366443] n=3 · askShiftY +0 [0, 0] n=3
  BOTHTOP   ask -6.26528 [-6.26528, -6.26528] n=1 ✔ · askShiftY +5.601208 [5.601208, 5.601208] n=1 ✔
REPORTED — THE SWALLOW SHARE (no gate; NaN where the PM gene is absent):
  ABSENT    @1 m: material ticks 0 · markStance NaN · stationWalk NaN
  ARMEDZERO @1 m: material ticks 0 · markStance NaN · stationWalk NaN
  PMTOP     @1 m: material ticks 186 · markStance 1 · stationWalk 0
  MTTOP     @1 m: material ticks 0 · markStance NaN · stationWalk NaN
  BOTHTOP   @1 m: material ticks 279 · markStance 0.935484 · stationWalk 0.064516
REPORTED — MARK-ASSIGNMENT DRIFT (no gate; the #202.2 emergent feedback channel):
  ARMEDZERO markShare +0 [0, 0] n=4 · farSide +0 [0, 0] n=4 · distToMark +0 [0, 0] n=4
  PMTOP     markShare +0 [0, 0] n=3 · farSide -0.172675 [-0.518026, 0] n=3 · distToMark +3.539545 [0.887623, 5.26269] n=3 ✔
  MTTOP     markShare -0.379596 [-0.76, 0] n=3 · farSide +0.015284 [-0.48748, 0.533333] n=3 · distToMark +7.108628 [6.364745, 7.480622] n=3 ✔
  BOTHTOP   markShare +0.333333 [0.333333, 0.333333] n=1 ✔ · farSide -0.472213 [-0.472213, -0.472213] n=1 ✔ · distToMark +3.248223 [3.248223, 3.248223] n=1 ✔
OFFSIDE FLAG quiet — ARMEDZERO +0 [0, 0] n=6 · PMTOP +0.666667 [-0.333333, 1.833333] n=6 · MTTOP -0.833333 [-1.666667, -0.166667] n=6 ✔ · BOTHTOP -0.333333 [-1.333333, 1.166667] n=6

X-FAMILY GREEN: xDet ok · xFpProd ok · xSrcZero ok · gArm ok · gInherit ok · gCtrlEq ok · gSeed ok · gStats ok · gReadOnly ok · gNDerived ok
X-DET digest cf0b63828d6dc92e869f01be18bd84b8a8fc02cb0ba07b9fafa9d54f4f001f5c
resultSha256 538bccf06e6a4f110ba9d8622ad074fd301c1ba41b01886c8dd527c472fa9172
wall 17.244s · 105.7 ms/match · pairYieldMin 0.1667 · artifact docs/world-model/data/mt-t1-ruler-rerun-smoke.json
VERDICT: SMOKE — PLUMBING ONLY; ADJUDICATES NOTHING
⚠ SMOKE ADJUDICATES NOTHING — every number above is plumbing evidence, not a finding.
```

**The plumbing verdict, and only that:** all ten X-family gates green (X-DET byte-identical
across two core passes; the production fingerprint re-derived `57b0bdab…c673`;
`git diff --stat -- src` empty; **both** arming checklists satisfied on all five arms with
`k_PM` and `markSagWeight` exact and **both** opt-in channels live; all nineteen (⚠ #203) inheritance
pins found; ARMEDZERO byte-identical to ABSENT on 6/6 seeds *through both live branches*;
seeds and stats disjoint; the read-only assertions and `stationEye === null` hold). Every
gate predicate evaluated and printed; the sag census is instrument-visible and **tightened
= 0 on every arm** (the seam only ever adds distance); `process.exit` verified — **exit 0**
in smoke mode even with `F-MT-b` and `F-MT-c` "fired", because smoke mode adjudicates
nothing.

**Exit-code verification, stated exactly.** `MTT1_MODE=full MTT1_N=2` (routed onto the
exit-semantics sub-block 12,313,100–101, artifact written to `/tmp` and discarded) printed
`gNDerived FAIL`, `X-FAMILY *** RED ***`, `VERDICT: X-FAMILY FAIL — the measurement is
invalid` and returned **exit 1** — and, on the way, printed the full-mode derivation
`pairsNeeded 213 / pairYield 0.1667 = 1278 → step 1300 · wall 6809 · cap 800 ⇒ N* 800
(reservedBandCap binds; projected wall 0.235 h)`. ⚠ **Exit 2 was NOT exercised**: it is
reachable only from a clean full-mode run, which is the commander's. What was verified is
the mode guard around it (smoke exits 0 with STOPs "fired") and that the four STOP
predicates each evaluate and print.

⚠ **Do not read the smoke's body movement, its sag magnitudes, its band results, its
GUARD-NI failures or its "firings" as findings.** They are n = 1–6 realisations of a
battery sized at n = 800. The RULER RE-RUN is the commander's detached run.

## §LAUNCH — the commander's detached full run

```bash
cd /Users/jamie/Documents/Promptfoo/evofootball-arena && \
  nohup env MTT1_MODE=full npx tsx scripts/probes/mt-t1-ruler-rerun.ts \
  > /tmp/mt-t1-full.log 2>&1 &
```

* **N is NOT passed**: `MTT1_N` in full mode turns `gNDerived` RED **and** diverts the run
  off the battery block. N = 800 comes from the frozen §4 rule on the committed smoke
  artifact, and the probe prints the whole derivation at startup before the first match.
* **Artifact**: `docs/world-model/data/mt-t1-ruler-rerun.json` (the smoke's own artifact at
  `…-smoke.json` is not overwritten).
* **Expected wall**: 800 seeds × 5 arms × 2 X-DET passes × 105.7 ms ≈ **0.235 h ≈ 14 min**
  of core, plus ~30 s for the 8-seed born-equivalence check and ~10 s for the in-probe
  fingerprint ⇒ **≈ 15–16 min** on the smoke's box; budget 2 h. A projection, gating
  nothing.
* **Progress cadence**: a `[mt-t1 passN] done/total · elapsed · s/match · ETA` line at
  least every **30 s** of wall time, on stderr.
* **Exit semantics**: `0` clean pass · `1` X-family failure (measurement invalid) · `2` a
  pre-named STOP fired (PRIMARY fail / F-MT-a / F-MT-b / F-MT-c) — by contract §3, forks
  go to the user.
* **At adjudication**: compare the run's `nDerivation.smokeArtifactSha256` against
  `633e336f…9353` (§4), and read `results.primary.achievedHalfWidthM` against the 0.5 m
  target before quoting any CI (the cap binds; §4).

## §RESULT — the gates run

*(Filled by the COMMANDER at adjudication, ruling #204 — every number quoted FROM the
committed [`data/mt-t1-ruler-rerun.json`](data/mt-t1-ruler-rerun.json) / its stdout;
recompute with `MTT1_MODE=full npx tsx scripts/probes/mt-t1-ruler-rerun.ts`.)*

**Run**: 2026-08-09, HEAD `82b0a08`, N = 800 (seeds 12,313,200–999), 5 arms × 2 X-DET
passes, wall 755.4 s, **EXIT = 0**. `resultSha256 fcbf7148…8ef2` · X-DET `39ea1c81…d4b7`
· X-FP-PROD unchanged. **X-FAMILY GREEN (all ten).** Adjudicated from the PER-ARM rows
(the #203-L4 discipline), not the verdict line.

**PRIMARY — PASS. The #199-null BODY contrasts RESOLVE:**

```
  PMTOP     body -0.967 [-1.756, -0.203] ✔ · shortfall -0.647 ✔ · detach -0.553 [-1.202, +0.089]
  MTTOP     body -2.411 [-3.226, -1.581] ✔ · shortfall -2.002 ✔ · detach -1.986 ✔ (falls)
  BOTHTOP   body -2.796 [-3.523, -1.997] ✔ · shortfall -2.491 ✔ · detach -2.454 ✔ (falls)
```

The sag FIRES exactly as designed (weak-side in-trigger stance 1.88 → 6.62 m at MTTOP,
77,848/77,848 slack-positive ticks sagged, ZERO tightened) and — the load-bearing
surprise — **the ask stays NULL at MTTOP (−0.15 [−0.42, +0.12])**: the body compressed
~2.4 m WITHOUT the station ask moving. The sag delivers the tuck-in through the mark
channel itself; the steer label stays markStance (~81 % at BOTHTOP) while the BODY
moves — the contract's gate-on-the-body choice validated. Replication note, honest:
PMTOP's body here is −0.97 [−1.76, −0.20] vs #199's null [−1.15, +0.47] — overlapping
CIs on fresh seeds; read as a small borderline PM-alone effect, not a contradiction.

**F-MT-a / F-MT-b / F-MT-c — NONE fired** (F-MT-b's ∀ quantifier: PMTOP holds the band,
so "every body-moving arm fails" is false). **GUARD-NI passes at every arm** — spreadYOut
FALLS (MTTOP −0.70, BOTHTOP −0.90 vs tol ±1.60), spacing/dupRun deep inside tolerance:
compression without clump, again. Mark drift (REPORTED): far-side share FALLS resolvedly
(MTTOP −7.9 pp, BOTHTOP −12.2 pp), distToMark +2.2–2.3 m (= the sag), markShare
unchanged (assignment kept, as designed). Offside flag: offsides FALL (MTTOP −0.71,
BOTHTOP −0.84 per match) — a fall, not the rise the flag watches.

⭐ **THE COST FINDING (formally not a STOP; substantively THE result the fork is about)**:
**MTTOP and BOTHTOP blow the equilibrium band on 4 of 5 gated dimensions** —

```
  BAND MTTOP   FAIL: goals 1.614 vs [2.035, 2.754] OUT · crosses 1.679 OUT ·
               headers 4.463 OUT · longBalls 3.333 OUT · cutbacks 2.986 ok
  BAND BOTHTOP FAIL: goals 1.666 OUT · crosses 1.760 OUT · headers 4.514 OUT ·
               longBalls 3.164 OUT · cutbacks 2.950 ok
```

At the FULL dose (gene = 1, both teams), a defence that tucks in properly suppresses the
aerial/wide attacking channel wholesale: goals −27 %, headers −46 %, crosses −26 %,
long balls −41 % vs the ABSENT control (2.195 → 1.61). The control itself holds the band
(no substrate drift excluded). This is the finely-tuned-equilibrium lesson in its
defensive mirror: a one-sided defensive improvement deflates scoring — the co-evolution
precedent says selection (attack's own counters) is the balance channel, and this exam
dosed defence only, at maximum, symmetrically. Intermediate doses are UNMEASURED (the
2×2 design ran top dose only). **The fork returns to the user: the arc's exit is the
play-test, and the dose/shape of the world to play-test is the decision.**
