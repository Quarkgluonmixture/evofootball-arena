# DF-T4 — THE CAP-OFF TRIAL (M-DF.2's final sequence step)

> **Ordered by** [ruling #336 item 5](PROGRAMME-RULINGS.md), unlocked by #335 item 2 (H-DF.1
> PASSES OF RECORD ⇒ "THE CAP-OFF ARM UNLOCKS").
> **Bound by** [`DF-DEFENSIVE-BRAIN-CONTRACT.md`](DF-DEFENSIVE-BRAIN-CONTRACT.md) §1
> (H-DF.1/H-DF.2) and §2 **M-DF.2** VERBATIM: *"the census (DF-C0) freezes the swarm's own
> face (the pile-up band the cap holds today); the surface slice arms dormant beside the cap;
> the exam proves H-DF.1(b) INSIDE the cap first, then **the cap-off arm proves the surface
> alone holds the band**. Two compensators may not be retired in one slice."*
> Plus **M-DF.4**: *"the ecological gate: EVERY DF exam reports the season ladder beside
> match-grain faces."*
>
> **Parents**: [DF-C0 §R2](DF-C0-DEFENSIVE-BRAIN.md) (the crowding faces and the band the
> census froze) · [DF-T2 §R6](DF-T2-DECISION-SURFACE.md) (the surface; the cap slice's sha of
> record) · [DF-T3 §R2/§R4](DF-T3-SURFACE-EXAM.md) (the exam idiom; the press-realisation
> walker; **§R4's honest splinter — the ordered first look**) · [DF-T3B](DF-T3B-POWER-EXTENSION.md)
> (H-DF.1(a2) at power; the contain-offer predicate rider).
>
> **THE STORY IN PLAIN FOOTBALL LANGUAGE.** The game's defenders used to swarm the ball. A
> hand-written rule stopped them: *one man goes, two if we are a pressing side — never three*.
> Since then the DF arc has built something better in principle: every defender now **prices**
> his own options (go at the man on the ball · keep my man · read the pass · take him on
> contact) in one currency the engine already had. That priced brain passed its exam **with the
> old rule still switched on**. This stage switches the old rule **off in one arm** and asks
> the only question left: **without the hand-written cap, does the priced brain on its own keep
> the defenders from piling onto the ball again?** If yes, the cap can be retired for real (an
> entries-round decision). If no, the cap stays — and that is just as much an answer.

---

## §P PRE-REGISTRATION — FROZEN BEFORE THE BATTERY

**Instrument**: [`scripts/probes/df-t4-cap-off-trial.ts`](../../scripts/probes/df-t4-cap-off-trial.ts),
frozen in the SAME commit as this section (canon **freeze-before-battery**, home ruling
#266.3(c)); the artifact records the instrument's own `sha256`.
**Seam**: DF-T4 commit 1 (`dfCapOff`, dormant) landed BEFORE this freeze; the probe's
`gSrcUntouched` gate proves the worktree matches HEAD at run time.

### §P1 THE ARMS (both carry the H-DF.1-PASSING STACK)

| arm | doors |
|---|---|
`capOn` | `a4MatchFlags(9)` + `bkFacingLaw` + `bkContactLaw` + `dfAssignPersist` + `dfSurface` — the Phase-31 cap **INTACT** |
`capOff` | the same + `dfCapOff` — the cap's rule **RETIRED IN THE ARM ONLY** |

`dfCapOff` is the **only** difference. ~40 virgin paired seeds from block **12,521,000–999**;
the same seeds are walked in both arms.

**WHAT "CAP OFF" MEANS, EXACTLY** (DF-T4 commit 1, two purely ADDITIVE flag-gated statements;
no shipped statement deleted, moved or reworded):

1. the Phase-31 **OR-collapse** is un-collapsed back to `assignChasers`' **own docblock
   arithmetic** — *"Count scales with pressing: 1 base, +1 in Press mode, +1 for extreme
   pressIntensity"* — so the extra body appears only where the shipped scoring already wanted
   him and the cap alone said no;
2. the **Phase-112 window's `Math.min(…, 3)` ceiling** — which that block's own comment calls
   *"the phase-31 'never three' rule"* reasserted inside the window — is lifted, so the
   window's one extra body is added without it.

Nothing else in that function moves: the gk-holding zero, the Phase-30.5 loose-ball duel, the
Phase-28.3/29 dead-ball counts and the Phase-32.1 landing chase are **not the cap** (M-DF.2:
one compensator per slice), and both arms are pinned identical on them.

### §P2 H-DF.4 — THE SURFACE ALONE HOLDS THE BAND (SCORED; three conjuncts)

**THE CI RULES.** BETWEEN-ARM faces: RESOLVED = the 95 % **seed-clustered PAIRED** bootstrap
interval of (capOff − capOn) EXCLUDES ZERO (2,000 draws, percentile, the SAME resampled seed
set used for both arms in every draw). The (ii) conjunct is a **within-arm containment** test:
the CAP-ON arm's own seed-clustered (unpaired) 95 % interval, with the cap-off **point
estimate** required to lie inside it. Per-seed cells are stored so every headline re-derives
(canon **per-seed cells**, home ruling #282.2(ii)); every percentile face has **stored bins**
(canon: *"the re-derivation gate covers EVERY published face; a percentile face requires
stored bins"*); every face publishes its own `denNote` (moving denominators); every rate is on
the 240 s match clock or dual-axis (**clock honesty**); a starred finding states its
**|Δ|÷half-width** ratio.

#### (i) THE FOUR-CHASER BIN, CAP-OFF, IS EXACTLY ZERO

**THE FORM IS CHOSEN HERE AND ARGUED HERE, NEVER AFTER** (the dispatch: *"pick ONE form at §P
from the parents' own definitions, argue it there, never after"*). The two candidate forms were
**exactly zero** and **inside the band form DF-C0 froze for the crowding face**. ⭐ **EXACTLY
ZERO IS TAKEN, because it is the only form the parents define for this bin**:

* DF-C0 §R2 VERBATIM: *"The cap's own output (`chaserCountBinsPooled` = [431172, 1719847,
  877388, 193222, 0]) … **the bin for four is EXACTLY ZERO**, so the cap does bind"* — a
  **structural count**, published with no interval;
* DF-T2 §R6 VERBATIM: *"**THE FOUR-CHASER BIN IS EXACTLY ZERO IN BOTH ARMS**"*;
* DF-T3 §R4 VERBATIM: *"**the four-chaser bin is EXACTLY ZERO in both arms**"*.
* DF-C0's interval bands — *"the swarm the cap holds today, measured, with its denominators
  stated"* — are attached to the **share** faces (`multiChaseShare2/3`, `swarmStanceShare2`,
  `swarmZoneShare3`), never to this bin. **A nonzero band for this bin would have to be
  invented at this stage**, which the frozen-rule discipline forbids.

⚠ **THE CONSEQUENCE, STATED BEFORE THE RUN.** DF-T4 commit 1's own constructed fixture proves
a **fourth chaser is reachable** with the cap off (cap-on licenses three, cap-off four). So a
single tick of that picture in 40 walks turns this conjunct **RED**. That is accepted: a red
gate stays red and is reported, never re-cut. To let the ruling **price** a red without
touching the rule, two REPORTED companions are published beside it — `chaserShare4` (the bin
as a share of out-of-possession team-ticks, structurally 0 cap-on) and `chaserShare3Plus` (the
licence the cap ceilings; DF-C0's own capped world read *"three 6.0 %"*).

#### (ii) `multiChaseShare2` AND `multiChaseShare3`, CAP-OFF, STAY INSIDE THE CAP-ON ARM'S OWN 95 % INTERVALS

The form is the dispatch's own words. Each face's **cap-off point estimate** must lie within
`[ciLo, ciHi]` of the **CAP-ON arm's** seed-clustered bootstrap interval. This is DF-C0 §R2's
band idiom applied to **this battery's own capped arm** rather than to the census's numbers, so
the two sides share every walk, every seed and every denominator. Both faces are DF-C0 §R2's
own (≥2 and ≥3 bodies in the BALL family on an out-of-possession team-tick — the pile-up as it
actually occurs, which is the disease the cap was written against). The paired delta is
published beside each as a REPORTED companion; the **gate is the containment**.

#### (iii) THE CHURN / COVERAGE FACES DO NOT DEGRADE RESOLVEDLY CAP-OFF

`markSwitchesPerDefenderMinute` (frozen bad direction **UP**) · `markHeldShare` (frozen bad
direction **DOWN**) · `dupMarkShare` (frozen bad direction **UP**). **NON-INFERIORITY**: each
fails only on a **RESOLVED** move in its own frozen bad direction. The three faces are DF-C0
§R2's own 乱跑 faces; the direction convention is DF-T1's.

**THE VERDICT RULE**: H-DF.4 PASSES iff (i) ∧ (ii) ∧ (iii). If it holds, **cap retirement
becomes an entry decision** (the width-floor sequence completes: the compensator retires by
measurement). If it fails, **the cap stays** — and that is the result (#336 item 5).

### §P3 ⭐ THE ORDERED FIRST LOOK (REPORTED, never gated)

DF-T3 §R4's **honest splinter** — the **≥5-body STANCE bin** (five or more bodies inside the
shipped 2.6 m stance radius, the shape *"the cap exists to prevent"*) — re-measured in **BOTH
arms** from **stored bins**, with **both prior readings quoted**:

* **DF-T3 §R4 VERBATIM**: `swarmBins.armedStance` = `[130299, 60720, 16289, 2697, 159, 26]` ·
  `swarmBins.shutStance` = `[120305, 66645, 14806, 2280, 165, 0]` — 26 armed against 0 shut,
  *"26 of 210,190 armed carrier-present defending team-ticks (**0.0124 %**)"*, read there as
  *"noise at the tail of a bin, not the swarm returning"*.
* **DF-T2 §R6 VERBATIM**: `swarmBins.armedStance` = `[111776, 68278, 13721, 1799, 133, 0]` ·
  `swarmBins.shutStance` = `[123293, 62895, 14170, 2035, 260, 77]` — **the sign the other way**
  (77 shut against 0 armed), which is exactly why DF-T3 left the splinter unresolved and
  ordered it looked at FIRST here.

It is **not** a conjunct of H-DF.4. It is published as counts, denominators and shares in both
arms, plus the paired face `swarmStanceShare5`.

### §P4 REPORTED, NEVER GATED

The press **ELECTION** faces by arm (the DF-T2 usage ledger is armed in **both** arms here, so
the seat can be read with and without the cap) · the press **REALISATION** faces by arm
(DF-T3 §R2's offer/act/episode walker, definitions verbatim) · the **chaser-count distribution
by arm, FULL BINS** · **interceptions and tackles at match grain** (⚠ NOT the ladder estimand
— DF-T2 §R11 item 6: the two numbers are never quoted as the same thing) · **goals at match
grain** · the swarm **stance/zone** shares · the R-乙 chain faces (Q01 · Q05 · Q06 · Q14) ·
the churn family's remaining faces · ⭐ **THE SEASON LADDER** (M-DF.4's ecological gate: goals ×
generation, both arms, 4 leagues × 20 generations, judged against DF-C0 §R4's **quoted**
atkFrozen floor +0.2211 — a reference line, not a matched control; **no between-arm slope test
is pre-registered and none will be invented**).

### §P5 INSTRUMENT LAW (all standing orders, discharged in the instrument)

* ⭐⭐ **A COMPOSITION FIXTURE PER WALK-SIDE PREDICATE** (canon: *"a scored face's walk-side
  predicate is pinned — anchored extraction or fixture — because the re-derivation gate proves
  arithmetic, not definitions"*, home DF-T3 §CORR item 2, REFINED at #334 item 2: *"anchored
  extraction protects the source line; a headline-bearing walk-side predicate ALSO needs a
  composition fixture"*, home BK-T3 §CORR item 2). The walk's classifiers are **named
  functions** — `familyOfAction` · `swarmBinOf` · `chaserBinOf` · `markTransitionOf` ·
  `containOffered` · `containActed` — and §3B's fixtures **call those same functions** on
  constructed inputs, positive and negative, one per term for the contain-offer geometry. A
  failure is a **construction-class refusal** (exit 3, nothing written).
* **ANCHORED EXTRACTIONS** with line receipts, each matching exactly once: `markStanceBand`
  2.6 · `zonalEngageRadius9` 9 · `markRange22` 22 · `touchControlDist` 4.2 · `containRadius8`
  8 · `containTerritory35` 35 (canon, home BK-C0 §CORR item 1).
* **RED-ROUTING, REQUIRED IDIOM**: `outPath = ALL_GREEN || IS_OVERRIDE ? OUT : OUT + '.RED.json'`.
* **BOOKED = WALKED gated against the per-seed CELLS' own distinct-seed set**, compared in
  BOTH directions, never a projection of the booked input (#335 item 4's correction).
* **LIVENESS ON EVALUATION COUNTS, NEVER ON CHOSEN OUTCOMES**: `gSurfaceLedgerLiveBothArms`
  requires `elections > 0` and `pressOffered > 0` on **every** walk in **both** arms;
  `gContainInstrumentAlive` requires a nonzero offer denominator on every walk;
  `gArmsDistinguishable` requires the arms to differ **somewhere** in the battery — never that
  a particular verdict be reached. **No gate here can be satisfied by the answer we want.**
* **GATES THAT CAN FAIL**: `gCapOnBinFourZero` is the explicit pin that #336 item 5 put in
  place of DF-T2's cap-slice sha discipline (the sha **moved by construction** at commit 1 —
  see §P6); `gShippedCapLinesVerbatim` counts each of the seven shipped cap statements exactly
  once inside the slice; `gCapSliceStableThroughRun` hashes the slice before AND after the
  battery.
* **`gFacesFromDisk`** re-parses a STAGING artifact off disk and re-derives EVERY published
  face, both H-DF.4 verdict booleans and the whole ladder through **DF-C0-FIX §RF1's one
  formula**; the body is hashed **LAST** (DF-C0 §CORR item 2).
* **ENV whitelist-or-refuse** (`DFT4_MODE|N|GENS|OUT`, any other `DFT4_*` or engine door is a
  fatal refusal); an override run may not write the canonical path.
* **NO DOSING ANYWHERE** ⇒ the `info.genome`-cleanliness idiom (#334 item 1) is N/A by
  construction: both arms are shut/armed FLAG worlds and no gene is written at all.
* **CANON QUOTED VERBATIM** (copied from [`CANON.md`](CANON.md), never re-typed — #301):
  *"WORKER-SIMMED fixtures play the SHIPPED world (League.toJSON omits matchFlags; true since
  #155, stated now, test-pinned; refines #270's E4 correction; matches the perf diagnostic)"*
  (home ruling #283.2(iv)) — the ladder is the **ecology**, armed through `League.matchFlags`,
  which `League.toJSON` does not serialize.

### §P6 THE SEED + STATS LEDGER (booked BEFORE the run)

* **Block 12,521,000–999, CONSUMED WHOLE of record.** Sub-ranges: `…000–039` the battery (40
  paired seeds) · `…800–802` the in-band smoke prefix (also DF-T4's pin-suite seeds) ·
  `…900–903` the ladder's four league seeds (booked once, walked in both arms) · `…999` the
  world-construction receipt (WALKED).
* **Stats from 116,800 on the step-200 lattice**, registry of record **69** entering (#335
  item 4 / #336 item 4). THREE draws: `116,800` the paired battery bootstrap · `117,000` the
  ladder's league-clustered slope bootstrap · `117,200` the within-arm bootstrap carrying
  (ii)'s containment test. The registry leaves at **72**. Bootstrap resamples of walked seeds
  consume no registry entry.
* ⚠ **THE CAP-SLICE SHA OF RECORD MOVED BY CONSTRUCTION.** DF-T2 §R6's
  `5b4a21d036e9d97027a360f166621d747374ec5c42ead20b9ed132919872703c` covered a slice that now
  contains the flag-gated bypass. The sha at DF-T4 commit 1 is
  `0ae63c71098c8fe12ddf262b8aed57c0b707d21ddef49dc581f1fa49d9c71d62`, and per #336 item 5 the
  **discipline** the old sha carried is now the explicit four-chaser-bin pin (in
  `tests/dfCapOff.test.ts` and as this probe's `gCapOnBinFourZero`).
* **Fingerprint of record** `57b0bdab389122af5e4cacd75c4e13020b8ff248a413a7fcd71cc6215ba4c673`,
  re-computed by the probe and gated.

---

## §R RESULTS — ⛔⛔ **H-DF.4 FAILS ON ALL THREE CONJUNCTS: THE CAP STAYS**

Artifact [`data/df-t4-cap-off-trial.json`](data/df-t4-cap-off-trial.json)
(`instrument.sha256` = `5b26d271cf1f430be3aa47f0be306db3f5777933394d78e8091f802164e4fbca`;
`bodySha256` = `5d39595f7b4a301d6f6f08f4f3b4642645b4afc1eaa053e1ad65033fd7f28204`).
**82 walks** (41 paired seeds × 2 arms), every `worldOk` true; **24/24 gates GREEN**;
`faceReDerivation` = **518 checks, 0 mismatches**; 31 walk-side predicate fixture cells all
held; `wall` = 8.83 s battery + 836 s ladder. `fingerprint.recomputed` =
`57b0bdab389122af5e4cacd75c4e13020b8ff248a413a7fcd71cc6215ba4c673` = of record.
`capSeam.shaBefore` = `capSeam.shaAfter` = `0ae63c71098c8fe12ddf262b8aed57c0b707d21ddef49dc581f1fa49d9c71d62`.

`hdf4.verdict` = **`FAIL — (i) and (ii) and (iii)`**.

**In the player's language: take the hand-written rule away and the defenders pile back onto
the ball.** Not at the tail, and not by a hair — a fourth man is now licensed on **2.5 % of
all defending moments**, three-or-more bodies actually reach the ball **76 % more often**, and
each defender **loses coverage of his man**. The priced brain is real and it is good at what
it prices, but it never prices *this*: it decides who marks whom, while the number of men sent
at the ball is a **different seat the surface does not sit in**. So the compensator does not
retire — and the trial names its own successor door.

### §R1 (i) THE FOUR-CHASER BIN — **RED**, and by 13,069 ticks, not by one

`hdf4.i_fourChaserBinZero.pass` = **false**.

`chaserBins.capOn`  = `[80103, 237131, 150241, 49995, 0]` (517,470 out-of-possession team-ticks)
`chaserBins.capOff` = `[79897, 236695, 115025, 75599, 13069]` (520,285)

**THE FOUR-CHASER BIN IS 13,069 CAP-OFF AGAINST 0 CAP-ON.** The frozen form was EXACTLY ZERO,
argued at §P from the parents' own definitions; it is red and it stays red. And the §P
companions say the red is **not** the tail-noise case §P worried about:

| face (REPORTED) | capOn | capOff | Δ [95 % paired CI] | \|Δ\|÷hw | resolved |
|---|---:|---:|---|---:|:--:|
| `chaserShare4` | **0** | **0.0251189252045** | +0.0251189252045 [0.00682898494603, 0.0472645693199] | **1.24242** | ⛔ UP |
| `chaserShare3Plus` | 0.096614296481 | **0.170421980261** | +0.0738076837799 [0.0423475417619, 0.109875656537] | **2.18598** | ⛔ UP |

The licence the cap ceilings **nearly doubles** (9.66 % → 17.04 % of defending team-ticks, a
resolved rise at 2.19 × the half-width), and the three-chaser bin itself is *reshaped*:
150,241 → 115,025 at three while 49,995 → 75,599 sit at four-or-more's neighbour. This is the
cap **binding hard in normal play**, not a rare picture.

### §R2 (ii) THE multiChase BAND — **RED on the ≥3 face**, inside on the ≥2 face

`hdf4.ii_multiChaseInsideCapOnIntervals.pass` = **false**.

| face | CAP-ON value [its own 95 % interval] | CAP-OFF point | inside? | paired Δ [95 % CI] | \|Δ\|÷hw |
|---|---:|---:|:--:|---|---:|
| `multiChaseShare2` | 0.438141341527 [0.391048859732, 0.488653909863] | 0.442178805847 | ✅ **yes** | +0.00403746431975 [−0.0124902360598, 0.0182984643588] | 0.262269 |
| `multiChaseShare3` | 0.154704620558 [0.121196290976, 0.191776705414] | **0.215118636901** | ⛔ **NO** | **+0.0604140163432** [0.0339484743211, 0.0914142153534] | **2.10261** |

⭐ **THE FACE THE ARC CHASED FOR FOUR STAGES FINALLY MOVES — IN THE BAD DIRECTION, AND ONLY
WITH THE CAP GONE.** `multiChaseShare3` — three or more bodies in the BALL family at once, the
"never three" as it actually occurs — sat unresolved at DF-C0 (0.1169), DF-T2, #324 and DF-T3
(0.1296 → 0.1236, *lower* armed). Here it rises **0.1547 → 0.2151**, its cap-off point
estimate **outside** the capped arm's own interval, on a **RESOLVED** paired delta at
**2.10 × the half-width**. The ≥2 face does not move at all, which is the shape you would
expect: the cap never banned two.

### §R3 (iii) THE CHURN / COVERAGE FACES — **RED on coverage**

`hdf4.iii_churnDoesNotDegrade.pass` = **false**.

| face (SCORED) | frozen bad direction | capOn | capOff | Δ [95 % paired CI] | \|Δ\|÷hw | degraded? |
|---|:--:|---:|---:|---|---:|:--:|
| `markSwitchesPerDefenderMinute` | UP | 6.62338161282 | 6.50811891429 | −0.115262698523 [−0.363497446718, 0.129152417677] | 0.467929 | no |
| `markHeldShare` | DOWN | 0.659930978307 | **0.640572655093** | **−0.0193583232135** [−0.0308130095439, −0.00846320971511] | **1.7323** | ⛔ **YES** |
| `dupMarkShare` | UP | 0.264359299029 | 0.262919176814 | −0.0014401222149 [−0.0132337573776, 0.00908916654369] | 0.129026 | no |

**乱跑 does NOT come back** (switches flat, accidental double-marking flat — DF-T0's cure is
untouched by the cap's retirement), but **COVERAGE DOES FALL, RESOLVEDLY**: a defender has a
man 66.0 % of his defending life with the cap on and 64.1 % with it off. That is the
arithmetic consequence of the extra licence — a body sent at the ball is a body not marking —
and it is exactly the trade the Phase-31 rule was written to refuse. `markSwitchesPerDefenderMatch`
on the 240 s clock: 26.4935264513 → 26.0324756572 (the same unresolved move, dual axis).

### §R4 ⭐ THE ORDERED FIRST LOOK — **THE ≥5-BODY STANCE SPLINTER IS RETIRED AS NOISE**

`stanceFive.capOnBins` = `[122129, 60528, 17117, 2704, 94, 0]` (202,572)
`stanceFive.capOffBins` = `[122671, 63553, 17226, 3592, 199, 0]` (207,241)

**BOTH ARMS READ EXACTLY ZERO** in the ≥5 bin (`capOnBinFive` = 0, `capOffBinFive` = 0,
`capOnShare` = `capOffShare` = 0; the paired face `swarmStanceShare5` is 0 → 0, Δ 0, CI
[0, 0]). The two prior readings, quoted at §P3, were **DF-T3 §R4**'s *26 armed against 0 shut*
(*"0.0124 %"*, called *"noise at the tail of a bin, not the swarm returning"*) and **DF-T2
§R6**'s *77 shut against 0 armed* — **the sign the other way**. A third independent battery, on
virgin seeds, with the cap **retired** in one arm — the most adversarial condition available —
finds **zero in both**. ⭐ **THE SPLINTER'S OWN QUESTION IS ANSWERED: it was bin noise, and the
five-body stance pile-up does not exist in this world.** DF-T3's reading is upheld against its
own worst case.

⚠ But note where the crowding **does** show up in the same histogram: the **four-body** stance
bin roughly doubles, 94 → 199. The bodies converge — they just never reach five.

### §R5 REPORTED — THE SEAT DID NOT MOVE, WHICH IS THE MECHANISM

| face (REPORTED) | capOn | capOff | Δ [95 % paired CI] | \|Δ\|÷hw | resolved |
|---|---:|---:|---|---:|:--:|
| `pressElectionShare` | 0.00249249887778 | 0.00260890668608 | +0.000116407808297 [−0.000207180636092, 0.000453108586538] | 0.352596 | — |
| `pressDeclinedByBookShare` | 0.254504504505 | 0.256111757858 | +0.00160725335347 [−0.0256029882604, 0.0294926024834] | 0.0583442 | — |
| `containActShareOfOfferTicks` | 0.433398564905 | 0.430875576037 | −0.00252298886855 [−0.0458927707761, 0.0439290723771] | 0.0561776 | — |
| `containEpisodesPerDefenderMinute` | 0.326908099008 | 0.314482547494 | −0.0124255515143 [−0.0420654494311, 0.0182581106744] | 0.411963 | — |
| `containOfferTicksPerDefenderMinute` | 10.7083358073 | 10.870520624 | +0.162184816738 [−0.839312753537, 1.29505351345] | 0.151975 | — |
| `chaseStartsPerDefenderMinute` | 10.176062364 | 10.3431361749 | +0.167073810879 [−0.263298921468, 0.567952041344] | 0.401982 | — |
| `swarmStanceShare2` | 0.0983107240882 | 0.101413330374 | +0.00310260628559 [−0.00656366689579, 0.0137458678208] | 0.305532 | — |
| `swarmZoneShare3` | 0.319644373359 | 0.325442359379 | +0.00579798602009 [−0.02062921067, 0.0284981519869] | 0.236039 | — |
| `reTargetLatencyMeanS` | 1.06352044352 | 1.06584993456 | +0.00232949104405 [−0.051818087638, 0.0459664500189] | 0.0476454 | — |

⭐⭐ **THE MECHANISM OF THE FAILURE, IN ONE LINE: THE PRICED SURFACE NEVER SEES THIS
DECISION.** The election seat is *flat* — the press-election share, the book's decline rate and
the realisation rate all sit inside their own noise while the chaser bins move by tens of
thousands of ticks. `assignChasers` and `assignMarks` are **two different seats**: the surface
prices *who marks whom* (and offers a body to the shipped contain branch), while *how many men
are licensed to hunt the ball* is decided upstream by a rule the surface neither reads nor
writes (M-DF.2's own design: *"`assignChasers` and the Phase-31 presser cap are never read,
written or consulted here"*). Retiring the cap therefore removes a compensator whose job **no
priced decision has taken over** — which is precisely what H-DF.4 was built to detect, and it
detected it.

Latency percentiles from **stored bins** (`latencyBins.capOn` = `[4228, 872, 518, 353, 276,
286, 165, 517]` · `capOff` = `[4024, 804, 533, 340, 259, 267, 166, 484]`): median 0.5 s and
p90 3 s in **both** arms.

**MATCH GRAIN — nothing resolves** (⚠ NOT the ladder estimand, DF-T2 §R11 item 6):
`goalsPerMatch` 2.58536585366 → 2.51219512195 (Δ −0.0731707317073 [−0.292682926829,
0.121951219512]) · `shotsPerMatch` 14.4390243902 → 14.0731707317 · `interceptionsPerMatch`
27.6341463415 → 28.2195121951 · `tacklesPerMatch` 2.0243902439 → 2.19512195122 ·
`tacklesPlusInterceptionsPerMatch` 29.6585365854 → 30.4146341463. **One friendly with random
genomes cannot see this door.** The R-乙 chain is likewise quiet: Q01 4.41183446884 →
4.40115288221 · Q05 2.47993943982 → 2.45338345865 · Q06 0.59213378755 → 0.582894329098 ·
Q14 0.760030280091 → 0.778947368421 (0.879212 hw — the biggest of them, still unresolved).

### §R6 ⭐ THE SEASON LADDER (M-DF.4's ecological gate) — **THE COST IS ECOLOGICAL**

160 league-seasons, **11,360 matches**, 4 league seeds × 20 generations × 2 arms, every
generation measured; `gLadderDoorHeld` GREEN (every `doorWrong` = 0).

| arm | face | early 1–5 | late 16–20 | Δ | half-width | \|Δ\|÷hw |
|---|---|---:|---:|---:|---:|---:|
| `liveCapOn` | goals | 2.44084507042 | 3.20352112676 | **+0.762676056338** | 0.625 | 1.22028 |
| `liveCapOff` | goals | 2.41478873239 | 3.67535211268 | **+1.26056338028** | 0.530281690141 | **2.37716** |
| `liveCapOn` | interceptions | 10.3714788732 | 7.82394366197 | **−2.54753521127** | 1.38573943662 | 1.83839 |
| `liveCapOff` | interceptions | 10.0468309859 | 6.79471830986 | **−3.25211267606** | 1.56830985915 | **2.07364** |
| `liveCapOn` | tackles | 6.17992957746 | 6.98063380282 | +0.800704225352 | 0.441021126761 | 1.81557 |
| `liveCapOff` | tackles | 6.46936619718 | 8.38556338028 | **+1.9161971831** | 1.96161971831 | 0.976844 |

`floorRead`: `capOnDistanceAboveFloor` = **0.541576056338** · `capOffDistanceAboveFloor` =
**1.03946338028** · `capOffMinusCapOn` = **+0.497887323942** · `bendsTowardFloor` = **false**,
against DF-C0 §R4's QUOTED atkFrozen floor **+0.2211**.

**Retiring the cap makes the user's own inflation face WORSE and the reading collapse
DEEPER.** The goals slope goes from +0.763 to +1.261 — nearly **twice** the distance above the
floor — while interceptions fall harder (−2.548 → −3.252 per team-match) and tackles rise
harder (+0.801 → +1.916): 防守从读球退化成身体接触, accelerated. Generation-1 → 20
interceptions: 11.3697183099 → 8.03873239437 cap-on vs 11.3257042254 → **6.84507042254**
cap-off.

⚠ **HONEST INTERVAL READING** (no between-arm slope test was pre-registered and none is
invented): the two goals-slope intervals **do overlap** (±0.625 and ±0.530 around +0.763 and
+1.261), so the ladder is a **direction, not a resolved separation** — it is REPORTED, and per
#320 item 3's frozen direction a deviation **routes to a slice, never to a nudge**.
⚠ **AND AN HONEST DISCLOSURE**: the arms' gen-1 **genome pools are identical** (same four
league seeds; `gen1Fingerprints` = `1b54bd55fdd6…`, `da849172eb85…`, `4eb2cc079cb4…`,
`cb443d795e6d…`), but gen-1 **PLAY is not** — 2.32042253521 vs 2.25352112676 goals/match — for
the same reason DF-T0 §R2 disclosed: arming a door produces a different world from the first
tick, so every trajectory and every denominator differs. The floor remains a **reference line,
not a matched control** (DF-C0's atkFrozen arm froze ATTACK genes on league seeds
12,508,900–903; this stage arms a defensive door on 12,521,900–903).

### §R7 THE CONSEQUENCE, AND THE DOOR IT NAMES

Per §P2's own verdict rule and #336 item 5: **H-DF.4 FAILS ⇒ THE CAP STAYS.** The width-floor
sequence does **not** complete at this stage, and `cap retirement` does **not** go to the
entries round. M-DF.2 is satisfied to the letter: the compensator was tested by measurement,
not deleted by taste, and it earned its place.

**THE NAMED DOOR (a slice question, not a nudge)**: a **PRICED CHASER LICENCE** — the same
one-currency treatment `assignMarks` received, applied at the `assignChasers` seat, so that
「派几个人去抢球」 becomes a decision with a price instead of a hand-written ceiling. §R5's
flat election faces are the evidence that this is a *different* seat and therefore a *different*
slice; §R1's `chaserShare3Plus` (Δ +0.0738076837799, |Δ|÷hw 2.18598) and §R3's `markHeldShare`
(Δ −0.0193583232135, |Δ|÷hw 1.7323) are the two faces such a slice would have to hold. The cap remains the
compensator standing in for that unbuilt price — now with a measured receipt saying what it is
worth.

