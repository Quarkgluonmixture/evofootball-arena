# THE OUTLET CONTRACT (压力出口 — the absorption half of #173.4)

> Drafted by the commander 2026-08-08 under ruling #175 (the user ruled 甲
> at #173.4). Stands on the #174 substrate inventory (every existence claim
> there is citation-traced; this contract does not restate it). Binding once
> the embedded §7 VISION audit is ruled. Parent instruments: the tempo
> census (`TEMPO-CENSUS.md`, the arc's standing ABSOLUTE ruler) and the
> #173.2 gap table (the arc's frozen baseline).

## §0 The finding this contract answers

The user's #169 verdict (VISION §2 anchor, verbatim): 没有拿住球 · 抬头观察
· 护球缺席,逼抢压力出不去,节奏 ~2.3× 真实足球。H-169a held on all three
arms (#173.3): pressure destroys RETENTION (51% vs 70%), safe release is
rare everywhere; the substrate offers no time-buying action under pressure.

The #174 inventory's correction to the naive reading: **the seats mostly
EXIST.** C7 shot wind-up is certified end-to-end and armed in the play-test
worlds; the ShieldHold body-mechanics passed T0R; the whether-seat is built.
What is actually missing, in causal order:
1. **RELEASE TIME on the pass family** — median reception-to-release 0.33 s,
   44% of touches inside the engine's own 0.28 s window; C7 covers SHOTS
   ONLY (pass wind-up was explicitly deferred to a future contract — this
   is that contract).
2. **PERCEPTION of hold-worthy moments** — the whether-seat was re-parked
   because the binding constraint is perception, not price (#65: 4× wedge,
   E-ABSTAIN-UNSEEN 70.7%). 抬头观察 is not built.
3. **A PRICED hold/shield choice** — ShieldHold is a forced reflex (score
   literal 1, pre-cands return); zero gene/attr input to WHETHER anywhere.
4. **Attribute hooks on control TIME** — the reception delay constants
   (3 ticks / 0.08 s) are fixed; only touch QUALITY is attribute-driven.

## §1 The claim (H-OUTLET)

Giving releases a skill-priced TIME cost (O1), perception a way to BUY
context (O2), and the hold/shield family a priced, gene-expressible seat in
the candidate list (O3) moves the world's time signature toward the
reference bands (spell length ↑, one-touch share ↓, pressed-loss ratio ↓)
WITHOUT breaking the football hard gates — and the user's eyes confirm the
feel (拿得住球、看得见观察、压力有出口). Watchability's verdict authority
is the user's play-test (the #152/#168 precedent).

## §2 Slices (sequenced by leverage; one slice = one contract-governed arc)

* **O1 — PASS WIND-UP (the tempo lever).** Extend the certified C7
  machinery (`pendingKick`, the attribute-scaled duration law) from shots
  to the PASS family. Everything C7-T0/T1/T2 certified transfers where
  applicable; what does NOT transfer is presumption: pass volume is ~8×
  shot volume, so O1 gets its own staged arc (census → dormant seam → A/B →
  re-run the TEMPO CENSUS as the outcome ruler → play-test). The one-touch
  window is the DESIGNED bypass (a one-touch release skips wind-up at its
  existing accuracy price — that is the real football tradeoff: 一脚出球
  快但糙,拿住再传准但要时间); free kicks excluded as in C7. Scope
  decision points frozen at stage docs: which pass kinds in the first cut
  (short pass first; cross/through/clearance staged), interruptibility
  inherited from C7 Q2.
* **O2 — 抬头观察 (the perception purchase).** The user's own words and
  the #65 unpark path. A LOOK action/moment that buys context visibility
  (the whether-seat's cell perception) at a time price — feeding the SAME
  perception queries (the awareness lesson: reading, never optimism).
  Unpark condition #65.3 applies verbatim: any perception-trunk change →
  re-run the whether-seat sizing smoke FIRST (cheap) before any build.
* **O3 — THE PRICED HOLD.** Promote hold/shield from reflex to a scored
  candidate on the ShieldHold (percept-honest) path — gene/attr-expressed
  (the #148-family idiom: born at incumbent-equivalent, differentiation
  earned), priced by the certified census cells (no subsidy — the E5h ×1.3
  ban and the A2b < 0.90 survival ceiling are inherited as HARD gates).
  Legacy HoldUp is LEFT UNTOUCHED (two-shields-two-epistemics: all new
  work extends the percept path; widening HoldUp's omniscient scan is
  REJECTED ex ante).
* **O4 — CONTROL-TIME HOOKS (small, last).** Attribute scaling on the
  reception control delay (the 3-tick/0.08 s constants), C5-T0R's measured
  gradient as the design reference. Gated behind O1's re-census (the delay
  only matters once releases cost time).

Sequencing rationale (recorded, not re-litigable without a ruling): O1
first because the tempo gap lives in release time and needs no perception;
O2/O3 after because the certified exchange rate ("waiting costs 6–15 pp")
was measured in the PRE-O1 world — O1 changes the equilibrium holding is
priced in, so the C5 re-census re-runs after O1 (the #65 cells are stale
the moment O1 arms). O4 last, smallest.

## §3 Invariants (binding on every slice)

* **I1 NO FREE TIME** (inherited from C5-TIME-DIMENSION): every time-buying
  action pays in exposure, stamina, or opportunity — subsidies are the
  certified disease.
* **NO DOUBLE-CHARGE** (C7 Q3): the accuracy/power half of hurry already
  exists on every kick path (`kickMisalignment`, oneTouchMul); O1 prices
  TIME only.
* **FLAG HYGIENE** (#174 inventory traps 1–3): new flags are EXPLICIT
  booleans, never defaulted to `EDS_BUNDLE_ARMED`, never armed via the env
  bundle; the a4World census-fidelity set is never silently widened —
  play-test worlds get their own frozen flag set per slice.
* **EPISTEMIC HONESTY**: all new decision inputs go through percepts
  (`perceivedSnapshot`), never truth scans; omniscience is the audited-out
  disease.
* **EQUILIBRIUM DISCIPLINE**: the ~2.0 goals band + the P3′ football hard
  gates ride every A/B; the tempo census re-runs as the OUTCOME ruler per
  slice (absolute, both time axes); one-sided attacking sharpeners are the
  known inflation hazard (the vision-attr lesson).
* Road B until a slice's play-test verdict; explicit-path staging; frozen-
  before-sight; FAILs recorded as-is.

## §4 Instruments

The tempo census (banked #172) re-runs per slice as the outcome ruler:
spell length, one-touch share, time-on-ball, pressed-loss ratio, watched-
clock turnover rate — REFERENCE bands, football hard gates GATING, feel =
the user. Per-slice probes follow the house pre-registration form. The two
defect counters registered at VISION §3.1 (passes-to-offside rate,
back-pass interception rate) ride every battery as REPORTED.

## §5 FAIL modes, pre-named

* F-O1a: pass wind-up collapses the equilibrium band (goals/shots blow out
  either direction beyond the frozen band) → STOP to the user.
* F-O1b: tempo moves < 20% of the #173.2 gap at full arming — the lever is
  too small; the fork (bigger duration law vs accept) is the user's.
* F-O2a: the look action re-runs the #65 sizing and the wedge does NOT
  close → the perception trunk needs different surgery; STOP.
* F-O3a: the priced hold breaches the A2b survival ceiling (free option) →
  the slice banks dormant, honestly.
* Any slice's play-test verdict is negative → keep/change/revert per lever,
  the user's authority.

## §6 Non-claims

No offside work (乙 hangs). No punish-compactness work (the supply half
stays queued; this arc is the absorption half). No de-gluing (C6 owns it;
the M3b revert stands). No coach/doctrine rungs. O1 does not claim to fix
乱抢 alone — the census adjudicates how much it buys.

## §7 VISION audit record (clause-by-clause at drafting, the #91 form)

* **vs §2 tempo anchor ("先落 C5/C7 再重判节奏")**: this arc IS that order —
  O1 lands C7's missing half, the census re-judges tempo after. PASS.
* **vs §2 前后摇 anchor (前摇为零 = the defect; 一脚出球绕过 = C5 家族)**:
  O1's one-touch bypass is the anchor's own design; the wind-up as 可被读
  的外显证据 (defenders may close during it) is inherited from C7. PASS.
* **vs §3.1 receive-phase anchor (座2 观察 / 座3 hold-draw-release)**: O2/O3
  are those seats, built on the percept path. PASS.
* **vs §1 emergence (capability-not-mandate, genes are evolution's seat)**:
  O3's whether is gene-expressed, born incumbent-equivalent; no role rules;
  O1's duration law is attribute-driven (dribbling), no hand behavior. PASS.
* **vs §2 watchability hard bar (机制让比赛更难看即失败)**: every slice
  exits at the user's eyes; the census carries the countable face. PASS.
* **vs 涌现 economy (no free options)**: I1 + E5h ceiling + A2b gate
  inherited as HARD. PASS.
* Amendments produced: none — the inventory-first order caught the traps
  before drafting (the #174 pre-scout is the reusable lesson).

## §8 Execution map

O1 stage docs under `O1-*.md` (phase-0 code map → census → dormant seam →
A/B → tempo re-census → play-test), the C7 doc family as the template; O2
begins with the #65.3 sizing re-run; O3 after O1's re-census; O4 last.
One executor step per round (§0.0 Workflow form); Opus 5 medium; the
commander reviews, rules, launches long runs resident. Seeds from
12,300,000+ (a NEW block family — the 12.2M pool is exhausted at
12,292,999; disjointness ledger restarts there). Self-drive may be granted
per-arc by the user (the #159 form); default is one step per "go".
