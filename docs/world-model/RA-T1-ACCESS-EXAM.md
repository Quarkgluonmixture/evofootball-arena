# RA-T1 — THE ACCESS-PRICE EXAM (装上「他赶得到吗」之后, 大脑还选幽灵球吗?)

> Authorized by **COMMANDER RULING #361 item 3**, on the seat RA-T0 landed (seam commit
> `3f2ed9f`) under #360 item 3's dispatch — the arc the user's fork election
> **「①′ 接应时间入价」** opened (rulings #358/#359/#360). Contract frame:
> [`DX-DELIVERY-EXECUTION-CONTRACT.md`](DX-DELIVERY-EXECUTION-CONTRACT.md).
> Instrument family: [`DX-C2-MEETABILITY-CENSUS.md`](DX-C2-MEETABILITY-CENSUS.md) — this
> exam runs that census's eye (the same wrappers, classifiers, outcome ladder and account
> re-read, byte for byte in substance) over TWO PAIRED ARMS.
> Instrument: `scripts/probes/ra-t1-access-exam.ts`.
> Artifact: `docs/world-model/data/ra-t1-access-exam.json` (or its `.RED.json` side path).
>
> ⭐ **AN EXAM**: H-RA.1 is scored by the frozen rules below; every other face is REPORTED,
> gated by nothing. ⛔ X-SRC-ZERO for the instrument (the seat under test carries its own
> 19-pin suite from birth). ⛔ Nothing ships: both doors stay absent from `a4World.ts`.

## §P THE FREEZE (all of it BEFORE any battery seed)

### §P.A THE ARMS — paired on seed

* **SHUT** = the DX-T1 walked composition: `a4MatchFlags(11)` + `dlcDeliveryChoice` +
  `dlcStrikePlane` + `bkGroundCorridor` + `dxWindupAim` + `armA4World(m, null, 11)` +
  `passLeadSupport = 1` MATCH-LOCAL — the pathology composition DX-C2 measured.
* **ARMED** = SHUT + `raAccessPrice: true` + `raAccessWeight = 1` MATCH-LOCAL — ⭐ the
  **exam-pinned maximum** (the DX-T1 idiom: measure the mechanism at full expression; what
  weight selection adopts is the season ladder's question, later). Genes written on both
  match-local views of both teams; the franchise genome stays clean (gated).

### §P.B THE POPULATION (DX-C2 §P.B, inherited byte for byte in substance)

Measured ground passes, the DX-C1 carry classes, the temporal outcome ladder, and DX-C2
§P.A's account re-read at the election instant (`marginOf` / `meetableOf`, the observer's
copy fixture-pinned against the seat's exported constants — G-TRACE).

### §P.C H-RA.1 — THE FROZEN RULES

* **(a)** the paired Δ (armed − shut) on **`unmeetableCarriedPerMatch`** — carried
  elections UNMEETABLE at the election instant, per match — has its 95 % cluster-bootstrap
  interval **ENTIRELY BELOW ZERO** ⇒ FALLS.
  ⚠ **DEVIATION FROM #361's WORDING, STATED AND FLAGGED FOR RATIFICATION**: the ruling
  says "share". The share (`unmeetableShareCarried`) is REPORTED beside, but it is
  undefined exactly when the door works hardest (carried volume → 0), so the COUNT — the
  quantity the door exists to reduce, defined in both regimes — is primary.
* **(b1)** the ARMED `groundPassesPerMatch` **POINT** estimate sits **ABOVE the SHUT arm's
  own 95 % interval LOWER EDGE** — DX-T2 §R3's band construction of record, verbatim.
* **(b2)** the paired Δ on `passCompletion` (the engine's own whole-match completion, the
  H-DX.2(b) face) is **NOT entirely below zero** ⇒ DOES-NOT-FALL (⚠ a non-fall form at its
  declared MDE, never "restored").
* **H-RA.1 = PASS ⇔ (a) FALLS ∧ (b1) HOLDS ∧ (b2) DOES-NOT-FALL.** Pre-committed reads
  (#361 item 3(v)): PASS ⇒ the RA entry candidate forms and #357's grid composition exam
  (on meetable candidates) is next · (a) fails ⇒ the weight question returns to the
  commander WITH numbers · (b) fails ⇒ the price over-charges the ground game and the arc
  pauses at the user's fork.

### §P.D REPORTED FACES (gated by nothing)

Per arm + paired Δ with LOO (⚠ conservative point-shift flip form, stated):
`unmeetableShareCarried` · `carriedPerMatch` · `completionCarried` ·
`unresolvedShareCarried` · `meanCarriedLeadMetres` · `meanCarriedMarginSeconds` ·
`goalsPerMatch` · `interceptionsPerMatch` · `measuredCompletionAll` ·
`groundPassesPerMatch`. Bins: the carried margin (±0.1 s × 21) and lead (0.5 m × 13)
histograms, per arm.

### §P.E SEEDS, SIZING, GATES

* **Block 12,531,000–999** (#361 item 3(iv)): battery pairs 12,531,000–12,531,**494**
  (N_FROZEN = **495** pairs = 990 walks), receipt pair 12,531,999 (both arms). Lockstep +
  smokes on out-of-band scratch (≥ 900,001,400). Booked = walked = 992.
* **Sizing (house form; §DEV-PREFLIGHT's 12-pair smoke as variance source)**: (a)'s
  target = −25 % of DX-C2's own unmeetable-carried volume, READ from its artifact
  (3,696/900 ⇒ target −1.0267/match) ⇒ **42 pairs needed**; (b2)'s target = 0.01 (the
  DX-T1 completion-fall magnitude's order) ⇒ **276 pairs needed**. Both resolvable at 495
  with headroom.
* **Gates** (liveness/receipt only): gWorld · gGeneValuePinned (RA gene = 1 by VALUE in
  the armed arm, ABSENT in the shut arm, every match) · gGenomeClean · gSeamSitesPinned
  (the RA fork/term/gate needles included) · gAnchoredConstants + G-TRACE fixtures ·
  gWalkFixtures · gCarryPartition · gClassesNonVacuous (⚠ the ARMED arm's unmeetable
  count is (a)'s own question and is NOT gated) · gDepositCarriesElection · gLockstep
  (byte-inert wrappers, PER ARM) · ⭐ gArmsDiverge (the receipt that the two arms are
  genuinely different worlds) · gQuotedSourceIntact · gSrcUntouched ·
  gSeedsBookedEqualWalked · gN · gFaces (every face, Δ, verdict — H-RA.1's own INCLUDED —
  bin and sizing row re-derived off the serialized artifact). Estimator: cluster bootstrap
  over pairs, 2,000 draws, seeded from the block base. **Stats consumed: ZERO.**

## §DEV-PREFLIGHT — the sizing smoke, disclosed in full

12-pair scratch smoke (`RAT1_MODE=smoke RAT1_N=12`, seeds **900,001,500–511**, artifact
off the canonical path), run BEFORE this freeze; realised paired-Δ half-widths read out of
the smoke artifact's own `deltas[].halfWidth` fields and hardcoded in `SIZING_INPUTS`:

| paired Δ | realised hw (12 pairs) | target | N required | resolvable at 495 |
|---|---|---|---|---|
| unmeetableCarriedPerMatch | 1.3333333333333335 | −1.0267 (−25 % of DX-C2's own volume) | 42 | ✅ |
| passCompletion | 0.03354021139811786 | 0.01 | 276 | ✅ |

⚠ 12 pairs is a NOISY variance estimate; said before the battery. The smoke also
confirmed instrument liveness (all 16 gates green, both smokes) and G-ARMS-DIVERGE. ⚠ The
smoke's own point readings (Δ ≈ −2.75 on (a); mean lead 7.8 → 5.3 m) are UNPOWERED and
bind nothing.

## §R RESULTS — filled by the battery run; empty at the freeze

*(the freeze commit ends here; §R lands with the results commit, quoting the artifact's
own fields — the #357 standing order)*
