# GC-T2 — THE POWER EXTENSION (同样的四条规矩,这次带够了样本)

> **GC-T1B's four frozen H-GC.2 conjunct FORMS, re-armed at an n PRE-REGISTERED from GC-T1B's own
> published variances — with a LEAVE-ONE-OUT SENSITIVITY FACE beside every scored face.**
> Authorized by **COMMANDER RULING #347 item 2**; bound by
> [`GC-GROUND-CORRIDOR-CONTRACT.md`](GC-GROUND-CORRIDOR-CONTRACT.md) §3. The conjunct forms are
> **#345 item 5's**, re-dispatched unchanged; the sensitivity face is **#346 item 1's standing
> order**. Predecessor: [`GC-T1B-ALTERNATIVES-ARM.md`](GC-T1B-ALTERNATIVES-ARM.md) (freeze
> `8481e18` → results `6282f21` → rider `641e20a`), whose §COMMANDER CORRECTIONS (#346) supplies
> the OUTLIER-ROBUST magnitudes this exam is sized against.
> Instrument: `scripts/probes/gc-t2-power-extension.ts`.
> Artifact: `docs/world-model/data/gc-t2-power-extension.json`
> (**or its `.RED.json` SIDE PATH** if any gate is red — the red-routing idiom, #334 item 5,
> implemented as the instrument's own line).
>
> **THIS STAGE SHIPS NOTHING.** `bkGroundCorridor`, `dlcDeliveryChoice` and `dlcStrikePlane` all
> stay default OFF and **absent from `a4World.ts` at every version** (re-asserted at battery time
> by `gSeamSitesPinned`); the production fingerprint
> `57b0bdab389122af5e4cacd75c4e13020b8ff248a413a7fcd71cc6215ba4c673` is unmoved. ⛔ **X-SRC-ZERO**:
> no file under `src/` is edited — the probe arms every flag IN-INSTRUMENT, as construction flags
> on its own `Match`.

## §0 WHAT THIS EXAM IS, AND WHAT IT IS NOT

GC-T1B answered the mechanism question and #346 banked it: **alternatives cure the suppression**.
(b) passed — the ground-pass volume held inside its own band where GC-T1's fell 2.01 half-widths
out — and the pre-registered §P6 signature read RE-AIM, partially (blocked column −318, clear
column **+132**, where GC-T1's clear column fell).

What #346 also found is why this exam exists. The verify HIGH was real: **one seed (12,525,152)
sat inside every scored face** and supplied ≈27 % of (a)'s and ≈47 % of (c)'s Δ. Leave it out and
both intervals open past zero. So **(a1) and (c) are OUTLIER-FRAGILE of record**, and (a2) and (d)
never resolved at all. Four faces are outstanding and the honest reason is the same in all four:
**160 paired seeds is not enough sample to answer them.**

**THIS EXAM, in one sentence**: the SAME arms, the SAME predicates, **not one of them re-cut** —
on the sample size GC-T1B's own published variances say those questions need, and with the
single-seed influence reported beside every answer.

⛔ **IT IS NOT A NEW QUESTION AND NOT A REDESIGN.** No new face is scored, no threshold moves, no
weight ladder is walked, no season ladder is run, and the graded-hazard door (contract §4) stays
shut — that is the commander's next fork if this one fails.

---

# §P PRE-REGISTRATION (frozen at the FREEZE COMMIT, BEFORE any battery seed was read)

## §CORRECTIONS-READ — every canon sentence COPIED from [`CANON.md`](CANON.md), never re-typed

Per ruling #301 item 2's mechanism fix: the ledger is where a brief copies from. ⚠ Per **#342
item 3** (the MED-1 lesson), a constraint that binds this executor beyond the ruling's own
sentences is cited as **"the dispatch brief"**, never as the ruling.

| canon, verbatim | its home | how it binds here |
| --- | --- | --- |
| freeze-before-battery — freeze the instrument commit BEFORE the battery; artifact records the instrument hash (paraphrase) | **ruling #266.3(c)** | COMMIT 1 lands this §P + the probe; the artifact records `instrumentSha256` and `headAtRun`. **No battery seed was walked before it.** |
| *"the hashed body is built from an explicit ALLOWLIST SCHEMA — a field not in the schema never enters the body; forbidden-name lists are retired"* | **PC-T0-LATENCY-SEAM.md §COMMANDER CORRECTIONS item 1** | `BODY_SCHEMA` is the 20-key allowlist (GC-T1B's 18 plus `sizing` and `sensitivity`); `hashedBodySha256` is computed LAST, over the final gate values |
| mutant liveness — every gate conjunct provably alive, exactly-one enforced, or the probe refuses to run (paraphrase) | **ruling #268.3(a)** | `gPriceFires` gates the price's EVALUATION (the corrected form, #334 item 4), `gArmsDiverge` gates the bite, `gAlternativesLive` gates the DLC machinery's delivery, and **`gN` can fail** (§N) |
| per-seed cells — per-seed/per-cluster cells stored so every headline re-derives (paraphrase) | **ruling #282.2(ii)** | `perSeedCells` stores BOTH arms' full rows per cell, all 800 of them — and the sensitivity face is computed from nothing else |
| *"the re-derivation gate covers EVERY published face; a percentile face requires stored bins"* | **PC-C0-REACTION-BASELINE.md §COMMANDER CORRECTIONS item 4** | `gFaces` re-derives every face, every Δ point estimate, every stored bin table, the composition relations, the quotations, **the SIZING rows, the SENSITIVITY rows** and the VERDICT itself off the SERIALIZED artifact |
| *"a field carries the unit its name claims"* | **ruling #294 item 3** | every `…PerMatch` face is on the 240 s match clock; every `…Share` is a share of its own named denominator; `…Metres` is metres |
| *"a src-extracted constant pins its extraction to the NAMED call site — anchored match + line receipt — never first-occurrence"* | **BK-C0-BODYBALL-CENSUS.md §COMMANDER CORRECTIONS item 1** | the shell, the open-lane line and `KICK_COOLDOWN` are anchored with occurrence counts and line receipts |
| *"a seam-map gate pins occurrence COUNTS per needle and enumerates EVERY occurrence's site"* | **PC-C0-REACTION-BASELINE.md §COMMANDER CORRECTIONS item 1** | `gSeamSitesPinned` re-asserts at battery time: ONE `bkGroundCorridor` fork, ONE pricer statement, ONE hazard call, ONE definition, ONE `dlcDeliveryChoice` fork, ONE `dlcStrikePlane` fork, ONE PRECEDENCE GUARD, and ZERO of the three doors in `a4World.ts` |
| *"a stage doc's prose quotes artifact FIELDS verbatim or the number becomes a gated face"* | **PC-T2-ARMED-WORLD-READ.md §COMMANDER CORRECTIONS item 4** | every number in §R below is a FIELD of the committed artifact at source precision |
| *"a starred finding states its \|Δ\|÷half-width ratio"* | **BU-T0B-PRICE-SEPARATION.md §COMMANDER CORRECTIONS item 2** | every paired Δ publishes `absDeltaOverHalfWidth` |
| *"a scored face's walk-side predicate is pinned — anchored extraction or fixture — because the re-derivation gate proves arithmetic, not definitions"*; REFINED at #334 item 2 | **DF-T3-SURFACE-EXAM.md §COMMANDER CORRECTIONS item 2** (+ **BK-T3 §CORR item 2**) | `klassOf` · `isDelivery` · `isGroundLaunch` · `isMeasurableGroundPass` are PURE functions called by BOTH the walk and a published fixture table, gated by `gWalkFixtures`. **They are GC-T1B's, byte for byte.** |
| *"a dose-source guard should hash the bytes it reads, not a self-declared field"* | **BU-T1-MT-COMPOSITION.md §COMMANDER CORRECTIONS item 6** | BK-C2's, GC-T1's **and GC-T1B's** quoted numbers are READ out of their committed artifacts with the bytes hashed first, never re-typed from prose. **The sizing inputs are in that class** (§N) |
| ⭐ provenance hashes are COPIED from the artifact's own field, never from a terminal scroll-back (paraphrase) | **ruling #345 item 1 (the standing order)** | GC-T1B's `hashedBodySha256` is published by COPYING that field out of its artifact; the file's own BYTE hash is published separately and re-checked by `gFaces` |
| ⭐⭐ **dose placement** — *"dose NEVER in info.genome; truth-dosing writes census values through the shipped writer."* (paraphrase); ratified form = the match-local-copy idiom PLUS an info.genome-cleanliness world conjunct | **ruling #270.2** (+ #334 item 1) | unchanged from GC-T1B §P1b: both genes on MATCH-LOCAL views only, `gGenomeClean` proves the franchise object carries NEITHER |
| ⭐ the geneOk VALUE-check order | **GC-T1-GROUND-CORRIDOR-EXAM.md §COMMANDER CORRECTIONS item 2** (the #345 rider) | `gGeneValuePinned` checks BOTH genes **by VALUE** on BOTH match-local views of BOTH teams, every walked match, and reads the DLC gene back through the SHIPPED `passLeadSupportWeight` map on the receipts |
| ⭐⭐ **composition proof** — *"any world arming a new seam alongside the CB/L3 stack proves the doors/lifecycle at THAT composition first."* (paraphrase) | **BU contract M-BU.2 (ruling #285)** | re-run in full at §P11 on **this** exam's own scratch seeds — inheritance is not proof |
| *"arming receipts, not football findings"* (receipts ≠ effect sizes) | **ruling #289 item 1** (+ BU-T1 §CORR item 5) | every composition-proof relation, `strikeAttributionCompleteness`, `priceEvalNonZeroShare` and the world receipts are labelled INSTRUMENT RECEIPTS wherever they appear |
| *"verifier scratch walks use the stage's own consumed band or the out-of-band scratch range (≥ 900,000,000) — never the next virgin block"* | **PW-T0C-OBJECTIVE-FIDELITY.md §COMMANDER CORRECTIONS item 6** | the COMPOSITION PROOF walks **900,000,500–502** and the PREFLIGHT SMOKE walked **900,000,600–602** only |
| seed discipline — BOOKED = WALKED reporting; blocks consumed whole of record (paraphrase) | the standing frontier practice (**rulings #286 item 5 onward**) | `gSeedsBookedEqualWalked` compares the CELLS' OWN distinct-seed set to the booked list and checks every walked seed is inside the block |
| clock honesty — every rate on the 240 s match clock or dual-axis (1 sim-s = 22.5 display-s); APPLIED values, never nominal (paraphrase) | **ruling #280.2(iii)** + PC-T2 §CORR item 3 | every per-match count carries the clock in its unit string; §人话 opens with the dual-clock declaration (#339) |
| *"WORKER-SIMMED fixtures play the SHIPPED world (League.toJSON omits matchFlags; …)"* | **ruling #283.2(iv)** | this probe builds `Match` DIRECTLY and never round-trips a League, so no worker fixture is generated |

## §P1 THE ARMS — IDENTICAL TO GC-T1B's SCORED PAIR, ONE CONSTRUCTION FLAG APART

| arm | construction | `bkGroundCorridor` | `dlcDeliveryChoice` | `dlcStrikePlane` | `dvExposureWeight` | `passLeadSupport` |
|---|---|---|---|---|---|---|
| ⭐ **`shut`** | `a4MatchFlags(11)` + both DLC doors + `armA4World(m, null, 11)` + the gene | **false** | true | true | **0.5** | **1** |
| ⭐ **`armed`** | THE SAME, plus `bkGroundCorridor: true` | **true** | true | true | **0.5** | **1** |

* **THE WORLD'S OWN COMPOSER IS CALLED, NEVER COPIED** — `a4MatchFlags(CORRIDOR_WORLD_VERSION)`
  is the substrate and the three doors are the only literals. `gArmsIsolated` compares the two
  constructed worlds AS OBJECTS over `a4MatchFlags(11)`'s own key set plus the three doors, and
  requires the difference set to be **exactly `['bkGroundCorridor']`**.
* ⭐⭐ **THE DLC AXIS DOES NOT DIFFER BETWEEN THE ARMS** — asserted off the REAL constructed
  matches by `gWorld`'s `dlcAxisIdentical` conjunct, never merely stated.
* **SHARED VIRGIN SEEDS, PAIRED WALKS**: every seed is walked by both arms and the bootstrap
  CLUSTER IS THE CELL, so every Δ below is paired by construction.
* **`passLeadSupport` is pinned at 1** (DLC-T1's own `PTP_GENE_MAX`, the value its CONTEST arms
  used) and **`dvExposureWeight` at world 11's own 0.5** in BOTH arms. ⛔ **NO WEIGHT LADDER ON
  EITHER AXIS.** Per-cell `geneOk` is a **VALUE** check (the #345 rider's order, #347 item 2's
  re-statement — now binding).
* ⛔ **GC-T1B's REPORTED-ONLY PLANE PAIR IS NOT RE-WALKED.** It was in no conjunct and its record
  stands; this block's seeds buy POWER on the scored pair instead. The plane-alone WORLDS remain
  in the composition proof as RECEIPTS (§P11), so the precedence law is still measured here and
  not inherited. **A declared departure from GC-T1B's arm list, and the only one** (§DEV 2).

## §N ⭐⭐ THE SIZING, SHOWN — n FROM GC-T1B's OWN PUBLISHED VARIANCES

**The house form** (DV-T1B §N / DF-T3B, applied face by face). Every input is a **FIELD** of
`docs/world-model/data/gc-t1b-alternatives-arm.json`, read from bytes that are **hashed first**;
`gN` reds if the source artifact has drifted.

```text
1  half-width   = (ci95[1] − ci95[0]) / 2                     ← GC-T1B's PUBLISHED field
2  se(160)      = half-width / z.975      (z.975 = 1.959963985)
3  se(needed)   = |target| / (z.975 + z.80)   (z.80 = 0.8416212336; sum = 2.8015852186)
4  N            = ceil( 160 · (se(160) / se(needed))² )
```

Step 4 uses the fact that a **paired cluster** bootstrap's SE scales as `1/√N` in the number of
**clusters**, and here the cluster IS the match seed.

⭐ **THE TARGET MAGNITUDES ARE THE DISPATCH'S, NAMED FACE BY FACE** (#347 item 2). For (a1) and
(c) that is the **OUTLIER-ROBUST** magnitude — #346 item 1's leave-one-out point, ⛔ **not** the
fragile headline. The robust points are **RE-DERIVED IN-PROBE from GC-T1B's own stored cells**
(find the max-single-seed-influence cell, recompute the paired point without it), never re-typed
from #346's rounded interval; `gN` proves the accessors are the same definitions by requiring
them to reproduce GC-T1B's **published** full-sample Δ exactly, and requires each derived robust
point to lie inside #346's own quoted interval.

| face | conjunct | GC-T1B hw | se(160) | target magnitude | kind | se(needed) | **N required** |
|---|---|---:|---:|---:|---|---:|---:|
| `groundStrikesPerMatch` | **(a1)** | 1.865625 | 0.95186698 | **1.57232704** | ⭐ OUTLIER-ROBUST (LOO point; the seed dropped is **12,525,152**, influence 0.58392296) | 0.56122763 | **461** |
| `caromedGroundOnOpenLaneShare` | **(a2)** | 0.02528599 | 0.01290125 | **0.01397195** | T1B's own Δ (no #346 LOO point exists for this face) | 0.00498716 | **1071** |
| `teammateStrikesPerMatch` | **(c)** | 1.175 | 0.59950081 | **0.52830189** | ⭐ OUTLIER-ROBUST (LOO point; same seed **12,525,152**, influence 0.45919811) | 0.18857249 | **1618** |
| `crossesPerMatch` | **(d) control 2** | 0.353125 | 0.18016913 | **0.3125** | T1B's own Δ (the dispatch names the crosses ambiguity) | 0.11154399 | **418** |

**CONSISTENCY WITH #346's OWN QUOTED INTERVALS** (a `gN` conjunct, not a comment): the derived
robust point **−1.57232704** lies inside **[−2.69, +0.05]**, and **−0.52830189** lies inside
**[−1.02, +0.26]**. The 27 % / 47 % Δ-shares #346 published re-derive too:
0.58392296 ÷ 2.15625 = **27.08 %**, 0.45919811 ÷ 0.9875 = **46.50 %**.

**N required (max) = 1,618. The block holds 1,000 seeds and the dispatch caps the battery at
≈800 pairs. So the cap BINDS and**

> ### ⛔ **N_FROZEN = 800 PAIRED SEEDS — AND TWO OF THE FOUR FACES CANNOT BE BOUGHT INSIDE ONE BLOCK. SAID HERE, BEFORE THE BATTERY.**

| face | conjunct | N required | **MDE at n = 800** | resolvable at 800? |
|---|---|---:|---:|---|
| `groundStrikesPerMatch` | (a1) | 461 | **1.19260080** | ✅ **YES** — the robust magnitude 1.572 clears it |
| `crossesPerMatch` | (d) control 2 | 418 | **0.22573516** | ✅ **YES** — the Δ magnitude 0.3125 clears it |
| `caromedGroundOnOpenLaneShare` | (a2) | **1071** | **0.01616407** | ❌ **NO** — the magnitude 0.01397 is BELOW the MDE. Needs **1.34 blocks** |
| `teammateStrikesPerMatch` | (c) | **1618** | **0.75111876** | ❌ **NO** at its ROBUST magnitude 0.5283. Needs **2.02 blocks**. (It IS powered against its full-battery headline 0.9875 — that needs 463 — but that headline is the outlier-fragile one, so the honest reading is UNDERPOWERED) |

**WHAT THAT MEANS FOR THE VERDICT, PRE-COMMITTED**: (a2) and (c) are declared **UNDERPOWERED
HERE**. Whatever they read, **a non-resolution on those two is NOT evidence of no effect** — it
is a sample-size statement, and the honest next step for either is a further block, not a
re-cut. ⛔ **The predicates are NOT relaxed to compensate. Never promise power you do not have.**

**MDE bought at n = 800** = `se(160) · √(160/800) · 2.8015852186`; the shrink factor is
`√(160/800)` = **0.4472136**.

⚠ **WHAT THIS RULE ASSUMES, DECLARED.** That this battery's per-seed cluster variance is
GC-T1B's. Same world, same arms, same estimator, the same walk-side predicates — but **different
seeds**, so it is an assumption. The probe publishes the **ex-ante expected** half-width beside
the **realised** one for every sizing face, so the reader can see whether it held:
`exAnteExpectedHalfWidth` = `se(160) · √(160/800) · z.975` (a1 **0.83433**, a2 **0.01131**,
c **0.52548**, d **0.15793** — half-widths on the paired Δ). ⚠ These are the SIZING RULE's own
projections and enter no conjunct.

⚠ **AND WHAT IT DOES NOT CLAIM.** Resolving an estimate is not the same as the estimate being
real, and 80 % power is 80 % power: a face sized at its target can still fail to resolve one time
in five even if the target is exactly true.

## §P3 H-GC.2(a) — THE GROUND-STRIKE FACES FALL RESOLVEDLY

> **FROZEN RULE, GC-T1B §P3's VERBATIM.** At **BOTH** named faces, the **PAIRED per-cell
> bootstrap Δ (`armed` − `shut`)** has its **95 % interval ENTIRELY BELOW ZERO** (the house
> resolution form).

| face | what it is |
|---|---|
| ⭐⭐ `groundStrikesPerMatch` | attributed body strikes on a **GROUND** flight, per match — published as a per-match **COUNT** so a fall cannot be manufactured by a shrinking denominator |
| ⭐⭐ `caromedGroundOnOpenLaneShare` | of the measured ground passes that ACTUALLY caromed, the share played on a line the chooser's OWN gate called OPEN (BK-C2 §R2(i)'s face verbatim) |

* **THE PAIRED FORM**: the same cell in both arms is one pair; each of the 2,000 bootstrap draws
  resamples CELLS once and re-derives BOTH arms' pooled ratios inside that draw. Percentile
  interval, resample rng seeded from the block base **12,526,000**.
* `absDeltaOverHalfWidth` is published for every Δ (canon: starred |Δ|÷half-width).
* ⚠ **(a2) IS UNDERPOWERED AT THIS n AND §N SAYS SO.** The rule is not softened for it.

## §P4 H-GC.2(b) ⭐ NON-SUPPRESSION — THE BAND, FROM THIS EXAM'S OWN SHUT ARM

> **FROZEN RULE, GC-T1B §P4's construction VERBATIM.** `armed.groundPassesPerMatch`'s point
> estimate sits **AT OR ABOVE THIS EXAM'S OWN `shut` ARM'S 95 % interval's LOWER EDGE**.

* **THE BAND IS DERIVED FROM THIS BATTERY'S OWN CONTROL ARM** — BK-T4 §P3 / GC-T1 §P4 / GC-T1B
  §P4's form exactly, applied to **THIS exam's shut arm**. **No taste constant**, and **not
  GC-T1B's 76.98125** — that number belongs to a different block. The band is
  `faces['shut.groundPassesPerMatch'].ci95[0]`, computed by the same bootstrap as every other
  interval and published beside the verdict.
* ⚠⚠ **THIS CONJUNCT GETS HARDER AT POWER, AND THAT IS DELIBERATE.** Five times the clusters
  narrows the shut arm's own interval by ≈ √5, so the floor the armed point must clear rises
  toward the shut point estimate. GC-T1B's (b) passed with 0.87 passes a match of margin against
  a band 2.03 below its own shut point; at n = 800 that slack is ≈ 0.91. **A (b) that passed only
  on a wide band will not pass here, and that is the test doing its job — not a re-cut.**
* **THE UNIT IS PER MATCH** on the engine's own 240 s match clock (one row = one match).
* ⛔ **IF (a) PASSES ONLY WHERE VOLUME COLLAPSES, (b) FAILS AND THAT IS THE RESULT.**
* **REPORTED BESIDE IT, GATED BY NOTHING**: `passCompletion` · `possessionSpellSeconds` ·
  `possessionFlipsPerMatch` · `flipsCaromLastContactShare` · `deliveriesPerMatch`.

## §P5 H-GC.2(c) THE TEAMMATE FACE · H-GC.2(d) THE LOFTED CONTROLS

> **(c) FROZEN RULE, VERBATIM.** `teammateStrikesPerMatch` — attributed strikes whose struck body
> is the PASSER'S OWN TEAMMATE, per match — has its **paired Δ 95 % interval ENTIRELY BELOW
> ZERO**, the same form as (a). ⚠ It is **UNDERPOWERED at n = 800 against its outlier-robust
> magnitude** (§N: 1,618 pairs needed). This is the THIRD time this face is asked; GC-T1 missed
> by 0.0625, GC-T1B by 0.05625, and **the rule is still not re-cut.**

> **(d) FROZEN RULE, VERBATIM.** Each lofted-family control's **ARMED point estimate lies INSIDE
> THIS EXAM'S OWN `shut` arm's 95 % interval** [lo, hi] (BK-T4 §P2's control form):
> `loftedDeliveriesPerMatch` · `crossesPerMatch`.

⚠⚠ **(d) ALSO GETS HARDER AT POWER, FOR THE SAME REASON AS (b)** — it is a LEVEL test against a
band that narrows like 1/√n. GC-T1B's crosses missed by 0.0125 on a band of half-width 0.2875;
at n = 800 that band is ≈ 0.129 wide either side. **Declared before the run.** §N sizes the
crosses **Δ** (which does not narrow-shift the way the level test does) so the reader can tell
"the price moved crosses" from "the band got tight" — the Δ requirement 418 is met, the level
test is the frozen conjunct, and **both are published**.

⚠ **THE CONTROLS ARE NOT PERFECTLY INSULATED AND THAT IS DECLARED** (GC-T1 §P5's limit, inherited
through GC-T1B): the ground price changes what the chooser picks at ONE argmax that also contains
the lofted switch, so a *substitution* effect can move a lofted volume without the term ever
being applied to it. (d) tests that the price does not REACH the flighted lines; it cannot
separate "not priced" from "not substituted into".

## §P6 ⭐ REPORTED, NEVER GATED — THE RE-AIM SIGNATURE AND THE REST

* ⭐⭐ **THE RE-AIM SIGNATURE, GC-T1B §P6's OWN PRE-REGISTERED READING RULE RE-APPLIED.** The
  `jointLaneOpenByShellBlocked` cells (`[laneOpen, laneContested]` × `[shellBlocked, shellClear]`)
  per arm, their within-pair cell differences, the blocked/clear column deltas, and
  `groundPassesPerMatch` + `deliveriesPerMatch` beside them. **The reading rule, stated before the
  battery (GC-T1B's words, unchanged)**: *a RE-AIM signature is blocked mass falling while the
  CLEAR column RISES and the delivery volume holds; a SUPPRESSION signature is blocked mass
  falling with the clear column flat and the deliveries falling too.* GC-T1's and GC-T1B's
  published cells are quoted beside ours as ⚠ **DIFFERENT-BATTERY CONTEXT** — different blocks,
  and for GC-T1 a different world.
* **THE USAGE SHARES** (GC-T1B §P2b's two instruments, unchanged: `ledDeliveredShare` and
  `ledDeliveredShareSupportScoped`, ⛔ **usage, never a decline rate** — DLC-T1s's #243 item 1
  retraction rides in full, and the liveness conditioning it requires is still unavailable here).
* **completion + possession spells** · **the interception decomposition** · **goals/shots** ·
  **the strike anatomy split** (`bySideTeammateOpponentNoFlight`) · **perf**.
* ⛔ **NO SEASON LADDER.** #347 item 2 orders none; the block is not spent on it.

## §P6B ⭐⭐ THE LEAVE-ONE-OUT SENSITIVITY FACE — PRE-REGISTERED, REPORTED, GATED BY NOTHING

**RULING #346 item 1's standing order, verbatim in substance**: *"A future exam pre-registers an
outlier-sensitivity face (leave-one-out influence, REPORTED) alongside its primary faces."*

For **every scored face** — both (a) faces, (b)'s face, (c)'s face and **both** (d) controls —
the artifact publishes:

1. **THE INFLUENCE CENSUS** — all n cells are examined; the cell whose removal moves the scored
   statistic FURTHEST is named (`maxInfluenceSeed`), with the movement (`maxInfluence`) and its
   share of the full Δ (`influenceShareOfDelta`).
2. **THE LEAVE-THAT-ONE-OUT RE-BOOTSTRAP** — the #346 verifier's own procedure, run with **this
   exam's own estimator** on n − 1 cells (2,000 draws, its own rng stream seeded from
   `BLOCK_BASE + 1`). For a resolution face that is the Δ interval; for a level face ((b), (d))
   it is the SHUT band **and** the ARMED point, both recomputed without that cell.
3. **`conjunctFlips`** — whether the **FROZEN predicate** reads differently on the LOO draw.

> ⛔⛔ **NO GATE READS THIS FACE, AND NOTHING IS TRIMMED.** The primary faces are the **FULL-n
> UNTRIMMED** readings and they alone carry H-GC.2's verdict. No seed is ever dropped from a
> published face. The sensitivity reading stands **beside** the primary one and **the commander
> reads both** (#347 item 2). A face that flips is reported as **FRAGILE**, exactly as (a1) and
> (c) were at #346 — it is not re-scored, re-cut, or quietly upgraded.

## §P7 THE PERF FACE — GC-T1 §P7's METHOD, INHERITED VERBATIM

`wallSecondsPerMatch`, per arm. **METHOD**: each walk is timed end to end (`Date.now()` around
the walk); the two arms are walked **BACK TO BACK on the same seed** (shut first, armed second),
so scheduler and thermal drift are spread across both arms rather than concentrated in one; the
face is Σ wall seconds ÷ walks, and its paired Δ is published like any other.

⚠ **WHAT IT MEASURES AND WHAT IT DOES NOT.** The timed region is the **WALK**, not the engine
alone: the observer's own `laneOpenness` and `groundShellHazard` reads and the `performPass`
trace sit inside it **in EVERY arm**, so the **DIFFERENCE** is the priced chooser's cost and the
**LEVEL** is not the game's frame cost. It is a **MACHINE reading on one machine**, never a
portable number.

## §P8 THE GATES (frozen; a red gate is REPORTED, never patched, and ROUTES THE ARTIFACT)

`gWorld` · `gArmsIsolated` · `gSharedSeeds` · `gAnchoredConstants` · `gSeamSitesPinned` ·
`gWalkFixtures` · `gStrikeLedgerAgrees` · `gStrikeAttributionComplete` · `gJointPartition` ·
`gPriceFires` · `gArmsDiverge` · `gQuotationsFaithful` · **`gN`** · `gGenomeClean` ·
`gGeneValuePinned` · `gCompositionProof` · `gAlternativesLive` · `gNonVacuous` · `gSrcUntouched` ·
`gSeedsBookedEqualWalked` · `gFaces` — **21 gates** (GC-T1B's 20 plus **`gN`**).

1. ⭐⭐ **NO GATE THAT CANNOT FAIL** (#334 item 3). There is deliberately **no `gStatsZero`**: a
   hardcoded `true` is not a gate. Zero registry statistics are drawn anywhere in this file and
   the stats ledger is a **FIELD** (`stats.consumed = 0`).
2. ⭐⭐ **NO TAUTOLOGICAL GATE, AND NO GATE ON A DIRECTION.** `gPriceFires` is the corrected
   liveness form (#334 item 4); `gArmsDiverge` requires only that SOME paired cell differ (a bite
   receipt); `gAlternativesLive` requires only that the DLC machinery DELIVER a non-zero lead
   somewhere in every arm. **No gate asks any face to move in any direction**, and ⛔ **no gate
   reads the sensitivity face.**
3. ⭐⭐ **`gN` CAN FAIL, AND HERE IS HOW** (DV-T1B's G-N form): a drifted sizing source (the
   quoted deltas no longer match GC-T1B's hashed bytes), a sizing accessor that no longer
   reproduces GC-T1B's published Δ exactly, a derived robust point outside #346's own quoted
   leave-one-out interval, or **a battery that ran at an n other than `N_FROZEN`**. ⛔ A battery
   that quietly ran a different n reds the gate rather than publishing a smaller MDE as if it
   were the frozen one.
4. **`gFaces` FROM DISK** covers every published face, every Δ point estimate, every stored bin
   table, the composition relations, all three quotations, **every SIZING row's arithmetic**,
   **every SENSITIVITY row's point estimates and flip flag**, and **the VERDICT ITSELF** (the
   four conjuncts are re-evaluated from the serialized faces and deltas and compared to the
   published verdict).
5. **BOOKED = WALKED FROM THE CELLS** (#335 item 4): `gSeedsBookedEqualWalked` derives the walked
   set from the CELLS' own distinct seeds, checks the count against the booked list, checks the
   walk arithmetic, and checks every walked seed lies inside the authorized block and every
   composition seed is out-of-band scratch.
6. ⭐ **THE RED-ROUTING IDIOM, IN CODE** (#334 item 5): `outPath = ALL_GREEN ? OUT : OUT +
   '.RED.json'` is the instrument's own line, evaluated after `gFaces`.

## §P9 SEEDS, SIZING AND STATS

* **BLOCK 12,526,000–999**, opened by #347 item 2 and **consumed WHOLE of record**. The sub-band
  split, declared here:
  * ⭐ **the SCORED pair** = **12,526,000–799** (**800** seeds × 2 arms = **1,600 walks**);
  * **world-construction receipts** = **12,526,999**, one per arm (**2 constructions**);
  * ⛔ **no plane sub-band** — GC-T1B's reported pair is not re-walked (§P1).
  * **BOOKED = WALKED**: `gSeedsBookedEqualWalked` requires **1,602** against 800 distinct
    scored seeds.
* **OUT-OF-BAND SCRATCH ONLY** (canon: verifier scratch seeds): the COMPOSITION PROOF walks
  **900,000,500–502** (7 worlds × 2 trace states × 3 seeds = **42 walks**) and the PREFLIGHT
  SMOKE walked **900,000,600–602**. **No battery seed was walked before this freeze.**
* **SIZING**: §N, from GC-T1B's published variances. ⛔ **NO SIZING SMOKE WAS RUN** — this exam
  does not need one, because the variance it sizes against is a published field rather than a
  scratch estimate. (GC-T1B ran a 40-seed scratch smoke for exactly the reason this exam does
  not: it had no same-world paired variance to size from. It does now.)
* **STATS CONSUMED: ZERO.** Every interval — primary and leave-one-out alike — is a percentile
  bootstrap over the WALKED CELLS (the IN-T0 / DF-T2 / IN-T1 / BK-C1 / BK-C2 / GC-T1 / GC-T1B
  precedent, #329 item 4), not a registry-consuming statistic. Next stats base remains ≥
  **117,600**, registry of record **73**.

## §P10 HONEST LIMITS, STATED BEFORE THE BATTERY

1. ⛔⛔ **TWO OF THE FOUR OUTSTANDING FACES ARE UNDERPOWERED AT THIS n AND §N NAMES THEM**:
   (a2) needs 1,071 pairs, (c) needs 1,618 at its robust magnitude. A non-resolution on either is
   a SAMPLE-SIZE statement, not evidence of no effect.
2. ⚠⚠ **(b) AND (d) GET HARDER AT POWER** (§P4, §P5) — both are LEVEL tests against a band that
   narrows like 1/√n. A GC-T1B pass does not transfer.
3. ⚠ **NO CLAIM THAT THE CAROM DISAPPEARS** (contract §4): the price is a price, not a wall.
4. ⚠⚠ **THE FROZEN PRECEDENCE LAW STILL MAKES THE K = 9 GRID STRUCTURALLY INERT IN THESE ARMS**
   (measured again at §P11's `G-PRECEDENCE`). The alternatives the verdict is about are **the
   two-point contest's led candidates**.
5. ⚠⚠ **`o1PassWindup` STILL KEEPS PRECEDENCE OVER THE LEAD** (§P11's `O1-WINDUP-PRECEDENCE`;
   GC-T1B measured `altDisplacedShare` = 0 over 30,318 wind-up decisions). A delivered re-aim on
   this composition rides the ONE-TOUCH BYPASS only — **a NAMED DOOR, not this exam's to open.**
6. ⚠ **BOTH GENES ARE PINNED** (0.5 and 1). No ladder on either axis.
7. ⚠ **THE USAGE SHARES ARE NOT DECLINE RATES** (GC-T1B §P2b's limit rides in full).
8. ⚠ **THE CONTROLS CANNOT SEPARATE "NOT PRICED" FROM "NOT SUBSTITUTED INTO"** (§P5).
9. ⚠ **THE INTERCEPTION DECOMPOSITION IS TEMPORAL, NOT CAUSAL** (BK-C2 §P.7's own warning).
10. ⚠ **THE PERF FACE IS A MACHINE READING** (§P7), not a portable cost.
11. ⚠ **THIS EXAM'S SHUT ARM IS NEITHER GC-T1's NOR BK-C2's `w11` WORLD**, and it is not
    GC-T1B's shut arm either (different block). Every quotation from any of the three is labelled
    DIFFERENT-BATTERY CONTEXT, and **no Δ is computed across batteries**.
12. ⚠ **THE SIZING RULE ASSUMES GC-T1B's PER-SEED VARIANCE** (§N) — different seeds, so it is an
    assumption, and the realised half-widths are published against the ex-ante projections.
13. ⚠ **THE SENSITIVITY FACE IS A ONE-OUT FACE, NOT A ROBUSTNESS THEORY.** It answers "does the
    single most influential seed carry this verdict?"; it does not answer "do the two most
    influential", and it is not a trimmed estimator.
14. ⚠ **NO SEASON LADDER**, so nothing here is a claim about evolution or about the shipped
    League.

## §P11 ⭐⭐ THE COMPOSITION PROOF — RE-RUN, NOT INHERITED

Canon's own form. Seven worlds are constructed and walked to completion on the scratch seeds
**900,000,500–502**, twice each (traced / untraced), and compared by **WHOLE-MATCH SIGNATURE**
(tick · score · phase · ball · rng stream state · every body's pos/vel/heading/stamina):

| world | construction | relation |
|---|---|---|
| `base` | world 11, no DLC door, gene absent | the reference |
| `bothAbsent` | + BOTH doors, gene **ABSENT** | ≡ `base` — **G-BORN.bothDoors** |
| `bothZero` | + BOTH doors, gene **0** | ≡ `base` — **G-ZERO.contest** |
| `bothOne` | + BOTH doors, gene **1** — THE SCORED ARMS' WORLD | ≠ `base` — **G-BITE.bothOne** (⚠ an arming receipt, never an effect size) |
| `contestOnly` | + `dlcDeliveryChoice` ALONE, gene 1 | ≡ `bothOne` — ⭐⭐ **G-PRECEDENCE** |
| `planeAbsent` | + `dlcStrikePlane` ALONE, gene ABSENT | ≡ `base` — **G-BORN.planeAlone** |
| `planeOne` | + `dlcStrikePlane` ALONE, gene 1 | ≠ `base` — **G-BITE.planeOne** |

Plus **LIFECYCLE**, **CANDIDATES-FORM**, ⭐ **G-LOCKSTEP** (the `performPass` trace is INERT —
byte-identical signatures traced vs untraced) and ⭐⭐ **O1-WINDUP-PRECEDENCE** (the wind-up
seat's own aim displaced from the target's pre-step position on **exactly zero** decisions, in
every world).

⚠ **THESE ARE ARMING RECEIPTS, NEVER FOOTBALL FINDINGS**, and `gCompositionProof` is red if any
relation fails. **They are re-run here rather than inherited from GC-T1B: inheritance is not
proof.**

## §DEV-PREFLIGHT — the ONE thing seen before the freeze, disclosed HERE (GC-T1B §DEV 1's form)

⚠⚠ **A 3-SEED PREFLIGHT SMOKE ON SCRATCH SEEDS 900,000,600–602 WAS RUN AND ITS NUMBERS WERE
SEEN**, to prove the instrument runs and all 21 gates are reachable. It read (3 seeds, so pure
noise, and quoted here only because I saw it): `groundStrikesPerMatch` Δ **+2.333333** ·
`caromedGroundOnOpenLaneShare` Δ **+0.091599** · `groundPassesPerMatch` Δ **−8** ·
`teammateStrikesPerMatch` Δ **−0.333333** · `crossesPerMatch` Δ **−0.666667**; H-GC.2 read
(a) FAIL · (b) FAIL · (c) FAIL · (d) PASS on it. ⛔ **NOT ONE PREDICATE, AND NOT `N_FROZEN`, WAS
RE-CUT ON THE STRENGTH OF THAT SIGHT** — `N_FROZEN` comes from §N's arithmetic over GC-T1B's
published fields alone, and every conjunct form is GC-T1B's verbatim. If a conjunct fails on the
battery, that failure is the result.

---

<!-- ⛔ NOTHING ABOVE THIS MARKER IS EDITED AFTER THE FREEZE COMMIT. -->

# RESULTS

> Freeze `be42ac4` → this commit. **21/21 GATES GREEN**, so the artifact sits at the **CANONICAL
> path** `docs/world-model/data/gc-t2-power-extension.json` (the red-routing branch was live and
> not taken). **1,602 battery walks** (800 seeds × 2 arms + 2 world-construction receipts)
> **+ 42 composition-proof scratch walks**; `batteryWallSeconds` **180.417**.
> `gFaces` re-derived **88/88** face-and-Δ checks and **56/56** stored-bin / composition /
> quotation / **sizing** / **sensitivity** / **verdict** checks off the serialized artifact,
> 0 failures; **30/30** walk-side fixtures pass.
> `hashedBodySha256 = 80ab04ecbaf0280b86cefb4100eef935d442b629f699466834a8d6555c2a4ed2`.
> `attributedStrikes ÷ ledgerAppliedStrikes` = **1** in BOTH arms — ⚠ an instrument receipt,
> never a football finding.
>
> ⭐ **EVERY NUMBER BELOW IS A QUOTED ARTIFACT FIELD AT SOURCE PRECISION** (canon: doc-prose
> fidelity). No number in this section is computed here; where two faces are compared, both are
> quoted with their intervals and the comparison is stated in words.

## §R0 THE VERDICT, IN ONE LINE

**H-GC.2 STILL FAILS AS A CONJUNCTION — AND AT POWER THE PATTERN INVERTS: (a) FAIL · (b) ⛔ FAIL
(GC-T1B's headline PASS REVERSES) · (c) ⭐⭐ PASS (the third asking, the first pass) · (d) PASS.**

At 800 paired seeds the two faces #346 downgraded to OUTLIER-FRAGILE both come back **robust**:
the ground-strike count falls **−0.82625** with the whole interval below zero and the teammate
face falls **−0.6925** with the whole interval below zero — and **NOT ONE SCORED FACE FLIPS ON
LEAVE-ONE-OUT** (the largest single-seed influence anywhere is **0.09550375**, against GC-T1B's
**0.45919811** on the same face). But the conjunct GC-T1B passed — 「他没有少传」 — **fails
here**: the armed arm plays **77.64375** ground passes a match against a band of **78.9125**, and
the paired Δ **−2.31875** [−3.17375, −1.54375] is **2.8451 half-widths** clear of zero. ⭐⭐
**THE VOLUME DOES FALL. GC-T1B's (b) PASS WAS AN UNDERPOWERED PASS.** ⛔ Reported exactly as
frozen. Nothing is re-cut.

## §R1 ⭐⭐ THE SIZING, CHECKED AGAINST WHAT IT BOUGHT

`gN` GREEN. `nFrozen` **800**, `capBinds` **true**, `ranAtFrozenN` **true**. The §N arithmetic
is re-derived from disk by `gFaces` (four rows, every step).

| face | ex-ante EXPECTED Δ half-width at n = 800 | ⭐ REALISED Δ half-width | did the assumption hold? |
|---|---:|---:|---|
| `groundStrikesPerMatch` (a1) | **0.83433286** | **0.61** | **BETTER than assumed** |
| `caromedGroundOnOpenLaneShare` (a2) | **0.01130824** | **0.01168361** | held (slightly worse) |
| `teammateStrikesPerMatch` (c) | **0.52547597** | **0.40375** | **BETTER than assumed** |
| `crossesPerMatch` (d) | **0.1579223** | **0.1525** | held |

⭐⭐ **AND THE REASON THE TWO "BETTER" ROWS ARE THE TWO OUTLIER-FRAGILE ONES IS ITSELF THE
FINDING.** §N sized (a1) and (c) using GC-T1B's published half-widths — **which the 12,525,152
outlier had inflated**. So the ex-ante power statement was CONSERVATIVE on exactly those two
faces, and both resolved despite §N's declaration. ⚠ **This is stated as an explanation of a
pre-registered projection, not as a new instrument**: §N's numbers were frozen before the battery
and neither the rule nor the predicates were touched afterwards.

⛔ **THE ONE HONEST SHORTFALL SURVIVES**: (a2) was declared UNDERPOWERED at §N (MDE
**0.01616407** against a target of **0.01397195**) and it is still unresolved here — its realised
half-width **0.01168361** buys a minimum detectable effect of the same order, and its point
estimate has **changed sign** (see §R2). ⚠ (c) was ALSO declared underpowered against its
outlier-robust magnitude **0.52830189**; it passed anyway, because the realised Δ (**−0.6925**)
is larger than that magnitude and the realised half-width smaller than projected. **A declared
shortfall that turns out not to bind is still an honest declaration — it was not known ex ante.**

## §R2 H-GC.2(a) — THE COUNT FALLS ROBUSTLY; THE STALE-MAP SHARE MOVES THE OTHER WAY ❌ (1 of 2)

| face | shut | armed | paired Δ | Δ ci95 | \|Δ\|÷hw | interval below 0? |
|---|---|---|---|---|---|---|
| ⭐⭐ `groundStrikesPerMatch` | **17.56625** [17.0225, 18.095] (14053/800) | **16.74** [16.205, 17.2925] (13392/800) | **−0.82625** | [−1.415, **−0.195**] | **1.3545** | ✅ |
| ⛔ `caromedGroundOnOpenLaneShare` | **0.4955045** [0.48596132, 0.50535988] (5456/11011) | **0.50240015** [0.49196692, 0.51271431] (5233/10416) | **+0.00689566** | [−0.00461787, +0.01874934] | **0.5902** | ❌ |

**(a1) IS NOW A ROBUST RESOLUTION AND THAT IS THE #346 DOWNGRADE DISCHARGED.** The largest
single-seed influence on this Δ is **0.08907854** (seed **12,526,615**, `influenceShareOfDelta`
**0.107811**), and the leave-that-one-out re-bootstrap reads **−0.73717146** [−1.33166458,
**−0.14392991**] — **still entirely below zero**. ⚠ GC-T1B's own reading is quoted as
DIFFERENT-BATTERY CONTEXT (different block): there the same face read Δ **−2.15625**
[−4.15625, −0.425] with **one** seed supplying **0.58392296** of it. No Δ is computed across the
two batteries and none is implied.

⛔ **(a2) FAILS, AND IT FAILS DIFFERENTLY THIS TIME: THE POINT ESTIMATE IS NOW POSITIVE.** GC-T1B
read Δ **−0.01397195**; here it reads **+0.00689566**, and neither resolves. ⚠ §N declared this
face UNDERPOWERED at n = 800 before the battery, so **a non-resolution here is a sample-size
statement**. What the two batteries together do say, in words rather than in a computed Δ: **the
stale-map share has now been measured twice with opposite signs and never resolved.** The honest
reading of record is that the price does **not** visibly move which caroms happen on lines the
chooser called open — and the next probe of that face needs **1,071 pairs**, not another 800.

The base rate DOES move, and resolvedly: `groundCaromRate` **0.17212756** [0.16886716,
0.17515814] → **0.16768896** [0.16464014, 0.17068097], Δ **−0.0044386** [−0.0079783,
**−0.00088542**], **1.2516** half-widths. ⚠ It is in no conjunct.

## §R3 H-GC.2(b) ⛔ NON-SUPPRESSION FAILS — THE HEADLINE, AND IT REVERSES GC-T1B's

> Frozen band = **THIS EXAM'S OWN** shut arm's 95 % interval LOWER EDGE, **78.9125** ground
> passes per match. Armed point estimate: **77.64375** — **BELOW IT.**

| face | shut | armed | paired Δ | Δ ci95 | \|Δ\|÷hw |
|---|---|---|---|---|---|
| ⛔ `groundPassesPerMatch` | **79.9625** [78.9125, 80.94375] (63970/800) | **77.64375** [76.69125, 78.60125] (62115/800) | **−2.31875** | [−3.17375, **−1.54375**] | **2.8451** |

**(b) FAILS ON BOTH READINGS AT ONCE — the level test AND the Δ.** §P4 pre-registered that this
conjunct gets harder at power because the band narrows like 1/√n, and it did; but the Δ does not
depend on the band at all, and **the Δ is now resolved 2.8451 half-widths below zero.** ⚠
GC-T1B's (b), as DIFFERENT-BATTERY CONTEXT: **79.0125 → 77.85**, band **76.98125**, Δ **−1.1625**
[−3.00625, +0.69375], **0.6284** half-widths — **inside** its band, the PASS #346 made the
headline of. **At five times the sample, on the same arms and the same predicate, the same
direction resolves and the same band excludes it.** The sensitivity face agrees: max influence
**0.05421621** (`influenceShareOfDelta` **0.023382**), LOO Δ **−2.37296621** [−3.23028786,
−1.56320401], LOO band **79.05757196** vs LOO armed **77.61326658** — **no flip**.

⭐⭐ **AND THE RE-AIM SIGNATURE, READ AGAINST GC-T1B's OWN PRE-REGISTERED RULE (§P6), NOW READS
MOSTLY SUPPRESSION.** `jointLaneOpenByShellBlocked`, rows `[laneOpen, laneContested]` × cols
`[shellBlocked, shellClear]`, over every measured ground pass:

| arm | measured | open·blocked | open·clear | contested·blocked | contested·clear |
|---|---|---|---|---|---|
| `shut` | 63970 | **7432** | 34091 | 10200 | 12247 |
| `armed` | 62115 | **6211** | 34538 | 9392 | 11974 |

⚠ FIELDS of `reAimSignature.byPair[0]`, re-derived by `gFaces` from the cells above:
`cellDeltas` **[[−1221, +447], [−808, −273]]** · `blockedColumnDelta` **−2029** ·
`clearColumnDelta` **+174** · `measuredGroundPassDelta` **−1855** · `deliveriesDelta` **−1724**.

**THE RULE, FROZEN BEFORE THE BATTERY**: *a RE-AIM signature is blocked mass falling while the
CLEAR column RISES and the delivery volume holds; a SUPPRESSION signature is blocked mass falling
with the clear column flat and the deliveries falling too.* **The blocked column falls 2,029; the
clear column rises 174; the delivery volume does NOT hold** (`deliveriesPerMatch` **84.98125**
[84.0325, 85.8925] → **82.82625** [81.965, 83.7125], Δ **−2.155** [−2.9225, **−1.47375**],
**2.975** half-widths — **resolved down**). ⭐ **The signature reads SUPPRESSION with a small
re-aim component.** ⚠ GC-T1B's own cells, as DIFFERENT-BATTERY CONTEXT: blocked **−318** with
clear **+132** and deliveries unresolved. **In this battery 174 of the 2,029 blocked lines come
back as clear ground passes; in GC-T1B's it was 132 of 318.** ⚠ Two different batteries on
different blocks and different n — no Δ is computed between them; each fraction is a
within-battery statement of its own cells.

**REPORTED BESIDE (b), GATED BY NOTHING:**

| face | shut | armed | Δ ci95 |
|---|---|---|---|
| `passCompletion` | **0.58646151** [0.58287138, 0.59018] | **0.58824866** [0.58424363, 0.59216216] | [−0.00267697, +0.00626839] |
| `possessionSpellSeconds` | **4.33889244** [4.29705721, 4.38311922] | **4.37871443** [4.33387026, 4.4233888] | [−0.01010003, +0.09029933] |
| `possessionFlipsPerMatch` | **48.8425** [48.33875, 49.31] | **48.24375** [47.7275, 48.7375] | — |
| `flipsCaromLastContactShare` | **0.11283718** [0.10930692, 0.11615147] | **0.11105065** [0.10770619, 0.11449137] | — |
| `goalsPerMatch` | **3.4275** [3.3025, 3.55625] | **3.37125** [3.24, 3.50375] | [−0.21, +0.09375] |
| `shotsPerMatch` | **12.725** [12.4475, 13.0075] | **12.7125** [12.43, 13.00625] | — |

⭐ **THE POSSESSION FAMILY IS STILL FLAT** — completion, spell length, flips and goals all
unresolved. **The team that passes less is not keeping the ball better for it.**

⭐ **THE PHYSICS PER LINE CLASS IS UNCHANGED, so this is selection and not luck**:
`caromRateOnOpenLaneShellBlocked` **0.31633477** [0.30398466, 0.32884641] → **0.3344067**
[0.31967081, 0.34899006] and `caromRateOnOpenLaneShellClear` **0.09107976** [0.08787351,
0.09434348] → **0.09137761** [0.08834292, 0.09443892]. The price's own liveness census
`priceEvalNonZeroShare` falls **0.2756292** [0.2716435, 0.27956785] → **0.25119536** [0.24762347,
0.25516014], and `groundOpenLaneButShellBlockedShare` **0.11617946** [0.11350559, 0.11879756] →
**0.09999195** [0.09750077, 0.10243966].

## §R4 H-GC.2(c) ⭐⭐ THE TEAMMATE FACE PASSES ✅ · H-GC.2(d) THE LOFTED CONTROLS HOLD ✅

| face | shut | armed | paired Δ | Δ ci95 | \|Δ\|÷hw |
|---|---|---|---|---|---|
| ⭐⭐ `teammateStrikesPerMatch` | **7.6225** [7.2775, 7.99] (6098/800) | **6.93** [6.64, 7.23625] (5544/800) | **−0.6925** | [−1.10625, **−0.29875**] | **1.7152** |

**(c) PASSES — ON THE THIRD ASKING, WITH THE SAME PREDICATE THAT MISSED TWICE.** GC-T1 missed by
**0.0625** and GC-T1B by **0.05625** on the interval's upper edge; here the upper edge is
**−0.29875**. ⚠ Both prior numbers are DIFFERENT-BATTERY CONTEXT and **neither was used to
re-size or re-cut anything** — §N sized this face from GC-T1B's variance against #346's own
leave-one-out point, and §P5 froze the rule unchanged.

⭐ **AND IT IS ROBUST**: max single-seed influence **0.09550375** (seed **12,526,615**,
`influenceShareOfDelta` **0.137912**); LOO Δ **−0.59699625** [−0.99499374, **−0.21276596**] —
still entirely below zero. ⚠ At GC-T1B the same face's single-seed influence was **0.45919811**
and the LOO interval crossed zero. **The #346 fragility is gone, and it is gone because the
sample is five times larger, not because anything was trimmed.**

The anatomy agrees: `bySideTeammateOpponentNoFlight` **[6098, 8288, 6222]** → **[5544, 8179,
6689]** — the teammate bucket falls, the opponent bucket is flat, and the growth sits in the
no-live-flight bucket which enters **no scored face**. ⚠ Unlike GC-T1B, `strikesPerMatch` (ALL
attributed strikes) is now quiet: **25.76** [24.4425, 27.19] → **25.515** [24.4725, 26.63375],
Δ **−0.245** [−1.9075, +1.295], **0.153** half-widths. **GC-T1B's +2.81875 anomaly was the
12,525,152 cell; at n = 800 nothing like it appears.**

⭐ BK-C2 §R1(iii)'s sharpest fact survives again: the striking body's `perpDistanceFromLineAtKick`
median bin lower edge is **0.5** m in both arms (shut bins **[5669, 3066, 1691, 869, 622, 480,
369, 240, 212, 164, 133, 90, 781]**, armed **[5272, 2744, 1580, 828, 673, 449, 400, 272, 238,
200, 110, 101, 856]**) — **the man who gets hit was still standing on the line when the ball
left.**

| control | shut ci95 (the band) | armed point | inside? |
|---|---|---|---|
| `loftedDeliveriesPerMatch` | **5.01875** [4.85625, **5.18375**] | **5.1825** | ✅ (**0.00125** below the UPPER edge) |
| `crossesPerMatch` | **3.66625** [3.54125, 3.80375] | **3.73375** | ✅ |

**(d) PASSES, AND THE CROSS AMBIGUITY IS RESOLVED IN THE CONTROL'S FAVOUR.** GC-T1B's crosses sat
**0.0125** BELOW its band; here they sit inside it and the Δ is **+0.0675** [−0.08125, +0.22375],
**0.4426** half-widths — **unresolved, and now pointing UP rather than down.** §N sized the
crosses Δ for exactly this (requirement 418 pairs, met), so the reading is not a power excuse:
**at 800 pairs the ground price does not move the cross count.** ⚠⚠ **BUT LOFTED DELIVERIES PASS
BY 0.00125 OF A DELIVERY PER MATCH** — the armed point sits a hair under its own band's UPPER
edge, and its Δ **+0.16375** [−0.025, +0.35625] (**0.859** half-widths) is drifting up
unresolved. **Declared loudly: (d) passes, and one of its two controls passes by a margin
thinner than GC-T1B's crosses FAILED by.** §P5's limit rides in full — this conjunct cannot
separate "not priced" from "not substituted into", and a ground price that suppresses ground
volume is exactly the situation where lofted substitution would show.

## §R5 ⭐⭐ THE LEAVE-ONE-OUT SENSITIVITY FACE — NOT ONE SCORED FACE FLIPS

⛔ **REPORTED, GATED BY NOTHING, NOTHING TRIMMED.** `anyScoredFaceFlips` = **false**;
`facesThatFlip` = **[]**. Each row's max-influence cell is found over **all 800 cells**, and the
leave-that-one-out re-bootstrap is 2,000 draws on 799 cells, rng seeded from **12,526,001**.

| conjunct | face | max-influence seed | `maxInfluence` | share of Δ | LOO reading | primary | LOO | flips? |
|---|---|---|---:|---:|---|---|---|---|
| (a1) | `groundStrikesPerMatch` | 12,526,615 | **0.08907854** | **0.107811** | Δ **−0.73717146** [−1.33166458, −0.14392991] | PASS | PASS | **no** |
| (a2) | `caromedGroundOnOpenLaneShare` | 12,526,021 | **0.00083266** | **0.120752** | Δ **+0.006063** [−0.00500796, +0.01766901] | FAIL | FAIL | **no** |
| (c) | `teammateStrikesPerMatch` | 12,526,615 | **0.09550375** | **0.137912** | Δ **−0.59699625** [−0.99499374, −0.21276596] | PASS | PASS | **no** |
| (b) | `groundPassesPerMatch` | 12,526,527 | **0.05421621** | **0.023382** | band **79.05757196**, armed **77.61326658** | FAIL | FAIL | **no** |
| (d) c1 | `loftedDeliveriesPerMatch` | 12,526,621 | **0.01356227** | **0.082823** | shut [4.85106383, **5.19399249**], armed **5.17396746** | PASS | PASS | **no** |
| (d) c2 | `crossesPerMatch` | 12,526,233 | **0.010097** | **0.149585** | shut [3.53566959, 3.78473091], armed **3.73717146** | PASS | PASS | **no** |

⭐⭐ **THIS IS THE ANSWER TO #346's DOWNGRADE.** At GC-T1B one cell moved (a) by **0.58392296**
and (c) by **0.45919811**; here the largest movement anywhere in the six scored faces is
**0.09550375**. ⚠ Different batteries, so those two numbers are context — but within THIS
battery every scored verdict survives the removal of its own most influential seed.

⚠ **THE LIMIT §P10 item 13 DECLARED RIDES IN FULL**: this is a ONE-out face. It answers "does the
single most influential seed carry this verdict?" — not "do the two most influential", and it is
not a trimmed estimator. No seed was dropped from any published face.

## §R6 ⭐ REPORTED — THE COMPOSITION PROOF, THE USAGE, THE INTERCEPTIONS, THE PERF

**THE COMPOSITION PROOF — 10/10 RELATIONS HOLD, RE-RUN NOT INHERITED** (`gCompositionProof`
GREEN; 7 worlds × 2 trace states × 3 scratch seeds **900,000,500–502** = **42 walks**, compared
by whole-match signature). ⚠ ARMING RECEIPTS, NEVER FOOTBALL FINDINGS. `G-BORN.bothDoors` ·
`G-ZERO.contest` · `G-BITE.bothOne` · ⭐⭐ `G-PRECEDENCE` · `G-BORN.planeAlone` ·
`G-BITE.planeOne` · `LIFECYCLE` · `CANDIDATES-FORM` · `G-LOCKSTEP` · ⭐⭐
`O1-WINDUP-PRECEDENCE`. **Both precedence laws reproduce on this exam's own scratch seeds**: the
K = 9 grid is structurally inert beside the contest, and the wind-up seat's aim is displaced from
the target's pre-step position on exactly zero decisions.

**AND THE BATTERY CONFIRMS THE WIND-UP LAW AT SCALE AGAIN**: `altDisplacedShare` = **0** in BOTH
arms, over **37450** (shut) and **35629** (armed) wind-up-seat decisions — **73,079 decisions**,
share exactly zero. ⚠ That total is bookkeeping over two quoted fields, not a face.

**THE USAGE SHARES** (⛔ usage, never a decline rate — §P6's limit and DLC-T1s's #243 retraction
ride in full):

| arm | `performPass` calls | to a support target | led strikes (non-zero) | `ledDeliveredShare` | `ledDeliveredShareSupportScoped` | mean delivered lead (m) |
|---|---|---|---|---|---|---|
| `shut` | 56610 | 20222 | **973** | **0.01718778** [0.01611849, 0.01839072] | **0.04811591** [0.04519379, 0.051435] | **6.5928979** |
| `armed` | 54275 | 19888 | **932** | **0.01717181** [0.01593368, 0.01836556] | **0.04686243** [0.04370975, 0.04996978] | **6.47188435** |

⭐ **THE STRIKE_GUARD IDENTITY HOLDS IN BOTH ARMS** (`ledStrikesHandled === ledStrikesNonZero`,
973 = 973 · 932 = 932) — ⚠ an instrument receipt. And the price does **not** change how often the
alternatives are delivered: `ledDeliveredShare` Δ **−0.00001597** [−0.00147491, +0.00133404]
(**0.0114** half-widths), support-scoped Δ **−0.00125348** [−0.00524212, +0.0024256] (**0.327**).
⭐⭐ **So whatever the chooser did differently, it was not "played the led ball more" — and at
this n that statement is no longer a small-sample impression.**

**THE INTERCEPTION DECOMPOSITION (BK-C2 §R4's form):**

| face | shut | armed | paired Δ | Δ ci95 | \|Δ\|÷hw |
|---|---|---|---|---|---|
| `interceptionsPerMatch` | **29.32375** [28.88375, 29.7325] | **28.3125** [27.885, 28.72125] | **−1.01125** | [−1.4325, **−0.605**] | **2.4441** |
| `interceptionCaromPrecededShare` | **0.36327209** [0.35703049, 0.36917377] | **0.36238411** [0.35630148, 0.36843965] | **−0.00088799** | [−0.008233, +0.00658724] | **0.1198** |

⭐ **THE COUNT FALLS RESOLVEDLY AGAIN — AND IT FALLS BECAUSE THE PASSING FALLS WITH IT**, exactly
the mechanism GC-T1 showed and GC-T1B's flat passing removed. **The share is dead flat**: over a
third of everything this engine scores as an "interception" still has a body carom on the ball
before it, in both arms. ⚠ Temporal, not causal (§P10 item 9).

**THE PERF FACE (§P7's method):**

| arm | wall seconds total | walks | `wallSecondsPerMatch` |
|---|---|---|---|
| `shut` | **89.799** | 800 | **0.11224875** [0.1115875, 0.11303625] |
| `armed` | **90.618** | 800 | **0.1132725** [0.11234375, 0.1143] |

Paired Δ **+0.00102375** s [**+0.00009625**, +0.0019825], **1.0855** half-widths — **the priced
chooser's cost is now resolved above zero**, at about one millisecond a match on this machine.
⚠ Read §P7 before quoting: the timed region is the WALK (observer reads and the `performPass`
trace included, in every arm), so the DIFFERENCE is the number and the LEVEL is not the game's
frame cost. It is a machine reading on one machine.

## §R7 THE GATES

All twenty-one GREEN: `gWorld` (both arms world 11, the DLC axis identical) · `gArmsIsolated`
(the difference set is exactly `['bkGroundCorridor']`) · `gSharedSeeds` · `gAnchoredConstants` ·
`gSeamSitesPinned` (ONE GC fork, ONE pricer statement, ONE hazard call, ONE definition, ONE
`dlcDeliveryChoice` fork, ONE `dlcStrikePlane` fork, ONE PRECEDENCE GUARD, ZERO of the three
doors in `a4World.ts`) · `gWalkFixtures` (30/30) · `gStrikeLedgerAgrees` ·
`gStrikeAttributionComplete` · `gJointPartition` · `gPriceFires` · `gArmsDiverge` ·
`gQuotationsFaithful` · **`gN`** · `gGenomeClean` · **`gGeneValuePinned`** (both genes by VALUE
on both match-local views of both teams, every walked match; the DLC gene read back through the
shipped `passLeadSupportWeight` map on both receipts) · **`gCompositionProof`** (10/10) ·
`gAlternativesLive` · `gNonVacuous` · `gSrcUntouched` · `gSeedsBookedEqualWalked` · `gFaces`.

**THE SEAM'S OWN RECEIPT**: the production fingerprint
`57b0bdab389122af5e4cacd75c4e13020b8ff248a413a7fcd71cc6215ba4c673` is unmoved, and
`bkGroundCorridor`, `dlcDeliveryChoice` and `dlcStrikePlane` are still named by no world and no
preset.

**PROVENANCE, COPIED FROM THE ARTIFACTS' OWN FIELDS** (the #345 item 1 standing order):
GC-T1B's byte hash of record `0ac3cf84a7605db0b13b375a19ccc3ae85f47974b7eccd75ca7368fd4e046ebb`
(this artifact's `quotedContext.gcT1b.source.sha256`) and GC-T1B's own
`hashedBodySha256 = f6cf8262b3e803caa879e5aad686d245f421f6424e07d345dadaca6561bce207`
(`quotedContext.gcT1b.hashedBodySha256`, which the probe reads out of GC-T1B's artifact — **not**
from a terminal) · BK-C2's byte hash
`84a78ea92d895a0f3fc5afbf1af61ae0704a0527188cea7adb4411dc553cdcba` · GC-T1's byte hash
`8c99601e1f0f5b7126c2cd9f736525b64734841e3462b4d9faa732c5127b59c2`.

## §R8 SEEDS AND STATS, AS CONSUMED

Block **12,526,000–999 CONSUMED WHOLE**: the SCORED pair **12,526,000–799** × 2 arms = **1,600
walks** (booked = walked, gated from the CELLS' own distinct-seed set: 800 distinct seeds, 800
paired rows) · the **12,526,999** construction receipts, one per arm (**2**) — **1,602 total**.
The COMPOSITION PROOF walked the out-of-band scratch range **900,000,500–502** (**42 walks**) and
the PREFLIGHT SMOKE walked **900,000,600–602**; the bootstrap's own resample rng was seeded from
**12,526,000** and the leave-one-out resample's from **12,526,001**. ⛔ **NO PLANE PAIR WAS
WALKED.** **STATS CONSUMED: ZERO** — every interval, primary and leave-one-out alike, is a
percentile bootstrap over walked cells; registry of record stays **73**, next stats base ≥
**117,600**, next sim ≥ **12,527,000** (⚠ derived bookkeeping — block + 1,000 — not an artifact
quotation).

## §DOUBTS (declared)

1. ⭐⭐⭐ **THE HEADLINE OF #346 DID NOT SURVIVE POWER, AND THAT IS THE MAIN RESULT.** "Alternatives
   cure the suppression" was read off a conjunct that PASSED at 160 pairs on a band **2.03** wide
   and a Δ of **0.6284** half-widths. At 800 pairs the same predicate on the same arms FAILS on
   both readings and the Δ resolves at **2.8451** half-widths. ⚠ **This does not retract GC-T1B's
   published numbers** — they were correctly computed and honestly reported — it says the
   conclusion drawn from them was **underpowered**, which is precisely what a power extension
   exists to find out.
2. ⭐⭐ **WHAT SURVIVES IS SMALLER AND REAL: alternatives SOFTEN the suppression, they do not cure
   it.** GC-T1 (no DLC pair, different block) lost its ground passes with the clear column
   FALLING; GC-T1B and GC-T2 both show the clear column RISING while the blocked column falls.
   The re-aim component is real and it is small — **174 of 2,029** here. ⚠ A within-battery
   statement of stored cells, not a cross-battery Δ.
3. ⭐⭐ **THE #346 DOWNGRADE IS DISCHARGED IN BOTH DIRECTIONS.** (a1) and (c) are no longer
   outlier-fragile: at n = 800 they resolve, and they survive their own leave-one-out. ⭐ (c) has
   now been asked three times and passed once, at the only sample size that could see it.
4. ⛔ **(a2) IS THE ONE FACE THIS ARC HAS NEVER SEEN.** Twice measured, opposite signs, never
   resolved, and §N declared it underpowered here BEFORE the run (needs **1,071** pairs). ⚠ **A
   non-resolution on it is not evidence that the price leaves the stale map alone** — that
   distinction is the whole point of §N's declaration.
5. ⚠⚠ **(d) PASSED, BUT ONE CONTROL PASSED BY 0.00125.** `loftedDeliveriesPerMatch`'s armed point
   sits just under its own band's upper edge with an unresolved upward Δ. Read with §P5's
   declared limit and with (b)'s resolved fall, the substitution reading — ground volume down,
   lofted volume drifting up — is **consistent with the numbers and not established by them**.
   ⭐ 有故事就要有探针: it is a LABELLED HYPOTHESIS and the probe that would settle it (the
   argmax's runner-up) is still unbuilt.
6. ⭐ **THE SIZING'S OWN ASSUMPTION WAS CONSERVATIVE ON EXACTLY THE TWO OUTLIER-INFLATED FACES**
   (§R1). Honest, but worth naming as a mechanism rather than luck: **an outlier inflates the
   variance you size from, so sizing off an outlier-contaminated battery over-buys.** That is a
   reusable lesson, and it cuts the other way too — sizing off a battery that happened to be
   quiet would under-buy.
7. ⚠⚠ **TWO PRECEDENCE LAWS STILL NARROW WHAT WAS TESTED** (§R6): the K = 9 grid is inert beside
   the contest, and `o1PassWindup` strips the lead off every wound-up pass (**73,079** wind-up
   decisions, share exactly 0). **A world that wanted the alternatives DELIVERED rather than
   merely PRICED must resolve the wind-up seat's precedence — still a NAMED, UNMEASURED door.**
8. **ONE COMPOSITION, TWO PINNED GENES, NO LADDER, NO SEASON LADDER.** DV + GC double-arming
   remains unmeasured (contract §4); nothing here is a claim about evolution or the shipped
   League.
9. **THE PERF DIFFERENCE IS NOW RESOLVED** (+0.00102375 s/match, 1.0855 hw) where GC-T1B's was
   not — ⚠ a MACHINE reading on one machine, quoted as an order of magnitude and never as a
   portable cost.
10. ⚠ **THE SENSITIVITY FACE IS ONE-OUT ONLY** (§P10 item 13), and nothing here is a robustness
    theory.

## §DEV — the deviations, declared

1. ⭐⭐ **GC-T1B's REPORTED-ONLY PLANE PAIR WAS NOT RE-WALKED**, declared at §P1 in the freeze
   commit. It was in no conjunct of H-GC.2 and its record stands; the block's 1,000 seeds bought
   power on the scored pair instead. **The plane-alone WORLDS stayed in the composition proof**,
   so `G-BORN.planeAlone` and `G-BITE.planeOne` are still MEASURED here rather than inherited.
   ⛔ The dispatch's arms were built EXACTLY AS ORDERED.
2. ⚠⚠ **A 3-SEED PREFLIGHT SMOKE ON SCRATCH SEEDS 900,000,600–602 WAS SEEN BEFORE THE FREEZE**,
   and its Δ point estimates were disclosed at §DEV-PREFLIGHT in the freeze commit itself
   (GC-T1B §DEV 1's ratified form). **Not one predicate and not `N_FROZEN` was re-cut** after
   that sight or after the battery. ⛔ **NO SIZING SMOKE WAS RUN AT ALL** — §N sizes from
   published fields, which is why none was needed.
3. ⭐ **A NEW GATE WAS ADDED: `gN`** (21 against GC-T1B's 20). It is the power rule's own gate and
   it can fail; §P8 item 3 enumerates how.
4. ⭐ **THE ARTIFACT GREW TWO ALLOWLIST-SCHEMA KEYS** (`sizing`, `sensitivity`), both covered by
   `gFaces` from disk. No published GC-T1B face was removed.
5. **THE GENE-WRITING IDIOM, THE NON-INHERITED FIDELITY GATE, THE USAGE-SHARE LIMIT AND THE
   ABSENCE OF LADDERS ARE GC-T1B's, UNCHANGED** (its §DEV 3–6 ride verbatim in substance).
6. **`PROGRAMME.md` / the rulings file are NOT edited by this session** (executor iron rule:
   governance files are the commander's). The queue's status line, the verdict of record and the
   next dispatch are the commander's to write.

## §人话 — 样本给够了以后,上一版那个好消息没站住

> ⚠ **先说钟**(#339 立的双钟法条):我们一场球显示钟走满 90 分钟,按 sim 秒直读只有 240 秒
> (1 sim 秒 = 22.5 显示秒)。下面所有「每场几次」都按**我们这一场**读,也就是显示钟的一场
> 90 分钟。占比类的数换钟不变。
>
> 上一轮(GC-T1B)我们给他「别的线可以选」,量出来的好消息是:**他没有少传**。但那一轮只踢了
> 160 对球,而且有一颗种子特别怪(那一场里撞身体的次数是平常的十几倍),把两个结论撑起来了。
> 这一轮**同样的两个世界、同样的四条规矩、一个字没改**,只是踢了 **800 对**,并且**每条结论
> 旁边都报一句「把影响最大的那一场拿掉,结论还成立吗」**。

### 一、上一轮那条好消息 —— 没站住 ❌

每场地面传球 **79.9625 → 77.64375**,对照区间的下边缘是 **78.9125** —— **掉出去了**。
而且这次「掉了多少」也量准了:每场少 **2.31875** 次,区间整段在零以下(**2.8451** 个半宽)。

上一轮同一个数是 79.0125 → 77.85,下边缘 76.98125,**在里面**。⭐ **同样的两个世界、同一条
规矩,样本大五倍以后,结论反过来了。** 上一轮的数没算错,是**样本不够看不出来**。

那少掉的球去哪了?挡着的那一列少了 **2029** 次,干净的那一列只多了 **174** 次 ——
**大部分是真的不传了**,只有一小部分换了线。上一轮是 318 换回 132(比例高得多),
但那是另一批球、另一个块,两者之间我们不做减法。

### 二、你抱怨的「弹身体」 —— 这次是结实的 ✅

每场撞身体 **17.56625 → 16.74**,区间整段在零以下。**而且这次经得起挑刺**:把影响最大的那
一场(种子 12,526,615)整场拿掉再算一遍,还是 **−0.73717146**,还是整段在零以下。
上一轮那颗怪种子一颗就占了 **0.58392296**,这一轮全场最大的一颗只占 **0.09550375**。

### 三、撞自己人 —— 问了三次,这次过了 ✅

每场 **7.6225 → 6.93**,区间上边缘 **−0.29875**,整段在零以下。
上上轮差 0.0625,上轮差 0.05625,**这轮过了**,而且同样经得起挑刺。
⭐ **规矩一个字没改,只是这次样本够了。**

### 四、老地图那条占比 —— 还是没看见 ❌

真的弹了的球里有多少是「老图说这条线是通的」:上轮 **−0.01397195**,这轮 **+0.00689566** ——
**两次符号相反,两次都没量准**。这一条我们**出手前就写明了这轮买不起**(要 1071 对,我们只有
800 对)。所以它没结论,不是「证明价格不管用」,是**还没看见**。

### 五、传中和吊传 —— 这次都在区间里 ✅(但吊传只差 0.00125 就出去了)

传中 **3.66625 → 3.73375**,在区间里,而且方向还朝上了(上轮是掉出去 0.0125)。
吊传 **5.01875 → 5.1825**,区间上边缘是 **5.18375** —— **只差 0.00125 就顶出去了**。
⚠ 说清楚:这条规矩本来就分不清「被扣分了」和「他改去传高球了」,而这一轮地面球确实少了
—— 所以「地面少了、高球往上飘」这个说法**和数字一致,但没被数字证明**。要证明它得再做一个
探针(看他到底在跟什么比),那个探针还没造。

> **一句话收尾**:这一版把上一版的好消息**推翻了一半、坐实了另一半**。
> 坐实的:**弹身体确实降了,撞自己人确实降了**,而且这次不是靠一颗怪种子撑着。
> 推翻的:**「他没有少传」不成立 —— 他还是少传了**,只是比 GC-T1 那次少传得轻一些。
> 四条规矩过了两条,⛔ **这一版什么都没上线**,你现在玩的那个世界一个字节都没动。
