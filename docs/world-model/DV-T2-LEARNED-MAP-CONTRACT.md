# DV-T2 — 学出来的地图 (THE LEARNED-MAP CONTRACT: the earned-knowledge pilot, second half)

> Drafted by the commander 2026-08-12 under ruling #253.2 (the unit-of-evolution
> question — the user's richer-labels direction: 需要学的标签应该更多,不只是输赢)
> and the user's ratification of the SIX-SOURCE LABEL LEDGER and the fifth
> registration ("按照你说的完整的这个自走"). AMENDS
> [`DELIVERY-VALUE-CONTRACT.md`](DELIVERY-VALUE-CONTRACT.md) §3's DV-T2 line: the
> CO-EVOLUTION reading is SUPERSEDED by the LEARNING reading — selection is
> instrument-deaf to the belief (DV-T1's measured fact: the truth dose reshapes
> the delivered mix while outcomes stay unresolved ⇒ win-only fitness has no
> gradient onto the map); co-evolution / inheritance of learned maps is a NAMED
> LATER SLICE. Binding once the §6/§7 audits are ruled.

## §0 THE SIX-SOURCE LABEL LEDGER (ratified 2026-08-12; completeness by actor-closure)

Labels can only come from the world's actors, and the actor list is CLOSED:

| source | cluster | state |
| --- | --- | --- |
| SELF | 吃亏簇 — outcome punishment (丢球→丢分); institutional sub-family: offside/foul/card labels (already instrumented) | ⭐ THIS CONTRACT (pass-level form) |
| TEAMMATES | 观察簇 — co-occurrence → 默契 (pairwise tacit weights; the substrate already emits dupRun/thirdMan-family events) | queued rung (VISION 默契) |
| COACH | 教练簇 — vertical compressed teaching; horizontal sub-family: peer verbal | queued rung (VISION 教练; M-DV.2's named fast channel) |
| OPPONENTS | 对手簇 — in-match reading + cross-match targeting; ⭐ the EARNED version of the #248 assignMarks innate-doctrine debt | queued future arc |
| THE LEAGUE'S OTHERS | 模仿/传播簇 — copy-what-beat-me; the tactics-fashion channel (ecosystem-level evolution of tactics) | queued future arc |
| THE GENERATIONS | 输赢簇 — selection: keeps disposition/style; CANNOT carry knowledge (measured) | in place (evolve.ts) |

The training ground is a VENUE (label greenhouse), not a source. Body growth is
attrs, not labels. ⭐ COMPLETENESS RULE: a proposed label that fits no cell
indicts the taxonomy, not the ledger — surface it, never bolt on a seventh
source silently.

## §1 The claim (H-DV2 — ⭐⭐ THE FIFTH REGISTRATION, the user's hypothesis, frozen verbatim)

「不用输赢：只给球队自己的丢球→丢分账本，它们能自己学出正确形状的风险地图——
后场 > 中场 > 前场的顺序自己长出来，不用任何人告诉它们。」

Scored predicate (frozen ex ante): with the learning seam armed and ZERO
instrument dosing, after the exam's learning run EVERY team's own account book
independently shows the strict ordering own > middle > final at set grain (the
SHAPE claim — #246: shape is the fidelity check, magnitudes are this world's).
Convergence distance to the truth cut is REPORTED, never gated.

## §2 Mechanism (M-DV2, dormant behind its own explicit flag)

* **M-DV2.1 — THE PASS-LEVEL LABEL.** A delivery aimed into zone z whose
  possession outcome is a LOSS followed by a concession inside the census's OWN
  10 s window writes one punishment tick (z, 1); a delivery into z that is not
  so punished writes (z, 0). The label's index = the AIM zone — the pricing's
  own read, so the §HONESTY 8 index mismatch is fixed AT THE SOURCE. No
  invented constants: the 10 s window is the census's own; the zoning is
  `HALF_L/3` re-derived; the loss semantics trace to the census's loss-tick
  definitions (#215.3-H1).
* **M-DV2.2 — THE ACCOUNT BOOK.** Per-team in-world state; belief[z] = the
  running frequency of punishment over the team's OWN deliveries into z
  (zero-constant running-mean form). Windowing/decay is OUT of slice one —
  one season's book, reset at the season boundary (structural, not tuned).
* **M-DV2.3 — THE WRITE PATH.** The book writes the SAME `dvLossBelief`
  representation the banked seam reads — self-dosing from own experience: the
  truth-dosing instrument's exact write path with the SOURCE swapped from the
  census artifact to the team's own book. Armed = `dvDeliveryValue` + a NEW
  explicit learning flag (all limbs listed in the arming checklist); the genes
  stay born-absent; learning OFF ⇒ byte-identical world (the full identity
  stack + two-doors inherited from DV-T0).
* **M-DV2.4 — SCOPE.** Fixed rosters, NO evolution inside the exam (pure
  within-lifetime learning). The exposure weight is NOT learned (it has no
  truth table; it rides at the parity dose for T1/T1b comparability). Named
  later slices: inheritance/Lamarck interplay · the coach compression channel ·
  per-player books (credit-to-the-individual) · the opponent and imitation
  clusters (§0).

## §3 Instruments & the arc

* **T2-C0 — THE PASS-LEVEL CENSUS** (instrument-only, no src change): measure
  the label's own truth globally — P(conceded ≤ 10 s | delivery into z, lost)
  by aim zone, production world — because the banked GGC cut indexes losses by
  LAST-OWNED zone and cannot be re-cut by aim. Its ordering = the convergence
  yardstick; it also proves the label well-defined (chain semantics traced).
  ⭐ #246 shape check pre-registered: own > middle > final expected; an
  inversion routes to the 街机偏离 test, never silently corrected.
* **T2-T0 — THE DORMANT LEARNING SEAM**: the book + write path, born-off,
  full identity stack, two-doors vs every banked seam; G-NOTABLE extended —
  no census value reachable from src AND the learner reads ONLY the team's own
  events (grep + fork inventory).
* **T2-T1 — THE CONVERGENCE EXAM** (the registration scored): learning-armed
  world vs learning-off anchor on virgin seeds; run length sized EX ANTE from
  T2-C0's measured event rates (enough deliveries per zone per team for the
  book to speak — the arithmetic shown before any battery). Frozen: ⭐ the
  ORDERING-emergence predicate (H-DV2's letter) · REPORTED convergence
  distance vs T2-C0's truth · the football guards at the banked tolerances.
  Forks: **F-DV2-a** the ordering never emerges ⇒ the registration scores
  NEGATIVE honestly (a result about the label's audibility, not an exam
  failure) · **F-DV2-b** the books converge to a DIFFERENT stable shape ⇒ the
  world's pass-level truth differs from expectation — diagnostic, commander's ·
  **F-DV2-c** guards breach ⇒ STOP.
* **Exit** = the play-test with learned maps armed in a play world (传球看起来
  聪明了吗 — the map's value is chiefly BEHAVIOURAL, #253.2), keep/change/
  revert per lever. USER GATE.

## §4 Non-claims

No coach channel, no per-player books, no opponent/imitation clusters, no
evolution-learning interplay, no exposure learning, no value-side nonlinearity,
no reception-context term. The account book gives no team the census's map
(#247 intact — each map is earned from own events; wrong books are legal and
are STYLE). Nothing ships without the play-test verdict.

## §6 VISION audit (the #91 form)

* vs §1 底座给能力: the CAPACITY to remember one's own punishments is
  substrate (real players have memory); the CONTENT is earned per-team. PASS.
* vs #200: zero hand-painted values — the book is filled by the team's own
  match events; the truth table stays instrument-side. PASS.
* vs 感知诚实: the label reads WORLD events (own loss + the public scoreboard
  inside 10 s) — no new perception channel, no opponent-mind reading. PASS.
* vs the E4/certified-price idiom: extends measured-price from
  instrument-measured to SELF-measured — the #246 mechanism made in-world
  (real players' risk sense is learned from their own world). PASS.
* vs emergence: born-off, born-empty; the ordering must EMERGE from events or
  the registration scores negative. PASS.

## §7 REALITY audit (the #201 rule)

* Real players do learn positional risk from their own punished mistakes.
  HONEST LIMIT, stated: in reality this knowledge usually arrives coach-first
  (青训第一课 "别在后场玩火") and is then fixed by experience; slice one opens
  ONLY the experience channel, so convergence is expected SLOWER than
  real-football transmission — the coach compression channel is the named
  accelerator, later. PASS with the limit stated.
* One-season book reset ≈ a memory simplification (real memory decays, not
  resets). Structural, declared. PASS.
* The pass-level label (aim-indexed) matches how players narrate the lesson
  ("那球不该往里传") better than release-indexed accounting. PASS.
