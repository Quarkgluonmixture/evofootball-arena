# BU-T0 — THE DV LEARNED RISK MAPS IN THE v7 COMPOSITION (does earned knowledge open the lane?)

Status: **PRE-REGISTERED — everything above [§RESULT](#result) was written and COMMITTED BEFORE
THE BATTERY RAN** (#266.3(c), the freeze-commit canon). The measured numbers arrive only in
§RESULT, and every number there is printed by
[`scripts/analysis/bu-t0-dv-composition-result.ts`](../../scripts/analysis/bu-t0-dv-composition-result.ts)
from the committed artifact
[`data/bu-t0-dv-composition.json`](data/bu-t0-dv-composition.json) — none is typed (#229.2).

Authority chain: the **BUILD-UP CONTRACT** [`BU-BUILDUP-CONTRACT.md`](BU-BUILDUP-CONTRACT.md)
§2 (**M-BU.1** the inventory is the law · **M-BU.2** composition debts bind · **M-BU.3** pricing
honesty · **M-BU.4** scope) and §3 (the assembly slices), bound by **#285.1**; the slice order
bound by **#286.3** (*"BU-T0 = ARM THE DV LEARNED RISK MAPS (#259) IN THE v7 COMPOSITION"*).
Hygiene canon in full: **#163** (booked = walked) · **#181.2** (gates computed in-probe) ·
**#200** (no invented literal) · **#229.2** (nothing typed the artifact can print) · **#246**
(the pre-registered direction) · **#256.2** (COMMENSURABILITY — the engine's own machinery, never
a parallel oracle) · **#261.2** (env WHITELIST-OR-REFUSE) · **#264.2 / #268.3** (the DEAD-PREDICATE
class) · **#266.3(a)** (the hashed body excludes ALL invocation context) · **#266.3(c)** (the
freeze commit) · **#268.3(a)** (LIVENESS BY MACHINE, EXACTLY-ONE ENFORCED) · **#270** (the dose
never in `info.genome`) · **#272.3(ii) / #280.2(iii)** (clock honesty, APPLIED values) ·
**#273.3 as CORRECTED by #286.5** (tree-clean gates compare WORKTREE vs **HEAD**, both conjuncts) ·
**#282.2(ii)** (per-seed cells stored — every headline re-derives) · **#283.2(iv)** (worker-simmed
fixtures play the SHIPPED world) · **#276.3 → #278.2(ii) → #279.2(i) → L3-T2 MED-4 → #286.4**
(⭐ THE STANDING CITATION HUNT, at FIVE strikes).

> ⭐ **INSTRUMENT-AND-COMPOSITION ROUND.** `src/**` is **BYTE-UNTOUCHED** — `xSrcUntouched` is a
> HARD gate. **ZERO `src/**` edits were needed**: both seams are banked and every arming this
> stage performs is flag wiring plus the two banked doses, through the shipped writers.
>
> ⭐⭐ **NOTHING HERE IS SCORED.** H-BU.1 is scored at **ARC EXIT** on the assembled composition
> (#286.3's amended seat). Every football face below is **REPORTED**; no gate reads one.

---

### ⭐ §CORRECTIONS-READ — the standing hunt, discharged explicitly

Every prior-document number quoted below was read **after** opening that document's own
corrections section, and the checks are stated rather than implied.

* **BU-C0 [`BU-C0-RECEPTION-OPTION-CENSUS.md`](BU-C0-RECEPTION-OPTION-CENSUS.md) §COMMANDER
  CORRECTIONS OF RECORD (#286) — READ IN FULL.** Binding here:
  1. **(MED 1) THE LADDER IS GK-INCLUSIVE and the outfield ladder is NOT re-derivable from
     BU-C0's committed cells.** Its bounds of record: keeper conversion ≥ 42.1 %, OUTFIELD
     end-to-end conversion ≤ 22.2 %, the behind/ahead conversion contrast 2.52× → ≈1.85×
     outfield-only, and the corridor owns **≥ ~67 %** of the outfield loss (the 77.45 % figure is
     GK-inclusive). ⭐ **THE DEBT IT NAMES — "any future instrument that quotes this ladder
     carries GK-SPLIT RUNGS (L1–L4 each split GK/outfield)" — IS DISCHARGED BY THIS STAGE'S
     INSTRUMENT** (§FORM, instrument 1).
  2. **(MED 2) L1 owns 0 % of the ladder loss BY CONSTRUCTION**; the statistic for a
     body-adding seam is END-TO-END CONVERSION (29.85 % GK-inclusive / ≤ 22.2 % outfield). The
     "22.5 % of the loss" sentence is L2 (range, 10.9 %) + L3 (race, 11.7 %). Quoted correctly
     below.
  3. **(MED 3) THE ARMED TERMINAL CENSUS IS ENTANGLED WITH THE L3 VETO**: ~14.5 pp moved OUT of
     `tackled` (19.01 % → 4.49 %) and ~9 pp INTO `intercepted` (40.48 % → 49.46 %) while TOTAL
     loss-to-opponent FELL (71.93 % → 70.03 %). ⭐ **Quote the BARE arm when arguing the
     diagnosis** — interception is the largest terminal class there (40.48 %, 2.1× the next) with
     no entanglement. ⚠ **BOTH ARMS OF THIS STAGE CARRY THE VETO**, so this stage's *contrast* is
     entanglement-free by construction while its *levels* are not; the aggregate
     `lossToOpponentShare` is published as the honest cross-arm quantity.
  4. **(LOW 4) THE FIFTH CITATION STRIKE**: BU-C0's §SLICE-ORDER quoted
     `belief[zone(aim)]·passBase` as "M-DV.3's price". It is not — that is
     [`DV-T0-DORMANT-SEAM.md`](DV-T0-DORMANT-SEAM.md)'s **implementation** of `lossCost`. The
     contract's own text ([`DELIVERY-VALUE-CONTRACT.md`](DELIVERY-VALUE-CONTRACT.md) §2 M-DV.3)
     reads **`score′ = score − w_exposure · exposure − w_loss · lossCost`**. Both are quoted
     below, each to its own source.
  5. **(LOW 5) `xSrcUntouched`'s first conjunct compared WORKTREE vs INDEX** while the header
     claimed the #273.3 fix — the class named of record: **AN INHERITED FIX ANNOUNCED IS NOT A
     FIX RIDDEN**. This stage's gate runs `git diff --stat **HEAD** -- src` AND
     `git status --porcelain -- src`. The corrected implementation is copied, not its header.
  6. **(LOW 6) rhetorical hardenings**: #226's interception rise carries its binding qualifier
     ("INSIDE THE FROZEN TOLERANCE"); E7 is a **GRAIN CORRECTION**, not a contradiction; the
     attempt-share faces use den=`att` while completion uses den=`att+attU`; #226's F-CTB-a
     endpoint is 0.09 % → ~97.24 %.
* **CB-T2 [`CB-T2-CHOICE-SEAT.md`](CB-T2-CHOICE-SEAT.md) §COMMANDER CORRECTIONS (#269.2/#269.3) —
  READ IN FULL.** Binding here: **(iv) THE NAMED DEBT** — *"`clearTouchPastArming`'s safety is
  empirically clean here (0 stale, 0 unarmed) but structurally unproven for a world arming
  `o2Look`/`ekHoldVeto` alongside — any future world that arms those together must prove the
  arming lifecycle at that composition FIRST"*. §LIFECYCLE below is that proof, at CB+L3+DV. Also
  binding: (i) the offside term is **structurally unsatisfiable on a knock** (0/2,400) and the
  §DEV 1 "strain" is WITHDRAWN — no reading of it is carried here; (ii) the true dominant
  degeneracy is the SHORT-BALL BAND (`d < 5 ⇒ ×0.75`, 100 % of knock candidates) — noted, not
  patched, and not this stage's; (iii) the L3-cost re-read is CROSS-ARM BY CONSTRUCTION and the
  +0.9 pp null is quoted nowhere here; the LOW about bare-boolean gates riding outside the
  liveness map is honoured — every conjunct of every gate in this stage owns a mutant.
* **DV-T2-T1 [`DV-T2-T1-CONVERGENCE-EXAM.md`](DV-T2-T1-CONVERGENCE-EXAM.md) §COMMANDER
  CORRECTIONS (#258.3) — READ IN FULL.** Binding here: (1) ⚠ **that artifact's `resultSha256`
  rides a timing field and is MACHINE-DEPENDENT**; its portable anchor is the G-DET digest
  `9bc1aaf9…`. This stage's `gDose` therefore guards on **BOTH** — the declared file SHA (a
  file-identity guard, the `L3_T1_SHA` idiom) **and** the portable G-DET digest the correction
  names. (2) the artifact's stored copy of the fifth registration has ASCII punctuation slips —
  the doc's own §REGISTRATION is the authority; nothing verbatim is re-quoted from the artifact
  here. (3) F-DV2-b's coded predicate was narrower than its letter — no fork of that exam is
  re-used here. (4) the LOWs are noted; none touches the per-book cells this stage doses from.
* **DV-T2 [`DV-T2-LEARNED-MAP-CONTRACT.md`](DV-T2-LEARNED-MAP-CONTRACT.md) (no corrections
  section — checked) §2 M-DV2.3/M-DV2.4**: the learned book writes the SAME `dvLossBelief`
  representation the banked DV seam reads; **the exposure weight is NOT learned** ("it has no
  truth table"). This stage therefore arms the LOSS-COST BELIEF limb **only** — `dvExposureWeight`
  stays absent, so `deliveryRiskPrice`'s exposure term is exactly `+0`.
* **DV-T0 [`DV-T0-DORMANT-SEAM.md`](DV-T0-DORMANT-SEAM.md) (no corrections section — checked)
  §LAW**: the implementation of the loss-cost limb is `belief[zone(aim)] · passBase`, and
  `passBase` is *the pricer's own base value of a pass* — traced, not invented.
* **L3-ENTRY-RUNG [`L3-ENTRY-RUNG.md`](L3-ENTRY-RUNG.md) §COMMANDER CORRECTIONS (#283.2) — READ
  IN FULL.** Binding here: **(iv)** worker-simmed fixtures play the SHIPPED world (`League.toJSON`
  omits `matchFlags`), so this probe constructs every match directly and asserts the arming on the
  very match it walks; and the entry's **pooled matured dose is a PRESENTATION of committed cells**
  (#270's form) — the idiom this stage applies a second time, to the DV family's own bank.
* **L3-T2 [`L3-T2-ARMED-WORLD-READ.md`](L3-T2-ARMED-WORLD-READ.md) §COMMANDER CORRECTIONS
  (#282.2/#282.3) — READ IN FULL.** Binding here: the per-cluster-rows canon (discharged by
  `perSeedCells`), the arm-C dose idiom, and #282.3(1)'s registered surprise (the armed world is
  slightly QUICKER, not calmer) as the background for the tempo-shaped faces. **No football
  number is taken from L3-T2.**
* **R-乙 [`R-YI-STANDING-GAP-TABLE.md`](R-YI-STANDING-GAP-TABLE.md) §FIX + §RESULT-2's
  SUPERSESSIONS — READ IN FULL.** Binding here: the rows this stage re-runs — **Q01 · Q05 · Q06 ·
  Q07 · Q14** — are NOT among the eight superseded lines (Q09/Q10/Q11/Q13/Q17/Q18/Q20/Q21);
  Q07's own semantics note (its complement POOLS backward and lateral) is the reason the split
  instrument exists; the declared clock convention **1 sim-second = 22.5 display-seconds** is
  inherited, both terms extracted from `src/**` at run time.
* ⚠ **THE #246 BAND IS THE PROGRAMME'S OWN PRE-REGISTRATION, NOT A CITED FOOTBALL STATISTIC**, and
  BU-C0 §DOUBTS 2 records that it **was never scaled to 6v6**. It is used here only as a
  direction, never as a band a number is scored against.

---

## §0 THE QUESTION, in football

BU-C0 answered *what does a body with the ball actually have* and its answer was blunt: **人是有的,
球路是死的** — 2.605 behind-ball BODIES exist at every reception, and **77.45 %** of the option
loss (GK-inclusive; **≥ ~67 %** outfield-only, per §CORRECTIONS-READ 1) dies in **THE CORRIDOR**.
Of the 0.7775 behind-ball options that survive, **54.20 % are the goalkeeper**, leaving outfield
supply at **0.356 per reception** and only **0.274** inside the engine's own 6–30 m choice window.
On the BARE arm — the un-entangled one — **interception is the largest terminal class, 40.48 %,
2.1× the next**.

⭐ **THE SLICE'S QUESTION, in one sentence: if a team already knows, from its own punished losses,
that balls into a zone get cut — does it circulate better?** #286.3 bound this slice because the
DV learned map is **the only banked seam whose object is the choice among lanes**, and because
arming it is arming **knowledge the world EARNED** (M-BU.3: no taste constants, ever).

⭐⭐ **AND THE HONEST DOUBT IS PRE-WRITTEN, RATIFIED BY #286.3**: **DV prices the destination ZONE,
not the LANE.** Its label is *"deliveries aimed into zone z were punished"* — a zone-grain proxy
for a corridor-grain fact. If no usage or supply face moves resolvedly, the verdict is
**"zone-grain knowledge cannot price lane-grain risk"**, that is a REAL RESULT, it routes to the
commander's menu, and **no weight is touched** to manufacture an effect.

---

## §FORM

### The two arms — PAIRED, SAME SEEDS

| arm | construction | role |
| --- | --- | --- |
| **`v7`** | `new Match({ seed, teamA, teamB, ...a4MatchFlags(7) })` + `armA4World(m, null, 7, poolT1DoseCells(L3-T1))` | **THE BASE** — the `?a4world=7` world exactly: the CB layer (`cbCommitPhysics` + `cbTouchPast` + `cbChoiceSeat` at the declared proneness dose) + `l3DefenceLearn` + `l3DefenceVeto`, the defence books dosed with the SHIPPED entry's own pooled matured cells. |
| ⭐ **`v7dv`** | THE SAME, plus `dvLearnedMap: true`, `dvDeliveryValue: true`, `dvLearnedBooks: <the matured DV dose>` | **THE SLICE** — the banked DV seam composed in. Two limbs, both required: `dvLearnedMap` LEARNS, `dvDeliveryValue` CONSUMES. |

Squads are redrawn per fixture (`team('A', seed·2+1)`, `team('B', seed·2+2)`) — the banked draw.
The clock is the **ENGINE DEFAULT** (`MATCH_DURATION` 240 sim-s, never overridden; `gClock`
asserts it on every walk; 1 sim-s = 22.5 display-s, both terms extracted from `src/**`).

⭐⭐ **#283.2(iv), STATED AS THE DESIGN REQUIRES.** `League.toJSON` omits `matchFlags`, so a
fixture-routed arm would silently measure the SHIPPED world. Every match here is **constructed
directly with its flags**, and `gArms` asserts **on every walked match**: `l3ArmedVersion(m) === 7`,
all three CB doors live, both L3 doors live, the L3 books carrying the dose cell-for-cell, the
engine clock at its default, **no arming at construction**, and — on the slice arm — both DV doors
live, the DV books carrying the dose cell-for-cell, **and the learned belief actually present on
the genome the chooser reads**. On the base arm the mirror conjuncts assert the DV doors are SHUT
and `dvLossBelief` is ABSENT.

### ⭐ THE ARMING PATH — the dose and its provenance (M-BU.3, house law #270)

**The DV maps' maturity: committed cells EXIST, so no in-run maturation is needed.** The dose is
derived at run time from the committed DV-T2-T1 convergence exam
[`data/dv-t2-t1-convergence-exam.json`](data/dv-t2-t1-convergence-exam.json):

* the source rows are `result.perClusterCells[].consume[][last]` — the **`learnConsume` arm's**
  books (the books that matured in a world where the map was BEING CONSUMED, which is the world
  this stage arms), at the **LAST checkpoint (M\* = 440 matches)**;
* **POOLED over all 40 books** (20 replicates × 2 sides). ⭐ **WHY POOLED**, the L3-ENTRY-RUNG
  §DOSE argument applied a second time: a composed match has no replicate index and no honest way
  to invent one, both teams are dosed symmetrically anyway, and the pooled book is the aggregate
  of exactly the evidence that exam banked;
* written into each match's two `DeliveryAccountBook`s through the book's **own public `note()`** —
  the `doseL3Books` idiom verbatim in form, so a dosed book is a state the world could itself have
  reached, by the only writer the shipped seam has. **No field surgery. Nothing anywhere near
  `info.genome`** (house law #270 — the engine writes the served belief onto the MATCH-LOCAL
  genome views in `Match.dvLearnWriteBelief`, and `gArms` asserts `info.genome` stays clean).
* **fresh books per match**: every match is an independent *team that has already learned*, so
  nothing accumulates across the battery and the pairing is exact.
* `gDose` guards the provenance: the committed file's declared SHA **and** the portable G-DET
  digest (#258.3(1)'s correction), non-emptiness, every belief zone carrying evidence, and that
  **the exam's own scored SHAPE (own > middle > final) survives the pooling**.

**The exposure limb is NOT armed.** M-DV2.4 is explicit that the exposure weight is not learned;
`dvExposureWeight` therefore stays absent and `deliveryRiskPrice`'s first term is exactly `+0`.
The ONLY live limb is the learned loss-cost belief. The price, quoted to its two sources:
the contract's form is **`score′ = score − w_exposure · exposure − w_loss · lossCost`**
(DELIVERY-VALUE-CONTRACT.md §2 M-DV.3); DV-T0's implementation of `lossCost` is
**`belief[zone(aim)] · passBase`** (DV-T0-DORMANT-SEAM.md §LAW), `passBase` being the pricer's own
statement of what a pass is worth before its qualities.

### ⭐⭐ Instrument 1 — THE OPTION LADDER, now with GK-SPLIT RUNGS (#286's new canon)

BU-C0's ladder **VERBATIM in definition**, so every face is commensurable with the committed
census, with the #286.1 DEBT discharged: **L1–L4 are each split GK / outfield.**

| rung | what it asks | whose code answers |
| --- | --- | --- |
| **L1 POSITION** | is the team-mate BEHIND the ball line? `Δ = localX(mate) − localX(ball)`; BEHIND ⇔ `Δ ≤ −2 m` | ⭐ the ±2 m band is **Q07's own**, EXTRACTED at run time from `src/sim/mechanics.ts`'s forward-pass line — never typed |
| **L2 THE BALL GETS THERE** | does the engine's own ground-pass prediction say the ball ARRIVES? | `evaluatePassAffordance` (`flight.reachable`) |
| **L3 THE RECEIVER WINS THE RACE** | `arrivalMargin > 0` — a SIGN test on the engine's own quantity | `evaluatePassAffordance` |
| **L4 THE CORRIDOR IS NOT CUT** | can any opponent meet the ball ON ITS PATH? | `evaluatePassCorridorInterception` (`earliestFeasiblePoint !== null`) |
| **L5 (reported beside)** | inside the engine's OWN 6–30 m pass-choice window? | `passChoiceCandidateGids` |
| ⭐ **THE GK SPLIT** | every one of L1–L4 is counted twice: keeper and outfield | `Player.role === 'GK'` |

**THE PUBLISHED "OPTION" IS L1 ∧ L2 ∧ L3 ∧ L4.** The snapshot fed to both evaluators is
`oraclePerceptionSnapshot` — the FULL-TRUTH snapshot the engine ships for offline oracle probes.
⚠ **This is a CAPABILITY census, not a perception census** and not a choice census.

### Instruments 2–4 — unchanged from BU-C0, so the faces travel

**Instrument 2, the direction mix**: FORWARD is R-乙 **Q07 VERBATIM** (the engine's own
`team.stats.passesForward`, attributed per attempt by its per-tick delta); the probe's ±2 m
re-derivation splits ONLY the engine's pooled complement into backward vs lateral, so a
disagreement can mis-file only *inside* that complement; the agreement rate is published.
**Instrument 3, the spell terminal census**: the #173 / Q01 segmentation VERBATIM, class = the
LATEST qualifying engine event inside the spell's own span. **Instrument 4, E7 at world grain**:
the distribution of team-mate longitudinal offsets at receptions.

### The three populations

| population | definition |
| --- | --- |
| **reception** | every tick at which the ball's owner CHANGES while `phase === 'playing'` |
| **pressed reception** | a reception whose receiver has an opponent inside **4.2 m** (`TOUCH_CONTROL_DIST`, imported not typed) |
| **pressed-carrier moment** | a NON-reception carrier tick, sampled every **12 ticks (0.2 sim-s)**, at which the carrier is pressed. ⚠ A declared SAMPLE. |

---

## §LIFECYCLE — ⭐⭐ THE #269.2(iv) DEBT, AND IT RUNS FIRST

**The order of proof is binding (#286.3): this precedes the battery.** A staleness defect at the
authorized composition **REFUSES THE RUN (exit 4)** and reports for adjudication — a fix is a
`src` change needing its own authorization.

### The class, stated exactly

`Match.forcedTouchPast` is a **SINGLE match-scoped slot**. It is WRITTEN by `armTouchPast` from
the ONE call site in `PlayerBrain` (the CB-T2 choice seat), WITHDRAWN by `clearTouchPastArming` at
that same site when the body's next decision no longer wants the knock, and CONSUMED by the ONE
fork in `Match.stepBall`. **The debt**: a world that arms OTHER seams beside it may take an
**EARLY RETURN above the seat's block**, so the withdrawal never runs and an aim survives its own
tick — and a surviving aim can fire into a **LATER possession**, i.e. stale.

### How it is proven, without touching `src`

The decision loop and `stepBall` both run **inside** `Match.step`, so in a clean lifecycle the
slot is **always null when `step` returns**. Every non-null observation at a step boundary is a
**CARRY-OVER**, and each is measured for the three ways it could leak: across a change of ball
**OWNER**, across a change of **PHASE**, and live at the **WHISTLE** — plus the two end facts
(null at construction, so nothing enters a match; null at full time, so nothing leaves one).

### ⭐⭐ THE DOORS MATRIX — the FULL composition, exhaustively

Seven doors, **2⁷ = 128 cells**, on the v7 substrate (`a4MatchFlags(3)` — CALLED, not copied), so
**every pairwise flag interaction is enumerated** and so is every higher-order one:

```text
C cbCommitPhysics   T cbTouchPast   S cbChoiceSeat (+ the proneness dose)
L l3DefenceLearn (+ the matured L3 dose)   V l3DefenceVeto
M dvLearnedMap (+ the matured DV dose)     D dvDeliveryValue
```

⚠ Two axes carry their own dose, **declared**: `S` without a non-absent `cbCarryProneness` cannot
form a seat and `M` without a non-empty book serves no belief, so the matrix would otherwise be
measuring absence rather than composition.

**THE IDENTITY LAWS (every cell, every seed — "a door with no partner never moves the world"):**

1. `cbTouchPast` is INERT without the choice seat (nothing can write the arming slot);
2. `l3DefenceLearn` is INERT without the veto (the book fills, nothing reads it);
3. `l3DefenceVeto` is INERT without the learning door (there is no book to read);
4. `dvLearnedMap` is INERT without `dvDeliveryValue` (the book fills, nothing reads it);
5. `dvDeliveryValue` is INERT without `dvLearnedMap` (the belief gene stays absent ⇒ no seat).

**THE LIVENESS LAWS (setwise — a door that can never move the world is a DEAD door):**
`cbCommitPhysics` moves it; the choice seat moves it when its capability door is open; the L3 veto
moves it on a dosed book; ⭐ **the DV pair moves it** — if that one failed, the slice would be a
dead door and the round would be over before the football.

### ⭐ THE LAW WAS RE-SPECIFIED MID-BUILD, AND HERE IS THE DISCLOSURE

The law as **first coded** was *"no arming survives its own tick in ANY cell"*. The exhaustive
matrix **FALSIFIED it**, and the falsification is worth more than the law was. The carry-overs
live **entirely** in the cells where the CHOICE door is open and the CAPABILITY door is SHUT
(`S ∧ ¬T`). There the arming has **no consumer** — `stepBall`'s fork requires `cbTouchPast` — so
the slot sits until the same body's next decision withdraws it, and it can sit across possessions,
across restarts, and to the whistle.

⭐ **The point of the class is a stale aim that FIRES.** The honest law is therefore a
**DICHOTOMY**, and both halves are proven:

* **(a)** in **every cell where an aim CAN fire** (`T`), no arming survives its own tick at all —
  so a stale aim is not merely unobserved, it **never exists**;
* **(b)** in **every cell where armings persist** (`S ∧ ¬T`), **ZERO knocks fire**, by the firing
  fork's own conjunct — the persistence is inert bookkeeping with no behavioural surface.

`S ∧ ¬T` is a configuration **no armed world constructs** (the v7 entry opens both doors; CB-T2
§ARMING #4 is explicit that the choice buys the choice and the capability buys the firing). It is
**REPORTED rather than excluded**, because an exhaustive matrix that quietly dropped its own
inconvenient cells would be worthless. It is **routed to the commander** in §FINDING.

### The structural half, machine-read from `src/**`

`gLifecycle` asserts, from the source text itself: exactly ONE arming write site, exactly ONE
withdrawal call site, the slot cleared in exactly TWO places (the fire and the withdrawal), and
exactly ONE firing fork. And it asserts the **scope** of the discharge as a conjunct:

* ⚠ **the two seams #269.2(iv) NAMED — `o2Look` and `ekHoldVeto` — are NOT armed here**, so this
  stage discharges the debt for **CB+L3+DV ONLY**; their own compositions remain **UNDISCHARGED**;
* ⭐ **the proof is NON-VACUOUS**: two OTHER early returns above the seat's block, `o1PassWindup`
  and `c7Windup`, **ARE** armed in the v7 substrate, so the withdrawal genuinely can be skipped.
  A conjunct fails if they are ever off — a zero measured in a world with no exposure would be a
  zero of absence, and the gate refuses to let it pass as a proof.

The same lifecycle read rides **every walk of the battery**, so the proof is re-taken on the
measured population, not only on the matrix.

---

## §DORMANCY

**Flags off ⇒ `src` byte-identity is the HARD GATE, and the expectation was ZERO `src/**` edits.**
That expectation held: both seams were banked dormant behind their own explicit flags, and every
arming this stage performs is (i) construction flags, (ii) the shipped entry's own `armA4World`,
(iii) two doses through the two books' own public writers. `xSrcUntouched` is computed in-probe in
the **#286-CORRECTED** form — `git diff --stat HEAD -- src` (worktree vs **HEAD**, not vs the
index) **and** `git status --porcelain -- src` — and every conjunct owns a live mutant.

The doors matrix is dormancy's other half: the five identity laws are exactly the statement that
each seam's door, standing alone anywhere in the composition, leaves the world byte-identical.

---

## §NRULE — the sizing, frozen before the battery

**N = 300 seeds per arm, paired**, matching BU-C0 so the two stages' faces are read on the same
precision. The binding term is the per-reception option count, whose cluster is the MATCH
(~140 receptions/match ⇒ ~40,000+ receptions per arm). ⚠ **No formal MDE is claimed: nothing here
is scored**, so N buys descriptive precision for the reported directions, not power against a
hypothesis. The doors matrix is sized separately: **128 cells × 3 seeds = 384 walks**, exhaustive
in the flags, small in the seeds — it is a STRUCTURAL claim (a byte-identity and a lifecycle
count), not a statistical one.

Estimator: **PAIRED cluster bootstrap over match seeds** (#20), **2,000 resamples**, percentile
95 % CI, **ratio of sums**. ONE resample-index matrix is drawn once and shared by every face and
both arms, so the levels and the contrast are the same resampled worlds and the pairing is inside
the interval.

## §SEEDS — booked = walked

```text
block            12,487,000 – 12,487,999   (ruling #286.5)
core battery     12,487,100 – 12,487,399   300 seeds, WALKED BY BOTH ARMS (paired)
smoke            12,487,000 – 12,487,019
guard/preflight  12,487,040 – 12,487,059
lifecycle/doors  12,487,500 – 12,487,502   3 seeds × 128 door cells
world identity   12,487,900
stats stream     base 111,800 (floor from #286.5; step 200 from every published base)
```

## §GATES — the set, frozen ex ante (**18**)

| gate | what it proves |
| --- | --- |
| `gDet` | G-DET: the whole core runs TWICE and re-derives bit-identically. |
| `xSrcUntouched` | ⭐ `src/**` byte-untouched — **WORKTREE vs HEAD** (#273.3 as corrected by #286.5) plus no untracked/staged src file. |
| `gArms` | ⭐⭐ #283.2(iv): every walked match carries its arm LIVE — world 7, all three CB doors, both L3 doors, both doses cell-for-cell, the learned belief present on the genome the chooser reads (slice) / absent (base), nothing in `info.genome`, no arming at construction; the arms walk the SAME seeds. |
| `gDose` | BOTH doses come from committed artifacts: L3's declared SHA; DV's declared SHA **and** its portable G-DET anchor; non-empty; every belief zone evidenced; the exam's own own>middle>final SHAPE survives the pooling. |
| ⭐⭐ `gLifecycle` | **THE #269.2(iv) DEBT**: (a) in every cell where an aim can fire, no arming survives its own tick / a possession / a phase / the whistle, and none exists at construction; (b) where armings persist, zero knocks fire; the battery population re-proves it; the seat DID arm (non-vacuity); the src call-site census (1 write, 1 withdrawal, 2 clears, 1 fire); the scope (o2Look/ekHoldVeto not armed) and the NON-VACUITY of the exposure (o1PassWindup + c7Windup ARE armed). |
| ⭐⭐ `gDoors` | the five identity laws hold on EVERY cell and EVERY seed; the four liveness laws each fire; the matrix is the full 2⁷ power set. |
| `gNonPerturbing` | THE INSTRUMENT DOES NOT MOVE THE FOOTBALL: the same worlds re-walked with the option oracle OFF end on the identical signature, spell count and pass count. |
| `gOracle` | the engine's evaluator was called and answered; both race verdicts and both corridor verdicts occur (no constant predicate); ⭐ the GK split sees BOTH sides (a one-sided split is a dead split); the ±2 m band still traces to `src/**`. |
| `gQ07` | the direction mix book-keeps against the engine's own counters. |
| `gSpells` | every spell lands in EXACTLY one terminal class; the open-play population is non-empty. |
| `gNonVacuity` | ⭐ NO PUBLISHED RATE HAS A ZERO DENOMINATOR (a zero-denominator cell is published as UNMEASURED, never as 0); the histogram sums to its own denominator. |
| `gFaces` | #282.2(ii): every published face re-derives from the STORED PER-SEED CELLS alone. |
| `gClock` | APPLIED, not nominal: the engine-default clock, the 90 out of `Match.minute()`'s own expression, the mapping derived not typed, every walk stepped. |
| `gSeed` | booked = walked: no clash with the consumed ledger, no internal clash, every walked seed inside the claimed bands. |
| `gStats` | the stats base is the dispatched floor 111,800, ≥200 from every published base; the resample count is the frozen one. |
| `gEnvClean` | WHITELIST-OR-REFUSE: no rogue `BUT0_*`, no engine env door set, a preflight may never write a canonical path. |
| `gHashEnvelope` | #266.3(a): the body re-derives its digest from disk, a cross-OUT with a DIFFERENT envelope has the IDENTICAL digest, and no invocation fact is inside the hashed body. |
| `gMutants` | ⭐⭐ #268.3(a): every conjunct of every gate owns a mutant, and flipping it (EXACTLY ONE at a time) flips the outcome. An incomplete coverage map REFUSES THE RUN (exit 3). |

## §ENVELOPE — what `resultSha256` covers

`resultSha256` is taken over the RESULT BODY only: the frozen design, the lifecycle proof, the
doors matrix (per-cell signatures and per-cell lifecycle rows), the run counts, the faces, the
histograms, the GK-split ladder, the terminal census, the receipts, the doses, the per-seed cells,
the seeds/stats ledgers, the G-DET digests, the gates and the mutant map. **OUTSIDE it**:
`generatedAt`, `head`, `outPath`, `preflight`, `wallMs` — the whole `envelope` object.
(#258.3(1)'s correction is honoured: **no timing field rides the hashed body**.)

## §PRE-REGISTRATION — written before the battery, REPORTED, never gated

**THE DIRECTIONS** (#286.3's list, verbatim in substance):

1. **backward/lateral completed-pass USAGE up** — Q07 conventions verbatim (the engine's own
   forward counter; the complement split on the same ±2 m rule);
2. **interception terminal share down** — ⚠ read with the veto entanglement stated: both arms
   carry the veto, so the CONTRAST is clean and the LEVELS are not; `lossToOpponentShare` is the
   aggregate that does not move mass between `tackled` and `intercepted`;
3. **completion up** (Q06);
4. **behind-ball option EXISTENCE** — the BU-C0 instrument re-run, commensurable, now GK-split.

**THE R-乙 RE-RUN CLAUSE** (REPORTED on both arms): **Q01** spell length · **Q05** touches/spell ·
**Q06** completion · **Q07** direction · **Q14-shaped** pressed share.

⭐⭐ **THE LABELLED HYPOTHESIS, PRE-WRITTEN OF RECORD (#286.3)**: **DV prices the destination ZONE,
not the LANE.** If no usage or supply face moves resolvedly, the verdict is **"zone-grain knowledge
cannot price lane-grain risk"** — a REAL RESULT that routes to the commander's menu. **No weight is
touched to force an effect** (M-BU.3: no taste constants, ever). Equally pre-registered: an
effect, if it appears, is an effect **of arming earned knowledge**, and its mechanism (does the
chooser avoid punished zones, or does it merely pass shorter?) is a question this stage can
describe but not settle.

**NO PREDICTION IS MADE about the supply faces' direction.** The DV map changes *which pass is
chosen*, not *where bodies stand* — but chosen passes move the ball, and the ball's position is
what the option census is taken relative to, so a supply movement is possible in either direction
and is DESCRIPTIVE.

---

<a id="result"></a>

## §RESULT

**300 seeds × 2 paired arms · block 12,487,100–12,487,399 · 18/18 gates PASS · 87/87 mutants
LIVE**, `resultSha256` **`188bfb20…60d7`**. Every number below is printed by
`scripts/analysis/bu-t0-dv-composition-result.ts` from the committed artifact; none is typed.

```text
match clock       240 sim-seconds ⇔ 90′  (1 sim-second = 22.5 display-seconds; both terms
                  extracted from src at run time)
walks             600 measured + 50 NON-PERTURBATION controls + 384 doors-matrix walks
                  + the whole core re-run for G-DET
receptions        v7 41,536 · v7+DV 41,619      pressed receptions  27,211 · 27,124
pressed-carrier   63,747 · 63,425               open-play spells    10,521 · 10,530
pass attempts     27,745 · 27,887               oracle calls        1,319,301 (+4,867,044 corridor)
estimator         PAIRED cluster bootstrap by match seed, 2,000 resamples, percentile 95 % CI,
                  ratio of sums; stats base 111,800
G-DET             the second run re-derived the core BIT-IDENTICALLY (1d9108a9…5ff3)
L3 dose           27,368 labels  [{lunges 23896, punished 18812}, {lunges 3472, punished 2867}]
DV dose           685,815 labels [{del 182954, pun 7664}, {del 411422, pun 11582},
                  {del 91439, pun 1746}] ⇒ belief own 0.041890 · middle 0.028151 · final 0.019095
```

### ⭐⭐ 1. THE #269.2(iv) ARMING-LIFECYCLE PROOF — the debt is DISCHARGED at CB+L3+DV

The matrix walked **384 cells** (128 door combinations × 3 seeds), split by whether a knock can
fire at all:

| half | cells | armings | knocks fired | carry-overs | across a possession | across a phase | live at whistle | at construction | longest life |
|---|---|---|---|---|---|---|---|---|---|
| ⭐ **an aim CAN fire** (`cbTouchPast` open) | 192 | **3,202** | **3,202** | **0** | **0** | **0** | **0** | **0** | **0 ticks** |
| an aim CANNOT fire (`S ∧ ¬T`) | 192 | 8,396 | **0** | 241,094 | 2,951 | 415 | 15 | 0 | 3,397 ticks |
| ⭐ **the measured BATTERY** (600 walks, 9,076,744 ticks) | — | **13,016** | **13,016** | **0** | **0** | **0** | **0** | **0** | **0 ticks** |

⭐⭐ **THE VERDICT: CLEAN AT THE AUTHORIZED COMPOSITION.** In every cell where an aim can fire —
and across the entire measured battery of 600 matches — **no arming survives its own tick**, none
crosses a possession or a restart, none is live at the whistle, and none exists at construction.
`armings === knocks fired` exactly (3,202 = 3,202 in the matrix, 13,016 = 13,016 in the battery):
every arming written is consumed on the tick it is written. The zero is **not a zero of absence**
— the seat armed 13,016 times in the battery alone, and 20,436 seats formed in the matrix.

The structural half, machine-read from `src/**`: **1** arming write site
(`src/ai/PlayerBrain.ts:1282`), **1** withdrawal call site (`:1283`), **2** places the slot is
cleared (`src/sim/Match.ts:2931` the fire, `:3956` the withdrawal), **1** firing fork. And the
proof is **non-vacuous**: `o1PassWindup` and `c7Windup` — two early returns above the seat's block
— **are both armed** in the v7 substrate, so the withdrawal genuinely can be skipped here. The two
seams the debt NAMED (`o2Look`, `ekHoldVeto`) are **not** armed, so ⚠ **their compositions remain
undischarged**.

#### ⚠ §FINDING — the class EXHIBITED, and routed to the commander

The first coding of the law (*"no arming survives its own tick in ANY cell"*) was **falsified**,
and the falsification is the round's structural result. In the **96 CHOICE-WITHOUT-CAPABILITY
cells** (`cbChoiceSeat` armed, `cbTouchPast` SHUT) the arming slot is left set with **no
consumer**: 241,094 carry-over tick-boundaries, **2,951** across a change of possession, **415**
across a change of phase, **15** live at the whistle, longest life **3,397 ticks**. That is the
`clearTouchPastArming` staleness class, exhibited.

**Why it is INERT, and why it is nonetheless published**: in exactly those cells **zero knocks
fire** (`stepBall`'s fork requires `cbTouchPast`), so a persisting aim has no behavioural surface;
and `S ∧ ¬T` is a configuration **no armed world constructs** (the v7 entry opens both doors;
CB-T2 §ARMING #4 is explicit that the choice buys the choice and the capability buys the firing).
It is reported rather than excluded because **an exhaustive matrix that quietly dropped its own
inconvenient cells would be worthless** — and because the withdrawal counter says the same thing
from the other side: `armingsCleared` is **1,793** in that half and **0** in the firing half. The
commander's call: whether the CB seam should refuse to arm a knock its world cannot fire.

### ⭐ 2. THE DOORS MATRIX — every pairwise interaction, and the slice is NOT a dead door

| law | kind | result |
|---|---|---|
| `cbTouchPast` inert without the choice seat | identity, EVERY cell × seed | **96/96 hold** |
| `l3DefenceLearn` inert without the veto | identity | **96/96 hold** |
| `l3DefenceVeto` inert without the book | identity | **96/96 hold** |
| `dvLearnedMap` inert without `dvDeliveryValue` | identity | **96/96 hold** |
| `dvDeliveryValue` inert without `dvLearnedMap` | identity | **96/96 hold** |
| `cbCommitPhysics` moves the world | liveness, setwise | 192/192 |
| the choice seat moves it (capability open) | liveness | 96/96 |
| the L3 veto moves it on a dosed book | liveness | 95/96 |
| ⭐ **the DV pair moves the world** | liveness | **93/96** |

⭐⭐ **THE DV SEAM IS LIVE. It changes the world in 93 of 96 cells where it can act** — the
matched-pair signature differs at full time. Whatever follows is therefore **not** "the flag did
nothing"; it is "the flag did something that the faces cannot see."

### ⭐ 3. THE GK-SPLIT LADDER — #286.1's DEBT DISCHARGED, and BU-C0's bounds CONFIRMED

BU-C0's corrections could only BOUND the outfield ladder. It is now measured (v7 arm, pooled over
41,536 receptions):

```text
                L1 bodies    L2 arrives      L3 wins race     L4 uncut        end-to-end
GK-INCLUSIVE      108,456   100,048 (92.25%)  91,068 (91.02%)  32,240 (35.40%)   29.73 %
⭐ OUTFIELD        70,509    69,435 (98.48%)  60,536 (87.18%)  14,866 (24.56%)   21.08 %
GK ONLY           37,947    30,613 (80.67%)  30,532 (99.74%)  17,374 (56.90%)   45.78 %

⭐ THE OUTFIELD LOSS SPLIT:  range 1,074 (1.93 %) · race 8,899 (15.99 %) · LANE 45,670 (82.08 %)
```

* **BU-C0's bound "OUTFIELD end-to-end conversion ≤ 22.2 %" is CONFIRMED and sharpened: 21.08 %.**
* **BU-C0's floor "the corridor owns ≥ ~67 % of the outfield loss" is CONFIRMED and sharpened:
  82.08 %** — the corridor is *more* dominant outfield-only than the GK-inclusive 77.45 % said.
* **BU-C0's "keeper conversion ≥ 42.1 %" is CONFIRMED: 45.78 %.** The keeper's ladder is a
  different shape entirely — he loses a fifth of his supply to RANGE (80.67 % at L2, because he
  stands far away) and then wins essentially every race (99.74 %) and survives the corridor at
  **56.90 %** against the outfielder's **24.56 %**. **The keeper's ball is 2.3× more likely to
  survive the lane than a team-mate's.**
* keeper share of surviving options **53.89 %** (BU-C0 armed: 54.20 %). Outfield behind-ball
  supply **0.3579 per reception**; only **35.66 %** of behind-ball options sit inside the engine's
  own 6–30 m choice window.

⭐ **The v7 arm independently REPLICATES BU-C0's armed arm on virgin seeds** (BU-C0 → here):
options/reception 0.7775 → **0.7762** · zero-option share 0.4373 → **0.4387** · 2+ band 0.1812 →
**0.1792** · behind-ball bodies 2.6051 → **2.6111** · keeper share 0.5420 → **0.5389** ·
intercepted 0.4946 → **0.4894** · backward completions 0.2982 → **0.2971**. Different seeds, same
picture.

### ⭐⭐ 4. THE FACES — and the answer is that NOTHING MOVES

**Not one of the 57 published faces has a resolved contrast.** The pre-registered directions:

| pre-registered direction | v7 (base) | v7+DV (slice) | slice − base [95 % CI] | verdict |
|---|---|---|---|---|
| ⭐ backward completed-pass share **UP** | 0.2971 | 0.3009 | **+0.0038** [−0.0024, +0.0099] | direction right, **UNRESOLVED** |
| lateral completed-pass share up | 0.1312 | 0.1276 | −0.0037 [−0.0091, +0.0016] | direction wrong, unresolved |
| ⭐ circulation (back + lateral) up | 0.4284 | 0.4285 | **+0.0001** [−0.0064, +0.0067] | **FLAT** |
| ⭐ interception terminal share **DOWN** | 0.4894 | 0.4973 | **+0.0079** [−0.0021, +0.0184] | direction **WRONG**, unresolved |
| ⭐ completion **UP** (Q06) | 0.6660 | 0.6659 | **−0.0000** [−0.0055, +0.0051] | **FLAT** |
| ⭐ behind-ball option existence | 0.7762 | 0.7757 | **−0.0005** [−0.0088, +0.0087] | **FLAT** |
| … outfield-only (the honest supply) | 0.3579 | 0.3605 | +0.0026 [−0.0038, +0.0093] | unresolved |
| total loss to an opponent (un-entangled) | 0.6814 | 0.6803 | −0.0011 [−0.0109, +0.0090] | flat |

The R-乙 re-run clause, REPORTED on both arms:

| | v7 | v7+DV | slice − base |
|---|---|---|---|
| **Q01** open-play spell mean (sim-s) | 4.1368 [4.0190, 4.2575] | 4.1314 [4.0166, 4.2537] | −0.0055 unresolved |
| **Q05** touches / spell | 2.5878 | 2.5779 | −0.0099 unresolved |
| **Q06** completion | 0.6660 | 0.6659 | −0.0000 unresolved |
| **Q07** forward share of attempts | 0.5781 | 0.5771 | −0.0010 unresolved |
| **Q14-shaped** pressed-reception share | 0.6551 | 0.6517 | −0.0034 unresolved |
| goals / match (guard, reported) | 2.4833 | 2.5367 | +0.0533 unresolved |

The terminal census (open-play spells) — ⚠ **both arms carry the L3 veto**, so the CONTRAST is
entanglement-free while the LEVELS carry BU-C0 §CORRECTIONS 3's ~14.5 pp `tackled`→`intercepted`
displacement:

| class | v7 (n=10,521) | v7+DV (n=10,530) |
|---|---|---|
| intercepted | 48.94 % | 49.73 % |
| lost, unattributed | 13.70 % | 13.15 % |
| outOfPlay | 9.46 % | 9.34 % |
| foul won | 8.52 % | 9.02 % |
| shot | 8.36 % | 8.16 % |
| tackled | 4.96 % | 4.58 % |
| goal | 3.92 % | 3.87 % |
| forced long | 1.61 % | 1.57 % |
| bad touch | 0.54 % | 0.57 % |

### ⭐⭐ 5. THE VERDICT — THE LABELLED HYPOTHESIS FIRES, AND IT IS A REAL RESULT

**「区域的知识买不到球路的安全。」**

The hypothesis was written before the battery and ratified by #286.3: *DV prices the destination
**ZONE**, not the **LANE***. The battery says exactly that. The seam is **live** (93/96 doors
cells move), the map is **matured and correctly shaped** (685,815 earned labels, own 4.19 % >
middle 2.82 % > final 1.91 % — the fifth registration's own scored ordering, pooled intact), the
arming is **honest** (earned knowledge, no constant touched), and **no face moves.**

**THE VERDICT OF RECORD: zone-grain knowledge cannot price lane-grain risk.** It routes to the
commander's menu, **not** to a weight nudge (M-BU.3).

⭐ **AND THE MEASUREMENT SAYS WHY, in the ladder's own arithmetic.** 82.08 % of the outfield
option loss is the CORRIDOR — a fact about *the line between two bodies*. The DV label's index is
the AIM ZONE: one of three pitch thirds. Two backward balls into the same third, one down an open
channel and one through a defender's shadow, carry **the identical price**. The map cannot
distinguish the two events the corridor distinguishes, so it can only shift the chooser's taste
*between thirds* — and the chooser was never third-phobic: it already plays **29.71 %** of its
completions backward.

⚠ **A SECOND, ARGUED (NOT MEASURED) OBSERVATION, declared as argued** — the deliberate discipline
#286.4 praised: the price's MAGNITUDE is small. `lossCost = belief[zone] · passBase`, and
`passBase`'s shipped default is **0.2** (`src/sim/types.ts:204`), so the learned price is
**0.00838 / 0.00563 / 0.00382** in score units for own / middle / final. Against candidate scores
of order 10⁻¹, the earned map moves the table by order 10⁻³. **No post-hoc probe was added to
measure the score spread** — that would be a counterfactual invented after sight, and the freeze
canon forbids it. The observation is arithmetic over two committed numbers, and it is offered as a
HYPOTHESIS for the commander's menu: *the map may be both wrong-grained AND quiet, and those are
different repairs.*

### Gate table

| gate | result | evidence |
|---|---|---|
| `gDet` | **PASS** | the whole core ran twice and re-derived bit-identically (`1d9108a9…5ff3`) |
| `xSrcUntouched` | **PASS** | ⭐ `git diff --stat HEAD -- src` AND `git status --porcelain -- src` both empty — the #286.5-corrected form, RIDDEN |
| `gArms` | **PASS** | 600/600 walks carried their arm LIVE (world 7, three CB doors, two L3 doors, both doses cell-for-cell, the learned belief present on the chooser's genome / absent on the base arm, nothing in `info.genome`, no arming at construction); the arms walked the SAME 300 seeds |
| `gDose` | **PASS** | L3: declared SHA, 27,368 labels, both groups · DV: declared SHA **and** the portable G-DET anchor `9bc1aaf9…`, 685,815 labels, all three zones evidenced, the exam's own own>middle>final shape intact |
| ⭐⭐ `gLifecycle` | **PASS** | 18 conjuncts: the firing half's six zeros, the non-firing half's zero knocks, the battery re-proof, non-vacuity (3,202 armings), the four src call-site counts, the scope (o2Look/ekHoldVeto off) and the exposure's own non-vacuity (o1PassWindup + c7Windup ON) |
| ⭐⭐ `gDoors` | **PASS** | 480 identity checks, 0 failures; all four liveness laws fire; the matrix is the full 2⁷ |
| `gNonPerturbing` | **PASS** | 50/50 control walks ended on the IDENTICAL signature, spell count and pass count with the oracle off |
| `gOracle` | **PASS** | 1,319,301 affordance calls · **0 nulls** · 4,867,044 corridor evaluations · both race and both corridor verdicts occur · the GK split sees both sides · the ±2 m band still traces to `src/**` |
| `gQ07` | **PASS** | attribution **100.00 %** · agreement 99.85 % · 37,048/37,048 completions attributed · no count exceeds the engine's own |
| `gSpells` | **PASS** | 31,354/31,354 spells classified, 21,051/21,051 open-play, the frozen class set |
| `gNonVacuity` | **PASS** | 0 zero-denominator cells across 114 published arm-cells · the histogram sums to its own denominator on 600/600 rows |
| `gFaces` | **PASS** | 114 re-derivations from the stored per-seed cells, 0 mismatches |
| `gClock` | **PASS** | 240 s applied, the 90 out of `Match.minute()`, the mapping derived, every walk stepped |
| `gSeed` | **PASS** | no clash with the consumed ledger, no internal clash, every walked seed inside the claimed bands |
| `gStats` | **PASS** | base 111,800 · min gap 200 · 2,000 resamples |
| `gEnvClean` | **PASS** | no rogue `BUT0_*`, no engine door set, the canonical-write guard live |
| `gHashEnvelope` | **PASS** | the body re-derived from disk; a cross-OUT with a DIFFERENT envelope has the IDENTICAL digest; no invocation fact inside the body |
| `gMutants` | **PASS** | ⭐⭐ **87 conjuncts, 87 mutants, 87 LIVE, 0 dead** — EXACTLY-ONE enforced |

---

## §DOUBTS

1. ⚠ **A NULL IS NOT A PROOF OF THE MECHANISM I NAMED.** The faces do not move; I have argued the
   grain mismatch from the ladder's arithmetic (82.08 % of the outfield loss is the corridor, and
   the DV label cannot see a corridor). But "zone-grain knowledge cannot price lane-grain risk"
   and "the price is too quiet to move any chooser" are **two different explanations of the same
   null**, and this battery does not separate them. The magnitude observation above is ARGUED, not
   measured. ⭐ The separating instrument is obvious and is NOT run here (it would be a
   post-sight counterfactual): dose the same map at an amplified weight and see whether the faces
   move at all. That belongs to the commander's menu, and it would need its own authorization
   because an amplified dose is a taste constant unless it is derived.
2. ⚠ **THE DOSE'S PROVENANCE WORLD IS NOT THIS WORLD.** The matured DV books were earned in the
   DV-T2-T1 exam's substrate, **not** in the CB+L3 composition. A team that had learned its loss
   map *inside this world* might hold a different map (the L3 veto changes which balls get cut).
   The dose is a DECLARED PRESENTATION (#270's form), exactly as the L3 entry's is, and this is
   its honest limit. In-run maturation was NOT needed (committed cells exist) and was NOT done.
3. ⚠ **BOTH ARMS CARRY THE L3 VETO**, so every terminal LEVEL here is entangled per BU-C0
   §CORRECTIONS 3 (~14.5 pp displaced from `tackled` into `intercepted`). The CONTRAST is clean by
   construction; the levels are not comparable to a bare-production number.
4. ⚠ **THE LEARNING DOOR STAYS OPEN DURING THE BATTERY.** `dvLearnedMap` learns as well as
   consumes, so each match adds ~10² labels to a book holding 685,815 — a <0.02 % perturbation. I
   did not run a static-map arm; the choice follows the L3 entry's precedent (learn + consume,
   dosed), and the effect is bounded by that ratio rather than measured.
5. ⚠ **THE `S ∧ ¬T` FINDING IS A CONFIGURATION NOBODY BUILDS.** I believe it is inert and I proved
   the two halves that make it inert (zero fires; no armed world constructs it). What I have NOT
   proven is that no FUTURE seam could read `forcedTouchPast` for something other than firing —
   the inertness is a property of today's single consumer, which `gLifecycle` pins at exactly one.
6. ⚠ **THE CORRIDOR RUNG IS STILL A SIGN TEST WITH NO TOLERANCE** (BU-C0 §DOUBTS 1, inherited): a
   defender arriving level with the ball counts as cutting it. Pressed-vs-unpressed survival
   (34.34 % vs 35.40 % on the v7 arm) again says the rung measures lanes, not markers.
7. ⚠ **THE #246 BAND WAS NEVER SCALED TO 6v6** (BU-C0 §DOUBTS 2, inherited) and no source for it
   was ever located. Nothing here is judged against it.
8. ⚠ **THE DOORS MATRIX IS EXHAUSTIVE IN FLAGS AND THIN IN SEEDS** (3). Its claims are structural
   — byte-identity and a lifecycle count — so a seed is a whole match's worth of evidence rather
   than a sample point; but a rare interaction that fires once per thousand matches would not be
   seen. The two liveness laws that missed a cell (veto 95/96, DV pair 93/96) are exactly that
   sampling texture: on those seeds the armed door had nothing to change.
9. ⚠ **NO PERFORMANCE CLAIM.** The artifact carries no timing (#266.3(a) / #258.3(1)), so no
   per-walk cost is quoted.

## §NON-CLAIMS

1. **NOTHING HERE IS SCORED.** H-BU.1 is scored at ARC EXIT on the assembled composition
   (#286.3's amended seat). Every football face is REPORTED, and no gate reads one.
2. The null is a result **about this arming of this seam in this composition** — not a verdict on
   the DV family, whose own registration (the fifth) scored POSITIVE on its own question.
3. The option oracle answers *could the engine's own machinery get the ball there* — capability,
   never choice, never perception.
4. **No weight was touched.** The pre-written hypothesis fired and the M-BU.3 line held.
5. The `S ∧ ¬T` finding is **routed, not fixed**: a fix is a `src` change needing its own
   authorization.
