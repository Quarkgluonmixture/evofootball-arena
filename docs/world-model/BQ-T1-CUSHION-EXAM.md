# BQ-T1 — 「缓冲留球」 THE CUSHION EXAM（球留在脚边了吗；几何那一类下去了吗；对手还抢得走吗；世界还是足球吗）

> **Authorized by COMMANDER RULING #385 item 5** (the exam form of record: BF-T1 / RC-T1b; the
> exact forms frozen here, at §P, BEFORE any battery seed). **Binding contract:**
> [`BK-BODYBALL-CONTRACT.md`](BK-BODYBALL-CONTRACT.md) **§2-AMENDMENT M-BK.5** — THE CUSHION KEEPS
> THE BALL; THE ROLL DECIDES THE TOUCH (#384 item 5). **The law under exam is the seam BQ-T0
> landed dormant** ([`BQ-T0-CUSHION-LAW.md`](BQ-T0-CUSHION-LAW.md), commit `0ae2bf8`).
>
> **Lineage.** [`PT-C0-PLAYTEST-FORENSIC-CENSUS.md`](PT-C0-PLAYTEST-FORENSIC-CENSUS.md) (the
> play-test forensic) → [`RC-T1B-READY-EXAM.md`](RC-T1B-READY-EXAM.md) (**FAIL** — not the
> receiver's readiness) → [`BN-C0-BOUNCE-CENSUS.md`](BN-C0-BOUNCE-CENSUS.md) (#382: the user's
> 「传到人身上弹回」 is a CONTROL-QUALITY event) →
> [`BQ-C0-FIRST-TOUCH-CENSUS.md`](BQ-C0-FIRST-TOUCH-CENSUS.md) (#383: the roll is an honest coin
> with no heavy face — and it is NOT the story) →
> [`BQ-C1-ATTEMPT-WINDOW-CENSUS.md`](BQ-C1-ATTEMPT-WINDOW-CENSUS.md) (#384: the window is MIXED,
> and its largest single piece is GEOMETRY) → [`BQ-T0-CUSHION-LAW.md`](BQ-T0-CUSHION-LAW.md)
> (#385: the honest law behind a shut door) → **this exam, which opens the door on world 12 and
> asks the three things the law owes.**
>
> Instrument: `scripts/probes/bq-t1-cushion-exam.ts`.
> Artifact: `docs/world-model/data/bq-t1-cushion-exam.json`
> (**or its `.RED.json` SIDE PATH** if any gate is red — the red-routing idiom, #334 item 5).
>
> ⛔ **THIS STAGE SHIPS NOTHING** (Road B). No world is cut, no preset is touched, `a4World.ts`
> contains **no** `bqCushion`, and the production fingerprint is the literal of record
> **`57b0bdab389122af5e4cacd75c4e13020b8ff248a413a7fcd71cc6215ba4c673`** — UNCHANGED. ⛔ **World 12's
> composition and bytes are untouched**: the user's play-test gate stays open in parallel and stays
> the user's. Whether a world 13 is cut is the commander's and the user's, not this exam's.
> ⛔ **X-SRC-ZERO**: no file under `src/` **or** `tests/` is created or edited — the law is already
> in the tree with its own 24-pin suite (`tests/bqCushion.test.ts`). The probe **CALLS** the
> shipped exports and reads `Match` state per tick; the **contest-episode ledger** and the **E1a
> first-touch ledger** are READ, never re-implemented; every `this.pendingControl = null` site is
> **ANCHORED at THIS head**. **THERE IS NO WRAPPER** — `gLockstep` proves observed ≡ unobserved
> byte for byte per arm, and `gTraceInert` proves both trace flags change no byte of the world.
> ⛔ **RECEIPTS ARE NEVER EFFECT SIZES.** The site counts, the creation-ledger agreement, the
> margin cross-check, the dose hashes and the divergence counts are ARMING PLUMBING (home: ruling
> #289 item 1 + [`BU-T1-MT-COMPOSITION.md`](BU-T1-MT-COMPOSITION.md) §COMMANDER CORRECTIONS item 5).
> ⛔ **NO SEASON LADDER**: a body law with no gene (#385 item 5(iii)).
>
> ⚠ **BQ-C1's FROZEN INSTRUMENT NO LONGER RE-ANCHORS AT THIS HEAD** (BQ-T0 §4 and #385 item 3: the
> ten `= null` line receipts moved and the contiguous cushion needle is split by the branch). This
> exam **COPIES** the classes, sites, attribution and precedence it reuses into its own instrument
> and **RE-DERIVES every receipt against the file in front of it**; ⛔ the banked census's own
> instrument is **NOT edited**.

## §0 THE WORDS OF RECORD

### RULING #385 item 5 — THE SCOPE (verbatim)

```text
5. ⭐⭐ **BQ-T1 DISPATCHED — 「缓冲留球」 THE CUSHION EXAM** (the exam form
   of record: BF-T1 / RC-T1b; definitions frozen at the executor's §P;
   X-SRC-ZERO): (i) ARMS — FOUR, PAIRED on shared seeds, world 12's
   composition, the composer CALLED, ALL FOUR with `traceContests: true`
   + `traceFirstTouch: true` (observation-only; inertness re-proven by
   gTraceInert): **E-SHUT** (empty book; `bqCushion` absent; every RC/BF
   flag absent) · **E-ARMED** (+ `bqCushion: true` in the constructor) —
   THE SCORED PAIR; **D-SHUT / D-ARMED** (dosed via the shipped loaders,
   the two byte-hashes pinned) — REPORTED with the frozen rules' words
   STORED. (ii) **H-BQ.1 on E-ARMED − E-SHUT**, the frozen form prints
   the words: **(a) 「留球」** — Δ of the intended target's NON-POSSESSION
   share (BQ-C1's `population.intended.nonPossessionShare`: pending
   controls for the intended target that end without his side holding
   the ball; BQ-C1's population, sites and attribution REUSED and
   RE-ANCHORED at this head) lies ENTIRELY BELOW ZERO ⇒ FALLS; **(b)
   「几何那一类」** — Δ of the RESOLVED-NOT-REACHED-margin class as a share
   of intended attempts lies ENTIRELY BELOW ZERO ⇒ FALLS (the mechanism
   moved); **(c) 「对抗不减」** — Δ of the ABANDONED-BY-CONTACT (opponent)
   class as a share of intended attempts is NOT entirely below zero
   (`!(ciHi < 0)`) ⇒ DOES-NOT-FALL (the duel intact; declared target
   from the smoke); **(d) DO-NO-HARM, BF-T1's bands verbatim**: (d1) Δ
   goals/match NOT entirely outside [−0.30, +0.30] ⇒ WITHIN-BAND · (d2)
   Δ `passCompletion` NOT entirely below −0.010 ⇒ DOES-NOT-FALL · (d3) Δ
   interceptions/match NOT entirely above +1.0 ⇒ DOES-NOT-RISE. **H-BQ.1
   = PASS ⇔ (a) ∧ (b) ∧ (c) ∧ (d1) ∧ (d2) ∧ (d3).** ⚠ A non-fall /
   non-rise certifies nothing smaller than its declared MDE. (iii)
   REPORTED (all four arms, every pair, same precision; the frozen rules
   applied to every reported pair and the words STORED): BQ-C1's FULL
   COMPOSITION of non-possession endings (every class, the sub-class
   splits, observed vs inferred) · the roll's realised fail share and
   mean pFail on intended targets (the roll now sees the attempts the
   margin used to swallow — its COUNT per match and its rate, both) ·
   BN-C0's own-target BOUNCE rate and the non-target teammate class
   (its predicate re-used; the user's third-sentence face) and BN-C0's
   settle ladder at +K after a failed touch · the user's three PT-C0
   faces (opponent-first-contact · `ownTargetSideBackShare` · 撞车) ·
   the receiver's first-touch sector shares on completed passes ·
   completion by sector · the contest-episode counts per match (opponent
   contacts inside the window — the duel's COUNT, not only its share) ·
   goals · shots · E4 (the anchored four) · the DF faces (乱跑 · marking
   coverage · tackles vs interceptions) · keeper faces · context (ground
   passes · carries · metres · possession spells). No season ladder (a
   body law, no gene). (iv) ESTIMATOR: cluster bootstrap over the shared
   seeds, 2,000 draws seeded from the block base **12,543,000**,
   ratio-of-sums; LEAVE-ONE-OUT flip counts on every SCORED Δ and on the
   dosed (a) Δ. (v) SEEDS: block **12,543,000–999**; four arms share
   seeds ⇒ N ≤ 998; N_FROZEN sized by a disclosed 12-seed smoke (scratch
   **900,003,200–211**; smoke receipt 900,003,220; curve pin
   900,003,270; trace-inert 900,003,280–281; lockstep 900,003,290–291)
   for (a) and (b) at their smoke-MDEs, (c) at its MDE, (d1) 0.30, (d2)
   0.010, (d3) 1.0 — the LARGEST N the block affords after the receipt
   **12,543,999**; BOOKED = WALKED; the tail declared; ZERO stats;
   registry 73. (vi) PRE-COMMITTED READS (frozen literals on stored
   booleans): PASS ⇒ *"BQ-T1 BANKS; THE CUSHION LAW IS AN ENTRY CANDIDATE
   — world 13 = world 12 + bqCushion — decided WITH the user's world-12
   verdict and read WITH the dosed pair; steps ②/③ open next."* · (a)
   or (b) fails ⇒ *"THE LAW DOES NOT KEEP THE BALL ON THE PITCH — THE FORM
   RETURNS TO THE COMMANDER WITH THE CLASS TABLE FIRST."* · (c) fails ⇒
   *"THE LAW WEAKENS THE DUEL — the commander decides with the contact
   counts."* · any (d) fails ⇒ *"THE LAW COSTS FOOTBALL — THE ARC PAUSES
   AT THE USER'S FORK."* (several ⇒ print each) · THE ENTRY QUESTION on
   D-ARMED − D-SHUT: dosed (a) `ciHi < 0` ∧ the dosed (d) words hold ⇒
   *"THE DOSED WORLD MOVES — the entry candidate is LIVE in the form the
   user plays."*; the dosed (a) interval contains zero ⇒ *"THE DOSED
   WORLD DOES NOT MOVE — no entry yet; the commander decides with
   numbers."*; otherwise ⇒ *"THE DOSED READ IS UNRESOLVED — the commander
   decides with numbers."* (vii) GATES (liveness / receipt, never
   direction): gWorld per arm (`raArmedVersion` 12; `bqCushion` per arm
   as declared; both traces true; `edsTouchCost` pinned; every RC/BF flag
   absent; `info.genome` clean) · gDoseSource (pinned) · gTraceInert ·
   gSiteEnumeration (BQ-C1's `= null` needle re-counted and re-anchored
   at THIS head — ten sites + the creation site, with the NEW line
   receipts; the cushion seam's own lines anchored) · gAttributionExhaustive
   (BQ-C1's exact-sum receipts) · gRuleWords (the six rules applied to
   every reported pair) · gArmsDiverge (SOME on the scored pair) ·
   gClassesNonVacuous (the margin class non-empty on SHUT — the mechanism
   exists to be moved; opponent contacts exist on every arm; roll
   failures exist on every arm) · gLockstep · gSrcUntouched ·
   gSeedsBookedEqualWalked · gN · gLOO · gHashOrder · gFaces. (viii)
   ARTIFACT `docs/world-model/data/bq-t1-cushion-exam.json` (compact;
   per-seed × per-arm cells; bins; the hash last with the non-body
   receipt); DOC `BQ-T1-CUSHION-EXAM.md` (the BF-T1 / RC-T1b form; §0
   this item VERBATIM; §P.A–F; §DEV-PREFLIGHT; §R…; HONEST LIMITS single
   home; the prose sweep; universal sentences as stored booleans or not
   at all — FIVE stages in a row have fallen to that canon); INSTRUMENT
   `scripts/probes/bq-t1-cushion-exam.ts` (BQ-C1's classes and
   attribution + BF-T1's paired-arm form, estimator, bands, stored words
   and faces + BN-C0's bounce predicate and ladder + PT-C0's three
   faces). FREEZE before any battery seed; RESULTS after; instrument diff
   EMPTY between them.```

### THE PRE-COMMITTED READS (verbatim, from #385 item 5(vi)) — FROZEN HERE, BEFORE ANY BATTERY SEED

| selector (STORED booleans only) | the sentence PRINTED, verbatim |
|---|---|
| **H-BQ.1 = PASS** | *"BQ-T1 BANKS; THE CUSHION LAW IS AN ENTRY CANDIDATE — world 13 = world 12 + bqCushion — decided WITH the user's world-12 verdict and read WITH the dosed pair; steps ②/③ open next."* |
| **(a) or (b) fails** | *"THE LAW DOES NOT KEEP THE BALL ON THE PITCH — THE FORM RETURNS TO THE COMMANDER WITH THE CLASS TABLE FIRST."* |
| **(c) fails** | *"THE LAW WEAKENS THE DUEL — the commander decides with the contact counts."* |
| **any (d) fails** | *"THE LAW COSTS FOOTBALL — THE ARC PAUSES AT THE USER'S FORK."* (several ⇒ print each) |
| **THE ENTRY QUESTION**, on D-ARMED − D-SHUT: dosed (a) `ciHi < 0` ∧ the dosed (d1)(d2)(d3) words hold | *"THE DOSED WORLD MOVES — the entry candidate is LIVE in the form the user plays."* |
| the dosed (a) interval CONTAINS ZERO | *"THE DOSED WORLD DOES NOT MOVE — no entry yet; the commander decides with numbers."* |
| otherwise | *"THE DOSED READ IS UNRESOLVED — the commander decides with numbers."* |

### in plain football language

四次普查把用户那句 「传到人身上弹回」 拆到了底。**不是**接球人没准备好（RC-T1b），**不是**球碰到
了他身体的哪一侧（BN-C0），**也不是**停球那一掷不公平（BQ-C0：硬币是诚实的，没有哪一面偏重）。
剩下的是**三拍那个窗口**——而窗口里最大的一块是**几何**：脚一碰到球，引擎把球**推离**这只脚，三拍
之后再问一句「球还在他脚边两厘米以内吗」。在够得着的**边缘**碰到的那一下，光凭几何就输了，连技术
都还没被问到。在用户玩的那个世界里，这一类占了他丢掉的接球的 **0.414040**（BQ-C1）。

BQ-T0 把诚实的那条法则放进了一扇**关着**的门：开门时，球接过人的速度，别的什么也不接。三拍的窗口
留着，两厘米留着，那一掷留着，撞身体和折射两条通道留着，对手照样能进来把球捅走。

这一版**开门**，在世界 12 上跑 998 个种子的对照，问四件事：

1. **球留住了吗？** 那个被传的人，丢掉的接球比例**下降**了吗。
2. **几何那一类下去了吗？** 法则瞄准的正是「三拍时人不在球边」这一类——它自己动了吗。
   （分母用的是**尝试次数**，不是「丢球的那些结局」，否则总量一变这个数就会跟着漂。）
3. **对手还抢得走吗？** 窗口里对方球员把球从他脚下捅走的那一类**不许**下降——那是真正的被断，
   是窗口存在的理由。这里同时报**次数**，不只报**比例**：比例不动而次数在动，是看得见的。
4. **世界还是足球吗？** 进球在带内、传球不更差、被断球不上升。

旁边还报三件事，一件都不作为闸门：**停球那一掷现在看到了什么**（被两厘米吞掉的那些尝试现在轮到
它来判，所以**次数**和**失败率**都要报）、**用户自己那三张脸**（传到对面身上 · 侧身接球 · 撞车），
以及**成熟账本**那一对（用户真正在玩的那个形态）。

⛔ **这一版什么都不上线。** 它只把数字摆出来。世界 13 开不开，是指挥官和用户的事。

## §P THE FREEZE (all of it BEFORE any battery seed)

### §P.A THE ARMS — FOUR, PAIRED on shared seeds, the world's own composer CALLED

Arm *k* walks seed *s* with the **IDENTICAL population construction** (BF-T1's own `buildMatch`
plumbing: `teamInfo('A', 2s+1)` / `teamInfo('B', 2s+2)`, `randomGenome` + `randomSquad`, the same
240 s match), so the four arms differ **ONLY** in the constructor flag and the doses. That is what
makes every Δ **PAIRED per seed**.

| arm | composition — the composer **CALLED, never copied** | `bqCushion` | role |
|---|---|---|---|
| **E-SHUT** | `new Match({…base, …a4MatchFlags(12), traceFirstTouch: true, traceContests: true})` + `armA4World(m, null, 12)` | **ABSENT** | the scored pair's shut side |
| **E-ARMED** | E-SHUT **+ `bqCushion: true` in the CONSTRUCTOR flags** | **`true`** | **THE SCORED PAIR** |
| **D-SHUT** | as E-SHUT but `armA4World(m, null, 12, L3_DOSE, PC_DOSE)` through the **SHIPPED loaders** | **ABSENT** | REPORTED |
| **D-ARMED** | D-SHUT + the constructor flag, exactly as E-ARMED | **`true`** | REPORTED |

**THE SCORED PAIR IS `E` (E-ARMED − E-SHUT) AND NOTHING ELSE.** The dosed pair is **REPORTED**, at
the same precision and the same prominence (§R5), with **the six frozen rules applied to it and
their WORDS STORED** (`gRuleWords`) — BF-T1 §COMMANDER CORRECTIONS item 1's own order.

**ALL FOUR ARMS carry `traceContests: true` AND `traceFirstTouch: true`.** Both are
**observation, not a world**. `gTraceInert` proves it on shared out-of-band scratch seeds
(**900,003,280–281**): the same seed built with both traces ON and both OFF runs to completion
with a **byte-identical whole-match signature**, on **every** arm.

**⭐⭐ THE CURVE IS PINNED BEFORE THE BATTERY** on a constructed match of **each** arm at scratch
seed **900,003,270**, stored, and asserted on **every** walked match by `gWorld` (`edsTouchCost`,
expected **false** — the base curve). The string `edsTouchCost` occurs **zero** times in the whole
world composer `src/game/a4World.ts` (an anchor with `want = 0`), and the probe's §1 envelope
**refuses the `EDS_BUNDLE` env outright**.

**⭐⭐ THE FLAG IS ARMED IN THE CONSTRUCTOR AND NOWHERE ELSE.** `bqCushion` occurs **zero** times in
`src/game/a4World.ts` (anchored, `want = 0`) — no world, preset, env or bundle names it. The
seam-map gate also counts it in `src/sim/Match.ts` (**5** occurrences — the initialiser names it
twice) and in `src/sim/League.ts` (**1**, the `matchFlags` key union), and enumerates every one by
line.

**THE DOSE PINS.** Canon, VERBATIM: *"a dose-source guard should hash the bytes it reads, not a
self-declared field"* (home: [`BU-T1-MT-COMPOSITION.md`](BU-T1-MT-COMPOSITION.md) §COMMANDER
CORRECTIONS item 6). `gDoseSource` hashes the FILE BYTES this process reads and compares them to
the two **PINNED** values of #385 item 5(i):

| dose file | PINNED expected sha256 |
|---|---|
| `docs/world-model/data/l3-t1-convergence-exam.json` | `a41a114c4727a2a6702bf4ca79b46b1d6924d80fa7fefb388d5364fd57da37db` |
| `docs/world-model/data/pc-t1-learning-exam.json` | `0301d7109cb0883a410a55cef9ff838dbce48d3627c418cbedd3e9e34448982f` |

⛔ **On any mismatch the instrument EXITS 3 before any walk** — a dose is never approximated.

### §P.B THE POPULATION AND THE FACES

#### ⭐⭐ THE SITE ENUMERATION, RE-COUNTED AND RE-ANCHORED AT THIS HEAD

The needle **`this.pendingControl = null`** is counted in `src/sim/Match.ts`. `gSiteEnumeration`
pins the count and lists EVERY occurrence with its line and its purpose. Canon, VERBATIM: *"a
seam-map gate pins occurrence COUNTS per needle and enumerates EVERY occurrence's site"* (home:
[`PC-C0-REACTION-BASELINE.md`](PC-C0-REACTION-BASELINE.md) §COMMANDER CORRECTIONS item 1).
**At this head the needle occurs TEN times** and the **creation** needle `this.pendingControl = {`
occurs **once** — BQ-C1's own list, at its NEW lines (BQ-T0 §4 declared the move; #385 item 3
recorded it):

| line | site | purpose (read from the surrounding code) | the ending class it serves |
|---|---|---|---|
| **3694** | `kickBall` | the low-level kick releases the ball with velocity and a re-capture cooldown; any pending control on that ball is void | `abandonedPossessionElsewhere` / `abandonedOther` |
| **3718** | `giveBall`'s OFFSIDE branch | the flagged target touches the ball, so the "reception" is a dead ball, not a control | `resolvedOffside` / `abandonedOffside` |
| **3861** | `giveBall`'s TAIL | a body is given clean control; the attempt is consumed by the possession it produced | `resolvedClean` |
| **4834** | `awardRestart` | a restart kills the live ball; the contest resolves `out` or `deadBall` | `abandonedDeadBallOut` |
| **5615** | ⭐ `bkApplyBodyStrike` | **THE DEFLECTION PRECEDENT**, in the engine's own comment | `abandonedBodyStrike` (ledger kind `body`) |
| **5669** | `applyControlContact`'s OFFSIDE branch | the cushioning contact itself is an offside reception | `abandonedOffside` |
| **5685** | ⭐⭐ `resolvePendingControlAttempt`'s OWN CLEAR | fires **UNCONDITIONALLY** once `stepCount >= readyTick`, BEFORE the pre-roll returns and before the roll | `resolvedClean` · `resolvedRollFail` · `resolvedNotReached*` |
| **5730** | `tryCapture`'s APPLIED-DEFLECTION branch | `mech.tryDeflection` succeeded | `abandonedDeflection` |
| **5847** | `setupKickoff` | a kickoff resets the world | `abandonedDeadBallOut` |
| **5914** | `endMatch` | THE WHISTLE | `abandonedDeadBallOut` |

**AND THE ELEVENTH ENDING, WHICH IS NOT A `= null` AT ALL** — the creation site at line **5674**
assigns straight over any attempt already there. That overwrite **is** ABANDONED-BY-CONTACT.

**⭐⭐ THE SEAM UNDER EXAM IS ANCHORED IN TWO HALVES.** BQ-C1 pinned the shipped cushion as ONE
contiguous needle; the branch now sits between the `release` clamp and the assignments, so this
instrument anchors **the release clamp**, **the branch test `if (!this.bqCushion) {`**, **the
SHIPPED two assignment lines** and **the ARMED two assignment lines** as four separate anchors,
each with its own line receipt.

#### THE FROZEN PRECEDENCE, AND WHY — BQ-C1 §P.B's, COPIED

`tryCapture` opens with `if (this.resolvePendingControlAttempt()) return;` (anchored), and the
resolver's own clear fires **before** its pre-roll returns and before the roll. Therefore an ending
**at or after `readyTick` is ALWAYS the resolver's site** and is classified `RESOLVED-*` before any
claim is considered. Inside the RESOLVED branch: **offside → the roll's failure → possession → not
reached**. Inside the ABANDONED branch: **offside → the replacement** (the engine's own
`pendingControl` field is the stronger record) **→ the deflection kinds → dead ball → possession
elsewhere → other**. Every step carries a constructed fixture in `gWalkFixtures`.

#### THE CLASSES, THEIR PUBLIC SIGNATURES, AND OBSERVED vs INFERRED — BQ-C1's, COPIED

Canon, VERBATIM: *"an event attribution reads the engine's own record when one exists (`shotLog`,
the contest episodes, `lastTouch`); a heuristic is written only where no record exists, and says
so"* (home: [`RC-T1B-READY-EXAM.md`](RC-T1B-READY-EXAM.md) §COMMANDER CORRECTIONS item 5).

| class | the public signature | OBSERVED / INFERRED |
|---|---|---|
| `resolvedClean` | the body **OWNS the ball** at the end tick | **OBSERVED** |
| `resolvedRollFail` | an **E1a first-touch trace entry** for that gid at that tick with `clean === false` | **OBSERVED** |
| `resolvedNotReachedStunnedOrOff` | `p.sentOff \|\| p.stunTimer > 0` at the END of the tick | **INFERRED — a BOUND** |
| `resolvedNotReachedMargin` | the residual of the NOT-REACHED parent | **INFERRED — a BOUND** |
| `resolvedOffside` · `abandonedOffside` | a NEW `restart` object carrying `offside === true` | **OBSERVED** |
| `abandonedContactOpponent` · `…Teammate` · `…SameReceiver` | a NEW `pendingControl` at the end of that tick — the engine's own field; the SIDE off the replacing body's own `side`, the KIND off the contest-episode ledger | **OBSERVED** |
| `abandonedDeflection` · `abandonedBodyStrike` | a contest-episode contact of kind `deflection` / `body` at that tick | **OBSERVED** |
| `abandonedDeadBallOut` | a NEW `restart`, a phase outside `playing`, or the whistle | **INFERRED** |
| `abandonedPossessionElsewhere` | possession by a **different** body at the end of that tick | **INFERRED** |
| `abandonedOther` | none of the above — the **RECEIPT class**, ⛔ never imputed | **INFERRED** |

⚠ **THE STUNNED / MARGIN SPLIT IS A BOUND** and stays one, exactly as BQ-C1 declared: the
resolver's two pre-roll returns are invisible from outside the step, `stunTimer` can be raised
later in the same tick, and **only the SUM of the two cells is exact** (`gAttributionExhaustive`).
Beside them the exam publishes the same **cross-check receipt** — the SHIPPED `directBallAccess`
RE-CALLED at the end tick with the resolver's own arguments — as a **DECLARED RECONSTRUCTION one
physics step later**, ⛔ never a class definition and ⛔ never a football effect size.

#### THE POPULATION AND THE OTHER DEFINITIONS, FROZEN

| quantity | frozen form |
|---|---|
| **⭐⭐ THE POPULATION** | every `pendingControl` **CREATED** (the `applyControlContact` creation site — the ONE creation site, anchored, needle count 1) tracked to the tick it **ENDS**. Rates on the **240 s clock** |
| **⚠ THE PRIVATE-FIELD READ, DECLARED** | `Match.pendingControl` is `private` and the engine publishes no mirror; the exam reads it through a TypeScript **TYPE VIEW** — a **READ**, never a write (the BQ-C0 / BQ-C1 precedent, #383 item 3), proven byte-inert by `gLockstep` |
| **⚠ THE TICK BOUNDARY, DECLARED** | the population is observed **after `m.step(DT)`**; an attempt created **AND** ended inside ONE tick is invisible. `population.creationLedgerAgreementShare` is the receipt |
| **INTENDED TARGET** | at the **creation** tick: `pendingPass !== null && pendingPass.targetGid === gid && pendingPass.side === side`. PRIMARY; all bodies published beside |
| **⭐⭐ THE DISPLACEMENT RULE** | for a RESOLVED-NOT-REACHED ending: `ballDisplacement` = \|ball at END − ball at CONTACT\|, `bodyDisplacement` the same for the receiver; `ballLarger` / `bodyLarger` / `tie` on a **STRICT** comparison |
| **⭐⭐ THE ROLL** | the **E1a ledger READ**: adjudications, failures and the **LOGGED `pFail`** on entries whose own `intendedTarget` flag is true. **THE COUNT AND THE RATE ARE BOTH PUBLISHED** — armed, the roll SEES attempts the margin used to swallow, so the count can move where the rate does not |
| **⭐⭐ THE DUEL'S COUNT** | endings on intended targets at which a body OF THE OTHER SIDE replaced a live attempt, **per match**, published BESIDE the same class's SHARE; the contest-episode ledger's own length per match beside it |
| **⭐⭐ THE BOUNCE** | **BN-C0 §P.B's predicate and settle ladder, REUSED and anchored**: an OWN-body FIRST contact of a MEASURED GROUND PASS after which the PASSING SIDE does not hold the ball at `contactTick + K`, with `K = CONTACT_CONTROL_DELAY_TICKS` **imported** (the `readyTick` form is anchored, and it is a CONSTANT offset). Ladder `sameSide` / `opponent` / `loose` / `out`; **BOUNCE = not `sameSide`**; `unresolved` (the whistle came first) is **COUNTED** and enters **NO** bounce face |
| **⭐ THE SETTLE LADDER AFTER A FAILED ROLL** | the same ladder read at `failTick + K` — who has the ball three ticks after the touch was spilled. The "passing side" for that read is the **FAILING BODY's own side**, declared |
| **⭐⭐ THE USER'S THREE PT-C0 FACES** | `contact.opponentFirstContactShare` · `contact.ownTargetSideBackShare` (the BK `BodySector` classifier **CALLED**) · `crowd.crashShare` (the A4 min-pairwise limb under `DUP_RUN_M` = 4 m, anchored), with the dup-run and spacing limbs beside — PT-C0's own code |
| **THE SECTORS** | the receiver's facing SECTOR at his first touch on **COMPLETED** passes (read at the completion tick) **and** COMPLETION BY SECTOR (of own-target FIRST contacts made in a sector, the share whose pass completed — read at the contact tick) |
| **THE DF FACES** | 乱跑 = `df.markSwitchesPerDefenderMinute`; MARKING COVERAGE = `df.markHeldShare`; tackles beside interceptions — **DF-C0 §R2's definitions REUSED VERBATIM through DF-T1 §3's instrument**, anchored line by line |
| **E4 + the keeper + context** | `e4.forwardPassShare` (`mt-ladder.ts`'s own line, anchored) · `thirdMan` · `overlaps` · `bestPassChain` (the engine's own counters, anchored) · `keeper.savesPerMatch` · `keeper.gkMetresPerKeeperPerMatch` · ground passes · carries · metres · moving ticks · possession spells |
| **⚠ POSSESSION SPELLS ARE A DECLARED RECONSTRUCTION** | **the engine keeps NO possession-spell ledger.** The face counts open-play ticks at which `Match.possessionSide` CHANGED to a valid side, read at tick boundaries — the exam's own cut, said so |
| **⭐ GOALS CONCEDED = `goalsPerMatch`** | the two sides are symmetric and **BOTH carry the armed law**, so the match total IS both keepers' concession — no second face |

### §P.C H-BQ.1 — THE EXACT FORM, THE PRE-COMMITTED READ, AND THE STORED-WORDS RULE

> **SCORED ON THE EMPTY-BOOK PAIR ONLY** (E-ARMED − E-SHUT).
> **(a) 「留球」** ⇔ the 95 % paired interval of Δ `population.intended.nonPossessionShare` — pending
> controls created for the `pendingPass` target that end in any class **other than**
> `resolvedClean`, over **all such pending controls** — lies **ENTIRELY BELOW ZERO** (`ciHi < 0`)
> ⇒ **FALLS**; otherwise **DOES-NOT-FALL**.
> **(b) 「几何那一类」** ⇔ the interval of Δ of the **RESOLVED-NOT-REACHED-margin** class **as a share
> of INTENDED ATTEMPTS** (class count ÷ pending controls created for intended targets — **NOT** ÷
> non-possession endings) lies **ENTIRELY BELOW ZERO** ⇒ **FALLS**; otherwise **DOES-NOT-FALL**.
> **(c) 「对抗不减」** ⇔ the interval of Δ of the **ABANDONED-BY-CONTACT (replacing body an OPPONENT)**
> class **as a share of INTENDED ATTEMPTS** is **NOT** entirely below zero (`!(ciHi < 0)`) ⇒
> **DOES-NOT-FALL**; otherwise **FALLS**.
> **(d) DO-NO-HARM — BF-T1's bands VERBATIM.** **(d1)** Δ `goalsPerMatch` **NOT** entirely outside
> **[−0.30, +0.30]** (`!(ciLo > 0.30 || ciHi < −0.30)`) ⇒ **WITHIN-BAND**; otherwise
> **OUTSIDE-BAND**. **(d2)** Δ `passCompletion` **NOT** entirely below **−0.010** ⇒
> **DOES-NOT-FALL**; otherwise **FALLS**. **(d3)** Δ `interceptionsPerMatch` **NOT** entirely above
> **+1.0**/match ⇒ **DOES-NOT-RISE**; otherwise **RISES**.
> **H-BQ.1 = PASS ⇔ (a) ∧ (b) ∧ (c) ∧ (d1) ∧ (d2) ∧ (d3).**

Each conjunct's word **and** the verdict word are **PRINTED BY THE FROZEN RULE**, **STORED** in the
artifact, and **re-derived off the SERIALIZED artifact by `gFaces`** from the stored Δ rows and the
stored targets.

⚠ **A NON-FALL / NON-RISE / WITHIN-BAND CERTIFIES NOTHING SMALLER THAN ITS DECLARED MDE** — said
wherever the word appears. ⚠ **(b) and (c) are shares of ATTEMPTS** so that a change in the
non-possession total cannot move them by composition alone; the **same classes as shares of
non-possession endings are published BESIDE** (`composition.intended.*`), REPORTED. ⚠ **BOTH SIDES
carry the armed law** in the armed arms, so every match-total face is a MATCH TOTAL.

**⭐⭐ THE STORED-WORDS RULE.** The **six frozen rules are applied to EVERY REPORTED PAIR** and the
resulting **WORDS are STORED beside the intervals** (`gRuleWords`, 12 rows = 2 pairs × 6
conjuncts). ⛔ **Only the E pair is SCORED**; the dosed words gate nothing. Canon, VERBATIM: *"a
counterfactual verdict sentence ('had X been scored, the rule would read W') quotes a word the
instrument STORED by applying the frozen rule to X's stored interval; a universal sentence about a
table ('every bin', 'the one bin') is a stored boolean or is not written"* (home:
[`BF-T1-FACING-COST-EXAM.md`](BF-T1-FACING-COST-EXAM.md) §COMMANDER CORRECTIONS items 1–2, ruling
#378 item 2). ⭐⭐ **FIVE STAGES IN A ROW HAVE FALLEN TO THAT SENTENCE**; this doc writes no
universal that is not a stored boolean.

**THE PRE-COMMITTED READ LITERALS ARE §0's TABLE**, frozen here in the instrument's own source and
selected on **stored booleans only**. ⛔ No tie-break is ever invented after sight.

### §P.D THE ESTIMATOR

**CLUSTER BOOTSTRAP over the SHARED seeds** (clusters = seeds, **2,000 draws**, rng seeded from the
block base **12,543,000**) — the BF-T1 / RC-T1a estimator. All four arms of a seed move together
inside every draw, so **every interval is a PAIRED one BY CONSTRUCTION**. Point estimates are
**ratio-of-sums**, so every headline re-derives from the stored per-seed cells.

**LEAVE-ONE-OUT flip counting on every SCORED Δ and on the DOSED (a) Δ** (the #346/#348 orders;
#385 item 5(iv)): drop each seed, re-derive the POINT Δ, and count a **FLIP** when the frozen
rule's verdict changes with the interval **SHIFTED** by that seed's influence. ⚠ **THE CONSERVATIVE
POINT-SHIFT FORM** — stated, never hidden.

Canon quoted where it applies — VERBATIM: *"a starred finding states its |Δ|÷half-width ratio"*
(home: [`BU-T0B-PRICE-SEPARATION.md`](BU-T0B-PRICE-SEPARATION.md) §COMMANDER CORRECTIONS item 2).

### §P.E SEEDS AND SIZING

* **Block 12,543,000–999**, partitioned so no seed is ever used twice: battery
  **12,543,000–12,543,997** (**N_FROZEN = 998 SHARED SEEDS**, each walked ONCE PER ARM) · the
  **UNWALKED TAIL 12,543,998 is DECLARED** and stays virgin (`seeds.unwalkedTail`) · the
  construction **RECEIPT** seed **12,543,999** (all four arms). **BOOKED = WALKED = 3,996 walks**
  (999 × 4).
* **Scratch, out-of-band only** (canon, VERBATIM: *"verifier scratch walks use the stage's own
  consumed band or the out-of-band scratch range (≥ 900,000,000) — never the next virgin block"*,
  home: [`PW-T0C-OBJECTIVE-FIDELITY.md`](PW-T0C-OBJECTIVE-FIDELITY.md) §COMMANDER CORRECTIONS item
  6): the 12-seed sizing smoke **900,003,200–211** with its receipt at **900,003,220**; the **curve
  pin** at **900,003,270**; **gTraceInert** at **900,003,280–281**; **gLockstep** at
  **900,003,290–291**. ⭐ **EVERY scratch seed walked is STORED in the artifact's `seeds` block.**
* **Stats consumed: ZERO.** Registry **73** untouched. Env whitelist: `BQT1_MODE` · `BQT1_N` ·
  `BQT1_OUT` — any other `BQT1_*` or any engine door env aborts with exit 3, and an override run
  may never write the canonical artifact path.
* **SIZING** (the house form; §DEV-PREFLIGHT's 12-seed scratch smoke is the variance source).
  ⭐⭐ **(a), (b) and (c) CARRY NO EXTERNALLY GIVEN TARGET**, so — exactly as #385 item 5(v) directs
  — **each one's DECLARED TARGET IS ITS OWN SMOKE-MDE AT N_FROZEN, ROUNDED UP TO 6 dp** (the
  BQ-C0 / RC-T1b idiom; **declared here**, and the round-up itself carries a fixture). (d1) (d2)
  and (d3) take BF-T1's bands as their targets. `N_FROZEN` is the LARGEST N the block affords once
  the receipt seed is reserved and the tail declared.

| paired Δ (E pair) | realised hw (12 scratch seeds) | declared target | N required | resolvable at 998 |
|---|---|---|---|---|
| `population.intended.nonPossessionShare` **(a)** | 0.04071163895299788 | **0.006382** (its own smoke-MDE, 6-dp round-up) | **998** | ✅ — a binding row |
| `attempts.intended.notReachedMarginOfAttempts` **(b)** | 0.014612529150810863 | **0.002291** (the same construction) | **998** | ✅ — a binding row |
| `attempts.intended.opponentContactOfAttempts` **(c)** | 0.010491614134037918 | **0.001645** (the same construction) | **998** | ✅ — a binding row |
| `goalsPerMatch` **(d1)** | 1.3333333333333335 | 0.30 goals | **485** | ✅ |
| `passCompletion` **(d2)** | 0.046711837396364475 | 0.010 | **535** | ✅ |
| `interceptionsPerMatch` **(d3)** | 4.083333333333332 | 1.0/match | **409** | ✅ |

  ⭐ **WHY THE (a)/(b)/(c) ROWS READ N = 998 BY CONSTRUCTION.** Their target IS the MDE the block
  buys, so the requirement lands on N_FROZEN by definition; the row is an honest statement of
  **what the block certifies**, not evidence that 998 was chosen to satisfy it. The expected MDEs
  at N_FROZEN are **0.006381160746980521** (a) · **0.0022903744439990567** (b) ·
  **0.0016444603559655731** (c) · **0.20898727116169916** (d1) · **0.0073216345713114145** (d2) ·
  **0.6400235179327034** (d3), each re-derived off disk by `gFaces`.
  ⚠ **12 SEEDS IS A NOISY VARIANCE ESTIMATE** — a strictly weaker assumption than sizing off a
  published battery. Said here, before the battery.
  ⛔ **WHAT THE BLOCK CANNOT RESOLVE IS DECLARED, NOT SIZED**: every REPORTED face (the whole dosed
  pair, the full composition, the roll, the bounce ladder, the sectors, the DF and keeper faces, E4
  and context) is published with its OWN realised interval and **no null may be cut on any of
  them** — an interval containing zero reads *"unresolved at this power"*, never *"no difference"*.

### §P.F THE GATES (all liveness / receipt — **NEVER direction**; all stored)

`gWorld` (per arm, on EVERY walked match and the construction receipt: `raArmedVersion === 12`;
`bqCushion` matching its OWN arm; **BOTH** trace flags TRUE; `edsTouchCost` PINNED and equal on
every match of all four arms; every RC/BF flag ABSENT; `info.genome` clean — canon: dose placement,
#270.2 / #334 item 1) · **`gDoseSource`** (the shipped loaders CALLED; the FILE BYTES hashed
against the two PINNED values; exit 3 on mismatch) · `gTraceInert` (both traces ON vs OFF,
byte-identical whole-match signatures, on shared out-of-band scratch seeds, **per arm**) ·
**`gSiteEnumeration`** (the `= null` needle **RE-COUNTED AND RE-ANCHORED AT THIS HEAD** — ten sites
with their NEW line receipts plus the creation site; the seam's SHIPPED and ARMED lines anchored
**separately**; `bqCushion` occurrences counted and enumerated in `Match.ts` and `League.ts` and
**ZERO** in `a4World.ts`) · **`gAttributionExhaustive`** (BQ-C1's exact-sum receipts: every ended
attempt in exactly one class, created = ended, observed + inferred = endings, the displacement
cells summing to the NOT-REACHED parent, the side × kind table summing to the ABANDONED-BY-CONTACT
parent — plus BN-C0's settle ladder summing to the own-body first contacts and PT-C0's
first-contact classes summing to the flights) · `gAnchoredConstants` · `gWalkFixtures` ·
**`gRuleWords`** (the six frozen rules applied to EVERY reported pair and the words STORED) ·
**`gArmsDiverge`** (**SOME-not-EVERY**, #364 item 1's reading, on the **SCORED** pair; a match with
no edge-of-reach contact may walk byte-identically, which is LEGAL) · **`gClassesNonVacuous`** (on
**every** arm including both SHUT arms: the intended attempt population, its non-possession
endings, **THE RESOLVED-NOT-REACHED-MARGIN CLASS** — the mechanism exists to be moved — **THE
OPPONENT-CONTACT CLASS**, **ROLL FAILURES**, the measured ground passes, the completed-pass facing
sample, both own-body settle populations, the shots, the crowd sample and the defender population)
· `gLedgerNonVacuous` · `gLockstep` · **`gSrcUntouched`** (`git diff --stat HEAD -- <dir>` **AND**
`git status --porcelain -- <dir>` both empty, over **`src/` AND `tests/`** — canon: xSrcUntouched)
· `gSeedsBookedEqualWalked` · `gN` · `gLOO` · **`gHashOrder`** (the body hash computed **LAST** off
an explicit **ALLOWLIST SCHEMA**) · **`gFaces`** (**EVERY** published face, Δ, stored bin,
exact-sum partition, sizing row, **H-BQ.1's SIX conjunct words, the VERDICT word, the STORED RULE
WORDS ON EVERY REPORTED PAIR, the dosed ENTRY READ and every PRE-COMMITTED READ SENTENCE**, all off
the **SERIALIZED** artifact off disk). **Plus the NON-body receipt
`receipts.hashReproducesFromFile`.**

Canon quoted where it applies — VERBATIM: *"the hashed body is built from an explicit ALLOWLIST
SCHEMA — a field not in the schema never enters the body; forbidden-name lists are retired"*
(home: [`PC-T0-LATENCY-SEAM.md`](PC-T0-LATENCY-SEAM.md) §COMMANDER CORRECTIONS item 1); VERBATIM:
*"the body hash is computed after every body key is assigned, and a NON-body receipt field records
that the hash reproduces from the written file"* (home:
[`RC-T1A-PRECUE-EXAM.md`](RC-T1A-PRECUE-EXAM.md) §COMMANDER CORRECTIONS item 3, ruling #372 item
3); VERBATIM: *"an artifact is written as compact JSON — no indentation; the hash is over the
canonical body regardless; pretty-printing is a reader's tool, not a storage form"* (home: ruling
#372 item 5); VERBATIM: *"the re-derivation gate covers EVERY published face; a percentile face
requires stored bins"* (home: ruling #287 item 1 +
[`PC-C0-REACTION-BASELINE.md`](PC-C0-REACTION-BASELINE.md) §COMMANDER CORRECTIONS item 4) — **this
exam publishes NO percentile face**; VERBATIM: *"a field carries the unit its name claims"* (home:
ruling #294 item 3); VERBATIM: *"a src-extracted constant pins its extraction to the NAMED call
site — anchored match + line receipt — never first-occurrence"* (home:
[`BK-C0-BODYBALL-CENSUS.md`](BK-C0-BODYBALL-CENSUS.md) §COMMANDER CORRECTIONS item 1); VERBATIM:
*"a scored face's walk-side predicate is pinned — anchored extraction or fixture — because the
re-derivation gate proves arithmetic, not definitions"* (home:
[`DF-T3-SURFACE-EXAM.md`](DF-T3-SURFACE-EXAM.md) §COMMANDER CORRECTIONS item 2), REFINED at #334
item 2: *"anchored extraction protects the source line; a headline-bearing walk-side predicate ALSO
needs a composition fixture"*; VERBATIM: *"a stage doc's prose quotes artifact FIELDS verbatim or
the number becomes a gated face"* (home:
[`PC-T2-ARMED-WORLD-READ.md`](PC-T2-ARMED-WORLD-READ.md) §COMMANDER CORRECTIONS item 4); VERBATIM:
*"a stage doc's numeric sweep covers EVERY numeric literal in prose at ANY precision; a
hand-written percentage is the likeliest second copy"* (home:
[`BF-C0-MOVEMENT-FACING-CENSUS.md`](BF-C0-MOVEMENT-FACING-CENSUS.md) §COMMANDER CORRECTIONS item 6,
ruling #374 item 3); VERBATIM: *"a gate's NOTE derives from the same pinned values the gate checks;
a count typed beside its pin is a second copy"* (home:
[`PT-C0-PLAYTEST-FORENSIC-CENSUS.md`](PT-C0-PLAYTEST-FORENSIC-CENSUS.md) §COMMANDER CORRECTIONS
item 1); VERBATIM: *"a dose-source guard should hash the bytes it reads, not a self-declared
field"*; VERBATIM: *"a stage doc's HONEST LIMITS list is the ONE home; the artifact stores that
list verbatim or stores none"* (home:
[`RC-C0-COOPERATION-CENSUS.md`](RC-C0-COOPERATION-CENSUS.md) §COMMANDER CORRECTIONS item 3) —
**this artifact stores NONE; §HONEST LIMITS below is the ONE home**; VERBATIM: *"a seam-map gate
pins occurrence COUNTS per needle and enumerates EVERY occurrence's site"*; VERBATIM: *"an event
attribution reads the engine's own record when one exists (`shotLog`, the contest episodes,
`lastTouch`); a heuristic is written only where no record exists, and says so"*. Canon
(paraphrase): **dose placement** — the dose is NEVER in `info.genome` (home: ruling #270.2 + #334
item 1) — **INVOKED as the world conjunct only**: this exam writes no dose of any kind. Canon
(paraphrase): **receipts ≠ effect sizes** (homes: ruling #289 item 1 +
[`BU-T1-MT-COMPOSITION.md`](BU-T1-MT-COMPOSITION.md) §COMMANDER CORRECTIONS item 5). Canon
(paraphrase): **freeze-before-battery** (home: ruling #266.3(c)) and **per-seed cells** (home:
ruling #282.2(ii)).

**CLOCK HONESTY.** Every rate is on the **240 s MATCH clock**; 1 sim-s = 60 ticks = 22.5 display-s
(homes: ruling #280.2(iii) + PC-T2 §CORR item 3).

## §DEV-PREFLIGHT — the sizing smoke, disclosed in full

A **12-SEED SCRATCH SMOKE** (`BQT1_MODE=smoke BQT1_N=12 BQT1_OUT=/tmp/bq-t1-smoke.json npx tsx
scripts/probes/bq-t1-cushion-exam.ts`, seeds **900,003,200–211**, receipt 900,003,220, the curve
pin at 900,003,270, `gTraceInert` on 900,003,280–281, `gLockstep` on 900,003,290–291, the artifact
OFF the canonical path) was run **BEFORE this freeze**. Its realised paired-Δ half-widths were read
out of the smoke artifact's own `deltas[].halfWidth` fields on the **E** pair — **never re-typed
from the console's rounded print** — and are hardcoded in the instrument's `SIZING_INPUTS` (the six
rows in §P.E's table).

**Disclosed honestly:**

* On the smoke run **`gN` and `gFaces` were RED BY CONSTRUCTION**: `SIZING_INPUTS` still carried
  placeholder half-widths of `0`, so `nRequired` was `NaN` and `SIZING_OK` was false, and the
  artifact routed to its `.RED.json` side path — which is exactly what the red-routing idiom is
  for. **Every other gate was GREEN (16/18)**, including `gWorld`, `gDoseSource`, `gTraceInert`,
  `gSiteEnumeration`, `gAttributionExhaustive`, `gAnchoredConstants`, `gWalkFixtures`,
  `gRuleWords`, `gArmsDiverge`, `gClassesNonVacuous`, `gLedgerNonVacuous`, `gLockstep`,
  `gSrcUntouched`, `gSeedsBookedEqualWalked`, `gLOO` and `gHashOrder`.
* ⭐ **THE TRACE OVERHEAD, MEASURED AND STATED** (#385's run discipline asks for it). Four
  out-of-band scratch seeds (900,003,280–281 and 900,003,290–291) were walked to completion three
  times each with **both traces ON** and with **both traces OFF**, interleaved, after a discarded
  warm-up pass: **traces ON 0.093250 s/match · traces OFF 0.093000 s/match ⇒ 0.000250 s/match, or
  0.27 %.** ⚠ A MACHINE READING ON ONE MACHINE, and the two arms of that measurement are within
  each other's run-to-run noise — the honest statement is *"the traces cost at most a fraction of a
  percent of the walk"*, not a resolved effect. The traces are proven **byte-inert** by
  `gTraceInert`; the timing figure is a **PLUMBING receipt**, ⛔ never a football number.
* ⭐ **THE CURVE, PINNED AT THE SMOKE**: `m.edsTouchCost` read **false** on a constructed match of
  **all four** arms ⇒ the exam measures the BASE curve, the expected value of #385 item 5(vii), and
  no deviation is triggered.
* ⭐ **THE SITE COUNT AND THE FLAG'S MAP, RE-DERIVED AT THE SMOKE**: the `= null` needle occurs
  **10** times at the lines §P.B lists, the creation needle **1** time at line 5674, and
  `bqCushion` occurs **5** times in `Match.ts`, **1** time in `League.ts` and **0** times in
  `a4World.ts` — the re-anchoring BQ-T0 §4 left as this stage's homework, done before the freeze.
* ⭐ **THE WALK COST, MEASURED**: `perf.meanWallSecondsPerWalk` on the smoke was
  **0.138923** s per walk with both traces and the whole observation running ⇒ the full battery
  (999 seeds × 4 arms = 3,996 walks) is expected to take roughly **9.3 minutes** of walking plus
  the bootstrap and the fixture suite.
* ⚠ **12 clusters is a NOISY variance estimate.** Said here, before the battery.
* ⚠ **The smoke's own POINT readings are UNPOWERED and bind NOTHING.** For the record, so nobody
  can claim the freeze was written after seeing a battery: on 12 scratch seeds the E pair's six
  conjunct words came out FALLS / FALLS / DOES-NOT-FALL / WITHIN-BAND / DOES-NOT-FALL /
  DOES-NOT-RISE and the frozen form printed the PASS literal and the dosed-moves literal.
  **None of those words is a finding**: they were read on 12 seeds against targets that did not
  yet exist (the sizing rows were placeholders), the artifact was routed RED, and the battery's own
  §R replaces every one of them. A battery that printed different words would be reported as-is.
* The smoke ALSO confirmed instrument liveness: on all four arms the intended-target attempt
  population, its non-possession endings, the RESOLVED-NOT-REACHED-margin class, the
  opponent-contact class, roll adjudications and roll failures, the measured ground passes, the
  completed-pass facing sample, both own-body settle populations, the shots, the crowd sample and
  the defender population all carried counts; `gTraceInert` and `gLockstep` were green on all their
  arm × scratch-seed walks; `gArmsDiverge` fired on the scored pair; and
  `receipts.hashReproducesFromFile` read **true** off the written file.
* **This section binds nothing.** The freeze is §0–§P.F above.

## §R RESULTS (every number below QUOTES the artifact's own fields at 6 dp — the artifact
## is the numbers of record, per the #357 standing order)

**RUN RECEIPTS.** Freeze **`a3a4932`** (`stage.headAtRun` = `a3a493209f31f3576be01b9b9ffdad2467d8798f`).
`git diff a3a4932..<results> -- scripts/probes/bq-t1-*.ts` is **EMPTY (0 bytes)** — no frozen
constant, no frozen definition, no frozen rule and no frozen sentence moved after sight.
**18/18 gates green**; `gFaces` **1152/1152 face-and-Δ**
checks and **135/135** verdict-word / stored-rule-word /
read-sentence / stored-bin / exact-sum-partition / sizing checks re-derived from the SERIALIZED
artifact off disk (the two counts are the lengths of the artifact's own
`gFacesDetail.faceChecks` and `gFacesDetail.binChecks` arrays, every entry `ok`).
Artifact `docs/world-model/data/bq-t1-cushion-exam.json` — **compact JSON**, **8,483,308 bytes**;
`instrumentSha256 = b1dd2d19554dff146fc69e39a696d2042df1cd0feef89dd7570a63eacb6f8908`;
`hashedBodySha256 = 8f59c4d544d254f9f2ddfd615c59361a31cc071ff3ed5ee07f19acf5360dad2f`; the NON-body receipt
`receipts.hashReproducesFromFile` = **true**;
**file byte-hash `45405256f1dbd781e30076caf5d922ff60302d01a5e27cd735604e362280434a`**.
Battery **998 seeds (12,543,000–12,543,997) × 4 ARMS + the
construction receipt at 12,543,999 ⇒ BOOKED = WALKED = 3,996 walks**;
the **unwalked tail is DECLARED** (`seeds.unwalkedTail` = seed 12,543,998).
`gTraceInert` on scratch 900,003,280 and 900,003,281;
`gLockstep` on 900,003,290 and 900,003,291; the curve pin on
900,003,270; the sizing smoke on 900,003,200–900,003,211
— every one STORED in the artifact's `seeds` block.
**ZERO stats consumed** — registry **73**.
**56** anchored sites, **71/71** walk fixtures,
**768** face rows, **384** paired Δs and **12** stored rule-word rows.
`npm run typecheck` clean with the probe in the tree; `npm run fingerprint` = `57b0bdab389122af5e4cacd75c4e13020b8ff248a413a7fcd71cc6215ba4c673` — **the
literal of record in `tests/a4HomeGrant.test.ts`, UNCHANGED** (an exam cannot move it).
Wall **530.591 s** (`perf.totalWallSeconds`; `perf.meanWallSecondsPerWalk`
**0.129338**).

**⭐⭐ THE SITES OF RECORD, RE-ANCHORED AT THIS HEAD.** `gSiteEnumeration` found the needle
`this.pendingControl = null` **10** times in `src/sim/Match.ts`, at lines
**3694 · 3718 · 3861 · 4834 · 5615 · 5669 · 5685 · 5730 · 5847 · 5914**, and the creation needle
`this.pendingControl = {` **1** time, at line **5674**.
The flag `bqCushion` occurs **5** times in `Match.ts` (lines
647, 1557, 2388, 2388, 5653 — the initialiser names it twice),
**1** time in `League.ts` (line 297) and
**0** times in `a4World.ts`.

### §R1 ⭐⭐ H-BQ.1 — THE SIX CONJUNCTS, SCORED ON E-ARMED − E-SHUT

| conjunct | face | E-SHUT | E-ARMED | Δ (ARMED − SHUT), 95 % paired CI | \|Δ\|÷hw | LOO flips | WORD |
|---|---|---|---|---|---|---|---|
| **(a)** | `population.intended.nonPossessionShare` | 0.177590 | 0.102921 | **-0.074668** [-0.080230, -0.069321] | 13.689514 | 0 | **FALLS** |
| **(b)** | `attempts.intended.notReachedMarginOfAttempts` | 0.087235 | 0.003205 | **-0.084030** [-0.086649, -0.081493] | 32.595921 | 0 | **FALLS** |
| **(c)** | `attempts.intended.opponentContactOfAttempts` | 0.021252 | 0.017583 | **-0.003669** [-0.007111, -0.000164] | 1.056201 | 9 | **FALLS** |
| **(d1)** | `goalsPerMatch` | 3.223447 | 3.299599 | **+0.076152** [-0.068136, +0.219439] · **CONTAINS ZERO** | 0.529617 | 0 | **WITHIN-BAND** |
| **(d2)** | `passCompletion` | 0.578445 | 0.586051 | **+0.007606** [+0.003012, +0.012079] | 1.677529 | 0 | **DOES-NOT-FALL** |
| **(d3)** | `interceptionsPerMatch` | 27.299599 | 27.242485 | **-0.057114** [-0.466934, +0.361723] · **CONTAINS ZERO** | 0.137848 | 0 | **DOES-NOT-RISE** |

**⭐⭐ H-BQ.1 = FAIL** — printed by the frozen rule from the stored booleans
(`hBQ1.abLimb` **true** · `hBQ1.cLimb` **false** · `hBQ1.dLimb` **true**), and re-derived off the SERIALIZED artifact by `gFaces`.

⚠ **A NON-FALL / NON-RISE / WITHIN-BAND CERTIFIES NOTHING SMALLER THAN ITS DECLARED MDE.** The
declared MDEs at N_FROZEN, from the artifact's own `sizing.rows`, are **0.006381** ((a)) · **0.002290** ((b)) · **0.001644** ((c)) · **0.208987** ((d1)) · **0.007322** ((d2)) · **0.640024** ((d3)); the
MDEs at the REALISED half-widths are **0.007797** ((a)) · **0.003685** ((b)) · **0.004965** ((c)) · **0.205531** ((d1)) · **0.006481** ((d2)) · **0.592244** ((d3)).

⚠ **(b) and (c) ARE SHARES OF ATTEMPTS, NOT OF NON-POSSESSION ENDINGS** (§P.C), so a change in
the non-possession total cannot move them by composition alone. The same two classes as shares
of NON-POSSESSION endings are published in §R2 beside, REPORTED.

### §R2 ⭐⭐ THE WINDOW UNDER THE LAW — the full composition on BOTH denominators, and the roll

**THE DENOMINATORS, both stored:** INTENDED ATTEMPTS ENDED **49,361** (E-SHUT) and
**46,181** (E-ARMED); NON-POSSESSION ENDINGS on intended targets **8,766**
and **4,753**. Attempts created per match: **49.459920**
→ **46.273547**
(Δ -3.186373 [-3.858717, -2.521042]).
OBSERVED attributions **0.911691** →
**0.995388**; INFERRED (published as BOUNDS)
**0.088309** →
**0.004612**.

**EVERY CLASS, on both denominators (E pair). The `OBS` column is the OBSERVED/INFERRED flag
frozen at §P.B; an INFERRED row is a BOUND.**

| class | OBS | ÷ NON-POSSESSION: E-SHUT → E-ARMED (Δ) | ÷ ATTEMPTS: E-SHUT → E-ARMED (Δ) | counts (SHUT / ARMED) |
|---|---|---|---|---|
| `resolvedRollFail` | OBS | 0.319644 → 0.693667 (+0.374023) | 0.056765 → 0.071393 (+0.014628) | 2,802 / 3,297 |
| `resolvedNotReachedMargin` | **INF** | 0.491216 → 0.031138 (-0.460078) | 0.087235 → 0.003205 (-0.084030) | 4,306 / 148 |
| `abandonedContactOpponent` | OBS | 0.119667 → 0.170839 (+0.051173) | 0.021252 → 0.017583 (-0.003669) | 1,049 / 812 |
| `abandonedContactTeammate` | OBS | 0.018024 → 0.012834 (-0.005190 · CONTAINS ZERO) | 0.003201 → 0.001321 (-0.001880) | 158 / 61 |
| `abandonedBodyStrike` | OBS | 0.045403 → 0.077846 (+0.032443) | 0.008063 → 0.008012 (-0.000051 · CONTAINS ZERO) | 398 / 370 |
| `abandonedDeadBallOut` | **INF** | 0.006046 → 0.013676 (+0.007629) | 0.001074 → 0.001408 (+0.000334 · CONTAINS ZERO) | 53 / 65 |

⚠ **`resolvedClean` IS DELIBERATELY ABSENT FROM THE TABLE ABOVE.** The composition faces are
defined over the NON-POSSESSION denominator for every class uniformly, so the possession
class's own row is a RATIO of possession endings to non-possession endings — **4.630961** (E-SHUT) → **8.716179** (E-ARMED) — and **not a**
**share of anything** (BQ-C1 §HONEST LIMITS 10, inherited). Its ATTEMPTS-denominator row is a
real share and IS published: `attempts.intended.resolvedCleanOfAttempts` **0.822410** → **0.897079** (Δ +0.074668).

**THE 7 CLASSES EMPTY ON BOTH ARMS OF THE SCORED PAIR**, read off the stored counts: `resolvedNotReachedStunnedOrOff`, `resolvedOffside`, `abandonedContactSameReceiver`, `abandonedDeflection`, `abandonedOffside`, `abandonedPossessionElsewhere`, `abandonedOther` — on intended targets.
The `abandonedOther` RECEIPT class totals **0**
(E-SHUT) / **0** (E-ARMED) on ALL BODIES:
**nothing had to be imputed.**

**THE PARENTS AND THE SUB-CLASS SPLITS (share of NON-POSSESSION endings, E pair):**

| face | E-SHUT | E-ARMED | Δ, 95 % paired CI | \|Δ\|÷hw |
|---|---|---|---|---|
| `composition.intended.parent.resolvedNotReached` | 0.491216 | 0.031138 | -0.460078 [-0.474714, -0.445851] | 31.880603 |
| `composition.intended.parent.abandonedByContact` | 0.137691 | 0.183673 | +0.045982 [+0.021051, +0.071284] | 1.830756 |
| `composition.intended.parent.line` | 0.006046 | 0.013676 | +0.007629 [+0.004065, +0.011318] | 2.104036 |
| `notReached.intended.ballLargerOfNonPossession` | 0.315765 | 0.019777 | -0.295988 [-0.308339, -0.283801] | 24.125372 |
| `notReached.intended.bodyLargerOfNonPossession` | 0.175451 | 0.011361 | -0.164089 [-0.173344, -0.155509] | 18.400783 |
| `notReached.intended.ballLargerOfNotReached` | 0.642824 | 0.635135 | -0.007689 [-0.083708, +0.072404] · **CONTAINS ZERO** | 0.098504 |
| `notReached.intended.bodyLargerOfNotReached` | 0.357176 | 0.364865 | +0.007689 [-0.071696, +0.084040] · **CONTAINS ZERO** | 0.098742 |
| `abandonedSplit.opponent.total` | 0.869097 | 0.930126 | +0.061029 [+0.008886, +0.124659] | 1.054283 |
| `abandonedSplit.teammate.total` | 0.130903 | 0.069874 | -0.061029 [-0.124442, -0.008744] | 1.054967 |

**⭐⭐ THE ROLL — THE COUNT AND THE RATE, BOTH (the E1a ledger READ, intended targets):**

| face | E-SHUT | E-ARMED | Δ, 95 % paired CI | \|Δ\|÷hw |
|---|---|---|---|---|
| `roll.adjudicationsPerMatchIntended` | 28.555110 | 32.834669 | +4.279559 [+3.820641, +4.752505] | 9.184946 |
| `roll.failuresPerMatchIntended` | 2.807615 | 3.303607 | +0.495992 [+0.351703, +0.639279] | 3.449477 |
| `roll.realisedFailShareIntended` | 0.098323 | 0.100613 | +0.002291 [-0.002306, +0.006904] · **CONTAINS ZERO** | 0.497437 |
| `roll.meanLoggedPFailIntended` | 0.099676 | 0.099814 | +0.000138 [-0.000436, +0.000702] · **CONTAINS ZERO** | 0.242390 |
| `roll.adjudicationsPerMatchAll` | 44.026052 | 50.511022 | +6.484970 [+5.849699, +7.120240] | 10.208202 |
| `roll.realisedFailShareAll` | 0.103259 | 0.104999 | +0.001740 [-0.001873, +0.005389] · **CONTAINS ZERO** | 0.479140 |
| `context.miscontrolsPerMatch` | 4.587174 | 5.348697 | +0.761523 [+0.576152, +0.944890] | 4.130435 |

**THE PLUMBING RECEIPTS** (⛔ never football effect sizes): `population.creationLedgerAgreementShare`
**0.982278** (E-SHUT) /
**0.982659** (E-ARMED);
`notReached.marginCrossCheckAgreementShare` (over ALL BODIES' NOT-REACHED endings — the
intended-target parent is 4,306 / 148; §COMMANDER CORRECTIONS item 2)
**0.985415** (9,864 of 10,010) /
**0.715976** (363 of 507);
`reconciliation.resolutionsPerMatch` **121.945892** /
**115.160321**.

### §R3 ⭐⭐ THE DUEL — the opponent class's SHARE **and** its COUNT

| face | SHUT | ARMED | Δ, 95 % paired CI | \|Δ\|÷hw |
|---|---|---|---|---|
| `attempts.intended.opponentContactOfAttempts` | 0.021252 | 0.017583 | -0.003669 [-0.007111, -0.000164] | 1.056201 |
| `composition.intended.abandonedContactOpponent` | 0.119667 | 0.170839 | +0.051173 [+0.027682, +0.076754] | 2.085597 |
| `duel.opponentContactsPerMatch` | 1.051102 | 0.813627 | -0.237475 [-0.410822, -0.065130] | 1.373913 |
| `duel.teammateContactsPerMatch` | 0.158317 | 0.061122 | -0.097194 [-0.184369, -0.036072] | 1.310811 |
| `attempts.intended.abandonedContactTeammateOfAttempts` | 0.003201 | 0.001321 | -0.001880 [-0.003630, -0.000627] | 1.252378 |
| `duel.contestEpisodesPerMatch` | 110.695391 | 112.373747 | +1.678357 [+0.710421, +2.602204] | 1.774364 |
| `duel.tacklesPerMatch` | 1.330661 | 1.401804 | +0.071142 [-0.028056, +0.177355] · **CONTAINS ZERO** | 0.692683 |
| `df.interceptionsPerMatch` | 27.299599 | 27.242485 | -0.057114 [-0.466934, +0.361723] · **CONTAINS ZERO** | 0.137848 |

⭐ **THE SHARE AND THE COUNT ARE BOTH PUBLISHED**, which is the only way a stable share with a
moving count is visible: the opponent class is **0.021252** → **0.017583** of
intended attempts (Δ -0.003669 [-0.007111, -0.000164]) while the COUNT is
**1.051102** → **0.813627** contacts per 240 s match (Δ -0.237475
[-0.410822, -0.065130]).

⭐⭐ **THE THREE DENOMINATORS DISAGREE ON THIS ONE CLASS, AND THE FROZEN RULE READ THE ONE IT
WAS FROZEN ON.** The opponent class's stored interval as a share of NON-POSSESSION endings lies
entirely ABOVE zero (`composition.intended.abandonedContactOpponent` 0.119667 → 0.170839,
Δ +0.051173 [+0.027682, +0.076754]); its stored interval as a share of ATTEMPTS lies entirely
BELOW zero (Δ −0.003669 [−0.007111, −0.000164]); its stored interval as a COUNT per match lies
entirely BELOW zero (Δ −0.237475 [−0.410822, −0.065130]) — three stored intervals read as
intervals, no rule word stored for the two unscored faces (§COMMANDER CORRECTIONS item 3). The attempt
population itself fell — 49.459920 → 46.273547 intended attempts per match
(Δ -3.186373 [-3.858717, -2.521042]) — because armed, more attempts end
clean and are not re-opened. ⛔ **The exam adjudicates none of that**: (c) was frozen on the
ATTEMPTS denominator, that interval lies entirely below zero, the frozen rule prints **FALLS**,
and the pre-committed sentence follows.

⚠ **(c)'s OWN STABILITY NUMBERS, PRINTED BESIDE THE OTHER FIVE SO THE READER RANKS THEM
HIMSELF.** Its |Δ|÷half-width is
**1.056201**, against a 13.689514 · b 32.595921 · d1 0.529617 · d2 1.677529 · d3 0.137848; its interval's
upper edge is **-0.000164**; it carries **9** LEAVE-ONE-OUT flips out of
**998** seeds against a 0 · b 0 · d1 0 · d2 0 · d3 0 and dosed (a) 0; and its |Δ| **0.003669**
is SMALLER than the MDE at its own realised half-width, **0.004965**. ⛔ That is
a statement about this Δ's stability, ⛔ not a re-reading of the frozen rule: the word STANDS as
the frozen form printed it, and the commander decides with the counts.

### §R4 THE USER'S FACES, THE SECTORS, THE DEFENCE, E4 AND CONTEXT (the SCORED pair)

**THE USER'S THREE PT-C0 FACES (and the A4 limbs beside them):**

| face | SHUT | ARMED | Δ, 95 % paired CI | \|Δ\|÷hw |
|---|---|---|---|---|
| `contact.opponentFirstContactShare` | 0.322698 | 0.322064 | -0.000634 [-0.005001, +0.003695] · **CONTAINS ZERO** | 0.145826 |
| `contact.ownTargetSideBackShare` | 0.396608 | 0.400201 | +0.003593 [-0.001268, +0.008158] · **CONTAINS ZERO** | 0.762312 |
| `crowd.crashShare` | 0.454609 | 0.453484 | -0.001125 [-0.006531, +0.004672] · **CONTAINS ZERO** | 0.200856 |
| `crowd.dupRunPairsPerSample` | 0.655681 | 0.650171 | -0.005509 [-0.016455, +0.006137] · **CONTAINS ZERO** | 0.487699 |
| `crowd.nearestMateMeanMetres` | 8.949807 | 8.961644 | +0.011837 [-0.026288, +0.048769] · **CONTAINS ZERO** | 0.315401 |
| `contact.noneFirstContactShare` | 0.001232 | 0.001029 | -0.000202 [-0.000520, +0.000111] · **CONTAINS ZERO** | 0.642080 |

**BN-C0's BOUNCE FACE AND SETTLE LADDER (its predicate and ladder REUSED, anchored):**

| face | SHUT | ARMED | Δ, 95 % paired CI | \|Δ\|÷hw |
|---|---|---|---|---|
| `bounce.ownTargetBounceRate` | 0.227069 | 0.143344 | -0.083725 [-0.088710, -0.078558] | 16.495478 |
| `bounce.ownTargetFirstShare` | 0.570634 | 0.572475 | +0.001841 [-0.003110, +0.006606] · **CONTAINS ZERO** | 0.379049 |
| `bounce.ownTargetUnresolvedShare` | 0.000427 | 0.000257 | -0.000170 [-0.000428, +0.000067] · **CONTAINS ZERO** | 0.687481 |
| `bounce.ownNonTargetBounceRate` | 0.802799 | 0.745264 | -0.057535 [-0.069802, -0.046029] | 4.840443 |
| `bounce.ownNonTargetFirstShare` | 0.105436 | 0.104432 | -0.001005 [-0.004069, +0.002121] · **CONTAINS ZERO** | 0.324668 |
| `bounce.ownTargetSettle.sameSide` | 0.772601 | 0.856436 | +0.083835 [+0.078647, +0.088807] | 16.502370 |
| `bounce.ownTargetSettle.opponent` | 0.000000 | 0.000000 | +0.000000 [+0.000000, +0.000000] · **CONTAINS ZERO** | null |
| `bounce.ownTargetSettle.loose` | 0.221255 | 0.136232 | -0.085023 [-0.089821, -0.079912] | 17.160450 |
| `bounce.ownTargetSettle.out` | 0.005717 | 0.007075 | +0.001358 [+0.000370, +0.002332] | 1.384669 |
| `rollFailSettle.sameSide` | 0.000000 | 0.000000 | +0.000000 [+0.000000, +0.000000] · **CONTAINS ZERO** | null |
| `rollFailSettle.opponent` | 0.000000 | 0.000000 | +0.000000 [+0.000000, +0.000000] · **CONTAINS ZERO** | null |
| `rollFailSettle.loose` | 1.000000 | 1.000000 | +0.000000 [+0.000000, +0.000000] · **CONTAINS ZERO** | null |
| `rollFailSettle.out` | 0.000000 | 0.000000 | +0.000000 [+0.000000, +0.000000] · **CONTAINS ZERO** | null |
| `rollFailSettle.unresolvedShare` | 0.000000 | 0.000000 | +0.000000 [+0.000000, +0.000000] · **CONTAINS ZERO** | null |

**THE SECTORS — the receiver's first touch, and COMPLETION BY SECTOR:**

| face | SHUT | ARMED | Δ, 95 % paired CI | \|Δ\|÷hw |
|---|---|---|---|---|
| `contact.receiverFrontShareCompleted` | 0.400989 | 0.381768 | -0.019220 [-0.025375, -0.013269] | 3.175372 |
| `contact.receiverSideShareCompleted` | 0.530344 | 0.544123 | +0.013779 [+0.007159, +0.020356] | 2.088064 |
| `contact.receiverBackShareCompleted` | 0.068667 | 0.074109 | +0.005442 [+0.002055, +0.008770] | 1.620705 |
| `sector.firstTouch.frontShare` | 0.304970 | 0.300929 | -0.004040 [-0.010002, +0.001810] · **CONTAINS ZERO** | 0.684121 |
| `sector.firstTouch.sideShare` | 0.605337 | 0.610849 | +0.005512 [-0.000951, +0.011890] · **CONTAINS ZERO** | 0.858407 |
| `sector.firstTouch.backShare` | 0.089693 | 0.088222 | -0.001471 [-0.005206, +0.002253] · **CONTAINS ZERO** | 0.394467 |
| `sector.completionBy.front` | 0.902536 | 0.907426 | +0.004890 [-0.002364, +0.011755] · **CONTAINS ZERO** | 0.692688 |
| `sector.completionBy.side` | 0.926366 | 0.929890 | +0.003525 [-0.000736, +0.008154] · **CONTAINS ZERO** | 0.792927 |
| `sector.completionBy.back` | 0.853478 | 0.862361 | +0.008883 [-0.007055, +0.024123] · **CONTAINS ZERO** | 0.569820 |

**THE DEFENCE AND THE KEEPER (DF-T1 §3's instrument, REUSED VERBATIM):**

| face | SHUT | ARMED | Δ, 95 % paired CI | \|Δ\|÷hw |
|---|---|---|---|---|
| `df.markSwitchesPerDefenderMinute` | 7.106746 | 7.064673 | -0.042073 [-0.132290, +0.044706] · **CONTAINS ZERO** | 0.475409 |
| `df.markHeldShare` | 0.634515 | 0.635787 | +0.001272 [-0.000606, +0.003476] · **CONTAINS ZERO** | 0.623061 |
| `duel.tacklesPerMatch` | 1.330661 | 1.401804 | +0.071142 [-0.028056, +0.177355] · **CONTAINS ZERO** | 0.692683 |
| `keeper.savesPerMatch` | 5.126253 | 5.093186 | -0.033066 [-0.201403, +0.140281] · **CONTAINS ZERO** | 0.193548 |
| `keeper.gkMetresPerKeeperPerMatch` | 251.783692 | 252.603030 | +0.819338 [-0.778014, +2.412728] · **CONTAINS ZERO** | 0.513572 |

**GOALS, SHOTS AND E4 (the anchored definitions):**

| face | SHUT | ARMED | Δ, 95 % paired CI | \|Δ\|÷hw |
|---|---|---|---|---|
| `goalsPerMatch` | 3.223447 | 3.299599 | +0.076152 [-0.068136, +0.219439] · **CONTAINS ZERO** | 0.529617 |
| `shotsPerMatch` | 12.473948 | 12.545090 | +0.071142 [-0.192385, +0.348697] · **CONTAINS ZERO** | 0.262963 |
| `e4.forwardPassShare` | 0.557448 | 0.557699 | +0.000251 [-0.004060, +0.004702] · **CONTAINS ZERO** | 0.057383 |
| `e4.thirdManPerMatch` | 3.013026 | 3.104208 | +0.091182 [-0.048096, +0.245491] · **CONTAINS ZERO** | 0.621160 |
| `e4.overlapsPerMatch` | 0.028056 | 0.036072 | +0.008016 [-0.007014, +0.024048] · **CONTAINS ZERO** | 0.516129 |
| `e4.bestPassChainMeanPerTeam` | 3.869739 | 3.937375 | +0.067635 [-0.005010, +0.141283] · **CONTAINS ZERO** | 0.924658 |

**CONTEXT (every rate on the 240 s match clock):**

| face | SHUT | ARMED | Δ, 95 % paired CI | \|Δ\|÷hw |
|---|---|---|---|---|
| `passCompletion` | 0.578445 | 0.586051 | +0.007606 [+0.003012, +0.012079] | 1.677529 |
| `context.passesPerMatch` | 79.073146 | 79.978958 | +0.905812 [+0.252505, +1.593186] | 1.351271 |
| `context.groundPassesPerMatch` | 74.022044 | 74.954910 | +0.932866 [+0.179359, +1.706413] | 1.221785 |
| `context.carriesPerMatch` | 85.268537 | 86.609218 | +1.340681 [+0.591182, +2.070140] | 1.813008 |
| `context.metresPerMatch` | 9150.854361 | 9156.515499 | +5.661138 [-30.997988, +41.189416] · **CONTAINS ZERO** | 0.156846 |
| `context.movingTicksPerMatch` | 132283.807615 | 132466.662325 | +182.854709 [-288.428858, +649.858717] · **CONTAINS ZERO** | 0.389763 |
| `context.meanSpeedMps` | 4.150555 | 4.147390 | -0.003165 [-0.008566, +0.002343] · **CONTAINS ZERO** | 0.580285 |
| `context.openPlayTicksPerMatch` | 12614.235471 | 12628.380762 | +14.145291 [-24.564128, +52.591182] · **CONTAINS ZERO** | 0.366671 |
| `context.possessionSpellsPerMatch` | 30.992986 | 31.189379 | +0.196393 [-0.246493, +0.646293] · **CONTAINS ZERO** | 0.439955 |
| `context.meanPossessionSpellSimSeconds` | 6.736320 | 6.696354 | -0.039965 [-0.132350, +0.052244] · **CONTAINS ZERO** | 0.433008 |
| `context.ticksPerMatch` | 15277.260521 | 15283.545090 | +6.284569 [-21.908818, +34.502004] · **CONTAINS ZERO** | 0.222814 |

### §R5 ⭐⭐ THE DOSED PAIR — the form the user plays (REPORTED, at the same precision and the
### same prominence; the six frozen rules APPLIED and their WORDS STORED)

| conjunct | face | D-SHUT | D-ARMED | Δ, 95 % paired CI | \|Δ\|÷hw | LOO flips | STORED WORD |
|---|---|---|---|---|---|---|---|
| (a) | `population.intended.nonPossessionShare` | 0.188637 | 0.117556 | **-0.071082** [-0.076715, -0.065064] | 12.202208 | 0 | **FALLS** |
| (b) | `attempts.intended.notReachedMarginOfAttempts` | 0.077366 | 0.001666 | **-0.075700** [-0.077705, -0.073612] | 36.992011 | — | **FALLS** |
| (c) | `attempts.intended.opponentContactOfAttempts` | 0.033325 | 0.026586 | **-0.006739** [-0.011421, -0.002164] | 1.455814 | — | **FALLS** |
| (d1) | `goalsPerMatch` | 2.556112 | 2.568136 | **+0.012024** [-0.120240, +0.141283] · **CONTAINS ZERO** | 0.091954 | — | **WITHIN-BAND** |
| (d2) | `passCompletion` | 0.588281 | 0.592057 | **+0.003777** [-0.000592, +0.008162] · **CONTAINS ZERO** | 0.862827 | — | **DOES-NOT-FALL** |
| (d3) | `interceptionsPerMatch` | 30.845691 | 31.079158 | **+0.233467** [-0.239479, +0.686373] · **CONTAINS ZERO** | 0.504329 | — | **DOES-NOT-RISE** |

⛔ **THE DOSED PAIR IS SCORED BY NOTHING.** Its words are the frozen rules APPLIED and STORED
(`gRuleWords`), exactly as BF-T1 §COMMANDER CORRECTIONS item 1 ordered; they gate no verdict.
The stored selectors of the ENTRY QUESTION: `dosedAFalls` **true** ·
`dosedAContainsZero` **false** · `dosedDWordsHold`
**true**.

**THE DUEL, DOSED:**

| face | SHUT | ARMED | Δ, 95 % paired CI | \|Δ\|÷hw |
|---|---|---|---|---|
| `attempts.intended.opponentContactOfAttempts` | 0.033325 | 0.026586 | -0.006739 [-0.011421, -0.002164] | 1.455814 |
| `composition.intended.abandonedContactOpponent` | 0.176662 | 0.226160 | +0.049497 [+0.022614, +0.075039] | 1.888293 |
| `duel.opponentContactsPerMatch` | 1.900802 | 1.406814 | -0.493988 [-0.762525, -0.236473] | 1.878095 |
| `duel.teammateContactsPerMatch` | 0.124248 | 0.096192 | -0.028056 [-0.076152, +0.020040] · **CONTAINS ZERO** | 0.583333 |
| `attempts.intended.abandonedContactTeammateOfAttempts` | 0.002178 | 0.001818 | -0.000360 [-0.001227, +0.000496] · **CONTAINS ZERO** | 0.418537 |
| `duel.contestEpisodesPerMatch` | 116.796593 | 118.245491 | +1.448898 [+0.484970, +2.398798] | 1.514136 |
| `duel.tacklesPerMatch` | 2.183367 | 2.205411 | +0.022044 [-0.104208, +0.149299] · **CONTAINS ZERO** | 0.173913 |
| `df.interceptionsPerMatch` | 30.845691 | 31.079158 | +0.233467 [-0.239479, +0.686373] · **CONTAINS ZERO** | 0.504329 |

**THE USER'S THREE FACES, DOSED:**

| face | SHUT | ARMED | Δ, 95 % paired CI | \|Δ\|÷hw |
|---|---|---|---|---|
| `contact.opponentFirstContactShare` | 0.323588 | 0.324659 | +0.001071 [-0.003094, +0.005281] · **CONTAINS ZERO** | 0.255665 |
| `contact.ownTargetSideBackShare` | 0.429367 | 0.428876 | -0.000491 [-0.005026, +0.004383] · **CONTAINS ZERO** | 0.104397 |
| `crowd.crashShare` | 0.486591 | 0.490839 | +0.004249 [-0.001290, +0.009630] · **CONTAINS ZERO** | 0.778152 |
| `crowd.dupRunPairsPerSample` | 0.743981 | 0.749921 | +0.005939 [-0.005437, +0.017677] · **CONTAINS ZERO** | 0.513908 |
| `crowd.nearestMateMeanMetres` | 8.663886 | 8.657977 | -0.005909 [-0.042892, +0.031375] · **CONTAINS ZERO** | 0.159134 |
| `contact.noneFirstContactShare` | 0.000546 | 0.000515 | -0.000031 [-0.000256, +0.000187] · **CONTAINS ZERO** | 0.139531 |

**THE BOUNCE AND THE SETTLE LADDER, DOSED:**

| face | SHUT | ARMED | Δ, 95 % paired CI | \|Δ\|÷hw |
|---|---|---|---|---|
| `bounce.ownTargetBounceRate` | 0.220583 | 0.142724 | -0.077859 [-0.082202, -0.073305] | 17.501309 |
| `bounce.ownTargetFirstShare` | 0.579225 | 0.579068 | -0.000157 [-0.004748, +0.004424] · **CONTAINS ZERO** | 0.034324 |
| `bounce.ownTargetUnresolvedShare` | 0.000356 | 0.000393 | +0.000037 [-0.000213, +0.000285] · **CONTAINS ZERO** | 0.148575 |
| `bounce.ownNonTargetBounceRate` | 0.822303 | 0.758759 | -0.063544 [-0.075762, -0.052238] | 5.402527 |
| `bounce.ownNonTargetFirstShare` | 0.096641 | 0.095758 | -0.000882 [-0.003431, +0.001616] · **CONTAINS ZERO** | 0.349590 |
| `bounce.ownTargetSettle.sameSide` | 0.779140 | 0.856939 | +0.077799 [+0.073209, +0.082159] | 17.386155 |
| `bounce.ownTargetSettle.opponent` | 0.000000 | 0.000000 | +0.000000 [+0.000000, +0.000000] · **CONTAINS ZERO** | null |
| `bounce.ownTargetSettle.loose` | 0.214952 | 0.137185 | -0.077767 [-0.082097, -0.073160] | 17.402032 |
| `bounce.ownTargetSettle.out` | 0.005552 | 0.005483 | -0.000069 [-0.000903, +0.000759] · **CONTAINS ZERO** | 0.083240 |
| `rollFailSettle.sameSide` | 0.000000 | 0.000000 | +0.000000 [+0.000000, +0.000000] · **CONTAINS ZERO** | null |
| `rollFailSettle.opponent` | 0.000000 | 0.000000 | +0.000000 [+0.000000, +0.000000] · **CONTAINS ZERO** | null |
| `rollFailSettle.loose` | 1.000000 | 0.999844 | -0.000156 [-0.000475, +0.000000] · **CONTAINS ZERO** | 0.655673 |
| `rollFailSettle.out` | 0.000000 | 0.000156 | +0.000156 [+0.000000, +0.000475] · **CONTAINS ZERO** | 0.655569 |
| `rollFailSettle.unresolvedShare` | 0.000000 | 0.000156 | +0.000156 [+0.000000, +0.000475] · **CONTAINS ZERO** | 0.654327 |

**THE SECTORS, DOSED:**

| face | SHUT | ARMED | Δ, 95 % paired CI | \|Δ\|÷hw |
|---|---|---|---|---|
| `contact.receiverFrontShareCompleted` | 0.365435 | 0.345106 | -0.020329 [-0.026140, -0.014503] | 3.493983 |
| `contact.receiverSideShareCompleted` | 0.568348 | 0.584078 | +0.015730 [+0.010055, +0.022014] | 2.630587 |
| `contact.receiverBackShareCompleted` | 0.066217 | 0.070816 | +0.004600 [+0.001530, +0.007822] | 1.461936 |
| `sector.firstTouch.frontShare` | 0.258721 | 0.259368 | +0.000647 [-0.005039, +0.005947] · **CONTAINS ZERO** | 0.117707 |
| `sector.firstTouch.sideShare` | 0.656547 | 0.652755 | -0.003792 [-0.009459, +0.002350] · **CONTAINS ZERO** | 0.642154 |
| `sector.firstTouch.backShare` | 0.084732 | 0.087877 | +0.003145 [-0.000154, +0.006800] · **CONTAINS ZERO** | 0.904567 |
| `sector.completionBy.front` | 0.904276 | 0.902433 | -0.001843 [-0.008765, +0.005326] · **CONTAINS ZERO** | 0.261566 |
| `sector.completionBy.side` | 0.929791 | 0.928963 | -0.000828 [-0.004588, +0.003120] · **CONTAINS ZERO** | 0.214801 |
| `sector.completionBy.back` | 0.867705 | 0.865317 | -0.002389 [-0.015693, +0.011418] · **CONTAINS ZERO** | 0.176209 |

**THE DEFENCE AND THE KEEPER, DOSED:**

| face | SHUT | ARMED | Δ, 95 % paired CI | \|Δ\|÷hw |
|---|---|---|---|---|
| `df.markSwitchesPerDefenderMinute` | 6.693823 | 6.580811 | -0.113012 [-0.200843, -0.024890] | 1.284564 |
| `df.markHeldShare` | 0.635730 | 0.637148 | +0.001417 [-0.000759, +0.003658] · **CONTAINS ZERO** | 0.641682 |
| `duel.tacklesPerMatch` | 2.183367 | 2.205411 | +0.022044 [-0.104208, +0.149299] · **CONTAINS ZERO** | 0.173913 |
| `keeper.savesPerMatch` | 4.738477 | 4.769539 | +0.031062 [-0.140281, +0.200401] · **CONTAINS ZERO** | 0.182353 |
| `keeper.gkMetresPerKeeperPerMatch` | 251.111536 | 252.386527 | +1.274991 [-0.316694, +2.904706] · **CONTAINS ZERO** | 0.791575 |

**GOALS, SHOTS AND E4, DOSED:**

| face | SHUT | ARMED | Δ, 95 % paired CI | \|Δ\|÷hw |
|---|---|---|---|---|
| `goalsPerMatch` | 2.556112 | 2.568136 | +0.012024 [-0.120240, +0.141283] · **CONTAINS ZERO** | 0.091954 |
| `shotsPerMatch` | 11.719439 | 11.742485 | +0.023046 [-0.240481, +0.283567] · **CONTAINS ZERO** | 0.087954 |
| `e4.forwardPassShare` | 0.553357 | 0.553651 | +0.000294 [-0.003771, +0.004745] · **CONTAINS ZERO** | 0.068985 |
| `e4.thirdManPerMatch` | 3.591182 | 3.609218 | +0.018036 [-0.138277, +0.175351] · **CONTAINS ZERO** | 0.115016 |
| `e4.overlapsPerMatch` | 0.059118 | 0.059118 | +0.000000 [-0.022044, +0.022044] · **CONTAINS ZERO** | 0.000000 |
| `e4.bestPassChainMeanPerTeam` | 4.190882 | 4.191884 | +0.001002 [-0.077154, +0.076152] · **CONTAINS ZERO** | 0.013072 |

**CONTEXT, DOSED:**

| face | SHUT | ARMED | Δ, 95 % paired CI | \|Δ\|÷hw |
|---|---|---|---|---|
| `passCompletion` | 0.588281 | 0.592057 | +0.003777 [-0.000592, +0.008162] · **CONTAINS ZERO** | 0.862827 |
| `context.passesPerMatch` | 87.107214 | 88.208417 | +1.101202 [+0.298597, +1.863727] | 1.407170 |
| `context.groundPassesPerMatch` | 82.563126 | 83.627255 | +1.064128 [+0.199399, +1.888778] | 1.259786 |
| `context.carriesPerMatch` | 95.498998 | 96.195391 | +0.696393 [-0.184369, +1.496994] · **CONTAINS ZERO** | 0.828367 |
| `context.metresPerMatch` | 9082.006581 | 9098.446393 | +16.439812 [-29.585695, +59.306192] · **CONTAINS ZERO** | 0.369883 |
| `context.movingTicksPerMatch` | 132550.537074 | 132898.902806 | +348.365731 [-265.265531, +919.546092] · **CONTAINS ZERO** | 0.588053 |
| `context.meanSpeedMps` | 4.111039 | 4.107685 | -0.003354 [-0.009019, +0.002604] · **CONTAINS ZERO** | 0.577128 |
| `context.openPlayTicksPerMatch` | 12544.405812 | 12592.497996 | +48.092184 [+6.993988, +86.080160] | 1.216197 |
| `context.possessionSpellsPerMatch` | 36.430862 | 36.747495 | +0.316633 [-0.203407, +0.808617] · **CONTAINS ZERO** | 0.625743 |
| `context.meanPossessionSpellSimSeconds` | 5.675086 | 5.648766 | -0.026320 [-0.099753, +0.049992] · **CONTAINS ZERO** | 0.351526 |
| `context.ticksPerMatch` | 15155.704409 | 15147.493988 | -8.210421 [-36.429860, +19.158317] · **CONTAINS ZERO** | 0.295402 |

**THE ROLL, DOSED:**

| face | SHUT | ARMED | Δ, 95 % paired CI | \|Δ\|÷hw |
|---|---|---|---|---|
| `roll.adjudicationsPerMatchIntended` | 34.885772 | 39.442886 | +4.557114 [+3.970942, +5.101202] | 8.063830 |
| `roll.failuresPerMatchIntended` | 3.596192 | 4.046092 | +0.449900 [+0.294589, +0.613226] | 2.823899 |
| `roll.realisedFailShareIntended` | 0.103085 | 0.102581 | -0.000504 [-0.004473, +0.003518] · **CONTAINS ZERO** | 0.126077 |
| `roll.meanLoggedPFailIntended` | 0.103394 | 0.102680 | -0.000714 [-0.001239, -0.000186] | 1.355294 |

**THE DOSED COMPOSITION (share of NON-POSSESSION endings, D pair) — every class with a count:**

| class | D-SHUT | D-ARMED | Δ | ÷ ATTEMPTS: D-SHUT → D-ARMED |
|---|---|---|---|---|
| `resolvedRollFail` | 0.334234 | 0.650451 | +0.316217 | 0.063049 → 0.076464 |
| `resolvedNotReachedMargin` | 0.410132 | 0.014175 | -0.395957 | 0.077366 → 0.001666 |
| `abandonedContactOpponent` | 0.176662 | 0.226160 | +0.049497 | 0.033325 → 0.026586 |
| `abandonedContactTeammate` | 0.011548 | 0.015464 | +0.003916 · CONTAINS ZERO | 0.002178 → 0.001818 |
| `abandonedBodyStrike` | 0.063513 | 0.086823 | +0.023311 | 0.011981 → 0.010207 |
| `abandonedDeadBallOut` | 0.003911 | 0.006927 | +0.003015 | 0.000738 → 0.000814 |

### §R6 THE PRE-COMMITTED READS, PRINTED

Selected on **STORED booleans** by the frozen §P.C rules, from the §0 literals, and re-derived
off the serialized artifact by `gFaces`:

> **"THE LAW WEAKENS THE DUEL — the commander decides with the contact counts."**

> **"THE DOSED WORLD MOVES — the entry candidate is LIVE in the form the user plays."**

(`precommittedReads.selectors.verdict` = **FAIL**; `abLimb` **true**; `cLimb` **false**; `dLimb` **true**;
`precommittedReads.dosedEntryRead` = the literal printed last above.)

### §R7 CONTEXT — the 240 s clock, and what the exam cost

Every rate on this page is on the **240 s MATCH clock** (`MATCH_DURATION` = 240, anchored;
1 sim-s = 60 ticks = 22.5 display-s). A walked match is
**15277.260521** ticks on E-SHUT and
**15283.545090** on E-ARMED, of which
**12614.235471** / **12628.380762** are open play.
The battery walked **3,996** matches in **530.591 s** wall
(`perf.meanWallSecondsPerWalk` **0.129338** s — ⚠ A MACHINE READING ON
ONE MACHINE, and a PLUMBING receipt, ⛔ never a football number). `gTraceInert` proved both trace
flags byte-inert on every arm; §DEV-PREFLIGHT states the measured trace overhead.


## §HONEST LIMITS

**This list is the ONE home** — canon, VERBATIM: *"a stage doc's HONEST LIMITS list is the ONE
home; the artifact stores that list verbatim or stores none"* (home:
[`RC-C0-COOPERATION-CENSUS.md`](RC-C0-COOPERATION-CENSUS.md) §COMMANDER CORRECTIONS item 3, ruling
#367 item 3). **The artifact stores NONE.**

1. **⭐⭐ FOUR CLASSES ARE INFERRED, AND THEIR TOTAL IS PUBLISHED (inherited from BQ-C1 §HONEST
   LIMITS 1–2).** `resolvedNotReachedStunnedOrOff`, `resolvedNotReachedMargin`,
   `abandonedDeadBallOut`, `abandonedPossessionElsewhere` and `abandonedOther` carry no unique
   public signature; the resolver's two pre-roll returns are invisible from outside the step and
   `stunTimer` can be RAISED later in the same tick, so **only the SUM of the two NOT-REACHED cells
   is exact**. `population.intended.inferredShare` is published per arm, and the scored (b) face is
   the **residual** cell of that parent — a **BOUND**, said so before it is read.
2. **⚠ THE POPULATION IS OBSERVED AT TICK BOUNDARIES (BQ-C1 §HONEST LIMITS 3).** An attempt created
   AND ended inside ONE tick is invisible to the read. `population.creationLedgerAgreementShare` is
   the receipt against the contest ledger's own `controlAttempt` contacts; its gap pools the
   offside branch's pre-creation aborts with any same-tick create-and-end, and this exam cannot
   separate them.
3. **⭐⭐ THE ARMED BALL IS A FREE BALL AFTER THE CONTACT (inherited from BQ-T0 §4).** It takes the
   body's velocity at the instant of contact and is then a free ball again: the shipped ground
   friction bleeds its speed inside the window while the body does not slow. **So a body
   ACCELERATING or TURNING hard through the window can still lose the ball at the margin, armed** —
   the law removes a push, it does not glue the ball to the foot.
4. **⚠ THE WINDOW LENGTH AND THE MARGIN ARE UNTOUCHED** (#384 item 5's ⛔, inherited). Three ticks
   is the CONTEST's; the 2 cm bar is the resolver's; a stunned or sent-off body still loses the
   attempt. This exam moves neither and adjudicates neither.
5. **⭐⭐ THE ENGINE HAS NO BALL–BODY SEPARATION SOLVER AT ALL** (BQ-T0 §4 / #385 item 2, inherited).
   `resolveOverlaps` separates BODIES; a free ball's centre can sit inside a body's core in the
   SHIPPED world too (BK-C0's 球穿身). The law under exam introduces no closing term and does not
   close that geometry.
6. **⚠ THE MARGIN CROSS-CHECK IS A DECLARED RECONSTRUCTION ONE PHYSICS STEP LATE, AND IT AGREES
   LESS OFTEN ON THE ARMED ARM.** The SHIPPED `directBallAccess` is RE-CALLED at the END tick with
   the resolver's own arguments; the resolver read it earlier in the step. On the scored pair it
   agrees on **0.985415** of E-SHUT's ALL-BODIES NOT-REACHED endings (9,864 of 10,010) but on
   **0.715976** of E-ARMED's (363 of 507; the intended-target parent is 4,306 / 148 — §COMMANDER
   CORRECTIONS item 2) — armed, the class is small and the ball sits at the body's own velocity,
   so one integration step moves the comparison across the bar far more often. ⛔ That is a
   statement about the RECEIPT's reconstruction, **not** about the class: the class is the
   resolver's own residual and is booked from the engine's site, and this figure is a **RECEIPT**,
   ⛔ never a class definition and ⛔ never a football effect size.
7. **⭐⭐ BOTH SIDES CARRY THE ARMED LAW, SO EVERY MATCH-TOTAL FACE IS A MATCH TOTAL.** Goals, shots,
   interceptions, tackles, saves and the keeper's metres are the two teams pooled; nothing here
   separates an attacking from a defending effect, and the do-no-harm bands are read on the pooled
   total.
8. **⚠ THE SECTOR AND BOUNCE FACES ARE SELECTION STATISTICS.** Who reaches a first touch at all, and
   in which sector, can itself change under the law — the composition of the population moves with
   the treatment. The faces are REPORTED for exactly that reason and gate nothing.
9. **⚠ EVERY CONDITIONAL FACE IS AN ASSOCIATION ON ONE ENGINE'S REALISED JOINT DISTRIBUTION.**
   Completion by sector, the bounce rate by class and the settle ladder after a failed roll are
   conditional shares, not independent effects; the cells co-vary heavily. ⛔ Only the six conjuncts
   are scored.
10. **⚠ POSSESSION SPELLS ARE THE EXAM'S OWN RECONSTRUCTION.** The engine keeps no possession-spell
    ledger; the face counts `Match.possessionSide` changes at tick boundaries. Said so, per the
    engine-ledgers-before-heuristics canon.
11. **⚠ 12 SCRATCH CLUSTERS SIZED THIS BATTERY, and (a)/(b)/(c)'s TARGETS ARE THEIR OWN SMOKE-MDEs.**
    That construction makes their sizing rows read N = 998 by definition: the row states **what the
    block certifies**, not that the block was chosen to satisfy a prior target. A non-fall on (c)
    certifies nothing smaller than its declared MDE, and the same warning attaches to every (d)
    word.
12. **⛔⛔ WHAT THIS EXAM DOES NOT ADJUDICATE.** It does not say a world 13 should be cut; it does not
    say the roll's form is right; it does not price a reach-margin term (a HELD door); it does not
    touch the lane classes — the user's visible 「弹回」 is BN-C0's C3 plus the body strikes, and
    those belong to steps ②/③. **THE ENTRY IS THE COMMANDER'S AND THE USER'S.** Nothing here ships:
    no world is cut, no flag is armed outside this instrument's own constructor calls, the
    fingerprint is unchanged, and the user's world-12 play-test gate remains open and his.

## §DEVIATIONS (declared by the executor; the commander disposes)

1. **⭐⭐ BQ-C1's INSTRUMENT WAS COPIED, NOT REUSED IN PLACE.** #385 item 5(viii) says the instrument
   reuses "BQ-C1's classes and attribution". BQ-T0 §4 had already declared that BQ-C1's frozen
   probe **no longer re-anchors at this head**. ⛔ A banked census's instrument is not this stage's
   to edit, so the classes, the `= null` sites, the OBSERVED/INFERRED table, the precedence, the
   displacement rule and the exact-sum receipts were **COPIED into `bq-t1-cushion-exam.ts` and
   RE-DERIVED against the file in front of it**: the ten line receipts are this head's
   (3694 · 3718 · 3861 · 4834 · 5615 · 5669 · 5685 · 5730 · 5847 · 5914), the creation site is
   5674, and the cushion needle — one contiguous block in BQ-C1 — is **split into four separate
   anchors** (the `release` clamp, the branch test, the SHIPPED assignment pair, the ARMED
   assignment pair), because the branch now sits between the clamp and the assignments.
2. **⭐⭐ (a), (b) AND (c)'s TARGETS ARE THEIR OWN SMOKE-MDEs, ROUNDED UP TO 6 dp.** #385 item 5(v)
   left those three targets to the smoke to name; this is the named form (the BQ-C0 / RC-T1b
   idiom), and the consequence — their sizing rows read N = 998 **by construction** — is stated at
   §P.E and again at §HONEST LIMITS 11 rather than hidden.
3. **THE PRIVATE-FIELD TYPE-VIEW READ**, as at BQ-C0 and BQ-C1: `Match.pendingControl` is `private`
   and the engine publishes no mirror, so the population is read through a TypeScript TYPE VIEW —
   a read, never a write, proven byte-inert by `gLockstep`. Declared at §P.B (the #383 item 3
   precedent).
4. **THE POPULATION IS OBSERVED AT TICK BOUNDARIES** — §HONEST LIMITS 2, with the contest ledger's
   own contact count published as the receipt.
5. **POSSESSION SPELLS ARE A DECLARED RECONSTRUCTION.** #385 item 5(iii) asked for "possession
   spells if the engine records them, else say so". **The engine records none** — there is no
   possession-spell ledger in `Match.ts` — so the exam publishes its own tick-boundary count of
   `possessionSide` changes and says so, at §P.B, in the artifact's `definitions` and here.
6. **THE ROLL-FAIL SETTLE LADDER'S "PASSING SIDE" IS THE FAILING BODY'S OWN SIDE**, and its four
   outcome shares are computed over windows that RESOLVED; `unresolved` is published as its own
   share beside them rather than folded in (BN-C0's own treatment). Declared at §P.B.
7. **EIGHTEEN GATES, NOT THE FIFTEEN #385 item 5(vii) NAMES.** `gAnchoredConstants`,
   `gWalkFixtures` and `gLedgerNonVacuous` were added — the first two because the anchored-extraction
   and pinned-predicate canons require them of any instrument that extracts constants or defines a
   scored predicate, the third because the ruling's `gClassesNonVacuous` clause splits naturally
   into "the scored classes are non-vacuous" and "the engine's own ledgers are live". No gate reads
   direction.
8. **COMMITTED ON `main`** (no branch, no push) — the programme's own governance; of record.

## §COMMANDER CORRECTIONS (ruling #386 — the exam BANKED as the FAIL OF RECORD on (c) alone; the verifier's three MEDIUM and one LOW, disposed; the artifact, the instrument, every stored face, every printed word UNCHANGED)

The independent verifier re-derived all six conjuncts and the dosed pair with its own rng,
re-counted the sites, reproduced both hashes, swept 1,368 prose literals (every one traced), and
returned **PASS — zero HIGH**. The items:

1. **MEDIUM — (c)'s FAIL IS NOT STABLE TO THE BOOTSTRAP DRAW, AND §R DID NOT SAY SO.** The
   verifier re-ran (c)'s 2,000-draw cluster bootstrap with 25 independent rng seeds: `ciHi < 0` held
   in **20 of 25** (ciHi from −0.000245 to +0.000137); in the other five the frozen rule reads
   DOES-NOT-FALL and H-BQ.1 prints PASS. The author had zero discretion (the block-base seed was
   frozen) and the word of record STANDS as printed; the commander records that the FAIL on (c) is
   a four-in-five call on the draw, that |Δ| 0.003669 is below the exam's own realised MDE
   0.004965, and that 9 of 998 seeds each flip it alone. The pre-committed read handed exactly this
   case to the commander ("the commander decides with the contact counts" — ruling #386 item 4).
2. **MEDIUM — the margin cross-check fractions were printed over ALL BODIES inside an
   intended-targets section without naming the population.** Corrected at §R2 and HONEST LIMIT 6
   (the intended-target parent is 4,306 / 148).
3. **MEDIUM — "rose" / "fell" on two UNSCORED faces without stored rule words.** Rephrased to what
   is stored — the three intervals and where each lies against zero. The next paired exam stores a
   rule word for every published direction it names.
4. **LOW — `allGreen` sits outside the hashed body** (with `gFacesDetail`, `hashedBodySha256`,
   `receipts`); it is fully derivable from the hashed `gates[].ok`. Of record; the next instrument
   puts it inside the allowlist.
5. **OF RECORD (the executor's declared deviations, ratified at #386 item 3):** BQ-C1's instrument
   was COPIED and re-anchored (the seam split its cushion needle into four anchors); (a)/(b)/(c)'s
   targets are their own smoke-MDEs rounded up (N = 998 by construction); the type-view read; the
   tick-boundary population; possession spells a DECLARED reconstruction; the roll-fail ladder's
   `unresolved` published separately; EIGHTEEN gates (three added); committed on main; after sight
   the doc gained §R, §HONEST LIMITS and §DEVIATIONS with §DEV-PREFLIGHT untouched.
