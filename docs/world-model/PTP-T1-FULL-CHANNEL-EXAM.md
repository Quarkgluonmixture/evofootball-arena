# PTP T1 — THE FULL-CHANNEL EXAM (dose the RELATIONAL PAIR: the receiver's eyes × the passer's sight)

Status: **FROZEN BEFORE SIGHT, then SMOKED.** Everything from §FORM to §NON-CLAIMS — the world,
the seven arms and their exact two-seam declarations, the TWO-TIER pre-registered primary and its
frozen confirm/kill predicate, the ruler, the guards, the estimator, the seed ledger, the N rule
and the gate list — was written **before** any number of this stage existed. The measured numbers
live in [§RESULT — the smoke](#result--the-smoke). Anything sharpened after a run is recorded in
**Deviations**, never smoothed away.

Authority chain: contract [`PASS-TO-PATH-CONTRACT.md`](PASS-TO-PATH-CONTRACT.md) — §3 **PTP-T1**
(the arm set, the two-tier primary, **F-PTP-a/b/c** pre-named there), §1 H-PTP, §2 M-PTP.1–4, §4
the non-claims. Rulings **#230** (the OBM-T1 battery adjudicated: ⛔ F-OBM-a fired, the movement
is GUARD-CLEAN, the ONLY signal was `constructedGe5Share` at CHECK-AND-SHOW **boundary-resolved**
+2.9575 pp [+0.0025, +5.9299] under ~114 contrasts of multiplicity ⇒ FRAGILE, *"admissible only
as a confirmatory design's pre-registered primary, never as a claim"*) · **#231** (the relational
census: 到脚不到路 explains BOTH nulls; the #230.5 fork DISPOSED — 甲 folded into this exam's
tier 2, with the channel OPEN) · **#232** (PTP-T0 banked; ⭐ **the T1 fact of record: the percept
motion channel is THIN**) · **#225.3(c)** (per-dose STOP granularity) · **#203** (per-arm ROWS,
never verdicts) · **#181.2** (every HARD gate in-probe) · **#197-M1** (hashed body vs unhashed
envelope) · **#163** (seed/stats disjointness) · **#20** (cluster = match seed) · **#128** (wall
is CONTEXT ONLY) · **#207** (checkpoint) · **#226.1** (the transcript form).

The instrument this stage stands on: [`OBM-T1-POLICY-EXAM.md`](OBM-T1-POLICY-EXAM.md) and its
probe — inherited **whole** (world, ruler, guards, estimator, genealogy lift, N rule, checkpoint,
guard block, per-dose STOP granularity), and that inheritance is **proved** rather than asserted
by ⭐ **G-REPRO-OBMT1** (below) on top of OBM-T1's own four G-REPRO receipts, which ride here
unchanged. The seam being dosed on the passer's side is
[`PTP-T0-DORMANT-SEAM.md`](PTP-T0-DORMANT-SEAM.md), banked at `e7eb041` (ruling #232); the seam
on the receiver's side is [`OBM-T0-DORMANT-SEAM.md`](OBM-T0-DORMANT-SEAM.md), banked at `600ff04`
(ruling #228).

> ⭐ **INSTRUMENT-ONLY ROUND.** `src/**` is **byte-untouched** (X-SRC-UNTOUCHED is a HARD gate).
> **BOTH seams are already banked**; the arms are built by two MatchConfig flags (`obmMovement`,
> `ptpPassLead`) plus two gene channels (the 16-weight OBM matrix, the scalar `passLeadSupport`)
> written on all three genome views of both teams. No engine byte moves.

---

## §FORM

### The world — ONE percept-armed base world, IDENTICAL in every arm

```text
new Match({ seed, teamA, teamB, duration: 240, edsPerceivedChoice: true })
```

**The world is OBM-T1's, exactly** — and it is not a free choice. OBM's four-limb arming REQUIRES
it: OBM-T0's G-BLIND proved a fully armed, fully dosed eyes seat in a world without the percept
trunk is byte-identical to that world unarmed, and `edsPerceivedChoice` alone is the minimal flag
set that arms BOTH halves of the percept chain (the refresh **and** the scan-moment record). An
exam run blind would report *"the relational pair moves nothing"* while never having delivered the
receiver-side half at all. **G-BLIND-WORLD is HARD here for exactly that reason**, and using
OBM-T1's world verbatim is also what makes the CHECK-AND-SHOW arm the **same cell** the #230
signal came from.

> ⭐⭐ **AND THE EXAM THEREFORE TESTS THE *HONEST* (THIN) LEAD CHANNEL — STATED AT THE HEAD,
> NOT DISCOVERED AT THE FOOT (#232.3).** In a percept world the passer leads only the mates his
> OWN EYES carry a remembered velocity for. PTP-T0 measured that channel and it is **THIN**:
> **921 of 1,109** support projections were exactly zero in the percept world (mean remembered
> speed **0.717 m/s** against truth's **2.513**), and **21.7 %** of chosen passes carried a lead
> against **62.9 %** in the bare world. So **the expected delivered lead here is ≈ ONE THIRD of
> bare**, and this exam measures the channel a body can honestly have — not the channel a
> truth-reading passer would have. That degradation IS the design (contract §7(a) / §HONESTY 1),
> it is budgeted for in the arm set (a HALF dose is a declared row, not a rescue), and every arm
> publishes its **DELIVERED LEAD** so *dose ≠ delivered* stays visible on the passer's side just
> as it already is on the receiver's.

⚠ Declared, as OBM-T1 declared it: `edsPerceivedChoice` also moves the CARRIER onto the
perceived-snapshot pass chooser, so this world is **not** CTB-T1's bare production world and the
two exams' **absolute levels are not comparable**. The paired contrast is clean either way,
because all seven arms share this world exactly and differ by nothing but the two seams' flags and
genes.

⭐ **THE TWO-DOORS DECLARATION.** `ctbSupportPlane` is **FALSE in every arm** — asserted per arm
in-probe, never merely stated — so the OBM policy's intercept is a hard 0 (the #228 fix) and the
banked static plane (#224) cannot leak in through either door. Each arm additionally declares
**both** seams' flags and gene states, and FLAG-HYGIENE asserts them off the REAL constructed
matches.

### The arms — 7, paired on ONE shared seed list (#20: cluster = match seed)

The OBM matrix rows are `[planeDepth, planeWidth, supportScore, runScore]` over the features
`[f1 carrierPlight, f2 ownMarker, f3 targetCongestion, f4 readingAge]`; every non-zero weight is a
**domain corner** (±1) of the frozen signed domain `[OBM_WEIGHT_MIN, OBM_WEIGHT_MAX]`, derived in
code. The lead gene `passLeadSupport` takes only `{0, ½, 1}` — the frozen `clamp01` domain's two
ends and its **arithmetic midpoint**, computed in code, never typed as an invented constant.

| arm | `obmMovement` / matrix | `ptpPassLead` / gene | the football sentence |
| --- | --- | --- | --- |
| **ABSENT** | off / none | off / absent | the CONTROL — the percept-armed world, both seats unreached |
| **ARMED-ZERO-BOTH** | **on** / all 16 slots `0` | **on** / **0** | the IDENTITY arm: BOTH doors open, BOTH doses inert. Must be **byte-identical to ABSENT** per seed — the strongest single statement in the stage |
| **CHECK-AND-SHOW** | **on** / `depth ← [−1,0,0,0]` · `support ← [+1,0,0,0]` | off / absent | ⭐ **the #230 cell REPRODUCED VERBATIM** — the arm the only battery signal came from, its matrix frozen in OBM-T1 and re-walked here as a receipt (G-REPRO-OBMT1) |
| **LEAD** | off / none | **on** / **1** | ⭐ the passer's sight ALONE, at full trust: 传球到路 with nobody moving differently. The #231 limb on its own |
| ⭐⭐ **COMBINED** | **on** / CHECK-AND-SHOW's matrix | **on** / **1** | ⭐ **THE CELL THE STAGE EXISTS FOR**: the receiver comes short and asks, AND the passer can see him moving. The relational PAIR, both halves live |
| **COMBINED-HALF** | **on** / CHECK-AND-SHOW's matrix | **on** / **½** | the DOSE-RESPONSE row: the same pair at half the passer's trust. A monotone read is evidence the channel is what moved; a non-monotone one is evidence it is not |
| **KITCHEN-SINK-LEAD** | **on** / all four rows full (`depth −1×4` · `width +1×4` · `support +1×4` · `run −1×4`) | **on** / **1** | ⭐ **THE CEILING PROBE of the PAIR, justified ex ante by #230.4's H-OBM-T1b/c**: the ceiling arm delivered **4.33 m** of mean movement in OBM-T1 and moved no primary — the recorded question was whether that was DOSE or CHANNEL. With the channel OPEN, the same ceiling movement is re-asked. **Not** a football recommendation |

**Why exactly these seven, and no eighth.** The contract's set is ABSENT · ARMED-ZERO(s) ·
CHECK-AND-SHOW · LEAD · ⭐ COMBINED · the lead-dose corner. That is six; the seventh
(KITCHEN-SINK-LEAD) is the ONE addition, and it is justified **before sight** by ruling #230.4's
own labelled hypotheses — H-OBM-T1b (dose) and H-OBM-T1c (coverage) are exactly what a
ceiling-movement × open-channel cell addresses, and it costs one column. **A single ARMED-ZERO is
used rather than two**, deliberately: the interesting identity is that BOTH doors open with BOTH
doses inert leaves the world untouched (each seam's own single-door zero identity is already
gated at its T0 — PTP-T0 G-ZERO/G-BORN and OBM-T0's own). The cap is 8; this stage spends 7.

**Two halves, two deliveries, and now a third.** OBM's matrix drives the PLANE (geometry) and the
two candidate SCORES; PTP's gene drives the PASS AIM. G-ARM checks delivery on the axes an arm
doses **and silence on the axes it does not** — for BOTH seams, including that an arm with the
lead gene absent or zero hands the strike **exactly zero** displacement.

### The ruler — ALL INHERITED, each with its own G-REPRO gate

| # | quantity | provenance | gate |
| --- | --- | --- | --- |
| **1** | **TRUE-holdable supply** (share of eligible moments) | the O2-T1 `trueCellOf` instrument VERBATIM on the #186 population | G-REPRO-O2T1 |
| **2** | **pressed-first-reception** (openPlay spells, `TOUCH_CONTROL_DIST`) | the #173 tempo-census instrument VERBATIM | G-REPRO-173 |
| 3 | short-option supply (poss ticks · first receptions) | #224.4(i)'s debt; radius family PARSED from source | G-TRACE-RADIUS |
| 4 | support-existence at PRESSED moments | (3) under (2)'s pressure test | — |
| **5** | the #218 arc shares (constructed ≥3/4/5 · scramble · set-piece) | the goal-genealogy ORIGIN CLASSIFIER, LOSS-TICK semantics verbatim | G-REPRO-GGC |

⭐ **AND THE WHOLE SET IS PROVED TO BE OBM-T1's** — **G-REPRO-OBMT1**: this probe re-walks the
first rows of the **committed OBM-T1 battery block** in OBM-T1's own world, on **TWO** of its arms
— **ABSENT** (the world) and ⭐ **CHECK-AND-SHOW** (the frozen matrix this exam re-uses verbatim)
— and must reproduce every published per-match field **exactly**, whole-match **signature** (rng
stream state inside) included. A single changed instrument constant, sampling rule, walk order or
matrix slot reds it. OBM-T1's own four G-REPRO receipts (O2-T1 · #173 · GGC · CTB-T1) ride here
unchanged, so the chain back to CTB-T1 is unbroken.

⚠ **G-REPRO-OBMT1 carries a second load, and it is the one that makes the new instrument safe**:
this probe wraps `performPass` on every walked match to read the delivered LEAD (below). If that
wrapper perturbed anything, the re-walked OBM-T1 signatures could not reproduce. The
non-perturbation is therefore a **receipt**, not a promise.

### The estimator

The OBM-T1 set verbatim: per-match (seed-clustered) **paired** bootstrap, ratio-of-totals,
2.5/97.5 percentiles, 2,000 resamples, **one resampled seed-index set feeds every arm**.
`resolved` is a **mechanical CI flag** (the paired-delta CI excludes zero), **never a verdict**
(#203).

## §SUCCESS — the TWO-TIER pre-registered primary, restated VERBATIM from contract §3

> PRE-REGISTERED PRIMARY, two-tier: (i) the supply rulers 1+2 (inherited verbatim, the CTB/OBM
> chain); (ii) ⭐ the #218 shares at the COMBINED cell (constructed ≥5 + scramble — H-OBM-T1a's
> confirmatory primary, sized so the #230 boundary signal is decisively confirmed or killed).
> Guards + per-dose STOP granularity inherited verbatim.

Operationally, frozen here **before any number exists**:

* **TIER 1** — rulers 1 + 2, helpful = TRUE-holdable supply **UP** or pressed-first-reception
  **DOWN**, `resolved`, at a **dosed** cell, with **that cell's** guards held (#225.3(c)).
* **TIER 2 (the #230.5(甲) confirmatory, CHANNEL OPEN)** — `constructedGe5Share` **AND**
  `scrambleShareOfGoals`, read **at the COMBINED cell**. Helpful = constructed ≥5 **UP** /
  scramble **DOWN**.
* **SUCCESS = EITHER TIER** moves resolvedly helpful at a dosed cell with that cell's guards held.

### ⭐ THE #230 BOUNDARY SIGNAL — the confirm/kill predicate, FROZEN EX ANTE

The #230 point of record is `constructedGe5Share` at CHECK-AND-SHOW, **Δ = +2.9575 pp**
(0.029575 in share units), CI [+0.0025, +5.9299] pp. At the **COMBINED** cell, on that same
column:

| verdict | the exact predicate (mechanical, computed in-probe) |
| --- | --- |
| **CONFIRMED** | the paired-delta CI **excludes zero in the helpful direction** (lower > 0) |
| **KILLED (a) — excluded low** | the CI's **upper bound < +0.029575** (the #230 point), i.e. the design that opened the channel rules the #230 effect size out |
| **KILLED (b) — tight null** | the CI **contains 0** AND is **entirely inside ±0.029575**, i.e. the interval is narrower than the effect it was sized to detect and centred on nothing |
| **INCONCLUSIVE** | anything else (a wide interval spanning both 0 and the #230 point) |

The same four-way predicate is computed for `scrambleShareOfGoals` at COMBINED against **its** own
#230-cell point of record (SPACE-SEEK Δ −6.0720 pp = −0.060720 share units, sign-flipped for the
helpful direction). ⚠ **The probe computes these flags mechanically and fires nothing** (#203);
which branch the arc takes is the commander's.

### Pre-named failure branches, verbatim from the contract

* **F-PTP-a** — *the combined cell moves nothing*: the relational-layer hypothesis itself takes
  the hit (the arc's honest death branch, and H-OBM-T1a dies with it). **Reported as-is if it
  fires.**
* **F-PTP-b / F-PTP-c** — the inherited guard/pathology STOPs. ⭐ **The NAMED risk is
  interceptions**: a led pass into traffic is a through-ball-shaped gamble, and the chooser must
  price it rather than be subsidised. This stage therefore publishes, beside the plain
  interception guard, the **led-pass counts and the interception attribution** the guard is read
  against (below) — cheaply liftable because the strike wrapper already sees every chosen pass.

### The STOP granularity — FROZEN, inherited verbatim (#225.3(c))

F-PTP-b/c fire **per dose**: a dose whose guard **breaches** (resolved AND beyond the frozen
tolerance) is **disqualified** as a candidate; the **arc-level STOP** fires only if **every** dose
that moves a primary ruler helpfully is disqualified. Frozen with it: every row is read **beside
its delivered dose** — on BOTH seams — so a null can never be read as *a strong dose that failed*
when it was *a thin channel that arrived*.

⚠ **The probe fires none of them** (#203): it emits per-arm rows, paired deltas and mechanical CI
flags. Adjudication is the commander's.

## §GUARDS — every tolerance frozen ex ante (inherited verbatim)

| limb | direction | tolerance |
| --- | --- | --- |
| `interceptionsPerMatch` | ceiling | `NI_FRACTION · |control|` |
| `spreadYOut` | floor | `NI_FRACTION · |control|` |
| `spacingMedian` | floor | `NI_FRACTION · |control|` |
| `spacingUnder4` | ceiling | `NI_FRACTION · |control|` |
| offsides/match | the #157 **FLAG** form — a resolved increase returns to the commander, flips no gate |
| equilibrium band (goals/crosses/headers/longBalls/cutbacks) | **gates at battery N only**; the #198-form exclusion applies (a dimension the ABSENT arm itself fails is EXCLUDED and DISCLOSED) |

`NI_FRACTION = 1 − 0.275/0.380` (PM-T1 §5, from A4-S2P1-VECTOR-CENSUS §4). `breach = resolved AND
beyondTolerance` — **evidence** for F-PTP-b/c, never the firing of it.

⭐ **THE LED-PASS INTERCEPTION ATTRIBUTION (the F-PTP-b named risk, made visible).** Alongside the
inherited interception guard, every arm publishes: chosen passes, of which **handed a lead**, of
which **a non-zero lead**, the mean/max delivered lead in metres, the lead as a share of the pass
distance, and the **interceptions per led pass** — the closest cheap attribution the substrate
allows, stated for what it is: **a RATIO of two per-arm totals, not a causal attribution of any
individual interception.** It is REPORTED beside the guard; **the guard itself is unchanged.**

## §SEEDS — fresh, strictly above everything PTP-T0 consumed (#163)

PTP-T0's own committed ledger (read off `data/ptp-t0-pass-lead.json`'s
`gates.seedDisjoint.intervals`): **12,425,000–024** (receipts) · **12,425,025** (cost read) ·
**12,425,900–906** (test seeds). This stage therefore starts at **12,425,026** and every block
ends **below 12,425,899**.

| block | range | use |
| --- | --- | --- |
| **smoke** | **12,425,026 – 12,425,037** (12) | all 7 arms |
| **delivered-dose read** | **12,425,040** (1 seed × 7 arms) | the DECLARED fourth block — an OBSERVATIONAL match per arm (the OBM-T1 idiom: reading the FEATURES and the two score multipliers pulls a percept, so it may never touch exam data) |
| **exit-semantics guard** | **12,425,050 – 12,425,099** | any `PTPT1_N`/`PTPT1_SKIP_FP` override is routed here and adjudicates nothing |
| **battery** | **12,425,100 – (12,425,100 + N − 1)**, contiguous, cap **628** (⇒ ≤ 12,425,727) | the battery, if dispatched |
| free above | 12,425,038–039 · 12,425,041–049 · 12,425,728–899 · 12,425,907 + | later |

Disjointness is computed **in-probe** against the COMPLETE consumed ledger (now 33 blocks,
extended with OBM-T1's four and PTP-T0's three), never asserted here, and for **every one of the
ten blocks this stage touches**: 2 FRESH (exam · dose read) · 2 RESERVED (guard · battery) ·
**6 RE-WALKS** (O2-T1 · #173 · GGC · CTB-T1 · ⭐ OBM-T1 ABSENT · ⭐ OBM-T1 CHECK-AND-SHOW). The
re-walks' predicate is **inverted**: each must land **inside** its source's consumed interval.

⭐ **The stage-own pairwise predicate is the CORRECTED one (#230.1's lesson, inherited as code):**
intersecting stage-own intervals FAIL **unless they are EXACTLY equal**, in which case they are
one block under two names (`stageOwnUnified`) — the FULL-mode reality that the exam walk
**redeems** the reserved battery block. **A PARTIAL overlap still FAILS.**

**Stats stream**: base **105,200** (OBM-T1 consumed 105,000), min gap to any published base
**≥ 200**, complete namespace published in-probe.

## §NRULE — the OBM-T1 MDE form, recomputed for THIS stage

```text
m_iid  = (z.975 + z.80)² · (p0(1−p0) + p1(1−p1)) / (p1 − p0)²
DEFF   = MAX( inherited O2-T1 paired-delta DEFF , this stage's own smoke DEFF when it exists )
m_req  = DEFF · m_iid
N(q)   = ceil( m_req / momentsPerSeed )
N      = max_q N(q) , capped by the LEDGER ROOM and by the CTB-T1 precedent cap (628)
```

⚠ **The honest part, inherited unchanged**: `p0` and moments-per-seed are **world-dependent**.
Where **this probe's own committed smoke exists** they are read from ITS ABSENT arm and the
smoke's own paired-delta CI on the CEILING arm supplies a **same-world DEFF** (noisy at 12
clusters — hence used only through a MAX with the inherited one, the conservative direction).
Where it does not, they are the inherited out-of-world numbers and the substitution is published
as `sourceOfP0`. The **MDEs stay the traced committed ones** (the O2-T1 resolved delta for q1; the
#173 census's own smallest cross-arm gap for q2): no same-world MDE exists, and choosing one after
sight is exactly what frozen-before-sight forbids. **The tier-2 column is NOT re-cut into the N
rule** — its sizing question is the confirm/kill predicate above, which is a statement about the
CI this N produces, not a second N.

**The cap is a CEILING, not a target, and it is FLAGGED when it binds** (`capBinds` publishes the
fork beside `nRaw`; the probe re-cuts nothing).

## §GATES — frozen ex ante, ALL computed in-probe (#181.2)

| gate | predicate | kind |
| --- | --- | --- |
| **X-DET** | the whole core (7 arms + 6 receipt walks + 7 dose reads + summaries + bootstrap) runs **TWICE**, byte-identical digests | HARD |
| **X-FP-PROD** | the production fingerprint re-derives `57b0bdab…c673` in-probe | HARD |
| **X-SRC-UNTOUCHED** | `git diff --stat -- src` is EMPTY | HARD |
| ⭐ **G-REPRO-OBMT1** | a re-walk of the committed OBM-T1 battery block's first rows on **TWO arms** (ABSENT · CHECK-AND-SHOW) reproduces **every published field exactly, signature included** — the world, the frozen matrix, AND the non-perturbation of this stage's strike wrapper, in one receipt | HARD |
| **G-REPRO-CTBT1** | OBM-T1's own receipt, unchanged: the CTB-T1 battery block re-walked in CTB-T1's world, every field exact | HARD |
| **G-REPRO-O2T1** | ruler 1's rows reproduce the committed O2-T1 control rows | HARD |
| **G-REPRO-173** | ruler 2 reproduces the committed tempo-census numbers field for field | HARD |
| **G-REPRO-GGC** | ruler 5's ported classifier reproduces the committed genealogy census counts (63 fields) | HARD |
| **G-TRACE-RADIUS** | ruler 3's radius family is PARSED from `src/ai/formations.ts`, never typed | HARD |
| ⭐ **G-TRACE-PTP** | the lead seam's own constants are read from `src/**`, never typed: `PTP_FLIGHT_SPEED === 18` and `PTP_LEAD_FLIGHT_MUL === 1.6` matched at their declarations, the through-ball loop's `/ 18` line and `runBurstPoint`'s `* 1.6` line matched VERBATIM, and the gene map checked at **both domain ends and at absence** (`passLeadSupportWeight`) so the `{0, ½, 1}` dose set is the domain's own | HARD |
| ⭐ **G-BLIND-WORLD** | the percept trunk is **LIVE** in every arm's constructed world **and** the features are **non-degenerate** (snapshots exist · `someFeatureNonZeroShare > 0` · all four feature means > 0). ⚠ `allFeaturesZeroShare` is published as an **upper bound** on blindness | HARD |
| ⭐ **G-ARM** | **BOTH SEAMS, delivery and silence.** OBM half (OBM-T1 verbatim): the matrix on all three genome views of both teams; the seat REACHED (policy-cache writes > 0 on every seed); the four support-tick classes **partition exactly**; delivery on the axes an arm doses and **silence** on the ones it does not; ARMED-ZERO delivers exactly zero shift with both multipliers exactly 1. ⭐ PTP half (new): the gene on all three views of both teams; an arm with the flag armed hands the strike a lead on **every** chosen pass (a code-path receipt), an arm with the gene **dosed** delivers a **non-zero** lead on at least one chosen pass, an arm with the gene **zero or absent** delivers **exactly zero** lead metres, and on every led pass the delivered displacement obeys the frozen law — **0 sign violations, 0 magnitude violations** against an INDEPENDENT re-derivation | HARD |
| **FLAG-HYGIENE** | ARMED-ZERO-BOTH ≡ ABSENT per seed on the whole-match signature **and** every row field; the doses well-formed (matrices at domain corners, genes in `{0, ½, 1}`); ⭐ the **two-doors** row read off the REAL constructed matches: `ctbSupportPlane` FALSE and `edsPerceivedChoice` TRUE in **every** arm, and each seam's flag exactly matching its arm's declaration | HARD |
| ⭐ **G-FORK-TOKENS** | **BOTH token families** enumerated over `src/**` and classified, **zero unclassified**: the OBM set (OBM-T1's, verbatim, with the #228.5(b) completion) **and** the PTP set (PTP-T0's own), with the named forks/apply/strike sites counted exactly — **exactly ONE** `ptpPassLead` fork, ONE led-strike statement, THREE aim-priced inputs | HARD |
| **SEED-DISJOINT** | ⭐ **EVERY block this stage touches** vs the COMPLETE consumed ledger, each under its own predicate: FRESH + RESERVED clash-free and pairwise disjoint (with the corrected `stageOwnUnified` identity form — PARTIAL overlap still fails); the **six RE-WALKS** must land **INSIDE** their sources' consumed intervals. Sub-blocks ordered; battery clash-free and ending below 12,425,899 | HARD |
| **STATS-DISJOINT** | base 105,200, min gap ≥ 200 against the complete published namespace | HARD |
| **G-CLEAN-INVOCATION** | any `PTPT1_N` / `PTPT1_SKIP_FP` override routes onto the guard block, reds this gate and exits 1 | HARD |
| **REPORTED (a)** | ⭐ the **DELIVERED LEAD** per arm — chosen passes, led passes, non-zero-lead share, mean/max lead metres, lead as a share of pass distance — the thin-channel visibility row (#232.3). Rows, no verdict | REPORTED |
| **REPORTED (b)** | ⭐ the **DELIVERED OBM DOSE** per arm (OBM-T1's own seam table verbatim) + the **led-pass interception attribution** (interceptions per led pass) | REPORTED |
| **REPORTED (c)** | the mechanical TIER-2 confirm/kill predicate flags at COMBINED, computed against the frozen #230 points | REPORTED |

Checkpoint/resume is the #207 form (`PTPT1_MODE` / `PTPT1_RESUME` / `PTPT1_CHECKPOINT`),
resilience only: the unit is the per-(pass, seed) set of 7 arm rows, nothing pooled is stored, a
resumed run is byte-identical to a fresh one, and the checkpoint is /tmp scratch read by no gate.

**Pre-named FAIL ⇒ STOP**: any HARD gate failing, any `src/**` diff, or any contradiction between
this document and the run — reported as-is, never re-cut after sight.

## §NON-CLAIMS

This stage claims **no** football effect and adjudicates **nothing**: it produces per-arm rows and
paired deltas. The smoke below is **12 seeds** — a plumbing shakedown and a delivered-dose /
delivered-lead reading, **not** evidence about H-PTP. It changes no TeamBrain assignment or
licence, no price table, no receiver-side law and no engine byte; `src/**` is untouched and the
production fingerprint is unchanged. It does not claim the seven cells exhaust the joint space
(they are seven points in a 16-dimensional matrix × a scalar gene), it does not claim the percept
motion channel is GOOD information (only that it is HIS information, and that it is THIN), and it
cannot authorize the battery — only the commander can. **F-PTP-a/b/c are the commander's.**

---

## §RESULT — the smoke

*(every number below is quoted FROM the committed artifact
[`data/ptp-t1-full-channel-smoke.json`](data/ptp-t1-full-channel-smoke.json), recomputed by
`PTPT1_MODE=smoke npx tsx scripts/probes/ptp-t1-full-channel.ts`, and every table below was
GENERATED **programmatically** from that artifact rather than typed — the #229.2 lesson (the
OBM-T1 smoke's fabricated MAX column) enforced by construction. The doc carries no evidence the
artifact does not.)*

**12 seeds × 7 arms** (12,425,026–037), paired on one shared seed list, **plus** the SIX receipt
walks (O2-T1 12 · #173 40 · GGC 12 · CTB-T1 8 · ⭐ OBM-T1 8 × 2 arms) and the 7 delivered-dose
reads — and the whole core runs **twice** (X-DET). Verdict: **ALL 18 GATES PASS**
(`allGatesPass: true`), probe exit 0.

* **`resultSha256`** `7ec507f412bf40b8ef7590ca41c5f66cac17d08f93a829e2ca2cdf4cc8c2a958`
* **X-DET core digest** `5f416245f5aa53babf4e0c81954099d60cdeb6bff109f852bb1b8784f30a9bbd` (both passes)
* Wall ≈ **321 s** — CONTEXT ONLY (#128), used in no rate and no gate. Per #197-M1 the hashed
  body is commit-free, timing-free and path-free.

### Gate table

| gate | verdict | evidence (all recomputed in-probe, #181.2) |
| --- | --- | --- |
| **X-DET** | ✅ PASS | two passes of the whole core, identical digests |
| **X-FP-PROD** | ✅ PASS | `57b0bdab…c673` re-derived unchanged (seed 1337, 2 seasons) |
| **X-SRC-UNTOUCHED** | ✅ PASS | `git diff --stat -- src` **EMPTY** — INSTRUMENT-ONLY, no engine byte moved |
| ⭐ **G-REPRO-OBMT1** | ✅ PASS | block 12,424,100–107 against the committed OBM-T1 battery (`9f42b0b6…2091`): **8 rows × 22 fields, 0 mismatches on the ABSENT arm AND 0 on CHECK-AND-SHOW** — signature (rng stream state inside) included. ⭐ Which also means this stage's `performPass` wrapper perturbs **nothing**: it is installed on these walks too |
| ⭐ **G-TRACE-PTP** | ✅ PASS | all four source lines matched VERBATIM (`PTP_FLIGHT_SPEED = 18` · `PTP_LEAD_FLIGHT_MUL = 1.6` · the through-ball loop's `/ 18` · `runBurstPoint`'s `* 1.6`); the gene map probed through the shipped `passLeadSupportWeight`: absent **0**, min **0**, half **0.5**, max **1**, and clamped at **0 / 1** beyond both ends |
| ⭐ **G-FORK-TOKENS-PTP** | ✅ PASS | **50 src occurrences, ZERO unclassified**; exactly **1** `FLAG_FORK` (in `PlayerBrain.ts`) · 1 `LEAD_COMPUTE` · 1 `AIM_COMPOSE` · **3** `AIM_APPLY` (lane · open · gain) · 2 `LEAD_CAPTURE` · 1 `STRIKE_GUARD` · **1** `STRIKE_LED` |
| **G-REPRO-CTBT1** | ✅ PASS | 8 rows × 20 fields, 0 mismatches, signature included |
| ⭐ **G-BLIND-WORLD** | ✅ PASS | every arm percept-armed; snapshots **99.940–99.943 %**, `someFeatureNonZeroShare` **98.78–99.20 %**, all four feature means > 0 in every arm. ⚠ `allFeaturesZeroShare` **0.80–1.22 %** is an **UPPER BOUND** on genuine silence |
| **G-FORK-TOKENS (OBM)** | ✅ PASS | 37 src occurrences, 0 unclassified — OBM-T1's own inventory, unchanged |
| **G-REPRO-O2T1** | ✅ PASS | 12/12 rows, 0 mismatches |
| **G-REPRO-173** | ✅ PASS | pressedShare **0.7959** · pressed **1,049** · unpressed **269** · all **1,318**, field for field |
| **G-REPRO-GGC** | ✅ PASS | **63/63** committed fields, 0 mismatches |
| **G-TRACE-RADIUS** | ✅ PASS | `radius = 10 + g.supportDistance * 8` parsed from source |
| **SEED-DISJOINT** | ✅ PASS | ⭐ all **NINE** block rows machine-checked: FRESH exam 12,425,026–037 · dose read 12,425,040; RESERVED guard 12,425,050–099 · battery 12,425,100–727; RE-WALKS O2-T1 · #173 · GGC · CTB-T1 · ⭐ OBM-T1, each landing **inside** its source's consumed interval (the inverted predicate). `stageOwnOverlaps` **empty**, `stageOwnUnified` empty (smoke), sub-blocks ordered, battery room **800**, next consumed **12,425,900** |
| **STATS-DISJOINT** | ✅ PASS | base **105,200**, min gap **200** against the complete published namespace (48 bases) |
| **FLAG-HYGIENE** | ✅ PASS | **12/12** seeds ARMED-ZERO-BOTH ≡ ABSENT — whole-match signature **and** every row field; doses well-formed (matrices at domain corners, genes in `{0, ½, 1}`); two-doors row: `ctbSupportPlane` FALSE and `edsPerceivedChoice` TRUE in 7/7 arms, `obmMovement` matching the matrix and `ptpPassLead` matching the gene in 7/7, and the gene read back **through the shipped map** on both teams |
| ⭐ **G-ARM** | ✅ PASS | BOTH seams. OBM half: matrix on 6/6 views on 12/12 seeds, **120 policy writes per armed arm**, the four support-tick classes `partitionExact` in 7/7, ARMED-ZERO-BOTH shift **exactly 0** with 148,217 planes PRESENT and both multipliers exactly 1. PTP half: `handed === nonZero` in **every** arm, zero lead metres in every inert arm, and on the law check **0 sign violations and 0 magnitude violations** in all four dosed arms |
| **G-CLEAN-INVOCATION** | ✅ PASS | no override in force |

### ⭐⭐ THE DELIVERED LEAD — the thin-channel visibility row (#232.3), and the stage's most informative table

| arm | gene | passes chosen | led (NON-ZERO) | led share | mean lead | max lead | lead / pass dist | interceptions per led pass |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| ABSENT | absent | 1,408 | 0 | 0.00 % | 0 m | 0 m | 0 | n/a (no led passes) |
| ARMED-ZERO-BOTH | 0 | 1,408 | **0** | **0.00 %** | **0 m** | 0 m | 0 | n/a (no led passes) |
| CHECK-AND-SHOW | absent | 1,353 | 0 | 0.00 % | 0 m | 0 m | 0 | n/a (no led passes) |
| LEAD | 1 | 1,456 | 307 | **21.09 %** | **5.6261 m** | 15.6533 m | 0.43797 | 1.17590 |
| ⭐ **COMBINED** | 1 | 1,460 | 280 | **19.18 %** | **5.5159 m** | 15.9480 m | 0.43232 | 1.36786 |
| COMBINED-HALF | ½ | 1,406 | 265 | 18.85 % | **2.8859 m** | 7.7382 m | 0.21069 | 1.23019 |
| KITCHEN-SINK-LEAD | 1 | 1,689 | 374 | 22.14 % | 5.2839 m | 15.4777 m | 0.41528 | 1.13904 |

⭐ **Read exactly.** (i) **The thin channel arrived at the size the stage predicted before it
ran**: the led share is **18.85–22.14 %** against PTP-T0's percept-world reading of 21.7 % and its
bare-world 62.9 % — i.e. **≈ one third of bare**, exactly as §FORM declared at the head. The exam
is measuring the HONEST channel, and it says so with a number rather than a promise.
(ii) ⭐ **THE DOSE-RESPONSE IS CLEAN ON THE PASSER'S AXIS**: COMBINED-HALF delivers **2.8859 m**
of mean lead against COMBINED's **5.5159 m** — a ratio of **0.523**, i.e. the half gene delivers
half the metres — while the two arms' led SHARES are within 0.33 pp of each other (18.85 % vs
19.18 %). The gene scales the SIZE of the lead, not how often one is available; that is what the
frozen law says it should do, measured on real chosen passes. (iii) The three arms with the gene
absent or at zero deliver **exactly zero metres on exactly zero passes**, which is PTP-T0's
arithmetic-exact zero point cashed at T1 grain. (iv) `interceptionsPerLedPass` is **n/a** where no
led pass exists — the ratio does not exist there (Deviation 2), and where it does exist it is a
ratio of two per-arm totals, **not** an attribution of any individual interception.

### THE DELIVERED OBM DOSE (the receiver's side — OBM-T1's own table, this stage's arms)

| arm | support ticks | policy writes | shifted | plane-zero | plane-absent | mean shift | max shift | moved | plane depth/width (on present) |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| ABSENT | 148,217 | 0 | 0 | 0 | 148,217 | 0 m | 0 m | 0.00 % | 0 / 0 |
| ARMED-ZERO-BOTH | 148,217 | 120 | **0** | 148,217 | 0 | **0 m** | 0 m | **0.00 %** | 0 / 0 |
| CHECK-AND-SHOW | 138,049 | 120 | 54,131 | 83,015 | 0 | 0.7993 m | 3.7295 m | 39.21 % | −0.06246 / 0 |
| LEAD | 136,114 | **0** | **0** | 0 | 136,114 | **0 m** | 0 m | **0.00 %** | 0 / 0 |
| ⭐ **COMBINED** | 127,479 | 120 | 49,188 | 77,465 | 0 | 0.7363 m | 3.7720 m | 38.59 % | −0.05786 / 0 |
| COMBINED-HALF | 144,148 | 120 | 53,966 | 89,128 | 0 | 0.7363 m | 3.7295 m | 37.44 % | −0.05802 / 0 |
| KITCHEN-SINK-LEAD | 145,338 | 120 | 142,038 | 3,300 | 0 | **4.5722 m** | 13.0198 m | **97.73 %** | −0.30878 / +0.30878 |

The receiver-side doses land where OBM-T1's did (its battery read 0.6993 m mean / 37.44 % moved at
CHECK-AND-SHOW and 4.317 m / 97 % at the ceiling), which is what a re-used matrix in a re-used
world should do — and G-REPRO-OBMT1 is the receipt rather than this sentence.

### ⭐ TIER 1 — the supply rulers. NOTHING IS ADJUDICATED (#203)

**Ruler 1 — TRUE-holdable supply** (ABSENT **0.5391 %**):

| arm | point | Δ (pp) | 95 % CI (pp) | `resolved` |
| --- | --- | --- | --- | --- |
| ARMED-ZERO-BOTH | 0.5391 % | +0.0000 | [0, 0] | no |
| CHECK-AND-SHOW | 0.5510 % | +0.0119 | [−0.6655, +0.6868] | no |
| LEAD | 0.2907 % | −0.2484 | [−0.9367, +0.3219] | no |
| ⭐ **COMBINED** | 0.5556 % | **+0.0165** | [−0.8130, +0.8803] | no |
| COMBINED-HALF | 0.4138 % | −0.1253 | [−0.9410, +0.6775] | no |
| KITCHEN-SINK-LEAD | 0.4138 % | −0.1253 | [−0.8533, +0.5398] | no |

**Ruler 2 — pressed first reception** (ABSENT **83.5968 %**):

| arm | point | Δ (pp) | 95 % CI (pp) | `resolved` |
| --- | --- | --- | --- | --- |
| ARMED-ZERO-BOTH | 83.5968 % | +0.0000 | [0, 0] | no |
| CHECK-AND-SHOW | 82.2266 % | −1.3703 | [−7.1737, +4.8149] | no |
| LEAD | 80.8989 % | **−2.6980** | [−8.8794, +3.3220] | no |
| ⭐ **COMBINED** | 83.0078 % | −0.5890 | [−6.3100, +5.4581] | no |
| COMBINED-HALF | 83.3333 % | −0.2635 | [−6.5415, +5.5694] | no |
| KITCHEN-SINK-LEAD | 86.8030 % | +3.2061 | [−1.5880, +7.9647] | no |

**No cell of TIER 1 resolves, in either direction, at 12 seeds** — which is what a smoke is
expected to say, and says nothing about the battery.

### ⭐⭐ TIER 2 — the #230.5(甲) confirmatory, read AT THE COMBINED CELL

**constructed ≥5 (non-set-piece pool)** — ABSENT **18.1818 %**:

| arm | point | Δ (pp) | 95 % CI (pp) | `resolved` |
| --- | --- | --- | --- | --- |
| ARMED-ZERO-BOTH | 18.1818 % | +0.0000 | [0, 0] | no |
| CHECK-AND-SHOW | 0.0000 % | −18.1818 | [−35.7143, −5.0000] | **YES** |
| LEAD | 6.2500 % | −11.9318 | [−31.4480, +4.4444] | no |
| ⭐ **COMBINED** | 4.7619 % | **−13.4199** | [−34.9702, +6.1086] | no |
| COMBINED-HALF | 8.0000 % | −10.1818 | [−32.0099, +8.5165] | no |
| KITCHEN-SINK-LEAD | 0.0000 % | −18.1818 | [−35.7143, −5.0000] | **YES** |

**scramble share of goals** — ABSENT **57.1429 %**:

| arm | point | Δ (pp) | 95 % CI (pp) | `resolved` |
| --- | --- | --- | --- | --- |
| ARMED-ZERO-BOTH | 57.1429 % | +0.0000 | [0, 0] | no |
| CHECK-AND-SHOW | 45.4545 % | −11.6883 | [−32.7273, +5.7190] | no |
| LEAD | 65.0000 % | +7.8571 | [−22.0000, +30.3030] | no |
| ⭐ **COMBINED** | 68.0000 % | **+10.8571** | [−26.0766, +39.8824] | no |
| COMBINED-HALF | 55.2632 % | −1.8797 | [−21.9595, +15.8730] | no |
| KITCHEN-SINK-LEAD | 61.1111 % | +3.9683 | [−19.3939, +25.0000] | no |

**The frozen confirm/kill predicate, evaluated mechanically at COMBINED:**

| column | Δ | CI | #230 point of record | mechanical verdict |
| --- | --- | --- | --- | --- |
| `constructedGe5Share` | −13.4199 pp | [−34.9702, +6.1086] pp | **+2.9575 pp** (CHECK-AND-SHOW, #230.4) | **INCONCLUSIVE** |
| `scrambleShareOfGoals` | +10.8571 pp | [−26.0766, +39.8824] pp | **−6.0720 pp** (SPACE-SEEK, #230.4) | **INCONCLUSIVE** |

⚠⚠ **AND THE CAPTION THAT MATTERS MORE THAN THE CELLS.** At 12 seeds each arm scores **18–38
goals in total**, and the pool the construction ladder is read on — non-set-piece goals — is
**13–25 goals per arm** (ABSENT 28 goals / pool 22; COMBINED 25 / 21; KITCHEN-SINK-LEAD 18 / 13).
A single goal is therefore worth **4.5–7.7 pp** on these shares: they move in steps and their
CI is ±25–40 pp — three times the effect it is sized against. That is why two arms read **exactly
0.0000 %** on the ≥5 rung and why two `resolved=true` flags appear on a column nobody should read
at this grain. **These are plumbing readings, not evidence**, and the INCONCLUSIVE verdict at
COMBINED is the predicate correctly reporting that a 12-seed CI cannot separate the #230 point
from zero. The predicate exists to be evaluated at **battery N**, where it will have the CI it was
frozen for.

### The other three ruler-5 shares (REPORTED, no gate, not primary)

`constructedGe3Share`: ABSENT 40.9091 %; CHECK-AND-SHOW −40.9091 pp [−71.4286, −15.0000]
**resolved**, LEAD −28.4091 pp [−59.7222, −4.5238] **resolved**, COMBINED −26.6234 pp
[−63.6364, +5.2536], COMBINED-HALF −8.9091 pp, KITCHEN-SINK-LEAD −40.9091 pp **resolved**.
`constructedGe4Share`: ABSENT 18.1818 %; CHECK-AND-SHOW and KITCHEN-SINK-LEAD −18.1818 pp
**resolved**, everything else unresolved. `setPieceShareOfGoals`: ABSENT 21.4286 %;
CHECK-AND-SHOW +24.0260 pp [+0.1208, +45.2489] **resolved**, everything else unresolved. Same
caption: **~22 goals per arm, nothing here is evidence.**

### REPORTED — rulers 3/4 with their ceilings

`4b support @ pressed first reception`: ABSENT **98.818 %** ⇒ helpful headroom **1.182 pp**.
`3b short option / first reception`: ABSENT **98.814 %** ⇒ helpful headroom **1.186 pp**. Both are
even more saturated in this stage's smoke than in OBM-T1's battery (97.72 % / 97.73 %), which is
exactly why the exam is carried by rulers 1 + 2 and by the tier-2 pair.

### THE GUARDS — **ZERO breaches at every dose**

| limb | control | tolerance | resolved arms | **breaches** |
| --- | --- | --- | --- | --- |
| interceptions/match | 29.1667 | ±8.0592 | **COMBINED +2.75**, KITCHEN-SINK-LEAD +6.33 | **none** |
| spreadYOut (floor) | 5.4019 | ±1.4926 | LEAD +0.42, COMBINED +0.27, COMBINED-HALF +0.20, KITCHEN-SINK-LEAD +0.86 (**wider** — the helpful direction) | **none** |
| spacingMedian (floor) | 12.6701 | ±3.5009 | KITCHEN-SINK-LEAD +0.65 (wider) | **none** |
| spacingUnder4 (ceiling) | 0.10426 | ±0.02881 | LEAD −0.0126, COMBINED −0.0102, KITCHEN-SINK-LEAD −0.0183 (**less** clumping) | **none** |

⭐ **The F-PTP-b named risk, read as the contract asked.** Interceptions **do** rise at the two
arms that combine movement with a full lead — COMBINED **+2.75/match resolved** and
KITCHEN-SINK-LEAD **+6.33/match resolved** — and **both sit inside the ex-ante tolerance
(±8.06), so neither is a breach and no dose is disqualified.** LEAD alone is +0.92 unresolved and
COMBINED-HALF is **−2.00** unresolved, so the rise is not monotone in the lead gene at this grain.
It is the direction F-PTP-b was written to watch, it is **not** firing, and at 12 seeds it is a
direction rather than a finding — the battery is where it gets a CI worth reading.

Offsides (the #157 FLAG form): **COMBINED +1.5833/match `resolved` — a resolved INCREASE**, the
only offside flag in the table (LEAD +0.58, COMBINED-HALF +0.92, KITCHEN-SINK-LEAD +0.50,
CHECK-AND-SHOW +0.25, all unresolved). Per the frozen form this **returns to the commander and
flips no gate**; a led pass aimed where a support runner is GOING is a mechanically plausible
offside generator, and at battery N it is the second thing to read after the interception limb.

Equilibrium band: **`headers`, `longBalls` and `cutbacks` are EXCLUDED** because the ABSENT arm
itself fails them at 12 seeds (the #198-form exclusion, disclosed); of the two gated dimensions
(`goals`, `crosses`) ABSENT · ARMED-ZERO-BOTH · CHECK-AND-SHOW hold both and the four
lead-carrying arms do not. **The band gates at battery N only**, so this row is context.

### THE N RULE (in-probe, from the committed artifacts)

DEFF **0.8302** (INHERITED — no same-stage committed smoke existed when this run started, and the
probe refuses to size a smoke off a previous smoke). q1 ⇒ **N 628** (p0 0.006391, MDE 0.001575,
59.65 eligible moments/seed), q2 ⇒ N 66 (p0 0.8085, MDE 0.03073, 34.48 first receptions/seed),
binding **q1**, **N\* = 628**; ledger room 800 (binds=false), cap 628 (**binds=false**). Battery
block **12,425,100 – 12,425,727** — inside the ledger and below the 12,425,899 ceiling. At 7 arms
× 628 with the armed-world cost, budget **≈45–55 min** of wall (this smoke: 7 arms × 12 seeds +
the six receipt walks in **321 s**).

⭐ When the battery runs, the rule **re-reads p0 and moments-per-seed from this committed smoke's
ABSENT arm** and takes `MAX(inherited DEFF, same-stage smoke DEFF)`; the substitution publishes
itself as `sourceOfP0` / `deffProvenance` on that run.

### §CHECKS

```text
$ npx tsc --noEmit
(clean)

$ PTPT1_N=1 PTPT1_SKIP_FP=1 npx tsx scripts/probes/ptp-t1-full-channel.ts   (the OVERRIDE route)
  ⚠ OVERRIDE IN FORCE — routed onto the EXIT-SEMANTICS GUARD BLOCK 12,425,050..12,425,099
  gCleanInvocation *** FAIL ***   xFpProd *** FAIL ***   seedDisjoint *** FAIL ***   ALL *** FAIL ***
  exit 1 · artifact written to /tmp/ptp-t1-guard-run.json — NEVER to docs/
  (⭐ seedDisjoint reds here BY CONSTRUCTION and correctly: the routed exam block sits INSIDE the
   reserved guard block without being EQUAL to it, which is exactly the PARTIAL overlap the
   corrected #230.1 predicate is built to fail. An override run adjudicates nothing.)

$ PTPT1_MODE=smoke npx tsx scripts/probes/ptp-t1-full-channel.ts
  ALL                PASS      (18 gates)
  exit 0 · resultSha256 7ec507f412bf40b8ef7590ca41c5f66cac17d08f93a829e2ca2cdf4cc8c2a958
  wall 321 s (CONTEXT ONLY) · artifact docs/world-model/data/ptp-t1-full-channel-smoke.json

$ npm test
  Test Files  1 failed | 130 passed (131)
       Tests  1 failed | 1269 passed (1270)
  × tests/formationEvolution.test.ts > league-level style ecology — "Test timed out in 180000ms"

$ npx vitest run tests/formationEvolution.test.ts                (re-run in isolation)
  Test Files  1 passed (1)   Tests  3 passed (3)   —  147.50 s for that one file
```

⚠ **The single red is the known wall-clock flake family (#196.2), disclosed not excused**: the
test needs ~148 s against a 180 s limit, so under a loaded parallel suite it times out and in
isolation it is green. It cannot be this stage's: `src/**` is **byte-untouched**
(X-SRC-UNTOUCHED PASS, `git diff --stat -- src` empty) and **no test file was edited or added** —
this round's only changes are one probe, one doc and one artifact.

### Deviations recorded

1. ⭐⭐ **A GATE PREDICATE FROZEN EX ANTE WAS FALSE ABOUT THE SHIPPED MECHANISM, AND WAS MADE
   STRONGER RATHER THAN LOOSER** (the OBM-T1 Deviation-1 precedent, followed exactly). §GATES'
   G-ARM said an arm with `ptpPassLead` armed *"hands the strike a lead on **every** chosen
   pass"*. The **first guard-block run reported exactly that as a FAIL**, and the cause is
   PTP-T0's own §PINS 1: the incumbent `match.performPass(p, passMate!, offsideExemptKick);`
   line is **pinned VERBATIM** by the O1 wind-up's seam-singularity test, so the led strike could
   never become an extra argument on it — it is its **own armed-only statement behind a
   STRIKE_GUARD** (`passMate === bestMate && (bestLeadX !== 0 || bestLeadY !== 0)`). A lead object
   is therefore handed **if and only if** the priced displacement was non-zero. **The corrected
   predicate asserts that EQUALITY** — `ledPassesHandled === ledPassesNonZero`, in **every** arm,
   armed or not — which the original inequality could never have caught, and which is also the
   mechanical reason ARMED-ZERO-BOTH can be byte-identical to ABSENT at the strike. No arm, dose,
   ruler, guard, estimator or seed block changed; the failing run's cause is published here rather
   than smoothed away.
2. ⚠ **A REPORTED COLUMN PUBLISHED A NUMBER THAT DID NOT EXIST, AND WAS CAUGHT BEFORE THE
   ARTIFACT WAS COMMITTED.** The first smoke computed `interceptionsPerLedPass` with a
   `max(1, denominator)` clamp inherited from the sibling rate helpers, so the three arms with NO
   led passes published **350 / 350 / 330** — the arm's whole interception count wearing a
   per-led-pass rate's label, and the paired deltas against it were −348 nonsense. It is exactly
   the #229.2 class of defect (a cell that reads as a measurement and is not one). Fixed to
   **NaN** — the ratio does not exist there — so the bootstrap's finite filter drops it, the CI is
   non-finite and `resolved` is false. The smoke was **re-run whole**; `resultSha256`
   `01d72d6d…4cb4` and X-DET digest `81085cd5…5fb7` are **SUPERSEDED** by the numbers above, and
   **every other measured cell is unchanged** (the delivered-lead, delivered-dose, ruler, guard
   and gate rows are identical between the two runs).
3. **The LEAD LAW CHECK LIVES IN THE OBSERVATIONAL DOSE READ, NOT IN THE EXAM WALK** — declared,
   not discovered. Re-deriving the delivered lead independently requires `passLeadSeatOf`, which
   pulls `match.perceivedSnapshot(p)` and **advances that body's percept memory**; inside an exam
   arm that would be an intervention wearing an instrument's clothes (the OBM-T1 rule, inherited).
   So the exam walk records only the strike's own arguments — zero pulls — and the law check runs
   on the declared dose-read seed. **Its N is therefore small** (13–20 led passes per dosed arm,
   one match each) and it is a LAW check, not a rate: **0 sign and 0 magnitude violations** in all
   four dosed arms.
4. **The world is OBM-T1's, by necessity** (§FORM), so absolute levels are not comparable with
   CTB-T1's or with the bare production world; every contrast here is within-world and paired.

### Disposition

The instrument is built, twice-deterministic, gate-green on **18/18**, and **proved to be
OBM-T1's own** on two of its arms — with the strike wrapper's innocence riding in the same
receipt. The two-tier primary and the #230 confirm/kill predicate are frozen **with their
numbers** and evaluate mechanically. Nothing is adjudicated: **F-PTP-a/b/c are the commander's**,
and at 12 seeds the honest summary is *both seats are reached, both doses arrive at measurable and
published sizes, the lead channel is thin exactly as predicted and scales cleanly with the gene,
the identity arm is exact, and no guard breaches at any dose*. The battery (N = 628, block
12,425,100–12,425,727, ≈45–55 min) is the commander's to dispatch.
