# BU-T1 — THE MT KEEP/HOLD SEAM IN THE v7 COMPOSITION (does the tuck-in change how the ball is kept?)

Status: **PRE-REGISTERED — everything above [§RESULT](#result) was written and COMMITTED BEFORE
THE BATTERY RAN** (#266.3(c), the freeze-commit canon). The measured numbers arrive only in
§RESULT, and every number there is printed by the probe from the committed artifact
[`data/bu-t1-mt-composition.json`](data/bu-t1-mt-composition.json) — none is typed (#229.2).

Authority chain: the **BUILD-UP CONTRACT** [`BU-BUILDUP-CONTRACT.md`](BU-BUILDUP-CONTRACT.md)
§2 (**M-BU.1** the inventory is the law · **M-BU.2** composition debts bind · **M-BU.3** pricing
honesty · **M-BU.4** scope) and §3 (the assembly slices), bound by **#285.1**; this stage
authorized by **#288.7** (*"BU-T1 = MT KEEP/HOLD (#213) dispatches NOW — the bound second, the
arc's LAST assembly slice: arms v7 vs v7+MT (DV stays unarmed — it nulled), the M-BU.2
lifecycle/doors proof at CB+L3+MT FIRST"*). Hygiene canon in full: **#163** (booked = walked) ·
**#181.2** (gates computed in-probe) · **#200** (no invented literal) · **#229.2** (nothing typed
the artifact can print) · **#256.2** (COMMENSURABILITY — the engine's own machinery, never a
parallel oracle) · **#261.2** (env WHITELIST-OR-REFUSE) · **#264.2 / #268.3** (the DEAD-PREDICATE
class) · **#266.3(a)** (the hashed body excludes ALL invocation context) · **#266.3(c)** (the
freeze commit) · **#268.3(a)** (LIVENESS BY MACHINE, EXACTLY-ONE ENFORCED) · **#270** (the dose
never in `info.genome`) · **#272.3(ii) / #280.2(iii)** (clock honesty, APPLIED values) ·
**#273.3 as CORRECTED by #286.1** (tree-clean gates compare WORKTREE vs **HEAD**, both
conjuncts — the canon lives at BU-C0 §COMMANDER CORRECTIONS 5) · **#282.2(ii)** (per-seed cells
stored) · **#283.2(iv)** (matches constructed DIRECTLY with `matchFlags`; `League.toJSON` omits
them) · **#287.1** (a re-derivation gate reads the SERIALIZED artifact) · **#288.4** (⭐ every
starred finding states its |Δ| ÷ half-width) · **#276.3 → #278.2(ii) → #279.2(i) → L3-T2 MED-4
→ #286.4 → #288.4** (⭐ THE STANDING CITATION HUNT, at **SIX** strikes — and #288.4 extends it
to **DISPATCH PROMPTS**).

> ⭐ **INSTRUMENT-AND-COMPOSITION ROUND.** `src/**` is **BYTE-UNTOUCHED** — `xSrcUntouched` is a
> HARD gate, in the #286.1-corrected form. **ZERO `src/**` edits were needed**: every arming
> this stage performs is construction-flag wiring plus two gene writes through match-local
> genome views.
>
> ⭐⭐ **NOTHING HERE IS SCORED.** H-BU.1 is scored at **ARC EXIT** on the assembled composition
> (#286.3's amended seat). Every football face below is **REPORTED**; no gate reads one.
>
> ⭐ **AND THE KEEP/HOLD VERDICT IS NOT TAKEN HERE.** It is the user's (#213.3(丙)).

---

### ⭐ §CORRECTIONS-READ — the standing hunt, discharged explicitly

Every prior-document number quoted below was read **after** opening that document's own
corrections section.

* **BU-T0b [`BU-T0B-PRICE-SEPARATION.md`](BU-T0B-PRICE-SEPARATION.md) §COMMANDER CORRECTIONS OF
  RECORD (#288) — READ IN FULL.** Binding here:
  1. **(MED 2) THE |Δ| ÷ HALF-WIDTH CANON.** "5–20× their half-widths" was FALSE for the usage
     and supply headlines; of record the supply faces sit at **options/reception 1.94×** and
     **the starred pressed-supply rise 1.26× — MARGINAL**. ⭐ This stage applies the canon **by
     machine**: every published contrast carries `absDeltaOverHalfWidth` and a `strength` label
     (`< 1` UNRESOLVED · `< 2` ⚠ MARGINAL · else RESOLVED), so no finding can be rounded up in
     prose.
  2. **(LOW 4) undeclared deviations from frozen sizing numbers are a finding in themselves** —
     every deviation in this stage is declared in §FORM or §NRULE.
  3. **(LOW 5 — ⭐ THE SIXTH CITATION STRIKE)**: the corrected `xSrcUntouched` form is BU-C0
     §COMMANDER CORRECTIONS 5, ruling **#286.1** — **NOT "#286.5"** (which is the seed ledger).
     ⚠ **THIS STAGE'S OWN DISPATCH PROMPT CARRIED THE CORRECT NUMBER**, and every clause number
     it cited was re-opened before being propagated here.
  4. **(MED 1)** the DV price reached knocks as well as passes — noted; **irrelevant here, the
     DV seam is UNARMED on both arms** (`gArms` asserts it shut).
* **BU-T0 [`BU-T0-DV-COMPOSITION.md`](BU-T0-DV-COMPOSITION.md) §COMMANDER CORRECTIONS OF RECORD
  (#287) — READ IN FULL.** Binding here: (1) ⭐ **a re-derivation gate must read the SERIALIZED
  artifact** — this probe's `gFaces` parses the JSON off disk and rebuilds a `Row` per stored
  cell (BU-T0b's `rowFromCell` inverse, copied); (3) **cells-vs-walks unit slippage** — every
  doors count published below says WALKS where it means walks; (5) replication on virgin seeds
  is **replication-consistency**, not a re-measurement, and the numbers of record going forward
  are BU-T0's (corridor **82.08 %** of the outfield option loss · outfield end-to-end
  **21.08 %** · keeper lane survival **2.3×**).
* **BU-C0 [`BU-C0-RECEPTION-OPTION-CENSUS.md`](BU-C0-RECEPTION-OPTION-CENSUS.md) §COMMANDER
  CORRECTIONS OF RECORD (#286) — READ IN FULL.** Binding here: (1) **the ladder is GK-INCLUSIVE**
  and every instrument quoting it carries **GK-SPLIT RUNGS** — discharged here, L1–L4 split;
  (2) **L1 owns 0 % of the ladder loss by construction** — the statistic for a body-adding change
  is END-TO-END CONVERSION; (3) ⚠ **THE ARMED TERMINAL CENSUS IS ENTANGLED WITH THE L3 VETO** —
  **both arms of this stage carry the veto**, so this stage's *contrasts* are entanglement-free
  by construction while its *levels* are not, and `lossToOpponentShare` is the honest cross-arm
  aggregate; (5) **the corrected `xSrcUntouched` implementation is COPIED, not its header**
  (AN INHERITED FIX ANNOUNCED IS NOT A FIX RIDDEN).
* **CB-T2 [`CB-T2-CHOICE-SEAT.md`](CB-T2-CHOICE-SEAT.md) §COMMANDER CORRECTIONS (#269.2/#269.3)
  — READ IN FULL.** Binding here: **(iv) THE NAMED DEBT** (the `clearTouchPastArming` staleness
  class at a new composition) — §LIFECYCLE is that proof, at **CB+L3+MT**.
* **L3-ENTRY-RUNG [`L3-ENTRY-RUNG.md`](L3-ENTRY-RUNG.md) §COMMANDER CORRECTIONS (#283.2) — READ
  IN FULL.** Binding here: **(iv)** worker-simmed fixtures play the SHIPPED world, so this probe
  constructs every match directly and asserts the arming on the very match it walks.
* **MT-LADDER [`MT-LADDER.md`](MT-LADDER.md) §RESULT + §ENTRY (no corrections section — checked;
  its own ⚠ CORRECTION-INHERITED rows read).** Binding here: the **ruled knee = 0.2** via the
  **NONE_ABOVE_FLOOR** branch; ⚠ the knee is *"an EXHIBIT DOSE for the user's play-test verdict …
  NOT a ship decision"*; ⚠ **#203: adjudicate from the per-arm rows, never from the knee line**;
  the arming checklist (flag + evolve opt-in + non-absent gene) and **§ENTRY's own reading that a
  fixed armed world arms NO evolution opt-in**; and ⭐ **the ladder's own load-bearing finding —
  "the trade is CONTINUOUS, there is no free dose"** (at 0.2 on ITS substrate: body −0.586 m
  **NOT resolved**, goals 2.19 → 1.99, headers −23 %).
* **MT-T0 [`MT-T0-DORMANT-SEAM.md`](MT-T0-DORMANT-SEAM.md) §SEAM + §GATES (no corrections
  section — checked).** Binding here: the seam is `markDist′ = markDist + clamp01(markSag) ·
  sagOf(slack)`, it **only ever ADDS distance and never tightens**, and **G-BORN** — armed with
  the gene ABSENT is byte-identical to OFF *through the live branch*. That law is re-proven here
  on every cell of the doors matrix.
* **#270.2 (the ruling itself) — READ.** *"the dose is NOT written to `info.genome` (the A4/MT
  idiom would have persisted a dormant gene into the user's save and fed crossover) — the
  `dvLearn` de-aliasing form used instead."* This is why §FORM's gene write deviates from
  `setMtDose`.
* ⚠ **THE #246 BAND IS THE PROGRAMME'S OWN PRE-REGISTRATION, NOT A CITED FOOTBALL STATISTIC**
  (BU-C0 §DOUBTS 2: never scaled to 6v6). Used as a direction, never as a band a number is
  scored against.

---

## §0 THE QUESTION, in football

Three stages have now said the same thing three different ways: **人是有的, 球路是死的.** BU-C0
counted the bodies (2.605 behind the ball per reception) and found the lane killing the ball;
BU-T0 armed the only banked seam that prices lanes and got a **57/57-face null**; BU-T0b proved
the price CAN be heard and that what a louder honest map buys is **less** build-up — while the
corridor's lethality stayed **UNMOVED at every rung**. ⭐ Of record (#288.3): **THE LANE HAS NO
BANKED COVER.**

**So this slice is not aimed at the lane.** MT is the arc's last banked seam and it is a
DEFENSIVE one: 松盯内收 — the marker holds off the man he is watching while the ball is in
flight, and the back line drifts toward the ball's lane. #218.2 recorded, from the goal-genealogy
census, that **the tuck-in IMPROVED the user's 倒脚 complaints**: own-third time 52 % → 42 %,
more completed passes — *"field position frees up when the defence stops chasing"*. That was
measured on the MT worlds' own substrate, as a by-product, on a different instrument.

⭐ **THE SLICE'S QUESTION, in one sentence: when the OPPONENT defends like that, does a team in
the polished world keep the ball differently — and does the census's own picture move?**

⚠ **AND THE HONEST FRAME, PRE-WRITTEN.** MT is armed **symmetrically** (both teams), so this is
not "we attack a tuck-in"; it is "the whole world defends this way". Every face below is a
property of that world, not of one team's build-up against a fixed defence.

---

## §FORM

### The two arms — PAIRED, SAME SEEDS

| arm | construction | role |
| --- | --- | --- |
| **`v7`** | `new Match({ seed, teamA, teamB, ...a4MatchFlags(7) })` + `armA4World(m, null, 7, poolT1DoseCells(L3-T1))` | **THE BASE** — the `?a4world=7` world: the CB layer (`cbCommitPhysics` + `cbTouchPast` + `cbChoiceSeat` at the declared proneness dose) + `l3DefenceLearn` + `l3DefenceVeto` with the shipped entry's pooled matured cells. |
| ⭐ **`v7mt`** | THE SAME, plus `pmLaneConvergence: true`, `mtMarkSag: true`, and `defLaneConvergence = markSag = ` **the ruled knee**, on both teams | **THE SLICE** — the banked tuck-in composed in. |

⭐⭐ **DV STAYS UNARMED ON BOTH ARMS** (#287.2 nulled it; #288.2 struck the derived-calibration
fix). `gArms` asserts both DV doors SHUT and `dvLearn === null` on every walked match.

Squads are redrawn per fixture (`team('A', seed·2+1)`, `team('B', seed·2+2)`) — the banked draw.
The clock is the **ENGINE DEFAULT** (`MATCH_DURATION` 240 sim-s, never overridden; `gClock`
asserts it on every walk; 1 sim-s = 22.5 display-s, both terms extracted from `src/**` at run
time).

⭐⭐ **#283.2(iv), STATED AS THE DESIGN REQUIRES.** Every match is **constructed directly with its
flags**, and `gArms` asserts on **every walked match**: `l3ArmedVersion(m) === 7`, all three CB
doors live, both L3 doors live, the L3 books carrying the dose cell-for-cell, the engine clock at
its default, no arming at construction, the DV pair shut — and, on the slice arm, both MT doors
live with `markSagWeight(effGenome) === ` the knee and `pmLaneConvergenceK(effGenome) > 0`, the
entry's own `mtArmedVersion(m) === 4`, and **nothing in `info.genome`**.

### ⭐⭐ THE ARMING PATH — what MT's OWN banked docs prescribe, and the one deviation

**MT is a CAPABILITY seam with no books.** There is no maturation, no truth-dosing question and
no "committed cells" to pool: the dose is a NUMBER a ruling fixed. Its provenance:

* the **ruled knee**, read at run time from the committed
  [`data/mt-ladder.json`](data/mt-ladder.json)'s own `results.knee.kneeDose` (SHA-guarded), and
  cross-checked against **`a4World.MT_WORLD_DOSE[4]`** — the shipped play-test entry's copy of the
  same number. `gDose` fails if the two ever disagree, so the stage cannot be silently re-dosed;
* **both genes, EQUAL**, on **both teams** — MT-LADDER §2's coupled axis (#209.1), which is also
  what `MT_WORLD_FLAGS` + `armMtWorld` ship;
* **both consumption flags together** (`pmLaneConvergence` + `mtMarkSag`) — MT-LADDER §2: *"each
  dosed arm throws ALL switches of BOTH seams"*;
* **NO evolution opt-in** — §ENTRY's own reading (#165.2.ii): a fixed armed world mutates nothing.

⚠⚠ **THE ONE DECLARED DEVIATION, AND HOUSE LAW #270 IS WHY.** `a4World.setMtDose` writes the two
genes onto **all three** genome views **including `info.genome`** — the pre-#270 A4/MT idiom.
Ruling **#270.2** ratified the opposite as the better form (*"the dose is NOT written to
`info.genome`"* — it would persist a dormant gene into the save and feed crossover). This probe
therefore writes the genes through the **#270 de-aliasing form** (`setCbProneness`'s: replace
`baseGenome` with a copy, point `effGenome` at it), on **match-local views only**. The consumers
read `team.genome` — which **IS** `effGenome` (`Team.ts:227`) — so the seam receives the identical
dose; `gArms` asserts the dose on that view AND that `info.genome` stays clean, and `gDoors`
proves both doors still move the world at this dose.

⚠ **THE SUBSTRATE IS A SUPERSET OF MT'S OWN, DECLARED.** `MT_WORLD_FLAGS`'s percept pair
(`edsPerceivedDefence` + `edsPerceivedChoice`) is inside `a4MatchFlags(7)`, but v7 also carries
`edsValueAxis`, `c5Hold`, `c6Carry`, `c7Windup`, `o1PassWindup` and the CB + L3 doors. **That is
what a composition slice IS.** Consequence, binding on every quotation: **no MT-LADDER football
number is re-quoted as this stage's measurement** — its body gap, its band rows and its
goals 2.19 → 1.99 were measured on the bare percept substrate with no CB layer and no defence
books.

### ⭐⭐ THE SEAM'S OWN EVENT GRAIN — and the honest reading of "keep/hold"

#288.7 asks for *"keep/hold usage measured at the seam's own event grain (its docs name the
events)"*. **Opened and read: MT's docs name NO keep/hold event.** #213.3(丙) reads *"the MT
worlds' **keep/hold verdict**"* — that is the still-open **USER decision** on whether to KEEP the
banked tuck-in worlds (the #211.3 play-test entry's four exit questions), **not an event class**.

The seam's own instrument, named by its own docs, is **the SAG CENSUS** (MT-LADDER §RESULT
`sagCensus`, MT-T0's `stanceCensus`). This stage re-takes it, at MT-LADDER's own cadence
(`SAG_SAMPLE_EVERY = 15` ticks), on out-of-possession players whose action is `MarkOpponent` with
a resolvable target, publishing **two quantities, both the ENGINE's own**:

* **sag metres** = `markSagMetres(ball.pos, mark.pos, p.pos, p.topSpeed)` — the geometric
  access-time slack, a property of the WORLD that exists on both arms;
* ⭐ **metres ADDED** = `markSagWeight(team.genome) · sag metres` — **the seam's own write**,
  `actionExecutor.ts`'s single line, structurally **0 on the base arm** (born-absent gene).

⚠ **ONE DECLARED SIMPLIFICATION vs MT-LADDER, and it is a HARDENING**: the ladder compared the
sagged stance against a **REPLICA** of the engine's base `markDist` (a probe-side
re-implementation). This probe computes no replica — so there is **no parallel oracle** (#256.2)
— and therefore does **not** re-publish the ladder's `sagged > base` / `tightened` rows. It does
not need them: MT-T0 §SEAM proves the seam **only ever adds**, so *metres added* **is** the bite.

### Instruments 2–4 — unchanged from BU-C0 / BU-T0 / BU-T0b, so the faces travel

**Instrument 2, the option ladder**: BU-C0's definition VERBATIM (L1 POSITION on Q07's own ±2 m
band, EXTRACTED from `src/sim/mechanics.ts` at run time · L2 `evaluatePassAffordance`'s flight ·
L3 `arrivalMargin > 0` · L4 `evaluatePassCorridorInterception`), **GK-SPLIT at every behind-ball
rung** (#286.1's debt). The snapshot is `oraclePerceptionSnapshot` — a **CAPABILITY** census,
never a perception or choice census. **Instrument 3, the direction mix**: FORWARD is R-乙 **Q07
VERBATIM** (the engine's own `passesForward` counter); the ±2 m re-derivation splits ONLY the
engine's pooled complement. **Instrument 4, the spell terminal census**: the #173 / Q01
segmentation VERBATIM.

### The four populations

| population | definition |
| --- | --- |
| **reception** | every tick at which the ball's owner CHANGES while `phase === 'playing'` |
| **pressed reception** | a reception whose receiver has an opponent inside **4.2 m** (`TOUCH_CONTROL_DIST`, imported not typed) |
| **pressed-carrier moment** | a NON-reception carrier tick, sampled every **12 ticks**, at which the carrier is pressed. ⚠ A declared SAMPLE. |
| ⭐ **marker tick** | an out-of-possession `MarkOpponent` body with a resolvable target, sampled every **15 ticks** (MT-LADDER's own cadence). ⚠ A declared SAMPLE. |

---

## §LIFECYCLE — ⭐⭐ THE M-BU.2 PROOF AT A **NEW** COMPOSITION, AND IT RUNS FIRST

**The order of proof is binding (#288.7): this precedes the battery.** A staleness defect at the
authorized composition **REFUSES THE RUN (exit 4)** and reports for adjudication — a fix is a
`src` change needing its own authorization.

#287.3 discharged the debt at **CB+L3+DV**. **CB+L3+MT is a different composition**, so the
proof is re-taken in full — the class, the matrix and the laws below are BU-T0's, re-instantiated
on this stage's own doors.

### The class, stated exactly

`Match.forcedTouchPast` is a **SINGLE match-scoped slot**: WRITTEN by `armTouchPast` from the ONE
call site in `PlayerBrain`, WITHDRAWN by `clearTouchPastArming` at that same site, CONSUMED by
the ONE fork in `Match.stepBall`. **The debt**: a world that arms OTHER seams beside it may take
an **EARLY RETURN** above the seat's block, so the withdrawal never runs, an aim survives its own
tick, and a surviving aim can fire into a **LATER possession**.

### ⭐⭐ THE DOORS MATRIX — the full power set of THIS composition

Seven doors, **2⁷ = 128 cells**, on the v7 substrate (`a4MatchFlags(3)` — CALLED, not copied):

```text
C cbCommitPhysics   T cbTouchPast   S cbChoiceSeat (+ the proneness dose)
L l3DefenceLearn (+ the matured L3 dose)   V l3DefenceVeto
⭐ P pmLaneConvergence (+ defLaneConvergence at the ruled knee)
⭐ M mtMarkSag        (+ markSag at the ruled knee)
```

⚠ **FOUR AXES CARRY THEIR OWN DOSE, DECLARED**: `S` without a proneness cannot form a seat, `L`
without a dosed book has nothing to read, and `P`/`M` without their genes are the seam's own
born-absent no-op — so each axis is "door + its banked dose", exactly as the shipped entries
compose them.

**THE IDENTITY LAWS (every cell, every seed — "a door with no partner never moves the world"):**

1. `cbTouchPast` is INERT without the choice seat (nothing can write the arming slot);
2. `l3DefenceLearn` is INERT without the veto (the book fills, nothing reads it);
3. `l3DefenceVeto` is INERT without the learning door (there is no book to read);
4. ⭐ `pmLaneConvergence` is INERT with `defLaneConvergence` **ABSENT**;
5. ⭐ `mtMarkSag` is INERT with `markSag` **ABSENT**.

⭐⭐ Laws 4–5 are **MT-T0's own G-BORN law** at this composition, and they are the MT family's
answer to "byte-inertness of each door without its partner": MT has no partner DOOR — its partner
is its **GENE**. They are proven by walking the flag-ON / gene-ABSENT world at **every** cell and
comparing signatures with the flag-OFF cell — byte-identity **through a live branch** (the branch
is entered; the weight evaluates to 0).

**THE LIVENESS LAWS (setwise — a door that can never move the world is a DEAD door):**
`cbCommitPhysics` moves it; the choice seat moves it when its capability door is open; the L3
veto moves it on a dosed book; ⭐ **the PM door moves it at the ruled knee**; ⭐⭐ **the MT door
moves it at the ruled knee** — if that one failed, **the slice would be a dead door** and the
round would be over before the football.

### The two halves of the honest law (#287.3's dichotomy, re-proven here)

* **(a)** in **every cell where an aim CAN fire** (`T`), no arming survives its own tick;
* **(b)** in **every cell where armings persist** (`S ∧ ¬T`), **ZERO knocks fire**.

`S ∧ ¬T` is a configuration **no armed world constructs**; it is **REPORTED, never excluded**.

### The structural half, machine-read from `src/**`

`gLifecycle` asserts from the source text: exactly ONE arming write site, ONE withdrawal call
site, the slot cleared in exactly TWO places, ONE firing fork — and the **scope** and
**non-vacuity** as conjuncts: ⚠ `o2Look` and `ekHoldVeto` are **NOT armed here**, so this stage
discharges the debt for **CB+L3+MT ONLY**; ⭐ `o1PassWindup` and `c7Windup` **ARE** armed, so the
early-return exposure is REAL and the measured zero is not a zero of absence. The same read rides
**every walk of the battery**.

---

## §DORMANCY

**Flags off ⇒ `src` byte-identity is the HARD GATE, and the expectation was ZERO `src/**` edits.**
That expectation held: the seam was banked dormant behind its own explicit flag and gene, and
every arming this stage performs is (i) construction flags, (ii) the shipped entry's own
`armA4World`, (iii) two gene writes on match-local genome views. `xSrcUntouched` is computed
in-probe in the **#286.1-CORRECTED** form — `git diff --stat HEAD -- src` **and**
`git status --porcelain -- src` — and every conjunct owns a live mutant.

The doors matrix is dormancy's other half: laws 1–5 are exactly the statement that each seam's
door, standing alone anywhere in the composition, leaves the world byte-identical.

---

## §NRULE — the sizing, frozen before the battery

**N = 200 seeds per arm, paired** (#288.7's budget). The binding term is the per-reception option
count, whose cluster is the MATCH (~140 receptions/match ⇒ ~28,000 receptions per arm).
⚠ **No formal MDE is claimed: nothing here is scored.** ⚠ **BU-T0b's sizing note binds and is
applied BY MACHINE**: marginal faces resolve less at N = 200, so every contrast publishes
`absDeltaOverHalfWidth` with a `strength` label — **< 1 UNRESOLVED · < 2 ⚠ MARGINAL · ≥ 2
RESOLVED** — and a MARGINAL face is **never** rounded up in prose.

The doors matrix is sized separately: **128 cells × 3 seeds**, plus the born-absent walks
(2 × 64 cells × 3 seeds) — exhaustive in the flags, small in the seeds, because it is a
STRUCTURAL claim (byte-identity and a lifecycle count), not a statistical one.

Estimator: **PAIRED cluster bootstrap over match seeds** (#20), **2,000 resamples**, percentile
95 % CI, **ratio of sums**. ONE resample-index matrix is drawn once and shared by every face and
both arms, so the levels and the contrast are the same resampled worlds and the pairing is inside
the interval. Per-seed cells stored (#282.2(ii)).

## §SEEDS — booked = walked

```text
block            12,489,000 – 12,489,999   (ruling #288.6: next block ≥ 12,489,000)
core battery     12,489,100 – 12,489,299   200 seeds, WALKED BY BOTH ARMS (paired)
smoke            12,489,000 – 12,489,019
guard/override   12,489,020 – 12,489,039
lifecycle/doors  12,489,500 – 12,489,502   3 seeds × (128 door cells + 128 born-absent walks)
world identity   12,489,900
stats stream     base 112,200 (floor from #288.6; step 200 from every published base)
```

## §GATES — the set, frozen ex ante (**19**)

| gate | what it proves |
| --- | --- |
| `gDet` | G-DET: the whole core runs TWICE and re-derives bit-identically. |
| `xSrcUntouched` | ⭐ `src/**` byte-untouched — **WORKTREE vs HEAD** plus no untracked/staged src file (#273.3 as corrected by **#286.1**). |
| `gArms` | ⭐⭐ #283.2(iv): every walked match carries its arm LIVE (world 7, the CB and L3 doors, the L3 dose cell-for-cell, DV SHUT, the two MT doors and both genes at the knee on the view the consumers read, nothing in `info.genome`); the two arms are **different worlds**; the arms walk the SAME seeds. |
| `gDose` | BOTH doses come from committed artifacts: L3's declared SHA; the MT ladder's declared SHA, its own `kneeDose`, the shipped entry's copy of it, the knee's own branch of record, and that the two doors are the entry's own pair. |
| ⭐⭐ `gLifecycle` | **THE M-BU.2 DEBT AT CB+L3+MT**: (a) where an aim can fire, no arming survives its tick / a possession / a phase / the whistle; (b) where armings persist, zero knocks fire; the battery population re-proves it; the seat DID arm; the early-return exposure is REAL; the scope (`o2Look`/`ekHoldVeto` unarmed, eye null); the src call-site census; the matrix is the full power set. |
| ⭐⭐ `gDoors` | the five identity laws hold on EVERY cell and EVERY seed (including MT-T0's born-absent law); all five liveness laws fire — ⭐ **including the slice's own door**. |
| `gNonPerturbing` | THE INSTRUMENTS DO NOT MOVE THE FOOTBALL: the same worlds re-walked with every instrument off end on the identical signature, spell count and pass count. |
| `gOracle` | the engine's evaluator was called and answered; both race verdicts and both corridor verdicts occur; ⭐ the GK split sees BOTH sides; the ±2 m band still traces to `src/**`. |
| ⭐ `gSag` | THE SEAM'S OWN INSTRUMENT IS ALIVE AND SIDED: the marker population is non-empty on both arms, the slack geometry exists on both, ⭐ the base arm adds **exactly zero** metres, ⭐ the slice arm genuinely widens stances, and the seam's own line still traces to `src/**` as the ONE write site. |
| `gQ07` | the direction mix book-keeps against the engine's own counters. |
| `gSpells` | every spell lands in EXACTLY one terminal class; the open-play population is non-empty. |
| `gNonVacuity` | ⭐ NO PUBLISHED RATE HAS A ZERO DENOMINATOR (a zero-denominator cell is published as UNMEASURED, never as 0); the histogram sums to its own denominator. |
| `gFaces` | ⭐⭐ #287.1: every published face re-derives from the **SERIALIZED artifact parsed back off disk**, cell by cell. |
| `gClock` | APPLIED, not nominal: the engine-default clock, the 90 out of `Match.minute()`'s own expression, the mapping derived not typed, every walk stepped. |
| `gSeed` | booked = walked: no clash with the consumed ledger, no internal clash, every walked seed inside the claimed bands. |
| `gStats` | the stats base is the dispatched floor 112,200, ≥200 from every published base; the resample count is the frozen one. |
| `gEnvClean` | WHITELIST-OR-REFUSE: no rogue `BUT1_*`, no engine env door set, an override may never write a canonical path. |
| `gHashEnvelope` | #266.3(a): the body re-derives its digest from disk, a cross-OUT with a DIFFERENT envelope has the IDENTICAL digest, and no invocation fact is inside the hashed body. |
| `gMutants` | ⭐⭐ #268.3(a): every conjunct of every gate owns a mutant, and flipping it (EXACTLY ONE at a time) flips the outcome. An incomplete coverage map REFUSES THE RUN (exit 3). |

## §ENVELOPE — what `resultSha256` covers

`resultSha256` is taken over the RESULT BODY only: the frozen design, the lifecycle proof, the
doors matrix, the run counts, the faces (with their |Δ|÷half-width fields), the sag census, the
histograms, the GK-split ladder, the terminal census, the receipts, the doses, the per-seed cells,
the seeds/stats ledgers, the G-DET digests, the gates and the mutant map. **OUTSIDE it**:
`generatedAt`, `head`, `outPath`, `preflight`, `wallMs` — the whole `envelope` object.

---

## §PRE-REGISTRATION — written before the battery, REPORTED, never gated

**THE FACES, ALL REPORTED (#288.7's list, verbatim in substance):**

1. **TEMPO / RETENTION (the seam's own axis)**: **Q01** open-play spell mean · **Q05**
   touches/spell · ⭐ **keep/hold usage at the seam's own event grain** (the sag census: metres
   added per marker-tick, and the share of marker-ticks the seam actually widens) · **pass
   attempts/match** · **receptions/match**.
2. **CENSUS FACES** (BU-C0 definitions verbatim, commensurable): behind-ball options/reception
   **WITH GK-SPLIT RUNGS** · zero-option share · the #246 2+ band · ⭐⭐ **THE NAMED HYPOTHESIS
   FACE — pressed-reception supply**.
3. **DIRECTION MIX** (Q07 conventions) · **Q06** completion · **TERMINALS** (⚠ L3-veto entangled
   at the LEVEL; contrasts clean by construction — BU-C0 §CORRECTIONS 3) · **goals/match**.

⭐⭐ **THE NAMED HYPOTHESIS, PRE-WRITTEN OF RECORD (#288.3): 持球买身后支援** — *pressed-reception
supply RISES when the carrier holds longer*, banked from BU-T0b at **+2.27 pp, 1.26× half-width,
MARGINAL**. **This slice is its scheduled test.** The verdict is stated **either way**, with its
`|Δ| ÷ half-width`, and a result inside 2× is reported as **MARGINAL** — never rounded up. ⚠ The
honest limit is pre-written too: this stage changes how the DEFENCE behaves, not how long the
carrier holds; a rise here is **consistent with** the hypothesis, not a proof of its mechanism,
and a fall does not falsify the carrier-side story either. **The mechanism question is not
settled by this instrument** and no post-hoc probe will be bolted on to chase it (the freeze
canon's own move, ratified twice — #287.2, #288.5).

**NO PREDICTION IS MADE about the tempo faces' direction.** A tuck-in gives the ball-carrier more
room (fewer chasers) AND takes away the space behind (the block sits deeper); both stories are
football-plausible and the measurement decides.

⭐ **WHAT WOULD MAKE THIS SLICE MATTER TO THE ARC**: options/reception and the pressed-supply
face moving TOWARD the ceiling of record (0.78 → 2.19 is the honest span, #286.4), with
completion and interception improving. ⚠ **WHAT IT CANNOT DO**: treat the lane. The corridor's
lethality is **price-invariant** (#288.3) and this seam does not touch pass physics — if outfield
end-to-end conversion is flat again, that is the FOURTH independent confirmation, and the arc
exits into the play-test with the lane's cure sitting behind the user's doors (B4 pass-weight ·
perception · coached movement).

---

<a id="result"></a>

## §RESULT

*(Filled by the executor AFTER the battery, from the committed artifact. Empty by design at the
freeze commit — #266.3(c).)*

## §DOUBTS

*(Filled with §RESULT.)*
