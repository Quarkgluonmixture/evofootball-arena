# RA-ENTRY-RUNG — `?a4world=12` 传球先问「他赶得到吗」(the receiver-access world)

> Dispatched by **COMMANDER RULING #364 item 3** (on RA-T1B's H-RA.1B = PASS), landed at
> **#365**. The TWELFTH entry of the play-test family (#155 → #337.5); the arc the user's
> own reality question opened (#358: 「这个用vision和现实重新思考下，为什么现实不这样」 →
> the ①′ election → DX-C2 DISCRIMINATES → RA-T0 → RA-T1 FAIL-on-the-old-ruler → the
> numbers-first ruler correction → RA-T1B PASS).
> Pin suite: `tests/raPlaytestEntry.test.ts`. ⛔ Nothing ships to the default world; the
> production fingerprint is unchanged; **the play-test verdict is the USER GATE**
> (watchability has no instrument — the standing law).

## §1 THE BUNDLE (fidelity: the exam's own armed composition, called)

`a4MatchFlags(12) = a4MatchFlags(11) ∪ RA_WORLD_DOORS` — world 11 (the corridor world)
plus **exactly the five doors RA-T1B's ARMED arm carried**, flag for flag:
`dlcDeliveryChoice` (to-feet vs led candidates in one argmax) · `dlcStrikePlane` (the
K = 9 direction × power grid) · `bkGroundCorridor` (striking a body pays) · `dxWindupAim`
(the leg kicks at the elected point) · `raAccessPrice` (⭐ the receiver-access price).
Genes pinned MATCH-LOCAL by `armRaWorld` (the `setCorridorWeight` idiom — the franchise's
`info.genome` is never written): `passLeadSupport = 1` and `raAccessWeight = 1`, the exam
pins; world 11's own `dvExposureWeight = 0.5` is carried through by the spread.
⚠ PRESENTATION, NOT A WORLD-MODEL CLAIM: 1/1 are the rungs the exams walked; what
selection would pick is a later (season-ladder) question.

## §2 THE HONEST BRIEF (what the blurbs carry — pinned by the suite)

* **The gains, measured** (RA-T1B, 495 paired seeds, block 12,532,000–999): unreachable
  led balls 3.905 → 2.820/match (5.68 hw, zero LOO flips) · shots 11.69 → 12.20 (resolved
  UP) · interceptions conceded 27.90 → 27.30 (resolved DOWN) · goals 3.14 → 3.16 (flat) ·
  carried completion 47.7 % → 53.5 % (resolved).
* **⚠ The cost, first**: the world plays FEWER passes — ~2.1 ground passes/match thinner;
  players carry or hold where the only pass on offer was a bad one. Whole-match completion
  actually rose ~0.5 pt. By the old pass-count ruler this world FAILS (RA-T1's banked
  verdict); it passes the coach's ruler the user chose at #363 (create as much, give it
  away no more). Both exams and the ruler change are on the record; the question only the
  user's eyes can answer is exactly the one the old ruler couldn't: 少传两脚球的比赛,
  更好看还是更闷?
* **⚠ The first look**: both exams ran the EMPTY-BOOK form (`armA4World(m, null, 11)` —
  no L3/PC dose). The entry's dosed DEFAULT (matured books, the family convention) is this
  composition's first time on the pitch; `&pcdose=0` is the exam-measured form.

## §3 THE SURFACES

| file | what |
|---|---|
| `src/game/a4World.ts` | `RA_WORLD_VERSION = 12` · `RA_WORLD_DOORS` · `RA_WORLD_LEAD`/`RA_WORLD_WEIGHT` · `setRaGenes` · `armRaWorld` · `raArmedVersion` (containment: 12 ⊃ 11) · URL/sticky accept `12` |
| `src/game/GameApp.ts` | the pc-stack dose predicate + the armed-match guard include world 12 · the feed blurb (both dose forms) · the status line |
| `src/ui/A4WorldBadge.ts` | `A4_BADGE_TEXT_RA`(+`_EMPTY`) · both tables keyed at 12 |
| `src/ui/SettingsScreen.ts` | the checkbox + the long honest blurb |
| narrows (ruling #365) | the `a4World.ts`-absence prohibition pins in `dxWindupAim.test.ts` / `gcGroundCorridor.test.ts` / `raAccessPrice.test.ts` narrowed to "worlds 6–11 carry nothing; world 12 carries it, positively" (the DF-T0 §P7 form); the five `?a4world=12 → null` bound assertions moved to 13 |

## §4 WHAT THE USER'S EYES ARE FOR

提前球像给人的球了吗?带球变多的场面像足球吗?少传两脚球,更好看还是更闷?对比对象是
v11,不是原版。A verdict here gates the entry; the grid composition exam (#357's, now on
meetable candidates) runs behind this gate per #364 item 4.
