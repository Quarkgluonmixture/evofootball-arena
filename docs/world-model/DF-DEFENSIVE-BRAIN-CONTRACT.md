# DF — 防守的大脑 (THE DEFENSIVE-BRAIN CONTRACT: assignment as one continuous, learnable, information-consuming decision)

> Opened by the USER's two 2026-08-19 messages (verbatim in [`DEF-DOCTRINE.md`](DEF-DOCTRINE.md)
> §-1); bound by ruling #319. THE DOCTRINE'S BINDING CLAUSES (§2) GOVERN: press-vs-mark-vs-
> cover-vs-intercept is ONE CONTINUOUS decision surface per defender — priced,
> snapshot-consuming, book-informed — never an enum, never a cap; styles are emergent
> regions of that surface; coordination belongs to the shared-information cluster;
> every exam carries the season ladder; 乱跑 is diagnosed before treatment.

## §0 The diagnosis chain (verified 2026-08-19, #318 item 2)

The Phase-31 cap is live and verbatim (TeamBrain.ts:363-367: "One presser, two for a
pressing side — NEVER three") — the user's own old order, correct when nothing priced a
press, a compensator now that L3's access-time account, the defence books and commitment
physics exist. The team mode is a BINARY threshold (TeamBrain.ts:90, pressScore > 0.62).
assignMarks is hand-written (the #248 "assignMarks→对手簇" debt names it). The ecological
face: goals inflate across seasons (goals-warming.ts, phase-82; BK-T2's CONTROL arm at
2.865 vs the 2.7536 band ceiling) — attack evolves along a rich gene surface while the
defensive coordination layer is part hand-coded, part missing (cover/handoff/line-link
primitives). 区域防守/链式防守长不出来 and 乱跑 are the visible symptoms.

## §1 The claims

* **H-DF.1 (scored, mechanism grain, at the assembled composition)**: with the continuous
  decision surface armed and priced through EXISTING accounts, (a) per-defender decisions
  GENUINELY DIFFERENTIATE (a non-degenerate distribution over the press/mark/cover/
  intercept surface at claim grain, by situation and by body — not one corner, not a
  re-labelled cap) AND (b) **THE SWARM DOES NOT RETURN**: the multi-presser pile-up
  disease the Phase-31 cap was built to stop is MEASURED ABSENT at matched grain (the
  capped world's own band) — only then does the cap retire (the width-floor sequence:
  make it pay, THEN retire the compensator).
* **H-DF.2 (REPORTED, never gated)**: ⭐ THE SEASON LADDER (goals × generation — the
  user's inflation face, the arc's own ruler) · the R-乙 chain faces · the 乱跑 faces
  (assignment churn, chase abandonment) · the STYLE faces (the 范戴克/佩佩 test: does
  the surface's usage differentiate by attrs/books along a reading↔physical axis).

## §2 Mechanism (M-DF)

* **M-DF.1 — ONE SURFACE, EXISTING ACCOUNTS**: the defender's moment-to-moment option
  set (press the carrier · hold my mark · drop to cover · sit on a lane) is priced by
  ONE chooser consuming what is ALREADY BUILT — the L3 access-time account (盯多紧 is
  its shipped continuous law), the defence books (learned threat value), commitment
  physics (what a lunge costs), and the recognition/latency layer; PRIVATE SNAPSHOTS
  when the IN arc arms them (until then the surface reads truth like every other chooser
  — stated, not hidden). NO new pricing tables; derived thresholds only (#200).
* **M-DF.2 — THE CAP RETIRES BY MEASUREMENT, NEVER BY DELETION**: the census (DF-C0)
  freezes the swarm's own face (the pile-up band the cap holds today); the surface slice
  arms dormant beside the cap; the exam proves H-DF.1(b) INSIDE the cap first, then the
  cap-off arm proves the surface alone holds the band. Two compensators may not be
  retired in one slice.
* **M-DF.3 — STYLES EMERGE OR THE SUBSTRATE IS BIASED**: the surface's inputs must let
  a reading-heavy body win by positioning/interception AND a physical body win by
  tightness/duel — both regions PAYABLE (the SQUAD_BUDGET logic: distinct archetypes
  need both routes to pay). The 范戴克/佩佩 axis is REPORTED at the exam, never
  templated.
* **M-DF.4 — SCOPE & DEBTS**: coordination (换人盯 · 补位 · 链式联动 · 造越位) is OUT —
  the shared-information cluster's (six-source/coach; DF-C0 maps its primitive gaps for
  later contracts); no coach channel; no formation redesign. The #248 assignMarks debt
  attaches to this arc. Flags off ⇒ byte-identity per seam; composition proof at the
  world-9 stack; pins from birth; the ecological gate: EVERY DF exam reports the season
  ladder beside match-grain faces.

## §3 Instruments & the arc

* **DF-C0 — THE DEFENSIVE-BRAIN CENSUS (instrument-only, first)**: (a) THE HAND-RULE
  INVENTORY — every hardcoded defensive-coordination rule/cap/threshold (the presser
  cap, the 0.62 mode threshold, assignMarks' scheme, shadow rules, chaser counts),
  needle counts with a stated prefix, TOKENIZER-STRIPPED per the text-census corpus-
  integrity canon; (b) THE 乱跑 DIAGNOSIS — assignment churn at world grain in the
  world-9 base (mark-target switches per defender-minute · chase abandonments · the
  dupRun lineage faces) with stored bins; (c) THE ZONAL/CHAIN PRIMITIVE-GAP ANALYSIS —
  code-facts: what state/actions a line-link, a cover rotation, a handoff would consume
  vs what exists (classed: exists-dormant / exists-live / missing); (d) ⭐ THE SEASON
  LADDER — goals + defensive faces × generation on long leagues, WITH FROZEN-GENOME
  CROSS ARMS (evolved-attack × frozen-defence and the converse — the fork-and-force
  idiom at ecology grain: splitting 攻在进化 from 防没长); (e) THE DECISION-SURFACE
  SIZING — where the per-defender chooser would live, what it consumes, the perf bound.
  The census PICKS the slice design and order.
* **DF-T0.. — the dormant slices** (order set by DF-C0): the surface beside the cap →
  the exam (H-DF.1(a)+(b) inside the cap) → the cap-off arm → the entry rung. **Exit =
  the play-test (USER GATE)**: 防守像在思考吗 · 乱跑消失了吗 · 赛季后期还守得住吗.

## §4 Non-claims

No promise zonal/chain defence emerges in slice 1 (coordination is later cluster work —
the census maps it honestly); no equilibrium promise (the season ladder REPORTS); the cap
is not deleted on faith; nothing ships without the play-test.

## §6 VISION audit (the #91 form)

* vs §-1 (tactics emerge): the surface prices options — who presses, who sits, who cuts
  lanes emerges from accounts/books/genes; 区域/链式 are meant to EMERGE from later
  shared-information primitives, never be written. PASS.
* vs 底座给能力: the arc arms a decision CAPABILITY consuming earned knowledge; content
  stays per-team/per-body. PASS.
* vs #200: no taste constants — prices from shipped accounts; the cap retires by
  measurement. PASS.
* vs the assembly law: L3/books/commitment/PC exist and are composed, not duplicated;
  the census argues row-by-row what is genuinely missing. PASS.

## §7 REALITY audit (the #201 rule)

* Real defending IS one continuous decision (jockey tighter ⇄ show the line ⇄ leave your
  man to press ⇄ drop and cover), informed by what you can see, what you know about the
  threats, and what your commitment costs — the exact quantities M-DF.1 consumes. Real
  defensive styles are real (the user's 范戴克/佩佩); real coordination is coached and
  telepathic (默契) — honestly deferred to the cluster that models shared information.
  PASS.
* Honest limits: 1防2 shepherding quality, offside-trap choreography and line-linking
  need shared snapshots/coach machinery this contract does not build; the census maps
  the gap instead of faking it. PASS.
