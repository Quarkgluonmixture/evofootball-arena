# DF-T3 — THE SURFACE EXAM (H-DF.1(a)+(b) scored on virgin seeds, INSIDE the cap)

> **Ordered by** COMMANDER RULING #331 item 5. **Bound by**
> [`DF-DEFENSIVE-BRAIN-CONTRACT.md`](DF-DEFENSIVE-BRAIN-CONTRACT.md) §1 (H-DF.1 / H-DF.2)
> and §2 — M-DF.1 (ONE surface, EXISTING accounts, derived thresholds only), M-DF.2
> (*"the census freezes the swarm's own face; the surface slice arms dormant beside the cap;
> the exam proves H-DF.1(b) INSIDE the cap first, then the cap-off arm proves the surface
> alone holds the band. Two compensators may not be retired in one slice."*), M-DF.3 (the
> 范戴克/佩佩 axis is **REPORTED** at the exam, never templated) and M-DF.4 (*"the ecological
> gate: EVERY DF exam reports the season ladder beside match-grain faces"*).
> **The seam under examination**: [`DF-T2-DECISION-SURFACE.md`](DF-T2-DECISION-SURFACE.md) —
> `dfSurface`, dormant beside the intact cap, one currency (metres of net access), three
> shipped accounts. **Its frozen bytes STAND**; every correction this stage carries lands
> HERE.
> **Measured mandate**: [`DF-T1-PERSISTENCE-EXAM.md`](DF-T1-PERSISTENCE-EXAM.md) §R5 / ruling
> #324 item 2 — **保住自己的人买回了「阅读」,但光有阅读买不回进球**.
> **Road B**: nothing ships. This stage is **INSTRUMENT-ONLY after commit 1** — `src/**` is
> UNTOUCHED by the exam and the door stays dormant.

---

## §COMMIT 1 — THE RIDERS (#327 §CORR 1/5, behaviour-free; landed BEFORE the freeze)

Result commit: `572f23a`. Three ordered items, one declined.

### §C1.1 ⭐⭐ THE DISAMBIGUATED PIN (#327 §CORR item 1)

DF-T2 §COMMANDER CORRECTIONS item 1 records the gap in its own words: neutralising
`- slackMetres(threat, p)` at `TeamBrain.ts:696` — **the PRESS ELECTION's
best-marking-alternative price** — killed **ZERO** of the 20 pins, and §R10's M4 edit text
matches **BOTH** that line and the priced greedy's copy at `:743` as a substring (same line,
different indentation), so *"ACCOUNT 1 load-bearing"* was **PROVEN AT THE GREEDY ONLY**.

The new pin (`tests/dfSurface.test.ts`, *"THE PRESS ELECTION'S CANDIDATE-SIDE SLACK (:696) —
the DISAMBIGUATED pin"*) squeezes the account's own slack **between the two distances** so
the VACATE decision flips on that one term:

```
priced    bestMarkPrice = dist(d, man) − slack(man, d)  <  dist(d, carrier)   ⇒ he MARKS
unpriced  bestMarkPrice = dist(d, man)                  >  dist(d, carrier)   ⇒ he PRESSES
```

Both inequalities are **ASSERTED in the fixture before the pass**, so the pin cannot pass for
the wrong reason (M5's lesson, ratified at #327 §CORR 6). Every rival for the same man is
outpriced either way, so the greedy's copy is not exercised by this fixture.

**THE MUTANTS, RUN LIVE** on an UNCOMMITTED tree, restored from `/tmp` byte copies
(`cmp`-verified), never by `git checkout`. The mutant text is anchored by a **leading
newline + the exact indentation**, which is what makes the two sites separable at all:

| mutant | edit (verbatim) | site | result |
|---|---|---|---|
| **M4′ THE PRESS ELECTION'S CANDIDATE SIDE** (new) | `\n        const priceM = d - slackMetres(threat, p);` → `\n        const priceM = d - 0 * slackMetres(threat, p);` | `TeamBrain.ts:696`, **8-space** indent | **1 pin dies — the new one** (20 pass) |
| **M4 THE PRICED GREEDY, RE-RECORDED SITE-ANCHORED** | `\n          const priceM = d - slackMetres(threat, p);` → `\n          const priceM = d - 0 * slackMetres(threat, p);` | `TeamBrain.ts:743`, **10-space** indent | **1 pin dies — THE PRICED GREEDY's** (20 pass) |

⭐ **M4 OF RECORD IS NOW SITE-ANCHORED TO `:743`** — this table is the re-record ruling #331
item 5 ordered. DF-T2 §R10's row keeps its asterisk in its own doc (frozen bytes stand); the
correction lives here. **The two sites now die on DIFFERENT single-line mutants**, and
ACCOUNT 1 is proven load-bearing at BOTH.

After the last restore: `cmp` clean, `git diff --stat HEAD -- src` comment-only,
`npm run fingerprint` = `57b0bdab389122af5e4cacd75c4e13020b8ff248a413a7fcd71cc6215ba4c673`
— **the fingerprint of record, UNMOVED**.

### §C1.2 THE DOCBLOCK FIX (#327 §CORR item 5)

`ledger.idle`'s description — *"no option was affordable"* → *"no option was affordable **or
legal**"* — in `src/sim/Match.ts` (the field docblock and the `dfSurfaceLedger` prose) and in
`src/ai/TeamBrain.ts`'s branch comment. The branch also absorbs the **NOT-LEGAL** exclusions
(the Phase-28.4 WG width discipline and the zonal zone gate), which affordability alone never
said. **Comment-only.**

### §C1.3 THE OPTIONAL TIDY IS **NOT** TAKEN — stated, not silent

The dead `d` recompute at `TeamBrain.ts:750-752` (`best`'s `d` field is never read once the
armed branch reassigns `best`) is **LEFT IN PLACE**. Removing it would move the `best` object
literal's shape — a signature the riders' behaviour-free licence does not cover — for zero
measured benefit. Named as a standing tidy for whichever slice next opens this function.

### §C1.4 THE FINGERPRINT, RE-VERIFIED AFTER COMMIT 1

`57b0bdab389122af5e4cacd75c4e13020b8ff248a413a7fcd71cc6215ba4c673` — unmoved, as comments
must be and as the declined tidy guarantees. 21 pins green in `dfSurface.test.ts` (20 + the
new one), 54 tests green across `dfSurface` + `dfAssignPersist` + `markSagGene`.

---

## §PRE-REGISTRATION (frozen before the battery — THIS commit is the freeze commit)

### §P0 What this stage is, and is NOT

An **EXAM**. DF-T2 published arming RECEIPTS and made no football claim (canon:
*receipts ≠ effect sizes*, homes ruling #289 item 1 + BU-T1 §CORR item 5). This stage scores
**two pre-registered hypotheses on virgin seeds with frozen CI rules**, and reports
everything else.

⛔ **NAMED OUT, EXPLICITLY**: **THE CAP-OFF ARM**. M-DF.2's own order — *"the exam proves
H-DF.1(b) INSIDE the cap FIRST, then the cap-off arm proves the surface alone holds the
band"* — restated at dispatch (#331 item 5: *"the cap-off arm EXPLICITLY OUT … a later
stage"*). Nothing here licenses retiring the Phase-31 cap.

⛔ **ALSO OUT**: the standing lane-sit and 「drop to cover」 (DF-T2 §R11 items 2–3: no action
primitive, no cover-fact producer — M-DF.4's later cluster); the shared-information
coordination cluster; any nudge answering a ladder deviation (#320 item 3's frozen direction:
**deviations ROUTE TO SLICES, never to nudges**).

### §P1 ⭐⭐ THE SCORED CLAIMS (frozen before the battery, NEVER re-cut after sight)

> **H-DF.1** (contract §1, VERBATIM): *"with the continuous decision surface armed and priced
> through EXISTING accounts, (a) per-defender decisions GENUINELY DIFFERENTIATE (a
> non-degenerate distribution over the press/mark/cover/intercept surface at claim grain, by
> situation and by body — not one corner, not a re-labelled cap) AND (b) **THE SWARM DOES NOT
> RETURN**: the multi-presser pile-up disease the Phase-31 cap was built to stop is MEASURED
> ABSENT at matched grain (the capped world's own band)."*

⭐ **THE MECHANISM H-DF.1(a) IS PHRASED ON** (#327 §CORR item 5, binding): the surface of
record is **TWO PRICED ELECTIONS + ONE DERIVED LABEL** — *press-vs-mark* and
*hold-vs-switch* are **CHOSEN**; *jump-vs-take* is the L3 account's own **sign LABELLING** the
outcome (`actionExecutor.ts`'s `slack <= 0` branch). The conjuncts below therefore score the
**PRESS ELECTION** (a chosen thing) by situation, and the tercile gradient of press **and**
take by body — never "four choosable acts".

| conjunct | the rule (FROZEN) | kind |
|---|---|---|
| **(a1) BY SITUATION** | the **press ELECTION share** resolves apart across team modes: `byModeOptionShareDefend[press]` vs `byModeOptionSharePress[press]`, each with its own seed-clustered bootstrap interval, **DISJOINT** | CI (unpaired, within-arm) |
| **(a2) BY BODY** | the `attrs.defending` **TERCILE gradient** resolves for **PRESS** *and* for **TAKE**: top vs bottom tercile intervals **DISJOINT** *and* the three point estimates **STRICTLY MONOTONE** in tercile index, for BOTH options | CI (unpaired, within-arm) |
| **(b1) THE CAP'S BAND HOLDS** | the four-chaser bin is **EXACTLY ZERO in BOTH arms** | structural |
| **(b2) THE SWARM DOES NOT RETURN** | `multiChaseShare2` **AND** `multiChaseShare3` do **NOT RISE** resolvedly armed | CI (paired, non-inferiority) |
| **(b3) THE CAP IS UNMOVED** | the `assignChasers` slice is **sha-identical** to DF-T2 §R6's sha of record `5b4a21d036e9d97027a360f166621d747374ec5c42ead20b9ed132919872703c`, hashed **before AND after** the battery | git/bytes |

**H-DF.1(a) passes iff (a1) AND (a2). H-DF.1(b) passes iff (b1) AND (b2) AND (b3).** The
verdict names which limb failed if either does.

⚠⚠ **THE INHERITED CAUTION, STATED AS LOUDLY AS THE PASS** (DF-T2 §R11 item 1, ratified
#327 §CORR item 2): at the receipts' grain **407 of 410 bodies were HOLD-MODAL** and not one
body was modal on press or jump. That census is **RE-RUN here at exam grain and published
beside the verdict**. It is **NOT a conjunct**: if (a1)/(a2) pass while the body-modal
degeneracy persists, **BOTH are reported** — the verdict is the conjuncts', the caution is
the reader's.

⚠ **WHY (b2)'s TWO FACES**: `multiChaseShare3` is the face #324 left **grazing zero upward**
at −1.21 × 10⁻⁵ and DF-T2's receipt left **UNRESOLVED** on a different estimand. #331 item 5
names both shares. Neither was re-cut here.

### §P2 ⭐ THE FROZEN CI RULES (pre-registered; NEVER re-cut after sight)

* **PER-SEED CELLS** are stored so every headline re-derives (canon, home ruling #282.2(ii)).
* **BETWEEN-ARM faces** (same seeds walked twice, the door the only difference) use the
  **PAIRED DELTA** (armed − shut) with a **SEED-CLUSTERED PAIRED bootstrap**: resample the
  walked seeds with replacement; in **each** draw compute both arms' ratios over the **SAME**
  resampled seed set, then the delta. A face **MOVES RESOLVEDLY** iff its delta CI **EXCLUDES
  ZERO**; the direction is the sign of the interval. (b2) is a **non-inferiority** read: each
  face fails only on a **RESOLVED move UP**.
* **WITHIN-ARM contrasts** ((a1)'s two modes, (a2)'s terciles) are **UNPAIRED**: each side
  gets its own seed-clustered bootstrap interval and the frozen test is **INTERVAL OVERLAP** —
  **DISJOINT = resolved apart**. (This is the dispatch's *"interval overlap for unpaired
  faces"*.) In every draw the tercile CUTS are recomputed from the resampled bodies, so the
  cut is inside the bootstrap, not outside it.
* **2,000 resamples** everywhere; **95 % percentile** intervals; each bootstrap's rng is
  seeded from its **own published STATS BASE** (block-base discipline).
* Canon VERBATIM: *"a starred finding states its |Δ|÷half-width ratio"* (home
  BU-T0B-PRICE-SEPARATION.md §COMMANDER CORRECTIONS item 2) — every between-arm face carries
  `ratioToHalfWidth`, and (a1) publishes the gap over the larger half-width.
* Canon (paraphrase): **moving denominators disclosed per face** (home PW-C0 §CORR item 2) —
  every face publishes its own `denNote`.
* Canon (paraphrase): **clock honesty** — every rate on the 240 s match clock or dual-axis;
  APPLIED values, never nominal.
* Canon VERBATIM: *"a max−min face reports a noise-floor comparison, not a zero-null CI"*
  (home PC-T1-LEARNING-EXAM.md §COMMANDER CORRECTIONS item 3) — **no max−min face is
  published by this stage**.

### §P3 The arms, and the world

* **shut** = the **world-9 stack** (`a4MatchFlags(9)` + `bkFacingLaw` + `bkContactLaw` +
  `armA4World` with the matured L3/PC doses) **+ `dfAssignPersist`** — DF-T1's **BANKED
  world**, the matched floor.
* **armed** = the same **+ `dfSurface`**. That is the ONLY difference; `worldOk` is asserted
  per walk with `persistenceArmedBothArms` and `surfaceDoorMatchesArm` as conjuncts.
* ⭐ **THE SEASON LADDER runs the SHIPPED world**, not world-9 — canon VERBATIM:
  *"WORKER-SIMMED fixtures play the SHIPPED world (League.toJSON omits matchFlags; true since
  #155, stated now, test-pinned; refines #270's E4 correction; matches the perf diagnostic)"*
  (home: ruling #283.2(iv)). The doors are armed through `League.matchFlags`, the League's own
  probe surface, which the **shipped `createMatch` spread** carries into every fixture —
  nothing is hand-written onto `info.genome` (dose-placement canon, home ruling #270.2).

### §P4 REPORTED, never gated

* ⭐⭐ **THE SEASON LADDER** — see §P5.
* ⭐ **INTERCEPTIONS / TACKLES AT LADDER GRAIN** — **DF-C0 §R4's ESTIMAND** (evolved league
  play across generations, per team-match). ⚠ DF-T2 §R11 item 6's warning binds and is
  repeated in the artifact: the friendly-match `team.stats` numbers are a **DIFFERENT
  NUMBER** and the two must never be quoted as the same thing. Both are published, each
  labelled with its own estimand.
* ⭐ **THE PRESS REALISATION RATE** (#327 §CORR item 3's ordered face) — the election-vs-act
  honest denominator. The ledger's press counter increments on the **ABSENCE of an
  assignment**; whether Phase-29.1's contain branch (its own goal-side test and ONE-container
  rule, untouched) **licenses** the body was never measured. Two faces:
  - `containActShareOfOfferTicks` (**both arms**, tick grain): of the defender-ticks the
    contain branch's own geometry **OFFERS** — no mark, not a chaser, inside the anchored
    8 m of the carrier, carrier inside the anchored 35 m of our goal, goal-side — the share
    where the brain's **realised argmax IS the contain candidate** (read off `p.action`:
    type, `targetIdx`, and the winning candidate's own `why`);
  - `episodesPerPressElection` (armed arm): realised contain **EPISODES** (maximal contiguous
    runs) per press **ELECTION**. ⚠ **A CONVERSION INDEX, NOT A PROBABILITY** — numerator and
    denominator are counted at different cadences and it is not bounded by 1; the honest
    denominator face above is published beside it, in both arms.
  ⚠ **HONEST LIMIT**: the instrument cannot see **which** body vacated (the ledger carries
  counts, not identities, and src is untouched), so the join is at **team/walk grain, never
  body-to-body**. The shut arm's contain episodes — which exist with **no election at all**
  (the shipped spare-body state) — are published as the background rate the index sits on.
* the **churn / coverage family** at DF-T0 grain, with **STORED latency bins** (canon
  VERBATIM: *"the re-derivation gate covers EVERY published face; a percentile face requires
  stored bins"*, home PC-C0-REACTION-BASELINE.md §COMMANDER CORRECTIONS item 4).
* the **swarm companions** (`swarmStanceShare2`, `swarmZoneShare3`) with stored bins.
* the **R-乙 chain faces** — Q01 spell seconds · Q05 touches/spell · Q06 completion · Q14
  pressed share · ⭐ **Q07 forward share** (the **#324 labelled hypothesis**, re-measured
  against the SURFACE door), definitions reused **VERBATIM** and cited to
  [`R-YI-STANDING-GAP-TABLE.md`](R-YI-STANDING-GAP-TABLE.md) §definitions (ported through
  [`BK-T2-COMPOSITION-EXAM.md`](BK-T2-COMPOSITION-EXAM.md) §(d)'s spell walker; #324 §CORR 1's
  restored loose-ball clause carried in the artifact's quote).
* **goals + the §2 equilibrium faces** at match grain, both arms, with CIs. **REPORT ONLY: no
  band is a gate here**, because nothing ships from an exam (contract §4: *"no equilibrium
  promise"*).
* the **usage distribution** (by option, by mode, by body) and the **body-modal census** —
  the caution of §P1.

### §P5 ⭐⭐ THE SEASON LADDER (the ecological gate, M-DF.4), and the floor

DF-T1's design reused: **2 arms** (`liveShut` = the live world + `dfAssignPersist`, door shut;
`liveArmed` = the same + `dfSurface`) × **4 paired league seeds** × **20 generations**, every
generation measured — ≈ 160 league-seasons. Early/late windows are DF-C0's: **1–5 vs 16–20,
disjoint**. The slope point estimate goes through **DF-C0-FIX §RF1's ONE FORMULA**,
`slopeDeltaThroughOneFormula = mean(per-league (late − early))`, called by BOTH the publish
side and the on-disk re-derivation so the two cannot drift.

**THE FLOOR**: DF-C0 §R4's `atkFrozen` goals slope **+0.2211** (half-width 0.1423, |Δ|÷hw
1.55) — the ecology's inflation with the ATTACK half frozen. **QUOTED, never re-run.**

⚠ **The floor is a REFERENCE LINE, not a matched control** — a different counterfactual
(frozen attack genes vs an armed defensive door) on different league seeds (DF-C0's
12,508,900–903 vs this stage's 12,515,900–903). REPORTED, never gated.

⚠ **No between-arm SLOPE test is pre-registered and none will be invented after sight**: each
arm carries its own league-clustered interval and the comparison is read as **overlap** —
DF-T1 §R8 item 2's discipline, inherited verbatim.

⭐ **DIRECTION PRE-REGISTERED** (#320 item 3's frozen direction, restated at dispatch): the
ladder is **REPORTED**, and a deviation **ROUTES TO A SLICE, never to a nudge**. DF-T1
measured the persistence door **widening** the excess over the floor (+1.513 armed vs +1.256
shut, inside overlapping intervals) while the **reading half moved the predicted way**. This
exam asks the same two questions of the SURFACE door.

### §P6 Gates (frozen; a RED gate stays red and is reported)

`gWorldOkEveryWalk` · `gSeedsBookedEqualWalked` · `gArmsPairedPerSeed` ·
`gAnchorsResolveOnce` · ⭐ `gSrcUntouched` (porcelain AND `diff --stat HEAD` over `src` — the
exam is instrument-only) · ⭐ `gCapIntactBothArms` · `gCapBinsNonEmpty` ·
⭐ `gCapSliceShaIdentical` (before AND after the battery) · `gLatencyBinsStored` ·
`gSwarmBinsStored` · `gLedgerZeroWhenShut` (dormancy measured in-battery) ·
⭐ `gEveryOptionUsed` (**NON-DEGENERACY LIVENESS** — a one-corner surface goes RED and is
reported as such) · `gUsageCellsStored` · ⭐ `gContainInstrumentAlive` (a zero OFFER
denominator is a silently dead instrument, not a pass) · `gArmsDistinguishable` ·
`gRyiInstrumentAlive` · `gLadderComplete` · `gLadderDoorHeld` (the doors read back off
**every** created match) · `gLadderGen1Identical` · `gSeedDiscipline` · `gStatsDisjoint` ·
`gFingerprintUnmoved` · ⭐ `gFacesFromDisk` (canon, home ruling #287 item 1: the body is
**STAGED to disk, re-parsed, and every published face re-derived** — every between-arm face's
shut/armed/delta, latency percentiles from stored bins, chaser/swarm bins, the usage block,
the tercile table, (a1)/(a2)'s point estimates and booleans, the press-realisation join,
every ladder face × arm × generation, every slope through the ONE FORMULA, the floor read's
arithmetic, the body-modal census, and **all five H-DF.1 conjunct booleans + the verdict**).

⭐ **THE BODY IS HASHED LAST**, after every gate is written including `gFacesFromDisk`
(DF-C0 §CORR item 2, ruling #321). A RED run writes `…RED.json`; the canonical path is only
reached all-green.

### §P7 Seeds and stats (pre-registered — BOOKED = WALKED, the block consumed whole)

* **Block 12,515,000–999**, opened by #331 item 5, **CONSUMED WHOLE**. Sub-ranges:
  `…000–…039` the exam battery (40 paired seeds) · `…800–…802` the in-band smoke prefix ·
  `…900–…903` the ladder's four league seeds (booked once, walked in both arms) · `…999` the
  xxx,999 world-construction receipt seed (**WALKED**, so 41 seeds × 2 arms = **82 walks**).
* **Stats base 115,200, step 200.** ⭐ **THE REGISTRY OF RECORD ENTERING THIS STAGE IS 61**,
  not 59: IN-C0's completed 56 + 114,200 (IN-C0/IN-C0-FIX, #317 item 4) + 114,400 and 114,600
  (DF-C0, #320 item 4) = the "59" the rulings quote, **plus 114,800 and 115,000, which DF-T1
  ITSELF consumed** (DF-T1 §R7). The running "registry 59" in #329/#331 is the count as of
  DF-T1's **freeze**; DF-T1's two draws are appended here. Stated, not hidden (§R-DEV).
* **THREE draws ⇒ THREE bases**: **115,200** (the paired match-battery bootstrap) ·
  **115,400** (the ladder's league-clustered slope bootstrap) · **115,600** (the WITHIN-ARM
  bootstrap that carries H-DF.1(a1) and (a2)). Next base ≥ **115,800**; next sim block
  ≥ **12,516,000**.
* **Override discipline**: a smoke / N / GENS / OUT run may NOT write the canonical artifact
  path (the probe refuses, exit 2).

### §P8 The instrument

`scripts/probes/df-t3-surface-exam.ts`, frozen in **this** commit **BEFORE** the battery
(canon: **freeze-before-battery**, home ruling #266.3(c)); the artifact records its `sha256`.
The hashed body is built from an explicit **ALLOWLIST SCHEMA** — canon VERBATIM: *"the hashed
body is built from an explicit ALLOWLIST SCHEMA — a field not in the schema never enters the
body; forbidden-name lists are retired"* (home PC-T0 §CORR item 1). Env surface is
**whitelist-or-refuse**: `DFT3_MODE` (required) · `DFT3_N` · `DFT3_GENS` · `DFT3_OUT`; any
other `DFT3_*` var and any engine door is a fatal refusal.

**THE SIX ANCHORED EXTRACTIONS** (values REPORTED by the artifact with line receipts, never
asserted in prose): `markStanceBand` · `zonalEngageRadius9` · `markRange22` ·
`touchControlDist` · and DF-T2's two — `containRadius8` and `containTerritory35`, both from
the shipped Phase-29.1 contain branch's ONE line, which is also the press-realisation
walker's own geometry. Canon VERBATIM: *"a src-extracted constant pins its extraction to the
NAMED call site — anchored match + line receipt — never first-occurrence"* (home BK-C0 §CORR
item 1). The contain candidate's `why` line is pinned to **exactly one occurrence** as the
realisation needle, and a non-singular needle **bits the construction class** before any
battery runs.

---

## §RESULTS

> **Written at commit 3.** The battery runs at THIS commit's tree.
