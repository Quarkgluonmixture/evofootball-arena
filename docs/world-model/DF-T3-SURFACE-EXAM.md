# DF-T3 — THE SURFACE EXAM (H-DF.1(a)+(b) scored on virgin seeds, INSIDE the cap)

> **Ordered by** COMMANDER RULING #331 item 5. **Bound by**
> [`DF-DEFENSIVE-BRAIN-CONTRACT.md`](DF-DEFENSIVE-BRAIN-CONTRACT.md) §1 (H-DF.1 / H-DF.2)
> and §2 — M-DF.1 (ONE surface, EXISTING accounts, derived thresholds only), M-DF.2
> (*"the census freezes the swarm's own face; the surface slice arms dormant beside the cap;
> the exam proves H-DF.1(b) INSIDE the cap first, then the cap-off arm proves the surface
> alone holds the band. Two compensators may not be retired in one slice."*), M-DF.3 (the
> 范戴克/佩佩 axis is **REPORTED** at the exam, never templated) and M-DF.4 (*"the ecological
> gate: EVERY DF exam reports the season ladder beside match-grain faces"*).
> **The seam under examination**: [`DF-T2-DECISION-SURFACE.md`](DF-T2-DECISION-SURFACE.md) —
> `dfSurface`, dormant beside the intact cap, one currency (metres of net access), three
> shipped accounts. **Its frozen bytes STAND**; every correction this stage carries lands
> HERE.
> **Measured mandate**: [`DF-T1-PERSISTENCE-EXAM.md`](DF-T1-PERSISTENCE-EXAM.md) §R5 / ruling
> #324 item 2 — **保住自己的人买回了「阅读」,但光有阅读买不回进球**.
> **Road B**: nothing ships. This stage is **INSTRUMENT-ONLY after commit 1** — `src/**` is
> UNTOUCHED by the exam and the door stays dormant.

---

## §COMMIT 1 — THE RIDERS (#327 §CORR 1/5, behaviour-free; landed BEFORE the freeze)

Result commit: `572f23a`. Three ordered items, one declined.

### §C1.1 ⭐⭐ THE DISAMBIGUATED PIN (#327 §CORR item 1)

DF-T2 §COMMANDER CORRECTIONS item 1 records the gap in its own words: neutralising
`- slackMetres(threat, p)` at `TeamBrain.ts:696` — **the PRESS ELECTION's
best-marking-alternative price** — killed **ZERO** of the 20 pins, and §R10's M4 edit text
matches **BOTH** that line and the priced greedy's copy at `:743` as a substring (same line,
different indentation), so *"ACCOUNT 1 load-bearing"* was **PROVEN AT THE GREEDY ONLY**.

The new pin (`tests/dfSurface.test.ts`, *"THE PRESS ELECTION'S CANDIDATE-SIDE SLACK (:696) —
the DISAMBIGUATED pin"*) squeezes the account's own slack **between the two distances** so
the VACATE decision flips on that one term:

```
priced    bestMarkPrice = dist(d, man) − slack(man, d)  <  dist(d, carrier)   ⇒ he MARKS
unpriced  bestMarkPrice = dist(d, man)                  >  dist(d, carrier)   ⇒ he PRESSES
```

Both inequalities are **ASSERTED in the fixture before the pass**, so the pin cannot pass for
the wrong reason (M5's lesson, ratified at #327 §CORR 6). Every rival for the same man is
outpriced either way, so the greedy's copy is not exercised by this fixture.

**THE MUTANTS, RUN LIVE** on an UNCOMMITTED tree, restored from `/tmp` byte copies
(`cmp`-verified), never by `git checkout`. The mutant text is anchored by a **leading
newline + the exact indentation**, which is what makes the two sites separable at all:

| mutant | edit (verbatim) | site | result |
|---|---|---|---|
| **M4′ THE PRESS ELECTION'S CANDIDATE SIDE** (new) | `\n        const priceM = d - slackMetres(threat, p);` → `\n        const priceM = d - 0 * slackMetres(threat, p);` | `TeamBrain.ts:696`, **8-space** indent | **1 pin dies — the new one** (20 pass) |
| **M4 THE PRICED GREEDY, RE-RECORDED SITE-ANCHORED** | `\n          const priceM = d - slackMetres(threat, p);` → `\n          const priceM = d - 0 * slackMetres(threat, p);` | `TeamBrain.ts:743`, **10-space** indent | **1 pin dies — THE PRICED GREEDY's** (20 pass) |

⭐ **M4 OF RECORD IS NOW SITE-ANCHORED TO `:743`** — this table is the re-record ruling #331
item 5 ordered. DF-T2 §R10's row keeps its asterisk in its own doc (frozen bytes stand); the
correction lives here. **The two sites now die on DIFFERENT single-line mutants**, and
ACCOUNT 1 is proven load-bearing at BOTH.

After the last restore: `cmp` clean, `git diff --stat HEAD -- src` comment-only,
`npm run fingerprint` = `57b0bdab389122af5e4cacd75c4e13020b8ff248a413a7fcd71cc6215ba4c673`
— **the fingerprint of record, UNMOVED**.

### §C1.2 THE DOCBLOCK FIX (#327 §CORR item 5)

`ledger.idle`'s description — *"no option was affordable"* → *"no option was affordable **or
legal**"* — in `src/sim/Match.ts` (the field docblock and the `dfSurfaceLedger` prose) and in
`src/ai/TeamBrain.ts`'s branch comment. The branch also absorbs the **NOT-LEGAL** exclusions
(the Phase-28.4 WG width discipline and the zonal zone gate), which affordability alone never
said. **Comment-only.**

### §C1.3 THE OPTIONAL TIDY IS **NOT** TAKEN — stated, not silent

The dead `d` recompute at `TeamBrain.ts:750-752` (`best`'s `d` field is never read once the
armed branch reassigns `best`) is **LEFT IN PLACE**. Removing it would move the `best` object
literal's shape — a signature the riders' behaviour-free licence does not cover — for zero
measured benefit. Named as a standing tidy for whichever slice next opens this function.

### §C1.4 THE FINGERPRINT, RE-VERIFIED AFTER COMMIT 1

`57b0bdab389122af5e4cacd75c4e13020b8ff248a413a7fcd71cc6215ba4c673` — unmoved, as comments
must be and as the declined tidy guarantees. 21 pins green in `dfSurface.test.ts` (20 + the
new one), 54 tests green across `dfSurface` + `dfAssignPersist` + `markSagGene`.

---

## §PRE-REGISTRATION (frozen before the battery — THIS commit is the freeze commit)

### §P0 What this stage is, and is NOT

An **EXAM**. DF-T2 published arming RECEIPTS and made no football claim (canon:
*receipts ≠ effect sizes*, homes ruling #289 item 1 + BU-T1 §CORR item 5). This stage scores
**two pre-registered hypotheses on virgin seeds with frozen CI rules**, and reports
everything else.

⛔ **NAMED OUT, EXPLICITLY**: **THE CAP-OFF ARM**. M-DF.2's own order — *"the exam proves
H-DF.1(b) INSIDE the cap FIRST, then the cap-off arm proves the surface alone holds the
band"* — restated at dispatch (#331 item 5: *"the cap-off arm EXPLICITLY OUT … a later
stage"*). Nothing here licenses retiring the Phase-31 cap.

⛔ **ALSO OUT**: the standing lane-sit and 「drop to cover」 (DF-T2 §R11 items 2–3: no action
primitive, no cover-fact producer — M-DF.4's later cluster); the shared-information
coordination cluster; any nudge answering a ladder deviation (#320 item 3's frozen direction:
**deviations ROUTE TO SLICES, never to nudges**).

### §P1 ⭐⭐ THE SCORED CLAIMS (frozen before the battery, NEVER re-cut after sight)

> **H-DF.1** (contract §1, VERBATIM): *"with the continuous decision surface armed and priced
> through EXISTING accounts, (a) per-defender decisions GENUINELY DIFFERENTIATE (a
> non-degenerate distribution over the press/mark/cover/intercept surface at claim grain, by
> situation and by body — not one corner, not a re-labelled cap) AND (b) **THE SWARM DOES NOT
> RETURN**: the multi-presser pile-up disease the Phase-31 cap was built to stop is MEASURED
> ABSENT at matched grain (the capped world's own band)."*

⭐ **THE MECHANISM H-DF.1(a) IS PHRASED ON** (#327 §CORR item 5, binding): the surface of
record is **TWO PRICED ELECTIONS + ONE DERIVED LABEL** — *press-vs-mark* and
*hold-vs-switch* are **CHOSEN**; *jump-vs-take* is the L3 account's own **sign LABELLING** the
outcome (`actionExecutor.ts`'s `slack <= 0` branch). The conjuncts below therefore score the
**PRESS ELECTION** (a chosen thing) by situation, and the tercile gradient of press **and**
take by body — never "four choosable acts".

| conjunct | the rule (FROZEN) | kind |
|---|---|---|
| **(a1) BY SITUATION** | the **press ELECTION share** resolves apart across team modes: `byModeOptionShareDefend[press]` vs `byModeOptionSharePress[press]`, each with its own seed-clustered bootstrap interval, **DISJOINT** | CI (unpaired, within-arm) |
| **(a2) BY BODY** | the `attrs.defending` **TERCILE gradient** resolves for **PRESS** *and* for **TAKE**: top vs bottom tercile intervals **DISJOINT** *and* the three point estimates **STRICTLY MONOTONE** in tercile index, for BOTH options | CI (unpaired, within-arm) |
| **(b1) THE CAP'S BAND HOLDS** | the four-chaser bin is **EXACTLY ZERO in BOTH arms** | structural |
| **(b2) THE SWARM DOES NOT RETURN** | `multiChaseShare2` **AND** `multiChaseShare3` do **NOT RISE** resolvedly armed | CI (paired, non-inferiority) |
| **(b3) THE CAP IS UNMOVED** | the `assignChasers` slice is **sha-identical** to DF-T2 §R6's sha of record `5b4a21d036e9d97027a360f166621d747374ec5c42ead20b9ed132919872703c`, hashed **before AND after** the battery | git/bytes |

**H-DF.1(a) passes iff (a1) AND (a2). H-DF.1(b) passes iff (b1) AND (b2) AND (b3).** The
verdict names which limb failed if either does.

⚠⚠ **THE INHERITED CAUTION, STATED AS LOUDLY AS THE PASS** (DF-T2 §R11 item 1, ratified
#327 §CORR item 2): at the receipts' grain **407 of 410 bodies were HOLD-MODAL** and not one
body was modal on press or jump. That census is **RE-RUN here at exam grain and published
beside the verdict**. It is **NOT a conjunct**: if (a1)/(a2) pass while the body-modal
degeneracy persists, **BOTH are reported** — the verdict is the conjuncts', the caution is
the reader's.

⚠ **WHY (b2)'s TWO FACES**: `multiChaseShare3` is the face #324 left **grazing zero upward**
at −1.21 × 10⁻⁵ and DF-T2's receipt left **UNRESOLVED** on a different estimand. #331 item 5
names both shares. Neither was re-cut here.

### §P2 ⭐ THE FROZEN CI RULES (pre-registered; NEVER re-cut after sight)

* **PER-SEED CELLS** are stored so every headline re-derives (canon, home ruling #282.2(ii)).
* **BETWEEN-ARM faces** (same seeds walked twice, the door the only difference) use the
  **PAIRED DELTA** (armed − shut) with a **SEED-CLUSTERED PAIRED bootstrap**: resample the
  walked seeds with replacement; in **each** draw compute both arms' ratios over the **SAME**
  resampled seed set, then the delta. A face **MOVES RESOLVEDLY** iff its delta CI **EXCLUDES
  ZERO**; the direction is the sign of the interval. (b2) is a **non-inferiority** read: each
  face fails only on a **RESOLVED move UP**.
* **WITHIN-ARM contrasts** ((a1)'s two modes, (a2)'s terciles) are **UNPAIRED**: each side
  gets its own seed-clustered bootstrap interval and the frozen test is **INTERVAL OVERLAP** —
  **DISJOINT = resolved apart**. (This is the dispatch's *"interval overlap for unpaired
  faces"*.) In every draw the tercile CUTS are recomputed from the resampled bodies, so the
  cut is inside the bootstrap, not outside it.
* **2,000 resamples** everywhere; **95 % percentile** intervals; each bootstrap's rng is
  seeded from its **own published STATS BASE** (block-base discipline).
* Canon VERBATIM: *"a starred finding states its |Δ|÷half-width ratio"* (home
  BU-T0B-PRICE-SEPARATION.md §COMMANDER CORRECTIONS item 2) — every between-arm face carries
  `ratioToHalfWidth`, and (a1) publishes the gap over the larger half-width.
* Canon (paraphrase): **moving denominators disclosed per face** (home PW-C0 §CORR item 2) —
  every face publishes its own `denNote`.
* Canon (paraphrase): **clock honesty** — every rate on the 240 s match clock or dual-axis;
  APPLIED values, never nominal.
* Canon VERBATIM: *"a max−min face reports a noise-floor comparison, not a zero-null CI"*
  (home PC-T1-LEARNING-EXAM.md §COMMANDER CORRECTIONS item 3) — **no max−min face is
  published by this stage**.

### §P3 The arms, and the world

* **shut** = the **world-9 stack** (`a4MatchFlags(9)` + `bkFacingLaw` + `bkContactLaw` +
  `armA4World` with the matured L3/PC doses) **+ `dfAssignPersist`** — DF-T1's **BANKED
  world**, the matched floor.
* **armed** = the same **+ `dfSurface`**. That is the ONLY difference; `worldOk` is asserted
  per walk with `persistenceArmedBothArms` and `surfaceDoorMatchesArm` as conjuncts.
* ⭐ **THE SEASON LADDER runs the SHIPPED world**, not world-9 — canon VERBATIM:
  *"WORKER-SIMMED fixtures play the SHIPPED world (League.toJSON omits matchFlags; true since
  #155, stated now, test-pinned; refines #270's E4 correction; matches the perf diagnostic)"*
  (home: ruling #283.2(iv)). The doors are armed through `League.matchFlags`, the League's own
  probe surface, which the **shipped `createMatch` spread** carries into every fixture —
  nothing is hand-written onto `info.genome` (dose-placement canon, home ruling #270.2).

### §P4 REPORTED, never gated

* ⭐⭐ **THE SEASON LADDER** — see §P5.
* ⭐ **INTERCEPTIONS / TACKLES AT LADDER GRAIN** — **DF-C0 §R4's ESTIMAND** (evolved league
  play across generations, per team-match). ⚠ DF-T2 §R11 item 6's warning binds and is
  repeated in the artifact: the friendly-match `team.stats` numbers are a **DIFFERENT
  NUMBER** and the two must never be quoted as the same thing. Both are published, each
  labelled with its own estimand.
* ⭐ **THE PRESS REALISATION RATE** (#327 §CORR item 3's ordered face) — the election-vs-act
  honest denominator. The ledger's press counter increments on the **ABSENCE of an
  assignment**; whether Phase-29.1's contain branch (its own goal-side test and ONE-container
  rule, untouched) **licenses** the body was never measured. Two faces:
  - `containActShareOfOfferTicks` (**both arms**, tick grain): of the defender-ticks the
    contain branch's own geometry **OFFERS** — no mark, not a chaser, inside the anchored
    8 m of the carrier, carrier inside the anchored 35 m of our goal, goal-side — the share
    where the brain's **realised argmax IS the contain candidate** (read off `p.action`:
    type, `targetIdx`, and the winning candidate's own `why`);
  - `episodesPerPressElection` (armed arm): realised contain **EPISODES** (maximal contiguous
    runs) per press **ELECTION**. ⚠ **A CONVERSION INDEX, NOT A PROBABILITY** — numerator and
    denominator are counted at different cadences and it is not bounded by 1; the honest
    denominator face above is published beside it, in both arms.
  ⚠ **HONEST LIMIT**: the instrument cannot see **which** body vacated (the ledger carries
  counts, not identities, and src is untouched), so the join is at **team/walk grain, never
  body-to-body**. The shut arm's contain episodes — which exist with **no election at all**
  (the shipped spare-body state) — are published as the background rate the index sits on.
* the **churn / coverage family** at DF-T0 grain, with **STORED latency bins** (canon
  VERBATIM: *"the re-derivation gate covers EVERY published face; a percentile face requires
  stored bins"*, home PC-C0-REACTION-BASELINE.md §COMMANDER CORRECTIONS item 4).
* the **swarm companions** (`swarmStanceShare2`, `swarmZoneShare3`) with stored bins.
* the **R-乙 chain faces** — Q01 spell seconds · Q05 touches/spell · Q06 completion · Q14
  pressed share · ⭐ **Q07 forward share** (the **#324 labelled hypothesis**, re-measured
  against the SURFACE door), definitions reused **VERBATIM** and cited to
  [`R-YI-STANDING-GAP-TABLE.md`](R-YI-STANDING-GAP-TABLE.md) §definitions (ported through
  [`BK-T2-COMPOSITION-EXAM.md`](BK-T2-COMPOSITION-EXAM.md) §(d)'s spell walker; #324 §CORR 1's
  restored loose-ball clause carried in the artifact's quote).
* **goals + the §2 equilibrium faces** at match grain, both arms, with CIs. **REPORT ONLY: no
  band is a gate here**, because nothing ships from an exam (contract §4: *"no equilibrium
  promise"*).
* the **usage distribution** (by option, by mode, by body) and the **body-modal census** —
  the caution of §P1.

### §P5 ⭐⭐ THE SEASON LADDER (the ecological gate, M-DF.4), and the floor

DF-T1's design reused: **2 arms** (`liveShut` = the live world + `dfAssignPersist`, door shut;
`liveArmed` = the same + `dfSurface`) × **4 paired league seeds** × **20 generations**, every
generation measured — ≈ 160 league-seasons. Early/late windows are DF-C0's: **1–5 vs 16–20,
disjoint**. The slope point estimate goes through **DF-C0-FIX §RF1's ONE FORMULA**,
`slopeDeltaThroughOneFormula = mean(per-league (late − early))`, called by BOTH the publish
side and the on-disk re-derivation so the two cannot drift.

**THE FLOOR**: DF-C0 §R4's `atkFrozen` goals slope **+0.2211** (half-width 0.1423, |Δ|÷hw
1.55) — the ecology's inflation with the ATTACK half frozen. **QUOTED, never re-run.**

⚠ **The floor is a REFERENCE LINE, not a matched control** — a different counterfactual
(frozen attack genes vs an armed defensive door) on different league seeds (DF-C0's
12,508,900–903 vs this stage's 12,515,900–903). REPORTED, never gated.

⚠ **No between-arm SLOPE test is pre-registered and none will be invented after sight**: each
arm carries its own league-clustered interval and the comparison is read as **overlap** —
DF-T1 §R8 item 2's discipline, inherited verbatim.

⭐ **DIRECTION PRE-REGISTERED** (#320 item 3's frozen direction, restated at dispatch): the
ladder is **REPORTED**, and a deviation **ROUTES TO A SLICE, never to a nudge**. DF-T1
measured the persistence door **widening** the excess over the floor (+1.513 armed vs +1.256
shut, inside overlapping intervals) while the **reading half moved the predicted way**. This
exam asks the same two questions of the SURFACE door.

### §P6 Gates (frozen; a RED gate stays red and is reported)

`gWorldOkEveryWalk` · `gSeedsBookedEqualWalked` · `gArmsPairedPerSeed` ·
`gAnchorsResolveOnce` · ⭐ `gSrcUntouched` (porcelain AND `diff --stat HEAD` over `src` — the
exam is instrument-only) · ⭐ `gCapIntactBothArms` · `gCapBinsNonEmpty` ·
⭐ `gCapSliceShaIdentical` (before AND after the battery) · `gLatencyBinsStored` ·
`gSwarmBinsStored` · `gLedgerZeroWhenShut` (dormancy measured in-battery) ·
⭐ `gEveryOptionUsed` (**NON-DEGENERACY LIVENESS** — a one-corner surface goes RED and is
reported as such) · `gUsageCellsStored` · ⭐ `gContainInstrumentAlive` (a zero OFFER
denominator is a silently dead instrument, not a pass) · `gArmsDistinguishable` ·
`gRyiInstrumentAlive` · `gLadderComplete` · `gLadderDoorHeld` (the doors read back off
**every** created match) · `gLadderGen1Identical` · `gSeedDiscipline` · `gStatsDisjoint` ·
`gFingerprintUnmoved` · ⭐ `gFacesFromDisk` (canon, home ruling #287 item 1: the body is
**STAGED to disk, re-parsed, and every published face re-derived** — every between-arm face's
shut/armed/delta, latency percentiles from stored bins, chaser/swarm bins, the usage block,
the tercile table, (a1)/(a2)'s point estimates and booleans, the press-realisation join,
every ladder face × arm × generation, every slope through the ONE FORMULA, the floor read's
arithmetic, the body-modal census, and **all five H-DF.1 conjunct booleans + the verdict**).

⭐ **THE BODY IS HASHED LAST**, after every gate is written including `gFacesFromDisk`
(DF-C0 §CORR item 2, ruling #321). A RED run writes `…RED.json`; the canonical path is only
reached all-green.

### §P7 Seeds and stats (pre-registered — BOOKED = WALKED, the block consumed whole)

* **Block 12,515,000–999**, opened by #331 item 5, **CONSUMED WHOLE**. Sub-ranges:
  `…000–…039` the exam battery (40 paired seeds) · `…800–…802` the in-band smoke prefix ·
  `…900–…903` the ladder's four league seeds (booked once, walked in both arms) · `…999` the
  xxx,999 world-construction receipt seed (**WALKED**, so 41 seeds × 2 arms = **82 walks**).
* **Stats base 115,200, step 200.** ⭐ **THE REGISTRY OF RECORD ENTERING THIS STAGE IS 61**,
  not 59: IN-C0's completed 56 + 114,200 (IN-C0/IN-C0-FIX, #317 item 4) + 114,400 and 114,600
  (DF-C0, #320 item 4) = the "59" the rulings quote, **plus 114,800 and 115,000, which DF-T1
  ITSELF consumed** (DF-T1 §R7). The running "registry 59" in #329/#331 is the count as of
  DF-T1's **freeze**; DF-T1's two draws are appended here. Stated, not hidden (§R-DEV).
* **THREE draws ⇒ THREE bases**: **115,200** (the paired match-battery bootstrap) ·
  **115,400** (the ladder's league-clustered slope bootstrap) · **115,600** (the WITHIN-ARM
  bootstrap that carries H-DF.1(a1) and (a2)). Next base ≥ **115,800**; next sim block
  ≥ **12,516,000**.
* **Override discipline**: a smoke / N / GENS / OUT run may NOT write the canonical artifact
  path (the probe refuses, exit 2).

### §P8 The instrument

`scripts/probes/df-t3-surface-exam.ts`, frozen in **this** commit **BEFORE** the battery
(canon: **freeze-before-battery**, home ruling #266.3(c)); the artifact records its `sha256`.
The hashed body is built from an explicit **ALLOWLIST SCHEMA** — canon VERBATIM: *"the hashed
body is built from an explicit ALLOWLIST SCHEMA — a field not in the schema never enters the
body; forbidden-name lists are retired"* (home PC-T0 §CORR item 1). Env surface is
**whitelist-or-refuse**: `DFT3_MODE` (required) · `DFT3_N` · `DFT3_GENS` · `DFT3_OUT`; any
other `DFT3_*` var and any engine door is a fatal refusal.

**THE SIX ANCHORED EXTRACTIONS** (values REPORTED by the artifact with line receipts, never
asserted in prose): `markStanceBand` · `zonalEngageRadius9` · `markRange22` ·
`touchControlDist` · and DF-T2's two — `containRadius8` and `containTerritory35`, both from
the shipped Phase-29.1 contain branch's ONE line, which is also the press-realisation
walker's own geometry. Canon VERBATIM: *"a src-extracted constant pins its extraction to the
NAMED call site — anchored match + line receipt — never first-occurrence"* (home BK-C0 §CORR
item 1). The contain candidate's `why` line is pinned to **exactly one occurrence** as the
realisation needle, and a non-singular needle **bits the construction class** before any
battery runs.

---

## §RESULTS

> **Instrument**: `scripts/probes/df-t3-surface-exam.ts`
> (`instrument.sha256` = `1d2ec988cb95dcd818b228723ed33725d1f0fc27053693dc43658cda9f6bcc09`),
> frozen at `7d36de7` **before** the battery.
> **Artifact of record**: `docs/world-model/data/df-t3-surface-exam.json`
> (`bodySha256` = `f983f6195a2c7f25e83537d78067eecc189b0c37b876de91c47a78fea6c44643`).
> **82 walks** (41 paired seeds × 2 arms) + **160 league-seasons / 11,360 matches**; every
> `worldOk` true; **all 23 gates GREEN**; **530 re-derivation checks off disk, 0 mismatches**.
> Wall: battery **9.5 s**, ladder **953 s** (≈ 15.9 min).
> Every number below is quoted VERBATIM from an artifact field (canon: *"a stage doc's prose
> quotes artifact FIELDS verbatim or the number becomes a gated face"*, home PC-T2 §CORR
> item 4).

### §R0 ⭐⭐ THE VERDICT — **H-DF.1(b) PASS · H-DF.1(a) FAIL** (`verdict` = `FAIL — (a)`)

| conjunct | rule (frozen at §P1) | the number | verdict |
|---|---|---|:--:|
| **(a1)** press ELECTION share resolves apart by MODE | DEFEND vs PRESS intervals **DISJOINT** | **0.00378011162028** [0.00291981199259, 0.00479677277196] vs **0.000783475783476** [0.000420757363254, 0.00121818577333]; gap **0.0029966358368**, **|Δ|÷hw 3.19307** | ✅ |
| **(a2) PRESS** | top-vs-bottom tercile **DISJOINT** + **STRICTLY MONOTONE** | `[0.00150781962269, 0.00324652656566, 0.00362976406534]`; bottom [0.000909642207398, 0.00231122149781] vs top [0.00257751497233, 0.00481417292509] | ✅ |
| **(a2) TAKE** | the SAME rule | `[0.150150781962, 0.156426726245, 0.166339494055]` — **monotone TRUE**, bottom [0.139787870547, 0.160352359808] vs top [0.152372659991, 0.180705992363] **OVERLAP** | ⛔ |
| **(b1)** four-chaser bin ≡ 0 BOTH arms | structural | shut `[74734, 247213, 164625, 35656, 0]` · armed `[85962, 243828, 160721, 33973, 0]` | ✅ |
| **(b2)** multiChase2 **AND** 3 do NOT RISE | non-inferiority, paired CI | mc2 **−0.0132332537119** [−0.0375596091718, 0.00999398027263] · mc3 **−0.00603915308094** [−0.0175197714982, 0.00526049745224] — **both point estimates DOWN, neither resolved** | ✅ |
| **(b3)** the cap slice unmoved | sha before AND after | `5b4a21d036e9d97027a360f166621d747374ec5c42ead20b9ed132919872703c` twice; the Phase-31 rule line occurs **exactly once** | ✅ |

⛔ **THE RED STAYS RED, AND THE RULE WAS NOT RE-CUT.** (a2)'s TAKE limb has the gradient the
frozen rule asked for — **monotone, in the predicted direction, 0.1502 → 0.1564 → 0.1663** —
and its top and bottom intervals **overlap by 0.0080 of share** (top's low edge
0.152372659991 sits inside bottom's interval, whose high edge is 0.160352359808). At 41 seeds
that is an underpowered contrast, not a contradicted one; **but the criterion was frozen at
dispatch and a near miss is a miss.** H-DF.1(a) therefore **FAILS on one of four sub-limbs**,
and the honest statement of record is: *the surface differentiates BY SITUATION resolvedly
and BY BODY on the PRESS election resolvedly; the TAKE gradient points the right way and does
not resolve.*

⭐ **IN THE PLAYER'S LANGUAGE**: a defender in a team holding its block elects to leave his
man for the ball **~4.8× more often** than the same defender in a team already pressing
(0.00378 vs 0.00078) — the shipped geometry talking, and it resolves. And **the better
defenders press more**: bodies in the top third of `attrs.defending` elect the ball **2.4×**
as often as the bottom third (0.00363 vs 0.00151), disjoint. Whether they also *take* their
man on contact terms more often — the physical half of the 范戴克/佩佩 axis — this exam
cannot say.

### §R1 ⚠⚠ THE INHERITED CAUTION SURVIVES THE EXAM, UNCHANGED

`usage.bodiesCounted` **410** · `usage.bodyModalCounts` `[0, 407, 0, 3]` ·
`usage.bodyModalShare` `[0, 0.992682926829, 0, 0.00731707317073]`.

**407 of 410 bodies are HOLD-MODAL and not one body is modal on press or jump** — the DF-T2
receipt's split, **reproduced digit-for-digit on 41 VIRGIN seeds** (DF-T2 walked
12,512,000–039 + 999; this exam walked 12,515,000–039 + 999). Two independent draws of the
same ecology land on the same three-body tail, which is a strong statement that the
degeneracy is structural rather than a sampling accident.

**BOTH READINGS ARE THE RESULT, and they do not contradict each other**: the aggregate
distribution is non-degenerate (`byOptionShare`
`[0.00278031914262, 0.762841153476, 0.0768983995342, 0.157480127847]`, `gEveryOptionUsed`
GREEN, `usage.electionsArmed` **84163**, `usage.idleArmed` **8817**), the two priced
elections resolve apart by situation and by body — **and an argmax-per-body reading of this
surface still scores it as one corner.** Per §P1 this is REPORTED, not a conjunct; the
verdict above stands on the conjuncts alone.

**THE DEFENCE BOOK IS STILL BITING**: `usage.pressOfferedArmed` **921** ·
`usage.pressDeclinedByBookArmed` **228** — roughly a quarter of every press the shipped
geometry offered, removed by the team's own learned book, decline-only.

### §R2 ⭐ THE PRESS REALISATION RATE (#327 §CORR item 3's ordered face) — **THE ELECTION BUYS AN ACT**

| face | shut [95 % CI] | armed [95 % CI] | Δ [95 % paired CI] | \|Δ\|÷hw | resolved |
|---|---:|---:|---|---:|:--:|
| ⭐ `containActShareOfOfferTicks` | **0.279268701778** | **0.406973784678** | **+0.1277050829** [0.0669756354195, 0.19640470035] | **1.97336** | ↑ |
| `containEpisodesPerDefenderMinute` | 0.227035270837 | 0.355476356689 | +0.128441085852 [0.0552258585735, 0.19649876946] | 1.81834 | ↑ |
| `containOfferTicksPerDefenderMinute` | 8.25356271176 | 10.8268729103 | +2.57331019855 [0.895732488136, 4.30650174437] | 1.50893 | ↑ |

**THE ANSWER TO §CORR 3's QUESTION**: the usage tables' press numbers are ELECTION shares,
and the election **does** convert. Of the defender-ticks the Phase-29.1 contain branch's own
geometry offers, **27.9 % end in a realised contain with the door shut and 40.7 % with it
armed** — a resolved +12.8 points. `pressRealisation.episodesPerPressElection` =
**1.10256410256** (`containEpisodesArmed` **258** / `pressElectionsArmed` **234**).

⚠⚠ **READ ALL THREE ROWS TOGETHER — the denominator moves too** (moving-denominator canon):
the OFFER population itself rises resolvedly (+2.57 offer-ticks per defender-minute), which
is exactly what vacating a ledger slot does — it manufactures unassigned goal-side bodies
near the carrier. The share rises **on top of** that, so the surface is not merely making
more offers; a larger fraction of them are licensed. ⚠ `pressRealisation.containEpisodesShut`
= **164** episodes arise with **ZERO elections** (the shipped spare-body state) — that is the
background this index sits on, and it is why `episodesPerPressElection` is labelled a
**CONVERSION INDEX, not a probability** (numerator and denominator run at different cadences
and it is not bounded by 1). ⚠ The join is at **team/walk grain, never body-to-body**: the
ledger carries counts, not identities, and src is untouched by this exam.

### §R3 ⭐ THE SEASON LADDER, AGAINST THE ATKFROZEN FLOOR — **THE POINT ESTIMATE BENDS TOWARD IT, AND NOTHING RESOLVES**

2 arms × 4 paired leagues × 20 generations = **160 league-seasons, 11,360 matches**, every
generation measured. `gLadderDoorHeld` GREEN: both doors read back off **every** created
match, `doorWrong` 0 everywhere.

**goals/match, early (gens 1–5) → late (gens 16–20):**

| arm | gen 1 | gen 20 | early → late | Δ | half-width | \|Δ\|÷hw |
|---|---:|---:|---|---:|---:|---:|
| `liveShut` | 2.38732394366 | 3.80633802817 | 2.41126760563 → 3.6985915493 | **+1.28732394366** | 0.413732394366 | 3.11149 |
| `liveArmed` | 2.29929577465 | 3.71830985915 | 2.44084507042 → 3.60633802817 | **+1.16549295775** | 0.18485915493 | 6.30476 |
| *(reference)* `atkFrozen` **FLOOR** | — | — | — | **+0.2211** | 0.1423 | 1.55 |

⭐ **`bendsTowardFloor` = `true`** — `shutDistanceAboveFloor` **1.06622394366**,
`armedDistanceAboveFloor` **0.94439295775**, `armedMinusShut` **−0.12183098591**,
`fractionOfExcessClosed` **0.114263974875**. The armed world closes **11.4 %** of the shut
arm's excess over the floor. ⚠ **THAT IS A POINT ESTIMATE INSIDE OVERLAPPING INTERVALS**:
shut [0.766901408451, 1.59436619718] vs armed [0.995774647887, 1.36549295775] — each arm's
point estimate sits inside the other's interval, **no between-arm slope test was
pre-registered and none is invented**. The honest statement: **the sign is the first one in
this arc to point the right way, and nothing here resolves it.**

⚠ Per the frozen direction (#320 item 3, restated at dispatch), that routes to a **SLICE, not
to a nudge** — and it is a *deviation toward* the floor, so it orders nothing at all beyond
being reported.

**⭐ THE READING-vs-GOALS MANDATE AT ITS OWN ESTIMAND** (DF-C0 §R4's: evolved league play
across generations, per team-match — **NOT** the friendly numbers of §R5):

| arm | interceptions Δ [95 % CI] | \|Δ\|÷hw | tackles Δ [95 % CI] | \|Δ\|÷hw | interceptions gen 1 → gen 20 |
|---|---|---:|---|---:|---|
| `liveShut` | **−2.60950704225** [−4.08732394366, −1.23345070423] | 1.82875 | +2.15246478873 [1.69084507042, 2.85105633803] | 3.71047 | 11.7799295775 → 8.11971830986 |
| `liveArmed` | **−3.28274647887** [−4.23802816901, −2.46549295775] | 3.70401 | +1.74612676056 [1.3823943662, 2.00211267606] | 5.63523 | 11.7059859155 → 7.33450704225 |

⛔ **THE READING COLLAPSE IS 26 % DEEPER IN THE ARMED WORLD, NOT SHALLOWER** — the OPPOSITE
of what DF-T1's persistence door bought (there the collapse was 23 % *shallower* armed). The
contact rise is **smaller** armed (+1.746 vs +2.152). ⚠ The two arms' intervals **overlap on
both faces**, so nothing resolves; but the direction is stated unhedged because it is the
mandate's own face. **The shape of record**: keeping your man bought back READING and no
goals (#324); pricing the option buys back a slice of the GOALS slope and gives back some
reading. Neither is a nudge target — both are slice questions.

`passCompletion` is flat in both arms (shut 0.723116034138 → 0.719583399085; armed
0.722510774671 → 0.71941708886), so this is not a general chaos rise.

### §R4 H-DF.1(b)'s OWN BINS, AND THE ONE HONEST SPLINTER

`chaserBins.shut` = `[74734, 247213, 164625, 35656, 0]` · `chaserBins.armed` =
`[85962, 243828, 160721, 33973, 0]` — **the four-chaser bin is EXACTLY ZERO in both arms**
and the cap's own function is byte material this stage never touched.

`swarmBins.shutStance` = `[120305, 66645, 14806, 2280, 165, 0]` · `swarmBins.armedStance` =
`[130299, 60720, 16289, 2697, 159, 26]` · `swarmBins.shutZone` =
`[25048, 40324, 76839, 41988, 16844, 3158]` · `swarmBins.armedZone` =
`[27022, 45462, 74348, 43283, 16956, 3119]`.

| face | shut [95 % CI] | armed [95 % CI] | Δ [95 % paired CI] | \|Δ\|÷hw | resolved |
|---|---:|---:|---|---:|:--:|
| `multiChaseShare2` (SCORED) | 0.43910897156 | 0.425875717848 | −0.0132332537119 [−0.0375596091718, 0.00999398027263] | 0.556562 | — |
| `multiChaseShare3` (SCORED) | 0.12962728923 | 0.123588136149 | −0.00603915308094 [−0.0175197714982, 0.00526049745224] | 0.530209 | — |
| `swarmStanceShare2` | 0.0844804873629 | 0.0912079547076 | +0.0067274673447 [−0.00799017744165, 0.0214393755792] | 0.457191 | — |
| `swarmZoneShare3` | 0.303573439895 | 0.30143203768 | −0.00214140221481 [−0.0415211506711, 0.0343091672702] | 0.0564788 | — |

⭐ **`multiChaseShare3` FINALLY STOPS GRAZING UPWARD.** #324 left it at +0.0059 with a low
edge of −1.21 × 10⁻⁵; DF-T2's receipt left it lower armed but unresolved on a different
estimand; here, on the paired estimand with the surface as the only difference, it sits
**LOWER armed** (0.1296 → 0.1236) and is still unresolved. The face the arc has chased for
three stages does not rise.

⚠ **THE ONE SPLINTER, reported as loudly as the pass**: `swarmBins.armedStance`'s **last bin
is 26** (five or more bodies inside the 2.6 m stance radius) against **0** shut. It is 26 of
210,190 armed carrier-present defending team-ticks (**0.0124 %**), it is not one of H-DF.1's
frozen faces, and DF-T2's receipt had the sign the other way (shut 77, armed 0) — so the
honest reading is noise at the tail of a bin, not the swarm returning. It is stated because
a five-body stance bin is exactly the shape the cap exists to prevent, and the next stage
(the cap-off arm) should look at it first.

### §R5 THE CHURN / COVERAGE FAMILY, AND THE FRIENDLY-GRAIN DEFENSIVE MIX (REPORTED)

| face | shut | armed | Δ [95 % paired CI] | \|Δ\|÷hw | resolved |
|---|---:|---:|---|---:|:--:|
| `markSwitchesPerDefenderMinute` | 6.33622216232 | 6.38755189771 | +0.0513297353923 [−0.396375329509, 0.540952489871] | 0.109524 | — |
| `markSwitchesPerDefenderMatch` (240 s clock) | 25.3448886493 | 25.5502075908 | +0.205318941569 [−1.58550131803, 2.16380995948] | 0.109524 | — |
| `markHeldShare` | 0.656078096442 | 0.662139777835 | +0.00606168139339 [−0.00476607411383, 0.0176922343195] | 0.539816 | — |
| `dupMarkShare` | 0.263726498414 | 0.271976633763 | +0.00825013534967 [−0.0231307857421, 0.0372031249734] | 0.273483 | — |
| `reTargetLatencyMeanS` | 1.03319630465 | 1.05604400684 | +0.0228477021957 [−0.0401234969291, 0.0780807595356] | 0.38658 | — |
| `markAbandonsPerDefenderMinute` | 13.8809918334 | 13.3096961458 | −0.57129568763 [−1.10510082806, 0.0128945862504] | 1.022 | — |
| `markStartsPerDefenderMinute` | 14.2409257994 | 13.6927288092 | −0.548196990182 [−1.08479469355, 0.0421680127582] | 0.972875 | — |
| `chaseStartsPerDefenderMinute` | 10.2705772826 | 9.84035712974 | −0.430220152817 [−0.835410404541, 0.00944143928156] | 1.01845 | — |
| `chaseAbandonsPerDefenderMinute` | 10.0933790224 | 9.68328618143 | −0.410092840956 [−0.809106362909, 0.0308664456257] | 0.976443 | — |

⭐ **THE SURFACE DOES NOT BUY BACK THE CHURN DF-T0 REMOVED.** DF-T2's 82-walk receipt read
6.244 → 6.814 switches per defender-minute and called it the seam's own predicted cost; at
exam grain on virgin paired seeds the same face moves **+0.051 with |Δ|÷hw 0.11 — as flat as
a face gets**, and coverage does not fall. The receipt's warning does not survive the exam,
and the exam is the instrument that was built to settle it.

Stored latency bins (canon: a percentile face requires stored bins) — `latencyBins.shut` =
`[4484, 909, 565, 361, 237, 301, 157, 527]`, `latencyBins.armed` =
`[4237, 843, 568, 343, 249, 292, 182, 497]`; `shutMedianS` 0.5 · `shutP90S` 3 ·
`armedMedianS` 0.5 · `armedP90S` 3.

**⚠ THE FRIENDLY-GRAIN DEFENSIVE MIX — A DIFFERENT NUMBER FROM §R3's** (DF-T2 §R11 item 6,
honoured): `interceptionsPerMatch` 28.5853658537 → 27.8048780488 (Δ −0.780487804878
[−2.65853658537, 1.34146341463], unresolved) · `tacklesPerMatch` 2.0243902439 → 2.0487804878
(unresolved) · `tacklesPlusInterceptionsPerMatch` 30.6097560976 → 29.8536585366 (unresolved).
These are `team.stats` read at the whistle of single friendlies with random genomes. **The
mandate's verdict is §R3's, not this table's**, and the two must never be quoted as the same
number.

### §R6 THE R-乙 CHAIN FACES AND THE §2 EQUILIBRIUM FACES (REPORTED)

| R-乙 row | field | shut | armed | Δ [95 % paired CI] | \|Δ\|÷hw | R-乙's REAL band |
|---|---|---:|---:|---|---:|---|
| ⭐ Q01 spell length | `ryiQ01SpellSeconds` | 4.12837741244 | **4.4804964539** | **+0.352119041463** [0.0643664033739, 0.64794065336] | **1.20677** | 9.6 – 10.4 s |
| Q05 touches/spell | `ryiQ05TouchesPerSpell` | 2.42244460329 | 2.52898019076 | +0.106535587468 [−0.0147025907603, 0.227906863785] | 0.878248 | 2.88 – 5.12 |
| Q06 completion | `ryiQ06PassCompletion` | 0.592772547829 | 0.597382362107 | +0.00460981427785 [−0.0141807205133, 0.0234943025674] | 0.244715 | 75.3 % – 88 % |
| Q14 pressed share | `ryiQ14PressedReceptionShare` | 0.781272337384 | 0.762289068232 | −0.018983269152 [−0.0573648117574, 0.017283670029] | 0.508604 | UNSOURCED |
| ⭐ Q07 direction mix | `ryiQ07ForwardPassShare` | 0.563923474036 | 0.559052664381 | −0.00487080965441 [−0.0253660372195, 0.0149673478486] | 0.241527 | UNSOURCED |

⭐ **Q07 — THE #324 LABELLED HYPOTHESIS DOES NOT REPRODUCE AGAINST THIS DOOR.** DF-T1 found
the attack playing 2.4 points MORE of its passes forward against a persistent defence
(+0.0235, |Δ|÷hw 1.96) and labelled it a hypothesis. The SURFACE door moves the same face
**−0.0049, |Δ|÷hw 0.24 — nothing.** The hypothesis is neither confirmed nor refuted (it was
about a different door); what is recorded is that pricing the option does not add to it.

⭐ **Q01 IS THE ONE CHAIN FACE THAT RESOLVES**: possessions last **0.35 sim-s longer**
against the priced surface. ⚠ Both arms remain far below R-乙's real band (4.1–4.5 s vs
9.6–10.4 s) — the standing gap is untouched, and no causal story is attached beyond the label.

**§2 equilibrium faces (REPORT ONLY — nothing ships from an exam):** `goalsPerMatch`
3.14634146341 → 2.70731707317 (Δ −0.439024390244 [−1.21951219512, 0.390243902439], |Δ|÷hw
0.545455, **unresolved**) · `shotsPerMatch` 14.0487804878 → 14.243902439 · `crossesPerMatch`
3.56097560976 → 3.60975609756 · `headersPerMatch` 7.70731707317 → 7.70731707317 ·
`longBallsPerMatch` 4.0243902439 → 4.07317073171 · `cutbacksPerMatch` 6.24390243902 →
5.65853658537 — **not one of them resolves.**

### §R7 THE ANCHORED EXTRACTIONS (line receipts; the numbers are REPORTED, never asserted)

| id | file | value | line no. AT THIS COMMIT | matches |
|---|---|---:|---|---:|
| `markStanceBand` | `src/ai/actionExecutor.ts` | 2.6 | 297 | 1 |
| `zonalEngageRadius9` | `src/ai/TeamBrain.ts` | 9 | 733 | 1 |
| `markRange22` | `src/ai/TeamBrain.ts` | 22 | 748 | 1 |
| `touchControlDist` | `src/sim/constants.ts` | 4.2 | 315 | 1 |
| `containRadius8` | `src/ai/PlayerBrain.ts` | 8 | 1893 | 1 |
| `containTerritory35` | `src/ai/PlayerBrain.ts` | 35 | 1893 | 1 |

Each line matched EXACTLY ONCE (`gAnchorsResolveOnce` GREEN), and the contain candidate's own
`why` line — the realisation walker's needle — occurs exactly once
(`containWhyNeedle.occurrences` 1). ⚠ The two TeamBrain anchors sit at **733/748, exactly
where DF-T2 §R7 found them** — commit 1's three comment lines land at :784, *below* both, so
neither moved. The two PlayerBrain anchors have moved **1868 → 1893** since DF-T2 §R7, which
is IN-T1's own landing (#329), not this stage's: the anchored-extraction canon demonstrating
itself again — the line number is a receipt discovered at run time, never typed.

### §R8 SEEDS AND STATS

**BOOKED = WALKED** (`gSeedsBookedEqualWalked` + `gArmsPairedPerSeed` GREEN):
`12,515,000–039` (40 paired seeds) **+ `12,515,999`** = 41 seeds × 2 arms = **82 walks**; the
ladder's four league seeds `12,515,900–903` walked in **both** arms; the smoke prefix
`12,515,800–802` in band. **BLOCK 12,515,000–999 CONSUMED WHOLE.** Next sim block
≥ **12,516,000**.

**STATS: THREE BASES CONSUMED** — **115,200** (the paired seed-clustered match-battery
bootstrap) · **115,400** (the ladder's league-clustered slope bootstrap) · **115,600** (the
WITHIN-ARM bootstrap carrying H-DF.1(a1) and (a2)), step 200,
`minGapToAnyPublishedBase` **200**, `gStatsDisjoint` GREEN. **REGISTRY APPEND**: the registry
entering this stage is **61** (`stats.registryEntries`) — IN-C0's 56 + 114,200 + 114,400 +
114,600 (the "59" the rulings quote) **+ 114,800 and 115,000, DF-T1's own two draws**. After
this stage: **64**. Next base ≥ **115,800**.

### §R9 MUTANTS

This stage's mutant work is COMMIT 1's and is recorded at §C1.1: the new `:696` mutant kills
exactly one pin (the new one) and the site-anchored `:743` M4 re-record kills exactly one pin
(the priced greedy's), each restored from a `/tmp` byte copy, `cmp`-verified, never by
`git checkout`. The exam itself is INSTRUMENT-ONLY (`gSrcUntouched` GREEN: both
`git status --porcelain -- src` and `git diff --stat HEAD -- src` empty at run time) and
mutates nothing.

### §R-DEV DEVIATIONS (honest)

1. ⛔ **H-DF.1(a) IS RED ON ITS TAKE LIMB** (§R0). Monotone in the predicted direction,
   intervals overlapping. **The criterion was NOT re-cut, the face is not re-binned and no
   post-hoc power argument rescues it.** The honest reading — an underpowered contrast at 41
   seeds on a face whose three point estimates span 1.6 points of share — is stated as a
   reading, not as a verdict.
2. ⚠⚠ **THE BODY-GRAIN DEGENERACY PERSISTS UNCHANGED** (§R1): 407/410 HOLD-modal, identical
   to DF-T2's split on independent seeds. Reported beside the pass exactly as §P1 said it
   would be. Any consumer reading this surface by per-body argmax still sees one corner.
3. ⚠ **THE PRESS REALISATION FACE HAS A MOVING DENOMINATOR IN THE SAME DIRECTION AS ITS
   NUMERATOR** (§R2) — both are published, and the index `episodesPerPressElection` is
   labelled a conversion index, not a probability. The join is team/walk grain, never
   body-to-body, because the ledger carries counts and not identities and src is untouched.
4. ⚠ **NO BETWEEN-ARM LADDER SLOPE TEST EXISTS** — none was pre-registered, none was invented
   (DF-T1 §R8 item 2's discipline). Both the goals bend and the reading-collapse deepening are
   point estimates inside overlapping intervals and are stated as such.
5. ⚠ **THE FLOOR IS A REFERENCE LINE, NOT A MATCHED CONTROL** (§P5, restated in the artifact's
   `floorRead.interpretationNote`): different counterfactual, different league seeds.
6. ⚠ **`swarmBins.armedStance`'s last bin is 26 against 0 shut** (§R4) — 0.0124 % of armed
   carrier-present defending team-ticks, not a scored face, sign-reversed from DF-T2's
   receipt. Named for the cap-off stage rather than explained away.
7. ⚠ **THE REGISTRY BOOKKEEPING IS CORRECTED OF RECORD** (§R8): the rulings' running
   "registry 59" is the count as of DF-T1's FREEZE and omits DF-T1's own two consumed bases.
   This stage appends them, publishes 61 entering / 64 leaving, and gates disjointness against
   the corrected list.
8. ⚠ **`gLadderGen1Identical` PINS GENOMES, NOT RESULTS** (DF-T1 §R8 item 3, inherited):
   arming the door changes the world from generation 1, so the arms' gen-1 *outcomes* differ
   (goals 2.38732394366 vs 2.29929577465). Stated, not hidden.
9. **THE R-乙 SPELL WALKER IS A PORT, NOT A SHARED MODULE** (DF-T1 §R8 item 5, unchanged):
   copied from BK-T2 §(d) with the definitions quoted verbatim into the artifact (including
   #324 §CORR 1's restored loose-ball clause), but not drift-gated against that file at run
   time. A future chain-face consumer should promote it rather than copy it a fourth time.
10. **THE OPTIONAL `d` TIDY WAS DECLINED** (§C1.3) — stated at commit 1, repeated here so the
    ruling does not have to hunt for it.
11. **`PROGRAMME.md` / the rulings file are NOT edited by this session** (executor iron rule:
    governance files are the commander's). The queue's status line, the frontier update (next
    sim block ≥ **12,516,000**, next stats base ≥ **115,800**) and the ruling are the
    commander's to write.
12. ⚠ **THE THREE COMMITS LAND ON `main`**, as every prior stage of this programme has: the
    rulings cite freeze/result commits on the programme's own trunk and a side branch would
    break the next round's resume. Nothing was pushed.

## §COMMANDER CORRECTIONS OF RECORD (ruling #332, 2026-08-20 — frozen bytes stand)

1. **THE FAIL-(a) DISPOSITION IS RATIFIED AS THE DISCIPLINE WORKING**: the TAKE limb is
   monotone in the predicted direction and misses disjointness by 0.008 of share at 41
   seeds — reported red, never re-cut, never re-binned. THE REMEDY IS POWER, NOT A NEW
   RULE: **DF-T3B (the power extension)** is QUEUED — the SAME frozen (a2) conjunct on a
   larger virgin battery, plus this round's ordered instrument pin (item 2); the CAP-OFF
   ARM IS HELD until (a) resolves (M-DF.2: retirement needs (a) AND (b); (b) is banked
   PASS all three conjuncts). DF-T3B queues behind the IN exam and the corridor-hazard
   slice.
2. **(verify MED) THE OFFER GEOMETRY IS AN UNPINNED WALK-SIDE DEFINITION — the fourth
   instance of the unpinned-term class, and it crosses from seams into INSTRUMENTS**:
   neutralising the offer predicate's goal-side conjunct moves the starred headline ~2×
   with all 23 gates green — gFacesFromDisk proves ARITHMETIC, not DEFINITIONS (the
   verifier demonstrated both teeth and blind spot). The published number is faithful
   TODAY (all three terms verified against PlayerBrain.ts:1893). ORDERED: DF-T3B's
   commit 1 pins the offer predicate (anchored three-term extraction + a fixture whose
   OFFER membership flips per term). ⭐ NEW CANON registered this round (CANON.md, same
   round per #301): "a scored face's walk-side predicate is pinned — anchored extraction
   or fixture — because the re-derivation gate proves arithmetic, not definitions."
3. **THE REGISTRY CORRECTION IS RATIFIED — registry of record: 61 entering DF-T3, 64
   leaving; next stats base ≥ 115,800.** The rulings' running "59" had been stale since
   DF-T1's own two consumptions (114,800/115,000, DF-T1 §R7) — an executor correcting a
   governance number, flagged not silent, verified at source by the verifier. The
   commander's frontier line now carries 64; the lesson is the one-authoritative-source
   drift shape again (the rulings' consumption items remain the authority AND must be
   summed, not cached).
4. **THE SPLIT OF RECORD, NAMED FOR THE ARC**: the two defensive doors trade OPPOSITE
   halves — persistence bought READING (collapse 23 % shallower) and widened the goals
   excess; the surface buys a slice of the GOALS slope (first arm to bend toward the
   floor, 11.4 % of excess closed, unresolved) and gives back reading (collapse ~26 %
   deeper, more containment, fewer lane jumps). Neither resolves between arms; per the
   frozen direction both ROUTE TO SLICES: the NAMED DOORS are (i) THE LANE-SIT PRIMITIVE
   (the missing station action DF-T2 named out — capability under 底座给能力; the CHOICE
   stays priced, the standing observation binds: 拦截线路 must EMERGE) and (ii) the
   DEFENDER-SIDE SNAPSHOT interlock (the IN arc's later slice; the surface reads truth
   today, stated). Neither dispatches this round.
5. **REPORT-ONLY SLIPS CORRECTED OF RECORD**: the new pin's seeds are DF-T2's block
   constants (12,512,800–802), not this stage's smoke prefix (the doc and artifact are
   correct; the report's seedLedger line was wrong); bodyModalShare is the four-vector
   [0, .9927, 0, .0073]. THE DEVIATIONS RATIFIED: the dead-`d` tidy DECLINED with the
   right reason (a shape change exceeds the behaviour-free licence) — a standing tidy
   for whichever slice next opens that function; the R-乙 spell walker stays
   promote-don't-copy (fourth copy forbidden); the 26-tick ≥5-body stance splinter is
   the cap-off stage's first look.
