# DX T0 — THE WIND-UP AIM DORMANT SEAM (`dxWindupAim`, 脑子会选,腿也会踢)

Status: **PRE-REGISTERED, then BUILT + RUN the same round** (the GC-T0 / DV-T0 / DF-T0
two-part form). The law, the seam, the discard site, the seam map, the pin inventory and
the Road B statement below were written **before** the receipts section; the measured
digests arrive only in [§RESULTS](#results--the-receipts) at the foot.

> **Ordered by** COMMANDER RULING **#352 item 3** (the dispatch; the arc opened at #352
> item 2). **Bound by** [`DX-DELIVERY-EXECUTION-CONTRACT.md`](DX-DELIVERY-EXECUTION-CONTRACT.md)
> §2 — **M-DX.1** (THE DOOR: a pass entering the WIND-UP channel stores and resolves
> through *"the elected candidate's OWN aim — the exact point the argmax scored"*; ⛔
> *"NOTHING about the flight itself changes … the ONE change is WHICH POINT the
> already-shipped kick is struck toward"*) and **M-DX.2** (NO NEW CHOICE: zero candidates,
> zero scoring terms, zero genes; *"a world arming `dxWindupAim` without DLC armed changes
> NOTHING measurable"* — G-INERT, measured never assumed).
> **The structural fact of record**: [`GC-T1B-ALTERNATIVES-ARM.md`](GC-T1B-ALTERNATIVES-ARM.md)
> §P11's **`O1-WINDUP-PRECEDENCE`** — the wind-up seat's own aim is displaced from the
> target's pre-step position on **EXACTLY ZERO** decisions, in every world (30,318 wind-up
> decisions).
> **The user's word this exists for**: 「对啊,肯定得修,包括高空球,弧线球,力度等,」 (#352 item 1).
> **Road B**: nothing ships. `dxWindupAim` is default OFF and — like `bkGroundCorridor` —
> is **named by NO world and NO preset at all**.

---

## §CORRECTIONS-READ — every canon sentence COPIED from [`CANON.md`](CANON.md), never re-typed

Per ruling #301 item 2's mechanism fix: the ledger is where a brief copies from. ⚠ Per
**#342 item 3** (the MED-1 lesson), a constraint that binds this executor beyond the
ruling's own sentences is cited as **"the dispatch brief"**, never as the ruling.

| canon, verbatim | its home | how it binds here |
| --- | --- | --- |
| *"a seam-map gate pins occurrence COUNTS per needle and enumerates EVERY occurrence's site"* | **PC-C0-REACTION-BASELINE.md §COMMANDER CORRECTIONS item 1** | [§SEAM MAP](#seam-map--every-occurrence-counted-and-sited) counts both needles in every `src/**` file and names every site; the counts are re-asserted inside the pin suite, so the map cannot drift from the code |
| pin suites from birth — no one-shot-probe-only seams; every src seam ships its permanent pin suite (paraphrase) | **ruling #297 item 7** | `tests/dxWindupAim.test.ts` lands in the SAME commit as the seam (14 pins) |
| *"WORKER-SIMMED fixtures play the SHIPPED world (League.toJSON omits matchFlags; true since #155, stated now, test-pinned; refines #270's E4 correction; matches the perf diagnostic)"* | **ruling #283.2(iv)** | the flag gets a `League.matchFlags` key so a probe world can arm it, and the pin suite proves the key never reaches `League.toJSON()` |
| *"verifier scratch walks use the stage's own consumed band or the out-of-band scratch range (≥ 900,000,000) — never the next virgin block"* | **PW-T0C-OBJECTIVE-FIDELITY.md §COMMANDER CORRECTIONS item 6** | ⭐ EVERY walk in this stage — receipts and pin suite alike — uses seeds **900,000,100–102**. **ZERO frontier consumption**: next sim stays ≥ 12,527,000, next stats ≥ 117,600, registry 73 |
| *"arming receipts, not football findings"* (receipts ≠ effect sizes) | **ruling #289 item 1** (+ BU-T1 §CORR item 5) | every number in §RESULTS is a digest, a count or a geometric identity. **No football claim is made anywhere in this document.** H-DX.1 is DX-T1's business |
| *"a stage doc's prose quotes artifact FIELDS verbatim or the number becomes a gated face"* | **PC-T2-ARMED-WORLD-READ.md §COMMANDER CORRECTIONS item 4** | every digest below is quoted at full 64 hex characters from the run that produced it, its CELL is labelled (world shape × seed × arm), and the method that reproduces it is stated beside it — ⭐ the **#344 lesson** (GC-T0 §COMMANDER CORRECTIONS item 1: a mislabelled seed retires the receipt, not the claim) |
| a build/measurement of record runs on a CLEAN tree at a named commit (paraphrase) | **PC-ENTRY-RUNG.md §COMMANDER CORRECTIONS item 4** | the G-OFF baseline was walked on a **pristine `git worktree` checkout of the dispatch commit `5174a6c`**, containing no byte of this stage; see §R1's method note |
| *"a src-extracted constant pins its extraction to the NAMED call site — anchored match + line receipt — never first-occurrence"* | **BK-C0-BODYBALL-CENSUS.md §COMMANDER CORRECTIONS item 1** | this stage introduces **no constant of its own at all**; the only literal on a seam line is the certified power argument `1`, and the pin suite asserts no seam line carries any other number |

---

## §P0 WHAT THIS STAGE IS, AND IS NOT

It is a **dormant `src` seam plus its permanent pin suite plus arming RECEIPTS**. It makes
**no football claim**: not that the ground game improves, not that the GC suppression
converts, not that displaced deliveries are good. Those are **H-DX.1**'s conjuncts
(contract §3), scored by DX-T1 on virgin seeds at a frozen §P. What lands here is the
machinery, proven inert unarmed and proven faithful armed.

**⛔ WHAT IS NOT TOUCHED** (restated as a prohibition): the flight itself — no speed, no
height, no curl, no launch parameterization, no arc, no timing (`readyTick`, `c7WindupTicks`,
the facing law's `bkNoteFacing` extension are byte-untouched); the chooser — no new
candidate, no new scoring term, no new comparison, no new gene, no new constant, no new
perception channel, no rng draw; the **one-touch bypass** (`firstTouchWindow > 0`), which
already honours elected aims through its own synchronous statement; the PW weight deposit
and its ledger; `mechanics.ts` (not one byte); `genome.ts`; `a4World.ts` (not edited at
all); every world 6–11; the render layer; `Team.ts`; `cloneState.ts`.

---

## §LAW — the frozen law of the wind-up aim

```text
THE WIND-UP AIM DOOR (M-DX.1 + M-DX.2)
  armed  ⇔  match.dxWindupAim === true
            AND the decision's own two SHIPPED guards hold at the arm site:
                passMate === bestMate            [the chooser's OWN winner is the man
                                                  being passed to — a substituted target
                                                  was never priced with a displacement]
                bestLeadX !== 0 || bestLeadY !== 0   [the election is DISPLACED at all]

  ⭐ THE GUARDS ARE NOT NEW. They are, character for character, the guards the SHIPPED
    synchronous led-strike statement already applies. The door can therefore only carry an
    election the synchronous path would itself have struck.

THE CARRIED VALUE — the ELECTED DISPLACEMENT, not a new quantity
  bestLeadX / bestLeadY are the winning candidate's OWN displacement from the target's
  body, written by whichever candidate won the ONE argmax:
    (a) TO FEET       ⇒ 0,0 unless the banked PTP forced-aim door is open  ⇒ nothing carried
    (b) LED           ⇒ ledDelivery(...).lead          (DLC-T0's second delivery)
    (c) STRIKE PLANE  ⇒ strike.strike                   (DLC-T0s's K = 9 grid member)

THE CHANNEL (the PW T0b deposit idiom, verbatim in form)
  PlayerBrain (the ONE fork)   match.dxStrikeAim = { gid, lead, tick }
  Match.armPendingPass         captured iff dxWindupAim ∧ gid = passer ∧ tick = now,
                               then CONSUMED (dxStrikeAim = null) ⇒ one election is at
                               most one strike; stored as pendingPassWindup.aimLead
  Match.resolvePendingPassWindup
                               performPass(passer, mate, offsideExempt, 1, aimLead)
                               — the SHIPPED lead input, the SAME argument the synchronous
                               led-strike statement hands it.

THE STRIKE, unchanged (mechanics.performPass, not one byte edited)
  lead  = struckLead + aimLead,  struckLead = mate.pos + mate.vel · flight · 0.8
  d     = dist(passer.pos, lead)
  speed = clamp(d·0.6 + 8.2, 9, 22) · executedMul
  dir   = rotate(norm(lead − passer.pos), noise)
  ⇒ with the incumbent strike-time correction ZERO (a stationary target), the struck point
    IS the elected point exactly. With the target moving, the ball lands BEYOND the elected
    point by exactly the incumbent correction — PTP-T0 §HONESTY 5's disclosed composition,
    inherited UNCHANGED because reproducing the shipped semantics is the whole point
    (§HONESTY 1).

THE ZERO POINT OF THE WORLD, two forms, gated separately
  FLAG OFF        ⇒ the fork never runs, nothing is deposited, aimLead is null and the
                    resolve's call is the certified 3-argument release by DEFAULT ARGUMENT
                    (G-OFF — STRUCTURAL).
  ARMED, NO DLC/PTP DOOR ⇒ every election is a to-feet election, its displacement is exactly
                    0,0, the second guard is false and NOTHING is deposited. The armed world
                    is the shut world (G-INERT — MEASURED, not assumed).

NO PREDICATES (#200) — the complete conditional set is GATE and GUARD, and nothing else
  GATE   the arming rule (the ONE flag fork) + the arm-side gid/tick keying.
  GUARD  the two SHIPPED guards above, neither invented here.
  ⛔ NOTHING IS BANNED and NOTHING IS PRICED: no candidate gains or loses a point, and the
     argmax that elected the aim is not consulted a second time.
```

---

## §DISCARD — where the elected aim dies today, line-anchored

**THE DISCARD SITE** is `src/ai/PlayerBrain.ts`, `decideOnBall`'s `case 'Pass'`, in the
three-branch commit statement. As it stands at the dispatch commit `5174a6c`:

```ts
if (match.o1PassWindup && !mustKick && p.firstTouchWindow <= 0) {
  match.armPendingPass(p, passMate!, offsideExemptKick);          // ⛔ THE DISCARD
} else if (passMate === bestMate && (bestLeadX !== 0 || bestLeadY !== 0)) {
  match.performPass(p, passMate!, offsideExemptKick, 1, v2(bestLeadX, bestLeadY));
} else match.performPass(p, passMate!, offsideExemptKick);
```

The elected displacement `bestLeadX`/`bestLeadY` reaches the strike **only through the
second branch's fifth argument**. The wind-up branch takes neither it nor anything derived
from it: `armPendingPass(passer, mate, offsideExempt)` re-derives its own aim as
`{ x: mate.pos.x, y: mate.pos.y }` and `resolvePendingPassWindup` then called
`this.performPass(passer, mate, pp.offsideExempt)` — three arguments, so `performPass`'s
own `ptpLead` default `null` applied and `mechanics.performPass` struck at
`struckLead` alone. **That is the whole of `O1-WINDUP-PRECEDENCE`**: with `o1PassWindup`
armed (as world 11 arms it, via `a4MatchFlags(3)`) the execution branch takes the wind-up
FIRST, so on that composition a delivered re-aim could only ride the one-touch bypass —
which is exactly the zero GC-T1B measured.

**THE PLUMB-THROUGH** (this stage), three line-anchored statements and no fourth:

| # | site | statement |
| --- | --- | --- |
| **1** | `src/ai/PlayerBrain.ts`, immediately ABOVE the wind-up branch (the ONE fork) | `if (match.dxWindupAim && passMate === bestMate && (bestLeadX !== 0 \|\| bestLeadY !== 0)) { match.dxStrikeAim = { gid: p.gid, lead: { x: bestLeadX, y: bestLeadY }, tick: match.simTick }; }` |
| **2** | `src/sim/Match.ts`, `armPendingPass`, beside the PW capture | the deposit is captured iff `dxWindupAim` ∧ gid ∧ tick, cleared, and stored as the record's `aimLead` |
| **3** | `src/sim/Match.ts`, `resolvePendingPassWindup`, the release | `this.performPass(passer, mate, pp.offsideExempt, 1, pp.aimLead);` |

⭐ **Statement 1 does not replace the discard — it stands beside it.** The banked
`match.armPendingPass(p, passMate!, offsideExemptKick);` statement is UNTOUCHED (its
signature and its call site are pinned VERBATIM by O1-T1's and PTP-T0's own suites, and a
pinned test is a STOP), which is precisely why the value travels on the match rather than
in the call — the PW-T0b idiom, adopted here for the same reason it was adopted there.

### ⭐⭐ WHICH PASS KINDS FLOW THROUGH THE WIND-UP CHANNEL — with evidence

| kind | enters `pendingPassWindup`? | evidence |
| --- | --- | --- |
| **the open-play shortPass** (`case 'Pass'`, non-cutback, `!mustKick`, `firstTouchWindow <= 0`) | ✅ **the only one** | the ONE arm site: `count(brainSource, /match\.armPendingPass\(/g) === 1` and `count(matchSource, /armPendingPass\(/g) === 1` (the definition) — O1-T1 G4, re-asserted in this stage's suite |
| ⛔ the ONE-TOUCH release (`firstTouchWindow > 0`) | ❌ **the DESIGNED BYPASS** | the fork's own third conjunct; it releases synchronously and already carries the elected aim through the shipped second branch |
| ⛔ the restart taker (`mustKick`) · the kickoff pass | ❌ | the fork's second conjunct; the kickoff line is pinned verbatim by O1-T1 |
| ⛔ the CUTBACK | ❌ | its own branch above the fork, pinned verbatim by O1-T1 (`match.performCutback(p, cutbackMate!);`) |
| ⛔ the lofted switch · the punt · the dink · the keeper throw · the cross · the through ball · the clearance · the shot | ❌ | O1-T1 G4's sweep: no line calling any of those may name `o1PassWindup` or `armPendingPass` |

⇒ **the door's reach is exactly one pass kind**, and inside that kind exactly the
elections the synchronous path would itself have struck.

---

## §SEAM MAP — every occurrence, COUNTED and SITED

**PREFIX STATED**: this seam's whole needle family is exactly **two names** — the flag
`dxWindupAim` and the deposit slot `dxStrikeAim`. There is no third spelling, no type, no
constant, no gene and no alternate casing (the pin suite asserts the case-insensitive
count equals the case-sensitive count, file by file, across all of `src/**`). The wind-up
record's field `aimLead` and the two local bindings are counted separately below.

| file | `dxWindupAim` | `dxStrikeAim` | the sites |
| --- | ---: | ---: | --- |
| `src/sim/Match.ts` | **9** | **4** | the `MatchConfig` field (1) · the `readonly` field (1) · the `this.`/`cfg.` pair on the ONE initialiser line (2) · the arm-time gate (1) · FOUR prose mentions inside docblocks. `dxStrikeAim`: its declaration (1) · the arm-time read (1) · the arm-time clear (1) · one prose mention (1) |
| `src/ai/PlayerBrain.ts` | **2** | **1** | the ONE fork line (1) + one PROSE mention in its comment (1); the ONE deposit write (1) |
| `src/sim/League.ts` | **1** | **0** | the `matchFlags` key union, and nowhere else |
| every other file in `src/**` | **0** | **0** | ⛔ nothing: no executor, mechanics, physics, render, evolution, entry-layer, snapshot or League consumer |

The record field, same discipline: `aimLead` appears **5×** in `Match.ts` (the record type ·
the record literal · the resolve's read · two prose mentions) and **0×** anywhere else in
`src/**`. The locals: `dxDeposit` **5×** and `dxArmLead` **3×**, both confined to
`armPendingPass`.

Every count above is re-asserted inside `tests/dxWindupAim.test.ts` (`DX T0 §SEAM MAP`), so
the map in this document cannot drift from the code without a red test.

---

## §PINS — the PIN INVENTORY (a NAMED deliverable)

`tests/dxWindupAim.test.ts`, in the house form (`gcGroundCorridor.test.ts` /
`dfCapOff.test.ts`) — **14 pins**, all green from birth.

| group | what it pins |
| --- | --- |
| **ROAD B** | ⭐⭐ the PROHIBITION SET (the `dfCapOff` form): `a4World.ts` does not contain the string, `a4MatchFlags(6…11)` never carries the key, a bare `Match` / a world-11 match / a League match are all `false`, no `envArmed` / `EDS_BUNDLE_ARMED` / `process.env` on any seam line · ⭐ NO SERIALIZATION (`League.toJSON()`) · NO NEW GENE (`passLeadSupport` still born absent and outside `GENE_KEYS`) · NO NEW CONSTANT (no seam line carries a number but the certified `1`) |
| **G-OFF** | ⭐⭐ ABSENT ≡ EXPLICIT-FALSE, byte for byte, BOTH world shapes (bare · world 11) × 2 scratch seeds, per-cell AND pooled digest; the four cells are proven DISTINCT (a non-vacuity check) |
| **G-INERT** | ⭐⭐ ARMED WITHOUT a delivery-choice door ≡ shut, both shapes × 2 seeds, pooled — and the arm is proven REAL: the flag is on, world 11's wind-up channel genuinely strikes (`o1WindupLedger.struck > 0`) and the deposit slot is written **zero times** in a full match |
| **G-BITE (fixture)** | ⭐⭐ the STRUCK direction equals the direction to the ELECTED point EXACTLY (mate frozen ⇒ the incumbent correction is the zero vector; the gaussian stubbed to 0 ⇒ the spray term is exactly 0), while the SHUT arm of the same fixture flies at the target's BODY — the discard, reproduced and then repaired in one deterministic fixture · ⭐⭐ the WEIGHT LAW's distance is the ELECTED point's (the speed ratio equals the pure-geometry band ratio to 1e-9) · ⭐ the resolution site itself: `performPass` receives the elected displacement UNMUTATED, and receives `null` when the door is shut |
| **G-BITE (walk)** | ⭐ armed + world 11 + the DLC contest door + gene 1 diverges from the same world shut (an ARMING receipt, never an effect size) |
| **the carry law** | ⭐⭐ the stored aim IS the deposited aim (no mutation between arm and resolve), the deposit is CONSUMED at capture, and it is keyed to THIS body at THIS tick — a wrong-gid or wrong-tick deposit is never carried · ⭐⭐ shut, an identical deposit is not carried and not even consumed |
| **§SCOPE** (3 pins) | ⭐⭐ the ONE `match.dxWindupAim` fork in `src/**`, pinned VERBATIM, carrying the SHIPPED synchronous statement's own two guards; the banked O1/PTP statements re-asserted untouched; still ONE arm site · ⭐⭐ ONE consumption site (inside `armPendingPass`) and ONE plumb-through (the resolve, pinned verbatim); `mechanics.ts`, `actionExecutor.ts` and `performPass`'s own slice name none of the needles, so no other strike path — the one-touch bypass included — can see the deposit; and no seam line names `speed` / `loft` / `spin` / `curl` / `vz` / `kickBall` / `readyTick` / `rng` · ⭐ NO RNG at the fork or at the arm, and the fork forms no candidate and calls no pricer |
| **§SEAM MAP** | ⭐⭐ every count in the table above, plus the whole-tree "no other file" sweep |
| **the fingerprint** | ⭐ `57b0bdab…c673` a literal in this suite (⚠ the INHERITED HOUSE IDIOM, named at GC-T0 §CORR item 3: the enforceable receipt is §R0's real run) |

### ⚠⚠ THE TWO PRE-EXISTING PINS THIS STAGE NARROWS (disclosed at freeze, for ratification)

The **DF-T0 §P7 precedent** applies exactly (ratified at **ruling #323 item 1**): where a
banked stage's frozen assertion and the letter of a new dispatch cannot both stand, the pin
is **NARROWED, NOT DELETED, and made POSITIVELY**, in the same commit, and flagged.
**A pinned test is a STOP, never a silent edit.**

1. ⚠⚠ **`tests/o1PassWindup.test.ts` — "G7 RELEASE ONCE, AT readyTick"** asserted
   `expect(spy).toHaveBeenCalledWith(passer, mate, false)`, an EXACT argument-list match on
   `performPass`. #352 item 3 orders the elected aim to reach the release, and the release
   is the ONE call the aim can ride. **NARROWED to the positive form**:
   `toHaveBeenCalledWith(passer, mate, false, 1, null)` — the two dormant inputs are now
   pinned AT THEIR CERTIFIED VALUES (power exactly `1`, elected aim exactly `null` on this
   and every certified path). O1-T1's substantive claim — released ONCE, at `readyTick`, to
   the ARM-TIME mate with the ARM-TIME offside flag, never at commit and never twice — is
   **unweakened**, and is now strictly stronger about what the release carries.
2. ⚠ **`tests/o1PassWindup.test.ts` — "(i) INT-MATE is not vacuous"** carried the identical
   assertion for the same reason and is narrowed identically. Its own claim (an untouched
   arm-time mate really receives at `readyTick`; `cancelledMate` 0, `struck` 1) is
   **unweakened**.

**⚠ BOTH ARE FLAGGED FOR COMMANDER RATIFICATION.** No other test file was edited by this
stage; the only other `tests/**` change is the NEW `dxWindupAim.test.ts`.

---

## §HONESTY — the epistemic limits, stated plainly

1. ⭐⭐ **"THE STRUCK POINT IS THE ELECTED POINT" IS EXACT ONLY WHERE THE INCUMBENT
   CORRECTION IS ZERO — and that is DELIBERATE.** `mechanics.performPass` ADDS the handed
   displacement to its OWN strike-time correction (`mate.vel · flight · 0.8`), so against a
   MOVING target the ball is struck BEYOND the elected point by exactly that correction.
   That is PTP-T0 §HONESTY 5 / §DEV 2's already-disclosed composition, and this seam
   **inherits it unchanged rather than inventing a second aiming semantics** — M-DX.1's
   *"no new physics"* and the untouched one-touch bypass both require the wound-up strike
   to behave exactly as the synchronous led strike does. §R2's fixture therefore freezes
   the target to isolate the aim, and the honest general statement is: **the wound-up kick
   is now struck through the SAME aim composition the shipped bypass uses.**
2. ⚠ **THE AIM IS THE ARM-TIME ELECTION, and the world moves during the window.** The
   chooser is not re-run at `readyTick` (that would be a new decision, not this contract's
   slice), so a displaced aim can be W ticks stale — exactly as the wind-up's incumbent
   `aim`, `faceTarget`, mate identity and PW weight already are. The mate-identity guard
   (INT-MATE) is unchanged and still owns the cancellation cases.
3. **NO EXECUTION-ERROR MODEL** (contract §4): expression without error makes the body more
   precise than a human. The door is named, not smuggled — it is not opened here.
4. ⭐ **THE DOOR CANNOT EXPRESS AN ELECTION THE SYNCHRONOUS PATH WOULD NOT HAVE STRUCK.**
   Both guards are the shipped statement's own. If those guards are wrong about something,
   DX does not fix it — it makes the wind-up agree with them.
5. **NO PERF READING IS PUBLISHED.** The dispatch ordered no cost face and inventing one
   would be a number without a method. The armed cost is one small object per armed
   displaced election; unarmed it is one boolean read per pass commit.
6. ⚠ **G-INERT IS MEASURED ON FOUR CELLS** (2 shapes × 2 seeds) plus a whole-match deposit
   count — a strong structural claim (the second guard is false by arithmetic), not an
   exhaustive one.
7. ⚠ **THE MUTANT COVERAGE IS NOT UNIFORM** — see §R4 rows M2 and M4: the flag-inversion
   mutant is invisible to G-OFF (the arm-side gate is a second limb, so a shut world still
   carries nothing) and the leak-into-the-synchronous-path mutant is killed by SOURCE-TEXT
   pins rather than by a behavioural fixture. Stated rather than glossed (the GC-T0
   §HONESTY 7 form).
8. **NO LEDGER COUNTER WAS ADDED** (§DEV 3): DX-T1's `altDisplacedShare` face must be read
   from the wind-up record or from its own instrument, not from `o1WindupLedger`.

---

## §ROAD B — nothing ships

`dxWindupAim` is **OFF in every production path** — a hard `false` default, **absent from
`a4World.ts` entirely**, absent from every play-test world, absent from every
`League.matchFlags` unless a probe sets it explicitly — and even ARMED it does nothing in a
world without a delivery-choice door (G-INERT). No gene is created. The production
fingerprint is unchanged and the flag-off world is byte-identical to the dispatch commit's
on every walked seed with the trace, the score, the stats and the event count included.
**Nothing about the game the user plays changes in this commit.** The seam exists so DX-T1
can run the exam.

**Road B statement**: fingerprint
`57b0bdab389122af5e4cacd75c4e13020b8ff248a413a7fcd71cc6215ba4c673` — **must not move, and
did not** (§R0).

<!-- ⛔ NOTHING ABOVE THIS MARKER IS EDITED AFTER THE FREEZE COMMIT. -->

---

## §RESULTS — the receipts

> ⭐ **These are RECEIPTS, not effect sizes** (canon: *"arming receipts, not football
> findings"*, home ruling #289 item 1). No football claim is made here.
> **Every walk is out-of-band scratch (≥ 900,000,000). ZERO frontier consumption.**

### §R0 THE FINGERPRINT

```
$ npm run fingerprint
seed=1337 seasons=2 matches=142
sha256=57b0bdab389122af5e4cacd75c4e13020b8ff248a413a7fcd71cc6215ba4c673
```

**UNMOVED**, run twice: once mid-stage and once at the finished tree.

### §R1 ⭐⭐ G-OFF — THE CROSS-COMMIT BYTE-IDENTITY WALK (the GC-T0 / DF-T0 receipt method)

**Method** (stated so it re-derives): a scratch instrument walks a full 240 s match and
digests `sha256(trace ⊕ score ⊕ stats ⊕ events.length ⊕ ticks)`, where `trace` samples the
ball's position/velocity, the score and every body's `pos`/`heading.x` every 37th tick,
rounded to 1e-9 — the house world-identity signature (PW-T0b's, via PC-T0 / BK-T0 / DF /
GC-T0). Two world shapes × two scratch seeds. The BEFORE column was produced on a
**pristine `git worktree` checkout of the dispatch commit `5174a6c`** — a tree containing
no byte of this stage — and the AFTER column on the finished tree with the flag OFF.
⚠ Stated precisely: the two runs were made in that order against those two trees; the
baseline tree's identity is the dispatch commit, not a stashed working tree.

⭐ **CROSS-VALIDATION OF THE INSTRUMENT** (the #344 lesson): before any new cell was
quoted, the instrument reproduced **GC-T0 §R1's published digests character for character**
on the same two shapes and seeds — `bare · 900,000,100 = 91247fa2…3a6d` and
`world 11 · 900,000,100 = c45188a7…dc72`.

| cell | digest at `5174a6c` (before) | digest at the finished tree, flag OFF (after) |
| --- | --- | --- |
| bare · 900,000,100 | `91247fa2d9980706223a23ab4d8f15eec2c9d55bf43b15b4ebf2a3940a983a6d` | **identical** |
| bare · 900,000,101 | `aa05174583333b99cf3c06ab228805928ba4994c6367401155f50d0636807d43` | **identical** |
| world 11 · 900,000,100 | `c45188a7ea3f725c2a874082a34d3cae5d515ec666914bae5839eae0b8c7dc72` | **identical** |
| world 11 · 900,000,101 | `41c5b63ab16380c13e50bdb6cc11e834510e225cfa72c3907e691a126cd26bb6` | **identical** |
| **POOLED** (the four cells above, joined by `\|`, sha256) | **`47adc9ebc3f69de6d824b28a7ed89905e4aa3924352949332a908d934d882080`** | **`47adc9ebc3f69de6d824b28a7ed89905e4aa3924352949332a908d934d882080`** |

⭐ **THE FLAG-OFF WORLD IS THE DISPATCH COMMIT'S WORLD, BYTE FOR BYTE**, including world 11
— the composition the DX-T1 exam will run, and the one that ARMS THE WIND-UP ITSELF. The
permanent pin suite carries the same claim in its drift-resistant relational form (ABSENT ≡
EXPLICIT-FALSE, per cell and pooled), so a future engine change moves the digests honestly
without going red for the wrong reason.

### §R2 ⭐⭐ G-INERT — ARMED WITHOUT A DELIVERY-CHOICE DOOR, MEASURED

| cell | shut | **ARMED (`dxWindupAim: true`), no DLC/PTP door** |
| --- | --- | --- |
| bare · 900,000,100 | `91247fa2d9980706223a23ab4d8f15eec2c9d55bf43b15b4ebf2a3940a983a6d` | `91247fa2d9980706223a23ab4d8f15eec2c9d55bf43b15b4ebf2a3940a983a6d` |
| bare · 900,000,101 | `aa05174583333b99cf3c06ab228805928ba4994c6367401155f50d0636807d43` | `aa05174583333b99cf3c06ab228805928ba4994c6367401155f50d0636807d43` |
| world 11 · 900,000,100 | `c45188a7ea3f725c2a874082a34d3cae5d515ec666914bae5839eae0b8c7dc72` | `c45188a7ea3f725c2a874082a34d3cae5d515ec666914bae5839eae0b8c7dc72` |
| world 11 · 900,000,101 | `41c5b63ab16380c13e50bdb6cc11e834510e225cfa72c3907e691a126cd26bb6` | `41c5b63ab16380c13e50bdb6cc11e834510e225cfa72c3907e691a126cd26bb6` |

**IDENTICAL IN ALL FOUR CELLS** — M-DX.2's *"changes NOTHING measurable"*, measured. ⭐ And
the arm is proven REAL rather than mis-built: in the world-11 armed cell the flag reads
`true`, `o1PassWindup` reads `true`, the wind-up channel genuinely strikes
(`o1WindupLedger.struck > 0`) and the deposit slot `dxStrikeAim` is written **zero times**
across the whole match — the door had nothing to carry, which is exactly the claim.

### §R3 ⭐⭐ G-BITE — THE STRUCK POINT IS THE ELECTED POINT

**(a) THE FIXTURE (the aim-fidelity proof, at the resolution site).** The O1-T1
`armedFixture` idiom with the target FROZEN (position held, velocity exactly zero, so
`performPass`'s own strike-time correction is the zero vector) and the gaussian stubbed to
0 (so the spray term is exactly 0). A displaced election `lead = (3.5, −2.25)` is deposited
exactly as the ONE fork deposits it, the pass is wound up, and the strike is captured at the
SHIPPED `kickBall` — downstream of every aim statement in `mechanics.performPass`.

| arm | what the body actually struck |
| --- | --- |
| **SHUT** (today's world) | `dir` **equals `norm(mate.pos − passer.pos)` exactly** — the wound-up kick flies at the target's BODY. ⛔ `O1-WINDUP-PRECEDENCE`, reproduced in one fixture |
| **ARMED** | `dir` **equals `norm((mate.pos + lead) − passer.pos)` exactly** — the ELECTED point |
| both | exactly ONE strike each; the two directions differ; `performPass` received the elected displacement UNMUTATED when armed and `null` when shut |

⭐ **AND THE WEIGHT LAW AGREES WITH THE AIM** (seed 900,000,101's fixture): `speed =
clamp(d·0.6 + 8.2, 9, 22) · executedMul` where `executedMul` reads the misalignment TO THE
BODY — identical in both arms — so the armed/shut speed ratio is pure geometry. Measured
ratio **equals `band(d_elected) / band(d_body)` to 1e-9**, i.e. the shipped speed law is
reading the ELECTED point's distance. The struck point is the elected point in **both**
components, direction and distance.

**(b) THE WALK (a whole match reprices).** Seed **900,000,102**, world 11 + the DLC
delivery-contest door + `passLeadSupport` = 1, the ONLY difference being `dxWindupAim`:

| arm | seed 900,000,102 |
| --- | --- |
| world 11 + DLC, gene 1, **SHUT** | `b8dd6074cdbbdc1794af2180c74ec6c404c172da2a3932f0c224917b1bc8fd45` |
| world 11 + DLC, gene 1, **ARMED** | **different** — `d82b41f234472fef76a934a2583203626b99ae95c88ca9103f35204e2e785151` |

The seam is not inert-by-accident on the composition DX-T1 will examine. **⛔ This is an
arming receipt. It says NOTHING about whether the football improves** — that is H-DX.1.

### §R4 MUTANTS (run live on an UNCOMMITTED tree, restored from `/tmp` byte copies,
`cmp`-verified)

| mutant | edit | result |
| --- | --- | --- |
| **M1 THE PLUMB-THROUGH REVERTED** | the resolve's fifth argument `pp.aimLead` → `null` | **KILLED** — 5 pins die (both G-BITE fixture pins, the G-BITE walk, the plumb-through source pin, the seam map) |
| **M2 THE FLAG INVERTED AT THE FORK** | `if (match.dxWindupAim && …` → `if (!match.dxWindupAim && …` | **KILLED** — 2 pins die (the ONE-FORK verbatim pin, the G-BITE walk). ⚠ **HONEST NOTE**: G-OFF does NOT see this mutant, because the arm-side gate is a second limb — a shut world deposits but never carries (§HONESTY 7) |
| **M3 THE STORED AIM MUTATED BETWEEN ARM AND RESOLVE** | `aimLead: { x: dxArmLead.x * 2, y: … }` | **KILLED** — 5 pins die (the carry-law pin, both G-BITE fixture pins, the no-constant pin, the seam map) |
| **M4 THE AIM APPLIED TO THE SYNCHRONOUS PATH TOO** | a deposit-consuming block inserted into `Match.performPass`, overwriting `ptpLead` | **KILLED** — 2 pins die (the ONE-CONSUMPTION-SITE pin, the seam map). ⚠ **HONEST NOTE**: caught by SOURCE-TEXT pins, not by a behavioural fixture (§HONESTY 7) |
| **M5 THE SUBSTITUTED-TARGET GUARD DROPPED** | `passMate === bestMate` removed from the fork | **KILLED** — 1 pin dies: the ONE-FORK verbatim pin (the guards are the shipped statement's own) |
| **M6 THE gid/tick KEYING DROPPED** | the deposit is carried regardless of body and tick | **KILLED** — 2 pins die (the carry-law pin's wrong-gid / wrong-tick cases, the seam map) |

Every mutant was reverted by byte-copy restore (`cmp`-verified), never by `git checkout`;
`npx tsc --noEmit` and the suite were re-run green on the restored tree.

### §R5 CHECKS

* `npx tsc --noEmit` — **clean** (on the finished tree, and again after the mutant round).
* `npx vitest run tests/dxWindupAim.test.ts` — **14/14 green**.
* The touched/neighbouring suites, re-run together with `--no-file-parallelism` —
  `o1PassWindup` (26) · `ptpPassLead` (24) · `bkFacingLaw` · `pwWeightChooserSeat` ·
  `dlcDeliveryChoice` · `dlcStrikePlane` · `dvDeliveryValue` · `gcGroundCorridor` ·
  `passPower` · `entriesW10W11` · `carryChoiceSeat` — **green** (the two narrowed pins of
  §PINS green in their narrowed positive form; every other assertion in those files
  untouched).
* `npm test` (FULL suite) — ⚠ **NOT RUN TO COMPLETION BY THIS STAGE, and the honest reading
  is stated rather than glossed.** The known load-induced timeout class (GC-T0 §R5 item 5 /
  DF-T0 §R7 item 4: a ~4,560 s in-test wall time across 156 parallel files produces
  DIFFERENT failing sets on repeated runs, every failure a `Test timed out`, zero assertion
  failures, all green under `--no-file-parallelism`) makes a full parallel run a clock
  reading rather than an assertion reading on this machine. What IS established here is
  that every suite that touches this seam's files, plus this seam's own, passes.

### §R6 SEEDS AND STATS

**ZERO FRONTIER CONSUMPTION.** Every walk in this stage — receipts and permanent pin suite
alike — used **900,000,100 / 900,000,101 / 900,000,102**, in the out-of-band scratch class
(canon: *"verifier scratch walks use the stage's own consumed band or the out-of-band
scratch range (≥ 900,000,000) — never the next virgin block"*, home:
PW-T0C-OBJECTIVE-FIDELITY.md §COMMANDER CORRECTIONS item 6). **No stats stream was drawn** —
this stage publishes digests, counts and geometric identities, so the registry stays **73**,
next stats ≥ **117,600**, next sim ≥ **12,527,000**. DX-T1's block is reserved at ITS
dispatch, not here.

### §DEV — the deviations, declared

1. ⚠⚠ **TWO BANKED PINS NARROWED** (`o1PassWindup.test.ts`'s G7 and INT-MATE argument-list
   assertions) — the DF-T0 §P7 situation exactly, handled in the DF-T0 form (narrow, never
   delete; state it positively; preserve the owner's substantive claim; flag it). Full text
   in §PINS. **FLAGGED FOR COMMANDER RATIFICATION.**
2. ⭐ **THE DEPOSIT IS SPELLED AS AN OBJECT LITERAL, NOT `v2(…)`, AND THE FORK'S COMMENT
   AVOIDS THE PINNED LITERALS — DELIBERATELY.** PTP-T0's banked `G-PINS` asserts
   `passCase.indexOf('match.o1PassWindup') < passCase.indexOf('v2(bestLeadX, bestLeadY)')`,
   an ordering claim ABOUT THE LED STRIKE STATEMENT; writing my deposit with that spelling
   (or naming it in prose above the fork) would have silently re-pointed that pin at my
   statement instead. The value is identical (`{ x, y }` is what `v2` returns). Disclosed
   because it is a real editorial constraint the next executor will meet: **a pinned test
   is a STOP, and the cheapest honest answer is often not to trigger it.**
3. **NO LEDGER COUNTER WAS ADDED.** A `dxAimCarried` counter was written and then removed:
   `o1PassWindup.test.ts` pins the WHOLE `o1WindupLedger` shape with `toEqual`, so a new key
   would have narrowed a third banked pin for a convenience this dispatch did not order.
   DX-T1 reads the carry from `pendingPassWindup.aimLead` or from its own instrument
   (§HONESTY 8).
4. **THE CHANNEL IS THE PW-T0b DEPOSIT IDIOM, NOT A NEW ARGUMENT.** `armPendingPass`'s
   signature and its ONE call site are pinned VERBATIM by two banked suites; the deposit
   form carries the value without touching either, exactly as PW-T0b did for the weight
   (#291.5 correction 4). This adds two local bindings and one record field to the seam
   map, all counted and sited.
5. **NO PERF READING IS PUBLISHED** (§HONESTY 5). The dispatch ordered no cost face and one
   was not invented.
6. **`PROGRAMME.md` / the rulings file are NOT edited by this session** (executor iron
   rule: governance files are the commander's). The queue's status line, the ratification
   of §DEV 1, and the ruling are the commander's to write.

## §COMMANDER CORRECTIONS OF RECORD (#353, 2026-08-26)

1. **The G-INERT "written ZERO times" claim is RE-SCOPED** (verify MED): the counter
   samples `dxStrikeAim` at step boundaries, but a CARRIED deposit is written and
   consumed inside the same step — the counter detects UNCARRIED deposits only and is
   blind to exactly the case the wording excluded. The substantive M-DX.2 claim stands
   on what actually proves it: the 4-cell byte-identity walks (a carried deposit that
   changed behaviour would move a digest). The claim of record is now: "no deposit
   SURVIVES a step uncarried, and no deposit changes unarmed behaviour (byte-proven)".
2. **The deposit-side behavioural pin is ORDERED INTO DX-T1's §P** (verify LOW): a
   captured armed-world decision must assert `pendingPassWindup.aimLead` equals the
   elected candidate's own displacement — closing the one link currently held by a
   source-text pin alone (the fork line, pinned character-for-character).
3. **Two frozen-half notes** (verify LOW): §HONESTY 1's "§R2's fixture" should read
   §R3 (a forward cross-reference that landed one section off — the frozen text stands,
   this note is the pointer); and the elected deposit is NOT cleared when the wind-up
   branch is not taken — unreachable in practice (single arm site, gid+tick keying,
   one decision per body per tick) and byte-proven inert, now STATED as a limit rather
   than left as undisclosed residue. DX-T1's instrumentation may clear-or-read it
   explicitly if its counters need to.
