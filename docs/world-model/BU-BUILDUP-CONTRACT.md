# BU — 组织进攻 (THE BUILD-UP CONTRACT: the assembly arc — teaching the world to keep the ball on purpose)

> Drafted by the commander 2026-08-15 under the user's play verdict #274
> (「没有build up」 — disease 2 of record) and their 自走 of this date; bound
> by ruling #285. THE SHAPE OF THIS ARC IS DIFFERENT: build-up's missing
> parts are mostly ALREADY BUILT and dormant — this is an ASSEMBLY-AND-
> MEASUREMENT arc over banked seams, not a new engine cut. New cuts, if any,
> must be argued against the banked inventory first.

## §0 The diagnosis chain (#274.3, the measured faces)

Spells 4.17 s vs real 9.6–10.4 · touches/spell 2.56 (real ≥ 2.88) · pressed
receptions ~84 % · pass completion 66 % armed / 74 % bare. The mechanism map
(R-甲's corrected rows + the census record): **E7 — support is ALWAYS ahead
of the ball** (aheadBias 0.75/0.35; nobody comes short; the check-to-ball
axis is the dormant CTB seam) · **A10 — hold-up play extinct** (5/120,000;
the B9 lay-off bonus keys to it, dead) · E10 — no between-the-lines
reference in attack · D2 — no directional first touch (receptions spray) ·
B4 — one pass pace per range. Plus the two treated upstreams: the swarm
(L3, user-confirmed) and the carry threat (CB). ⭐ THE STRUCTURAL HALF IN
ONE SENTENCE: even an unpressed carrier has NOWHERE BACKWARD TO GO — the
world cannot circulate because the circulation options do not exist, and
where they exist the pricing has never been asked to value them.

## §1 The claims

* **H-BU.1 (scored, mechanism grain)**: with the short-support axis armed
  (the CTB seam family) in the polished world, REAL backward/lateral
  options exist at pressed receptions (option-existence measured at
  reception grain, a world event count) AND the one table uses them
  (backward/lateral completed-pass share rises resolvedly; usage > 0 at
  claim grain). 支点回接的存在与被使用 — capability + choice, no tempo
  promise.
* **H-BU.2 (REPORTED, never gated)**: the chain faces move toward real
  football on the standing gap table (Q01 spells · Q05 touches/spell ·
  Q06 completion · Q14 pressed share) — the R-乙 institution is this
  arc's before/after ruler (the re-run clause fires per arc slice).

## §2 Mechanism (M-BU — ASSEMBLY, each slice dormant-armed and measured)

* **M-BU.1 — THE INVENTORY IS THE LAW**: the arc assembles BANKED seams —
  CTB (check-to-ball support) · MT keep/hold (#213) · the EK hold beliefs ·
  the DV learned risk maps (#259) · O1/O2 (wind-up + outlet) · the B9
  lay-off channel (keyed to hold-up). A new engine cut is admissible ONLY
  where the census (BU-C0) proves no banked seam covers the hole, argued
  row by row.
* **M-BU.2 — COMPOSITION DEBTS BIND**: ⭐ the #269.2(iv) NAMED DEBT is due
  here — any world arming o2Look / ekHoldVeto alongside the CB/L3 stack
  must prove the ARMING LIFECYCLE at that composition FIRST (the
  clearTouchPastArming staleness class); the doors matrix grows to the
  full composition; worker-fixtures-play-shipped-world (#283.2(iv)) is
  stated in every arm design.
* **M-BU.3 — PRICING HONESTY**: no taste constants; where backward passes
  are under-priced (passBackPen, the aheadBias family), the fix is ARMING
  THE VALUE KNOWLEDGE (DV maps price the risk honestly), never hand-
  nudging a weight. The #200 line holds: capability + honest prices, then
  the chooser decides.
* **M-BU.4 — SCOPE**: no coach channel, no perception latency (their own
  gated arcs), no formation redesign, no new pass mechanics (B4's pace
  axis = a named LATER capability slice, not this arc's). Flags off ⇒
  byte-identical, per seam, as always.

## §3 Instruments & the arc

* **BU-C0 — THE RECEPTION-OPTION CENSUS** (instrument-only, first): in the
  polished world (v7's arming as the base — the treated swarm is the
  honest background), photograph the CIRCULATION STRUCTURE: at every
  reception and every pressed carrier moment — how many teammates offer a
  BEHIND-THE-BALL option (position + reachability, the engine's own pass
  machinery as the reachability oracle); the direction mix of completed
  passes (the R-乙 Q07 instrument); how spells actually END (the terminal-
  event census: tackled / forced long / bad touch / pass into pressure);
  where the aheadBias structure puts bodies at reception moments (the E7
  row MEASURED at world grain). #246 pre-registered: real build-up teams
  keep 2–3 behind-ball options; ours expected ≈ 0–1 (the E7 structure) —
  an inversion routes to diagnosis. The census PICKS the slice order by
  arithmetic (which missing option, if armed, binds first).
* **BU-T0.. — THE ASSEMBLY SLICES** (order set by BU-C0): each slice arms
  ONE banked seam family in the composition, proves the composition debts
  (M-BU.2), and measures option-existence + usage + the gap-table faces
  (H-BU.1 scored at the slice that arms the support axis; the rest
  REPORTED).
* **Exit** = the play-test (USER GATE): 能看出他们在组织进攻吗 — the user
  named the disease; the user closes it.

## §4 Non-claims

No tempo cure promised (the faces are reported; the contract promises the
OPTIONS and their honest pricing). No new mechanics without a census-proved
hole. Nothing ships without the play-test.

## §6 VISION audit (the #91 form)

* vs §-1: 配合 made visible by EMERGENCE — options + honest prices, the
  chooser and the learners do the rest; no scripted patterns. PASS.
* vs 底座给能力: the arc arms capabilities that exist and knowledge that
  was earned; content stays per-team. PASS.
* vs #200: no hand-painted weights; under-pricing is fixed by arming
  knowledge, not nudging constants. PASS.
* vs the assembly idea: reusing banked seams IS the substrate philosophy —
  build once, compose honestly. PASS.

## §7 REALITY audit (the #201 rule)

* Real build-up is exactly this decomposition: someone comes short (the
  option), the carrier trusts the risk map (the price), the receiver can
  take it (capability), the team has time (the treated swarm). PASS.
* Honest limits, stated: real circulation is also coached shape (the
  pattern half is the coach channel's, later) and needs the receiving
  crafts (D2 directional touch, A10 hold-up body work — capability slices
  this arc may PROVE missing but does not build without the census's
  proof). Slice one opens options + prices; texture follows. PASS.
