# IN — 拿住球买信息 (THE PRIVATE-SNAPSHOT CONTRACT: INFO-DOCTRINE slice 2 — scanning as an act, staleness as the price of not looking)

> Opened by the USER twice: #169 (没有拿住球/抬头/护球) and #303 item 3(viii), verbatim:
> 「现在球员感觉还是不喜欢拿住球,可能因为拿住球能不能买信息和抬头观察?我们之前所说的
> 信息差?」; held at priority through #310/#311; bound by ruling #316. THE DOCTRINE'S OWN
> WORDS (INFO-DOCTRINE.md §1 primitive 3, verbatim): "**私有快照 + 注意力/视野/身体朝向** —
> slice 2 (scanning as an act; staleness; 侧身)". And the user's mechanism story
> (INFO-DOCTRINE §-1(3), verbatim): 「比如抬头观察,好的中场队员不仅抬头观察更频繁(能实时
> 获取信息)并且能在接球前观察(预判队友和对手)还有一点,就是能在球到来的时候基本上零处理
> 时间就传到应该传到的人了(因为他知道人在哪里)」.

## §0 The diagnosis chain (why holding buys nothing today)

Slice 1 (the PC arc, #296→#303 CONFIRMED) built PROCESSING TIME: a surprised body pays a
recognition-tiered delay. But perception itself is still FULL TRUTH — every decision reads
every body's true position every tick, so looking buys NOTHING, scanning cannot exist as a
choice, and the chooser correctly never pays for time on the ball (拿住球 has no product to
buy). The engine ALREADY OWNS the ingredients: (a) a BLINDNESS ALGEBRA — the shipped
`blind = (1 + heading·dir)/2` form with CONTACT_BLIND_PEN / DEFLECT_BLIND_PEN
(constants.ts:132/:145) prices facing on CONTACT; nothing prices facing on KNOWLEDGE;
(b) a TURN SUBSTRATE — TURN_RATE + the BK facing law (a body that must turn to kick is the
body that must turn to LOOK, #304 M-BK.4's note); (c) a DORMANT LOOK — the banked o2Look /
lookSeat seam (its composition discharge is a standing debt that falls due here if slice 2
extends it). What is missing is exactly the doctrine's primitive 3: the PRIVATE SNAPSHOT
that goes stale, and the LOOK that refreshes it.

## §1 The claims

* **H-IN.1 (scored, mechanism grain, at the assembled composition)**: with the snapshot
  law and the look armed and priced through EXISTING machinery, (a) LOOKS ARE GENUINELY
  TAKEN (usage > 0 at claim grain, distribution by situation reported, not one degenerate
  corner) at their derived time cost; AND (b) INFORMATION DIFFERENTIATES OUTCOMES: at
  matched situations, the carrier acting on a FRESH snapshot chooses/releases resolvedly
  better than the one acting on a STALE one (the user's 中场 story made measurable —
  接球前观察 ⇒ 球到来时零处理传到应该传到的人). Capability + honest prices; NO usage
  promise beyond non-degeneracy.
* **H-IN.2 (REPORTED, never gated)**: the standing institutions — R-乙 chain faces (Q01 ·
  Q05 · Q06 · Q14), the pressing ratios (the doctrine's 时间预算攻击 should SHARPEN:
  press-immunity of the pre-scanned body), direction mix, goals/§2 faces, the corridor
  rung. Direction pre-registered: 拿住球 gains a PRODUCT (looks) — if holding usage rises
  it must rise because looks pay, never because a weight was nudged.

## §2 Mechanism (M-IN)

* **M-IN.1 — THE SNAPSHOT LAW**: a body's DECISIONS read his PRIVATE SNAPSHOT of other
  bodies (last-seen position/velocity), refreshed each tick for bodies inside his VISION
  FIELD, stale outside it. The vision field is DERIVED from the engine's own blind
  algebra (the (1+dot)/2 form and its shipped pens) — never a taste cone (#200). PHYSICS
  STAYS TRUTH: contact/collision/capture read the world (a body you do not see still
  blocks you); the BALL stays slice 1's domain (its surprises pay the PC latency; slice 2
  covers BODIES). Wrongness during staleness is FREE — the body plans against old
  positions and the world corrects him (slice 1's 延迟期间 principle extended from time
  to space; no new penalty terms).
* **M-IN.2 — SCANNING AS AN ACT**: the look is a BODY ACT with a TIME cost derived from
  the shipped facing/turn algebra (a look away from the ball is a turn paid at TURN_RATE;
  the census derives the exact form). The banked o2Look/lookSeat seam is the presumptive
  home — IN-C0 inventories it and rules extend-vs-new; if extended, the standing o2Look
  composition-discharge debt falls due IN THIS ARC. 拿住球买信息 becomes STRUCTURAL:
  holding = time = looks = freshness — the chooser then decides if that is worth buying,
  per situation, per genes.
* **M-IN.3 — NO NEW KNOBS**: no new attributes, no new genes in this slice; refresh
  quality/width may be wired to EXISTING attrs only where the census proves the wire
  (and the equilibrium lesson stands: any one-sided attacking-read sharpener inflates
  goals — symmetric or nothing). Recognition books stay slice 1's; shared/teammate
  snapshots (默契) stay the six-source cluster's; feints/leakage stay slice 3's.
* **M-IN.4 — SCOPE & DEBTS**: flags off ⇒ byte-identity, per seam; composition proof at
  the world-9 stack (M-BU.2 lineage — the doors matrix now includes both BK laws);
  pin suites from birth; the ⭐ standing observation binds every design choice here:
  "positioning is the latency-free answer (卡身位 · 侧身 · 封角 · 逼单边) — it must
  EMERGE from the learning layers, never be hand-coded" (INFO-DOCTRINE §1, verbatim).
  接球身体朝向 = 用站位买零处理 (the catalogue) must be an EMERGENT consequence of the
  snapshot geometry, never a written rule.

## §3 Instruments & the arc

* **IN-C0 — THE PERCEPTION-SURFACE CENSUS (instrument-only, first)**: (a) THE TRUTH-READ
  SURFACE — every site where a DECISION reads another body's position/velocity (needle
  counts, prefix stated; classed chooser-grade vs executor-grade vs physics-grade —
  physics stays truth by M-IN.1); the snapshot must interpose at a BOUNDED surface or the
  slice STAGE-STOPS. (b) THE VISION ALGEBRA INVENTORY — the shipped blind forms, pens,
  TURN_RATE, the BK facing substrate; the derived vision-field candidates with their
  algebra (the λ_LIN idiom: find where the engine's own prices bite). (c) THE o2Look
  INVENTORY — what the banked seam does, its debts, extend-vs-new material. (d) THE
  STALENESS-OPPORTUNITY CENSUS — in the world-9 base: how often decisions read bodies
  OUTSIDE the reader's derived vision field (the would-be-stale share), and a
  counterfactual staleness dose ladder at census grain (freeze those reads at k-tick-old
  positions; which choices flip) — the census PICKS the seam design, the refresh law and
  the slice order. (e) PERF SIZING — the snapshot bookkeeping bound (12 bodies × 11
  seen; the hot path budget stated).
* **IN-T0.. — the dormant seam slices** (order set by IN-C0): snapshot law · the look ·
  the exam at the composition · the entry rung. **Exit = the play-test (USER GATE)**:
  拿住球有用了吗 · 抬头的人踢得更明白吗 · 压迫压的是没看的人吗.

## §4 Non-claims

No promise that holding RISES (the chooser decides; H-IN.2 reports it); no scanning
patterns coached; no feints; nothing ships without the play-test; an all-scanning world
is a FAILURE mode (the look must cost real time, so situations must differentiate).

## §6 VISION audit (the #91 form)

* vs §-1 (tactics emerge): the slice arms an information SUBSTRATE (snapshots + a priced
  look); who scans, when, and the positioning that buys zero-processing all EMERGE from
  prices and learning — the standing observation is quoted as binding law in M-IN.4. PASS.
* vs 底座给能力: vision/attention is body capability (innate-legal); knowledge stays
  earned; content per-team. PASS.
* vs #200 (no taste constants): the vision field from the shipped blind algebra; the
  look's cost from TURN_RATE; no new attrs/genes (M-IN.3). PASS.
* vs the assembly law: slice 1 (PC) built processing TIME and is CONFIRMED at the gate;
  the BK arc built the turn substrate; the o2Look seam is banked dormant — this slice
  ASSEMBLES them into the doctrine's primitive 3; the census proves no banked seam
  already covers it (o2Look inventory is exactly that row-by-row argument). PASS.

## §7 REALITY audit (the #201 rule)

* Real football: elite players scan measurably more (the scanning literature the user's
  §-1(3) describes from lived experience); scanning happens BETWEEN touches and before
  receiving; a defender on your blind side is exactly the man you get robbed by; 侧身接球
  exists to see two things at once. The quantity this contract prices — the staleness of
  what you have not looked at, and the time a look costs — is the real one. PASS.
* Honest limits, stated: no eye/head model (the engine has ONE facing — head-independent
  scanning is approximated by the look act's turn cost; a finer head model is a later
  capability slice if ever); peripheral vision is a derived field, not a biological
  model; teammate knowledge via 默契 is deliberately NOT here (six-source cluster). The
  census decides which approximations bind first. PASS.
