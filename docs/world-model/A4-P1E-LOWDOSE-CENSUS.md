# A4-P1e — THE LOW-DOSE REFINEMENT CENSUS (certifying the resolved low-dose region of the P1d dose curve into a measured M3′ term)

Status: **PRE-REGISTERED 2026-08-03, FROZEN BEFORE ANY RUN. INTERVENTIONAL,
paired same-seed MULTI-DOSE forks over a FINE LOW-DOSE grid; the banked
default-OFF map seam (`Match.homeMapGrant`) set ONLY inside the probe's forked
branch, taking STRENGTH AS A PARAMETER — ZERO src changes.** This document
freezes — before a single census match is simulated and before any
`docs/world-model/data/*.json` is opened — the estimand, the R3p world
reconstruction, the HOME MAP (unchanged from P1d), the FINE low-dose grid (its
unit derivation), the PRIMARY dose (pinned from P1d's independent seeds BEFORE
this run), the fork-sampling rule, the horizon, the RAW deep-entry metric, the
NEW frozen gate (its exact predicate, with the monotone-dose leg dropped and the
dropping reasoned), the fork-context Simpson exhibit, the PRE-REGISTERED REPORTED
mediators (carried verbatim from P1d), the mid-run progress lines, the
sizing-before-floors plan, the seed pins (a ratified freeze), and the receipts
idiom. The probe (`scripts/probes/a4-p1e-lowdose-census.ts`) is the P1d probe
RE-PARAMETERIZED and is built in the SAME executor step as this freeze; the probe
carries no default mode; the two REAL runs (sizing smoke → census) are the
commander's detached resident (#49.5). **This step commits the freeze + the probe
locally and does NOT push** (the push rides the commander's adjudication ruling).
Nothing ships (Road B): `Match.homeMapGrant` AND `Match.homeRegionGrant` are
`null` in every production path, every EDS flag dormant, `stationEye` null in
production, the production fingerprint `57b0bdab…c673` unchanged throughout.

Authority: **ruling #146** (the user rules #145.5 = option A — A4-P1e, the
low-dose refinement census — and the green path RESUMES; the seven binding design
constraints #146.2.i–vii) under the **A4-ASSIGNMENT-CONTRACT §4** estimand family
(M1′ = the WHOLE agreed home distribution) and invariants **§3 I-A1..I-A7** (esp.
**I-A2/I-A4** hand-built = the coordinate frame + blending rule ONLY, the
formation IS the evolved home distribution; **I-A3** no free hand-weights — the
strength is a DOSE in instruments, a gene when shipped; **I-A5** Road B; **I-A6**
pre-registration discipline — gates freeze before runs, sizing before floors, the
attainability-knee N rule, wall-derived N_MAX, no optional stopping; **I-A7**
dormancy) · ruling **#145** (A4-P1d RAN AND STOPPED AS FROZEN: the primary dose
1.0× read NULL, but the dose curve is the arc's FIRST RESOLVED BENEFIT — resolved
at 0.25× and 0.5×, null at 1.0×, resolved HARM at 2.0×; the primary sat at the
crossing) · **#143** (the A4-P1d design — the whole-distribution grant census this
refines; the map seam, the mediators, the X-family, the N arithmetic form) ·
**#142/#140** (H5 counter-press thinning + the forensic mediator set, carried) ·
**#136/#132** (the semantics: coarse HOME PRIOR — 约定要"大概意思"才有效,"规定死"有害) ·
**#128** (the X-DET wall: the byte-compare reads a wall-FREE deterministic core) ·
**#127** (the Simpson sign-reversal tightening as an ex-ante NOT-ADVANCE conjunct;
vacuity ex ante) · **#94.3** (stratify-by-context Simpson) · **#20** (CI cluster =
match seed) · **#46.2** (seed disjointness) · **#48.4** (windows pinned ex ante) ·
**#49.3** (per-record receipts) · **#105.4** (optional stopping foreclosed) ·
**#134.2** (the executor follows the CODE's reality over the brief's letter and
FLAGS) · **#117** (a ratified freeze outranks any dispatch brief) · Road B
(nothing ships).

Instruments reused (SHA'd, published — freeze honesty; every number below traces
here): the **P1d probe machinery** re-parameterized (the banked map seam
`Match.homeMapGrant` + the 2D `homeMapBias` + its `actionExecutor` threading,
committed at P1d `135d51a`/#144; the deep/box detectors, the mediator counters,
the fork/bootstrap engine — kept BYTE-IDENTICAL) · the **P1-calibration deep
detector** (`scripts/probes/stage3-v4-p1-calibration.ts` lines **326–344**;
box detector **337–343**) — REUSED VERBATIM · the **admitted P1 deep price**
(`L = 0.043455` [0.030790, 0.055817]) — LABELLED goal-value conversion ONLY; the
gate reads RAW deep-entry rates · the **P1 match-cluster bootstrap engine**
(`meanCI`, cluster = match seed, `B = 2000`) · the **injected P3p-1 merged table**
(`mergedTableSha 39662445…9d6105`, `baseSha 171a6dad…6559f`) + its control · the
**formation base-spot table** (`ATTACK_FORMATIONS`, `src/ai/formations.ts:24-59`)
· the production fingerprint `57b0bdab…c673`.

**World / HEAD / flags (#26.5 / #67.3).** Every census match runs the **ENRICHED**
world (`edsPerceivedDefence + edsPerceivedChoice + edsValueAxis` ON, `c5Hold`,
`c6Carry`, `c7Windup` ON, `c5TouchFork` off) with the **stationEye ARMED to the
R3p arm** (v3 base+children+`mergedTableSha`; v4 `{inSupportLaw:true,
deliveryBit:true, offsideBit:true}`; scope both) and **`homeMapGrant` NULL in the
base run** (as is `homeRegionGrant`). This is the same R3p world P1d's dose curve
lives on. The counterfactual (the MAP grant) is created ONLY inside branch B of
each fork. In production every EDS flag defaults OFF, `stationEye` is null, both
grants are null, and the fingerprint `57b0bdab…c673` is unchanged (see X-family,
§7).

---

## §1 — THE ESTIMAND + THE MOTIVATING EXHIBIT (why P1e, and why it is FINAL)

A4-P1e **refines the RESOLVED low-dose region of the P1d dose curve into a
certified measured term for M3′**. It is the **SECOND AND FINAL instrument on the
distribution question** (#146.1): it CLOSES the question either way. Branch A is
the R3p world as-is; branch B_dose grants **EVERY side-d OUTFIELD body his OWN
coarse 2D home** (unchanged from P1d) as a soft additive bias at the established
v3 station-scoring point, uniform strength = the dose. The estimand is the
**paired same-seed contrast on the RAW opponent deep-entry rate**:
`Δdeep(dose) = nDeepB_dose(10 s) − nDeepA(10 s)`, pooled over admitted forks, at
the PRIMARY dose (fraction 0.25).

**THE MOTIVATING EXHIBIT (P1d, ruling #145 — quoted with CIs; it does NOT gate
P1e).** The P1d whole-distribution census (8000 matches @ 12.10M, 160,000 paired
forks) STOPPED as frozen — the primary dose 1.0× read NULL — but delivered the
arc's **FIRST RESOLVED DISCIPLINE BENEFIT**, at low dose:

| dose | Δdeep (RAW) | CI | reading |
|---|---|---|---|
| 0.25× | **−0.0139** | [−0.0188, −0.0089] | **RESOLVED BENEFIT** |
| 0.5× | **−0.0084** | [−0.0137, −0.0030] | **RESOLVED BENEFIT** |
| 1.0× | +0.0038 | [−0.0013, +0.0092] | null (the frozen primary sat at the crossing) |
| 2.0× | **+0.0140** | [+0.0085, +0.0195] | **RESOLVED HARM** |

A clean monotone benefit→harm curve crossing zero between 0.5× and 1.0×. The
user's #132/#136 ontology, measured: 约定要"大概意思"(coarse, weakly binding)才有
效;"规定死"有害。 P1e is the certification pass on the BENEFICIAL side of that
crossing — on FRESH seeds, with a FRESH frozen gate.

---

## §2 — THE WORLD, THE FORK SAMPLING, THE PAIRED MULTI-DOSE FORK (frozen; = P1d)

### 2.1 The R3p arm
Reconstructed EXACTLY as the P1c/P1d census built it: the ENRICHED eye-null world
(`CENSUS_FLAGS`) + the ARMED R3p eye (v3 base+children+`mergedTableSha`; v4 all
three bits; scope both). Its arm gates **X-MERGE-IDENT** (the injected P3p-1
identity) and **E-NONSTATION** (the eye ACTIVATES and NEVER overrides the ball
carrier) are inherited HARD gates (§7). The base run carries the eye; both grants
null.

### 2.2 The fork-moment rule (P1d idiom, pre-registered)
Step the parent R3p match; a fork MOMENT qualifies iff `phase==='playing'` AND the
ball has an owner AND ≥ `FORK_SPACING_S = 4.0 s` since the last fork. `d` = the
current owner's side (an OWN-POSSESSION moment for side d). `FORK_CAP = 20`
forks/match (wall guard; the smoke reports whether it binds). The fork context =
side d's ball third at t_fork (own/mid/their) for the Simpson exhibit.

### 2.3 The paired multi-dose fork (ZERO leakage)
At each qualifying moment the base R3p state is cloned into INDEPENDENT deep copies
(`cloneSimulationState`): branch A (both grants null) + ONE branch B per dose
(shared branch A ⇒ the whole low-dose grid is same-seed paired). The parent is
NEVER stepped inside the fork. **X-FORK-IDENT (100 % coverage):** an independent
plain clone (both grants null) stepped `W_MAX_TICKS` must equal branch A's
signature (zero leakage). A fork tuple is EXCLUDED iff ANY branch (A or any dose B)
ends within the window (truncated horizon); exclusions are published, never pooled.

### 2.4 The horizon + the metric
A SINGLE primary horizon `W_PRICE_S = 10 s` (the certified P0b concede horizon).
Per fork, per dose: `Δdeep = nDeepB − nDeepA`, the RAW opponent deep-entry count
against side d in `(t_fork, t_fork+10 s]` — the P1 deep detector VERBATIM
(`oppOwns && phase==='playing' && localX(ball) < -HALF_L/3`, null→true). The
goal-value conversion (`× L_DEEP`) is LABELLED ONLY, non-gating.

### 2.5 THE HOME MAP (UNCHANGED from P1d — the seam is UNTOUCHED src)
Branch B grants EVERY side-d outfield body (index 1..5; GK excluded) his OWN
coarse 2D home, exactly as P1d banked it in src:

* **CENTER = HIS formation BASE spot** — `ATTACK_FORMATIONS[formationAtk][p.index]`
  (the team's EVOLVED formation, genome-selected — the world's own M2′ gen-0
  content, NOT a hand-authored shape).
* **EXTENTS — published-anchored, BOTH axes (#136), LIVING IN SRC:** half-depth
  `HALF_L/6`, half-width `BOX_WIDTH/4`, decay `HALF_L/3` (the constants banked in
  `stationEye.ts` at P1d, accepted at #144.2).
* **STRENGTH = the dose**, UNIFORM across the map's bodies.
* **THE BIAS FORM (M3′, no clamps):** the P1d 2D soft EXPONENTIAL (`homeMapBias`):
  full `strength` inside the axis-aligned home box, exp-decayed by the EUCLIDEAN
  distance to that box outside it — added to the body's per-candidate advantage at
  the v3 consumption point. Per-body homes are DERIVED in the consumption path.

**ZERO src changes (#146.2.i, FLAGGED to nobody — it is the design).** The map
seam takes `strength` as a parameter, so P1e re-parameterizes it with the fine
grid and touches NO src/test file. `git diff src` is EMPTY (unlike P1d, which
added the seam). Road B is proven by X-FP-PROD + the seam's own banked flag-off
byte-identity test (`tests/a4HomeMap.test.ts`), untouched here.

---

## §3 — THE FINE LOW-DOSE GRID (RAW rates; goal-value labelled)

### 3.1 The grid (the unit derivation, X-MERGE-gated)
Strengths = FROZEN FRACTIONS **{0.125, 0.25, 0.375, 0.5}** × `VAL_SCALE`, where
`VAL_SCALE` = the SD of the neutral-weight station value `val = 0.5·score −
0.5·concede` over the injected v3 base table's in-power cells — the eye's OWN
native score dispersion, **deterministic from the SHA-pinned merged table, NOT
from smoke**. Recomputed in-probe; it MUST equal **`0.163494`** (the P1c/P1d
X-MERGE-gated derivation; verified in the bounded preflight, §7 — the per-body
bias at 0.25× read `0.0409` = `0.25 × 0.163494`). The **ZERO ANCHOR** (dose 0 =
branch A vs itself = 0) is present by construction.

### 3.2 THE PRIMARY DOSE (pinned BEFORE this run, on independent seeds)
**PRIMARY = fraction 0.25×** — P1d's **STRONGEST RESOLVED RUNG** (Δdeep −0.0139
[−0.0188, −0.0089] on the independent 12.10M census). This is pinned from P1d's
result BEFORE the P1e run, on seeds DISJOINT from P1e's (12.20M census). The
provenance is stated explicitly so the pin cannot be read as post-hoc
data-dredging: the primary is chosen from LAST NIGHT's exhibit, not from this
census.

### 3.3 The pricing (RAW; goal-value labelled)
The GATE reads RAW deep-entry rates. The goal-value conversion (`× L_DEEP =
0.043455`, `[0.030790, 0.055817]`) is LABELLED ONLY, non-gating.

---

## §4 — THE FROZEN GATE (the NEW form; smoke data may NOT inform it)

At the PRIMARY dose (fraction 0.25), on the POOLED paired RAW Δdeep, match-cluster
bootstrap (cluster = match seed, `B = 2000`, `BOOTSTRAP_SEED = 100803`). PASS
requires **ALL**:

* **(a) RESOLVED BENEFIT** — pooled `Δdeep(primary 0.25×)` CI **UPPER < 0** (the
  MAP REDUCES opponent deep entries at the primary dose).
* **(b) NO RESOLVED HARM ANYWHERE** — no rung in the grid {0.125, 0.25, 0.375, 0.5}
  has CI **LOWER > 0** (the future gene range must be non-harmful across its WHOLE
  span).
* **(c) NO SIMPSON REVERSAL** — a Simpson-genre SIGN REVERSAL on the primary cell
  (pooled vs fork-context-standardized) is an **AUTOMATIC NOT-ADVANCE** (the #127
  form).

**THE MONOTONE-DOSE LEG IS DROPPED (#146.2.iii), with the pre-named reasoning:**
the P1c/P1d gate required dose-monotonicity (increasingly negative Δdeep with
dose) and a resolved top−bottom ladder. **Inside the beneficial region an interior
optimum is plausible** — P1d's own curve already shows the benefit is STRONGEST at
0.25× and weakens toward 0.5× before crossing to harm; a finer grid could easily
peak between rungs. Demanding monotonicity across the low-dose grid would therefore
be dishonest — it is not the claim P1e makes. The honest claim is: the primary
dose resolves BENEFICIAL, and NOTHING in the tested range resolves HARMFUL. So the
gate is a resolved-benefit floor + a no-harm ceiling, not a shape.

Empty pooled cell ⇒ leg (a) UNRESOLVED ⇒ STOP (attainability vacuity, declared ex
ante). Any leg fails ⇒ **STOP at A4-P1e** (I-A3: no certified non-harmful causal
term ⇒ no build), the distribution question **CLOSES**, return to the user. **PASS
⇒ A4-P2 opens** (the #145.5/#143.1 green path: commander review + a numbered ruling
gate the transition, no user veto on PASS); the **measured strength REGION becomes
M3′'s term** and the gene range spans the tested grid. The goal-value conversion is
LABELLED only.

### 4.1 CONFOUNDING — the fork-context Simpson exhibit (#94.3 / #127)
Stratified by fork context (side d's ball third at t_fork). WITH SAME-SEED PAIRING
this is a **HETEROGENEITY exhibit, NOT a confound repair**. The gate binds on the
pooled primary cell; a pooled-vs-standardized SIGN REVERSAL on the primary (0.25×)
cell is FLAGGED and forces NOT-ADVANCE.

### 4.2 THE SHAPE EXHIBIT (labelled, NON-GATING — early F-SLIDE sight)
At the TOP low-dose rung (0.5×), per-tick nearest-teammate spacing + dup-runs
(pairs < 4 m) for side d over the fork window, branch B vs A (also carried per-dose
in the spacing/dupRun mediators, §5). Early F-SLIDE sight only; the frontier shape
gates live at A4-P3.

---

## §5 — THE PRE-REGISTERED REPORTED MEDIATORS (carried verbatim from P1d — NOT a gate)

The #140 forensic counters, carried verbatim from P1d, computed for **branch A and
EVERY dose branch** (level + paired delta vs A), match-cluster CIs:

* **turnovers** — side-d possession losses (`possessionSide` d→opp; −1 dead-ball
  transients ignored) per window.
* **firstTurnoverTicks** — ticks-since-fork of the FIRST loss, **CENSORED at
  `W_MAX_TICKS`** when none (retention proxy).
* **turnoverThird** — the side-d-local third at the FIRST turnover; own-share
  reported.
* **boxEntries** — the P1 calibration box detector VERBATIM.
* **teamPassesForward / Backward** — side-d NEW `pendingPass` attempts, classified
  by the same backward test `passBackPen` uses.
* **spacing / dupRun** — nearest-teammate spacing + dup-run pairs < 4 m, sampled at
  6 Hz.
* **entriesPerOppPossession (derived, H5)** — pooled deep/box entries ÷ pooled
  opponent possessions per branch.

**H5 carried context (REPORTED, NOT a gate):** P1d CONFIRMED at low dose that the
whole-team grant does NOT thin the press (deep entries fall; spacing compacts
without clumping). The mediators tell whether that holds across the fine grid. The
commander reads the mediators beside the gate at review.

---

## §6 — SIZING BEFORE FLOORS + SEEDS (no optional stopping)

**The smoke** (`A4P1E_MODE=smoke`, 40 matches @ `12,150,000 + k`, k∈0..39)
realises the fork/dose populations, the eye consumption frequency, the pooled
Δdeep σ̂ **at the PRIMARY (0.25×) dose**, and the per-match wall INCLUDING
forks×doses; it double-runs for X-DET. **The frozen N arithmetic** (P1d form,
RAW-rate MDL): `MDL = min(0.5·|Δdeep_smoke@primary|, 0.01)` deep-entries/window;
`SE_N = σ̂·√(1/N)`; `N* =` smallest 200-step N with `SE_N ≤ MDL/POWER_Z`
(`POWER_Z = 3.605`), capped at a **WALL-DERIVED N_MAX** (`N·perMatchWall·2 ≤ 12 h`,
the ×2 for the X-DET double-run, #128) and a hard cap `N_CAP = 8,000` (keeps the
census band ≤ 12,207,999). **The reduced-power disclosure rule stands: if the cap
binds (N* > N_MAX), the census runs at N_MAX and the primary reads UNRESOLVED — the
disclosure is published, exactly as P1d's power-0.873 disclosure stood.** The
**attainability-knee**: if the priceable-fork cell or a gate stratum is too rare,
its leg reads UNRESOLVED ⇒ the gate STOPS. No optional stopping (#105.4). The smoke
data may NOT inform the gate.

**The census** (`A4P1E_MODE=census A4P1E_N=<N* from the smoke>`, @ `12,200,000 + k`,
k∈0..N−1, N ≤ 8,000 ⇒ ≤ 12,207,999) is the gate-bearing run — the commander's
detached resident (#49.5).

**PROGRESS LINES (#146.2.v — the overnight lesson).** The census loop prints one
**STDOUT** line every 500 matches (`match index / N`, elapsed, ETA). **STDOUT
NEVER enters the compared deterministic core** — the X-DET byte-compare reads only
the JSON body, which records no timing — so progress printing cannot move X-DET.
The interval is overridable ONLY in the bounded preflight (`A4P1E_PROGRESS_EVERY`)
so the preflight can exercise the line.

**SEEDS (#146.2.vi — a RATIFIED FREEZE; inside 11.7M–12.3M, disjoint from ALL
consumed blocks 11.70/11.80/11.85/11.90/11.95/12.00/12.05/12.10M):** smoke
**12.15M** (k 0..39); census **12.20M** (k 0..N−1). Stats: bootstrap **100803**;
**100903** reserved-unused. Disjointness is asserted IN-PROBE as a HARD gate
against the reservation and every consumed block (§7).

---

## §7 — DELIVERABLES + THE X-FAMILY / GATES TABLE

**Deliverables (this executor step, one local commit, explicit paths):**
1. This freeze (`docs/world-model/A4-P1E-LOWDOSE-CENSUS.md`).
2. The probe (`scripts/probes/a4-p1e-lowdose-census.ts`, modes smoke|census) — the
   P1d probe re-parameterized with the fine grid + the new gate + the progress
   lines. **ZERO src/test changes** (the map seam is banked; the probe imports it).
3. A bounded preflight (tsc clean; a tiny capped census run proving the four doses
   bind for MULTIPLE bodies, branches pair, records emit, X gates hold, the
   progress line prints; scratch deleted; NO data files committed).

**THE X-FAMILY (each a HARD gate; verbatim from P1d):**

| Gate | What it asserts |
|---|---|
| **X-DET** (#128) | the probe output (the JSON body) is byte-identical across a double-run; the machine wall + the stdout progress lines are measured OUTSIDE the compared core. |
| **X-FORK-IDENT** | an independent plain clone (both grants null) == branch A on EVERY fork (zero leakage), 100 % coverage. |
| **X-MERGE-IDENT** | the injected P3p-1 merged table `mergedTableSha`/rehash == `39662445…9d6105`, base == `171a6dad…6559f`; `VAL_SCALE = 0.163494`. |
| **E-NONSTATION** | the eye ACTIVATES on the R3p world (arm not vacuous) AND never overrides the ball carrier; body-/team-scope honoured. |
| **X-FP-PROD** | the production fingerprint `57b0bdab…c673` is UNCHANGED. `git diff src` is **EMPTY** (P1e touches no src); Road B = X-FP-PROD + the seam's banked flag-off byte-identity test. |
| **SEED DISJOINTNESS** (#46.2) | smoke 12.15M + census 12.20M inside 11.7M–12.3M, mutually disjoint and disjoint from every consumed block (through 12.10M). |

**BOUNDED PREFLIGHT EVIDENCE (2026-08-03, HEAD `de318c9`).** `tsc --noEmit` clean.
Capped census (3 matches @ 12.20M, `FORK_CAP=3`, `PROGRESS_EVERY=1`, `SKIP_FP`,
`BODY_LOG`, writing to `/tmp`): the **progress line prints** on both X-DET runs
(`1/3 · 2/3 · 3/3`); the map binds for **5 distinct per-body homes**
(`narrow-122`: DF/MF/WG/WG/ST); the **four doses bind at distinct magnitudes** (at
a candidate 10 m in front of body-1's home: 0.125×→0.013, 0.25×→0.026,
0.375×→0.039, 0.5×→0.052); `branchB(0.25×)!=branchA` and `branchB(0.5×)!=branchA`
both TRUE (branches pair AND diverge under grant); `VAL_SCALE` bias at 0.25× read
`0.0409 = 0.25 × 0.163494`; records emit (Δ, per-rung deltas, `noHarm`); `xDet
true · xFork true · xMerge true`. Numbers on 3 matches are meaningless by design;
scratch deleted; NO data file committed.

**Percept-honesty note (instrument, fork-branch-only).** The MAP grant is an
INSTRUMENT in fork branches (soft per-body home priors in offline clones), NOT a
consumer ship. No live percept is created/read/leaked; no gene, no M1′–M5 build.
Hand-built = the coordinate frame + the blending rule + the published extents ONLY;
WHAT the distribution is worth is measured (I-A2/I-A4 诚实张力).

---

## §8 — THE PRE-LAID READINGS (the full sign space; none re-cut after sight)

* **PASS** — pooled Δdeep(primary 0.25×) CI upper < 0, no rung resolves harm, no
  Simpson reversal, empty-cell not triggered ⇒ CERTIFIED low-dose discipline
  benefit ⇒ **A4-P2 opens** (#145.5/#143.1); the measured strength REGION = M3′'s
  term, the gene range spans the grid. The distribution question CLOSES
  affirmatively.
* **NOT-ADVANCE (reading E, #127)** — legs (a)+(b) pass but a Simpson sign reversal
  fires on the primary cell ⇒ returns to the user with both exhibits.
* **STOP** — leg (a) fails (no resolved primary benefit / empty-cell vacuity) OR
  leg (b) fails (a rung resolves HARM anywhere in the grid) ⇒ **STOP at A4-P1e**
  (I-A3), the distribution question CLOSES, returns to the user. A non-replication
  of P1d's low-dose benefit is one STOP reading — reported honestly as such, with
  the mediators telling the story.

Whatever the gate reads, the mediators (§5) are reported. **P1e is the SECOND AND
FINAL instrument on the distribution question — it closes the question either way.**

---

## §9 — REGISTERED NON-CLAIMS + THE STAGE EXIT

* **Refines the MAP's LOW-DOSE discipline value, builds NO consumer** — no M1′–M5,
  no gene, no shipped consumer (A4-P2/P3).
* **The map grant is an INSTRUMENT** (soft per-body home priors in fork branches),
  not a percept ship; the per-body centre is the world's own formation variable.
* **The home map is COARSE + BOTH-AXES** (#136): a per-body 2D box home; convex
  regions cannot express bimodal homes.
* **The goal-value conversion is LABELLED ONLY**; the gate reads RAW rates.
* **Nothing ships (Road B)**: both grants null in production, fingerprint
  `57b0bdab…c673` unchanged; ZERO src/test changes.
* **P1e is the SECOND + FINAL planned pricing instrument on the DISTRIBUTION
  question** (#145.5/#146.1): it CLOSES the question either way; a third needs a
  fresh user ruling.
* **A4-P1e cannot authorize A4-P2**: only the commander's review of the census
  result opens A4-P2 (#143.1).

Rulings anchored: #146 / #145 / #143 / #142 / #140 / #136 / #132 / #128 / #127 /
#94.3 / #20 / #46.2 / #48.4 / #49.3 / #105.4 / #134.2 / #117; the P1 calibration
deep + box detectors; the P1c/P1d grant/dose machinery + the #140 forensic
counters; the P3p-3 battery R3p arm; the production fingerprint `57b0bdab…c673`.
