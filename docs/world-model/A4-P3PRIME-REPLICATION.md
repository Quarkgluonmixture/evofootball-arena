# A4-P3′ — THE REPLICATION EXAM (H-A4.1 re-examined on the AMENDED RULER)

Status: **PRE-REGISTERED 2026-08-04, FROZEN BEFORE ANY RUN.** Deliverables: this
pre-registration + the probe
[`scripts/probes/a4-p3prime-replication.ts`](../../scripts/probes/a4-p3prime-replication.ts)
(built with this doc, bounded-preflighted). **ZERO `src/**` changes** — the
home-map / home-prior seams are BANKED (map-grant `Match.homeMapGrant` at #144;
the shipped-form gene `homePriorObedience` + master flag `eye.v4.homePrior` at
#149). **The runs are a FUTURE authorized step** (freeze → review → run; §0.0).
**Nothing ships (Road B); the fingerprint `57b0bdab…c673` stands; the verdict
RETURNS TO THE USER regardless of outcome (#152.4.ii).**

Authority: **#152.4** (A4-P3′ dispatched — ZERO src, the P3 probe FORKED, the NEW
frozen gate, the fresh seeds, the descriptive reshuffle, the deep-leg N
arithmetic, progress every 250 sets, the #128 wall-free X-DET) · **#152.1** (the
P1e REPLICATION structure: the signals were SEEN, so the honest test is a frozen
gate on VIRGIN seeds — never a re-read of seen data; the #145.5-B refusal
carries) · **#152.2** (the contract §1 RULER AMENDMENT, quoted in §1 below) ·
**#151** (the first exam: the frozen gate FAILED F-SHAPE-ONLY — the record stands
unerased — while the REPORTED currency improved) · **#150** (the both-sides prior
seam ratified after commander code inspection) · **#149** (the five arms; the P2
gene/flag) · **#148** (the certified 0.25×VAL_SCALE primary dose; obedience 0.5) ·
**#145/#146** (the dose curve) · **#123/#121/#120** and
[`STAGE3-V4-P3P3-BATTERY.md`](STAGE3-V4-P3P3-BATTERY.md) (THE FORM: the five-arm
attribution chain, the whole-match instruments, the wall-derived N rule, the
X-family) · the contract [`A4-ASSIGNMENT-CONTRACT.md`](A4-ASSIGNMENT-CONTRACT.md)
**§1** (H-A4.1 + the #152 amendment), **§5** (the five arms), **§6** (the FAIL
modes — superseded for THIS exam by the #152.4.ii disposition) · the template
[`A4-P3-FRONTIER-BATTERY.md`](A4-P3-FRONTIER-BATTERY.md) (this doc forks it).
Method: [`../PROBE-CONTRACTS.md`](../PROBE-CONTRACTS.md) · #20 (cluster = match
seed; paired) · #46.2 (seed disjointness) · #68.2 (re-baseline on the world's own
R0) · #105.4 (optional stopping foreclosed) · #128 (wall measured outside the
X-DET core) · #49.5 (the resident supervises the long run).

**World / flags (#67.3).** Every run uses the **ENRICHED** world verbatim
(`edsPerceivedDefence + edsPerceivedChoice + edsValueAxis` ON, `c5Hold` /
`c6Carry` / `c7Windup` ON, `c5TouchFork` off). In production every EDS flag is
OFF, `stationEye` is null, the `eye.v4` flags (incl. `homePrior`) are absent, the
`homePriorObedience` gene is born-absent, and the fingerprint stays
`57b0bdab…c673`. No `src/**` change runs this stage.

---

## §0 — PURPOSE (why this exam exists, in plain football language)

The first exam (#151) asked: *does the kickoff agreement make the team defend
better without making it look worse?* It answered with the OLD yardstick — stand
in the designated slot, keep the old spacing — and said NO. But on the same runs,
the thing the whole pricing arc certified as the real cost of bad defending — **how
often the opponent gets into your third and into your box** — moved the other way:
the agreeing team CONCEDED LESS ground.

Those currency numbers were **REPORTED, non-gating, and SEEN**. Seen numbers are
not evidence you may promote after the fact (#145.5-B). So this stage does exactly
what A4-P1e did for the dose curve: **it re-freezes the seen signal as a gate and
re-runs it on VIRGIN seeds.** If it replicates, the frontier verdict is AFFIRMED on
the amended ruler and the slice returns to the user for the closing ruling and the
play-test. If it does not, H-A4.1 is unsupported on the honest ruler too, and we
stop.

**THE MOTIVATING EXHIBIT (quoted honestly; #151, the SEEN values on `PRIOR − R3p`
over 800 sets @ 12.21M — context only, never a threshold, never re-read):**

| seen quantity (#151) | value | role HERE |
| --- | --- | --- |
| **deep entries per set** | **−1.49/set** | ⭐ leg (a) replicates this |
| **box entries per set** | **−0.35/set** | ⭐ leg (b) guards this |
| restart ticks per set | **+161/set** | descriptive; FLAG above +322 |
| PRIOR-arm roleMixTV | **0.434** (R3p 0.556) | leg (c) floor is the incumbent 0.407 |
| DEGEN-RESTDEF occupancy | **−0.040** | DESCRIPTIVE (was the #151 frozen leg (i)) |
| spacing median · under-4 m · dupRun · scramble | −0.64 m · +0.0072 · +0.038 · −0.047 | descriptive (scramble is a leg-(c) hard gate) |

The plain-eye readings (#151: deep −4.38, box −0.96) are context too. **Nothing in
this table is a threshold in this exam.** The gate reads THIS run's fresh seeds.

---

## §1 — THE CLAIM UNDER EXAM + THE #152 RULER AMENDMENT (quoted verbatim)

> **H-A4.1 (the frontier claim, as re-scoped by #136/#137):** pre-match,
> per-head, gene-weighted **coarse home-region priors** — the agreement layer
> "你踢后卫" actually establishes (a soft anchor, not a job law; the P1/P1b
> measurements are the proof that the PRIOR, not the LAW, carries the value) —
> let an eye-consuming team recover the assignment-shaped discipline residual
> **without paying shape**.

> **#152 RULER AMENDMENT (2026-08-04, user-ruled after the #151 verdict).**
> The original H-A4.1 readout (DEGEN-RESTDEF + the P3p-3 proximity-shape
> limbs) was examined AS FROZEN and FAILED (#151 — the record stands,
> unerased). The instruments predate the P1 pricing arc, which certified
> ENTRIES (deep/box) as the causal discipline currency and showed the
> occupancy/proximity readouts price the OLD hand-authored shape. **P3′
> re-examines H-A4.1 on FRESH seeds with a NEW frozen gate** (the P1e
> replication structure): discipline = the certified currency (deep
> entries fall resolvedly; box does not worsen); the retained FOOTBALL
> hard gates = scramble (乱抢, VISION §2), ball-ledger 0, roleMixTV ≥ the
> incumbent floor, the X-family; the PROXIMITY readouts
> (spacing/under-4m/dupRun/occupancy) become DESCRIPTIVE — for a
> compact-BY-AGREEMENT mechanism their verdict authority passes to the
> USER's play-test (VISION §2: watchability has no instrument). User
> diagnoses recorded with the amendment: per-body obedience offsets =
> the named slice-2 candidate (M2′); a substrate that can PUNISH
> compactness (强弱侧 / opponent-responsive shape / co-evolution) = the
> long-line program that eventually retires ALL hand shape-judging.

This exam is that re-examination. Falsifiable both ways; the disposition for
EVERY outcome is pre-registered (§4.1).

---

## §2 — THE FIVE ARMS (contract §5 / #149.4 / #150.1) — UNCHANGED from A4-P3

All arms at DEPLOYMENT scope (`{kind:'both'}`) except the null control. The
**same N seeds are paired across every arm** (#20; cluster = match seed) —
**one seed = one five-arm set**; every contrast is a paired per-seed delta with a
match-seed cluster bootstrap CI.

```text
R0     CONTROL  stationEye = null                                        — the paired ENRICHED baseline (#68.2)
R3v3   BOTH     v3:{roleTable,control}; NO children; NO eye.v4           — the PLAIN v3 eye (P3p-3's R3v3 VERBATIM)
R3v3p  BOTH     R3v3 + the home prior BOTH SIDES (0.25×VAL_SCALE)        — the prior on the PLAIN eye
R3p    BOTH     v3:{…,children,mergedTableSha}; v4:{law,delivery,offside} — the P3p-3 R3p arm VERBATIM
PRIOR  BOTH     R3p + the home prior BOTH SIDES (0.25×VAL_SCALE)         — the exam arm (the gate reads PRIOR − R3p)
```

**The prior seam (RATIFIED at #150.1 — no longer an open flag).**
`Match.homeMapGrant` is structurally single-side, so a symmetric both-sides grant
is unreachable zero-src; the prior is injected on BOTH sides via the P2
shipped-form seam `eye.v4.homePrior === true` + `homePriorObedience = 0.5`
(**probe fixtures only**). Commander code inspection certified the equivalence: the
mapGrant and gene branches construct the IDENTICAL `homeMapBias` closure over the
IDENTICAL `ATTACK_FORMATIONS` home at the IDENTICAL consumption point; only the
SOURCE of the strength differs, and `homePriorStrength(0.5) = 0.25×VAL_SCALE =
0.040874` is asserted by the in-probe **priorEquivalence HARD gate**. The exam
therefore reads the mechanism in its SHIPPED form (the #150.1 bonus).

---

## §3 — INSTRUMENTS (identical to A4-P3; only their ROLE is reshuffled)

Every instrument is the A4-P3 battery's, unchanged; cluster unit = match seed.
The gate binds on **`PRIOR − R3p`**, read as the **resolved SIGN** of the paired
cluster CI (the #88.2 form).

```text
deep entries   P1 detector (both sides) opponent → own-third transitions / set        — ⭐ GATE leg (a)
box entries    P1 detector (both sides) opponent → own-box transitions / set          — ⭐ GATE leg (b)
scramble       I4 own-within-5 m       mean count within 5 m of the ball (side-avg)   — ⭐ GATE leg (c) 乱抢
eye ball-ledger                        eye-attributable + unattributable releases = 0 — ⭐ GATE leg (c)
role-mix TV    pairwise role TV        PRIOR-arm mean ≥ 0.407 (the incumbent floor)   — ⭐ GATE leg (c)
X-family       §7                      every gate HARD                                — ⭐ GATE leg (c)
--------------------------------------------------------------------------------------------------------
spacing        median · under-4 m      quantile(pairs,0.5) · share<4 m (side-avg)     — DESCRIPTIVE
dup-runs       I6 duplicate-run share  (side-avg)                                     — DESCRIPTIVE
DEGEN-RESTDEF  I5(b) designated slot   restSlotShare over possession ticks (side-avg) — DESCRIPTIVE
C-BOX          box at cross arrival    crossArrival-weighted attackers in the opp box — DESCRIPTIVE
restart ticks  restart occupancy       ticks in a restart state / set                 — DESCRIPTIVE + FLAG
turnovers      possession losses / set                                                — DESCRIPTIVE
telescope      R3v3−R0 · R3v3p−R3v3 · R3p−R3v3 · PRIOR−R3p · PRIOR−R0                 — DESCRIPTIVE
per-arm levels restSlot, deep, box, restart, offsides, delivery, turnovers, goals     — DESCRIPTIVE
```

⚠ Note the two "box" readouts are different instruments and must not be confused:
leg (b) reads the **P1-family box-ENTRY currency** (opponent → own box); **C-BOX**
(attackers in the opponent box at cross arrival) is a watchability readout and is
DESCRIPTIVE here.

---

## §4 — ⭐ THE FROZEN REPLICATION GATE (#152.4.ii) — frozen ex ante, verbatim

On the paired **`PRIOR − R3p`** contrast (cluster = set seed; the battery's own CI
machinery — bootstrap 101203, B = 2000):

* **leg (a)** — **deep entries per set RESOLVEDLY FALL: CI UPPER < 0.**
* **leg (b)** — **box entries per set do NOT resolve UP: CI lower ≤ 0.**
* **leg (c)** — **the football hard gates:** scramble I4 not resolved-up (CI lower
  ≤ 0), eye ball-ledger = 0, PRIOR-arm roleMixTV ≥ 0.407 (the incumbent floor),
  and **every X-family gate HARD** (§7).

**PASS := (a) ∧ (b) ∧ (c)** ⇒ **the frontier verdict is AFFIRMED on the amended
ruler**; slice 1 returns to the user for the closing ruling + play-test. **Any leg
fails ⇒ STOP, return to the user** (the replication failed; H-A4.1 unsupported on
the honest ruler too).

Every reading is pre-registered:

| leg | reading (frozen) | #151 seen value (context only) |
| --- | --- | --- |
| (a) deep entries | `resolved ∧ upper < 0` | −1.49/set |
| (b) box entries | `¬(resolved ∧ lower > 0)` | −0.35/set |
| (c) scramble I4 | `¬(resolved ∧ lower > 0)` | −0.047 (improved) |
| (c) eye ball-ledger | PRIOR eye-attributable + unattributable `= 0` | 0 on every armed arm |
| (c) roleMixTV | PRIOR-arm mean `≥ 0.407` | 0.434 |
| (c) X-family | all HARD gates true (§7) | all ten true at #151 |

No re-cutting after sight; the predicate never changes (I-A6). A Simpson-genre
reversal check is N/A — the gate reads a single paired contrast, no pooling.

### 4.1 The pre-registered disposition for ALL outcomes

| condition | disposition | meaning |
| --- | --- | --- |
| a HARD gate fails | **STOP** (measurement invalid) | X-family / structural gate down — read nothing else |
| (a) ∧ (b) ∧ (c) | **PASS — the replication holds** | the frontier verdict is AFFIRMED on the amended ruler; slice 1 returns for the closing ruling + the user's play-test |
| any leg fails | **STOP — the replication FAILED** | H-A4.1 unsupported on the honest ruler too; nothing ships, everything dormant |

In EVERY case the arc RETURNS TO THE USER, in plain football language (§0.0.6).
The contract §6 four-way classifier (F-SLIDE / F-NULL / F-SHAPE-ONLY) is
**superseded for THIS exam** by the #152.4.ii two-way disposition; the #151
F-SHAPE-ONLY record stands unerased.

---

## §5 — DESCRIPTIVE / REPORTED (labelled, NONE gating)

**The ruler-amendment rationale, stated verbatim (contract §1, #152):** *the
PROXIMITY readouts (spacing/under-4m/dupRun/occupancy) become DESCRIPTIVE — for a
compact-BY-AGREEMENT mechanism their verdict authority passes to the USER's
play-test (VISION §2: watchability has no instrument).*

Each reading carries its #151 observation as quoted context:

* **spacing median** (#151 obs: −0.64 m — closed) — descriptive.
* **under-4 m share** (#151 obs: +0.0072 — rose) — descriptive.
* **dupRun share** (#151 obs: +0.038 — rose) — descriptive.
* **DEGEN-RESTDEF occupancy** (#151 obs: −0.0401 — the frozen leg (i) that failed
  the first exam; P1b proved this proxy carries no causal price) — descriptive.
* **C-BOX at arrival** (quiet at #151) — descriptive.
* **restartTicks** (#151 obs: **+161/set**) — descriptive **with a FLAG**: if this
  run's `PRIOR − R3p` restart contrast **RESOLVES with lower > +322/set** (double
  the seen cost), `restartFlagged` is raised and the disposition string carries the
  flag **FOR THE COMMANDER**. It never changes PASS/FAIL.
* **the full telescope chain**, per light limb (restSlot occupancy, restart ticks,
  deep entries, box entries), paired cluster CIs on the complete-seed set:
  `R3v3 − R0` (the plain-eye disease) · `R3v3p − R3v3` (the prior on the PLAIN eye
  — the honest attribution contrast, #149.3) · `R3p − R3v3` (the remedies on the
  plain eye) · `PRIOR − R3p` (the primary) · `PRIOR − R0` (the full frontier).
* **per-arm levels**: restSlotShare, deep entries, box entries, restart ticks,
  offsides, delivery events, turnovers, goals (goal-value labelled only).
* **the consumption ledgers** per armed arm (structural: `R3v3`/`R3v3p` read zero
  children; `R3p`/`PRIOR` read both) + receipts + the roleMix pairwise table.

---

## §6 — SIZING-BEFORE-FLOORS (the smoke → the frozen N arithmetic → the cap)

**Leg (a) — deep entries — is the binding gate**, so the smoke sizes THAT contrast
(#152.4.iv). Legs (b)/(c) are resolved-sign guards (over-powered at this scale).

* **The smoke** — 40 sets @ 12,220,000 + k (§7), all five arms, the light
  instruments, X-DET double-run. Publishes: the per-set SD of the paired
  `PRIOR − R3p` deep-entry diff (`σ̂`), its pooled point + cluster CI, the per-set
  wall, and the frozen N arithmetic. Box / occupancy / restart smoke contrasts are
  printed as smoke CONTEXT — **no leg is read at the smoke** (it sizes only).
* **⚠ THE MDL, WITH ITS DERIVATION FLAGGED (executor's call under #152.4.iv):**
  `MDL = min( 0.5·|smoke deep PRIOR−R3p point| , MDL_ABS )` with
  **`MDL_ABS = 0.75 deep entries/set`** = **HALF the #151 seen effect (−1.49/set)**
  — the pre-named ABSOLUTE floor, chosen so that a replication reproducing even
  half of the seen signal is resolvable at the frozen power; the `0.5·|point|`
  guard stops smoke noise INFLATING the MDL. Named BEFORE any battery seed runs.
* **The frozen N rule.** `N* =` the smallest 200-step match count with
  `SE_N = σ̂/√N ≤ MDL / POWER_Z`, `POWER_Z = 3.605 = z.975 + z.95` (a two-sided
  95 % CI test at 95 % power to resolve the fall away from zero), **CAPPED at
  N_MAX**.
* **N_MAX is WALL-DERIVED** at the smoke: the largest 200-step N whose projected
  total wall (`per-set wall × N × 2` for X-DET) ≤ **12 hours**, itself capped at the
  hard **N_CAP = 7,000** — the seed budget (which keeps the battery band ≤
  12,236,999, inside the reservation — §7).
* **The reduced-power disclosure rule.** If the power rule projects `N* > N_MAX`,
  the cap binds and the reduced-power reading is **DISCLOSED BEFORE the
  gate-bearing run** (no optional stopping, #105.4); leg (a) is then read from
  whatever power the cap delivers (resolved, or UNRESOLVED ⇒ leg (a) fails ⇒ STOP).
  If a gate stratum is genuinely unmeasured, a #44.5 sizing sign-off returns to the
  commander before the run.

---

## §7 — SEEDS (#152.4.iii) + stats + disjointness

```text
smoke     12,220,000 + k,  k ∈ 0..39                         (40 sets; virgin)
battery   12,230,000 + k,  k ∈ 0..N−1,  N ≤ 7,000            (⇒ ≤ 12,236,999; a SINGLE contiguous block)
stats     bootstrap 101203   ·   reserved 101303 (unused)
bootstrap 2000 resamples (the P3p-3 form)
```

* **ONE seed = one five-arm set**, paired across all five arms.
* ⭐ **The A4-P3 pinned block `[12,208,000, 12,217,999]` is treated as CONSUMED IN
  FULL** (#152.4.iii) — the first exam burned it (smoke 12,208,000..12,208,039 +
  the 800-set battery from 12,210,000); the whole pinned band is retired, no
  partial reuse.
* **Disjointness asserted in-probe** vs EVERY consumed block: P3p-3 smoke
  (11.150M), P3p-3 battery (11.2M–11.6M), A4-P1 (11.700M smoke / 11.800M census),
  P1b (11.850M / 11.900M), P1c (11.950M / 12.000M), P1d (12.050M / 12.100M), P1e
  (12.150M / 12.200M–12.207999M), **A4-P3 [12.208M, 12.217999M] in full**. The
  smoke and battery blocks are mutually disjoint and inside the reservation tail
  `[12,218,000, 12,300,000]`. Stats seeds 101203/101303 are asserted disjoint from
  every arc stats seed (including P3's 101003/101103).

---

## §8 — GATES TABLE (X-family, frozen — inherited VERBATIM from A4-P3)

| gate | class | predicate |
| --- | --- | --- |
| **priorEquivalence** | HARD | VAL_SCALE recomputed from the SHA-pinned merged table `=== 0.163494`; `homePriorStrength(0.5) === 0.25×VAL_SCALE`; `HOME_MAP_STRENGTH_MAX === 0.5×0.163494` |
| **X-FP-PROD** | HARD | production fingerprint `57b0bdab…c673` unchanged, every flag OFF, the gene absent (Road B) |
| **X-SRC-ZERO** | HARD | `git diff --stat -- src` empty (the seams are BANKED; this probe adds ZERO src) |
| **X-OFF-IDENT** | HARD | R0 (enriched, `stationEye` null) byte-identical to the bare enriched world across the battery seeds (0 mismatches) |
| **X-SEAM / E-NONSTATION** (refined) | HARD | fresh `Match` null; `scope` honoured (body/team/both); a FRESH override DECISION while the body owns the ball asserted 0 (exercised on the PRIOR eye: both bits + `homePrior` armed) |
| **X-MERGE-IDENT** | HARD, **R3p + PRIOR** | `mergedTableSha == 39662445…9d6105` AND its `base` rehashes to `171a6dad…6559f`; R0/R3v3/R3v3p inject none |
| **X-DET** (wall-free, #128) | HARD | two `computeBattery` calls produce byte-identical output (machine-dependent wall stripped from the compare + the SHA); the SHA emitted |
| **STRUCTURAL eye-never-touches-ball** | HARD, every armed arm | the eye writes ONLY off-ball targets; eye-attributable = 0 (incl. PRIOR — leg (c)'s ball-ledger) |
| **seed disjointness** | HARD | the smoke/battery bands + stats seeds disjoint from every consumed block (§7) |

**E-NONSTATION** is the refined form (the P3a proxy false-fires on a benign
retained-window offset). **Simpson-genre X-family**: N/A — a single paired
contrast, no pooling.

---

## §9 — PROGRESS, RECEIPTS, ROAD B

* **Progress lines** every **250 sets** (five full matches per set) to stderr, on
  each X-DET pass; stderr NEVER enters the compared core (#128). The smoke logs its
  sizing line; the battery logs the three legs, the restart flag and the disposition.
* **Receipts** `{seed, tick, gid, cause}`, capped 1,000/class, first-N
  deterministic (#49.3), on the PRIOR arm's release ledger.
* **Road B.** Every EDS flag dormant, `stationEye` null, the `eye.v4` flags + the
  `homePriorObedience` gene absent in production, the fingerprint `57b0bdab…c673`
  unchanged throughout. The merged table + children live ONLY in the probe config.
  **Nothing ships; no deployment claim under any reading.** After the battery runs,
  the exam RETURNS TO THE USER — PASS (⇒ the closing ruling + the play-test) or
  STOP (⇒ the replication failed) — in plain football language.
* **The user's two banked diagnoses (#152.3, out of scope here, recorded so the
  next stage inherits them):** (i) per-body obedience offsets = the NAMED slice-2
  candidate, motivated by the roleMixTV collapse 0.556 → 0.434 (uniform agreement
  homogenizes); (ii) a substrate that can PUNISH compactness (强弱侧,
  opponent-responsive shaping, co-evolution) = the long line that eventually
  retires ALL hand shape-judging. Also banked: the static-world caveat — no
  adversarial adaptation anywhere in this arc.

---

## §10 — BOUNDED-PREFLIGHT EVIDENCE (this deliverable)

`npx tsc --noEmit` clean. A capped battery preflight
(`A4P3P_MODE=battery A4P3P_N=3 A4P3P_MATCH_CAP=3 A4P3P_MATCH_DURATION=60
A4P3P_SKIP_FP=1 A4P3P_BODY_LOG=1`, writing OUTSIDE the repo) proved: all five arms
construct; the prior binds BOTH sides (`BODY-LOG`: each side's index-1 body
diverges from R3p, `sigDiff true`); `priorEquivalence` true
(`homePriorStrength(0.5) = 0.0408735 = 0.25×VAL_SCALE`, VAL_SCALE recomputed
`0.163494`); the NEW gate evaluates — leg (a) `holds false` (unresolved at N=3, as
expected), leg (b) `holds true`, leg (c) football `true`, `restartFlagged false`,
disposition `STOP — THE REPLICATION FAILED (leg (a) deep entries)`; all ten hard
gates true (`hardGatesPass true`, X-FP-PROD skipped in preflight) and
`deterministic true` (X-DET). A capped smoke preflight proved the sizing
arithmetic on the DEEP leg (`MDL 0.75` — the absolute floor binding — `N* 1200`,
`N_MAX 7000`, `reducedPowerDisclosure false`). The canonical-write guard refuses a
capped invocation without an OUT override. Scratch deleted; **NO data files
committed.** ZERO `src/**` and ZERO test changes. The full N runs are the future
authorized step (#49.5).

**FREEZE HONESTY.** Every criterion cites only published sources: rulings #152,
#151, #150, #149, #148, #146, #145, #128, #123/#121/#120, #105.4, #68.2, #46.2,
#20; the contract §1 (with the #152 amendment) / §5 / §6; the A4-P3 freeze (the
forked template); the P3p-1 merged table (`mergedTableSha 39662445…9d6105`,
`baseTableSha 171a6dad…6559f`); the banked P1d map seam + the P2 gene/flag +
`tests/a4HomePriorGene.test.ts`; the fingerprint `57b0bdab…c673`. The #151 values
are quoted as the MOTIVATING EXHIBIT only — the gate reads virgin seeds. This
freeze + probe RETURN TO THE COMMANDER; the runs are the future authorized step.
