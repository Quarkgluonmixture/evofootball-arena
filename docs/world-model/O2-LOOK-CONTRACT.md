# O2 — 抬头观察 (THE LOOK CONTRACT: buying context with time)

> Drafted by the commander 2026-08-08 under ruling #189 (the user ruled the
> #188.3 sequencing 丙→乙→甲). Child of [`OUTLET-CONTRACT.md`](OUTLET-CONTRACT.md)
> §2 O2; its §3 invariants are INHERITED. Binding once the embedded §6 VISION
> audit is ruled.

## §0 The finding this slice answers

#65 parked the whether-seat: the binding constraint on holding is
PERCEPTION, not price (true-context 0.586% vs perceived 0.141%, ~4×;
E-ABSTAIN-UNSEEN 70.7%). #186 re-measured it over the O1-armed world:
the wedge STILL BINDS (7.88× [5.15, 27.5] armed; 14.8× baseline; CIs
entirely above 1×; E-ABSTAIN-UNSEEN ≈69% both arms). The user named the
seat twice, unprompted (2026-07-26: a passer cannot buy information by
looking; #169: 抬头观察 absent). The taxed-release world did not
dissolve it. The missing primitive: a way to SPEND TIME to SEE.

## §1 The claim (H-O2)

Giving the on-ball player a LOOK — a time-priced perception refresh of
the context features the whether-seat classifies on (pressure, support,
staleness) — closes the perceived-vs-true wedge: the perceived-context
hold rate approaches the true-context rate, E-ABSTAIN-UNSEEN falls, and
holding starts landing where the certified table says it pays — WITHOUT
subsidy (the price rule is untouched) and WITHOUT omniscience (the look
sharpens READING of what is visible, never reveals what is not).

## §2 Mechanism (M-O2, dormant behind its own explicit flag)

* **M-O2.1 — the LOOK moment.** While owning the ball and not in a
  one-touch window, the player may spend a LOOK: a bounded interval
  (ticks, frozen at stage doc from a traced constant family — the C7
  wind-up scale is the natural neighbour) during which his carry slows
  (the plant idiom) and his PERCEIVED snapshot of the whether-seat's
  context features is refreshed/sharpened toward truth. Epistemic
  honesty (contract §3): the look updates `perceivedSnapshot`-derived
  reads ONLY — no new information channel, no opponent mind-reading;
  what is behind him stays behind him unless he turns (heading-gated
  visibility if the percept layer supports it; else staleness-reset
  only, stated honestly).
* **M-O2.2 — the price (I1 NO FREE TIME).** The look consumes real
  ticks while pressure closes (the O1 world makes time honest); no
  score subsidy anywhere. Cost = exposure, exactly the currency the
  #65-family census prices.
* **M-O2.3 — the trigger is a DECISION, not a reflex.** The look enters
  the candidate flow on the percept path (the ShieldHold epistemic
  precedent), gene/attr-expressible in later slices (born
  incumbent-equivalent); slice-one instruments may force it (the
  forcedHold idiom) — vectors are measurement forks, never shipped
  content.
* **M-O2.4 — the whether-seat is the CONSUMER.** The look's refreshed
  context feeds the SAME whetherEyeDecision classification; the
  certified price table is untouched by this slice (it is O3's
  re-census gate). No new value model.

## §3 Instruments & gates (the arc)

* **O2-T0 — mechanism dormant** (the M-O2 seam behind `o2Look`, hard
  false, never bundle-defaulted; flag-off byte-identity + fingerprint;
  the #181.2 receipt rule; RNG-stream identity if any draw is added).
* **O2-T1 — the WEDGE EXAM** (the decisive test, the #186 sizing form
  re-run with the look FORCED at eligible moments): pre-registered
  success = perceived-context hold classification rate rises toward
  the true-context rate (wedge point falls toward 1×, CI excluding the
  baseline wedge), E-ABSTAIN-UNSEEN falls resolvedly; FAIL modes
  pre-named: F-O2a the look refreshes but classification does not
  move (the wedge is not staleness-driven — the perception trunk needs
  different surgery; STOP); F-O2b the look becomes a free option
  (survival/hold-EV improves with zero exposure cost — the E5h class;
  the A2b-form ceiling gates it).
* **O2-T2 — live A/B + the tempo dimensions** (does looking show up in
  the world: spell length, holds taken, the #157 combo counters), the
  equilibrium bands inherited verbatim.
* **Exit** = the user's play-test (看得见"抬头"吗 — the C5 chooser and
  renderer already carry action labels; a look needs a readable body
  cue, the C7 plant precedent), keep/change/revert per lever.

## §4 Non-claims

No price-table change (O3's re-census). No off-ball scanning (the
station eye's business). No coach/doctrine rungs. No offside work.
Nothing ships without the play-test verdict.

## §5 Sequencing note

O2-T0 dispatches after the #188.3(乙) mark-selection code map returns
(one writer; the map is cheap and first per the user's 丙→乙→甲). The
甲 modulation contract drafts after the map lands (its phase-0 may
share instruments with the map).

## §6 VISION audit record (the #91 form, clause-by-clause at drafting)

* **vs §1 感知诚实 (perception, never optimism)**: the look sharpens
  reading of the visible; heading-gated if supportable; no lane-
  openness multipliers (the vision-attr grave). PASS.
* **vs §2 watchability**: a look is a READABLE cue (the user asked for
  抬头观察 as visible behavior); render carries it at T2+. PASS.
* **vs §3.1 the three missing seats**: this IS 座2, the user's own
  anchor, built on the percept path. PASS.
* **vs I1/E5h (no free time / no free option)**: the price is
  exposure-time in a world where time is now honest (O1); F-O2b gates
  the free-option failure. PASS.
* **vs emergence (capability-not-mandate)**: decision not reflex;
  gene-expressible later, born equivalent; instruments force, ships
  never do. PASS.
* Amendments produced: none.
