# RA-T1B — THE PRUNING-DOOR RE-EXAM (创造不减、丢球不增, 幽灵球还得跌)

> Authorized by **COMMANDER RULING #363 item 3** — the #362 fork resolved in two recorded
> steps: the user's ① election (completed-passes ruler) → the commander's NUMBERS-FIRST
> correction (that ruler would ALSO read below-band on RA-T1's own cells: completed ground
> passes Δ −0.9737 [−1.6566, −0.2909]/match — the priced world genuinely substitutes
> carries/holds for bad passes) → the user's re-election, verbatim: **「①′ 创造+丢球双非劣
> 尺」**. Predecessor: [`RA-T1-ACCESS-EXAM.md`](RA-T1-ACCESS-EXAM.md) (H-RA.1 = FAIL on
> (b1) alone; ⛔ its battery is NEVER re-adjudicated — the re-cut ruler meets only this
> exam's own new block). Instrument: `scripts/probes/ra-t1b-access-exam.ts`
> (the RA-T1 instrument re-frozen). Artifact: `docs/world-model/data/ra-t1b-access-exam.json`
> (or `.RED.json`).

## §P THE FREEZE (before any battery seed)

Everything is RA-T1 §P verbatim — the paired arms (SHUT = the DX-T1 walked composition;
ARMED = + `raAccessPrice` + `raAccessWeight` = 1 match-local), the population, the account
re-read, the reported face set, the estimator, the gates — EXCEPT:

### §P.C′ H-RA.1B — the re-cut rules (#363 item 3(ii))

* **(a)** UNCHANGED: Δ `unmeetableCarriedPerMatch` (armed − shut) entirely below zero ⇒
  FALLS (the count-primary form, ratified at #362 item 3).
* **(b1′) CREATION**: Δ `goalsPerMatch` NOT entirely below zero ⇒ DOES-NOT-FALL.
  Declared target 0.25 goals/match; ⚠ the block CANNOT resolve it (needs 613 pairs; MDE at
  495 ≈ **0.278**, so 0.30 is what this block certifies — both rows in the sizing table,
  declared honestly; the rule is a CI-position rule and adjudicates regardless, with the
  non-fall caveat: nothing smaller than the MDE is excluded).
* **(b2′) TURNOVERS**: Δ `interceptionsPerMatch` NOT entirely above zero ⇒ DOES-NOT-RISE
  (declared target 1.0/match; resolvable — 192 pairs).
* **H-RA.1B = PASS ⇔ (a) FALLS ∧ (b1′) DOES-NOT-FALL ∧ (b2′) DOES-NOT-RISE.**
  Pre-committed reads (#363 item 3(v)): PASS ⇒ the RA entry candidate forms (the play-test
  gate is the user's) and #357's grid composition exam is next · (a) fails ⇒ the weight
  question returns WITH numbers · (b′) fails ⇒ the price genuinely costs football; the arc
  returns to the user with the dose rung named.
* The RA-T1 scoring faces (`groundPassesPerMatch` point-vs-band, `passCompletion` Δ) are
  DEMOTED TO REPORTED, gated by nothing; `shotsPerMatch` added as a reported creation eye.

### §P.E′ SEEDS, SIZING

Block **12,532,000–999**: 495 pairs (000–494) + the receipt pair (999); booked = walked =
992; scratch family ≥ 900,001,600. ⭐ Amended pre-freeze on this exam's own smoke:
`gArmsDiverge` is a SOME-not-EVERY receipt (a match where no argmax ever flips legally
walks byte-identically in both arms — scratch seed 900,001,600 was such a match; the term
reprices, it never forces).

## §DEV-PREFLIGHT — the sizing smoke, disclosed in full

12-pair scratch smoke (`RAT1B_MODE=smoke RAT1B_N=12`, seeds **900,001,700–711**, artifact
off-canonical); realised paired-Δ half-widths read from the smoke artifact's own fields:

| paired Δ | realised hw (12 pairs) | target | N required | resolvable at 495 |
|---|---|---|---|---|
| unmeetableCarriedPerMatch | 0.8333333333333336 | −1.0267 | 17 | ✅ |
| goalsPerMatch | 1.25 | 0.25 | 613 | ⛔ declared (MDE ≈ 0.278) |
| goalsPerMatch | (same) | 0.30 | 426 | ✅ |
| interceptionsPerMatch | 2.791666666666666 | 1.0 | 192 | ✅ |

⚠ 12 pairs is a NOISY variance estimate; said before the battery. The smoke's own point
readings bind nothing.

## §R RESULTS (results commit; every number QUOTES the artifact's own fields — the #357
## standing order; the artifact is the numbers of record)

**RUN RECEIPTS.** Freeze `24e2eb6`. **16/16 gates green**; `gFaces` 39/39 + 10/10 off
disk (H-RA.1B's own verdict re-derived). Artifact
`docs/world-model/data/ra-t1b-access-exam.json`, `hashedBodySha256 =
8b47a74db47385632514ed7b93c7af45e417d32854b0e6000dec9207b2294f17`, file byte-hash
`1be109f5a69520b35f6fcaecba52c96bebba282922b055595074513a6cd46f8a`. 495 pairs
(12,532,000–494) + the receipt pair (12,532,999), **booked = walked = 992**; seeds
12,532,495–998 declared UNWALKED. ZERO stats; registry 73. Wall 111.4 s.

### §R1 ⭐⭐⭐ H-RA.1B = **PASS — ALL THREE CONJUNCTS** (the frozen §P.C′ rules' own verdict)

| conjunct | reading | verdict |
|---|---|---|
| (a) `unmeetableCarriedPerMatch` | shut **3.905051** → armed **2.820202**; Δ **−1.084848** [−1.282828, −0.901010], **5.683 hw**, LOO flips 0 | ✅ **FALLS** |
| (b1′) `goalsPerMatch` | Δ **+0.018182** [−0.169697, +0.202020] (⚠ non-fall at the declared MDE 0.278 — nothing smaller excluded) | ✅ DOES-NOT-FALL |
| (b2′) `interceptionsPerMatch` | Δ **−0.597980** [−1.052525, −0.068687] — turnovers conceded FELL resolvedly | ✅ DOES-NOT-RISE |

Per #363 item 3(v)'s pre-commitment: **the RA ENTRY CANDIDATE FORMS** (the play-test
gate is the user's) and #357's grid composition exam on meetable candidates is next.

### §R2 REPORTED (gated by nothing; the RA-T1 signature REPLICATES on a fresh block)

`shotsPerMatch` **11.692929 → 12.204040** (Δ +0.511111 [+0.183838, +0.824242] — the
second creation eye ROSE resolvedly) · `completionCarried` 0.476894 → 0.534995
(Δ +0.058101 [+0.033233, +0.082077]) · `measuredCompletionAll` +0.005465 [+0.000072,
+0.010881] · `unmeetableShareCarried` −0.062974 [−0.085170, −0.042397] ·
`carriedPerMatch` 5.333333 → 4.214141 · `meanCarriedLeadMetres` 6.995109 → 6.512252 ·
`meanCarriedMarginSeconds` −0.234160 → −0.136973 · `groundPassesPerMatch` 76.630303 →
74.529293 (Δ −2.101010 — ⭐ the pruning signature replicates almost exactly: RA-T1 read
−2.149495 on ITS block; ⚠ stated as CONTEXT, no Δ across batteries) · zero LOO flips on
every scored and reported Δ.
