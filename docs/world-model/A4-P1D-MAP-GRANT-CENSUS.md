# A4-P1d — THE FORK-AND-GRANT WHOLE-DISTRIBUTION (MAP) CENSUS (the CAUSAL discipline value of the WHOLE agreed home distribution on the EYE world)

Status: **PRE-REGISTERED 2026-08-02, FROZEN BEFORE ANY RUN. INTERVENTIONAL,
paired same-seed MULTI-DOSE forks; a NEW DORMANT default-OFF src seam
(`Match.homeMapGrant`), set ONLY inside the probe's forked branch — the P1c
single-body `Match.homeRegionGrant` stays BANKED UNTOUCHED.** This document
freezes — before a single census match is simulated and before any
`docs/world-model/data/*.json` is opened — the estimand, the R3p world
reconstruction, the HOME MAP (per-body 2D home, its base-spot source, its
published extents), the dose grid (its unit derivation), the fork-sampling rule,
the horizon, the RAW deep-entry metric, the frozen gate (its exact predicate),
the fork-context Simpson exhibit, the labelled shape exhibit, the PRE-REGISTERED
REPORTED forensic mediators (built into the census, #143.2.iv — no second
forensics pass), the sizing-before-floors plan, the seed pins (#143.2.vi ratified
freeze), and the receipts idiom. The probe
(`scripts/probes/a4-p1d-map-grant-census.ts`) + the dormant seam + its dormancy
tests (`tests/a4HomeMap.test.ts`) are built in the SAME executor step as this
freeze; the probe carries no default mode; the two REAL runs (sizing smoke →
census) are the commander's detached resident (#49.5). **This step commits the
freeze + the seam + the tests + the probe locally and does NOT push** (the push
rides the commander's adjudication ruling). Nothing ships (Road B):
`Match.homeMapGrant` AND `Match.homeRegionGrant` are `null` in every production
path, every EDS flag dormant, `stationEye` null in production, the production
fingerprint `57b0bdab…c673` unchanged throughout.

Authority: **ruling #143** (the user rules #142.5 = option A — A4-P1d, the
whole-distribution grant census — and the #126 green path RESUMES; the seven
binding design constraints #143.2.i–vii) under the **A4-ASSIGNMENT-CONTRACT §4**
estimand family (M1′ = the WHOLE agreed home distribution) and invariants **§3
I-A1..I-A7** (esp. **I-A2/I-A4** hand-built = the coordinate frame + blending rule
ONLY, the formation IS the evolved home distribution; **I-A3** no free
hand-weights — the strength is a DOSE in instruments, a gene when shipped; **I-A5**
Road B; **I-A6** pre-registration discipline — gates freeze before runs, sizing
before floors, the attainability-knee N rule, wall-derived N_MAX, no optional
stopping; **I-A7** dormancy) · ruling **#142** (the forensics adjudicated: H1/H2/H4
all REJECTED; the surviving composite **H5 — counter-press thinning**: discipline
is a property of the team's agreed SHAPE, not one body's depth; the #139.3 closure
NARROWED to the SINGLE-BODY form — the whole-distribution form was never
instrumented and is a NEW question, not a fourth instrument) · **#139** (A4-P1c
STOPPED: the single-body grant priced RESOLVEDLY ADVERSE, dose-monotone) · **#140**
(the forensic mediator set — turnovers, box, first-turnover tick, turnover third,
per-possession penetration, spacing/dupRun — frozen VERBATIM here as REPORTED
mediators) · **#137** (the M1′ rescope + the P1c instrument this extends) · **#136**
(the semantics: coarse HOME PRIOR, BOTH axes, soft bias, clamps banned) · **#128**
(the X-DET wall: the byte-compare reads a wall-FREE deterministic core) · **#127**
(the deep-primary disposition + the Simpson sign-reversal tightening as an ex-ante
NOT-ADVANCE conjunct; vacuity ex ante) · **#106.6 / #143.1** (P1d is the FIRST and
only planned pricing instrument on the DISTRIBUTION question; its STOP forks, never
silently iterates) · **#94.3** (stratify-by-context Simpson) · **#20** (CI cluster =
match seed) · **#46.2** (seed disjointness) · **#48.4** (windows/ladder pinned ex
ante) · **#49.3** (per-record receipts) · **#105.4** (optional stopping
foreclosed) · **#134.2** (the executor follows the CODE's reality over the brief's
letter and FLAGS — the emergentStation/base-spot precedent, invoked below) · the
**P1c grant/dose machinery** (`scripts/probes/a4-p1c-grant-census.ts`) + the **P1c
forensics counters** (`scripts/probes/a4-p1c-forensics.ts`) · **#117** (a ratified
freeze outranks any dispatch brief) · Road B (nothing ships).

Instruments reused (SHA'd, published — freeze honesty; every number below traces
here): the **P1-calibration deep detector**
([`scripts/probes/stage3-v4-p1-calibration.ts`](../../scripts/probes/stage3-v4-p1-calibration.ts)
lines **326–344**: opponent deep entry; the box detector **337–343**) — REUSED
VERBATIM · the **admitted P1 deep price** (`L = 0.043455` [0.030790, 0.055817]) —
LABELLED goal-value conversion ONLY; the gate reads RAW deep-entry rates (#143,
two-pin honesty) · the **P1 match-cluster bootstrap engine** (`meanCI`, cluster =
match seed, `B = 2000`) · the **injected P3p-1 merged table**
([`docs/world-model/data/stage3-v4-p3p1-merged-role-census-table.json`](data/stage3-v4-p3p1-merged-role-census-table.json),
`mergedTableSha 39662445…9d6105`, `baseSha 171a6dad…6559f`) + its control
([`stage3-v3-p2-control-recovery.json`](data/stage3-v3-p2-control-recovery.json)) ·
the **formation base-spot table** (`ATTACK_FORMATIONS`, `src/ai/formations.ts:24-59`
— the per-body home source, §2.5 below) · the production fingerprint `57b0bdab…c673`.

**World / HEAD / flags (#26.5 / #67.3).** Every census match runs the **ENRICHED**
world (`edsPerceivedDefence + edsPerceivedChoice + edsValueAxis` ON, `c5Hold`,
`c6Carry`, `c7Windup` ON, `c5TouchFork` off) with the **stationEye ARMED to the
R3p arm** (v3 base+children+`mergedTableSha`; v4 `{inSupportLaw:true,
deliveryBit:true, offsideBit:true}`; scope both) and **`homeMapGrant` NULL in the
base run** (as is `homeRegionGrant`). This is the R3p world the −16.1 %
[−17.2, −15.0] DEGEN-RESTDEF residual lives on. The counterfactual (the MAP grant)
is created ONLY inside branch B of each fork. In production every EDS flag defaults
OFF, `stationEye` is null, both grants are null, and the fingerprint
`57b0bdab…c673` is unchanged (see X-family, §7).

---

## §1 — THE ESTIMAND (what A4-P1d measures, and why it is a NEW question)

A4-P1d prices the **CAUSAL discipline value of the WHOLE agreed home
distribution** on the EYE world. Branch A is the R3p world as-is; branch B_dose
grants **EVERY side-d OUTFIELD body his OWN coarse 2D home** as a soft additive
bias at the established v3 station-scoring point, uniform strength = the dose.
The estimand is the **paired same-seed contrast on the RAW opponent deep-entry
rate**: `Δdeep(dose) = nDeepB_dose(10 s) − nDeepA(10 s)`, pooled over admitted
forks, at the PRIMARY dose (fraction 1.0).

**Why this is a NEW question, not a fourth instrument (#142.4 / #143.1).** P1
(observational), P1b (removal), and P1c (grant) closed the **SINGLE-BODY
depth-anchor** question three ways: depth-anchoring ONE body during own
possession buys no rest-defence discipline — it costs, dose-monotone. The #140
forensics then REJECTED all three registered mechanisms for that cost and left
the surviving composite **H5 (#142.3): counter-press thinning** — anchoring one
body redistributes the OTHER five by their own value gradients, thinning
post-loss pressing density. **Discipline is a property of the team's agreed
SHAPE, not any single body's depth** (the user's #131 anatomy ② speaking through
data). The contract's ACTUAL claim (M1′: the whole agreed home distribution, the
野球 model's real content) was NEVER instrumented — P1c granted one body. P1d is
the direct **kill-or-confirm of H-A4.1 at pricing grain**: if coordinated
whole-distribution redistribution is the real variable, granting the MAP should
behave differently from anchoring one body. Its STOP returns to the user
(#106.6 idiom; one instrument for the distribution question).

---

## §2 — THE WORLD, THE FORK SAMPLING, THE PAIRED MULTI-DOSE FORK (frozen)

### 2.1 The R3p arm (reconstructed EXACTLY as the P1c census built it, #143.2.i)
The ENRICHED eye-null world (`CENSUS_FLAGS`) + the ARMED R3p eye (v3
base+children+`mergedTableSha`; v4 all three bits; scope both). Its arm gates
**X-MERGE-IDENT** (the injected P3p-1 identity) and **E-NONSTATION** (the eye
ACTIVATES on this world and NEVER overrides the ball carrier) are inherited HARD
gates (§7). The base run carries the eye; both grants null.

### 2.2 The fork-moment rule (P1b/P1c idiom, pre-registered)
Step the parent R3p match; a fork MOMENT qualifies iff `phase==='playing'` AND
the ball has an owner AND ≥ `FORK_SPACING_S = 4.0 s` since the last fork. `d` =
the current owner's side (an OWN-POSSESSION moment for side d). `FORK_CAP = 20`
forks/match (wall guard; the smoke reports whether it binds). The fork context =
side d's ball third at t_fork (own/mid/their) for the Simpson exhibit.

### 2.3 The paired multi-dose fork (ZERO leakage)
At each qualifying moment the base R3p state is cloned into INDEPENDENT deep
copies (`cloneSimulationState`): branch A (both grants null) + ONE branch B per
dose (shared branch A ⇒ the whole dose ladder is same-seed paired). The parent is
NEVER stepped inside the fork. **X-FORK-IDENT (100 % coverage):** an independent
plain clone (both grants null) stepped `W_MAX_TICKS` must equal branch A's
signature (zero leakage). A fork tuple is EXCLUDED iff ANY branch (A or any dose
B) ends within the window (truncated horizon); exclusions are published, never
pooled.

### 2.4 The horizon + the metric (pinned; the DOSE is the monotone axis)
A SINGLE primary horizon `W_PRICE_S = 10 s` (the certified P0b concede horizon).
Per fork, per dose: `Δdeep = nDeepB − nDeepA`, the RAW opponent deep-entry count
against side d in `(t_fork, t_fork+10 s]` — the P1 deep detector VERBATIM
(`oppOwns && phase==='playing' && localX(ball) < -HALF_L/3`, null→true). The
goal-value conversion (`× L_DEEP`) is LABELLED ONLY, non-gating.

### 2.5 THE HOME MAP (M2′ gen-0 content — NOTHING authored; #143.2.i, #136.3)
Branch B grants EVERY side-d outfield body (index 1..5; GK excluded — the GK never
reaches the v3 station eye) his OWN coarse 2D home:

* **CENTER = HIS formation BASE spot** — `ATTACK_FORMATIONS[team.style.formationAtk][p.index]`
  (`src/ai/formations.ts:24-59`), the world's OWN per-body variable in team-local
  coords (`+x` = attack direction; `localY = worldY`). `team.style.formationAtk`
  is the team's EVOLVED formation (`deriveTeamStyle`, genome-selected), so this
  reads the world's own variable — the formation IS the evolved home distribution
  (M2′) — NOT a hand-authored shape.
  **⚠ FLAGGED base-spot choice (#134.2/#138 precedent).** The LIVE positioning
  default is `emergentStation` (`emergentPosOn()` defaults ON), which derives
  depth/lane FRACTIONS ball-relatively rather than reading `ATTACK_FORMATIONS`. The
  fixed `ATTACK_FORMATIONS` table is nonetheless the world's OWN published per-body
  home distribution (M2′ content), **static, per-body, 2D**, and requires **NO**
  ball-at-centre neutral-reference extraction (the brief's fallback for affine base
  machinery) — so it is the cleanest per-body home centre and avoids the
  emergentStation anti-clump/live-state contamination. Chose the **ATTACK**
  (in-possession) table because the eye consumption fires at own-possession
  moments. This choice is FLAGGED for commander review; the divergence from the
  live emergentStation path is the honest, declared instrument seam.
* **EXTENTS — published-anchored, BOTH axes (#136).** ⚠ FLAGGED derivation
  (executor's choice, pitch-constant-anchored): half-depth `HALF_L/6`, half-width
  `BOX_WIDTH/4`, decay `HALF_L/3` (= REST_THIRD, the P1c decay reused). A coarse 2D
  home box on both axes (the deconfliction定义 needs lateral separation — #136.2).
* **STRENGTH = the dose**, UNIFORM across the map's bodies (#143.2.i).
* **THE BIAS FORM (M3′, no clamps):** the P1c distance-decayed soft EXPONENTIAL
  extended to 2D (`homeMapBias`, `src/ai/stationEye.ts`): full `strength` inside
  the axis-aligned home box `[homeX±halfDepth]×[homeY±halfWidth]`, exp-decayed by
  the EUCLIDEAN distance to that box outside it — added to the body's per-candidate
  advantage at the v3 consumption point. Candidate coords are team-local
  (`localX = localX(ball)+cand.dx`, `localY = ball.y+cand.dy`). NO clamps (the
  clamp is the P1b-certified disease). Per-body homes are DERIVED in the
  consumption path — NO per-body table is stored in `Match`.

---

## §3 — THE DOSE GRID (RAW rates; goal-value labelled)

### 3.1 The dose grid (the monotone axis; the unit derivation, X-MERGE-gated)
Strengths = FROZEN FRACTIONS **{0.25, 0.5, 1.0, 2.0}** × `VAL_SCALE`, where
`VAL_SCALE` = the SD of the neutral-weight station value `val = 0.5·score −
0.5·concede` over the injected v3 base table's in-power cells — the eye's OWN
native score dispersion, **deterministic from the SHA-pinned merged table, NOT
from smoke**. Recomputed in-probe; it MUST equal **`0.163494`** (the P1c
X-MERGE-gated derivation; verified in the bounded preflight, §7). PRIMARY dose =
fraction 1.0 (the "unit grant"). The grid brackets it below/above so monotonicity
is testable both ways.

### 3.2 The pricing (RAW; goal-value labelled)
The GATE reads RAW deep-entry rates. The goal-value conversion (`× L_DEEP =
0.043455`, `[0.030790, 0.055817]`) is LABELLED ONLY, non-gating — the admitted
price is eye-null-calibrated (two-pin honesty #26.5/#68.2).

---

## §4 — THE FROZEN GATE (the P1c form VERBATIM; smoke data may NOT inform it)

At the PRIMARY dose (fraction 1.0), on the POOLED paired RAW Δdeep, match-cluster
bootstrap (cluster = match seed, `B = 2000`, `BOOTSTRAP_SEED = 100603`). PASS
requires **ALL**:

* **(i) RESOLVED** — pooled `Δdeep(primary)` CI **UPPER < 0** (the MAP REDUCES
  opponent deep entries).
* **(ii) DOSE-MONOTONE** — point estimates **non-INCREASING** across
  `[0, 0.25, 0.5, 1.0, 2.0]` (the 0 anchor = branch A vs itself = 0): increasingly
  negative with dose.
* **(iii) LADDER RESOLVED** — the `(top−bottom)` contrast `Δdeep(2.0)−Δdeep(0.25)`
  CI **UPPER < 0**.

PLUS the **#127 tightenings**: a Simpson-genre SIGN REVERSAL on the primary cell
(pooled vs fork-context-standardized) is an **AUTOMATIC NOT-ADVANCE**; an EMPTY
pooled cell ⇒ leg (i) UNRESOLVED ⇒ STOP (attainability vacuity, declared ex ante).
Any leg fails ⇒ **STOP at A4-P1d** (I-A3: no measured causal term ⇒ no build),
return to the user. **PASS ⇒ A4-P2 opens** (the #143.1 green path: commander
review + a numbered ruling gate the transition, no user veto needed on PASS). The
goal-value conversion is LABELLED only.

### 4.1 CONFOUNDING — the fork-context Simpson exhibit (#94.3 / #127)
Stratified by fork context (side d's ball third at t_fork). WITH SAME-SEED
PAIRING this is a **HETEROGENEITY exhibit, NOT a confound repair**. The gate binds
on the pooled primary cell; a pooled-vs-standardized SIGN REVERSAL is FLAGGED and
forces NOT-ADVANCE.

### 4.2 THE SHAPE EXHIBIT (labelled, NON-GATING — early F-SLIDE sight)
At the TOP dose, per-tick nearest-teammate spacing + dup-runs (pairs < 4 m) for
side d over the fork window, branch B vs A (also carried per-dose in the spacing/
dupRun mediators, §5). Early F-SLIDE sight only; the frontier shape gates live at
A4-P3.

---

## §5 — THE PRE-REGISTERED REPORTED MEDIATORS (#140/#142/#143.2.iv — NOT a gate)

The #140 forensic counters are built INTO the census from the start (no second
forensics pass, #143.2.iv), computed for **branch A and EVERY dose branch** (level
+ paired delta vs A), match-cluster CIs. Copied verbatim (semantics) from
`a4-p1c-forensics.ts`:

* **turnovers** — side-d possession losses (`possessionSide` d→opp; −1 dead-ball
  transients ignored) per window.
* **firstTurnoverTicks** — ticks-since-fork of the FIRST loss, **CENSORED at
  `W_MAX_TICKS`** when none (retention proxy; higher = more retained).
* **turnoverThird** — the side-d-local third (own/mid/their) at the FIRST turnover
  (`'none'` if retained); own-share among turnovers reported.
* **boxEntries** — the P1 calibration box detector VERBATIM
  (`oppOwns && playing && localX ≤ BOX_INNER_X && |ball.y| ≤ BOX_WIDTH/2`).
* **teamPassesForward / Backward** — side-d NEW `pendingPass` attempts, classified
  by the SAME backward test `passBackPen` uses (`gain = clamp01((localX(target) −
  localX(passer) + 30)/60)·2 − 1 > 0 ⇒ forward`, `PlayerBrain.ts:320/356-357`).
* **spacing / dupRun** — the census shape-exhibit form (nearest-teammate spacing +
  dup-run pairs < 4 m, sampled at 6 Hz), now a per-dose mediator.
* **entriesPerOppPossession (derived, H5)** — pooled deep/box entries ÷ pooled
  opponent possessions (side-d→opp gains) per branch.

**The single-body `grantedReceives`/`passesToGranted` counters are DROPPED** (they
were index-1-specific; the map grants ALL bodies — the brief's "per-body receive
depth optional" → dropped as no single grantee exists). **H5 carried prediction
(#143.3), REPORTED context NOT a gate:** if coordinated redistribution is the real
variable, the MAP grant should NOT thin the press — spacing/dupRun should not
loosen and entriesPerOppPossession should NOT rise the way the single-body grant's
did (#142.3, +≈10 %). The commander reads the mediators beside the gate at review.

---

## §6 — SIZING BEFORE FLOORS + SEEDS (no optional stopping)

**The smoke** (`A4P1D_MODE=smoke`, 40 matches @ `12,050,000 + k`, k∈0..39)
realises the fork/dose populations, the eye consumption frequency, the pooled
Δdeep σ̂, and the per-match wall INCLUDING forks×doses; it double-runs for X-DET.
**The frozen N arithmetic** (P1c form, RAW-rate MDL): `MDL = min(0.5·|Δdeep_smoke|,
0.01)` deep-entries/window; `SE_N = σ̂·√(1/N)`; `N* =` smallest 200-step N with
`SE_N ≤ MDL/POWER_Z` (`POWER_Z = 3.605`), capped at a **WALL-DERIVED N_MAX**
(`N·perMatchWall·2 ≤ 12 h`, the ×2 for the X-DET double-run, #128) and a hard cap
`N_CAP = 8,000` (keeps the census band ≤ 12,107,999). The **attainability-knee**:
if the priceable-fork cell or a gate stratum is too rare, its leg reads UNRESOLVED
⇒ the gate STOPS. No optional stopping (#105.4). The smoke data may NOT inform the
gate.

**The census** (`A4P1D_MODE=census A4P1D_N=<N* from the smoke>`, @ `12,100,000 + k`,
k∈0..N−1, N ≤ 8,000 ⇒ ≤ 12,107,999) is the gate-bearing run — the commander's
detached resident (#49.5).

**SEEDS (#143.2.vi — a RATIFIED FREEZE; inside 11.7M–12.3M, disjoint from ALL
consumed blocks 11.70/11.80/11.85/11.90/11.95/12.00M):** smoke **12.05M**
(k 0..39); census **12.10M** (k 0..N−1). Stats: bootstrap **100603**; **100703**
reserved-unused. Disjointness is asserted IN-PROBE as a HARD gate against the
reservation and every consumed block (§7).

---

## §7 — DELIVERABLES + THE X-FAMILY / GATES TABLE

**Deliverables (this executor step, one local commit, explicit paths):**
1. This freeze (`docs/world-model/A4-P1D-MAP-GRANT-CENSUS.md`).
2. The DORMANT MAP SEAM (minimal src): `Match.homeMapGrant` (`{side,strength}|null`,
   default null) + the 2D `homeMapBias` + its threading in `actionExecutor` (every
   side-d outfield body gets his own bias closure in branch B). The P1c
   `homeRegionGrant` is untouched; both null ⇒ byte-identical to HEAD.
3. The DORMANCY TESTS (`tests/a4HomeMap.test.ts`, the a4HomeGrant idiom): default
   null, flag-off byte-identity + fingerprint `57b0bdab…c673`, flag-on
   multi-body effect existence, side-scoping, independence from the banked
   single-body flag, inert when `stationEye` null.
4. The probe (`scripts/probes/a4-p1d-map-grant-census.ts`, modes smoke|census).
5. A bounded preflight (tsc clean; suite green; fingerprint confirmed; a tiny
   capped run proving the map binds for MULTIPLE bodies in branch B; scratch
   deleted; NO data files committed).

**THE X-FAMILY (each a HARD gate; verbatim from P1c):**

| Gate | What it asserts |
|---|---|
| **X-DET** (#128) | the probe output is byte-identical across a double-run; the machine wall is measured OUTSIDE the compared core. |
| **X-FORK-IDENT** | an independent plain clone (both grants null) == branch A on EVERY fork (zero leakage), 100 % coverage. |
| **X-MERGE-IDENT** | the injected P3p-1 merged table `mergedTableSha`/rehash == `39662445…9d6105`, base == `171a6dad…6559f`; `VAL_SCALE = 0.163494`. |
| **E-NONSTATION** | the eye ACTIVATES on the R3p world (arm not vacuous) AND never overrides the ball carrier; body-/team-scope honoured. |
| **X-FP-PROD** | the production fingerprint `57b0bdab…c673` is UNCHANGED (both grants null in production ⇒ dormant). `git diff src` is NON-empty BY DESIGN (the dormant seam); Road B = X-FP-PROD + the flag-off byte-identity test. |
| **SEED DISJOINTNESS** (#46.2) | smoke 12.05M + census 12.10M inside 11.7M–12.3M, mutually disjoint and disjoint from every consumed block. |

**Percept-honesty note (instrument, fork-branch-only).** The MAP grant is an
INSTRUMENT in fork branches (soft per-body home priors in offline clones), NOT a
consumer ship. No live percept is created/read/leaked; no gene, no M1′–M5 build.
Hand-built = the coordinate frame (per-body centre = the world's own formation
variable) + the blending rule + the published extents ONLY; WHAT the distribution
is worth is measured (I-A2/I-A4 诚实张力).

---

## §8 — THE PRE-LAID READINGS (the full sign space; none re-cut after sight)

* **PASS** — pooled Δdeep(primary) CI upper < 0, dose-monotone, ladder resolved,
  no Simpson reversal, empty-cell not triggered ⇒ RESOLVED whole-distribution
  discipline recovery ⇒ **A4-P2 opens** (#143.1). H5 CONFIRMED at pricing grain
  (the map behaves unlike the single body).
* **NOT-ADVANCE (reading E, #127)** — the three legs pass but a Simpson sign
  reversal fires ⇒ returns to the user with both exhibits.
* **STOP** — any leg fails (null / non-monotone / unresolved dose-response) OR the
  empty-cell vacuity fires ⇒ **STOP at A4-P1d** (I-A3), returns to the user. An
  ADVERSE resolved result (Δdeep CI entirely positive, like P1c) is one STOP
  reading — reported honestly as such, with the H5 mediators telling whether the
  map thinned or held the press.

Whatever the gate reads, the mediators (§5) are reported: the H5 kill-or-confirm
lives in whether the whole-distribution grant thins the press like the single-body
one did.

---

## §9 — REGISTERED NON-CLAIMS + THE STAGE EXIT

* **Measures the MAP's discipline value, builds NO consumer** — no M1′–M5, no gene,
  no shipped consumer (A4-P2/P3).
* **The map grant is an INSTRUMENT** (soft per-body home priors in fork branches),
  not a percept ship; the per-body centre is the world's own formation variable.
* **The home map is COARSE + BOTH-AXES** (#136): a per-body 2D box home; convex
  regions cannot express bimodal homes (§7 non-claim); block movement lives in the
  ball-relative frame.
* **The goal-value conversion is LABELLED ONLY**; the gate reads RAW rates.
* **Nothing ships (Road B)**: both grants null in production, fingerprint
  `57b0bdab…c673` unchanged.
* **P1d is the FIRST + ONLY planned pricing instrument on the DISTRIBUTION
  question** (#106.6/#143.1): its STOP forks, never silently iterates; a second
  needs a fresh user ruling.
* **A4-P1d cannot authorize A4-P2**: only the commander's review of the census
  result opens A4-P2 (#143.1).

Rulings anchored: #143 / #142 / #140 / #139 / #137 / #136 / #135 / #128 / #127 /
#106.6 / #94.3 / #20 / #46.2 / #48.4 / #49.3 / #105.4 / #134.2 / #117; the P1
calibration deep + box detectors; the P1c grant/dose machinery + the P1c forensics
counters; the P3p-3 battery R3p arm; the production fingerprint `57b0bdab…c673`.
