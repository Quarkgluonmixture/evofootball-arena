# L3 T1 — THE CONVERGENCE EXAM (does the defence book learn 扑了→被过→受罚 from its own beatings?)

Status: **PRE-REGISTERED — everything above [§RESULT](#result) was written and COMMITTED BEFORE THE
BATTERY RAN** (#266.3(c), the freeze-commit canon). The measured numbers arrive only in §RESULT, and
every number there is quoted FROM the committed artifact.

Authority chain: the **CB-L3 DEFENCE-BOOK CONTRACT**
[`CB-L3-DEFENCE-BOOK-CONTRACT.md`](CB-L3-DEFENCE-BOOK-CONTRACT.md) §1 (**H-L3**) and §3 (**L3-T1 —
THE CONVERGENCE EXAM**, the #257.3 frame), bound by **#277.1**, dispatched by ⭐ **#280.3**, and
bound by **#279.3** (the label ruling) and ⭐⭐ **#280.2** (the applied-window law, the
phase-sensitivity rung, the two named pre-tasks). Hygiene canon in full: **#163** (booked = walked) ·
**#181.2** (gates computed in-probe) · **#200** · **#203** · **#226.1** · **#229.2** (no typed
number the artifact does not carry) · **#246** (the pre-registered direction) · **#247 / #248.1**
(the shape/answer split) · **#250.3** · **#256.2 / .3** (commensurability, ex-ante sizing) ·
**#260.2** · **#261.2** (env WHITELIST-OR-REFUSE) · **#264.2 / #268.3** (the DEAD-PREDICATE class:
a threshold must be provably passable AND failable) · **#266.2(i)** (carrier-anchored t0) ·
**#266.3(a)** (envelope + cross-OUT) · **#266.3(c)** (the freeze commit) · ⭐⭐ **#268.3(a)**
(LIVENESS BY MACHINE, EXACTLY-ONE ENFORCED) · **#270.2 / #272.3(ii)** (clock honesty) · **#273.3**
(tree-clean gates compare WORKTREE vs HEAD) · **#276.3 → #278.2(ii) → #279.3(i)** (⭐ THE HAT-TRICK:
a stage-doc number is quotable only after its **corrections section** has been opened and read).

### ⭐ §CORRECTIONS-READ — the standing hunt, discharged explicitly

Every stage-doc number quoted below was read **after** opening that document's own corrections
section, and the checks are stated rather than implied:

* **L3-C0 §COMMANDER CORRECTIONS OF RECORD (#278.2) — READ IN FULL.** Binding here: the label pick
  it recommended was **NOT ratified** (the window confound), so **no L3-C0 punishment rate is used
  as a sizing input**; its "37.1 %" bare-world contrast was a wrong-cell citation (true cell
  37.565 %) and is not quoted here at all; its own warning that the overcommitted band cannot fill
  in one team-season is *the* reason this stage exists.
* **L3-C0b §COMMANDER CORRECTIONS OF RECORD + THE LABEL RULING (#279.2/#279.3) — READ IN FULL.**
  Binding here: (i) the "charter ±3–4 pp" citation had **no source** and is DEMOTED — this stage
  therefore states its own target as an MDE it BUYS, never as a charter requirement; (ii) the
  applied short rung was 0.6500 s (39 ticks) not the nominal 0.654537 s — the applied-vs-nominal
  class, which #280.2(iii) then made law; (iii) the two-rung tie-break was exercised post-sight and
  its authority reverted to the ruling — so the **window this exam measures at is not chosen here at
  all**: it is the seam's own applied window. The rate vector quoted below is C0b's **g2 /
  `sepGainedCommonLong`** row, which its corrections leave unamended.
* **L3-T0 §COMMANDER CORRECTIONS OF RECORD (#280.2) — READ IN FULL.** Binding here: (i) the restart
  contamination is **13.7 %**, ACCEPTED of record with the phase-sensitivity rung **binding on this
  stage**; (ii) the falsified count-proxy comment — **fixed by this stage's pre-task commit**;
  (iii) ⭐ the APPLIED window **54 ticks = 0.9000 s IS THE LAW OF RECORD**, so this stage's
  yardstick measures at 54 ticks and every window statement below carries applied + nominal;
  (iv) the orphaned docblock — **fixed by this stage's pre-task commit**; (v) the PREFIX-form
  identity re-spec and the count-proxy retirement are RATIFIED, so the identity claim scored here is
  the **whole-match** one for the LEARN-ONLY arm (which #280.1 proved 16/16 cross-tree), **not** the
  prefix form (that form belongs to an armed book, and this exam arms no veto).
* **EK-T1 §COMMANDER CORRECTIONS OF RECORD (#263.2) — READ IN FULL** before re-using its machinery:
  its G-VETO headline was ≥ 40.7 % vacuous `every()` passes (⇒ every `every()`-shaped gate here
  carries an explicit **non-vacuity** conjunct with a published count), and its deff re-walk read
  626 punished where the seam's own book said 632 (⇒ this stage's re-walk is gated to reproduce
  L3-T0's **committed book cells exactly**, not approximately).

### ⭐ THE PRE-TASKS (#280.2(ii)/(iv)) — DISCHARGED BEFORE THE FREEZE

One comment-only commit, `tsc` clean, behaviour byte-identical (no expression, statement or literal
moved; the production fingerprint is re-derived in-probe as `xFpProd` regardless):

1. **(ii)** the FIRED METER's docblock in `Match.ts` no longer claims "an armed arm may never fire
   more lunges than the learn-only arm" (falsified of record: 47 vs 43 with 34 vetoes served). It
   now states the structural property: **decline-only is proven by POSITION** (the one veto site
   sits in series strictly between the untouched jockey gate and the line that commits the body, and
   its consequent is a bare early `return`) **and by the DOSE-DIRECTION test — never by arm counts.**
2. **(iv)** the orphaned **LABEL CAPTURE** docblock now sits on `l3DefenceNoteMiss`, the method it
   describes.

---

## §FORM

### The world — ⭐ THE POLISHED ARMED WORLD, and NO greenhouse

`new Match({ seed, teamA, teamB, ...a4MatchFlags(6) })` + `armA4World(m, null, 6)`, asserted
`cbArmedVersion(m) === 6` — **L3-C0 / L3-C0b / L3-T0's own world of record** (#273, the polished
armed world), at the ENGINE DEFAULT clock and the engine default duration.

⭐ **WHY NO DRILL WORLD (the EK-T1 contrast, stated because it is the interesting half).** EK-T1
needed a greenhouse because its live seat could only ever fill ONE band. This book has no such
problem: L3-T0 measured **683 closed labels over 20 armed matches** — **17.1 per team per match**,
with the binding RECKLESS group filling on its own. **The exam therefore runs in the world of
record**, and its result is about *this game*, not about a training ground. No dosing, no injected
table, no drill driver: **ZERO DOSING** (#280.3).

### The two arms

| arm | flags | role |
| --- | --- | --- |
| ⭐⭐ **LEARN-ONLY** | `l3DefenceLearn` ON, `l3DefenceVeto` **OFF** | **THE SCORED ARM.** The books fill and NOTHING reads them, so the world is byte-identical to the unarmed world (**gated**, F-L3-c) and the books sample exactly the world the yardstick is measured in. |
| **OFF** | neither flag | the identity anchor and the guards' control. |

⭐ **THERE IS NO TRUTH-DOSED ARM, AND THE REASON IS SAID PLAINLY** (#280.3 asks which): the frozen
design needs **no calibration receipt**. The label's calibration was banked by `gLabel` at T0
(0 mismatches vs an independent re-labelling over 416 closures), the sizing's only measured input
(`deff`) comes from a **deterministic re-walk of L3-T0's committed smoke block**, and the yardstick
is measured **inside this battery's own world** rather than imported. A dosed arm would buy nothing
this exam scores, and it would spend seeds the band does not have.

⭐ **AND THERE IS NO ARMED-NEUTRAL ARM.** #280.2(v) ratified the PREFIX identity form for an
**armed** book (a born-empty book earns evidence mid-match and may legitimately veto). This exam
arms no veto, so the identity claim available to it is the **stronger whole-match** one — and that
is what F-L3-c gates.

### ⭐ The replicate, and what a "season" is here

A **replicate** = **ONE persistent `l3DefenceBooks` pair** (two books, one per side) shared across
its **whole match sequence**. `League.startSeason()` **never fires** — the probe drives the sequence
directly — so **M-L3.2's season-reset clause is HONOURED, not amended**: the reset is the League's
act, and a probe-driven sequence is **one long season in substance**, declared as such (the EK-T1
precedent, verbatim).

**A SEASON = 7 FIXTURES PER TEAM**, traced from `League.ts` (eight clubs, a single round-robin,
7 rounds), never typed as a design choice. "M seasons per book" therefore means **7 M matches of
accumulated own-events**, and §DOUBTS 1 states plainly what that does and does not license about a
shipped, season-reset book.

⭐ **THE LEARNER IS THE SIDE, AND THE SQUADS ARE REDRAWN PER FIXTURE** (`teamA = team('A',
seed·2+1)`, `teamB = team('B', seed·2+2)` — L3-C0/C0b/T0's own draw). This keeps the book's sampling
population identical to the yardstick's; a fixed-roster sequence would converge each book to ONE
matchup's truth and would arm **F-L3-b** spuriously.

### The scored quantity

Per book, its **belief pair** — `punished[g] / lunges[g]` for g ∈ {CONTROLLED, RECKLESS}, the account
book's own zero-constant running frequency (M-L3.2), where `lunges[g]` counts **closed labels**
(§SHARP 1 of T0: the MISS population). **Rates are recomputed from RAW COUNTS** at scoring time, on
both sides of every comparison.

---

## §CLAIM — H-L3, and the predicate it is scored on

> **H-L3 (contract §1):** given only their own lunge outcomes, teams can EARN a lunge-risk map over
> their own arrival state whose SHAPE matches the world's measured truth — faster/less-reachable
> arrivals are more punished.

### ⭐⭐ THE SCORED PREDICATE — frozen here, before any battery

**The claim scored is #280.3's own sentence:** with the book learning from its own events and ZERO
dosing, after the exam's learning run **the team's own book independently shows RECKLESS more
punished than CONTROLLED** — the book's OWN quantity (carrier-anchored Δsep **≥ 0**, the seam's own
form, at the **APPLIED 54-tick window**; the `≥ 0` form closes #280.2(v)'s tie question by
commensurability, because the yardstick uses the identical predicate).

* ⭐⭐ **THE SCORED LIMB — THE BOOK SHARE.** At exam end (M = 15 seasons), the share of books whose
  OWN cells are strictly ordered `belief[RECKLESS] > belief[CONTROLLED]` is **≥ τ = 0.75** — i.e.
  **≥ 12 of the 16 books**. τ is **DERIVED**, not conventional (§M Step 6), and ties are **not**
  ordered (strictness machine-checked on synthetic ties).
* **THE SECOND LIMB — REPORTED, NOT GATED (the #257.3 conjunction, published in full).** The
  **replicate-mean gap** `belief[RECKLESS] − belief[CONTROLLED]` and its cluster bootstrap 95 % CI
  by replicate (2,000 resamples), and whether that CI excludes zero. ⚠ **DECLARED (§DEV 1):** the
  seed band #280.3 grants (1,000 seeds) cannot buy 0.80 power for the PRODUCT of the two limbs under
  the conservative correlation reading — the arithmetic is published — so the set-grain limb is
  carried as a REPORTED reading with its own power (0.733 conservative / 0.954 independent), and the
  scored predicate is the dispatch's own literal one.

**Convergence distance to the yardstick is REPORTED, never gated** (#246): the shape is the claim;
the magnitudes are this world's. Every book's raw cells are published.

---

## §M — ⭐⭐ THE EX-ANTE SIZING, EVERY STEP, FROZEN BEFORE ANY BATTERY

Nothing below is guessed and nothing is read off this stage's own battery. Every input is a number
in a **committed artifact** or a deterministic re-walk of one; the probe **recomputes the whole
derivation at run time** and **`gN` fails if any frozen literal disagrees**.

**Step 1 — THE TWO COMMITTED FILL SOURCES** (closed labels per TEAM-match, the book's own events).
Unlike EK-T1, **both sources measure the SAME venue** (the polished armed world), so #262.3's
"conservative per cell" rule applies without deviation.

| source | CONTROLLED | RECKLESS | exposure |
| --- | ---: | ---: | ---: |
| **L3-T0's committed armed smoke** (the seam's own instrument) | 615 | 68 | 40 team-matches |
| **L3-C0b's committed g2 tabulated misses** | 7,029 | 1,067 | 502 team-matches (251 matches) |
| one-sided 95 % lower Poisson bound `(n − z₀.₉₅·√n)/T`, T0 | 14.355224 | **1.360905** | |
| one-sided 95 % lower Poisson bound, C0b | **13.727285** | 2.018468 | |
| ⭐ **THE DESIGN RATE `d` (conservative PER CELL)** | **13.727285** | **1.360905** | per team-match |

**Step 2 — THE PUNISH RATES power is evaluated at.** The census-true vector is **L3-C0b's committed
g2 `sepGainedCommonLong` row**: `p = [CONTROLLED 0.7890, RECKLESS 0.8305]`, gap **4.15 pp**. ⚠ It is
measured at C0b's 1.0000 s rung, **not** at this seam's applied 0.9000 s window — the nearest
committed estimate, used **as a sizing prior only**; the exam's own yardstick (§YARDSTICK) is
measured at 54 ticks and may differ. ⚠⚠ The **second** committed source — L3-T0's smoke book rates
`[0.7935, 0.7647]` — is **NOT ORDERED** (it is inverted, on 68 reckless events), and the predicate's
power under THAT vector is **published beside** the sizing as the honest prior that **F-L3-a may
fire**, exactly as EK-T1 published its own unordered vector rather than burying it.

**Step 3 — ⭐ THE DESIGN EFFECT, MEASURED not assumed.** Labels of one team can be resolved by ONE
world event: a restart teleports the bodies and closes every window it spans in the same direction
(#280.2(i)'s own mechanism). The inflation is measured by **RE-WALKING L3-T0's committed smoke block
(12,482,100–119)** — a deterministic replay of a committed parent, **not** a fresh seed consumption
(the EK-C0b / EK-T1 re-walk idiom) — clustering punished labels by `(side, dead-ball episode index
at close)`:

```text
deff = punished labels / distinct punishing (side, episode) clusters = 540 / 288 = 1.875
n_eff(g, M) = round( d_g · 7 · M / deff )        [an equal-cluster LOWER bound, declared as such]
```

⭐ The re-walk is also a **RECEIPT**: it must reproduce L3-T0's committed book cells **exactly**
(615/488 controlled · 68/52 reckless · 683 closed) — the proof that this probe's walker is the
seam's own and not a look-alike (the EK-T1 correction #263.2(2) turned into a gate). Its seed
predicate is **INVERTED**: it *must* collide with the consumed ledger.

**Step 4 — q(M), the per-book ordering probability, EXACTLY.** With
`K_g ~ Binomial(n_eff(g, M), p_g)` independent,

```text
q(M) = Σ_{k_R} P(K_R = k_R) · P( K_C / n_C  <  k_R / n_R )      [STRICT: ties are NOT ordered]
```

by exact binomial convolution over the RECKLESS group (the scarce one), no normal approximation.

**Step 5 — THE NULL WORLD, for failability.** `q_null(M)` is the same arithmetic with
`p_R = p_C = ` the census's own **pooled** g2 rate `0.794469` (raw-count pooled). It is **not** 0.5:
strictness plus unequal n makes a no-ordering world pass **0.511307** of books at M = 15.

**Step 6 — ⭐⭐ τ, DERIVED FROM THE PERFECT-LEARNER ARITHMETIC, AND PROVED NOT DEAD.** With
`B = 2R` books, τ is the **smallest** k/B such that a world with **NO ordering** clears it with
probability ≤ 0.05:

```text
τ  =  min{ k/B  :  P( Bin(B, q_null) ≥ k ) ≤ 0.05 }
PASSABLE  ⇔  P( Bin(B, q_census) ≥ τ·B ) ≥ 0.80        (the power requirement, #280.3)
FAILABLE  ⇔  P( Bin(B, q_null)   ≥ τ·B ) ≤ 0.05        (the #264.2/#268.3 dead-predicate bar)
```

Both legs are recomputed in-probe (`gN`) **and** exercised on synthetic book sets (`gTauLive`): an
all-ordered set must PASS and a null set must FAIL, on this instrument's own scoring code.

**Step 7 — THE N RULE, frozen.** Power is a **constraint**, depth is the **objective** — because
#280.3 asks for a LEARNING CURVE and for what one season CANNOT do, and a shallow design cannot
speak to either:

```text
FEASIBLE(R, M)  ⇔  R·7M ≤ SEED_ROOM (840)
                   AND B = 2R ≥ 12                      (a share over fewer books is not a share)
                   AND M ≥ 2                            (#279.3(4): MULTI-season, ex ante)
                   AND P(Bin(B,   q_census) ≥ τB) ≥ 0.80   (independent-books reading)
                   AND P(Bin(R,   q_census) ≥ τR) ≥ 0.80   (⭐ the CONSERVATIVE reading: the two
                                                            books of a replicate as ONE cluster)
                   AND P(Bin(B,   q_null)   ≥ τB) ≤ 0.05   (failability)
(R*, M*) = argmax M over FEASIBLE, tie-break: larger power, then larger R
```

**THE RESULT, FROZEN HERE:**

| quantity | value |
| --- | ---: |
| **R\*** (replicates) | **8** |
| **M\*** (seasons per book) | **15** ( = 105 matches per replicate) |
| books B = 2R | **16** |
| battery matches (seeds) | **840** ( = 8 × 105 ) |
| `deff` (measured, re-walk) | **1.875** |
| design rate `d` per team-match | **[13.727285, 1.360905]** |
| `n_eff` at M\* (CONTROLLED / RECKLESS) | **769 / 76** |
| **q per book** (census-true rates) | **0.821087** |
| `q_null` (no-ordering world) | **0.511307** |
| ⭐ **τ** | **0.75** (12 of 16 books) |
| **POWER — independent reading** | **0.857030** ( ≥ 0.80 ✅ ) |
| **POWER — conservative reading** (B = R) | **0.841367** ( ≥ 0.80 ✅ ) |
| **FAILABILITY** — independent | **0.046561** ( ≤ 0.05 ✅ ) |
| ⚠ failability — conservative reading | 0.159877 (published; the reading's own limit) |
| ⭐ **MDE bought** (the gap at 80 % power) | **3.8200 pp** vs the census gap **4.15 pp** |
| ⚠ **power under L3-T0's INVERTED smoke vector** | **0.000199** (q = 0.291857) |
| the REPORTED limb's power | 0.732622 conservative / 0.954461 independent |
| seed room | 840 — **BINDS** (the band is 1,000 seeds, #280.3) |
| wall cap | 840 × 2 arms × 2 G-DET runs × 150 ms nominal ≈ **504 s** — does not bind |

⭐⭐ **WHAT ONE SEASON CANNOT DO, IN THE DESIGN'S OWN ARITHMETIC (#279.3(4), made concrete).** At
**one** season a book holds `n_eff = [51, 5]` and its ordering probability is **q = 0.6058** —
against a no-ordering world's **0.5113**. One season is therefore worth about **9 pp of book-level
signal**, and the predicate at τ = 0.75 would have power ≈ 0.09: **a single team-season cannot
resolve this ordering, and no threshold rescues it.** What **15** seasons buy is q = 0.8211 and
power 0.857. The whole curve is published:

| seasons | 1 | 2 | 3 | 5 | 8 | 12 | 15 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| `n_eff` RECKLESS | 5 | 10 | 15 | 25 | 41 | 61 | 76 |
| **expected q** | 0.6058 | 0.6492 | 0.6787 | 0.7078 | 0.7483 | 0.7954 | **0.8211** |

⚠ **THE MDE IS THE HONEST HEADLINE**: **3.82 pp against a committed census gap of 4.15 pp**. The
exam buys **exactly** the effect the decomposition measured and little more — so a NEGATIVE score is
an answer about *learning this label at this fill in this world*, not about an under-powered
instrument. And the honest prior is on the record: at L3-T0's own (inverted, tiny-n) smoke vector
the design's power is **0.0002**.

---

## §YARDSTICK — ⭐⭐ MEASURED INSIDE THIS INSTRUMENT, AT 54 TICKS (#279.3 / #280.2(iii))

The book converges toward **ITS OWN world's truth**, not toward a prior census's number. So the
yardstick is **this battery's own** pooled census, at claim grain:

```text
TRUTH(g) = Σ over ALL closed labels in the LEARN-ONLY arm of punished(g) / lunges(g)
           — the SAME predicate the book uses: carrier-anchored Δsep ≥ 0 over the APPLIED window
WINDOW   = ⭐ APPLIED 54 ticks = 0.9000 s   (NOMINAL sqrt(2R/a)+π/ω = 0.8886436883950108 s)
           — every closed label's realised span is gated to be EXACTLY 54 ticks (gWindowApplied)
GRAIN    = g2, CONTROLLED (< v*) vs RECKLESS (≥ v*), the census's own cut, bit-equal
CI       = cluster bootstrap by REPLICATE (2,000 resamples, stats stream base 111,200)
```

Published beside it, never gated: L3-C0b's committed g2 rates at ITS rungs (0.6545 s / 1.0000 s) —
so the reader can see how much of any divergence is the clock (#278.2(i)'s own lesson) rather than
the world. **The scored predicate does not read the yardstick**; the yardstick explains the verdict.

## §PHASE — ⭐ THE PHASE-SENSITIVITY RUNG (#280.2(i), BINDING)

The seam labels every one of its own beatings, whistle or not; the census excluded whistled duels;
#280.2(i) ACCEPTED the resulting contamination on two mitigations (the yardstick shares the bias;
the bias is approximately group-neutral) and made **measuring the second one binding here**.

Every closed label is stored with a probe-side flag: **`spansRestart` ⇔ any tick in
`[t0, t0 + 54 ticks]` is a NON-PLAYING tick** (`Match.phase !== 'playing'` — read per tick, outside
the seam). The rung then publishes, **both** ways and both **published**:

1. the **yardstick** (pooled truth per group) — ALL labels vs CLEAN labels only;
2. the **scored predicate's own reading** — the ordered-book share and the replicate-mean gap, ALL
   vs CLEAN, at exam end;
3. the spanning share, per group, with the punished rate inside and outside it.

⚠ **The CLEAN reading is a READING, never the verdict** (the verdict is scored on the book's own
cells, which are what the shipped seam would hold). If the two disagree, the group-neutrality
assumption #280.2(i) accepted is **measured false**, and that is a finding for the commander — the
rung is a diagnostic, not a gate (#203).

## §CURVE — the learning trajectory (REPORTED)

Every book is snapshotted at **FROZEN checkpoints, in SEASONS**: **1 · 2 · 3 · 5 · 8 · 12 · 15**. At
each checkpoint the artifact stores **every book's raw `(lunges, punished)` cells** (ALL and CLEAN),
and the generator publishes the ordered-book share, the mean belief pair and the mean gap. ⭐ **The
verdict must carry its trajectory**: a share that clears τ at 15 seasons but was flat to 8 is a
different fact from one that settled at 3.

## §GUARDS — the football guards, at the BANKED tolerances (REPORTED)

Inherited **verbatim** from DV-T1/#251 and EK-T1: tolerance = `NI_FRACTION · |control|` with
`NI_FRACTION = 1 − 0.275/0.380`; the control is the OFF arm measured **in this battery**; deltas are
paired per replicate.

| ruler | direction |
| --- | --- |
| `interceptions` per match | ceiling |
| `spreadYOut` | floor |
| `spacingMedian` | floor |
| `spacingUnder4` | ceiling |
| `offsides` per match | the #157 **FLAG** form — returns to the commander, flips no gate |
| `goals` per match | FLAG (published) |

⭐ **IN THIS EXAM THE GUARD ROW IS A PURE NULL CONTROL.** There is no consuming arm, and the
LEARN-ONLY arm's world is byte-identical to the OFF arm's — so **every delta must be EXACTLY ZERO**
on every ruler. A non-zero row is an instrument defect (or an identity breach), not football, and it
routes to **F-L3-c ⇒ STOP**.

## §FORKS — pre-named, printed MECHANICALLY, never resolved here (#203)

* **F-L3-a** — the scored share falls short of τ at M\* ⇒ *"an HONEST NEGATIVE about the label's
  audibility at real fills"* (the contract's own words). ⭐ Its frozen companion reading: whether
  the **yardstick itself** carries the ordering — a negative with an ordered world is a learning
  result; a negative with an UNORDERED world at 54 ticks is a result about the label, and the
  commander is told which.
* **F-L3-b** — the books CONVERGE to a DIFFERENT stable shape ⇒ *diagnostic, the commander's*.
  ⭐ THE FROZEN MECHANICAL READING of "converge": the replicate-mean gap's bootstrap 95 % CI
  half-width is **below the committed census gap (4.15 pp)** while the mean gap's sign is **not**
  RECKLESS-more-punished.
* **F-L3-c** — the LEARN-ONLY world is not byte-identical to OFF (or a guard delta is non-zero, or
  any veto is served) ⇒ **STOP**.

**A fired fork is STILL A COMMIT.** The honest result lands; the adjudication is the commander's.

## §SEEDS — the ledger (#163, booked = walked)

Band **12,483,000 – 12,483,999** (#280.3), and nothing outside it.

| block | seeds | use |
| --- | --- | --- |
| **THE BATTERY** | **12,483,000 – 12,483,839** | 8 replicates × 105 matches, `BASE + r·105 + i`; BOTH arms walk the SAME seed |
| smoke mode's own sub-block | 12,483,840 – 12,483,853 | `L3T1_MODE=smoke` (R = 2 × M = 1) — its own artifact, scores nothing |
| guard / preflight block | 12,483,880 – 12,483,899 | every override routes here; the pre-freeze applied-window structural check walked 880–881 |
| ⚠ **wall-clock preflight (already walked)** | 12,483,900 – 12,483,919 | the ms/match capacity read taken before the freeze — **declared, and NO number from it enters the design** (§DEV 3) |
| `gWorld` construction seed | 12,483,999 | constructed, never stepped |
| free | 12,483,854 – 879 · 920 – 998 | unconsumed |
| ⭐ **the deff RE-WALK** | 12,482,100 – 12,482,119 | L3-T0's committed smoke block, replayed deterministically — **NOT a consumption**; its seed predicate is INVERTED (it must collide) |

Disjointness is computed **in-probe** for every interval separately against the COMPLETE consumed
ledger (`gSeed`), not asserted here.

**STATS**: the bootstrap stream opens at **111,200** (#280.3's floor, drawn here — L3-T0 recorded it
NOT DRAWN), with min gap **≥ 200** to every published base (`gStats`).

## §GATES — frozen ex ante, ALL computed in-probe (#181.2). **24 HARD rows — count them**

⭐ **#250.3(i)**: the probe freezes this list, and the artifact's gate-object key set **must equal it
exactly**. ⭐ **#268.3(a)**: the coverage map is MACHINE-DERIVED from the gate objects and
EXACTLY-ONE is ENFORCED — every conjunct owns a mutant, every mutant names a real conjunct and flips
**its own** conjunct while the others stand, or the probe **refuses to run** (exit 3). ⭐ Every
`every()`-shaped conjunct carries a **non-vacuity** partner with a published count (EK-T1's #263.2(1)
correction, turned into a rule).

| # | gate | predicate | kind |
| --- | --- | --- | --- |
| 1 | **gDet** | the deterministic core runs **twice**, canonical-JSON digests identical | HARD |
| 2 | **xSrcUntouched** | `git diff --stat -- src` **and** `git status --porcelain -- src` both empty — an instrument-only round **on top of the committed pre-task fixes** | HARD |
| 3 | **xFpProd** | the shipped league fingerprint re-derived in this process = `57b0bdab…c673` | HARD |
| 4 | **gWorld** | the POLISHED ARMED WORLD of record (`cbArmedVersion === 6`, `a4MatchFlags(6)`, `armA4World`) on a never-stepped construction seed **and on every match of every arm**, with a published count | HARD |
| 5 | ⭐⭐ **gByteIdentical (F-L3-c's gate)** | the LEARN-ONLY arm's whole-run signature (**rng stream state inside**) equals the OFF arm's on **every** battery seed; count published, non-vacuity asserted | HARD |
| 6 | ⭐ **gArms** | the configuration-identity predicate DERIVED FOR THIS EXAM — learn flag on · veto flag off · books wired and PERSISTENT · armed world · foreign doors shut · no gene anywhere · squads redrawn per fixture — one mutant per conjunct, each RE-INVOKING the predicate | HARD |
| 7 | ⭐ **gBooksLive** | **non-vacuity at claim grain**: every one of the 16 books has closed labels in **BOTH** groups and at least one punishment; total labels closed > 0; the per-group minima published | HARD |
| 8 | **gBookMath** | `belief[g] = punished[g]/lunges[g]` re-derived on **every stored cell**; `punished ≤ lunges` everywhere; the STRICTNESS rule exercised on synthetic ties; cells sum along the checkpoint stream | HARD |
| 9 | ⭐⭐ **gLedgerEq** | the probe's own per-label re-derivation (from the ledger's `noted` stream) reproduces **every in-world book cell exactly**, 0 mismatches — this is what licenses the phase split to be trusted | HARD |
| 10 | ⭐⭐ **gWindowApplied** | **THE APPLIED WINDOW IS THE LAW** (#280.2(iii)): every closed label's realised span is **exactly 54 ticks = 0.9000 s**; the module's nominal window is `0.8886436883950108 s`; both published; applied ≥ nominal and `ceil(nominal/DT) = 54` | HARD |
| 11 | ⭐⭐ **gDeffRewalk** | the re-walk of L3-T0's committed block reproduces its **committed book cells exactly** (615/488 · 68/52 · 683 closed) and `deff` re-derives to the frozen literal | HARD |
| 12 | ⭐⭐ **gN** | the frozen sizing literals **ARE** the recomputed derivation from the committed artifacts + the deff re-walk; power ≥ 0.80 on **both** readings; failability ≤ 0.05; τ = the derived minimum; and (mode-conditioned) the FULL exam ran at R = 8, M = 15 | HARD |
| 13 | ⭐⭐ **gTauLive** | **THE DEAD-PREDICATE BAR** (#264.2/#268.3): on this instrument's own scoring code, an all-ordered synthetic book set **PASSES** τ, a null synthetic set **FAILS** it, and flipping the comparison direction on the REAL cells flips the verdict — the threshold is proved passable AND failable by construction | HARD |
| 14 | **gCurve** | the checkpoint stream is the frozen season list, every cell is monotone non-decreasing along it, and it ends at M\* | HARD |
| 15 | ⭐ **gCells** | the per-book, per-checkpoint raw cells (ALL and CLEAN) are IN the artifact, and the published share, mean pair and gap **re-derive from those stored cells alone** | HARD |
| 16 | ⭐ **gPhase** | the phase rung's partition is EXHAUSTIVE and DISJOINT (clean + spanning = all, per group, per book), both readings are published, and the spanning share is non-vacuous (> 0 labels on each side of the split) | HARD |
| 17 | ⭐ **gVetoDark** | **ZERO** vetoes served in the battery; `l3DefenceVeto` false in every constructed match; `declinesLunge` read from exactly **2** places in `src/**` (its definition and the one Match fork); the veto site still sits strictly between the jockey gate and the commit line in `mechanics.ts` | HARD |
| 18 | ⭐ **gValuesUnreachable** | no measured answer of L3-C0's, L3-C0b's or L3-T0's is reachable from `src/**` (values as written, at 5 dp, and as printed percentages), no artifact/schema name, no loader/doc path in seam source; the two DERIVED quantities (`v*`, `W`) exempted BY NAME as at T0; a CONTROL NEEDLE must be FOUND; excluded-form count published | HARD |
| 19 | **gSeed** | every claimed block disjoint from the COMPLETE consumed ledger and ordered; ⭐ the deff re-walk's predicate INVERTED (it must collide) | HARD |
| 20 | **gStats** | base 111,200, min gap ≥ 200 to every published base | HARD |
| 21 | ⭐ **gEnvClean** | WHITELIST-OR-REFUSE incl. the ENGINE's own doors; every override is a PREFLIGHT, routes onto the guard block and may not write a canonical repo path | HARD |
| 22 | **gHashEnvelope** | #266.3(a): the machine envelope sits OUTSIDE the hashed body, the body re-derives its digest from disk, and a **cross-OUT** written with a different envelope has the identical digest | HARD |
| 23 | **gMutants** | the MACHINE-DERIVED coverage map has no uncovered conjunct, no ghost and no duplicate, and every mutant is LIVE | HARD |
| 24 | **G-SUITE** | FULL `npm test` green + `tsc --noEmit` clean (the PTP-T0 load-flake disposition applies and is disclosed, never excused) | HARD |

*(The count is structural: the probe exits before writing anything if the artifact's gate-object key
set is not exactly this frozen list, #250.3(i). `G-SUITE` is the one row run outside the probe and it
is recorded in §CHECKS.)*

**No gate reads a rate.** The share, the gap, the yardstick, the curve, the phase rung and the forks
are **mechanical readings**, not gates: a NEGATIVE score turns nothing red.

**Pre-named FAIL ⇒ STOP** (#179): any HARD gate failing, any `src/**` diff beyond the committed
pre-task comments, any veto served, or any existing test breaking (a STOP-and-report, never a test
edit).

## §DEV — the deviations, declared BEFORE the battery

1. ⭐⭐ **THE #257.3 CONJUNCTION IS SPLIT: ONE LIMB SCORED, ONE REPORTED — because the seed band
   cannot buy the product.** DV-T2-T1 and EK-T1 scored both limbs at ≥ 0.80 joint power on bands of
   **10,000** and **19,000** seeds. #280.3 grants **1,000**. Under the conservative correlation
   reading the set-grain limb's own power is 0.7326, so the product is ≈ 0.62 — below the mandate at
   **every** feasible (R, M). Rather than pretend, the SCORED predicate is #280.3's own literal one
   (the ordered-book share at a derived τ, power 0.857 / 0.841 on the two readings) and the
   set-grain gap is REPORTED with its CI and its own power. Both limbs are published; the verdict is
   named on the scored one.
2. ⭐ **THE SIZING'S PUNISH RATES ARE MEASURED AT A DIFFERENT WINDOW FROM THE EXAM'S.** C0b's
   committed g2 rates are at its 1.0000 s rung; this seam's applied window is 0.9000 s. There is no
   committed rate at 54 ticks — measuring one before the freeze would be sighting this stage's own
   answer. The 1.0 s row is used **as a prior for sizing only**, said so here, and the yardstick at
   54 ticks is measured in-battery (§YARDSTICK). ⚠ If the 54-tick world turns out unordered, the
   sizing was optimistic and F-L3-a's companion reading says so explicitly.
3. ⚠ **A WALL-CLOCK PREFLIGHT WALKED 20 BATTERY-BAND SEEDS BEFORE THE FREEZE** (12,483,900–919), to
   size the background run. It read **ms per match** and nothing else entered the design: the frozen
   wall cap uses a **DECLARED nominal 150 ms/walk** (the banked EK-T1 literal), not the measured
   value, so no timing enters the hashed body (#258.3). The block is retired from the battery and
   booked in §SEEDS. Likewise the applied-window structural check on 12,483,880–881 read a **tick
   span**, never a rate.
4. **`deff` IS MEASURED ON A RE-WALK OF A COMMITTED PARENT**, not modelled and not drawn fresh, and
   it is an **equal-cluster LOWER bound** (the EK-T1 form). Its cluster definition — the dead-ball
   episode — is the mechanism #280.2(i) itself named, not a generic block.
5. **THE PHASE FLAG IS PROBE-SIDE.** The seam has no whistle-awareness and gains none here (that
   would be a NEW predicate, and #280.1 §DOUBTS 1 reserved that choice for the commander). The flag
   is computed from `Match.phase` per tick, outside the book, and it changes **no** book cell.
6. **THE GUARD ROW IS A NULL CONTROL ONLY.** With no consuming arm there is no football effect to
   guard; the rulers are still computed, and their required value is exactly zero.
7. **`SEED_ROOM` IS 840, NOT 1,000.** The band's remainder is reserved for the smoke mode, the guard
   block, the already-walked preflight and the construction seed, all booked above.

## §NON-CLAIMS

1. **NOTHING SHIPS.** `src/**` moves only by the two committed **comment** fixes; both flags stay
   hard `false` in every production path; the production fingerprint is re-derived unchanged.
2. ⭐ **THE EXAM SCORES THE SHAPE, NOT THE MAGNITUDE** (#246). A book ordered but far from the
   yardstick's level passes; a book at the right level but out of order fails.
3. ⭐⭐ **IT DOES NOT SCORE ANY FOOTBALL EFFECT.** Whether 乱抢 calms is **L3-T2's** REPORTED
   question and the play-test is the USER GATE. This stage arms no veto and moves no world.
4. **A WRONG BOOK IS LEGAL AND IS STYLE** (#247). The claim is about what the population of books
   grows, not about any one side being right.
5. **THE CONJUNCTION IS A PREDICATE, NOT A TRUTH.** τ = 0.75 is derived, not sacred; a share of
   0.6875 is published as what it is.
6. **THE BINOMIAL SIZING IS A MODEL.** Label counts are random and labels cluster; `deff` corrects
   the over-dispersion by a lower-bound factor. Realised power can therefore sit below the nominal
   0.857 — declared here, before the run, rather than after it.
7. **NO EVOLUTION, NO INHERITANCE, NO COACH, NO OPPONENT/IMITATION CHANNEL, NO PER-BODY BOOK**
   (contract §4). The seam writes no genome field at all, so no Lamarck channel exists to close.
8. ⚠ **THE SEASON-RESET QUESTION IS NOT SETTLED HERE.** This exam measures what **15 seasons' worth
   of own-events** buys a book. A shipped book is wiped every season (M-L3.2). §DOUBTS 1 states the
   consequence; the design choice is the commander's, not this instrument's.
